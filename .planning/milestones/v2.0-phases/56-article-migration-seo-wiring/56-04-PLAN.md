---
phase: 56
plan: 04
type: execute
wave: 4
depends_on: [56-02, 56-03]
files_modified:
  - app/guides/page.tsx
  - app/compare/page.tsx
autonomous: true
requirements:
  - MIG-05
  - MIG-06
user_setup: []
must_haves:
  truths:
    - "app/guides/page.tsx no longer exists (deleted)"
    - "app/compare/page.tsx no longer exists (deleted)"
    - "app/guides/ and app/compare/ directories are gone OR empty (no orphan files)"
    - "JSX_POSTS registry in lib/blog.ts contains correct metadata for all 3 migrated articles (verification only — MIG-06 was pre-completed)"
    - "Full test suite green and tsc clean after cleanup"
  artifacts:
    - path: "(no app/guides/page.tsx)"
      provides: "Absence — file deleted; next.config.ts redirects intercept /guides before routing"
      contains: "(file must not exist)"
    - path: "(no app/compare/page.tsx)"
      provides: "Absence — file deleted; next.config.ts redirects intercept /compare before routing"
      contains: "(file must not exist)"
  key_links:
    - from: "GET /guides"
      to: "/blog"
      via: "next.config.ts redirects() (Plan 02), no orphan page.tsx"
      pattern: "(absence of app/guides/page.tsx + redirect rule)"
---

<objective>
Remove the legacy hub pages (`app/guides/page.tsx`, `app/compare/page.tsx`) and any now-empty sub-directories so Next.js no longer builds orphan routes with stale canonicals. Verify (read-only) that `JSX_POSTS` in `lib/blog.ts` already contains the 3 migrated articles (MIG-06 was pre-completed in Phase 54 per RESEARCH.md).

Purpose: Eliminate the "contradictory canonical signal" anti-pattern (RESEARCH.md Pitfall 4) — if these hub pages remained, Next.js would emit them with `canonical: /guides` / `canonical: /compare` even though the redirect rules send users to `/blog`. Google would see contradictory signals.

Output: Both hub `page.tsx` files deleted; empty parent directories removed; `JSX_POSTS` verified; one atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-article-migration-seo-wiring/56-RESEARCH.md
@app/guides/page.tsx
@app/compare/page.tsx
@lib/blog.ts

<interfaces>
<!-- Hub pages to remove (verified present 2026-05-14):
     - app/guides/page.tsx  (listing/hub for /guides)
     - app/compare/page.tsx (listing/hub for /compare)
     Both directories will be empty after Plan 01's git mv removed their sub-routes.
     Plan 02's redirects() rules cover /guides and /compare hub paths. -->

<!-- JSX_POSTS verification target (lib/blog.ts, verified complete by RESEARCH.md):
     Must contain entries with slugs:
       'prague-airport-to-city-center'
       'prague-airport-taxi-vs-chauffeur'
       'prague-vienna-transfer-vs-train'
     Each with: title, description, date, coverImage, category, author (AuthorSlug). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete app/guides/page.tsx and app/compare/page.tsx + remove empty parent dirs</name>
  <files>app/guides/page.tsx, app/compare/page.tsx</files>
  <read_first>
    - app/guides/page.tsx (confirm what is being deleted; check for any imports from this file elsewhere — there should be none since it's a Next.js route handler)
    - app/compare/page.tsx (same)
    - .planning/phases/56-article-migration-seo-wiring/56-RESEARCH.md (Pattern 5 — Hub page cleanup; Pitfall 4 — Redirect chain from stale canonicals)
    - next.config.ts (confirm Plan 02 redirects ARE in place — `grep "source: '/guides'," next.config.ts` must already return 1; if not, do NOT proceed and report blocker)
  </read_first>
  <action>
    Pre-flight check — confirm Plan 02 has already landed:
    ```bash
    grep -c "{ source: '/guides', destination: '/blog', permanent: true }" next.config.ts   # MUST return 1
    grep -c "{ source: '/compare', destination: '/blog', permanent: true }" next.config.ts  # MUST return 1
    ```
    If either returns 0, ABORT and report the dependency violation — Plan 02 (Wave 2) must complete first or deleting hubs leaves /guides and /compare as 404.

    Step 1 — Search the codebase for any import from these hub files (defensive; they're route handlers and should not be imported):
    ```bash
    grep -rn "from '@/app/guides/page'\|from '@/app/compare/page'\|from '../guides/page'\|from '../compare/page'" --include="*.ts" --include="*.tsx" .
    ```
    Expected: zero hits. If hits exist, STOP and report.

    Step 2 — Delete the hub files via git:
    ```bash
    git rm app/guides/page.tsx
    git rm app/compare/page.tsx
    ```

    Step 3 — Check whether parent directories are now empty and remove them if so:
    ```bash
    ls -la app/guides/ 2>/dev/null
    ls -la app/compare/ 2>/dev/null
    ```
    If a directory contains only `.` and `..` (truly empty), remove it:
    ```bash
    rmdir app/guides 2>/dev/null || true
    rmdir app/compare 2>/dev/null || true
    ```
    (Git auto-removes tracked empty dirs; `rmdir` cleans the filesystem.)

    Step 4 — Verify migration cleanliness:
    ```bash
    test ! -e app/guides/page.tsx && echo "guides hub gone"
    test ! -e app/compare/page.tsx && echo "compare hub gone"
    grep -rn "/guides\|/compare" app/ 2>/dev/null   # Expected: zero hits in app/
    ```

    Step 5 — Final test pass:
    ```bash
    npx tsc --noEmit
    npx vitest run
    npx next build   # full production build to catch any orphan route issue
    ```
    All three MUST exit 0.

    Step 6 — Commit:
    ```bash
    git add -A
    git commit -m "feat(56): remove legacy /guides and /compare hub pages (MIG-05)"
    ```
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo && test ! -e app/guides/page.tsx && test ! -e app/compare/page.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -e app/guides/page.tsx` exits 1 (file gone)
    - `test -e app/compare/page.tsx` exits 1 (file gone)
    - `find app/ -path "*/guides/*" -o -path "*/compare/*" 2>/dev/null | wc -l` returns 0 (no orphan files under those dirs)
    - `grep -rn "/guides\|/compare" app/ 2>/dev/null | wc -l` returns 0 (no references left anywhere in app/)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` exits 0 (full suite green)
    - `npx next build` exits 0 (production build succeeds; no orphan route warnings)
    - `git log -1 --pretty=%s` equals `feat(56): remove legacy /guides and /compare hub pages (MIG-05)`
  </acceptance_criteria>
  <done>Hub pages deleted, parent directories cleaned, full build + tests green, committed.</done>
</task>

<task type="auto">
  <name>Task 2: Verify JSX_POSTS registry contains all 3 migrated articles (MIG-06 — read-only check)</name>
  <files>lib/blog.ts</files>
  <read_first>
    - lib/blog.ts (FULL file — confirm JSX_POSTS array contains the three required entries; this is a READ-ONLY verification per RESEARCH.md "MIG-06 already complete")
    - tests/blog.test.ts (the existing test that asserts JSX_POSTS completeness; running it is the canonical verification)
  </read_first>
  <action>
    This is a verification-only task. MIG-06 was pre-completed in an earlier phase (RESEARCH.md lines 19-20: "VERIFIED: all 3 entries already exist in `JSX_POSTS` with correct slugs, titles, descriptions, dates, coverImage, category, author").

    Verify each required field is present by reading `lib/blog.ts` and confirming:

    For slug `'prague-airport-to-city-center'`:
    - `slug: 'prague-airport-to-city-center'` present
    - `title` non-empty string
    - `description` non-empty string
    - `date` valid ISO date string
    - `coverImage` non-empty string starting with `/` (relative public path)
    - `category` non-empty string
    - `author` (typed as `AuthorSlug`) non-empty string

    Repeat for `'prague-airport-taxi-vs-chauffeur'` and `'prague-vienna-transfer-vs-train'`.

    Run:
    ```bash
    npx vitest run tests/blog.test.ts
    ```
    MUST exit 0.

    If any entry is missing or malformed (RESEARCH.md said this is already done, so this should not happen):
    1. Do NOT silently fix — report it as a discrepancy with RESEARCH.md
    2. If the user confirms a fix is needed, add the missing entries with values sourced from the original article files' metadata exports
    3. Run `npx vitest run` and commit `fix(56): complete JSX_POSTS registry (MIG-06)`

    If everything checks out (expected path): NO file changes, NO commit. Simply log "MIG-06 verified" in the plan SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo && npx vitest run tests/blog.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "slug: 'prague-airport-to-city-center'" lib/blog.ts` >= 1
    - `grep -c "slug: 'prague-airport-taxi-vs-chauffeur'" lib/blog.ts` >= 1
    - `grep -c "slug: 'prague-vienna-transfer-vs-train'" lib/blog.ts` >= 1
    - `grep -c "JSX_POSTS" lib/blog.ts` >= 1 (registry exists and is named per requirement)
    - `npx vitest run tests/blog.test.ts` exits 0
    - No git commit produced UNLESS a discrepancy was found and fixed (RESEARCH.md verified completeness, so no-commit is the expected outcome)
  </acceptance_criteria>
  <done>JSX_POSTS verified complete by reading file + green `tests/blog.test.ts`; no edits required.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. File deletions and read-only verification only. No code paths, no input handling.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-04-01 | N/A | (deletions only) | N/A | Per `<security_context>`: no security surface. Removing build-time route files cannot introduce auth/input/crypto risk. |
</threat_model>

<verification>
1. `test ! -e app/guides/page.tsx && test ! -e app/compare/page.tsx` exits 0
2. `grep -rn "/guides\|/compare" app/ 2>/dev/null` returns 0 hits
3. `npx next build` exits 0
4. `npx vitest run` exits 0
5. Phase-level smoke (manual, NOT blocking — listed in 56-VALIDATION.md): after deploy, `curl -sIL https://rideprestigo.com/guides` returns single 301 to `/blog`; same for `/compare`
</verification>

<success_criteria>
- Both hub pages deleted via `git rm`
- Empty `app/guides/` and `app/compare/` directories removed from filesystem
- No references to `/guides` or `/compare` paths anywhere under `app/`
- `next build`, `tsc --noEmit`, and full Vitest suite all green
- `JSX_POSTS` verified to satisfy MIG-06 (no changes required per RESEARCH.md)
- Commit references MIG-05
</success_criteria>

<output>
After completion, create `.planning/phases/56-article-migration-seo-wiring/56-04-SUMMARY.md` per template, explicitly noting "MIG-06 verified as pre-completed; no edits to lib/blog.ts required".
</output>
