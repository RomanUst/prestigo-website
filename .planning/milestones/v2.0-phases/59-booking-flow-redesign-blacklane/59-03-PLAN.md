---
phase: 59-booking-flow-redesign-blacklane
plan: 03
type: execute
wave: 1
depends_on: ["59-02"]
files_modified:
  - components/booking/EntryBar.tsx
  - components/booking/TripTypeTabs.tsx
  - components/booking/ProgressBar.tsx
  - components/booking/BookingWizard.tsx
  - lib/booking-store.ts
  - tests/EntryBar.test.tsx
  - tests/BookingWizard.test.tsx
autonomous: true
requirements: [BOOK-01, BOOK-02, BOOK-03, TRACK-01, TRACK-03, TRACK-05]
must_haves:
  truths:
    - "The booking flow opens with a single unified entry bar (From/To/Date/Time) replacing the old Step1+Step2 (D-01)"
    - "Pickup time is chosen via a 15-min AM/PM time-slot dropdown; the store still holds 24h HH:MM (D-07, D-08)"
    - "An inline flight number field appears only when the origin is an airport (D-06)"
    - "The wizard is renumbered to 5 steps; ProgressBar shows totalSteps=5; STEP_NAMES and step headings reflect the new structure"
    - "form_start fires on entry-bar mount and checkout_progress fires on submit; no analytics event is lost or double-fired (TRACK-01); the Step6 price snapshot is not disturbed (TRACK-03)"
  artifacts:
    - path: "components/booking/EntryBar.tsx"
      provides: "Unified entry bar replacing Step1TripType + Step2DateTime"
      min_lines: 120
    - path: "components/booking/BookingWizard.tsx"
      provides: "5-step wizard wiring EntryBar at step 1, Step3Vehicle at step 2"
      contains: "EntryBar"
    - path: "components/booking/TripTypeTabs.tsx"
      provides: "hideMultiDay prop for entry-bar context"
      contains: "hideMultiDay"
    - path: "lib/booking-store.ts"
      provides: "nextStep clamp updated to 5 steps"
      contains: "Math.min(5"
  key_links:
    - from: "components/booking/BookingWizard.tsx"
      to: "components/booking/EntryBar.tsx"
      via: "renderStepContent case 1"
      pattern: "<EntryBar"
    - from: "components/booking/EntryBar.tsx"
      to: "lib/booking-store"
      via: "setPickupTime with 24h value"
      pattern: "setPickupTime"
    - from: "components/booking/EntryBar.tsx"
      to: "types/booking"
      via: "isAirportPlace for flight field"
      pattern: "isAirportPlace"
---

<objective>
Build the unified Entry Bar (replaces Step1TripType + Step2DateTime) and renumber the wizard from 6 steps to 5. The Entry Bar shows From / To / Date / a 15-min AM/PM time-slot dropdown, a Transfer/Hourly tab strip, an "Add return" checkbox, and a conditional inline flight-number field for airport origins. BookingWizard plugs EntryBar at step 1, shifts every later step down by one, updates STEP_NAMES / headings / ProgressBar totalSteps, and relocates `begin_checkout` out of the step-driven effect (it moves to StickyBookingPanel in plan 59-04). The Zustand store, pricing APIs, deeplink logic, and all analytics signals are preserved (D-18, D-19).

Per D-01 the entry bar is a single screen (not two steps); per D-05 there is no passenger selector in the bar (the store keeps its default passengers=1; capacity is shown on the vehicle cards in plan 59-05).

Purpose: BOOK-01/02/03 deliver the Blacklane-style consolidated entry; the renumber keeps the GA4 funnel coherent (TRACK-01), leaves the downstream Step6 price snapshot untouched (TRACK-03), and keeps CSP/Consent gating intact (TRACK-05 — no new inline scripts).
Output: EntryBar.tsx, modified TripTypeTabs/ProgressBar/BookingWizard, store clamp fix, GREEN EntryBar + BookingWizard tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-booking-flow-redesign-blacklane/59-CONTEXT.md
@.planning/phases/59-booking-flow-redesign-blacklane/59-RESEARCH.md
@.planning/phases/59-booking-flow-redesign-blacklane/59-PATTERNS.md
@.planning/phases/59-booking-flow-redesign-blacklane/59-UI-SPEC.md
@components/booking/BookingWizard.tsx
@components/booking/steps/Step1TripType.tsx
@components/booking/steps/Step2DateTime.tsx
@components/booking/TripTypeTabs.tsx
@lib/booking-store.ts
</context>

## Artifacts this phase produces

New symbols/files: `EntryBar` (default export, components/booking/EntryBar.tsx); `TIME_SLOTS_AMPM`, `isSlotDisabled` (module-local in EntryBar.tsx); `hideMultiDay` prop on `TripTypeTabs`. Modified symbols: `STEP_NAMES`, `renderStepContent`, `canProceed`, step-heading map in `BookingWizard`; `nextStep` clamp in `useBookingStore`; ProgressBar aria-label string. Retired files (logic absorbed by EntryBar): `Step1TripType.tsx`, `Step2DateTime.tsx` imports removed from BookingWizard.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build EntryBar.tsx (unified Step1+Step2)</name>
  <files>components/booking/EntryBar.tsx, components/booking/TripTypeTabs.tsx, tests/EntryBar.test.tsx</files>
  <behavior>
    - Renders PICKUP LOCATION, DESTINATION (transfer) / DurationSelector (hourly), DATE, TIME fields on one screen (D-01)
    - No passenger/pax selector in the bar (D-05)
    - 15-min AM/PM dropdown (96 slots, D-07): selecting "2:15 PM" writes "14:15" to store via setPickupTime (D-08)
    - Slots before now+12h are disabled (MIN_LEAD_HOURS=12)
    - FLIGHT NUMBER field shows only when isAirportPlace(origin) is true; writes to passengerDetails.flightNumber (D-06)
    - "Add return" checkbox expands return date/time (transfer only); hidden in hourly (D-03)
    - form_start gtag event fires once on mount; checkout_progress fires on valid submit before nextStep()
    - CTA label is "Посмотреть варианты" (D-04)
  </behavior>
  <read_first>
    - components/booking/steps/Step1TripType.tsx (validation, store writes, AddressInput usage, swap icon, errors map)
    - components/booking/steps/Step2DateTime.tsx (HOURS/MINUTES generation, MIN_LEAD_HOURS=12 lead-time disabling, store writes)
    - components/booking/TripTypeTabs.tsx (TRIP_TYPES array, store setTripType, aria roles)
    - components/booking/AddressInput.tsx (props: label/placeholder/value/onSelect/onClear/hasError/errorMessage/ariaLabel)
    - components/booking/DurationSelector.tsx (hourly mode — reused as-is)
    - .planning/phases/59-booking-flow-redesign-blacklane/59-PATTERNS.md (EntryBar pattern block: TIME_SLOTS_AMPM, isSlotDisabled, conditional flight field, form_start push, theme-light wrapper, CTA pattern, dedup ref)
    - .planning/phases/59-booking-flow-redesign-blacklane/59-UI-SPEC.md (Entry Bar layout contract, field labels, validation error copy, light theme tokens)
    - types/booking.ts (isAirportPlace, PassengerDetails, PlaceResult)
    - lib/booking-store.ts (setOrigin/setDestination/setPickupDate/setPickupTime/setReturnDate/setReturnTime/setPassengerDetails/setTripType/nextStep, passengerDetails default null)
    - tests/EntryBar.test.tsx (the RED contract this task must satisfy)
  </read_first>
  <action>
    Create components/booking/EntryBar.tsx as a 'use client' component replacing Step1TripType + Step2DateTime in a single screen (D-01). Wrap the outermost div in className="theme-light" (D-20). Render: TripTypeTabs with the new hideMultiDay prop set to true (Transfer/Hourly only, D-02); a field grid (desktop 4-col From/To/Date/Time, mobile single column 16px gap per UI-SPEC); when tripType==='hourly' replace the To field with DurationSelector and hide the "Add return" checkbox; a 15-min AM/PM time-slot control (96 slots, D-07). Do NOT render any passenger or luggage selector in the bar (D-05) — leave the store's passengers default of 1 untouched (capacity is communicated on the vehicle cards in plan 59-05). Generate the 96 slots with the TIME_SLOTS_AMPM math from PATTERNS.md (display AM/PM, value24h "HH:MM"); on selection call setPickupTime(slot.value24h) so the store keeps 24h (D-08). Apply isSlotDisabled using MIN_LEAD_HOURS=12 against the selected pickupDate. Render a conditional FLIGHT NUMBER (optional) text input only when isAirportPlace(origin) is true (D-06); write its value via setPassengerDetails({ ...(useBookingStore.getState().passengerDetails ?? {}), flightNumber: value }) per RESEARCH Open Question 1 Option B — do NOT remove or relocate the Step5 flight field. Render "Add return journey" checkbox (transfer only, D-03); when checked expand RETURN DATE + RETURN TIME with the 300ms max-height ease transition from PATTERNS.md. Validation: required From, To-or-Duration, Date, Time; on invalid submit set red borders and a single `<p role="alert">` with "Please fill in all required fields before continuing." CTA button (btn-primary, full-width, uppercase) labeled "Посмотреть варианты" (D-04); on valid submit, read fresh state via useBookingStore.getState(), push checkout_progress (currentStep mapping) then call nextStep(). Fire form_start once on mount using the GA4 push helper + a useRef Set dedup (PATTERNS.md "Dedup ref"). Use only var(--...) CSS tokens, no hex literals. Then modify TripTypeTabs.tsx: add an optional `hideMultiDay?: boolean` prop (default false); when true, filter TRIP_TYPES to the kind==='store' entries (Transfer/Hourly), dropping the navigate Multi-Day tab. Existing TripTypeTabs callers pass nothing, so default false preserves their behaviour. Update tests/EntryBar.test.tsx only if a RED assertion needs a value correction (e.g. the airport address string) — do not weaken assertions.
  </action>
  <verify>
    <automated>npx vitest run tests/EntryBar.test.tsx 2>&1 | tail -5 | grep -qiE "passed|✓" && grep -q "hideMultiDay" components/booking/TripTypeTabs.tsx && grep -q "Посмотреть варианты" components/booking/EntryBar.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - components/booking/EntryBar.tsx exists, starts with 'use client', wraps content in className="theme-light"
    - `npx vitest run tests/EntryBar.test.tsx` passes (GREEN)
    - EntryBar.tsx contains `isAirportPlace`, `setPickupTime`, and `setPassengerDetails`
    - No passenger/luggage Stepper rendered in EntryBar (D-05): `grep -c "Stepper" components/booking/EntryBar.tsx` returns 0
    - TripTypeTabs.tsx contains `hideMultiDay` and still renders all tabs when the prop is omitted
    - `grep "Посмотреть варианты" components/booking/EntryBar.tsx` matches
    - No hardcoded hex color literals in EntryBar.tsx style values (use var(--...))
  </acceptance_criteria>
  <done>EntryBar renders the unified single-screen bar (D-01) with AM/PM->24h time slots, conditional flight field, add-return expander, no pax selector (D-05), and analytics; EntryBar tests are GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Renumber wizard to 5 steps + relocate begin_checkout out of step effect</name>
  <files>components/booking/BookingWizard.tsx, components/booking/ProgressBar.tsx, lib/booking-store.ts, tests/BookingWizard.test.tsx</files>
  <read_first>
    - components/booking/BookingWizard.tsx (renderStepContent lines 237-254, STEP_NAMES lines 149-156, analytics effect lines 104-204, canProceed lines 208-235, ProgressBar usage line 312-316, step heading lines 323-354, Back/Next bar lines 360-383)
    - components/booking/ProgressBar.tsx (props: currentStep/completedSteps/totalSteps; aria-label string referencing "of 6")
    - lib/booking-store.ts (nextStep clamps to Math.min(6, ...) at lines 61-65; resetBooking)
    - .planning/phases/59-booking-flow-redesign-blacklane/59-RESEARCH.md (Step Numbering Impact on Analytics; begin_checkout Trigger Point Drift — Pitfall 5)
    - .planning/phases/59-booking-flow-redesign-blacklane/59-PATTERNS.md (BookingWizard modify block: STEP_NAMES, renderStepContent, canProceed, headings, begin_checkout removal, Back/Next condition)
    - tests/BookingWizard.test.tsx (existing canProceed tests at step 2 — note these reference the OLD step-2=date_time semantics and must be migrated to the new mapping)
  </read_first>
  <action>
    In lib/booking-store.ts change `nextStep` to clamp `currentStep: Math.min(5, s.currentStep + 1)` (was Math.min(6) — the wizard now has 5 steps). Leave resetBooking and all other store logic unchanged (D-18). In components/booking/BookingWizard.tsx: replace renderStepContent so case 1 returns <EntryBar /> (import it; remove the Step1TripType and Step2DateTime imports), case 2 returns <Step3Vehicle />, case 3 <Step4Extras />, case 4 <Step5Passenger />, case 5 <Step6Payment />. Update STEP_NAMES to { 1:'entry_bar', 2:'vehicle', 3:'extras', 4:'passenger', 5:'payment' }. Update the analytics effect: view_item_list / view_item now fire at currentStep===2 (was 3); add_payment_info + AddPaymentInfo now at currentStep===5 (was 6); REMOVE the currentStep===5 begin_checkout + InitiateCheckout block entirely (it relocates to StickyBookingPanel in plan 59-04 — Pitfall 5). Keep form_start at currentStep===1. Update canProceed: case 1 returns true (EntryBar self-validates); case 2 returns vehicleClass !== null && (tripType !== 'round_trip' || (returnDate !== null && returnTime !== null)); case 3 returns true; case 4 returns the passengerDetails firstName/lastName/email/phone check; default true. Change ProgressBar prop to totalSteps={5}. Update step heading: "STEP X OF 6" -> "STEP X OF 5" and the heading-text map to step 1='Plan your journey', 2='Choose your vehicle', 3='Add extras', 4='Passenger details', 5='Payment'. Update the generic Back/Next bar condition from `currentStep > 1 && currentStep < 6` to `currentStep > 1 && currentStep < 5`, and keep the mobile-bar hidden at the vehicle step — change the `currentStep !== 3` guard to `currentStep !== 2` (StickyBookingPanel owns the CTA there). In components/booking/ProgressBar.tsx change the hardcoded aria-label from "Step ${currentStep} of 6" to "Step ${currentStep} of 5" (only change). Migrate tests/BookingWizard.test.tsx: the existing WIZD-06 step-2 tests assert date/time canProceed semantics that no longer apply at step 2 (now vehicle). Rewrite the describe to seed currentStep=2 with vehicleClass set and assert Continue is enabled, plus add an assertion that ProgressBar reports 5 steps and renderStepContent at step 1 mounts EntryBar (query a unique EntryBar element). Keep the next/navigation mock.
  </action>
  <verify>
    <automated>npx vitest run tests/BookingWizard.test.tsx 2>&1 | tail -5 | grep -qiE "passed|✓" && grep -q "Math.min(5" lib/booking-store.ts && grep -q "totalSteps={5}" components/booking/BookingWizard.tsx && grep -q "<EntryBar" components/booking/BookingWizard.tsx && ! grep -qE "import Step1TripType|import Step2DateTime" components/booking/BookingWizard.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - lib/booking-store.ts nextStep clamps to Math.min(5, ...)
    - BookingWizard.tsx renderStepContent case 1 returns <EntryBar />; Step1TripType/Step2DateTime imports removed
    - STEP_NAMES has exactly 5 keys ending at 5:'payment'; ProgressBar called with totalSteps={5}
    - The begin_checkout/InitiateCheckout block is gone from BookingWizard.tsx (`grep -c "begin_checkout" components/booking/BookingWizard.tsx` returns 0)
    - add_payment_info now gated on currentStep===5
    - ProgressBar aria-label says "of 5"
    - `npx vitest run tests/BookingWizard.test.tsx` passes (GREEN)
  </acceptance_criteria>
  <done>Wizard renumbered to 5 steps; EntryBar mounts at step 1; begin_checkout removed from the step effect; store clamp and ProgressBar updated; BookingWizard tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Regression — full booking suite + deeplink/analytics/snapshot integrity</name>
  <files>tests/BookingWizard.test.tsx</files>
  <read_first>
    - components/booking/BookingWizard.tsx (deeplink mount effect lines 30-78 — must remain intact; scroll effect)
    - components/booking/steps/Step6Payment.tsx (the writePurchaseSnapshot call site — must remain untouched, TRACK-03)
    - lib/analytics-snapshot.ts (writePurchaseSnapshot / consumePurchaseSnapshot — frozen in Phase 59, D-18/TRACK-03)
    - tests/Step3Vehicle.test.tsx, tests/Step5Passenger.test.tsx, tests/Step6Payment.test.tsx (downstream steps that must still pass after renumber)
    - .planning/phases/59-booking-flow-redesign-blacklane/59-RESEARCH.md (URL Deeplink Preservation; Google Maps CSP Compatibility — no new inline scripts; TRACK-03 price snapshot untouched)
  </read_first>
  <action>
    Run the full booking-related test suite and fix any breakage caused by the renumber. Verify the deeplink handler in BookingWizard.tsx is byte-for-byte preserved (it still reads URL params, writes to the store, removes the booking_deeplink flag, and calls history.replaceState) — do NOT alter its logic; if a renumber edit accidentally touched it, restore it. Confirm TRACK-03 is intact: the writePurchaseSnapshot call site in Step6Payment.tsx and lib/analytics-snapshot.ts are unchanged by this phase (the renumber only shifts step indices, never the payment/confirmation snapshot logic) — grep to prove the snapshot call still exists. Confirm no new inline <script> was introduced anywhere in the changed files (TRACK-05 — CSP nonce/strict-dynamic must stay valid; the entry bar adds no scripts). If any downstream step test fails purely because of the step-number shift, update the test's currentStep seed to the new mapping (vehicle=2, extras=3, passenger=4, payment=5) WITHOUT weakening behavioural assertions. Add one BookingWizard test asserting checkout_progress fires with step_name 'entry_bar' at step 1 and 'vehicle' at step 2 to lock the analytics renumber.
  </action>
  <verify>
    <automated>npx vitest run tests/BookingWizard.test.tsx tests/EntryBar.test.tsx tests/Step3Vehicle.test.tsx tests/Step4Extras.test.tsx tests/Step5Passenger.test.tsx tests/Step6Payment.test.tsx tests/booking-store.test.ts 2>&1 | tail -8 | grep -qiE "passed|✓" && ! grep -rnE "<script" components/booking/EntryBar.tsx && grep -q "writePurchaseSnapshot" components/booking/steps/Step6Payment.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All listed booking test files pass
    - The deeplink mount effect in BookingWizard.tsx still contains `sessionStorage.removeItem('booking_deeplink')` and `window.history.replaceState`
    - TRACK-03 preserved: `grep -q "writePurchaseSnapshot" components/booking/steps/Step6Payment.tsx` succeeds and lib/analytics-snapshot.ts is unmodified by this phase
    - No inline `<script` tag introduced in EntryBar.tsx (CSP-safe, TRACK-05)
    - A BookingWizard test asserts step_name 'entry_bar' (step 1) and 'vehicle' (step 2) for checkout_progress
  </acceptance_criteria>
  <done>Full booking suite green after renumber; deeplink, price-snapshot (TRACK-03), and CSP behaviour preserved; analytics step names locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Zustand store | User-typed entry-bar values (addresses, flight number) cross into client state |
| Browser → /api/calculate-price | Origin/destination coordinates submitted at the vehicle step (unchanged in this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-04 | Tampering (XSS) | EntryBar flight number / address fields | mitigate | Values rendered via React JSX interpolation (auto-escaped); no dangerouslySetInnerHTML in EntryBar (ASVS V5) |
| T-59-05 | Tampering | CSP nonce / strict-dynamic | mitigate | No new inline scripts added; EntryBar is a client component inside the already-nonce-trusted bundle (TRACK-05); Task 3 greps for `<script` to confirm |
| T-59-06 | Information Disclosure | deeplink URL params | accept | Deeplink params only write to Zustand fields (addresses/placeIds/date/time/class); no navigation redirect, no privilege change (RESEARCH security domain) |
</threat_model>

<verification>
- EntryBar renders the unified single-screen bar (D-01); AM/PM slot writes 24h to store; flight field is airport-conditional; no pax selector (D-05)
- Wizard is 5 steps end to end; ProgressBar totalSteps=5; STEP_NAMES 5 keys
- begin_checkout removed from BookingWizard step effect (relocates to 59-04)
- Deeplink, price snapshot (TRACK-03), and CSP behaviour preserved
- All booking test files green
</verification>

<success_criteria>
- BOOK-01 (D-01): single unified entry bar replaces Step1+Step2 (EntryBar at step 1)
- BOOK-02 (D-07/D-08): 15-min AM/PM dropdown stores 24h pickupTime
- BOOK-03 (D-06): inline flight number only for airport origins, Step5 field untouched
- TRACK-01: form_start + checkout_progress fire with correct step names; no event lost
- TRACK-03: Step6 price snapshot and server-side GA4 purchase path untouched by the renumber
- TRACK-05: no new inline scripts; CSP/Consent gating intact
</success_criteria>

<output>
Create `.planning/phases/59-booking-flow-redesign-blacklane/59-03-SUMMARY.md` when done
</output>
