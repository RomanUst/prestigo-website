---
phase: 59-booking-flow-redesign-blacklane
plan: 02
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/EntryBar.test.tsx
  - tests/RouteMap.test.tsx
  - tests/VehicleSlideshow.test.tsx
  - tests/StickyBookingPanel.test.tsx
autonomous: true
requirements: [BOOK-01, BOOK-02, BOOK-03, BOOK-04, BOOK-05, TRACK-01, TRACK-02]
must_haves:
  truths:
    - "A failing (RED) test file exists for each new component before its implementation"
    - "EntryBar tests assert field rendering, 15-min AM/PM slot -> 24h store write, conditional flight field, and form_start/checkout_progress analytics"
    - "RouteMap tests assert map container with aria-label and the empty/error state"
    - "VehicleSlideshow tests assert auto-advance and prev/next controls"
    - "StickyBookingPanel tests assert begin_checkout + InitiateCheckout fire on the Select CTA"
  artifacts:
    - path: "tests/EntryBar.test.tsx"
      provides: "RED tests for BOOK-01/02/03 + TRACK-01"
      contains: "describe('EntryBar'"
    - path: "tests/RouteMap.test.tsx"
      provides: "RED tests for BOOK-04"
      contains: "describe('RouteMap'"
    - path: "tests/VehicleSlideshow.test.tsx"
      provides: "RED tests for BOOK-05 slideshow"
      contains: "describe('VehicleSlideshow'"
    - path: "tests/StickyBookingPanel.test.tsx"
      provides: "RED tests for TRACK-02"
      contains: "describe('StickyBookingPanel'"
  key_links:
    - from: "tests/EntryBar.test.tsx"
      to: "components/booking/EntryBar.tsx"
      via: "import default"
      pattern: "from '@/components/booking/EntryBar'"
    - from: "tests/RouteMap.test.tsx"
      to: "@googlemaps/js-api-loader"
      via: "vi.mock"
      pattern: "vi.mock\\('@googlemaps/js-api-loader'"
---

<objective>
Create Wave 0 RED test scaffolds for the four new booking components (EntryBar, RouteMap, VehicleSlideshow, StickyBookingPanel). Each test file imports the not-yet-existing component and asserts its required behaviour, so it fails (RED) now and turns GREEN when plans 59-03/04/05 implement the components. This satisfies the Nyquist rule: every downstream `<verify>` runs against a real test that already exists.

Purpose: Lock behaviour contracts before implementation. The implementation plans reference these test files as their GREEN gate.
Output: 4 new RED test files following the project's established vi.hoisted / vi.mock pattern.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-booking-flow-redesign-blacklane/59-RESEARCH.md
@.planning/phases/59-booking-flow-redesign-blacklane/59-PATTERNS.md
@.planning/phases/59-booking-flow-redesign-blacklane/59-UI-SPEC.md
</context>

## Artifacts this phase produces

New file paths created by this plan: `tests/EntryBar.test.tsx`, `tests/RouteMap.test.tsx`, `tests/VehicleSlideshow.test.tsx`, `tests/StickyBookingPanel.test.tsx`. These reference (not yet created) component symbols: `EntryBar`, `RouteMap`, `VehicleSlideshow`, `StickyBookingPanel` (default exports, created by plans 59-03/04/05).

<tasks>

<task type="auto">
  <name>Task 1: RED tests for EntryBar (BOOK-01/02/03 + TRACK-01)</name>
  <files>tests/EntryBar.test.tsx</files>
  <read_first>
    - tests/BookingWizard.test.tsx (next/navigation mock + sessionStorage.setItem('booking_deeplink','1') guard pattern, useBookingStore.setState reset)
    - tests/Step3Vehicle.test.tsx (render + screen query conventions, userEvent usage)
    - tests/setup.ts (matchMedia mock, jest-dom, global mocks)
    - components/booking/steps/Step1TripType.tsx (validation + store writes the EntryBar will absorb)
    - components/booking/steps/Step2DateTime.tsx (time picker + MIN_LEAD_HOURS=12 behaviour)
    - .planning/phases/59-booking-flow-redesign-blacklane/59-RESEARCH.md (Pattern 2 — TIME_SLOTS_AMPM; Analytics event trigger map; Flight Number Placement Option B)
    - types/booking.ts (isAirportPlace signature, PlaceResult shape)
    - lib/booking-store.ts (setOrigin/setDestination/setPickupDate/setPickupTime/setPassengerDetails/nextStep signatures)
  </read_first>
  <action>
    Create tests/EntryBar.test.tsx importing the (not-yet-existing) default export from '@/components/booking/EntryBar'. Mock next/navigation useRouter as in BookingWizard.test.tsx. In beforeEach: set sessionStorage 'booking_deeplink'='1', reset useBookingStore via setState to a clean transfer state, and stub window.gtag with a vi.fn() so analytics pushes are observable. Write these RED test cases: (1) renders PICKUP LOCATION, DESTINATION, DATE, TIME field labels (BOOK-01); (2) renders the CTA with text "Посмотреть варианты" (D-04); (3) selecting a time slot whose AM/PM display is "2:15 PM" results in setPickupTime called with "14:15" — the 24h store value (BOOK-02, D-08) — assert via useBookingStore.getState().pickupTime; (4) when origin is set to an airport PlaceResult such that isAirportPlace(origin) is true, a FLIGHT NUMBER field appears (BOOK-03); (5) when origin is a non-airport place, no flight number field renders (BOOK-03); (6) form_start gtag event fires once on mount with form_id 'booking_wizard' (TRACK-01); (7) submitting a valid entry bar advances currentStep (calls nextStep) and pushes checkout_progress (TRACK-01). For airport detection in tests, set origin via useBookingStore.setState with a PlaceResult whose address contains 'Airport' (mirror how isAirportPlace detects — read its implementation in types/booking.ts to pick a value that returns true). Each test should currently FAIL because EntryBar.tsx does not exist yet. Do NOT create EntryBar.tsx.
  </action>
  <verify>
    <automated>npx vitest run tests/EntryBar.test.tsx 2>&1 | grep -qiE "Failed to resolve import|Cannot find module|EntryBar" && echo "RED-OK (expected failure)"</automated>
  </verify>
  <acceptance_criteria>
    - tests/EntryBar.test.tsx exists and contains `describe('EntryBar'`
    - File imports `from '@/components/booking/EntryBar'`
    - Has assertions for: 4 field labels, "Посмотреть варианты" CTA, AM/PM->24h slot write, conditional flight field (show + hide), form_start, checkout_progress
    - `npx vitest run tests/EntryBar.test.tsx` fails because EntryBar.tsx is not implemented yet (RED)
  </acceptance_criteria>
  <done>RED test file for EntryBar exists, fails on missing import, and encodes all BOOK-01/02/03 + TRACK-01 behaviour for EntryBar.</done>
</task>

<task type="auto">
  <name>Task 2: RED tests for RouteMap (BOOK-04)</name>
  <files>tests/RouteMap.test.tsx</files>
  <read_first>
    - .planning/phases/59-booking-flow-redesign-blacklane/59-PATTERNS.md (Vitest Wave 0 mock pattern for @googlemaps/js-api-loader + window.google stub)
    - .planning/phases/59-booking-flow-redesign-blacklane/59-UI-SPEC.md (Animated Route Map Contract — aria-label, empty/error state copy "Route unavailable — you can still select a vehicle class.")
    - components/booking/AddressInput.tsx (existing js-api-loader import shape: setOptions, importLibrary)
    - tests/setup.ts (existing global mocks)
  </read_first>
  <action>
    Create tests/RouteMap.test.tsx importing the (not-yet-existing) default export from '@/components/booking/RouteMap'. Mock '@googlemaps/js-api-loader' with vi.mock returning { setOptions: vi.fn(), importLibrary: vi.fn().mockResolvedValue(undefined) }. Define window.google with a maps stub (Map, DirectionsService, Marker, LatLng, SymbolPath, MapTypeId, InfoWindow as vi.fn()/objects) via Object.defineProperty so render does not throw. Write these RED cases: (1) given origin and destination PlaceResult props, renders a container element with an aria-label that includes "Route map" (BOOK-04, accessibility contract); (2) given origin=null or destination=null, renders the empty state text "Route unavailable — you can still select a vehicle class." (UI-SPEC empty/error state). Tests fail now because RouteMap.tsx does not exist. Do NOT create RouteMap.tsx.
  </action>
  <verify>
    <automated>npx vitest run tests/RouteMap.test.tsx 2>&1 | grep -qiE "Failed to resolve import|Cannot find module|RouteMap" && echo "RED-OK (expected failure)"</automated>
  </verify>
  <acceptance_criteria>
    - tests/RouteMap.test.tsx exists and contains `describe('RouteMap'`
    - Contains `vi.mock('@googlemaps/js-api-loader'` and a window.google.maps stub
    - Asserts aria-label contains "Route map" and the empty-state copy
    - `npx vitest run tests/RouteMap.test.tsx` fails on missing RouteMap import (RED)
  </acceptance_criteria>
  <done>RED test file for RouteMap exists, mocks Google Maps, and encodes BOOK-04 behaviour.</done>
</task>

<task type="auto">
  <name>Task 3: RED tests for VehicleSlideshow (BOOK-05) and StickyBookingPanel (TRACK-02)</name>
  <files>tests/VehicleSlideshow.test.tsx, tests/StickyBookingPanel.test.tsx</files>
  <read_first>
    - components/booking/VehicleCard.tsx (next/image usage, alt text pattern)
    - components/booking/BookingWizard.tsx (begin_checkout + trackMetaEvent('InitiateCheckout') push at lines 197-199 — the behaviour moving into StickyBookingPanel)
    - components/MetaPixel.tsx (trackMetaEvent export signature — to mock it)
    - .planning/phases/59-booking-flow-redesign-blacklane/59-UI-SPEC.md (Slideshow contract — 4s auto-advance, prev/next aria-labels "Previous slide"/"Next slide"; Sticky panel CTA copy "SELECT [CLASS]")
    - .planning/phases/59-booking-flow-redesign-blacklane/59-PATTERNS.md (StickyBookingPanel analog — handleSelectClass GA4 + Meta)
    - tests/setup.ts
  </read_first>
  <action>
    Create two RED test files. tests/VehicleSlideshow.test.tsx imports default from '@/components/booking/VehicleSlideshow': use vi.useFakeTimers(); assert (1) the first slide image renders, (2) after advancing 4000ms (vi.advanceTimersByTime) the active slide index changes (auto-advance, BOOK-05/D-15), (3) prev/next buttons with aria-label "Previous slide" and "Next slide" exist. tests/StickyBookingPanel.test.tsx imports default from '@/components/booking/StickyBookingPanel': mock '@/components/booking/RouteMap' (default export -> a stub div) to avoid Google Maps in this test, mock '@/components/MetaPixel' so trackMetaEvent is a vi.fn(), stub window.gtag with vi.fn(); seed useBookingStore with a selected vehicleClass='business' and a priceBreakdown; assert clicking the "SELECT" CTA (1) calls window.gtag with 'begin_checkout' and (2) calls trackMetaEvent with 'InitiateCheckout' (TRACK-02). Both files fail now because the components do not exist. Do NOT create the component files.
  </action>
  <verify>
    <automated>npx vitest run tests/VehicleSlideshow.test.tsx tests/StickyBookingPanel.test.tsx 2>&1 | grep -qiE "Failed to resolve import|Cannot find module|VehicleSlideshow|StickyBookingPanel" && echo "RED-OK (expected failure)"</automated>
  </verify>
  <acceptance_criteria>
    - tests/VehicleSlideshow.test.tsx contains `describe('VehicleSlideshow'`, uses fake timers, asserts 4000ms auto-advance and "Previous slide"/"Next slide" controls
    - tests/StickyBookingPanel.test.tsx contains `describe('StickyBookingPanel'`, mocks RouteMap + MetaPixel, asserts begin_checkout (gtag) and InitiateCheckout (trackMetaEvent) on CTA click
    - Both fail on missing component imports (RED)
  </acceptance_criteria>
  <done>RED test files for VehicleSlideshow (BOOK-05) and StickyBookingPanel (TRACK-02) exist and fail on missing imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test-only plan; no runtime trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-03 | Tampering | test scaffolds | accept | Tests are dev-only artifacts; no production code path, no untrusted input. Google Maps loader is mocked, never loaded. |
</threat_model>

<verification>
- All four test files exist under tests/
- Each fails on the missing component import (RED state confirmed)
- No component implementation files were created in this plan
</verification>

<success_criteria>
- tests/EntryBar.test.tsx, tests/RouteMap.test.tsx, tests/VehicleSlideshow.test.tsx, tests/StickyBookingPanel.test.tsx all exist
- Each encodes the behaviour for its requirement IDs and currently fails (RED)
- Tests follow the project vi.mock / sessionStorage-deeplink-guard / store-reset conventions
</success_criteria>

<output>
Create `.planning/phases/59-booking-flow-redesign-blacklane/59-02-SUMMARY.md` when done
</output>
