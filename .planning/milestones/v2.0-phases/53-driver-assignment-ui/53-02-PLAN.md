---
phase: 53-driver-assignment-ui
plan: 02
type: execute
wave: 1
depends_on: ["01"]
files_modified: []
autonomous: true
requirements:
  - DRIVER-ASSIGN-01
  - DRIVER-ASSIGN-02

must_haves:
  truths:
    - "All Phase 53 source files exist at their expected paths"
    - "Full test suite passes with zero failures (750+ tests)"
    - "TypeScript compilation reports zero errors"
    - "Phase 53 unit tests cover assign endpoint and DriverAssignmentSection component"
  artifacts:
    - path: "components/admin/DriverAssignmentSection.tsx"
      provides: "Driver assignment UI component with bookingStatus guard and onAssigned callback"
      contains: "DriverAssignmentSection"
    - path: "app/api/admin/bookings/[id]/assign/route.ts"
      provides: "POST endpoint for driver assignment with GNet push and email notification"
      contains: "POST"
    - path: "app/api/admin/bookings/[id]/assignment/route.ts"
      provides: "GET endpoint returning latest assignment with driver join"
      contains: "GET"
    - path: "components/admin/BookingsTable.tsx"
      provides: "BookingsTable with integrated DriverAssignmentSection and optimistic status update"
      contains: "DriverAssignmentSection"
    - path: "tests/admin-assignment.test.ts"
      provides: "Unit tests for assign API"
    - path: "tests/DriverAssignmentSection.test.tsx"
      provides: "Unit tests for DriverAssignmentSection component"
  key_links:
    - from: "components/admin/BookingsTable.tsx"
      to: "components/admin/DriverAssignmentSection.tsx"
      via: "onAssigned callback prop"
      pattern: "onAssigned"
    - from: "app/api/admin/bookings/[id]/assign/route.ts"
      to: "driver_assignments table"
      via: "supabase insert"
      pattern: "driver_assignments"
    - from: "app/api/admin/bookings/[id]/assign/route.ts"
      to: "bookings.driver_id"
      via: "supabase update"
      pattern: "driver_id.*driverId"
---

<objective>
Verify that the complete Phase 53 implementation is in place and all automated checks pass.
No code changes are expected — this plan runs the test suite and TypeScript compiler to
confirm that every artifact exists and behaves correctly.

Purpose: Formal sign-off that all DRIVER-ASSIGN-01 and DRIVER-ASSIGN-02 requirements are
satisfied before gsd-verify-work runs its acceptance checks.

Output: Green test suite + zero TS errors. A SUMMARY documenting actual pass counts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and verify Phase 53 tests pass</name>
  <files></files>
  <read_first>
    - tests/admin-assignment.test.ts — confirm test cases cover assign endpoint
    - tests/DriverAssignmentSection.test.tsx — confirm component test cases
    - .planning/phases/53-driver-assignment-ui/53-VALIDATION.md — per-task verification map
  </read_first>
  <action>
    Run the full vitest suite from the prestigo/ inner directory:

    ```
    cd /Users/romanustyugov/Desktop/Prestigo && npx vitest run --reporter=verbose 2>&1
    ```

    Capture and record:
    - Total tests passed count
    - Total tests failed count (must be 0)
    - Skipped count
    - Confirm these Phase 53 test files are included in the run:
      - `tests/admin-assignment.test.ts`
      - `tests/DriverAssignmentSection.test.tsx`

    If any test fails: read the failure output, diagnose the cause, fix the source code or
    test (do NOT comment out failing tests), re-run until all pass. The suite must exit 0.

    Do NOT use `--watch` flag.
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits with code 0
    - Output does NOT contain the word "failed" (no test failures)
    - Output contains "tests/admin-assignment.test.ts" in the results list
    - Output contains "tests/DriverAssignmentSection.test.tsx" in the results list
    - No test file is skipped due to import error or missing mock
  </acceptance_criteria>
  <done>
    vitest exits 0, zero failures, both Phase 53 test files execute successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run TypeScript type check and verify zero errors</name>
  <files></files>
  <read_first>
    - components/admin/DriverAssignmentSection.tsx — confirm exported types match consumers
    - app/api/admin/bookings/[id]/assign/route.ts — confirm no untyped variables
  </read_first>
  <action>
    Run TypeScript compiler in no-emit mode from the project root:

    ```
    cd /Users/romanustyugov/Desktop/Prestigo && npx tsc --noEmit 2>&1
    ```

    If errors are reported:
    - Read each error message carefully
    - Fix the type issues in the relevant source files
    - Re-run `npx tsc --noEmit` until output is empty (exit code 0)

    Do NOT suppress errors with `@ts-ignore` or `as any` casts unless the type is genuinely
    unresolvable and the cast is documented with a comment explaining why.

    Record:
    - Number of errors before fix (if any)
    - Number of errors after fix (must be 0)
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo && npx tsc --noEmit 2>&1; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits with code 0
    - Output is empty (no error lines printed)
    - No `@ts-ignore` comments added to Phase 53 files without justification comment
  </acceptance_criteria>
  <done>
    TypeScript reports zero errors across the entire codebase.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → POST /api/admin/bookings/[id]/assign | Authenticated admin POSTs driver_id; must be UUID-validated |
| assign API → drivers table | Lookup verifies driver exists before inserting assignment |
| assign API → GNet external service | Fire-and-forget after(); failure is logged, not surfaced to caller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-02-01 | Spoofing | POST /api/admin/bookings/[id]/assign | mitigate | getAdminUser() auth guard at route entry (lines 33-35 of assign/route.ts) |
| T-53-02-02 | Tampering | driver_id payload | mitigate | z.string().uuid() Zod validation rejects non-UUID values with 400 |
| T-53-02-03 | Elevation of privilege | DriverAssignmentSection visible to non-admins | accept | Component only rendered inside admin-gated BookingsTable; no direct route exposure |
| T-53-02-04 | Information disclosure | assignment token in API response | accept | Token is single-use driver response link; short-lived per token_expires_at column |
| T-53-02-05 | Denial of service | GNet push failure blocking assign response | mitigate | after() makes GNet push fire-and-forget; main response returns 201 regardless of GNet outcome |
</threat_model>

<verification>
Phase 53 complete when ALL of the following pass:

1. `ls components/admin/DriverAssignmentSection.tsx` — exits 0
2. `ls app/api/admin/bookings/\[id\]/assign/route.ts` — exits 0
3. `ls app/api/admin/bookings/\[id\]/assignment/route.ts` — exits 0
4. `grep -c "DriverAssignmentSection" components/admin/BookingsTable.tsx` — returns >= 1
5. `ls tests/admin-assignment.test.ts` — exits 0
6. `ls tests/DriverAssignmentSection.test.tsx` — exits 0
7. `ls supabase/migrations/041_booking_driver_id.sql` — exits 0
8. `npx vitest run` — exits 0 with 0 failures
9. `npx tsc --noEmit` — exits 0 with empty output
</verification>

<success_criteria>
- All 6 Phase 53 source artifacts exist at their declared paths
- Migration file 041_booking_driver_id.sql exists
- Full test suite: 0 failures, exit code 0
- TypeScript: 0 errors, exit code 0
</success_criteria>

<output>
After completion, create `.planning/phases/53-driver-assignment-ui/53-02-SUMMARY.md`
</output>
