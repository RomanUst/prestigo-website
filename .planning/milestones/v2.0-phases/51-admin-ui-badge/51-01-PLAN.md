---
phase: 51
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prestigo/components/admin/BookingsTable.tsx
autonomous: true
requirements:
  - ADMINUI-01
tags: admin, ui, gnet, badge
must_haves:
  truths:
    - "Booking interface in BookingsTable.tsx accepts booking_source value 'gnet' alongside 'online' and 'manual'"
    - "Every desktop table row where booking_source === 'gnet' renders a visible 'GNET' badge inline next to the booking reference"
    - "Every mobile card where booking_source === 'gnet' renders a visible 'GNET' badge in the top row next to the booking reference"
    - "GNet badge renders after any leg badge (RETURN/OUTBOUND) and after MANUAL badge if both apply"
  artifacts:
    - path: "prestigo/components/admin/BookingsTable.tsx"
      provides: "Booking type with 'gnet' source + GNet badge JSX in desktop and mobile"
      contains: "booking_source: 'online' | 'manual' | 'gnet'"
  key_links:
    - from: "prestigo/components/admin/BookingsTable.tsx desktop REF cell"
      to: "GNet badge element"
      via: "conditional render on row.original.booking_source === 'gnet'"
      pattern: "booking_source === 'gnet'"
    - from: "prestigo/components/admin/BookingsTable.tsx mobile card top row"
      to: "GNet badge element"
      via: "conditional render on booking.booking_source === 'gnet'"
      pattern: "gnet-badge-mobile"
---

<objective>
Extend the `Booking` interface in `BookingsTable.tsx` to include `'gnet'` as a valid `booking_source` value, and render a "GNET" badge inline next to the booking reference for every GNet-sourced booking — both in the desktop table view and in the mobile card view.

Purpose: Operators must be able to identify GNet-sourced bookings at a glance to make correct dispatch decisions (no Stripe actions, GNet partner handles passenger comms).
Output: Modified `BookingsTable.tsx` with type extension and two new badge render points (desktop + mobile).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-admin-ui-badge/51-UI-SPEC.md
@prestigo/components/admin/BookingsTable.tsx

<interfaces>
<!-- Existing Booking interface in BookingsTable.tsx (line 17). MUST be extended, not duplicated. -->
<!-- Existing MANUAL badge pattern (line 315 desktop, line 655 mobile) — reuse as structural reference for the new GNET badge. -->
<!-- Existing leg badge testids: `leg-badge-${id}` (desktop, line 331), `leg-badge-mobile-${id}` (mobile, line 670). -->

Current type literal (line 20):
```ts
booking_source: 'online' | 'manual'
```

Target type literal:
```ts
booking_source: 'online' | 'manual' | 'gnet'
```

Existing MANUAL badge desktop pattern (around line 315) — used as structural blueprint for GNET badge (different colors per UI-SPEC):
```tsx
{row.original.booking_source === 'manual' && (
  <span style={{ /* MANUAL badge styles */ }}>MANUAL</span>
)}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Booking interface — add 'gnet' to booking_source union</name>
  <files>prestigo/components/admin/BookingsTable.tsx</files>
  <read_first>
    - prestigo/components/admin/BookingsTable.tsx (full file — lines 1-50 for Booking interface; whole file to confirm no duplicate type declarations)
    - .planning/phases/51-admin-ui-badge/51-UI-SPEC.md (Component Inventory section, line 88)
    - prestigo/types/booking.ts (verify whether a shared Booking type lives here that must also be updated — grep showed only BookingStore lives there, so confirm no duplicate update needed)
  </read_first>
  <action>
    In `prestigo/components/admin/BookingsTable.tsx`, locate the `interface Booking` declaration (currently around line 17). Find the line:

    ```ts
    booking_source: 'online' | 'manual'
    ```

    Replace it with:

    ```ts
    booking_source: 'online' | 'manual' | 'gnet'
    ```

    Do NOT change any other field. Do NOT introduce a separate exported type. The interface remains local to `BookingsTable.tsx`.

    No other code in this file references the union exhaustively (e.g., no `switch (booking_source)` block exists), so no compile errors will surface from new comparisons; subsequent tasks add the `=== 'gnet'` checks.
  </action>
  <verify>
    <automated>cd prestigo && grep -n "booking_source: 'online' | 'manual' | 'gnet'" components/admin/BookingsTable.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "booking_source: 'online' | 'manual' | 'gnet'" prestigo/components/admin/BookingsTable.tsx` returns exactly one matching line inside the `interface Booking` block
    - `grep -n "booking_source: 'online' | 'manual'$" prestigo/components/admin/BookingsTable.tsx` returns ZERO matches (the old two-value union is gone)
    - `cd prestigo && npx tsc --noEmit` exits with code 0 (no TS errors introduced)
  </acceptance_criteria>
  <done>The `Booking` interface accepts `'gnet'` as a valid `booking_source` literal; tsc compiles clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add GNET badge to desktop REF column cell</name>
  <files>prestigo/components/admin/BookingsTable.tsx</files>
  <read_first>
    - prestigo/components/admin/BookingsTable.tsx (lines 300-360 — desktop REF column cell with existing MANUAL badge at line 315 and leg-badge at line 331)
    - .planning/phases/51-admin-ui-badge/51-UI-SPEC.md (lines 88-110 for badge styles; lines 116-122 for placement order)
  </read_first>
  <action>
    In the desktop table column definition for the REF column (the cell renderer that already contains the `MANUAL` badge at line ~315 and the leg badge at line ~331), append a new conditional GNET badge AFTER the leg badge. The render order in the cell must be: booking reference text → MANUAL badge (if manual) → leg badge (if leg) → GNET badge (if gnet).

    Insert this JSX immediately AFTER the leg badge JSX block (after the `<span data-testid={`leg-badge-${row.original.id}`}>` block closes):

    ```tsx
    {row.original.booking_source === 'gnet' && (
      <span
        data-testid={`gnet-badge-${row.original.id}`}
        style={{
          marginLeft: '8px',
          background: '#1a2a3a',
          color: '#60a5fa',
          border: '1px solid rgba(59,130,246,0.25)',
          fontSize: '11px',
          fontWeight: 500,
          textTransform: 'uppercase',
          letterSpacing: '0.08em',
          padding: '4px 8px',
          borderRadius: '2px',
          display: 'inline-block',
          fontFamily: 'var(--font-montserrat)',
          lineHeight: 1,
        }}
      >
        GNET
      </span>
    )}
    ```

    Use the exact values from UI-SPEC. Do NOT use Tailwind classes (the file uses inline styles throughout — keep the pattern). Do NOT add `disabled` or `opacity` styling. Badge text is exactly `GNET` (4 chars, uppercase, no whitespace).
  </action>
  <verify>
    <automated>cd prestigo && grep -n "gnet-badge-\${row.original.id}" components/admin/BookingsTable.tsx && grep -n "GNET" components/admin/BookingsTable.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "gnet-badge-\${row.original.id}" prestigo/components/admin/BookingsTable.tsx` returns at least 1
    - `grep -n "background: '#1a2a3a'" prestigo/components/admin/BookingsTable.tsx` returns at least 1 match
    - `grep -n "color: '#60a5fa'" prestigo/components/admin/BookingsTable.tsx` returns at least 1 match
    - `grep -n ">GNET<" prestigo/components/admin/BookingsTable.tsx` returns at least 1 match (the literal badge text)
    - `grep -n "row.original.booking_source === 'gnet'" prestigo/components/admin/BookingsTable.tsx` returns at least 1 match
    - `cd prestigo && npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Desktop table renders a copper-on-dark-blue "GNET" badge next to the booking reference for every row with `booking_source === 'gnet'`; no badge for `online` or `manual` rows.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add GNET badge to mobile card top row</name>
  <files>prestigo/components/admin/BookingsTable.tsx</files>
  <read_first>
    - prestigo/components/admin/BookingsTable.tsx (lines 611-720 — mobile cards block; existing MANUAL badge at line 655 and leg-badge-mobile at line 670)
    - .planning/phases/51-admin-ui-badge/51-UI-SPEC.md (lines 88-110, 116-122)
  </read_first>
  <action>
    In the mobile cards render block (`data-testid="mobile-cards"`, ~line 611), locate the booking reference top row that already contains the MANUAL badge (~line 655) and leg-badge-mobile (~line 670). Insert a new GNET badge AFTER the leg-badge-mobile span block, mirroring the desktop placement order.

    Use this JSX (note the `mobile` testid suffix):

    ```tsx
    {booking.booking_source === 'gnet' && (
      <span
        data-testid={`gnet-badge-mobile-${booking.id}`}
        style={{
          marginLeft: '8px',
          background: '#1a2a3a',
          color: '#60a5fa',
          border: '1px solid rgba(59,130,246,0.25)',
          fontSize: '11px',
          fontWeight: 500,
          textTransform: 'uppercase',
          letterSpacing: '0.08em',
          padding: '4px 8px',
          borderRadius: '2px',
          display: 'inline-block',
          fontFamily: 'var(--font-montserrat)',
          lineHeight: 1,
        }}
      >
        GNET
      </span>
    )}
    ```

    Identical visual style to desktop (UI-SPEC: same padding desktop and mobile). Do NOT introduce a separate component — keep the inline-style pattern that the rest of `BookingsTable.tsx` uses for badges.
  </action>
  <verify>
    <automated>cd prestigo && grep -n "gnet-badge-mobile-\${booking.id}" components/admin/BookingsTable.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "gnet-badge-mobile-\${booking.id}" prestigo/components/admin/BookingsTable.tsx` returns at least 1
    - `grep -c ">GNET<" prestigo/components/admin/BookingsTable.tsx` returns at least 2 (one desktop from Task 2, one mobile from Task 3)
    - `grep -c "booking.booking_source === 'gnet'" prestigo/components/admin/BookingsTable.tsx` returns at least 1
    - `cd prestigo && npx tsc --noEmit` exits with code 0
    - `cd prestigo && npm test -- BookingsTable --run 2>&1 | tail -20` shows existing tests still pass (no regression)
  </acceptance_criteria>
  <done>Mobile card view renders a "GNET" badge in the booking reference row for every card with `booking_source === 'gnet'`; visually identical to desktop badge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Admin client | `booking_source` value travels from Supabase via `/api/admin/bookings` GET to the React client. Already authenticated via `getAdminUser()` guard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Information Disclosure | GNET badge visible only to admin users | accept | `/admin/bookings` page is gated by `getAdminUser()` middleware (existing); badge is a UI marker on already-authorised data — no new data is exposed. |
| T-51-02 | Tampering | Client-side `booking_source` value | accept | The badge is a display-only marker. The authoritative `booking_source` value lives in Supabase and is enforced server-side (Phase 47 CHECK constraint, Phase 49 inserts). A tampered client `booking_source` cannot trigger any privileged action because Stripe-action suppression in Plan 02 is also gated server-side at `/api/admin/bookings/cancel` (existing flow). |
| T-51-03 | Spoofing | Operator misreads online booking as GNet (false positive) | mitigate | The badge color (`#60a5fa` blue) is distinct from MANUAL (amber) and leg badges (copper). Badge text `GNET` is unambiguous. Conditional render uses strict equality (`=== 'gnet'`) — a missing or unknown value never triggers the badge. |
</threat_model>

<verification>
After all three tasks:
1. `cd prestigo && npx tsc --noEmit` — zero errors
2. `cd prestigo && npm test -- BookingsTable --run` — all existing tests pass
3. `cd prestigo && grep -n ">GNET<" components/admin/BookingsTable.tsx | wc -l` — returns 2 (desktop + mobile)
4. Visual smoke (handled by Plan 02 checkpoint after Stripe suppression lands)
</verification>

<success_criteria>
- `Booking` interface accepts `'gnet'` value
- Desktop table renders GNET badge with `data-testid="gnet-badge-{id}"` for every GNet row
- Mobile cards render GNET badge with `data-testid="gnet-badge-mobile-{id}"` for every GNet row
- Badge styles match UI-SPEC exactly (`#1a2a3a` bg, `#60a5fa` text, `rgba(59,130,246,0.25)` border, 11px/500 weight, uppercase, 0.08em letter-spacing, 4px/8px padding, 2px radius)
- No Tailwind classes introduced (file uses inline styles only)
- Existing tests still green
</success_criteria>

<output>
After completion, append a brief result note to `.planning/phases/51-admin-ui-badge/51-01-SUMMARY.md`.
</output>
