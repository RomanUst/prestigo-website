---
phase: 54-mdx-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - next.config.ts
  - mdx-components.tsx
  - content/blog/premium-airport-transfer-prague-shortcut.mdx
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
must_haves:
  truths:
    - "@next/mdx, @mdx-js/loader, @mdx-js/react, @types/mdx, gray-matter, remark-gfm are listed in package.json dependencies with exact versions"
    - "next.config.ts default export is createMDX()-wrapped"
    - "next.config.ts contains pageExtensions including 'mdx'"
    - "mdx-components.tsx exists at repo root and exports useMDXComponents as a named export"
    - "content/blog/ directory exists and contains at least one .mdx file with valid frontmatter (title, description, date, coverImage, category, author)"
    - "npx tsc --noEmit exits 0 after changes"
  artifacts:
    - path: "mdx-components.tsx"
      provides: "useMDXComponents named export required by @next/mdx App Router"
      contains: "export function useMDXComponents"
    - path: "next.config.ts"
      provides: "Next.js config wrapped with createMDX(); pageExtensions includes mdx; existing redirects/images/headers preserved"
      contains: "createMDX"
    - path: "content/blog/premium-airport-transfer-prague-shortcut.mdx"
      provides: "First real MDX article stub with valid frontmatter satisfying INFRA-05 schema"
      contains: "author: roman-ustyugov"
    - path: "package.json"
      provides: "MDX toolchain pinned to versions confirmed in RESEARCH.md"
      contains: "@next/mdx"
  key_links:
    - from: "next.config.ts"
      to: "remark-gfm"
      via: "createMDX options.remarkPlugins"
      pattern: "remarkPlugins:\\s*\\[remarkGfm\\]"
    - from: "mdx-components.tsx"
      to: "mdx/types"
      via: "MDXComponents type import"
      pattern: "from\\s+['\"]mdx/types['\"]"
---

<objective>
Install the `@next/mdx` compilation pipeline and create the supporting files so that `next build` succeeds with MDX enabled. This plan delivers INFRA-01, INFRA-02, INFRA-03 from REQUIREMENTS.md. It does NOT create `lib/blog.ts` or the `app/blog/[slug]/page.tsx` route (those live in plan 02).

Purpose: Without `mdx-components.tsx` and a `createMDX()`-wrapped config, the build fails the moment any `.mdx` file is added. This plan establishes the foundation so plan 02 can prove the pipeline end-to-end with a real route render.

Output:
- 6 npm packages installed at exact versions
- `next.config.ts` wrapped with `createMDX()` (existing redirects/images/headers preserved)
- `mdx-components.tsx` at repo root
- `content/blog/premium-airport-transfer-prague-shortcut.mdx` — real article stub
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-mdx-infrastructure/54-CONTEXT.md
@.planning/phases/54-mdx-infrastructure/54-RESEARCH.md
@.planning/phases/54-mdx-infrastructure/54-VALIDATION.md
@next.config.ts
@lib/authors.ts
@tsconfig.json
@package.json

<interfaces>
<!-- Key types and contracts the executor needs. No codebase exploration required. -->

From lib/authors.ts (already exists — DO NOT modify in this plan):
```typescript
export type AuthorSlug = keyof typeof AUTHORS;
// Currently the ONLY valid AuthorSlug is 'roman-ustyugov'
```

From mdx/types (provided by @types/mdx after install):
```typescript
import type { MDXComponents } from "mdx/types";
// Phase 54 contract for mdx-components.tsx:
export function useMDXComponents(components: MDXComponents): MDXComponents;
```

From @next/mdx (provided after install):
```typescript
import createMDX from "@next/mdx";
// createMDX({ options: { remarkPlugins, rehypePlugins } }) returns a NextConfig wrapper.
```

Existing next.config.ts structure (MUST be preserved verbatim inside the wrap):
- async redirects() { ... } — 27 entries including www→apex, /airport-transfer, 20 removed red routes, /cs paths
- images.formats = ['image/avif', 'image/webp']
- images.remotePatterns = [{ protocol: 'https', hostname: 'images.unsplash.com' }]
- async headers() { ... } — X-Frame-Options DENY, X-Content-Type-Options nosniff, X-XSS-Protection 0, Referrer-Policy, Permissions-Policy, HSTS
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install MDX toolchain packages</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current dependencies and devDependencies)
    - .planning/phases/54-mdx-infrastructure/54-RESEARCH.md §Standard Stack (exact versions)
  </read_first>
  <action>
    From the repo root `/Users/romanustyugov/Desktop/Prestigo`, run exactly:

    ```
    npm install @next/mdx@16.2.6 @mdx-js/loader@3.1.1 @mdx-js/react@3.1.1 @types/mdx@2.0.13 gray-matter@4.0.3 remark-gfm@4.0.1
    ```

    Do NOT use `--save-dev`. All six packages must land in `dependencies` (not devDependencies) because `next.config.ts` and `lib/blog.ts` (plan 02) import them at build time, and `mdx-components.tsx` imports `mdx/types` at compile time.

    After install, verify the six packages appear under `"dependencies"` in package.json with the exact versions above (npm may prefix `^` — that is acceptable as long as the resolved version in package-lock.json matches the requested major.minor.patch).

    Do NOT touch any other dependency. Do NOT run `npm audit fix`.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json').dependencies; const need=['@next/mdx','@mdx-js/loader','@mdx-js/react','@types/mdx','gray-matter','remark-gfm']; const missing=need.filter(n=>!p[n]); if(missing.length){console.error('MISSING:',missing);process.exit(1)} else {console.log('OK')}"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '"@next/mdx"' package.json` returns a line containing `16.2.6`
    - `grep -E '"@mdx-js/loader"' package.json` returns a line containing `3.1.1`
    - `grep -E '"@mdx-js/react"' package.json` returns a line containing `3.1.1`
    - `grep -E '"@types/mdx"' package.json` returns a line containing `2.0.13`
    - `grep -E '"gray-matter"' package.json` returns a line containing `4.0.3`
    - `grep -E '"remark-gfm"' package.json` returns a line containing `4.0.1`
    - All six entries are inside the `"dependencies"` object, NOT `"devDependencies"`
    - `package-lock.json` exists and was updated (mtime newer than before install)
  </acceptance_criteria>
  <done>Six MDX packages installed at the exact versions from RESEARCH.md. No other dependency changes. package-lock.json regenerated cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create mdx-components.tsx at repo root</name>
  <files>mdx-components.tsx</files>
  <read_first>
    - .planning/phases/54-mdx-infrastructure/54-RESEARCH.md §Pattern 2 — Minimal Pass-Through
    - .planning/phases/54-mdx-infrastructure/54-CONTEXT.md §Decisions D-03
    - tsconfig.json (confirm `mdx-components.tsx` is covered by `**/*.tsx` include glob)
  </read_first>
  <action>
    Create `/Users/romanustyugov/Desktop/Prestigo/mdx-components.tsx` (NOT inside `app/` — this MUST be at the repo root, same level as `app/`, `lib/`, `next.config.ts`).

    File contents — exact, no additions, no JSDoc fluff:

    ```typescript
    import type { MDXComponents } from "mdx/types";

    /**
     * Required by @next/mdx App Router. Maps HTML elements rendered from MDX
     * to React components. Phase 54: minimal pass-through (no styling). Phase
     * 55 will replace this with Prestigo design system components (Display
     * font headings, copper accents, dark-theme prose styles).
     */
    export function useMDXComponents(components: MDXComponents): MDXComponents {
      return {
        ...components,
      };
    }
    ```

    The export MUST be a NAMED export called `useMDXComponents`. A default export will cause `next build` to fail with a confusing "MDX components not found" error (per RESEARCH.md Pitfall 2).

    Use `@types/mdx` for the `MDXComponents` import — DO NOT inline a custom type, DO NOT use `any`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `/Users/romanustyugov/Desktop/Prestigo/mdx-components.tsx` (NOT under `app/`)
    - `grep -c "^export function useMDXComponents" mdx-components.tsx` returns `1`
    - `grep -c "export default" mdx-components.tsx` returns `0`
    - `grep -c 'from "mdx/types"' mdx-components.tsx` returns `1`
    - `grep -c ": MDXComponents" mdx-components.tsx` returns at least `2` (param type + return type)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>mdx-components.tsx at repo root with named export `useMDXComponents`, typed via `MDXComponents` from `mdx/types`, TypeScript compile clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wrap next.config.ts with createMDX() and create test MDX article</name>
  <files>next.config.ts, content/blog/premium-airport-transfer-prague-shortcut.mdx</files>
  <read_first>
    - next.config.ts (FULL current file — every line must be preserved inside the wrap)
    - .planning/phases/54-mdx-infrastructure/54-RESEARCH.md §Pattern 1 (createMDX wrapper) and §Pattern 5 (frontmatter format)
    - .planning/phases/54-mdx-infrastructure/54-CONTEXT.md §Decisions D-02, D-04, D-05
    - lib/authors.ts (confirm `roman-ustyugov` is the only valid AuthorSlug)
  </read_first>
  <action>
    **Part A — Wrap next.config.ts:**

    Edit `/Users/romanustyugov/Desktop/Prestigo/next.config.ts`:

    1. Add three imports at the top, AFTER `import type { NextConfig } from "next";`:
       ```typescript
       import createMDX from "@next/mdx";
       import remarkGfm from "remark-gfm";
       ```

    2. Add `pageExtensions: ["js", "jsx", "mdx", "ts", "tsx"],` as the FIRST property of the `nextConfig` object (before `async redirects()`). The literal string `"mdx"` MUST appear in the array.

    3. Preserve `redirects()`, `images`, `headers()` exactly as they are — do NOT modify any redirect rule, image format, or security header.

    4. Replace the final line `export default nextConfig;` with:
       ```typescript
       const withMDX = createMDX({
         options: {
           remarkPlugins: [remarkGfm],
           rehypePlugins: [],
         },
       });

       export default withMDX(nextConfig);
       ```

    Do NOT remove the existing comments about removed red routes, AVIF format, X-XSS-Protection rationale, or CSP nonce.

    **Part B — Create content/blog/ directory with a real article stub:**

    Create `/Users/romanustyugov/Desktop/Prestigo/content/blog/premium-airport-transfer-prague-shortcut.mdx`. The directory `content/blog/` does NOT exist yet — create both directories.

    File contents — exact:

    ```mdx
    ---
    title: "Premium Airport Transfer: Prague's Hidden Shortcut"
    description: "Most travellers waste 40 minutes queueing for a taxi at Václav Havel Airport. Here is the faster, calmer option for arrivals who value time more than the saved koruna."
    date: "2026-05-13"
    coverImage: "/hero-airport-transfer.webp"
    category: "Airport Transfer"
    author: roman-ustyugov
    ---

    ## The taxi-rank problem

    Václav Havel Airport (PRG) sees three peaks every weekday — 07:00, 11:30, and 17:45 — when arriving flights overlap with the airport's only authorised taxi rank. During those windows, the wait alone is 20–40 minutes.

    A pre-booked chauffeur waits airside with a name board, takes your bags, and is on the D7 within four minutes of you clearing customs. That difference is often the entire reason a 09:30 meeting in Karlín runs to schedule.

    ## Who this actually serves

    This is not a budget option. The wage difference vs an Uber Comfort is around 600 CZK on the average city-centre run. The value is elsewhere: predictability, a Mercedes E-Class instead of a 12-year-old Octavia, no language friction, no surprise surge.

    For a business arrival on a tight schedule, a family with small children, or anyone landing after 23:00, the maths is straightforward. For a solo backpacker on a flexible afternoon, the airport bus and metro remain the right answer.
    ```

    All seven frontmatter fields MUST be present. `author` value MUST be the literal string `roman-ustyugov` (the only valid `AuthorSlug` in `lib/authors.ts`). The `coverImage` path `/hero-airport-transfer.webp` already exists under `public/` and is reused from the legacy guide article.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const fs=require('node:fs');const c=fs.readFileSync('next.config.ts','utf-8');if(!c.includes('createMDX'))throw new Error('createMDX missing');if(!c.includes('withMDX(nextConfig)'))throw new Error('withMDX(nextConfig) missing');if(!c.includes('\"mdx\"'))throw new Error('pageExtensions mdx missing');if(!c.includes('remarkPlugins: [remarkGfm]'))throw new Error('remarkGfm plugin missing');const m=fs.readFileSync('content/blog/premium-airport-transfer-prague-shortcut.mdx','utf-8');for(const k of ['title:','description:','date:','coverImage:','category:','author: roman-ustyugov']){if(!m.includes(k))throw new Error('Frontmatter missing: '+k)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import createMDX from \"@next/mdx\";" next.config.ts` returns `1`
    - `grep -c "import remarkGfm from \"remark-gfm\";" next.config.ts` returns `1`
    - `grep -c 'pageExtensions:' next.config.ts` returns `1` AND that line contains `"mdx"`
    - `grep -c "withMDX(nextConfig)" next.config.ts` returns `1`
    - `grep -c "remarkPlugins: \[remarkGfm\]" next.config.ts` returns `1`
    - `grep -c "redirects()" next.config.ts` returns `1` (existing redirects preserved)
    - `grep -c "headers()" next.config.ts` returns `1` (existing headers preserved)
    - `grep -c "images.unsplash.com" next.config.ts` returns `1` (existing remotePatterns preserved)
    - File `content/blog/premium-airport-transfer-prague-shortcut.mdx` exists
    - `head -8 content/blog/premium-airport-transfer-prague-shortcut.mdx | grep -c "^---$"` returns `1` (frontmatter opener)
    - `grep -c "^author: roman-ustyugov$" content/blog/premium-airport-transfer-prague-shortcut.mdx` returns `1`
    - `grep -c "^coverImage: \"/hero-airport-transfer.webp\"$" content/blog/premium-airport-transfer-prague-shortcut.mdx` returns `1`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>next.config.ts is createMDX-wrapped with pageExtensions including mdx and remark-gfm plugin enabled; all existing redirects, images, headers preserved verbatim. content/blog/ directory created with one real MDX article stub passing the INFRA-05 frontmatter schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase has no trust boundaries crossed at runtime. All MDX compilation happens at build time. The `content/blog/` directory contains author-controlled static MDX — no untrusted input enters the pipeline. No auth, sessions, API routes, or user input introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Tampering | content/blog/*.mdx | accept | Files committed to git; review on PR; only repo maintainers can add MDX. No runtime user write path. |
| T-54-02 | Information Disclosure | mdx-components.tsx | accept | File contains no secrets, no environment variables, no PII. Pure type-mapped pass-through. |

No high-severity threats; ASVS L1 not triggered by this phase scope.
</threat_model>

<verification>
After all tasks complete, run from repo root:

1. `npx tsc --noEmit` — must exit 0 (proves new files compile in strict mode)
2. `grep -l "createMDX" next.config.ts` — must print `next.config.ts` (proves wrap landed)
3. `test -f mdx-components.tsx && test -f content/blog/premium-airport-transfer-prague-shortcut.mdx && echo OK` — must print `OK`

Note: `npm run build` is the phase-level smoke gate, run AFTER plan 02 wires the route. Building now (before `lib/blog.ts` and the `[slug]` route exist) is unnecessary — the build smoke belongs in plan 02.
</verification>

<success_criteria>
- INFRA-01 satisfied: `mdx-components.tsx` exists at repo root with named `useMDXComponents` export typed via `mdx/types`
- INFRA-02 satisfied: All six MDX packages installed at exact versions; `next.config.ts` wrapped with `createMDX()` and `pageExtensions` includes `"mdx"`
- INFRA-03 satisfied: `content/blog/` directory exists with one real `.mdx` article (not a `.gitkeep`)
- All existing `next.config.ts` behaviour preserved (27 redirect rules, images config, security headers)
- TypeScript strict compile clean (`npx tsc --noEmit` exits 0)
</success_criteria>

<output>
After completion, create `.planning/phases/54-mdx-infrastructure/54-01-SUMMARY.md` documenting:
- Exact versions installed (from package.json after install)
- Confirmation that all 27 existing redirect rules survived the `createMDX` wrap
- Path to the test MDX article and its frontmatter values
- Any deviations from the plan (expected: none)
</output>
