---
phase: 55-blog-ui-listing-article-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/BlogCard.test.tsx
  - tests/blog-jsonld.test.ts
  - tests/sitemap.test.ts
autonomous: true
requirements: [LIST-02, ART-04, ART-05]
tags: [blog, testing, scaffolding, mdx]
must_haves:
  truths:
    - "RED test stub exists for BlogCard rendering required fields"
    - "RED test stub exists for BlogPosting JSON-LD shape"
    - "RED test stub exists for sitemap /blog entry presence"
    - "All three test files are picked up by vitest"
  artifacts:
    - path: tests/BlogCard.test.tsx
      provides: "BlogCard field-rendering test stub (LIST-02)"
      contains: "describe('BlogCard'"
    - path: tests/blog-jsonld.test.ts
      provides: "BlogPosting JSON-LD shape test stub (ART-04)"
      contains: "BlogPosting"
    - path: tests/sitemap.test.ts
      provides: "sitemap /blog entries presence test stub (ART-05)"
      contains: "/blog"
  key_links:
    - from: tests/BlogCard.test.tsx
      to: components/BlogCard.tsx
      via: "import (will fail until Plan 02 creates the component — expected RED)"
      pattern: "from ['\"]@/components/BlogCard['\"]"
    - from: tests/sitemap.test.ts
      to: app/sitemap.ts
      via: "default import + invoke"
      pattern: "from ['\"]@/app/sitemap['\"]"
---

<objective>
Create the three Wave 0 test stub files that downstream plans (02 article page, 02 listing
page) will turn GREEN. This plan exists to satisfy the Nyquist validation contract:
every requirement in `55-VALIDATION.md` referencing a `tests/*` file marked "❌ W0" must
have a concrete RED stub before any UI work begins.

Purpose: Eliminate "MISSING — Wave 0 must create..." placeholders in downstream `<verify>`
blocks. Each stub MUST fail today (component/file does not yet exist or assertion fails)
so that Plan 02 and Plan 03 can drive RED → GREEN.

Output: 3 new test files committed; `npx vitest run` shows 3 failing tests (or
test-collection errors for the missing BlogCard import). No production code touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-blog-ui-listing-article-pages/55-RESEARCH.md
@.planning/phases/55-blog-ui-listing-article-pages/55-VALIDATION.md
@.planning/phases/55-blog-ui-listing-article-pages/55-UI-SPEC.md
@tests/blog.test.ts
@lib/blog.ts
@app/sitemap.ts

<interfaces>
<!-- Types and signatures executors need verbatim. -->

From lib/blog.ts:
```typescript
export type BlogPost = {
  slug: string;
  title: string;
  description: string;
  date: string;           // ISO YYYY-MM-DD
  coverImage: string;     // relative path under /public, e.g. "/hero-airport-transfer.webp"
  category: string;
  author: AuthorSlug;     // e.g. "roman-ustyugov"
  dateModified?: string;
  source: "mdx" | "jsx";
};
export function getAllPosts(): BlogPost[];     // sorted newest-first, MDX + JSX merged
export const JSX_POSTS: BlogPost[];            // 3 legacy entries
```

From lib/authors.ts:
```typescript
export type AuthorSlug = "roman-ustyugov" | ...;
export function formatBylineDate(iso: string): string;  // "9 April 2026"
export function personSchemaFor(slug: AuthorSlug): { '@type': 'Person', name: string, ... };
```

From app/sitemap.ts:
```typescript
export default function sitemap(): MetadataRoute.Sitemap;
// Each entry: { url, lastModified, alternates: { languages: { en, 'x-default' } } }
```

Existing test infra (tests/blog.test.ts already exists and passes):
- Vitest 4.1.1, jsdom env
- React Testing Library via `@testing-library/react`
- Run: `npx vitest run tests/{name}.test.tsx`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/BlogCard.test.tsx — RED stub for LIST-02 field rendering</name>
  <files>tests/BlogCard.test.tsx</files>
  <read_first>
    - tests/blog.test.ts (existing test patterns — vi.hoisted, import paths, assertion style)
    - lib/blog.ts (BlogPost type)
    - .planning/phases/55-blog-ui-listing-article-pages/55-UI-SPEC.md (BlogCard Specification section, lines 183-220)
    - components/ArticleByline.tsx (project React+TS test target style)
  </read_first>
  <behavior>
    - Test 1 "renders title as link text accessible via aria-label" — query by role 'link', expect accessibleName === post.title
    - Test 2 "renders the category as a copper-light label" — query by text post.category, expect element to be present
    - Test 3 "renders the cover image with title alt text" — query by role 'img', expect alt === post.title and src === post.coverImage
    - Test 4 "renders the formatted date" — uses formatBylineDate(post.date); expect the returned string to be in the document
    - Test 5 "links to /blog/{slug}" — link's href === `/blog/${post.slug}`
  </behavior>
  <action>
    Create `tests/BlogCard.test.tsx` with this exact content:

    ```tsx
    import { describe, it, expect } from 'vitest'
    import { render, screen } from '@testing-library/react'
    import BlogCard from '@/components/BlogCard'
    import { formatBylineDate } from '@/lib/authors'
    import type { BlogPost } from '@/lib/blog'

    const post: BlogPost = {
      slug: 'premium-airport-transfer-prague-shortcut',
      title: "Premium Airport Transfer: Prague's Hidden Shortcut",
      description: 'Most travellers waste 40 minutes queueing for a taxi at Václav Havel Airport.',
      date: '2026-05-13',
      coverImage: '/hero-airport-transfer.webp',
      category: 'Airport Transfer',
      author: 'roman-ustyugov',
      source: 'mdx',
    }

    describe('BlogCard', () => {
      it('renders title accessible via aria-label on the link', () => {
        render(<BlogCard post={post} />)
        const link = screen.getByRole('link', { name: post.title })
        expect(link).toBeTruthy()
      })

      it('links to /blog/{slug}', () => {
        render(<BlogCard post={post} />)
        const link = screen.getByRole('link', { name: post.title }) as HTMLAnchorElement
        expect(link.getAttribute('href')).toBe(`/blog/${post.slug}`)
      })

      it('renders category label', () => {
        render(<BlogCard post={post} />)
        expect(screen.getByText(post.category)).toBeTruthy()
      })

      it('renders cover image with title as alt text', () => {
        render(<BlogCard post={post} />)
        const img = screen.getByRole('img') as HTMLImageElement
        expect(img.getAttribute('alt')).toBe(post.title)
        expect(img.getAttribute('src')).toBe(post.coverImage)
      })

      it('renders the formatted date string', () => {
        render(<BlogCard post={post} />)
        const formatted = formatBylineDate(post.date)
        expect(screen.getByText(formatted)).toBeTruthy()
      })
    })
    ```

    This MUST fail today because `components/BlogCard.tsx` does not exist yet. That is the RED state Plan 02 will turn GREEN.
  </action>
  <verify>
    <automated>npx vitest run tests/BlogCard.test.tsx 2>&1 | grep -E "BlogCard|Cannot find module|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File tests/BlogCard.test.tsx exists
    - `grep -c "describe('BlogCard'" tests/BlogCard.test.tsx` returns 1
    - `grep -c "screen.getByRole('link', { name: post.title })" tests/BlogCard.test.tsx` returns at least 1
    - `grep -c "/blog/\${post.slug}" tests/BlogCard.test.tsx` returns at least 1
    - `grep -c "formatBylineDate(post.date)" tests/BlogCard.test.tsx` returns 1
    - `npx vitest run tests/BlogCard.test.tsx` exits non-zero (RED — BlogCard component does not exist yet)
  </acceptance_criteria>
  <done>Stub committed, fails for the expected reason (missing component import), ready for Plan 02 to satisfy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/blog-jsonld.test.ts — RED stub for ART-04 BlogPosting schema</name>
  <files>tests/blog-jsonld.test.ts, lib/blog-jsonld.ts</files>
  <read_first>
    - lib/blog.ts (BlogPost type)
    - lib/authors.ts (personSchemaFor signature)
    - .planning/phases/55-blog-ui-listing-article-pages/55-RESEARCH.md (Pattern 3: BlogPosting JSON-LD schema, lines 146-185)
    - app/guides/prague-airport-to-city-center/page.tsx (existing Article schema pattern to mirror)
  </read_first>
  <behavior>
    - Test 1 "returns object with @context https://schema.org and @graph array of length 2"
    - Test 2 "@graph[0] is BreadcrumbList with 3 items (Home, Blog, post.title)"
    - Test 3 "@graph[1] is BlogPosting (NOT Article) — type discrimination locked in STATE.md"
    - Test 4 "BlogPosting.image.url is absolute https://rideprestigo.com{coverImage}"
    - Test 5 "BlogPosting.author === personSchemaFor(post.author) result"
    - Test 6 "BlogPosting.datePublished === post.date, dateModified === post.dateModified ?? post.date"
    - Test 7 "BlogPosting.url === https://rideprestigo.com/blog/{slug}"
  </behavior>
  <action>
    Create a tiny helper module that the article page will use AND test against. This avoids
    duplicating schema construction inline in `app/blog/[slug]/page.tsx`.

    Step 1 — Create a minimal RED helper stub at `lib/blog-jsonld.ts`:

    ```typescript
    // RED stub — Plan 03 fills this in. Throws so tests fail loudly.
    import type { BlogPost } from '@/lib/blog'
    export function buildBlogPostingJsonLd(_post: BlogPost): Record<string, unknown> {
      throw new Error('buildBlogPostingJsonLd not implemented — Plan 03 task to implement')
    }
    ```

    Step 2 — Create `tests/blog-jsonld.test.ts` with this exact content:

    ```typescript
    import { describe, it, expect } from 'vitest'
    import { buildBlogPostingJsonLd } from '@/lib/blog-jsonld'
    import { personSchemaFor } from '@/lib/authors'
    import type { BlogPost } from '@/lib/blog'

    const post: BlogPost = {
      slug: 'premium-airport-transfer-prague-shortcut',
      title: "Premium Airport Transfer: Prague's Hidden Shortcut",
      description: 'Most travellers waste 40 minutes queueing for a taxi.',
      date: '2026-05-13',
      coverImage: '/hero-airport-transfer.webp',
      category: 'Airport Transfer',
      author: 'roman-ustyugov',
      source: 'mdx',
    }

    describe('buildBlogPostingJsonLd', () => {
      it('returns @context schema.org and a @graph of length 2', () => {
        const ld = buildBlogPostingJsonLd(post) as { '@context': string; '@graph': unknown[] }
        expect(ld['@context']).toBe('https://schema.org')
        expect(Array.isArray(ld['@graph'])).toBe(true)
        expect(ld['@graph']).toHaveLength(2)
      })

      it('@graph[0] is a BreadcrumbList with 3 items', () => {
        const ld = buildBlogPostingJsonLd(post) as { '@graph': Array<Record<string, unknown>> }
        const crumb = ld['@graph'][0] as { '@type': string; itemListElement: unknown[] }
        expect(crumb['@type']).toBe('BreadcrumbList')
        expect(crumb.itemListElement).toHaveLength(3)
      })

      it('@graph[1] is BlogPosting (not Article)', () => {
        const ld = buildBlogPostingJsonLd(post) as { '@graph': Array<Record<string, unknown>> }
        expect(ld['@graph'][1]['@type']).toBe('BlogPosting')
      })

      it('BlogPosting.image.url is the absolute URL of coverImage', () => {
        const ld = buildBlogPostingJsonLd(post) as { '@graph': Array<Record<string, unknown>> }
        const article = ld['@graph'][1] as { image: { url: string } }
        expect(article.image.url).toBe('https://rideprestigo.com/hero-airport-transfer.webp')
      })

      it('BlogPosting.author equals personSchemaFor(post.author)', () => {
        const ld = buildBlogPostingJsonLd(post) as { '@graph': Array<Record<string, unknown>> }
        const article = ld['@graph'][1] as { author: unknown }
        expect(article.author).toEqual(personSchemaFor(post.author))
      })

      it('BlogPosting dates default dateModified to date when omitted', () => {
        const ld = buildBlogPostingJsonLd(post) as { '@graph': Array<Record<string, unknown>> }
        const article = ld['@graph'][1] as { datePublished: string; dateModified: string }
        expect(article.datePublished).toBe('2026-05-13')
        expect(article.dateModified).toBe('2026-05-13')
      })

      it('BlogPosting.url is the absolute canonical /blog/{slug}', () => {
        const ld = buildBlogPostingJsonLd(post) as { '@graph': Array<Record<string, unknown>> }
        const article = ld['@graph'][1] as { url: string }
        expect(article.url).toBe('https://rideprestigo.com/blog/premium-airport-transfer-prague-shortcut')
      })
    })
    ```

    Both files MUST commit together. Tests fail because the helper throws.
  </action>
  <verify>
    <automated>npx vitest run tests/blog-jsonld.test.ts 2>&1 | grep -E "buildBlogPostingJsonLd|FAIL|not implemented"</automated>
  </verify>
  <acceptance_criteria>
    - File tests/blog-jsonld.test.ts exists
    - File lib/blog-jsonld.ts exists and contains the text "not implemented"
    - `grep -c "@type': 'BlogPosting'" tests/blog-jsonld.test.ts` returns at least 1
    - `grep -c "BreadcrumbList" tests/blog-jsonld.test.ts` returns at least 1
    - `grep -c "personSchemaFor(post.author)" tests/blog-jsonld.test.ts` returns at least 1
    - `grep -c "https://rideprestigo.com/hero-airport-transfer.webp" tests/blog-jsonld.test.ts` returns at least 1
    - `npx vitest run tests/blog-jsonld.test.ts` exits non-zero (RED — helper throws)
  </acceptance_criteria>
  <done>Stub committed, fails because helper throws "not implemented", ready for Plan 03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create tests/sitemap.test.ts — RED stub for ART-05 sitemap /blog entries</name>
  <files>tests/sitemap.test.ts</files>
  <read_first>
    - app/sitemap.ts (current entries — note: NO /blog entry exists yet)
    - lib/blog.ts (getAllPosts and JSX_POSTS — JSX_POSTS slugs MUST NOT pollute /blog/* entries in this phase per RESEARCH Pitfall 6)
    - .planning/phases/55-blog-ui-listing-article-pages/55-RESEARCH.md (Pattern 5: Sitemap update)
  </read_first>
  <behavior>
    - Test 1 "sitemap includes /blog listing URL"
    - Test 2 "sitemap includes the MDX test post /blog/premium-airport-transfer-prague-shortcut"
    - Test 3 "sitemap does NOT include /blog/{jsx-post-slug} for any JSX_POSTS entry (Phase 56 concern)"
    - Test 4 "every /blog/* entry has a lastModified Date and en + x-default alternates"
  </behavior>
  <action>
    Create `tests/sitemap.test.ts` with this exact content. The import of the default sitemap()
    function comes from `app/sitemap.ts`; Next.js metadata routes are plain TS modules.

    ```typescript
    import { describe, it, expect } from 'vitest'
    import sitemap from '@/app/sitemap'
    import { JSX_POSTS } from '@/lib/blog'

    describe('sitemap /blog entries', () => {
      const entries = sitemap()
      const urls = entries.map((e) => e.url)

      it('includes the /blog listing URL', () => {
        expect(urls).toContain('https://rideprestigo.com/blog')
      })

      it('includes the MDX test post under /blog/{slug}', () => {
        expect(urls).toContain(
          'https://rideprestigo.com/blog/premium-airport-transfer-prague-shortcut',
        )
      })

      it('does NOT include /blog/{slug} for any JSX_POSTS entry (Phase 56 concern)', () => {
        for (const jsx of JSX_POSTS) {
          expect(urls).not.toContain(`https://rideprestigo.com/blog/${jsx.slug}`)
        }
      })

      it('every /blog/* entry has lastModified Date and en + x-default alternates', () => {
        const blogEntries = entries.filter((e) =>
          (e.url as string).startsWith('https://rideprestigo.com/blog'),
        )
        expect(blogEntries.length).toBeGreaterThan(0)
        for (const e of blogEntries) {
          expect(e.lastModified).toBeInstanceOf(Date)
          const langs = (e.alternates as { languages: Record<string, string> }).languages
          expect(typeof langs.en).toBe('string')
          expect(typeof langs['x-default']).toBe('string')
        }
      })
    })
    ```

    Tests will fail today because `app/sitemap.ts` does not yet contain any /blog entries.
  </action>
  <verify>
    <automated>npx vitest run tests/sitemap.test.ts 2>&1 | grep -E "sitemap|FAIL|/blog"</automated>
  </verify>
  <acceptance_criteria>
    - File tests/sitemap.test.ts exists
    - `grep -c "https://rideprestigo.com/blog'" tests/sitemap.test.ts` returns at least 1
    - `grep -c "premium-airport-transfer-prague-shortcut" tests/sitemap.test.ts` returns at least 1
    - `grep -c "JSX_POSTS" tests/sitemap.test.ts` returns at least 1
    - `npx vitest run tests/sitemap.test.ts` exits non-zero (RED — /blog entries absent from current sitemap)
  </acceptance_criteria>
  <done>Stub committed, fails because /blog entries are absent, ready for Plan 03 to add them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 55 Plan 01 is test-only — no runtime boundaries crossed. The stubs do not consume
user input or external data; all fixture values are hardcoded inside test files.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01-01 | Tampering | Test fixture path values | accept | Fixtures use hardcoded slug strings (lowercase alphanumeric + hyphen); same allowlist regex the production route enforces. No untrusted input. |
| T-55-01-02 | Information Disclosure | Test files committed to git | accept | No secrets, no PII — only public marketing copy and image paths under /public. |
</threat_model>

<verification>
After all three tasks:
- `ls tests/BlogCard.test.tsx tests/blog-jsonld.test.ts tests/sitemap.test.ts` returns 3 files
- `npx vitest run tests/BlogCard.test.tsx tests/blog-jsonld.test.ts tests/sitemap.test.ts` exits non-zero (each suite RED for the documented reason)
- `git diff --name-only HEAD` shows only the 4 new files (3 tests + lib/blog-jsonld.ts stub)
- No production source modified except the schema helper stub
</verification>

<success_criteria>
- 3 RED test stub files exist and run via vitest
- lib/blog-jsonld.ts helper stub exists and throws "not implemented"
- Each test fails for the documented reason (missing component, missing helper, missing sitemap entry) — not a syntax error
- Plans 02 and 03 can now use these tests as their RED → GREEN driver
</success_criteria>

<output>
After completion, create `.planning/phases/55-blog-ui-listing-article-pages/55-01-SUMMARY.md`
recording the 3 RED stubs and the lib/blog-jsonld.ts placeholder.
</output>
