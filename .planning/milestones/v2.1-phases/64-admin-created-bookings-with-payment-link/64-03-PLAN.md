---
phase: 64-admin-created-bookings-with-payment-link
plan: 03
type: execute
wave: 3
depends_on: ["64-01", "64-02"]
files_modified:
  - components/admin/ManualBookingForm.tsx
  - components/admin/BookingsTable.tsx
autonomous: true
requirements: [ANEW-01, ANEW-02, ANEW-03, ANEW-05]
user_setup: []

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # ── Goal-backward UI truths ──
    - "The create form has a 'Collect payment via link' toggle; when ON the submit CTA reads 'Create & Send Payment Link' and the POST sends collect_payment:true; when OFF a 'Booking status' choice appears (Confirmed default / Pending) driving the status field (D-01/D-02/D-03)."
    - "After a successful create-with-payment, the modal shows a payment-link result panel: heading 'Payment link ready', the (display-truncated) URL, a Copy Link control, a Resend Email control, and the EUR amount in copper (D-03)."
    - "The BookingsTable expanded row shows a 'Generate Payment Link' action for unpaid/pending bookings with no payment_link_url; once a link exists it is replaced by the same result panel (URL + Copy + Resend) (D-05)."
    # ── UI Considerations lift — E1 (collect-payment toggle + status choice) ──
    - "E1 empty: the form defaults to 'Collect payment via link' unchecked and, when unchecked, 'Booking status' defaults to Confirmed (D-02) — no zero-data variant beyond the unfilled form."
    - "E1 loading: while the create POST is in flight the submit button is disabled and shows 'Creating...' (no-link) or 'Creating & Sending Link...' (link path), preventing a duplicate booking from a double-click (also mitigates ANEW-01 idempotency)."
    - "E1 error: a create-flow failure distinguishes a booking that was created but whose link failed ('Booking was created, but the payment link could not be generated — use Generate Payment Link from the bookings list to retry') from a genuine create failure — the UI never implies the booking failed when only the link step did."
    - "E1 overflow/long-text: the toggle/status controls hold fixed short strings, not user content — no clip/scroll case (deduped against the form's existing field handling)."
    # ── UI Considerations lift — E2 (payment-link result panel) ──
    - "E2 empty: the result panel renders only once a link exists; the pre-link empty state belongs to the E3 row action."
    - "E2 loading: while a link is being generated a muted 'Generating link…' line (11px, warmgrey) replaces the URL/Copy/Resend controls."
    - "E2 error: a resend failure shows 'Failed to send — try again' (#f87171); Copy has no error state in supported browsers and falls back to auto-select-for-manual-copy on unsupported clipboard — never a silent no-op."
    - "E2 populated: happy path = heading 'Payment link ready', body copy, the truncated URL, Copy Link + Resend Email, and the EUR amount in copper — the single-focal-point layout."
    - "E2 overflow: the URL truncates visually with a middle ellipsis, but the FULL untruncated URL is always what is copied to clipboard and used as the email/href target — truncation is display-only, never data loss."
    - "E2 zero-one-many: exactly one link per booking; a link covering a round-trip leg surfaces the 'This link also covers the linked return/outbound leg' notice — never a many-links list."
    - "E2 long-text: same rule as overflow (deduped, one resolution covers both)."
    # ── UI Considerations lift — E3 (row-level Generate Payment Link) ──
    - "E3 empty: pre-link state shows 'No payment link yet.' / 'Generate one to let the client pay online.' in the row's payment section."
    - "E3 loading: the button shows 'Generating…' disabled (same treatment as DriverAssignmentSection's 'Assigning...')."
    - "E3 error: failure shows 'Could not generate payment link. Please try again.' (same shape/tone as DriverAssignmentSection's assignment-failure copy)."
    - "E3 populated: once a link exists the button is replaced by the E2 result panel; the button renders ONLY for unpaid/pending bookings with no payment_link_url (D-05)."
    - "The late-attach notice 'Generating a payment link will mark this booking as Unpaid until the client pays.' is shown under the row action specifically when the target booking is currently 'pending' (communicates the Pitfall-2 status flip before the click)."
  prohibitions:
    - "The copy action must never be a silent no-op — an unsupported clipboard falls back to auto-selecting the link text for manual copy."
    - "The result panel must never place a display-truncated URL on the clipboard or in the email href — only the full URL."
    - "The Generate Payment Link button must never render for a booking that already has a payment_link_url or whose status is not unpaid/pending."
    - "The result panel / email must never display admin-only internals (operator_notes, driver_price, override rationale)."
  artifacts:
    - components/admin/ManualBookingForm.tsx (collect-payment toggle, status radio, result panel)
    - components/admin/BookingsTable.tsx (row-level Generate Payment Link action + result panel)
  key_links:
    - "ManualBookingForm submit -> POST /api/admin/bookings { collect_payment, status } -> result panel from response.paymentLinkUrl"
    - "BookingsTable row action -> POST /api/admin/bookings/[id]/payment-link -> result panel; resend -> same route { resend:true }"
---

<objective>
Wire the operator-facing UI onto the Plan 01/02 backend: a "Collect payment via link" toggle + no-link status choice in `ManualBookingForm`, a post-save payment-link result panel (URL + copy + resend + EUR amount), and a row-level "Generate Payment Link" action in `BookingsTable` for existing unpaid/pending bookings. All using the established navy/gold inline-style admin idioms — no shadcn (per 64-UI-SPEC.md).

Purpose: Make ANEW-01/02/03/05 operable by the operator; expose the copyable, re-sendable link per D-03/D-05.
Output: Extended `ManualBookingForm.tsx` and `BookingsTable.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-admin-created-bookings-with-payment-link/64-CONTEXT.md
@.planning/phases/64-admin-created-bookings-with-payment-link/64-UI-SPEC.md
@.planning/phases/64-admin-created-bookings-with-payment-link/64-PATTERNS.md
@.planning/phases/64-admin-created-bookings-with-payment-link/64-01-PLAN.md
@.planning/phases/64-admin-created-bookings-with-payment-link/64-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ManualBookingForm — collect-payment toggle, status choice, result panel</name>
  <files>components/admin/ManualBookingForm.tsx</files>
  <read_first>
    - components/admin/ManualBookingForm.tsx (FULL — labelStyle/inputStyle/sectionLabelStyle at lines 9-46; the useState block lines 75-101; the fetch('/api/admin/bookings') submit at line ~232; the existing sections e.g. "TRIP DETAILS"/"PASSENGER DETAILS"; the Calculator/Check icon-button convention for reuse by Copy/Resend)
    - 64-UI-SPEC.md (## Copywriting Contract — all E1/E2 copy verbatim; ## Color — accent-reserved list; ## Typography — Body 13px/Label 11px; ## Spacing Scale; ## Visual Hierarchy)
    - components/admin/DriverAssignmentSection.tsx (loading/error copy shape, 2000ms feedback-timeout pattern) and components/admin/NotificationToggles.tsx (2000ms "Copied!"-style feedback convention)
  </read_first>
  <action>
    Add a new "PAYMENT" section (sectionLabelStyle, copper-light label) placed after "PASSENGER DETAILS" and before the footer buttons. Add state: `collectPayment` (default false), `bookingStatus` (default 'confirmed'), `paymentLinkUrl` (nullable), `copied` (bool, 2000ms reset), resend state (`resending`/`resendResult`).

    Controls (E1): a "Collect payment via link" toggle with helper text "Generates a Stripe payment link and emails it to the client immediately. The link never expires and can be resent anytime." When the toggle is OFF, render a "Booking status" choice with options "Confirmed — paid offline (cash/invoice)" (value 'confirmed', default) and "Pending" (value 'pending'). Import `Copy` and `Send` (or `RefreshCw`) from lucide-react alongside the existing `X, Calculator`.

    Submit wiring: include `collect_payment: collectPayment` and, when the toggle is OFF, `status: bookingStatus` in the POST body. Swap the submit button label: "Create & Send Payment Link" when the toggle is ON, else "Create Booking"; in-flight disabled label "Creating & Sending Link..." (link) / "Creating..." (no-link). On success, if the response has `paymentLinkUrl`, set it and render the result panel; if the response returns 201 but `paymentLinkUrl` is null AND the toggle was ON, show the create-flow error copy "Booking was created, but the payment link could not be generated — use \"Generate Payment Link\" from the bookings list to retry."

    Result panel (E2): background var(--anthracite-mid), left border 3px solid var(--copper); heading "Payment link ready"; body "Already emailed to {client email}. Copy the link to share it another way (e.g. WhatsApp), or resend the email."; the URL rendered display-truncated (middle ellipsis) but the FULL url is what is copied and used; a "Copy Link" icon-button (Calculator-style: border var(--anthracite-light), icon var(--copper)) → `navigator.clipboard.writeText(fullUrl)` with fallback auto-select of the link text if clipboard is unsupported, showing inline "Copied!" for 2000ms; a "Resend Email" icon-button → POST `/api/admin/bookings/[bookingId]/payment-link` with `{ resend: true }`, states "Sending..." (disabled) → "Sent" (2000ms) → "Failed to send — try again" (#f87171); the EUR amount shown in copper. While generating, show a muted "Generating link…" line instead of the controls.

    All spacing from {4,8,16,24,32}; exactly the 4 declared type sizes; accent color only on the listed elements (64-UI-SPEC ## Color).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "^$" | grep -i "ManualBookingForm" && echo "TYPE ERRORS" && exit 1 || echo "ManualBookingForm typechecks"</automated>
  </verify>
  <acceptance_criteria>
    - Toggle ON → submit button text is "Create & Send Payment Link" and POST body includes `collect_payment:true`; toggle OFF → "Create Booking" and body includes `status` (default 'confirmed').
    - The result panel renders only when `paymentLinkUrl` is truthy; the Copy action writes the FULL url (not the truncated display string) to the clipboard.
    - Resend posts to `/api/admin/bookings/[id]/payment-link` with `{ resend: true }` and shows the three-state feedback.
    - Only `Copy` and `Send`/`RefreshCw` are added from lucide-react (no new npm dependency); grep confirms no shadcn import.
    - `npx tsc --noEmit` reports no error in ManualBookingForm.tsx.
    - Every spacing literal added is a member of {4,8,16,24,32}; the copper/copper-light accent is used only on the PAYMENT label, Copy/Resend buttons, panel left-border, and EUR amount.
  </acceptance_criteria>
  <done>Operator can opt into payment collection, pick a no-link status, and see + copy + resend the generated link in the create modal, matching the UI-SPEC copy and color contract.</done>
</task>

<task type="auto">
  <name>Task 2: BookingsTable — row-level Generate Payment Link action + result panel</name>
  <files>components/admin/BookingsTable.tsx</files>
  <read_first>
    - components/admin/BookingsTable.tsx (the expanded-row action region near lines ~1998-2045 where DriverAssignmentSection mounts; the Booking type incl. `linked_booking_id` at line 57, `status` cast sites for StatusBadge at lines 1126-1127/1455-1456/1942-1943; the fetch patterns to /api/admin/bookings)
    - components/admin/DriverAssignmentSection.tsx (Assign-button convention: border var(--copper), transparent → solid copper on hover; "Assigning..." disabled; "Could not assign driver. Please try again." error)
    - 64-UI-SPEC.md (## Copywriting Contract E3 + late-attach notice + round-trip notice; ## Color accent list; ## Typography)
    - app/api/admin/bookings/[id]/payment-link/route.ts (Plan 02 — request/response shape: { paymentLinkUrl, linkedBookingId } and { resent })
  </read_first>
  <action>
    Extend the `Booking` type in BookingsTable with `payment_link_url: string | null` and `payment_link_id: string | null`. In the expanded-row action area, add a payment section that renders ONLY for bookings whose `status` is `'unpaid'` or `'pending'`:
    - When `payment_link_url` is null → the E3 empty/action state: heading "No payment link yet.", body "Generate one to let the client pay online.", and a "Generate Payment Link" button (DriverAssignmentSection Assign-button style: border var(--copper), transparent, fill copper on hover with anthracite text). When the row's status is 'pending', render the late-attach notice "Generating a payment link will mark this booking as Unpaid until the client pays." under the button. Click → POST `/api/admin/bookings/[id]/payment-link` (no body / `{}`); button shows "Generating…" disabled; on failure show "Could not generate payment link. Please try again." (#f87171).
    - On success → replace the button with the SAME result panel as ManualBookingForm (URL + Copy Link + Resend Email + EUR amount, left-copper-border). If the response `linkedBookingId` is non-null, show the notice "This link also covers the linked return/outbound leg — paying once settles both." Copy writes the full url; resend posts `{ resend: true }` to the same route.
    - When `payment_link_url` is already set on load → render the result panel directly (no generate button).
    Reuse the existing `StatusBadge` variant casting unchanged (no new status values). Keep the desktop table row and the mobile card path consistent (both expanded views).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "BookingsTable" && echo "TYPE ERRORS" && exit 1 || echo "BookingsTable typechecks"</automated>
  </verify>
  <acceptance_criteria>
    - The payment section + "Generate Payment Link" button render ONLY for `status IN ('unpaid','pending')` AND `payment_link_url` null (grep the conditional).
    - A 'pending' booking shows the late-attach notice; an 'unpaid' booking does not.
    - After generation (or when payment_link_url is pre-set) the result panel replaces the button; a non-null `linkedBookingId` shows the round-trip "covers both legs" notice.
    - Copy writes the full URL; Resend posts `{ resend:true }` to `/api/admin/bookings/[id]/payment-link`.
    - The `Booking` type includes `payment_link_url` and `payment_link_id`; `npx tsc --noEmit` clean for BookingsTable.tsx.
    - No shadcn import added; accent color confined to the UI-SPEC-listed elements.
  </acceptance_criteria>
  <done>Operator can generate + copy + resend a payment link from an existing unpaid/pending booking row, with the pending status-flip and round-trip notices surfaced per the UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → admin API | Client-side controls POST to already-guarded admin routes; no new trust boundary — the browser only renders server-authoritative data (the persisted link URL) and triggers guarded routes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-08 | Information Disclosure | result panel / row render | medium | mitigate | Panel renders only client-facing fields (URL, EUR amount, client email) — never operator_notes/driver_price/override rationale (prohibition). |
| T-64-04 | Tampering (IDOR) | row action → [id]/payment-link | high | mitigate | The action calls the Plan-02 route which enforces `getAdminUser()` + status guard server-side; the client cannot bypass it. |
| T-64-09 | Tampering | clipboard/href URL | low | mitigate | Copy/href always use the FULL persisted URL; display truncation is presentational only (never the copied value). |
| T-64-SC | Tampering | npm/pip/cargo installs | low | accept | No new package — two more `lucide-react` icons from the already-installed package. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for both components.
- `npx vitest run` full suite still green (no test regressions from UI changes).
- Manual smoke (VALIDATION.md Manual-Only): toggle → create-with-payment → copy/resend; row action on an existing unpaid booking.
</verification>

<success_criteria>
- Create modal exposes the collect-payment toggle, no-link status choice, and the link result panel.
- BookingsTable expanded row exposes Generate Payment Link + result panel for unpaid/pending bookings.
- All UI-SPEC copy, color, spacing, and typography contracts honored.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 03 subset)
- `components/admin/ManualBookingForm.tsx` — collect-payment toggle, booking-status radio, payment-link result panel (Copy/Resend/EUR).
- `components/admin/BookingsTable.tsx` — row-level "Generate Payment Link" action, result panel, late-attach + round-trip notices; `Booking` type gains `payment_link_url`/`payment_link_id`.
</artifacts_produced>

<output>
Create `.planning/phases/64-admin-created-bookings-with-payment-link/64-03-SUMMARY.md` when done.
</output>
