---
phase: 62-abandoned-unpaid-booking-capture
plan: 04
type: execute
wave: 3
depends_on: [62-01, 62-03]
files_modified:
  - supabase/migrations/054_admin_search_bookings_status_filter.sql
autonomous: false
requirements: [ABND-02, ABND-04]
user_setup:
  - service: supabase
    why: "Live schema migration apply + verification requires project DB credentials the sandbox does not hold"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens (only if using CLI; Supabase MCP is the preferred path here)"

estimate:
  tokens: 40000
  raw_tokens: 27000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Migration 054 is authored by starting from the VERBATIM live body of admin_search_bookings (pulled via pg_get_functiondef), adding a p_status parameter (default NULL) and a `(p_status IS NULL OR status = p_status)` predicate — no existing search/pagination/return behavior is dropped (RESEARCH Pitfall 1)"
    - { statement: "The live bookings_status_check CHECK constraint accepts an inserted row with status='unpaid' after migration 053 is applied", verification: backstop }
    - { statement: "The live bookings table has the attempt_id column and the partial unique index (attempt_id, leg) WHERE status='unpaid' after 053 is applied", verification: backstop }
    - { statement: "The live admin_search_bookings function accepts a p_status argument after 054 is applied", verification: backstop }
  prohibitions:
    - "migration 054 must never recreate admin_search_bookings from a guessed body — it must derive from the live pg_get_functiondef output"
  artifacts:
    - supabase/migrations/054_admin_search_bookings_status_filter.sql
  key_links:
    - "GET /api/admin/bookings p_status argument (62-03) matches the p_status parameter name added to the live RPC by 054"
    - "migrations 053 (62-01) + 054 are both applied to the live DB in the single [BLOCKING] apply step"
---

<objective>
Close the live-schema gap that build + typecheck cannot catch: apply migration 053
(unpaid CHECK value + attempt_id column/index) and author + apply migration 054 (the
p_status parameter on the admin_search_bookings RPC) to the live Supabase project, then
verify the constraint, column, index, and RPC parameter actually exist.

Purpose: types come from config, not the DB, so the phase passes build without a live
push — a false-positive. This [BLOCKING] plan makes the DB match the code before
verification. It also correctly handles the project's known hazard: the live
admin_search_bookings body has NO local migration file, so 054 must start from the live
definition, not a guess.
Output: migration 054 file, applied 053+054, and live verification evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-abandoned-unpaid-booking-capture/62-RESEARCH.md
@.planning/phases/62-abandoned-unpaid-booking-capture/62-01-SUMMARY.md
@.planning/phases/62-abandoned-unpaid-booking-capture/62-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 054 from the live admin_search_bookings body + p_status parameter</name>
  <files>supabase/migrations/054_admin_search_bookings_status_filter.sql</files>
  <precondition>Supabase MCP tools are available to run SELECT pg_get_functiondef('admin_search_bookings'::regproc) against the live project (no local migration defines this function). If MCP/live DB is unreachable, HALT and flag a blocking gap — do not guess the function body.</precondition>
  <read_first>
    - app/api/admin/bookings/route.ts (the admin_search_bookings rpc call + the p_status arg added in 62-03 ~line 73-81)
    - .planning/phases/62-abandoned-unpaid-booking-capture/62-RESEARCH.md (Pitfall 1 — no local migration for admin_search_bookings; Open Question 1 — pull live body first; Pitfall 2 — 053/054 numbering)
    - types/database.types.ts (admin_search_bookings Args/Returns ~line 1133-1146, for the expected shape only — NOT the body)
  </read_first>
  <action>
Use Supabase MCP `execute_sql` to run `SELECT pg_get_functiondef('admin_search_bookings'::regproc);` and capture the VERBATIM live function definition. Create `supabase/migrations/054_admin_search_bookings_status_filter.sql` as a `CREATE OR REPLACE FUNCTION` that reproduces that body unchanged EXCEPT: (1) add a new parameter `p_status text DEFAULT NULL` to the signature (place it consistently with the existing p_* parameters so the positional/named call from route.ts resolves — the GET handler passes it by name in the rpc object), and (2) add `AND (p_status IS NULL OR b.status = p_status)` to the WHERE clause of the row-selection query (use the actual table alias from the live body). Preserve the existing p_query/p_start_date/p_end_date/p_trip_type/p_offset/p_limit parameters, the search logic, the pagination, and the `{ rows, total_count }` return shape exactly. Add a leading SQL comment noting the body was pulled from the live DB via pg_get_functiondef on the apply date.
  </action>
  <verify>
    <automated>test -f supabase/migrations/054_admin_search_bookings_status_filter.sql && grep -q "p_status" supabase/migrations/054_admin_search_bookings_status_filter.sql && grep -q "admin_search_bookings" supabase/migrations/054_admin_search_bookings_status_filter.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File 054 exists, is a CREATE OR REPLACE FUNCTION admin_search_bookings, and contains the `p_status` parameter and the `(p_status IS NULL OR ... status = p_status)` predicate.
    - The original p_query/p_trip_type/pagination parameters and the `{ rows, total_count }` return remain present (verbatim from the live body).
    - A leading comment records that the body was derived from pg_get_functiondef (not guessed).
  </acceptance_criteria>
  <done>Migration 054 reproduces the live RPC body plus a p_status filter, ready to apply.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: [BLOCKING] Apply migrations 053 + 054 to the live DB and verify constraint/column/index/RPC</name>
  <precondition>Migrations 053 (62-01) and 054 (Task 1) exist on disk; Supabase MCP apply_migration + execute_sql are available for the live project. The phase CANNOT pass verification until this task completes.</precondition>
  <action>Apply migrations 053 then 054 to the live Supabase project and verify the constraint, column, index, and RPC parameter now exist.</action>
  <instructions>
Everything up to this point is automated (both migration files are authored on disk). The remaining step mutates the PRODUCTION schema, which is why it is a blocking-human gate and may need operator-held credentials.

Apply both migrations via Supabase MCP `apply_migration` (preferred over `supabase db push` — no local CLI project exists), 053 first then 054. Then verify via Supabase MCP `execute_sql`:
  1. In a rolled-back transaction, insert a throwaway `bookings` row with status='unpaid' to confirm `bookings_status_check` accepts the value.
  2. Confirm the `attempt_id` column exists and the partial unique index `bookings_attempt_id_leg_unpaid_key` exists (query information_schema.columns and pg_indexes).
  3. Call `admin_search_bookings` with a `p_status => 'unpaid'` argument and confirm it executes and returns the `{ rows, total_count }` shape.
Record the applied migration versions and the verification output in 62-04-SUMMARY.md.
  </instructions>
  <verification>Supabase MCP `execute_sql` confirms: the CHECK accepts status='unpaid'; the attempt_id column + partial unique index exist; admin_search_bookings accepts p_status and returns { rows, total_count }. Then `npx vitest run` still green.</verification>
  <resume-signal>Type "done" once 053 + 054 are applied and the four live checks pass (paste evidence into the SUMMARY).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → live Supabase schema | migration apply mutates production DDL |
| CREATE OR REPLACE admin_search_bookings | risk of dropping live behavior absent locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-62-06 | Tampering | CREATE OR REPLACE admin_search_bookings from a wrong body | high | mitigate | 054 is authored from the VERBATIM live pg_get_functiondef output (Task 1 precondition), not a guess; only the p_status parameter + predicate are added; search/pagination/return preserved. If the live body cannot be pulled, HALT rather than guess. |
| T-62-07 | Denial of Service | applying a bad migration to production | medium | mitigate | Apply 053 then 054 in order via Supabase MCP; verify each object with execute_sql immediately; blocking-human gate so an operator supervises the production change. |
| T-62-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase. |
</threat_model>

<verification>
- Migration 054 file present and grep-verified (Task 1 automated check).
- Live-DB verification via Supabase MCP execute_sql (Task 2): CHECK accepts 'unpaid'; attempt_id column + partial index exist; admin_search_bookings accepts p_status.
- After apply, re-run `npx vitest run` to confirm no regression, then the phase is ready for `/gsd-verify-work`.
</verification>

<success_criteria>
- 053 + 054 applied to the live project; unpaid status, attempt_id column/index, and p_status RPC parameter verified present (ABND-02, ABND-04).
- No live search/pagination behavior of admin_search_bookings was lost.
</success_criteria>

<output>
Create `.planning/phases/62-abandoned-unpaid-booking-capture/62-04-SUMMARY.md` when done, including the applied migration versions and the live verification evidence.
</output>
