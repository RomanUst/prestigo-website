---
phase: 03-booking-details
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - prestigo/tests/Step4Extras.test.tsx
  - prestigo/tests/Step5Passenger.test.tsx
  - prestigo/tests/PriceSummary.test.tsx
autonomous: true
requirements: [STEP4-01, STEP4-02, STEP4-03, STEP5-01, STEP5-02, STEP5-03, STEP5-04]

must_haves:
  truths:
    - "Test stub files exist for all Phase 3 requirements"
    - "All stubs pass via vitest (it.todo does not fail)"
  artifacts:
    - path: "prestigo/tests/Step4Extras.test.tsx"
      provides: "Test stubs for STEP4-01, STEP4-02, STEP4-03"
      contains: "describe.*STEP4"
    - path: "prestigo/tests/Step5Passenger.test.tsx"
      provides: "Test stubs for STEP5-01, STEP5-02, STEP5-03, STEP5-04"
      contains: "describe.*STEP5"
    - path: "prestigo/tests/PriceSummary.test.tsx"
      provides: "Extended stubs for STEP4-03 extras total"
      contains: "STEP4-03"
  key_links: []
---

<objective>
Create test stub files for all Phase 3 requirements (Steps 4 and 5).

Purpose: Establish Nyquist-compliant test scaffolding so subsequent plans have automated verification targets.
Output: Three test files with .todo() stubs grouped by requirement ID.
</objective>

<execution_context>
@/Users/romanustyugov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/romanustyugov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-booking-details/03-RESEARCH.md
@.planning/phases/03-booking-details/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Step4Extras and Step5Passenger test stubs</name>
  <files>prestigo/tests/Step4Extras.test.tsx, prestigo/tests/Step5Passenger.test.tsx</files>
  <read_first>
    prestigo/tests/Step3Vehicle.test.tsx
    prestigo/tests/PriceSummary.test.tsx
  </read_first>
  <action>
Create two new test stub files following the established Phase 2 test stub pattern (describe blocks keyed to requirement IDs, it.todo() for each behavior).

**File 1: `prestigo/tests/Step4Extras.test.tsx`**

```tsx
import { describe, it } from 'vitest'

describe('Step4Extras', () => {
  describe('STEP4-01: User can add extras', () => {
    it.todo('renders three extra toggle cards: Child Seat, Meet & Greet, Extra Luggage')
    it.todo('toggling Child Seat updates extras.childSeat in Zustand store')
    it.todo('toggling Meet & Greet updates extras.meetAndGreet in Zustand store')
    it.todo('toggling Extra Luggage updates extras.extraLuggage in Zustand store')
    it.todo('selected extra card shows aria-pressed=true')
    it.todo('unselected extra card shows aria-pressed=false')
  })

  describe('STEP4-02: Each extra shows its price increment', () => {
    it.todo('Child Seat card displays +€15')
    it.todo('Meet & Greet card displays +€25')
    it.todo('Extra Luggage card displays +€20')
  })

  describe('STEP4-03: PriceSummary updates to include selected extras', () => {
    it.todo('selecting Child Seat adds 15 to displayed total')
    it.todo('selecting multiple extras sums their prices into total')
    it.todo('deselecting an extra removes its price from total')
  })
})
```

**File 2: `prestigo/tests/Step5Passenger.test.tsx`**

```tsx
import { describe, it } from 'vitest'

describe('Step5Passenger', () => {
  describe('STEP5-01: Required fields present', () => {
    it.todo('renders First Name input field')
    it.todo('renders Last Name input field')
    it.todo('renders Email input field')
    it.todo('renders Phone input field')
    it.todo('all four fields are required (aria-required=true)')
  })

  describe('STEP5-02: Airport ride conditional fields', () => {
    it.todo('renders Flight Number field when origin is PRG airport')
    it.todo('renders Flight Number field when destination is PRG airport')
    it.todo('Flight Number is required for airport rides (aria-required=true)')
    it.todo('does not render Flight Number for non-airport rides')
    it.todo('renders Terminal field (optional) for airport rides')
  })

  describe('STEP5-03: Special Requests field', () => {
    it.todo('renders Special Requests textarea')
    it.todo('textarea has maxLength=500 attribute')
    it.todo('displays character counter showing current/500')
  })

  describe('STEP5-04: Inline validation on blur', () => {
    it.todo('no error messages shown on initial render')
    it.todo('shows "First name is required" after blur on empty First Name')
    it.todo('shows "Last name is required" after blur on empty Last Name')
    it.todo('shows "Enter a valid email address" after blur on invalid email')
    it.todo('shows "Enter a valid phone number" after blur on short phone')
    it.todo('shows "Flight number is required for airport rides" after blur on empty Flight Number for airport ride')
    it.todo('clears error message when valid value entered and field blurred again')
  })
})
```
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo/prestigo && PATH=/Users/romanustyugov/.nvm/versions/node/v22.22.1/bin:$PATH npx vitest run tests/Step4Extras.test.tsx tests/Step5Passenger.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - prestigo/tests/Step4Extras.test.tsx contains `describe('STEP4-01`
    - prestigo/tests/Step4Extras.test.tsx contains `describe('STEP4-02`
    - prestigo/tests/Step4Extras.test.tsx contains `describe('STEP4-03`
    - prestigo/tests/Step5Passenger.test.tsx contains `describe('STEP5-01`
    - prestigo/tests/Step5Passenger.test.tsx contains `describe('STEP5-02`
    - prestigo/tests/Step5Passenger.test.tsx contains `describe('STEP5-03`
    - prestigo/tests/Step5Passenger.test.tsx contains `describe('STEP5-04`
    - Both files contain only it.todo() calls (no implementation yet)
    - vitest run exits 0 (all todo tests skipped, none fail)
  </acceptance_criteria>
  <done>Two test stub files created with .todo() stubs covering all 7 Phase 3 requirement IDs. vitest run passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add STEP4-03 extras stubs to existing PriceSummary test file</name>
  <files>prestigo/tests/PriceSummary.test.tsx</files>
  <read_first>
    prestigo/tests/PriceSummary.test.tsx
  </read_first>
  <action>
Read the existing PriceSummary.test.tsx file and append a new describe block for STEP4-03 extras total display. Do NOT modify existing test stubs.

Add at the end, inside the outer describe block:

```tsx
  describe('STEP4-03: PriceSummary extras total', () => {
    it.todo('desktop panel displays base + extras total when extras are selected')
    it.todo('mobile bar displays base + extras total when extras are selected')
    it.todo('shows extras line items in desktop panel for each selected extra')
    it.todo('total updates when extras are toggled on and off')
  })
```
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo/prestigo && PATH=/Users/romanustyugov/.nvm/versions/node/v22.22.1/bin:$PATH npx vitest run tests/PriceSummary.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - prestigo/tests/PriceSummary.test.tsx contains `describe('STEP4-03`
    - prestigo/tests/PriceSummary.test.tsx contains `extras total`
    - Existing test stubs in the file are unchanged
    - vitest run exits 0
  </acceptance_criteria>
  <done>PriceSummary test file extended with STEP4-03 extras total stubs. All existing stubs unchanged. vitest passes.</done>
</task>

</tasks>

<verification>
cd /Users/romanustyugov/Desktop/Prestigo/prestigo && PATH=/Users/romanustyugov/.nvm/versions/node/v22.22.1/bin:$PATH npx vitest run --reporter=verbose 2>&1 | tail -30
All test files pass (todo stubs are skipped, not failures).
</verification>

<success_criteria>
- Three test files exist: Step4Extras.test.tsx, Step5Passenger.test.tsx, PriceSummary.test.tsx (extended)
- All 7 requirement IDs (STEP4-01 through STEP5-04) have corresponding describe blocks
- Full vitest suite passes with exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-booking-details/03-01-SUMMARY.md`
</output>
