---
phase: 01-foundation-trip-entry
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - prestigo/vitest.config.ts
  - prestigo/tests/setup.ts
  - prestigo/tests/booking-store.test.ts
  - prestigo/tests/BookingWizard.test.tsx
  - prestigo/tests/ProgressBar.test.tsx
  - prestigo/tests/TripTypeTabs.test.tsx
  - prestigo/tests/AddressInput.test.tsx
  - prestigo/tests/Step1TripType.test.tsx
  - prestigo/tests/Stepper.test.tsx
  - prestigo/package.json
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-03, WIZD-01, WIZD-02, WIZD-03, WIZD-04, WIZD-05, STEP1-01, STEP1-02, STEP1-03, STEP1-04, STEP1-05, STEP1-06, STEP1-07]

must_haves:
  truths:
    - "Vitest runs and finds test files in prestigo/tests/"
    - "All 8 test stub files exist with at least one describe block and one skipped/todo test per requirement"
    - "npx vitest run exits with 0 (all stubs are skipped/todo, not failing)"
  artifacts:
    - path: "prestigo/vitest.config.ts"
      provides: "Vitest config with jsdom, path aliases, setup file"
      contains: "jsdom"
    - path: "prestigo/tests/setup.ts"
      provides: "Testing Library global setup (cleanup, matchers)"
      contains: "@testing-library/jest-dom"
    - path: "prestigo/tests/booking-store.test.ts"
      provides: "Test stubs for ARCH-01 (Zustand store), ARCH-02 (sessionStorage persistence)"
      contains: "describe"
    - path: "prestigo/tests/BookingWizard.test.tsx"
      provides: "Test stubs for WIZD-01, WIZD-04, WIZD-05"
      contains: "describe"
    - path: "prestigo/tests/ProgressBar.test.tsx"
      provides: "Test stubs for WIZD-02"
      contains: "describe"
    - path: "prestigo/tests/TripTypeTabs.test.tsx"
      provides: "Test stubs for STEP1-01"
      contains: "describe"
    - path: "prestigo/tests/AddressInput.test.tsx"
      provides: "Test stubs for STEP1-02, STEP1-03 (Places API mocked)"
      contains: "describe"
    - path: "prestigo/tests/Step1TripType.test.tsx"
      provides: "Test stubs for WIZD-03, STEP1-04, STEP1-07"
      contains: "describe"
    - path: "prestigo/tests/Stepper.test.tsx"
      provides: "Test stubs for STEP1-05, STEP1-06"
      contains: "describe"
  key_links:
    - from: "prestigo/vitest.config.ts"
      to: "prestigo/tests/setup.ts"
      via: "setupFiles references setup.ts"
      pattern: "setup"
    - from: "prestigo/vitest.config.ts"
      to: "prestigo/tsconfig.json"
      via: "path alias @/ resolved to prestigo root"
      pattern: "@/"
---

<objective>
Install Vitest + Testing Library and create all test stub files required by the VALIDATION.md Wave 0 contract.

Purpose: Establish the test infrastructure so every subsequent implementation plan can run `npx vitest run` for behavioral verification. Without this, all `<verify>` commands referencing test files would fail.
Output: vitest.config.ts, tests/setup.ts, 8 test stub files, devDependencies in package.json
</objective>

<execution_context>
@/Users/romanustyugov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/romanustyugov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-trip-entry/01-CONTEXT.md
@.planning/phases/01-foundation-trip-entry/01-VALIDATION.md

<interfaces>
<!-- No prior plan outputs -- this is Wave 0. Test stubs reference component paths that will be created by Plans 01-04. -->

From prestigo/tsconfig.json:
```json
{
  "paths": { "@/*": ["./*"] }
}
```
Path alias `@/` maps to `prestigo/` root.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest and Testing Library devDependencies</name>
  <files>prestigo/package.json</files>
  <read_first>prestigo/package.json</read_first>
  <action>
Run from `prestigo/` directory:
```bash
npm install -D vitest @vitejs/plugin-react @testing-library/react @testing-library/user-event @testing-library/jest-dom jsdom
```

Do NOT modify any existing dependencies or scripts. Only add devDependencies.
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo/prestigo && node -e "const p=require('./package.json'); const deps=['vitest','@vitejs/plugin-react','@testing-library/react','@testing-library/user-event','@testing-library/jest-dom','jsdom']; deps.forEach(d => { if(!p.devDependencies[d]) throw new Error(d+' missing') }); console.log('All test deps present')"</automated>
  </verify>
  <done>All 6 test infrastructure devDependencies installed in package.json</done>
</task>

<task type="auto">
  <name>Task 2: Create Vitest config, setup file, and all 8 test stubs</name>
  <files>prestigo/vitest.config.ts, prestigo/tests/setup.ts, prestigo/tests/booking-store.test.ts, prestigo/tests/BookingWizard.test.tsx, prestigo/tests/ProgressBar.test.tsx, prestigo/tests/TripTypeTabs.test.tsx, prestigo/tests/AddressInput.test.tsx, prestigo/tests/Step1TripType.test.tsx, prestigo/tests/Stepper.test.tsx</files>
  <read_first>
    prestigo/tsconfig.json
    .planning/phases/01-foundation-trip-entry/01-VALIDATION.md
    .planning/phases/01-foundation-trip-entry/01-CONTEXT.md
  </read_first>
  <action>
Create `prestigo/tests/` directory if it does not exist.

Create `prestigo/vitest.config.ts`:
```typescript
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    setupFiles: ['./tests/setup.ts'],
    globals: true,
    css: false,
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, '.'),
    },
  },
})
```

Create `prestigo/tests/setup.ts`:
```typescript
import '@testing-library/jest-dom/vitest'
```

Create each test stub file with `describe` blocks mapping to VALIDATION.md requirements. Each test should use `it.todo()` so Vitest reports them as pending (not failing). Stubs reference the final import paths even though source files do not exist yet -- this is intentional; the stubs will fail to compile until the corresponding implementation plan runs.

**prestigo/tests/booking-store.test.ts** (ARCH-01, ARCH-02):
```typescript
import { describe, it } from 'vitest'

describe('booking-store', () => {
  describe('ARCH-01: Zustand store shape', () => {
    it.todo('initializes with default tripType "transfer"')
    it.todo('initializes with passengers=1, luggage=0, hours=2')
    it.todo('setTripType updates tripType')
    it.todo('setOrigin and setDestination update place fields')
    it.todo('setPassengers clamps between 1 and 8')
    it.todo('setLuggage clamps between 0 and 8')
    it.todo('nextStep increments currentStep and marks previous as completed')
    it.todo('prevStep decrements currentStep')
    it.todo('swapOriginDestination swaps origin and destination')
  })

  describe('ARCH-02: sessionStorage persistence', () => {
    it.todo('persists store state to sessionStorage')
    it.todo('rehydrates Set<number> from serialized array')
  })

  describe('airport auto-fill logic', () => {
    it.todo('airport_pickup sets destination to PRG_CONFIG')
    it.todo('airport_dropoff sets origin to PRG_CONFIG')
    it.todo('switching away from airport type clears PRG auto-fill')
  })
})
```

**prestigo/tests/BookingWizard.test.tsx** (WIZD-01, WIZD-04, WIZD-05):
```typescript
import { describe, it } from 'vitest'

describe('BookingWizard', () => {
  describe('WIZD-01: wizard renders on /book', () => {
    it.todo('renders BookingWizard with ProgressBar')
    it.todo('renders step content for currentStep')
  })

  describe('WIZD-04: Back navigation', () => {
    it.todo('Back button hidden on step 1')
    it.todo('Back button visible on step 2+')
    it.todo('clicking Back decrements currentStep')
  })

  describe('WIZD-05: step transitions', () => {
    it.todo('step container has animate-step-enter class')
    it.todo('step container uses key={currentStep} for remount')
  })
})
```

**prestigo/tests/ProgressBar.test.tsx** (WIZD-02):
```typescript
import { describe, it } from 'vitest'

describe('ProgressBar', () => {
  describe('WIZD-02: progress indicator', () => {
    it.todo('renders 6 circles')
    it.todo('active step circle has copper styling')
    it.todo('completed step shows check icon')
    it.todo('pending steps have anthracite styling')
    it.todo('has aria-label with current step')
    it.todo('active circle has aria-current="step"')
  })
})
```

**prestigo/tests/TripTypeTabs.test.tsx** (STEP1-01):
```typescript
import { describe, it } from 'vitest'

describe('TripTypeTabs', () => {
  describe('STEP1-01: trip type selector', () => {
    it.todo('renders 5 tabs: ONE-WAY, AIRPORT PICKUP, AIRPORT DROPOFF, HOURLY, DAILY')
    it.todo('active tab has aria-selected="true"')
    it.todo('clicking a tab calls setTripType')
    it.todo('active tab shows copper underline, not filled background')
    it.todo('has role="tablist" on container')
  })
})
```

**prestigo/tests/AddressInput.test.tsx** (STEP1-02, STEP1-03):
```typescript
import { describe, it } from 'vitest'

describe('AddressInput', () => {
  describe('STEP1-02: Google Places Autocomplete', () => {
    it.todo('renders input with aria-autocomplete="list"')
    it.todo('shows suggestions after 2+ characters typed')
    it.todo('selecting a suggestion calls onSelect with PlaceResult')
    it.todo('clear button calls onClear')
  })

  describe('STEP1-03: airport read-only mode', () => {
    it.todo('renders read-only div when readOnly=true')
    it.todo('shows plane icon when readOnlyIcon=true')
    it.todo('displays "Auto-set for airport transfers." helper text')
    it.todo('no clear button in read-only mode')
  })
})
```

**prestigo/tests/Step1TripType.test.tsx** (WIZD-03, STEP1-04, STEP1-07):
```typescript
import { describe, it } from 'vitest'

describe('Step1TripType', () => {
  describe('WIZD-03: Step 1 is trip type entry', () => {
    it.todo('renders TripTypeTabs')
    it.todo('renders origin and destination AddressInput')
    it.todo('renders Passengers and Luggage steppers')
  })

  describe('STEP1-04: conditional fields by trip type', () => {
    it.todo('hourly hides destination, shows DurationSelector')
    it.todo('airport_pickup shows read-only destination with PRG')
    it.todo('airport_dropoff shows read-only origin with PRG')
    it.todo('transfer shows swap icon between origin and destination')
  })

  describe('STEP1-07: validation and Next button', () => {
    it.todo('Continue button disabled when origin is empty')
    it.todo('Continue button disabled when destination is empty (non-hourly)')
    it.todo('shows error messages on Continue click with empty fields')
    it.todo('errors clear when field receives valid value')
    it.todo('Continue calls nextStep when all fields valid')
  })
})
```

**prestigo/tests/Stepper.test.tsx** (STEP1-05, STEP1-06):
```typescript
import { describe, it } from 'vitest'

describe('Stepper', () => {
  describe('STEP1-05: passengers stepper', () => {
    it.todo('renders label, minus button, count, plus button')
    it.todo('clicking plus increments value')
    it.todo('clicking minus decrements value')
    it.todo('minus disabled at min value')
    it.todo('plus disabled at max value')
  })

  describe('STEP1-06: luggage stepper', () => {
    it.todo('luggage starts at 0')
    it.todo('luggage cannot go below 0')
    it.todo('luggage cannot exceed 8')
  })
})
```

Key: All tests use `it.todo()` so they register as pending, not failures. This means `npx vitest run` will exit 0 while listing all planned behavioral tests.
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo/prestigo && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Vitest config created, setup file created, all 8 test stub files exist with todo tests, npx vitest run exits 0</done>
</task>

</tasks>

<verification>
1. `cd prestigo && npx vitest run --reporter=verbose` exits 0 and lists all test files
2. `ls prestigo/tests/` shows 9 files (setup.ts + 8 test files)
3. `prestigo/vitest.config.ts` exists with jsdom environment and @/ alias
4. All test devDependencies in package.json
</verification>

<success_criteria>
- Vitest runs successfully with 0 failures (all tests are .todo())
- All 8 test files listed in VALIDATION.md Wave 0 Requirements exist
- Path alias @/ resolves correctly in vitest.config.ts
- Testing Library setup file imports jest-dom matchers
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-trip-entry/01-00-SUMMARY.md`
</output>
