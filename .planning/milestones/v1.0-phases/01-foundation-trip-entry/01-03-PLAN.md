---
phase: 01-foundation-trip-entry
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - prestigo/components/booking/TripTypeTabs.tsx
  - prestigo/components/booking/Stepper.tsx
  - prestigo/components/booking/DurationSelector.tsx
autonomous: true
requirements: [STEP1-01, STEP1-05, STEP1-06, STEP1-07]

must_haves:
  truths:
    - "User can select any of the 5 trip types and the tab visually indicates the active selection with copper"
    - "User can increment/decrement passengers between 1-8 and luggage between 0-8"
    - "For Hourly trip type, a duration selector shows segmented buttons 1h through 12h"
  artifacts:
    - path: "prestigo/components/booking/TripTypeTabs.tsx"
      provides: "5-tab horizontal trip type selector"
      exports: ["default (TripTypeTabs)"]
    - path: "prestigo/components/booking/Stepper.tsx"
      provides: "Reusable increment/decrement counter"
      exports: ["default (Stepper)"]
    - path: "prestigo/components/booking/DurationSelector.tsx"
      provides: "Segmented hour buttons for Hourly trips"
      exports: ["default (DurationSelector)"]
  key_links:
    - from: "prestigo/components/booking/TripTypeTabs.tsx"
      to: "prestigo/lib/booking-store.ts"
      via: "useBookingStore().setTripType"
      pattern: "setTripType"
    - from: "prestigo/components/booking/Stepper.tsx"
      to: "prestigo/lib/booking-store.ts"
      via: "onChange callback connected to setPassengers/setLuggage"
      pattern: "onChange"
    - from: "prestigo/components/booking/DurationSelector.tsx"
      to: "prestigo/lib/booking-store.ts"
      via: "useBookingStore().setHours"
      pattern: "setHours"
---

<objective>
Build the three Step 1 sub-components: TripTypeTabs, Stepper, and DurationSelector.

Purpose: These are self-contained UI components that Plan 04 will assemble into the Step1TripType view. Building them separately enables parallel execution with Plan 02 (wizard shell).
Output: TripTypeTabs.tsx, Stepper.tsx, DurationSelector.tsx
</objective>

<execution_context>
@/Users/romanustyugov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/romanustyugov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-trip-entry/01-CONTEXT.md
@.planning/phases/01-foundation-trip-entry/01-UI-SPEC.md
@.planning/phases/01-foundation-trip-entry/01-RESEARCH.md

<interfaces>
From prestigo/types/booking.ts (created by Plan 01):
```typescript
export type TripType = 'transfer' | 'airport_pickup' | 'airport_dropoff' | 'hourly' | 'daily'
export interface BookingStore {
  tripType: TripType
  hours: number
  passengers: number
  luggage: number
  setTripType: (type: TripType) => void
  setHours: (h: number) => void
  setPassengers: (n: number) => void
  setLuggage: (n: number) => void
}
```

From prestigo/lib/booking-store.ts (created by Plan 01):
```typescript
export const useBookingStore: UseBoundStore<StoreApi<BookingStore>>
```

From prestigo/app/globals.css (existing):
```css
.label { font-size: 9px; letter-spacing: 0.4em; text-transform: uppercase; color: var(--copper); }
:focus-visible { outline: 2px solid var(--copper); outline-offset: 4px; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TripTypeTabs component</name>
  <files>prestigo/components/booking/TripTypeTabs.tsx</files>
  <read_first>
    prestigo/app/globals.css
    .planning/phases/01-foundation-trip-entry/01-UI-SPEC.md
    .planning/phases/01-foundation-trip-entry/01-CONTEXT.md
  </read_first>
  <action>
Create `prestigo/components/booking/TripTypeTabs.tsx`:

- `'use client'` directive
- Import `useBookingStore` from `@/lib/booking-store`
- Import `TripType` from `@/types/booking`

Tab data array (order matters — matches UI-SPEC):
```typescript
const TRIP_TYPES: { value: TripType; label: string }[] = [
  { value: 'transfer', label: 'ONE-WAY' },
  { value: 'airport_pickup', label: 'AIRPORT PICKUP' },
  { value: 'airport_dropoff', label: 'AIRPORT DROPOFF' },
  { value: 'hourly', label: 'HOURLY' },
  { value: 'daily', label: 'DAILY' },
]
```

Component reads `tripType` and `setTripType` from store.

Rendering:
- Container: `role="tablist"`, horizontal scrollable row using `overflow-x: auto` with hidden scrollbar (`-webkit-scrollbar: none, scrollbar-width: none`). Sticky within form via `position: sticky; top: 0; z-index: 10`. Background `var(--anthracite)` so content scrolls behind.
- Bottom border on container: `border-bottom: 1px solid var(--anthracite-light)`
- Each tab: `<button role="tab" aria-selected={isActive}>`
- Tab font: Montserrat 9px, weight 400, letter-spacing 0.35em, uppercase (inline styles, not .label class — different letter-spacing)
- Active tab: `color: var(--copper)`, bottom border 2px `var(--copper)` via `border-bottom`
- Inactive tab: `color: var(--warmgrey)`, no bottom border (or transparent)
- Hover (inactive only): `color: var(--offwhite)`
- Tab padding: 12px vertical (input-pad-v token), 16px horizontal
- `white-space: nowrap` on each tab to prevent wrapping
- On click: call `setTripType(tab.value)`
- NO filled background on active tab — copper underline or text only (locked decision)
- Transition: `color 0.2s ease, border-color 0.2s ease`
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo/prestigo && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - prestigo/components/booking/TripTypeTabs.tsx contains `role="tablist"`
    - prestigo/components/booking/TripTypeTabs.tsx contains `role="tab"`
    - prestigo/components/booking/TripTypeTabs.tsx contains `aria-selected`
    - prestigo/components/booking/TripTypeTabs.tsx contains all 5 labels: ONE-WAY, AIRPORT PICKUP, AIRPORT DROPOFF, HOURLY, DAILY
    - prestigo/components/booking/TripTypeTabs.tsx contains `setTripType`
    - prestigo/components/booking/TripTypeTabs.tsx contains `letter-spacing` value of `0.35em`
    - prestigo/components/booking/TripTypeTabs.tsx contains `var(--copper)` for active state
    - prestigo/components/booking/TripTypeTabs.tsx does NOT contain any filled background for active tab
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>TripTypeTabs renders 5 horizontal tabs with copper underline on active, ARIA tablist attributes, and Zustand integration</done>
</task>

<task type="auto">
  <name>Task 2: Create Stepper and DurationSelector components</name>
  <files>prestigo/components/booking/Stepper.tsx, prestigo/components/booking/DurationSelector.tsx</files>
  <read_first>
    prestigo/app/globals.css
    .planning/phases/01-foundation-trip-entry/01-UI-SPEC.md
  </read_first>
  <action>
Create `prestigo/components/booking/Stepper.tsx`:

- `'use client'` directive
- Import `Minus`, `Plus` from `lucide-react`
- Props interface:
```typescript
interface StepperProps {
  label: string          // "PASSENGERS" or "LUGGAGE"
  value: number
  min: number
  max: number
  onChange: (value: number) => void
}
```
- Layout: row with label on the left, `[minus] [count] [plus]` on the right
- Label: Montserrat 9px, weight 400, letter-spacing 0.4em, uppercase, `var(--copper)` — use `.label` class
- Minus button: 32px x 32px, `border: 1px solid var(--anthracite-light)`, displays `<Minus size={14} />` in `var(--copper)`. When `value === min`, opacity 0.3, `cursor: not-allowed`, `aria-disabled="true"`. `aria-label="Decrease {label.toLowerCase()}"`.
- Plus button: 32px x 32px, same styling as minus. When `value === max`, opacity 0.3, `cursor: not-allowed`, `aria-disabled="true"`. `aria-label="Increase {label.toLowerCase()}"`.
- Count display: Montserrat 14px, weight 300, `var(--warmgrey)`, min-width 32px, text-align center, between the two buttons
- On minus click: `onChange(Math.max(min, value - 1))`
- On plus click: `onChange(Math.min(max, value + 1))`
- Background on buttons: `var(--anthracite-mid)`
- The label and stepper controls are on the same horizontal line, with the label left-aligned and controls right-aligned using `flex justify-between items-center`

Create `prestigo/components/booking/DurationSelector.tsx`:

- `'use client'` directive
- Import `useBookingStore` from `@/lib/booking-store`
- Duration options: `[1, 2, 3, 4, 6, 8, 12]` (these are the hour values)
- Display labels: `"1h"`, `"2h"`, `"3h"`, `"4h"`, `"6h"`, `"8h"`, `"12h"`
- Read `hours` and `setHours` from store
- Label above: "DURATION" using `.label` class
- Segmented button group container: outer border `1px solid var(--anthracite-light)`, `display: flex`, no gap between segments, full width
- Each segment: minimum height 44px (touch-target), equal width via `flex: 1`
- Active segment: `border-bottom: 2px solid var(--copper)`, text `var(--offwhite)`
- Inactive segment: text `var(--warmgrey)`, border between segments `1px solid var(--anthracite-light)` (use `border-right` on all except last)
- Segment font: Montserrat 9px, weight 400, letter-spacing 0.2em
- Background: `var(--anthracite-mid)` for inactive, `var(--anthracite)` for active
- On click: `setHours(hourValue)`
- Each segment: `<button aria-pressed={isActive}>` with `aria-label="{N} hours"`
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo/prestigo && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - prestigo/components/booking/Stepper.tsx contains `aria-label` with "Decrease" and "Increase"
    - prestigo/components/booking/Stepper.tsx contains `aria-disabled`
    - prestigo/components/booking/Stepper.tsx imports `Minus` and `Plus` from `lucide-react`
    - prestigo/components/booking/Stepper.tsx contains `var(--copper)` for button icons
    - prestigo/components/booking/Stepper.tsx contains `opacity` value for disabled state
    - prestigo/components/booking/DurationSelector.tsx contains all 7 duration values: 1, 2, 3, 4, 6, 8, 12
    - prestigo/components/booking/DurationSelector.tsx contains `setHours`
    - prestigo/components/booking/DurationSelector.tsx contains `aria-pressed`
    - prestigo/components/booking/DurationSelector.tsx contains `var(--copper)` for active segment border
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Stepper renders +/- controls with clamped ranges and ARIA labels; DurationSelector renders 7 segmented hour buttons with active state</done>
</task>

</tasks>

<verification>
1. `cd prestigo && npx tsc --noEmit` exits 0
2. TripTypeTabs has 5 tabs with correct ARIA attributes
3. Stepper has +/- with copper icons and disabled states at bounds
4. DurationSelector has 7 segments with active/inactive styling
</verification>

<success_criteria>
- TripTypeTabs integrates with Zustand setTripType
- Stepper clamps values to min/max range
- DurationSelector shows 7 options (1h, 2h, 3h, 4h, 6h, 8h, 12h)
- All components follow UI-SPEC colors (copper accent, anthracite backgrounds)
- All interactive elements have aria-labels
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-trip-entry/01-03-SUMMARY.md`
</output>
