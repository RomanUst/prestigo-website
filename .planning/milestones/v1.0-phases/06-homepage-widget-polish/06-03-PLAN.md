---
phase: 06-homepage-widget-polish
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified: []
autonomous: false
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05, HOME-01, HOME-02, HOME-03]

must_haves:
  truths:
    - "Homepage widget is visually correct and functional at desktop and 375px mobile"
    - "Book Now CTA navigates to /book Step 3 with pre-filled data"
    - "All 6 wizard steps are usable at 375px with no overflow or hidden CTAs"
    - "Keyboard tab order is logical through all steps"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of the complete Phase 6 deliverable -- homepage widget and mobile/accessibility polish across all wizard steps.

Purpose: Human confirms the widget works end-to-end and the mobile UX is production-ready before marking Phase 6 complete.
Output: Verification sign-off or list of issues to fix.
</objective>

<execution_context>
@/Users/romanustyugov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/romanustyugov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-homepage-widget-polish/06-CONTEXT.md
@.planning/phases/06-homepage-widget-polish/06-UI-SPEC.md
@.planning/phases/06-homepage-widget-polish/06-01-SUMMARY.md
@.planning/phases/06-homepage-widget-polish/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and build verification</name>
  <files></files>
  <read_first>
    - prestigo/components/booking/BookingWidget.tsx (verify it exists and looks correct)
    - prestigo/components/BookingSection.tsx (verify iframe removed, widget rendered)
  </read_first>
  <action>
Run the full verification sequence:

1. `cd prestigo && npx vitest run` — all tests must pass
2. `cd prestigo && npx tsc --noEmit` — no TypeScript errors
3. `cd prestigo && npx next build 2>&1 | tail -30` — production build succeeds (catches runtime import issues)

If any step fails, diagnose and fix before proceeding to the human checkpoint.

Also verify with grep:
- `grep -rn "LIMOANYWHERE" prestigo/` — must return nothing (iframe fully removed)
- `grep -rn "safe-area-inset-bottom" prestigo/components/booking/` — must return 3+ matches
- `grep -rn "scrollIntoView" prestigo/components/booking/` — must return 2+ matches
- `grep -c "aria-label" prestigo/components/booking/Stepper.tsx` — must return 2
  </action>
  <verify>
    <automated>cd prestigo && npx vitest run && npx tsc --noEmit && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - `cd prestigo && npx vitest run` exits 0
    - `cd prestigo && npx tsc --noEmit` exits 0
    - `grep -rn "LIMOANYWHERE" prestigo/` returns no matches
    - `grep -rn "safe-area-inset-bottom" prestigo/components/booking/` returns 3+ matches
  </acceptance_criteria>
  <done>Full test suite passes, TypeScript compiles cleanly, no references to removed iframe remain, all UX polish patterns are present in code.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification</name>
  <what-built>
    1. Homepage booking widget replacing LimoAnywhere iframe — with trip type tabs, address fields, date/time, and Book Now CTA
    2. Widget CTA deep-links to /book wizard at Step 3 with pre-filled data
    3. Mobile sticky bars with safe-area-inset-bottom padding across all wizard steps
    4. scrollIntoView on input focus for mobile keyboard visibility
    5. Stepper touch targets increased to 44x44px
    6. All icon-only buttons have aria-labels
  </what-built>
  <how-to-verify>
    **Widget verification (desktop):**
    1. Visit homepage, scroll to booking section
    2. Verify widget shows: trip type tabs (Transfer/Hourly/Daily), pickup address, destination, date, time, Book Now button
    3. Select "Hourly" tab — destination field should disappear, duration selector should appear
    4. Select "Transfer" tab — destination field returns
    5. Fill in: pickup address, destination, date (today or future), time
    6. Click "Book Now" — should navigate to /book showing Step 3 (vehicle selection) with progress bar showing steps 1-2 as completed

    **Widget verification (mobile 375px):**
    7. Open Chrome DevTools, set viewport to 375px width
    8. Verify widget fields stack vertically with no horizontal overflow
    9. Verify "Book Now" button is full width and visible

    **Mobile UX verification (375px viewport in /book wizard):**
    10. Navigate through all 6 steps at 375px — verify no horizontal overflow at any step
    11. At Step 1: verify sticky Continue bar at bottom, tap an address field — verify the field scrolls into view above keyboard
    12. At Step 2: verify sticky Continue bar
    13. At Step 3: verify PriceSummary fixed bar at bottom (not wizard bar)
    14. At Steps 4-5: verify sticky Continue bar

    **Accessibility verification:**
    15. Tab through the widget fields — verify focus moves in logical order (trip type tabs, origin, destination/duration, date, time, Book Now)
    16. On /book Step 1, Tab through — verify logical order through all interactive elements
    17. Check Stepper +/- buttons are large enough to tap comfortably (44px target)
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 6, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
Human visual verification covers all Phase 6 requirements:
- HOME-01: Widget visible on homepage
- HOME-02: Widget contains all required fields
- HOME-03: CTA carries data to wizard Step 3
- UX-01: No overflow at 375px
- UX-02: PriceSummary positioning correct
- UX-03: CTAs visible above keyboard
- UX-04: aria-labels present (screen reader or DevTools inspection)
- UX-05: Tab order logical
</verification>

<success_criteria>
Human approves the widget and mobile UX, or provides specific issues to address in a gap-closure plan.
</success_criteria>

<output>
After completion, create `.planning/phases/06-homepage-widget-polish/06-03-SUMMARY.md`
</output>
