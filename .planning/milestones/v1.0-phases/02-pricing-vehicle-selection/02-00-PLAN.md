---
phase: 02-pricing-vehicle-selection
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - prestigo/tests/pricing.test.ts
  - prestigo/tests/calculate-price.test.ts
  - prestigo/tests/Step2DateTime.test.tsx
  - prestigo/tests/Step3Vehicle.test.tsx
  - prestigo/tests/PriceSummary.test.tsx
autonomous: true
requirements: [PRICE-01, PRICE-02, PRICE-03, PRICE-04, PRICE-05, PRICE-06, STEP2-01, STEP2-02, STEP2-03, STEP3-01, STEP3-02, STEP3-03, STEP3-04, STEP3-05]

must_haves:
  truths:
    - "Test stubs exist for all Phase 2 requirements before implementation begins"
    - "npx vitest run completes without errors (all stubs are .todo)"
  artifacts:
    - path: "prestigo/tests/pricing.test.ts"
      provides: "Test stubs for pricing module (PRICE-01 through PRICE-06)"
    - path: "prestigo/tests/calculate-price.test.ts"
      provides: "Test stubs for /api/calculate-price route handler"
    - path: "prestigo/tests/Step2DateTime.test.tsx"
      provides: "Test stubs for Step2DateTime component (STEP2-01 through STEP2-03)"
    - path: "prestigo/tests/Step3Vehicle.test.tsx"
      provides: "Test stubs for Step3Vehicle + VehicleCard (STEP3-01 through STEP3-04)"
    - path: "prestigo/tests/PriceSummary.test.tsx"
      provides: "Test stubs for PriceSummary panel (STEP3-04, STEP3-05)"
  key_links: []
---

<objective>
Create Vitest test stub files for all Phase 2 requirements. These are `.todo` stubs that define the behavioral expectations before any implementation begins, following the Nyquist rule.

Purpose: Every plan task has a corresponding test file with stubs that will be filled in as implementation progresses. This ensures verification coverage exists before code is written.
Output: 5 test stub files in prestigo/tests/ matching the Phase 1 test file convention
</objective>

<execution_context>
@/Users/romanustyugov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/romanustyugov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pricing-vehicle-selection/02-CONTEXT.md
@.planning/phases/02-pricing-vehicle-selection/02-VALIDATION.md

<interfaces>
<!-- Existing test infrastructure from Phase 1 -->

From prestigo/vitest.config.ts:
```typescript
// environment: 'jsdom', setupFiles: ['./tests/setup.ts'], globals: true
// alias: '@' -> project root
```

From prestigo/tests/booking-store.test.ts (pattern reference):
```typescript
import { describe, it } from 'vitest'

describe('booking-store', () => {
  describe('ARCH-01: Zustand store shape', () => {
    it.todo('initializes with default tripType "transfer"')
    // ... more .todo stubs
  })
})
```

Phase 2 test files follow the same pattern: describe blocks grouped by requirement ID, it.todo stubs for each behavior.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs for pricing module and API route</name>
  <files>prestigo/tests/pricing.test.ts, prestigo/tests/calculate-price.test.ts</files>
  <read_first>prestigo/tests/booking-store.test.ts, prestigo/vitest.config.ts, prestigo/types/booking.ts</read_first>
  <action>
**Create prestigo/tests/pricing.test.ts** with `.todo` stubs:

```typescript
import { describe, it } from 'vitest'

describe('pricing module', () => {
  describe('PRICE-02: Transfer pricing', () => {
    it.todo('calculates base = distanceKm * RATE_PER_KM for business class')
    it.todo('calculates base = distanceKm * RATE_PER_KM for first_class')
    it.todo('calculates base = distanceKm * RATE_PER_KM for business_van')
    it.todo('throws error when distanceKm is null for transfer trip type')
  })

  describe('PRICE-03: Hourly pricing', () => {
    it.todo('calculates base = hours * HOURLY_RATE for each vehicle class')
    it.todo('uses minimum 2 hours when hours is 0')
  })

  describe('PRICE-04: Daily pricing', () => {
    it.todo('calculates base = days * DAILY_RATE for each vehicle class')
    it.todo('dateDiffDays returns minimum 1 day')
    it.todo('dateDiffDays calculates correct day difference')
  })

  describe('PRICE-05: Rate tables server-side only', () => {
    it.todo('RATE_PER_KM contains all 3 vehicle classes')
    it.todo('HOURLY_RATE contains all 3 vehicle classes')
    it.todo('DAILY_RATE contains all 3 vehicle classes')
  })

  describe('buildPriceMap', () => {
    it.todo('returns prices for all 3 vehicle classes in a single call')
    it.todo('each PriceBreakdown has base, extras, total, currency fields')
    it.todo('total equals base + extras')
  })
})
```

**Create prestigo/tests/calculate-price.test.ts** with `.todo` stubs:

```typescript
import { describe, it } from 'vitest'

describe('/api/calculate-price route', () => {
  describe('PRICE-01: API route proxies Google Routes API', () => {
    it.todo('POST returns prices for transfer with valid origin/destination')
    it.todo('POST returns quoteMode: true when origin is missing')
    it.todo('POST returns quoteMode: true when Google API key is missing')
    it.todo('POST returns quoteMode: true when Google API returns error')
  })

  describe('PRICE-03: Hourly pricing via API', () => {
    it.todo('POST returns prices for hourly trip type without calling Google Routes')
    it.todo('POST uses hours from request body')
  })

  describe('PRICE-04: Daily pricing via API', () => {
    it.todo('POST returns prices for daily trip type with pickupDate and returnDate')
    it.todo('POST returns quoteMode: true when dates are missing for daily')
  })

  describe('PRICE-06: API key not exposed', () => {
    it.todo('response body does not contain API key')
    it.todo('API key is sent via X-Goog-Api-Key header to Google (server-side)')
  })
})
```

Follow the Phase 1 pattern exactly: `import { describe, it } from 'vitest'`, all tests are `it.todo(...)`.
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo/prestigo && npx vitest run tests/pricing.test.ts tests/calculate-price.test.ts --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>pricing.test.ts and calculate-price.test.ts exist with .todo stubs covering PRICE-01 through PRICE-06. Vitest runs without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs for Step2DateTime, Step3Vehicle, and PriceSummary</name>
  <files>prestigo/tests/Step2DateTime.test.tsx, prestigo/tests/Step3Vehicle.test.tsx, prestigo/tests/PriceSummary.test.tsx</files>
  <read_first>prestigo/tests/BookingWizard.test.tsx, prestigo/tests/Step1TripType.test.tsx</read_first>
  <action>
**Create prestigo/tests/Step2DateTime.test.tsx** with `.todo` stubs:

```typescript
import { describe, it } from 'vitest'

describe('Step2DateTime', () => {
  describe('STEP2-01: Pickup date selection', () => {
    it.todo('renders an inline DayPicker calendar (not a popover)')
    it.todo('past dates are disabled and not selectable')
    it.todo('selecting a date calls setPickupDate with ISO format string')
    it.todo('selected date shows copper background styling')
  })

  describe('STEP2-02: Pickup time selection', () => {
    it.todo('renders 96 time slots at 15-minute increments')
    it.todo('time slots are rendered in a listbox with role="listbox"')
    it.todo('each slot has role="option" and aria-selected')
    it.todo('selecting a time calls setPickupTime with HH:MM format')
    it.todo('selected time slot scrolls into view')
  })

  describe('STEP2-03: Daily Hire return date', () => {
    it.todo('shows second calendar when tripType is "daily"')
    it.todo('hides return date calendar for non-daily trip types')
    it.todo('return date cannot be before pickup date')
    it.todo('changing pickup date clears return date if now invalid')
  })
})
```

**Create prestigo/tests/Step3Vehicle.test.tsx** with `.todo` stubs:

```typescript
import { describe, it } from 'vitest'

describe('Step3Vehicle', () => {
  describe('STEP3-01: Vehicle class display', () => {
    it.todo('renders 3 vehicle cards: Business, First Class, Business Van')
    it.todo('each card shows vehicle photo, class name, passenger count, luggage count')
  })

  describe('STEP3-02: Vehicle card content', () => {
    it.todo('card displays calculated price when available')
    it.todo('card shows skeleton shimmer while price is loading')
    it.todo('card shows "Request a quote" in quoteMode')
  })

  describe('STEP3-03: Live price calculation', () => {
    it.todo('fetches prices from /api/calculate-price on mount')
    it.todo('fetch is called only once (not on every card click)')
    it.todo('stores all 3 vehicle prices in Zustand after fetch')
  })

  describe('STEP3-04: Vehicle selection', () => {
    it.todo('clicking a card calls setVehicleClass with the card key')
    it.todo('selected card shows copper border ring')
    it.todo('selected card has aria-pressed="true"')
  })

  describe('STEP3-05: Quote fallback', () => {
    it.todo('all 3 cards show "Request a quote" when route is unmappable')
    it.todo('user can still select a vehicle in quoteMode')
  })
})
```

**Create prestigo/tests/PriceSummary.test.tsx** with `.todo` stubs:

```typescript
import { describe, it } from 'vitest'

describe('PriceSummary', () => {
  describe('STEP3-04: Real-time price updates', () => {
    it.todo('shows selected vehicle class name')
    it.todo('shows price for selected vehicle class')
    it.todo('updates price display when vehicleClass changes in store')
    it.todo('shows em dash when no vehicle is selected')
  })

  describe('Desktop layout', () => {
    it.todo('renders journey summary with origin and destination')
    it.todo('truncates long addresses to 28 characters')
    it.todo('applies sticky positioning on desktop')
  })

  describe('Mobile layout', () => {
    it.todo('renders fixed bottom bar on mobile with price total')
    it.todo('mobile bar height is 56px')
  })

  describe('STEP3-05: Quote mode', () => {
    it.todo('shows "Request a quote" instead of price in quoteMode')
  })
})
```

Follow the Phase 1 pattern: `.tsx` extension for component test files, `import { describe, it } from 'vitest'`, all tests `it.todo(...)`.
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo/prestigo && npx vitest run tests/Step2DateTime.test.tsx tests/Step3Vehicle.test.tsx tests/PriceSummary.test.tsx --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>Step2DateTime.test.tsx, Step3Vehicle.test.tsx, and PriceSummary.test.tsx exist with .todo stubs covering STEP2-01 through STEP2-03 and STEP3-01 through STEP3-05. Vitest runs without errors.</done>
</task>

</tasks>

<verification>
- `cd prestigo && npx vitest run --reporter=verbose 2>&1 | grep -c "todo"` -- shows todo count including Phase 2 stubs
- `ls prestigo/tests/pricing.test.ts prestigo/tests/calculate-price.test.ts prestigo/tests/Step2DateTime.test.tsx prestigo/tests/Step3Vehicle.test.tsx prestigo/tests/PriceSummary.test.tsx` -- all 5 files exist
- `cd prestigo && npx vitest run` -- completes without errors
</verification>

<success_criteria>
1. All 5 test stub files exist in prestigo/tests/
2. Each file uses .todo stubs matching Phase 1 pattern
3. Stubs cover all Phase 2 requirement IDs (STEP2-01 through STEP2-03, STEP3-01 through STEP3-05, PRICE-01 through PRICE-06)
4. npx vitest run completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-pricing-vehicle-selection/02-00-SUMMARY.md`
</output>
