---
phase: 09-resend-domain-verification-email-sign-off
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified: []
autonomous: false
requirements:
  - EMAIL-03
  - EMAIL-04
user_setup:
  - service: vercel
    why: "Deploy updated email.ts to production"
    dashboard_config:
      - task: "Redeploy after email.ts domain fix is pushed"
        location: "Vercel Dashboard or git push triggers auto-deploy"
must_haves:
  truths:
    - "Client confirmation email arrives in inbox (not spam) from bookings@rideprestigo.com"
    - "Manager alert email arrives in inbox (not spam) from bookings@rideprestigo.com"
  artifacts: []
  key_links:
    - from: "prestigo/lib/email.ts"
      to: "Resend verified domain"
      via: "resend.emails.send() with verified from address"
      pattern: "bookings@rideprestigo\\.com"
    - from: "prestigo/app/api/webhooks/stripe/route.ts"
      to: "prestigo/lib/email.ts"
      via: "sendClientConfirmation + sendManagerAlert calls"
      pattern: "sendClientConfirmation|sendManagerAlert"
---

<objective>
Deploy the domain fix and verify end-to-end email delivery. Both client confirmation and manager alert emails must land in inbox (not spam) from the verified rideprestigo.com domain.

Purpose: This is the final sign-off for the entire v1.1 Go Live milestone. If emails land in spam or fail to deliver, the booking flow is broken for customers.
Output: Confirmed email delivery for both email types from the verified domain.
</objective>

<execution_context>
@/Users/romanustyugov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/romanustyugov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-resend-domain-verification-email-sign-off/09-CONTEXT.md
@.planning/phases/09-resend-domain-verification-email-sign-off/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy updated email.ts to production</name>
  <files></files>
  <read_first>
    - prestigo/lib/email.ts
  </read_first>
  <action>
Push the email.ts domain fix (from Plan 01) to main branch so Vercel auto-deploys.

Steps:
1. Verify the email.ts fix is committed: `git log --oneline -5` should show the domain fix commit
2. Push to remote: `git push origin main`
3. Wait for Vercel deployment to complete (check via `vercel ls` or Vercel Dashboard)
4. Confirm the production deployment is live with the updated code

If the commit from Plan 01 has not been pushed yet, push it now. If it was already pushed and deployed, skip to verification.
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo && git log --oneline -5 | grep -qi "email\|domain\|rideprestigo" && echo "PASS: domain fix commit found" || echo "FAIL: domain fix commit not found in recent history"</automated>
  </verify>
  <acceptance_criteria>
    - git log shows the email.ts domain fix commit
    - Vercel production deployment is live with the updated email.ts
  </acceptance_criteria>
  <done>Production deployment includes the rideprestigo.com domain fix in email.ts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Email delivery sign-off — client confirmation and manager alert</name>
  <files></files>
  <what-built>The email.ts domain fix is deployed to production. Both email types now send from bookings@rideprestigo.com via the verified Resend domain. This checkpoint verifies actual inbox delivery.</what-built>
  <read_first>
    - prestigo/lib/email.ts
    - .planning/phases/09-resend-domain-verification-email-sign-off/09-CONTEXT.md
  </read_first>
  <action>
User must trigger test emails and verify inbox delivery. Two options:

Option A — Trigger via real booking flow:
1. Go to the production site (rideprestigo.com)
2. Complete a test booking with a real email address you can check
3. Use Stripe test card if still in test mode, or a minimal real payment
4. After payment succeeds, check both emails arrived

Option B — Trigger via Resend Dashboard test send:
1. Go to https://app.resend.com
2. Use the "Send Test Email" feature to send from bookings@rideprestigo.com
3. This only tests deliverability, not the full template

For whichever option, verify ALL of these:

CLIENT CONFIRMATION EMAIL:
- Email arrives in inbox (not spam/junk folder)
- From address shows "PRESTIGO Bookings" or bookings@rideprestigo.com
- Email contains booking reference, journey details, vehicle info
- "Add to Calendar" link works
- "Need Assistance?" section shows roman@rideprestigo.com (not rideprestige.com)

MANAGER ALERT EMAIL:
- Email arrives at the MANAGER_EMAIL address inbox (not spam/junk)
- From address shows "PRESTIGO Bookings" or bookings@rideprestigo.com
- Email contains booking reference, client details, route, vehicle, payment amount

SPAM CHECK (Gmail baseline):
- Check Gmail inbox (primary tab, not Promotions/Updates)
- If in Promotions tab, note it but this is acceptable for v1.1
- If in Spam folder, this is a BLOCKER — do not approve
  </action>
  <how-to-verify>
    1. Trigger email send via booking flow or Resend Dashboard
    2. Check Gmail inbox for client confirmation email from bookings@rideprestigo.com
    3. Check MANAGER_EMAIL inbox for manager alert email from bookings@rideprestigo.com
    4. Verify neither email is in spam/junk folder
    5. Verify from address displays rideprestigo.com domain
    6. If using booking flow, verify email content (booking reference, route, vehicle, total)
  </how-to-verify>
  <verify>User confirms both emails delivered to inbox, not spam</verify>
  <acceptance_criteria>
    - User confirms client confirmation email arrived in inbox (not spam)
    - User confirms manager alert email arrived in inbox (not spam)
    - Both emails show from address containing rideprestigo.com
    - HTML contact section shows roman@rideprestigo.com
  </acceptance_criteria>
  <done>Both client confirmation and manager alert emails delivered to inbox from verified rideprestigo.com domain</done>
  <resume-signal>Type "approved" if both emails land in inbox correctly, or describe specific issues (spam, missing content, wrong from address, etc.)</resume-signal>
</task>

</tasks>

<verification>
- Client confirmation email delivered to inbox from bookings@rideprestigo.com
- Manager alert email delivered to inbox from bookings@rideprestigo.com
- Neither email lands in spam folder
- From address displays correctly in email client
</verification>

<success_criteria>
1. Client confirmation email is delivered to inbox, not spam, from verified domain (EMAIL-03)
2. Manager alert email is delivered to inbox, not spam, from verified domain (EMAIL-04)
3. Both emails show bookings@rideprestigo.com as sender
</success_criteria>

<output>
After completion, create `.planning/phases/09-resend-domain-verification-email-sign-off/09-02-SUMMARY.md`
</output>
