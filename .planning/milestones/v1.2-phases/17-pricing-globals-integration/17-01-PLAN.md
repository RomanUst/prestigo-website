---
phase: 17-pricing-globals-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prestigo/app/api/admin/pricing/route.ts
  - prestigo/lib/pricing-config.ts
autonomous: true
requirements: [PRICING-03, PRICING-04]

must_haves:
  truths:
    - "revalidateTag call compiles without TS error in Next.js 16.1.7"
    - "getPricingConfig() returns globals field with all 6 pricing_globals columns"
    - "All NUMERIC columns from pricing_globals are Number()-cast (not strings)"
  artifacts:
    - path: "prestigo/app/api/admin/pricing/route.ts"
      provides: "Fixed revalidateTag call with 2-arg signature"
      contains: "revalidateTag('pricing-config', 'max')"
    - path: "prestigo/lib/pricing-config.ts"
      provides: "Extended PricingRates type with PricingGlobals + dual-table query"
      exports: ["PricingRates", "PricingGlobals", "getPricingConfig"]
  key_links:
    - from: "prestigo/lib/pricing-config.ts"
      to: "pricing_globals table"
      via: "supabase.from('pricing_globals').eq('id', 1).single()"
      pattern: "pricing_globals.*eq.*1.*single"
    - from: "prestigo/app/api/admin/pricing/route.ts"
      to: "next/cache"
      via: "revalidateTag with 2 args"
      pattern: "revalidateTag\\('pricing-config',\\s*'max'\\)"
---

<objective>
Fix the Next.js 16 revalidateTag build error and extend getPricingConfig() to fetch pricing_globals alongside pricing_config under the same cache tag.

Purpose: Unblock the build (revalidateTag fix) and make globals data available for consumption by calculate-price and create-payment-intent routes in Plan 02.
Output: Working revalidateTag call + PricingRates type with globals field + getPricingConfig() returning both tables.
</objective>

<execution_context>
@/Users/romanustyugov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/romanustyugov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-pricing-globals-integration/17-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From prestigo/lib/pricing-config.ts (current — will be extended):
```typescript
export type PricingRates = {
  ratePerKm: Record<string, number>
  hourlyRate: Record<string, number>
  dailyRate: Record<string, number>
}
export const getPricingConfig: () => Promise<PricingRates>
```

From prestigo/app/api/admin/pricing/route.ts line 80 (broken call):
```typescript
revalidateTag('pricing-config')  // TS error — needs 2nd arg
```

From next/cache.d.ts (Next.js 16.1.7 — verified):
```typescript
export declare function revalidateTag(tag: string, profile: string | CacheLifeConfig): undefined;
```

pricing_globals columns (from Zod schema in admin/pricing/route.ts lines 24-31):
- airport_fee: NUMERIC (string from Supabase, needs Number() cast)
- night_coefficient: NUMERIC
- holiday_coefficient: NUMERIC
- extra_child_seat: NUMERIC
- extra_meet_greet: NUMERIC
- extra_luggage: NUMERIC
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix revalidateTag() signature in admin pricing route</name>
  <files>prestigo/app/api/admin/pricing/route.ts</files>
  <read_first>
    - prestigo/app/api/admin/pricing/route.ts (see line 80 — the broken call)
  </read_first>
  <action>
On line 80 of `prestigo/app/api/admin/pricing/route.ts`, change:

```typescript
revalidateTag('pricing-config')
```

to:

```typescript
revalidateTag('pricing-config', 'max')
```

This is the only change in this file. The `'max'` profile string is valid per `next/cache.d.ts` in Next.js 16.1.7. The second argument is mandatory in the TypeScript type — omitting it causes a compile error.

Do NOT modify any other part of this file. The GET handler, PUT handler, Zod schemas, and getAdminUser helper are all correct as-is.
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo/prestigo && npx tsc --noEmit --skipLibCheck 2>&1 | grep -c "revalidateTag" || echo "0 revalidateTag errors"</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "revalidateTag('pricing-config', 'max')" prestigo/app/api/admin/pricing/route.ts returns exactly 1 match
    - grep -c "revalidateTag('pricing-config')" prestigo/app/api/admin/pricing/route.ts returns exactly 1 (the fixed 2-arg call, not the old 1-arg call)
    - No other lines in the file are changed (diff shows only the revalidateTag line modified)
  </acceptance_criteria>
  <done>revalidateTag call uses 2-argument signature, TS compile error for this call is resolved</done>
</task>

<task type="auto">
  <name>Task 2: Extend PricingRates type and getPricingConfig() with pricing_globals</name>
  <files>prestigo/lib/pricing-config.ts</files>
  <read_first>
    - prestigo/lib/pricing-config.ts (current implementation — 30 lines)
    - prestigo/app/api/admin/pricing/route.ts (Zod schema lines 24-31 for column names)
  </read_first>
  <action>
Replace the entire `prestigo/lib/pricing-config.ts` with the extended version. Changes:

1. **Add `PricingGlobals` type** (exported):
```typescript
export type PricingGlobals = {
  airportFee: number
  nightCoefficient: number
  holidayCoefficient: number
  extraChildSeat: number
  extraMeetGreet: number
  extraLuggage: number
}
```

2. **Extend `PricingRates` type** to include `globals: PricingGlobals`:
```typescript
export type PricingRates = {
  ratePerKm: Record<string, number>
  hourlyRate: Record<string, number>
  dailyRate: Record<string, number>
  globals: PricingGlobals
}
```

3. **Extend `getPricingConfig()`** inside the same `unstable_cache` wrapper:
- Use `Promise.all` to fetch both `pricing_config` and `pricing_globals` in parallel
- Query: `supabase.from('pricing_globals').select('airport_fee, night_coefficient, holiday_coefficient, extra_child_seat, extra_meet_greet, extra_luggage').eq('id', 1).single()`
- If globalsError or !globals, throw with descriptive message (same pattern as config error)
- CRITICAL: Apply `Number()` cast to every globals field — Supabase returns NUMERIC as strings
- Return the existing ratePerKm/hourlyRate/dailyRate fields unchanged, plus `globals` field

The cache key `['pricing-config']` and tag `{ tags: ['pricing-config'] }` stay the same — both tables cached together, one revalidateTag busts both.

Full implementation matches the code example in 17-RESEARCH.md "Fix 2: Extended PricingRates type and getPricingConfig" section.
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo/prestigo && npx tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "export type PricingGlobals" prestigo/lib/pricing-config.ts returns a match
    - grep "globals: PricingGlobals" prestigo/lib/pricing-config.ts returns a match (in PricingRates type)
    - grep "pricing_globals" prestigo/lib/pricing-config.ts returns at least 1 match (the query)
    - grep "Number(globals.airport_fee)" prestigo/lib/pricing-config.ts returns a match (Number cast)
    - grep "Number(globals.night_coefficient)" prestigo/lib/pricing-config.ts returns a match
    - grep "Number(globals.extra_child_seat)" prestigo/lib/pricing-config.ts returns a match
    - grep "\.eq('id', 1)\.single()" prestigo/lib/pricing-config.ts returns a match (singleton query)
    - npx tsc --noEmit --skipLibCheck exits cleanly (no type errors from this file or consumers)
  </acceptance_criteria>
  <done>PricingRates type includes globals field with all 6 pricing_globals columns. getPricingConfig() fetches both tables under same cache tag. All NUMERIC values Number()-cast. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
1. `cd prestigo && npx tsc --noEmit --skipLibCheck` — zero errors (revalidateTag fix + type extension)
2. `cd prestigo && npm run build` — successful build (confirms both fixes work at build time)
3. `grep "revalidateTag('pricing-config', 'max')" prestigo/app/api/admin/pricing/route.ts` — confirms fix
4. `grep "globals: PricingGlobals" prestigo/lib/pricing-config.ts` — confirms type extension
</verification>

<success_criteria>
- npm run build succeeds (was failing due to revalidateTag)
- PricingRates type exported with globals: PricingGlobals field
- getPricingConfig() queries both pricing_config and pricing_globals
- All 6 globals fields Number()-cast
- Existing consumers of getPricingConfig() (calculate-price, create-payment-intent) still compile (they just ignore .globals for now)
</success_criteria>

<output>
After completion, create `.planning/phases/17-pricing-globals-integration/17-01-SUMMARY.md`
</output>
