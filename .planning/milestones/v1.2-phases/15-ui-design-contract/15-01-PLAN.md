---
phase: 15-ui-design-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/15-ui-design-contract/15-UI-SPEC.md
  - .planning/phases/15-ui-design-contract/15-VALIDATION.md
autonomous: true
requirements:
  - PRICING-01
  - PRICING-02
  - PRICING-03
  - PRICING-04
  - ZONES-01
  - ZONES-02
  - ZONES-03
  - BOOKINGS-01
  - BOOKINGS-02
  - BOOKINGS-03
  - BOOKINGS-04
  - BOOKINGS-05
  - STATS-01
  - STATS-02
  - STATS-03
  - STATS-04
  - STATS-05

must_haves:
  truths:
    - "UI-SPEC.md covers every component in the required inventory: AdminSidebar, StatusBadge, KPICard, BookingsTable, PricingForm, ZoneMap, StatsChart, FilterChips"
    - "Every requirement (PRICING-01-04, ZONES-01-03, BOOKINGS-01-05, STATS-01-05) has a traceable spec section in UI-SPEC.md"
    - "All color tokens in UI-SPEC.md match design-system/MASTER.md and STYLEGUIDE.md exactly"
    - "All PricingForm field names match the actual API response shape from /api/admin/pricing route"
    - "All BookingsTable column names match the bookings schema from migrations"
    - "UI-SPEC.md status is approved, VALIDATION.md sign-off is complete"
  artifacts:
    - path: ".planning/phases/15-ui-design-contract/15-UI-SPEC.md"
      provides: "Pixel-level design contract for all 4 admin pages"
      contains: "status: approved"
    - path: ".planning/phases/15-ui-design-contract/15-VALIDATION.md"
      provides: "Validation sign-off record"
      contains: "nyquist_compliant: true"
  key_links:
    - from: ".planning/phases/15-ui-design-contract/15-UI-SPEC.md"
      to: "design-system/MASTER.md"
      via: "Token values must match"
      pattern: "var\\(--copper\\)|var\\(--anthracite\\)|var\\(--offwhite\\)"
    - from: ".planning/phases/15-ui-design-contract/15-UI-SPEC.md"
      to: "prestigo/app/api/admin/pricing/route.ts"
      via: "PricingForm field names match API shape"
      pattern: "rate_per_km|hourly_rate|daily_rate|airport_fee|night_coefficient"
---

<objective>
Validate and finalize the UI-SPEC.md design contract so it is ready as the authoritative reference for Phase 16 admin UI implementation.

Purpose: Phase 16 will build all 4 admin pages from this spec. Any gaps, inconsistencies, or mismatches between the spec and the actual codebase (design tokens, API shapes, DB schema) will cause implementation bugs. This plan ensures the spec is airtight before any code is written.

Output: Approved UI-SPEC.md (status: approved) + completed VALIDATION.md sign-off.
</objective>

<execution_context>
@/Users/romanustyugov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/romanustyugov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-ui-design-contract/15-RESEARCH.md
@design-system/MASTER.md
@STYLEGUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-validate UI-SPEC.md against source of truth files</name>
  <files>.planning/phases/15-ui-design-contract/15-UI-SPEC.md</files>
  <read_first>
    - .planning/phases/15-ui-design-contract/15-UI-SPEC.md (the file being validated)
    - design-system/MASTER.md (authoritative color tokens, spacing, typography)
    - STYLEGUIDE.md (brand identity, tone rules)
    - .planning/REQUIREMENTS.md (requirement IDs to trace)
    - .planning/phases/15-ui-design-contract/15-RESEARCH.md (research findings to verify against)
    - prestigo/app/api/admin/pricing/route.ts (actual API field names for PricingForm)
    - prestigo/app/api/admin/bookings/route.ts (actual query params and response shape)
    - prestigo/app/api/admin/zones/route.ts (actual zone schema)
    - prestigo/components/admin/AdminSidebar.tsx (locked sidebar measurements)
    - prestigo/app/admin/(dashboard)/layout.tsx (locked layout measurements)
    - prestigo/app/admin/(dashboard)/page.tsx (locked page title pattern)
    - supabase/migrations/0001_create_bookings.sql (bookings table columns)
    - supabase/migrations/0002_create_pricing_config.sql (pricing table schema)
  </read_first>
  <action>
    Perform a systematic cross-validation of UI-SPEC.md. Check each of these dimensions and fix any issues found directly in the file:

    **1. Color token accuracy:**
    - Read design-system/MASTER.md and extract all color hex values for: --anthracite, --anthracite-mid, --anthracite-light, --copper, --copper-light, --copper-pale, --offwhite, --warmgrey
    - Compare each hex value in UI-SPEC.md Color Contract section against MASTER.md
    - If any mismatch: update UI-SPEC.md to match MASTER.md (MASTER.md is the source of truth)

    **2. Typography accuracy:**
    - Verify font family names match MASTER.md section 3
    - Verify the 4 declared sizes (11px, 13px, 28px, 32px) are the only sizes used throughout the spec
    - Search the entire UI-SPEC.md for any fontSize value not in {11, 13, 28, 32} and flag/fix

    **3. Spacing accuracy:**
    - Verify all padding/margin/gap values in new component specs (not AdminSidebar legacy) are multiples of 4
    - Verify the documented AdminSidebar legacy exceptions match actual source code values

    **4. API field name accuracy:**
    - Read prestigo/app/api/admin/pricing/route.ts — confirm PricingForm Section A field names (rate_per_km, hourly_rate, daily_rate) and vehicle class values (business, first_class, business_van)
    - Read prestigo/app/api/admin/pricing/route.ts — confirm PricingForm Section B field names (airport_fee, night_coefficient, holiday_coefficient, extra_child_seat, extra_meet_greet, extra_luggage)
    - Read prestigo/app/api/admin/bookings/route.ts — confirm query param names match FilterChips and Search input spec
    - Read prestigo/app/api/admin/zones/route.ts — confirm zone CRUD field names

    **5. BookingsTable column accuracy:**
    - Read supabase/migrations/0001_create_bookings.sql — confirm every column listed in BookingsTable primary row and expanded row exists in the actual schema
    - Verify column names are exact matches (not aliases)

    **6. Requirement traceability:**
    - For each of the 17 requirement IDs (PRICING-01 through 04, ZONES-01 through 03, BOOKINGS-01 through 05, STATS-01 through 05), verify the Validation section at the bottom of UI-SPEC.md has a row mapping that requirement to a specific spec section
    - If any requirement is missing from the Validation table, add it with the correct section reference

    **7. Anti-pattern enforcement:**
    - Search UI-SPEC.md for any occurrence of: #fff, #ffffff, bg-white, white background, border-radius: 8px, font-weight: 600, font-weight: 700 (on Cormorant), className=
    - If any found, fix or remove

    After all checks pass, or after fixing all issues found, the file should be internally consistent and aligned with every source of truth.
  </action>
  <verify>
    <automated>
      cd /Users/romanustyugov/Desktop/Prestigo && \
      echo "=== Component inventory ===" && \
      for comp in AdminSidebar StatusBadge KPICard BookingsTable PricingForm ZoneMap StatsChart FilterChips; do \
        grep -c "$comp" .planning/phases/15-ui-design-contract/15-UI-SPEC.md | xargs -I{} echo "$comp: {} mentions"; \
      done && \
      echo "=== Requirement traceability ===" && \
      for req in PRICING-01 PRICING-02 PRICING-03 PRICING-04 ZONES-01 ZONES-02 ZONES-03 BOOKINGS-01 BOOKINGS-02 BOOKINGS-03 BOOKINGS-04 BOOKINGS-05 STATS-01 STATS-02 STATS-03 STATS-04 STATS-05; do \
        grep -c "$req" .planning/phases/15-ui-design-contract/15-UI-SPEC.md | xargs -I{} echo "$req: {} mentions"; \
      done && \
      echo "=== Anti-pattern check ===" && \
      grep -in "#fff\|bg-white\|border-radius: 8px\|className=" .planning/phases/15-ui-design-contract/15-UI-SPEC.md || echo "No anti-patterns found" && \
      echo "=== Font size check ===" && \
      grep -oP 'fontSize.*?\d+px' .planning/phases/15-ui-design-contract/15-UI-SPEC.md | sort -u || echo "No inline fontSize found" && \
      grep -oP '\d+px' .planning/phases/15-ui-design-contract/15-UI-SPEC.md | sort -u | grep -vE '^(1|2|3|4|6|8|11|12|13|16|20|24|28|32|36|40|44|48|64|100|120|140|160|180|200|240|280|480|784|1024)px$' | xargs -I{} echo "UNEXPECTED SIZE: {}" || echo "All sizes expected"
    </automated>
  </verify>
  <acceptance_criteria>
    - Every component name (AdminSidebar, StatusBadge, KPICard, BookingsTable, PricingForm, ZoneMap, StatsChart, FilterChips) appears at least 3 times in UI-SPEC.md
    - Every requirement ID (PRICING-01 through PRICING-04, ZONES-01 through ZONES-03, BOOKINGS-01 through BOOKINGS-05, STATS-01 through STATS-05) appears at least once in UI-SPEC.md
    - grep for "#fff" or "bg-white" or "border-radius: 8px" or "className=" returns zero matches
    - All color hex values in UI-SPEC.md Color Contract match design-system/MASTER.md exactly
    - PricingForm field names match: rate_per_km, hourly_rate, daily_rate, airport_fee, night_coefficient, holiday_coefficient, extra_child_seat, extra_meet_greet, extra_luggage
    - BookingsTable columns match actual bookings schema columns from 0001_create_bookings.sql
  </acceptance_criteria>
  <done>UI-SPEC.md is internally consistent — all tokens match MASTER.md, all field names match API routes, all requirements have traceable spec sections, no anti-patterns present.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize UI-SPEC.md and VALIDATION.md — mark approved</name>
  <files>
    .planning/phases/15-ui-design-contract/15-UI-SPEC.md
    .planning/phases/15-ui-design-contract/15-VALIDATION.md
  </files>
  <read_first>
    - .planning/phases/15-ui-design-contract/15-UI-SPEC.md (current state after Task 1 fixes)
    - .planning/phases/15-ui-design-contract/15-VALIDATION.md (validation sign-off to update)
  </read_first>
  <action>
    **Update UI-SPEC.md frontmatter:**
    - Change `status: draft` to `status: approved`
    - Set `revised:` to today's date (2026-04-02)

    **Update UI-SPEC.md Checker Sign-Off section (bottom of file):**
    - Check all 6 dimension boxes: Copywriting, Visuals, Color, Typography, Spacing, Registry Safety
    - Change `**Approval:** pending` to `**Approval:** approved — 2026-04-02`

    **Update VALIDATION.md frontmatter:**
    - Change `status: draft` to `status: approved`
    - Change `nyquist_compliant: false` to `nyquist_compliant: true`
    - Change `wave_0_complete: false` to `wave_0_complete: true`

    **Update VALIDATION.md Per-Task Verification Map:**
    - Change all 4 task status values from `⬜ pending` to `✅ green`
    - Change all `File Exists` values from `❌ W0` to `✅`

    **Update VALIDATION.md Validation Sign-Off:**
    - Check all 6 boxes
    - Change `**Approval:** pending` to `**Approval:** approved — 2026-04-02`
  </action>
  <verify>
    <automated>
      cd /Users/romanustyugov/Desktop/Prestigo && \
      echo "=== UI-SPEC status ===" && \
      grep "status:" .planning/phases/15-ui-design-contract/15-UI-SPEC.md | head -1 && \
      echo "=== UI-SPEC approval ===" && \
      grep "Approval:" .planning/phases/15-ui-design-contract/15-UI-SPEC.md && \
      echo "=== VALIDATION status ===" && \
      grep "status:" .planning/phases/15-ui-design-contract/15-VALIDATION.md | head -1 && \
      echo "=== VALIDATION nyquist ===" && \
      grep "nyquist_compliant:" .planning/phases/15-ui-design-contract/15-VALIDATION.md && \
      echo "=== VALIDATION approval ===" && \
      grep "Approval:" .planning/phases/15-ui-design-contract/15-VALIDATION.md && \
      echo "=== Checker sign-off ===" && \
      grep -c "\[x\]" .planning/phases/15-ui-design-contract/15-UI-SPEC.md | xargs -I{} echo "Checked boxes in UI-SPEC: {}" && \
      grep -c "\[x\]" .planning/phases/15-ui-design-contract/15-VALIDATION.md | xargs -I{} echo "Checked boxes in VALIDATION: {}"
    </automated>
  </verify>
  <acceptance_criteria>
    - UI-SPEC.md frontmatter contains `status: approved`
    - UI-SPEC.md contains `**Approval:** approved`
    - VALIDATION.md frontmatter contains `status: approved`
    - VALIDATION.md frontmatter contains `nyquist_compliant: true`
    - VALIDATION.md frontmatter contains `wave_0_complete: true`
    - VALIDATION.md contains `**Approval:** approved`
    - UI-SPEC.md Checker Sign-Off has 6 `[x]` checked boxes
    - VALIDATION.md Validation Sign-Off has 6 `[x]` checked boxes
    - VALIDATION.md Per-Task Verification Map shows all 4 tasks as `✅ green`
  </acceptance_criteria>
  <done>Both UI-SPEC.md and VALIDATION.md are marked approved. Phase 15 design contract is finalized and ready for Phase 16 consumption.</done>
</task>

</tasks>

<verification>
Phase 15 is complete when:
1. UI-SPEC.md status is "approved" (not "draft")
2. All 8 components have spec sections: AdminSidebar, StatusBadge, KPICard, BookingsTable, PricingForm, ZoneMap, StatsChart, FilterChips
3. All 17 requirements (PRICING-01-04, ZONES-01-03, BOOKINGS-01-05, STATS-01-05) are traceable to spec sections
4. All token values match design-system/MASTER.md
5. All API field names match actual route handler code
6. No anti-patterns (white backgrounds, oversized border-radius, Tailwind classnames)
7. VALIDATION.md is signed off
</verification>

<success_criteria>
- `grep "status: approved" .planning/phases/15-ui-design-contract/15-UI-SPEC.md` returns a match
- `grep "nyquist_compliant: true" .planning/phases/15-ui-design-contract/15-VALIDATION.md` returns a match
- All 8 component names appear in UI-SPEC.md
- All 17 requirement IDs appear in UI-SPEC.md Validation section
- Zero anti-pattern grep matches (#fff, bg-white, border-radius: 8px, className=)
</success_criteria>

<output>
After completion, create `.planning/phases/15-ui-design-contract/15-01-SUMMARY.md`
</output>
