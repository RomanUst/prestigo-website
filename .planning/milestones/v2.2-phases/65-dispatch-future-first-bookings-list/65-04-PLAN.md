---
phase: 65-dispatch-future-first-bookings-list
plan: 04
type: execute
wave: 3
depends_on: ["65-02", "65-03"]
files_modified:
  - components/admin/BookingsTable.tsx
  - tests/BookingsTable.test.tsx
  - app/admin/(dashboard)/bookings/page.tsx
  - tests/admin-bookings-kpi-decoupling.test.tsx
autonomous: true
requirements: [DISP-02, DISP-03, DISP-04]
estimate:
  tokens: 66000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "BookingsTable holds TWO distinct state slots: the persisted default (from props, read-only after mount) and the ephemeral override driven by the segmented control; the control NEVER PATCHes /api/admin/settings and a remount resets the active segment to the persisted default, not the last-used override (DISP-03, D-04)"
    - "A Future / Past / All segmented control renders three segments; exactly one is active with copper styling (border 1px var(--copper), background rgba(191,160,106,0.09)); switching a segment changes only the horizon fetch param (E1 populated, UI-SPEC color correction)"
    - "The empty state is horizon-aware in BOTH the mobile-card and desktop-table renders: Future segment with zero rows shows 'No upcoming trips' + the body 'Every confirmed booking has passed. Switch to Past or All to see historical trips.'; Past/All shows 'No bookings found.' verbatim — the two messages read distinctly (E2 empty)"
    - "When the manual Date Range picker holds explicit values it takes session precedence (D-07): the active segment is grayed to inactive-chip style (border 1px var(--anthracite-light), color var(--warmgrey)) so the admin sees which control drives the query"
    - "E1 loading: the control stays mounted/interactive during a segment switch — no control-level spinner, no layout shift; the loading affordance is E2's existing 'Loading...' row"
    - "bookings/page.tsx fetches /api/admin/settings on mount and passes dispatch_default_horizon/dispatch_horizon_days to BookingsTable as the persisted default (DISP-02 read side)"
    - "KPI counters (TODAY count, THIS WEEK revenue) keep their own two independent date-scoped /api/admin/bookings fetches (explicit startDate/endDate, NO horizon param) and stay unchanged across every segment/horizon toggle; toggling the control triggers no additional KPI fetch (DISP-04, D-05)"
    - { statement: "On a refetch failure after a horizon/segment switch, BookingsTable renders a user-visible error distinct from the empty state (recommended copy 'Couldn't load bookings — check your connection and try again.' in the warmgrey/#f87171 error-text style) so a legitimately-empty Future view is not confused with a silent fetch failure", verification: backstop }
  artifacts:
    - components/admin/BookingsTable.tsx
    - tests/BookingsTable.test.tsx
    - app/admin/(dashboard)/bookings/page.tsx
    - tests/admin-bookings-kpi-decoupling.test.tsx
  key_links:
    - "bookings/page.tsx settings fetch → BookingsTable defaultHorizon/horizonDays props → ephemeral horizon state seed"
    - "segmented-control state → ?horizon fetch param ONLY; it must never be lifted into a value the KPI fetches read (D-05 guard test)"
---

<objective>
Complete the dispatcher UX (DISP-03) and lock the KPI decoupling guard-rail (DISP-04):
add the Future / Past / All segmented control to BookingsTable driven by two state slots
(persisted default + ephemeral override), horizon-aware empty states, the D-07 Date-Range
precedence graying, and a distinct refetch-error state; wire bookings/page.tsx to read the
persisted default and pass it down — WITHOUT touching the two independent KPI fetches.

Purpose: Turn the proven future-first data path (65-02) into the operator-facing control,
apply the persisted default (65-03) on load, and prove the KPI counters never route through
the list's horizon (D-05).
Output: segmented control + empty/error/precedence states in BookingsTable, settings-driven
default in bookings/page.tsx, and the D-05 KPI-decoupling guard test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-dispatch-future-first-bookings-list/65-CONTEXT.md
@.planning/phases/65-dispatch-future-first-bookings-list/65-UI-SPEC.md
@.planning/phases/65-dispatch-future-first-bookings-list/65-PATTERNS.md
@.planning/phases/65-dispatch-future-first-bookings-list/65-02-SUMMARY.md
@.planning/phases/65-dispatch-future-first-bookings-list/65-03-SUMMARY.md
</context>

## Artifacts this phase produces

Phase-wide artifact list is in 65-01-PLAN.md ("Artifacts this phase produces").
**This plan (65-04) produces:** the BookingsTable **Future/Past/All** segmented control +
its `defaultHorizon`/`horizonDays` props + two state slots (persisted default read-only,
ephemeral override); the horizon-aware empty states + the distinct refetch-error state; the
D-07 active-segment graying; the `/api/admin/settings` fetch + prop pass in
`app/admin/(dashboard)/bookings/page.tsx`; and tests
`tests/admin-bookings-kpi-decoupling.test.tsx` (D-05 guard) plus BookingsTable segmented/reset
assertions in `tests/BookingsTable.test.tsx`.

<tasks>

<task type="auto">
  <name>Task 1: Future/Past/All segmented control + empty/error/precedence states</name>
  <files>components/admin/BookingsTable.tsx, tests/BookingsTable.test.tsx</files>
  <read_first>
    - components/admin/BookingsTable.tsx (state block L1041-1052; fetchBookings L1228-1273 + deps; handleFilterChange L1287-1290; filter bar open L1465-1471; Date Range button + showDateFilter styling L1509-1526; existing chip styling L1596-1639; mobile empty state L1653-1663; desktop empty state L2044-2059; note the current fetchBookings only console.errors on failure L1241-1243/L1268-1269)
    - .planning/phases/65-dispatch-future-first-bookings-list/65-UI-SPEC.md (Copywriting Contract: segmented labels, empty-state copy; UI Considerations E1 + E2; Spacing 12px filter-bar gap; Color correction rgba(191,160,106,0.09))
    - .planning/phases/65-dispatch-future-first-bookings-list/65-PATTERNS.md (BookingsTable section: TWO state slots, fetch-param assembly, control must never PATCH settings)
    - .planning/phases/65-dispatch-future-first-bookings-list/65-02-SUMMARY.md (the ephemeral horizon state already added in 65-02 — extend it to be switchable)
    - tests/BookingsTable.test.tsx (existing component test harness / fetch mock)
  </read_first>
  <action>
    Add props defaultHorizon: string and horizonDays: number to BookingsTable. Keep TWO
    distinct state slots per D-03/D-04: the persisted default (initialized from the prop,
    treated read-only after mount) and the ephemeral horizon override (useState seeded from
    defaultHorizon). Convert the 65-02 read-only horizon into this switchable override state;
    the segmented control calls handleFilterChange(setHorizon) and NEVER calls PATCH
    /api/admin/settings. Render a Future / Past / All segmented control at the top of the
    filter bar (matching the 12px gap + 32px control height + existing chip button styling);
    the active segment uses border 1px var(--copper) + background rgba(191,160,106,0.09) (the
    correct current brand tint — do NOT copy the stale rgba(184,115,51,0.09) used by older
    chips), inactive segments use border 1px var(--anthracite-light) + color var(--warmgrey).
    Map the labels to horizon values: Future→'future', Past→'past', All→'all'. (Last N days
    is a persisted-default-only option — it is not a segment; if the persisted default is
    last_n_days, initialize the override to that and treat the Future segment as its nearest
    visible peer per UI-SPEC.) Make the empty state horizon-aware in BOTH the mobile-card and
    desktop-table renders: when the active horizon is 'future' and zero rows, show heading
    'No upcoming trips' and body 'Every confirmed booking has passed. Switch to Past or All to
    see historical trips.'; otherwise keep 'No bookings found.' verbatim. Implement D-07
    precedence: when showDateFilter holds explicit startDate/endDate, omit the horizon param
    from fetchBookings (so the manual Date Range wins) AND gray the active segment to the
    inactive-chip style so the admin sees the Date Range is driving the query. Add a distinct
    refetch-error state: track a fetch-failed flag in fetchBookings (set on !res.ok / catch,
    cleared on success) and, when set, render error copy 'Couldn't load bookings — check your
    connection and try again.' in the warmgrey/#f87171 error-text style, distinct from the
    empty state (so a legitimately-empty Future view is not confused with a silent failure).
    Add horizon to the fetchBookings deps. Extend tests/BookingsTable.test.tsx for the
    <acceptance_criteria> behaviors.
  </action>
  <verify>
    <automated>npx vitest run tests/BookingsTable.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Clicking a segment updates the horizon fetch param (assert the mocked fetch URL gains ?horizon=past) and issues NO PATCH to /api/admin/settings (assert no settings PATCH call)
    - Remounting BookingsTable with the same defaultHorizon prop resets the active segment to the persisted default, not the last-used override (D-04)
    - Future segment + zero rows renders 'No upcoming trips' (and the body sentence); Past/All + zero rows renders 'No bookings found.' — asserted as distinct strings, in the render path used by the test
    - With explicit startDate/endDate set, fetchBookings omits the horizon param and the active segment is grayed to the inactive style (D-07)
    - A mocked failed fetch renders the 'Couldn't load bookings — check your connection and try again.' copy, distinct from the empty-state copy
    - The active-segment style uses `rgba(191,160,106,0.09)` (grep count >= 1 in BookingsTable.tsx for the new literal) and `var(--copper)`
    - `npx vitest run tests/BookingsTable.test.tsx` passes
  </acceptance_criteria>
  <done>The dispatcher can switch Future/Past/All in-session (never persisting), sees horizon-aware empty + distinct error states, and the manual Date Range visibly takes precedence — all proven by component tests.</done>
</task>

<task type="auto">
  <name>Task 2: Settings-driven default in bookings/page.tsx + KPI decoupling guard (D-05)</name>
  <files>app/admin/(dashboard)/bookings/page.tsx, tests/admin-bookings-kpi-decoupling.test.tsx</files>
  <read_first>
    - app/admin/(dashboard)/bookings/page.tsx (whole file: it is 'use client'; the two KPI fetches in the mount useEffect L36-61 use explicit startDate/endDate + limit and NO horizon — these must stay byte-for-byte; <BookingsTable key={refreshKey} /> mount L121)
    - components/admin/BookingsTable.tsx (the new defaultHorizon/horizonDays props from Task 1)
    - app/api/admin/settings/route.ts (GET now returns dispatch_default_horizon/dispatch_horizon_days — from 65-03)
    - .planning/phases/65-dispatch-future-first-bookings-list/65-RESEARCH.md (System Architecture Diagram — KPI box "UNCHANGED, no horizon param ever"; Validation Architecture row 7, the concrete D-05 guard)
    - .planning/phases/65-dispatch-future-first-bookings-list/65-VALIDATION.md (KPI decoupling invariant)
  </read_first>
  <behavior>
    - On mount, bookings/page.tsx fetches /api/admin/settings once and passes dispatch_default_horizon → defaultHorizon and dispatch_horizon_days → horizonDays into <BookingsTable>
    - The two KPI fetches keep their explicit startDate/endDate + limit and never gain a horizon param
    - Toggling the segmented control from 'future' to 'past' issues exactly ONE additional /api/admin/bookings LIST fetch and ZERO additional fetches to the two KPI URL patterns (startDate=endDate=today&limit=1 and the monday..sunday&limit=100 pattern); todayCount and weekRevenue values are unchanged after the toggle
  </behavior>
  <action>
    In bookings/page.tsx add a mount fetch to /api/admin/settings (a THIRD, separate fetch —
    NOT one of the two KPI /api/admin/bookings fetches) to read dispatch_default_horizon and
    dispatch_horizon_days into state (fallback 'future' / 7 if the fetch fails), and pass them
    as defaultHorizon/horizonDays props to <BookingsTable>. Do NOT modify the two KPI fetches
    (they must keep explicit startDate/endDate + limit and no horizon param) and do NOT route
    the segmented-control state into anything the KPI fetches read (D-05). Create
    tests/admin-bookings-kpi-decoupling.test.tsx: render BookingsPage with fetch mocked,
    count calls to the two KPI URL patterns, toggle the horizon segment, and assert the KPI
    call-count to those two patterns stays exactly 2 across the toggle and that todayCount /
    weekRevenue render values are unchanged — the concrete DISP-04 guard.
  </action>
  <verify>
    <automated>npx vitest run tests/admin-bookings-kpi-decoupling.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - bookings/page.tsx fetches /api/admin/settings on mount and passes defaultHorizon/horizonDays to <BookingsTable>
    - The two KPI fetches are unchanged: each still uses explicit startDate/endDate + limit and no `horizon` query param (assert the KPI fetch URLs contain no 'horizon=')
    - tests/admin-bookings-kpi-decoupling.test.tsx asserts the KPI-pattern fetch count stays exactly 2 across a Future→Past segment toggle, and todayCount/weekRevenue values do not change
    - `npx vitest run tests/admin-bookings-kpi-decoupling.test.tsx` passes
  </acceptance_criteria>
  <done>The bookings page applies the persisted default on load, and a passing guard test proves the KPI counters never route through the list horizon (D-05/DISP-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (BookingsTable) → GET /api/admin/bookings | Segmented-control horizon value crosses here (already whitelisted server-side in 65-02) |
| bookings/page.tsx → GET /api/admin/settings | Reads the persisted default (behind the admin guard) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-65-02 | Spoofing/Elevation | client fetches to admin APIs | high | mitigate | Both /api/admin/bookings and /api/admin/settings enforce getAdminUser() 401/403 server-side (65-02/65-03); the client control adds no privileged path — horizon is re-whitelisted server-side regardless of what the client sends |
| T-65-08 | Information Disclosure | KPI vs list coupling | medium | mitigate | D-05 guard test asserts KPI totals never route through the list horizon, so a wrong/hostile horizon cannot skew the reported revenue/today counters |
| T-65-04 | Tampering | segmented-control horizon value | low | mitigate | Even a forged client horizon is bounded by the server KNOWN_HORIZONS whitelist (65-02); worst case is "no override" |
| T-65-SC | Tampering | package installs | low | accept | No packages installed this plan |
</threat_model>

<verification>
- `npx vitest run tests/BookingsTable.test.tsx` green (segmented control, reset-on-remount, empty/error/precedence)
- `npx vitest run tests/admin-bookings-kpi-decoupling.test.tsx` green (D-05 guard)
- `npx tsc --noEmit` passes (props threaded through page → table)
- `npx vitest run` full suite green
- Manual/UAT: default load shows future-first; switch to Past then reload → resets to saved default; KPI numbers identical while toggling
</verification>

<success_criteria>
- In-session Future/Past/All override works and never mutates the persisted default (DISP-03, D-04)
- Persisted default applies on every load (DISP-02 read side)
- Horizon-aware empty states + distinct refetch error + D-07 precedence graying (UI-SPEC E1/E2, D-07)
- KPI counters stay accurate and decoupled across every filter/horizon (DISP-04, D-05) — proven by the guard test
</success_criteria>

<output>
Create `.planning/phases/65-dispatch-future-first-bookings-list/65-04-SUMMARY.md` when done
</output>
