---
phase: 59-booking-flow-redesign-blacklane
plan: 04
type: execute
wave: 2
depends_on: ["59-02", "59-03"]
files_modified:
  - components/booking/RouteMap.tsx
  - components/booking/StickyBookingPanel.tsx
  - tests/RouteMap.test.tsx
  - tests/StickyBookingPanel.test.tsx
autonomous: true
requirements: [BOOK-04, TRACK-02, TRACK-05]
must_haves:
  truths:
    - "An animated Google Maps route renders with pickup and drop-off time labels at the vehicle step (D-11, D-12)"
    - "The map plays a one-shot moving-dot animation on load and respects prefers-reduced-motion (D-11)"
    - "The sticky right panel shows the map, selected class, price, 'All fees included', and a Select CTA (D-09)"
    - "Clicking Select fires begin_checkout (GA4) and InitiateCheckout (Meta) then advances the wizard"
    - "The map reuses the existing NEXT_PUBLIC_GOOGLE_MAPS_API_KEY via the js-api-loader singleton; the places library is not lost; no new inline scripts (D-13)"
  artifacts:
    - path: "components/booking/RouteMap.tsx"
      provides: "Google Maps JS SDK route map with animated dot + time labels"
      min_lines: 80
    - path: "components/booking/StickyBookingPanel.tsx"
      provides: "Sticky desktop panel: map + summary + Select CTA"
      min_lines: 60
  key_links:
    - from: "components/booking/RouteMap.tsx"
      to: "@googlemaps/js-api-loader"
      via: "importLibrary singleton"
      pattern: "importLibrary\\('maps'\\)"
    - from: "components/booking/StickyBookingPanel.tsx"
      to: "components/MetaPixel"
      via: "trackMetaEvent('InitiateCheckout')"
      pattern: "InitiateCheckout"
    - from: "components/booking/StickyBookingPanel.tsx"
      to: "components/booking/RouteMap.tsx"
      via: "import default"
      pattern: "from '@/components/booking/RouteMap'"
---

<objective>
Build the animated route map (Google Maps JS SDK) and the sticky right-hand booking panel that hosts it. RouteMap reuses the existing `@googlemaps/js-api-loader` singleton and the existing `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` already used for Places Autocomplete (D-13 — no new API credentials), draws a polyline from origin to destination, animates a copper dot once along the path (3s, reduced-motion aware), and shows pickup/drop-off time labels (D-11, D-12). StickyBookingPanel composes the map + selected-class summary + price + "All fees included" note + the "SELECT [CLASS]" CTA, and on click fires `begin_checkout` (GA4) and `InitiateCheckout` (Meta) — the trigger point relocated out of BookingWizard in plan 59-03 — then advances the wizard (D-09).

Purpose: BOOK-04 delivers the route map with times; TRACK-02 preserves the Meta InitiateCheckout + eventId dedup by re-homing the trigger to the CTA; TRACK-05 keeps CSP intact by reusing the already-trusted loader (no new script origins).
Output: RouteMap.tsx, StickyBookingPanel.tsx, GREEN RouteMap + StickyBookingPanel tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-booking-flow-redesign-blacklane/59-CONTEXT.md
@.planning/phases/59-booking-flow-redesign-blacklane/59-RESEARCH.md
@.planning/phases/59-booking-flow-redesign-blacklane/59-PATTERNS.md
@.planning/phases/59-booking-flow-redesign-blacklane/59-UI-SPEC.md
@components/booking/AddressInput.tsx
</context>

## Artifacts this phase produces

New symbols/files: `RouteMap` (default export, components/booking/RouteMap.tsx); `StickyBookingPanel` (default export, components/booking/StickyBookingPanel.tsx); module-local `ensureMapsLibraryLoaded`, `animateDotAlongPath`, `GREYSCALE_STYLES` in RouteMap.tsx; `handleSelectClass`, `VEHICLE_LABELS` in StickyBookingPanel.tsx.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build RouteMap.tsx (Google Maps JS SDK, animated dot, time labels)</name>
  <files>components/booking/RouteMap.tsx, tests/RouteMap.test.tsx</files>
  <behavior>
    - Given origin + destination, renders a map container with aria-label including "Route map" (D-11)
    - Given origin or destination null, renders empty state "Route unavailable — you can still select a vehicle class."
    - Loads Maps via the js-api-loader singleton with libraries ['maps','places','routes'], reusing NEXT_PUBLIC_GOOGLE_MAPS_API_KEY (D-13 — no new credentials)
    - Animates a copper dot once (3s) along the polyline; cancels the RAF on unmount (D-11)
    - Reduced-motion: dot placed at midpoint, no animation
    - Shows pickup time + drop-off time labels on the markers (D-12)
  </behavior>
  <read_first>
    - components/booking/AddressInput.tsx (existing singleton: setOptions({ libraries: ['places'] }), importLibrary('places'), NEXT_PUBLIC_GOOGLE_MAPS_API_KEY)
    - .planning/phases/59-booking-flow-redesign-blacklane/59-RESEARCH.md (Pattern 1 ensureMapsLibraryLoaded; GREYSCALE_STYLES; animateDotAlongPath; Pitfall 2 setOptions libraries; Pitfall 6 RAF cleanup; Open Q2 routes library)
    - .planning/phases/59-booking-flow-redesign-blacklane/59-PATTERNS.md (RouteMap pattern block: loader singleton, useEffect cleanup, RAF animation, return JSX)
    - .planning/phases/59-booking-flow-redesign-blacklane/59-UI-SPEC.md (Animated Route Map Contract: 220px height, one-shot 3s, reduced-motion midpoint, time-label style, empty-state copy)
    - .planning/phases/59-booking-flow-redesign-blacklane/59-CONTEXT.md (D-11, D-12, D-13 map decisions)
    - types/booking.ts (PlaceResult lat/lng shape, distanceKm usage for drop-off estimate)
    - tests/RouteMap.test.tsx (the RED contract; Google Maps is mocked there)
  </read_first>
  <action>
    Create components/booking/RouteMap.tsx as a 'use client' component. Props: origin: PlaceResult | null, destination: PlaceResult | null, pickupTime: string | null (24h HH:MM), and optional distanceKm for a drop-off estimate. Implement a module-level `ensureMapsLibraryLoaded()` singleton mirroring AddressInput's pattern: reuse the SAME existing key `process.env.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` (D-13 — do NOT introduce a new env var or credential) and call setOptions with `libraries: ['maps','places','routes']` (Pitfall 2 — must keep 'places' so AddressInput autocomplete keeps working; add 'routes' per Open Q2), then await importLibrary('maps'). All window.google access lives inside a useEffect (Pitfall 1 — never during SSR). In the effect: if origin or destination is null, render nothing on the map and show the empty-state div (UI-SPEC copy). Otherwise create a new google.maps.Map with GREYSCALE_STYLES and disableDefaultUI:true, draw the route with google.maps.DirectionsService (fall back to a straight-line polyline between origin/destination LatLng if Directions fails), fit bounds to the path. Animate a copper circle marker once along the decoded path over 3000ms with animateDotAlongPath (D-11 one-shot); store the RAF handle and cancel it in the effect cleanup (Pitfall 6). Check window.matchMedia('(prefers-reduced-motion: reduce)').matches once: if true, place the dot at the path midpoint and skip animation. Add pickup-time and drop-off-time labels (InfoWindow or custom overlay) styled per UI-SPEC (11px Montserrat, warmgrey bg) — D-12. Convert pickupTime (24h) to a display label; drop-off time = pickup + estimated duration if computable, else a static "+duration" placeholder. Container div: ref, height 220, aria-label `Route map from {origin.address} to {destination.address}`, role="img". Use only var(--...) tokens for any inline color. Update tests/RouteMap.test.tsx only to correct fixture values if needed; do not weaken assertions.
  </action>
  <verify>
    <automated>npx vitest run tests/RouteMap.test.tsx 2>&1 | tail -5 | grep -qiE "passed|✓" && grep -q "importLibrary('maps')" components/booking/RouteMap.tsx && grep -q "'maps'" components/booking/RouteMap.tsx && grep -q "'places'" components/booking/RouteMap.tsx && grep -q "NEXT_PUBLIC_GOOGLE_MAPS_API_KEY" components/booking/RouteMap.tsx && grep -q "cancelAnimationFrame" components/booking/RouteMap.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - components/booking/RouteMap.tsx exists, starts with 'use client', all google.maps access is inside useEffect
    - setOptions libraries array contains both 'maps' and 'places' (and 'routes')
    - Reuses the existing key: `grep -q "NEXT_PUBLIC_GOOGLE_MAPS_API_KEY" components/booking/RouteMap.tsx` succeeds and no new *_MAPS_* env var is introduced (D-13)
    - Contains `cancelAnimationFrame` in an effect cleanup (RAF leak prevention)
    - Reads `prefers-reduced-motion` and skips animation when set
    - `npx vitest run tests/RouteMap.test.tsx` passes (GREEN)
  </acceptance_criteria>
  <done>RouteMap renders an animated, reduced-motion-aware route with time labels, reuses the existing Maps key + loader singleton without dropping places (D-13), and its tests are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build StickyBookingPanel.tsx (map + summary + Select CTA with relocated analytics)</name>
  <files>components/booking/StickyBookingPanel.tsx, tests/StickyBookingPanel.test.tsx</files>
  <behavior>
    - Renders RouteMap, selected class name, price, "All fees included" note, and a Select CTA (D-09)
    - CTA label is "SELECT A CLASS" when none selected, "SELECT [CLASS]" when selected
    - Clicking Select fires begin_checkout (gtag) and InitiateCheckout (trackMetaEvent) with value/currency, then calls nextStep()
    - Hidden on mobile (desktop sticky panel only, D-10)
  </behavior>
  <read_first>
    - components/booking/BookingWizard.tsx (the begin_checkout + trackMetaEvent('InitiateCheckout') push that plan 59-03 removed — replicate its value/currency/items computation: VEHICLE_LABELS, computeExtrasTotal, promoDiscount math, items array)
    - lib/extras.ts (computeExtrasTotal signature)
    - components/MetaPixel.tsx (trackMetaEvent export — InitiateCheckout payload shape { value, currency, num_items })
    - .planning/phases/59-booking-flow-redesign-blacklane/59-PATTERNS.md (StickyBookingPanel pattern block: imports, handleSelectClass GA4+Meta, GA4 push helper, sticky layout D-09/D-10)
    - .planning/phases/59-booking-flow-redesign-blacklane/59-UI-SPEC.md (Right sticky panel contract: 320px, anthracite-mid bg, contents order, CTA copy, accessibility aria-label includes price)
    - lib/booking-store.ts (vehicleClass, priceBreakdown, extras, promoDiscount, origin, destination, pickupTime, distanceKm, nextStep)
    - types/booking.ts (PriceBreakdown shape, VehicleClass)
    - tests/StickyBookingPanel.test.tsx (the RED contract — RouteMap + MetaPixel are mocked there)
  </read_first>
  <action>
    Create components/booking/StickyBookingPanel.tsx as a 'use client' component. Class: hidden md:block sticky, top:24, width 320, background var(--anthracite-mid), 1px var(--anthracite-light) border, 24px padding (D-09, D-10 — hidden on mobile). Read vehicleClass, priceBreakdown, extras, promoDiscount, origin, destination, pickupTime, distanceKm from useBookingStore. Render in order: RouteMap (passing origin/destination/pickupTime/distanceKm) -> divider -> selected class name (11px uppercase) -> price (20px copper, weight 600) -> "All fees included" note (11px warmgrey) -> full-width btn-primary CTA. CTA text: "SELECT A CLASS" if vehicleClass is null, else "SELECT " + VEHICLE_LABELS[vehicleClass] uppercased. CTA aria-label includes the price when available (e.g. "Select Business class — €84"). handleSelectClass: read fresh state via useBookingStore.getState(); compute totalEur exactly as BookingWizard did (selectedPrice.base + computeExtrasTotal(extras), apply promoDiscount), currency from selectedPrice?.currency ?? 'EUR', build the items array; push 'begin_checkout' via the GA4 helper (gtag-or-dataLayer pattern from PATTERNS.md) and call trackMetaEvent('InitiateCheckout', { value: totalEur, currency, num_items: 1 }); then call useBookingStore.getState().nextStep(). If vehicleClass is null, the CTA is disabled (no analytics fired). Use only var(--...) tokens. Update tests/StickyBookingPanel.test.tsx only to correct fixtures if necessary; do not weaken assertions.
  </action>
  <verify>
    <automated>npx vitest run tests/StickyBookingPanel.test.tsx 2>&1 | tail -5 | grep -qiE "passed|✓" && grep -q "InitiateCheckout" components/booking/StickyBookingPanel.tsx && grep -q "begin_checkout" components/booking/StickyBookingPanel.tsx && grep -q "from '@/components/booking/RouteMap'" components/booking/StickyBookingPanel.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - components/booking/StickyBookingPanel.tsx exists, starts with 'use client', is `hidden md:block`
    - handleSelectClass fires both `begin_checkout` (gtag) and `InitiateCheckout` (trackMetaEvent), then nextStep()
    - CTA disabled / no analytics when vehicleClass is null
    - Imports RouteMap and renders it
    - `npx vitest run tests/StickyBookingPanel.test.tsx` passes (GREEN)
  </acceptance_criteria>
  <done>Sticky panel renders map + summary + CTA; Select fires begin_checkout + InitiateCheckout then advances; tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Google Maps JS SDK | Map scripts loaded dynamically into the page |
| Browser → GA4 / Meta Pixel | Analytics events emitted from the CTA handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-07 | Tampering | CSP / dynamic script load | mitigate | RouteMap reuses the @googlemaps/js-api-loader already authorized via nonce + strict-dynamic; no new script origin or inline script added (TRACK-05) |
| T-59-08 | Information Disclosure | NEXT_PUBLIC_GOOGLE_MAPS_API_KEY | accept | Key is intentionally public, restricted by HTTP referrer in Google Cloud Console; no server key exposed; reused not newly added (D-13, RESEARCH security domain) |
| T-59-09 | Denial of Service | RAF animation loop | mitigate | RAF handle cancelled on unmount (Pitfall 6); animation is one-shot (3s), not an infinite loop |
</threat_model>

<verification>
- RouteMap renders animated map + time labels; empty state on missing route; RAF cleaned up; reuses existing Maps key (D-13)
- Loader singleton keeps 'places' so AddressInput autocomplete is unaffected
- StickyBookingPanel CTA fires begin_checkout + InitiateCheckout then advances
- Both new test files GREEN; no new inline scripts
</verification>

<success_criteria>
- BOOK-04 (D-11/D-12): animated route map with pickup + drop-off time labels at the vehicle step
- TRACK-02: InitiateCheckout (Meta) fires from the CTA with eventId dedup preserved (trackMetaEvent unchanged)
- TRACK-05 (D-13): CSP intact — existing loader + key reused, no new inline scripts or credentials
</success_criteria>

<output>
Create `.planning/phases/59-booking-flow-redesign-blacklane/59-04-SUMMARY.md` when done
</output>
