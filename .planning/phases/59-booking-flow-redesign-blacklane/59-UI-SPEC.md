---
phase: 59
slug: booking-flow-redesign-blacklane
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-17
---

# Phase 59 — UI Design Contract: Booking Flow Redesign (Blacklane)

> Visual and interaction contract for the Entry Bar (Step1+Step2 unified) and Vehicle Selection screen (Step3 redesign).
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property         | Value                                                                 |
|------------------|-----------------------------------------------------------------------|
| Tool             | none (manual — project-specific CSS vars + Tailwind 4 utilities)     |
| Preset           | not applicable                                                        |
| Component library| none (custom components only — no Radix, no shadcn)                   |
| Icon library     | lucide-react (already installed; used in Step1TripType.tsx)           |
| Font — display   | Cormorant Garamond (`var(--font-display)` / `var(--font-cormorant)`) |
| Font — body/UI   | Montserrat (`var(--font-body)` / `var(--font-montserrat)`)           |

> Source: globals.css `@theme` block + components/booking/BookingWidget.tsx

---

## Theme

**Light theme throughout** (D-20). The booking flow uses `.theme-light` token overrides from globals.css, which inverts the dark anthracite palette to ivory/warm-white. All color values below are the **light-theme resolved values**, not the dark marketing defaults.

| Token (light)       | Resolved hex | Role in this phase                            |
|---------------------|--------------|-----------------------------------------------|
| `--anthracite`      | `#F7F4EF`    | Page background, card surfaces                |
| `--anthracite-mid`  | `#EFEAE2`    | Input field backgrounds, dropdown backgrounds |
| `--anthracite-light`| `#DDD7CE`    | Borders, dividers, skeleton bars              |
| `--offwhite`        | `#211F1C`    | Primary text (headings, labels, body)         |
| `--warmgrey`        | `#6E6962`    | Secondary text, placeholder text, icons       |
| `--copper`          | `#8A5A2E`    | Accent (see reserved list below)              |
| `--copper-light`    | `#8A5A2E`    | Required-field asterisk, active state borders (intentionally same as --copper in light theme — globals.css `.theme-light` sets both to #8A5A2E; the dark theme values differ) |
| `--copper-pale`     | `#9A6A3A`    | Discount badge, subtle accents (lighter than --copper/#8A5A2E in light theme) |

Implementation note: wrap the Entry Bar and Step3 container in `<div className="theme-light">` to inherit all inverted tokens automatically. Do not hardcode hex values inline — use `var(--anthracite)` etc. so dark/light inversion works.

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage in this phase                                         |
|-------|-------|-------------------------------------------------------------|
| xs    | 4px   | Icon-to-label gap (pax/luggage icons on cards), inline gap  |
| sm    | 8px   | Tab gap in TripTypeTabs, label margin-bottom                |
| md    | 16px  | Field-to-field gap within Entry Bar, card internal padding  |
| lg    | 24px  | Section gap in Entry Bar, card-to-card gap (mobile)         |
| xl    | 32px  | Left-panel / right-panel gap, section top margin            |
| 2xl   | 48px  | Above/below Entry Bar section                               |
| 3xl   | 64px  | Page-level vertical rhythm (not used inside the wizard)     |

**Exceptions:**
- Touch targets (tabs, time slots, CTA buttons): min-height 44px (WCAG 2.5.5 / Apple HIG). Already established in TripTypeTabs.tsx and btn-primary class.
- Card exterior image: aspect-ratio 3/2 (width × 0.667 height) — wider than standard 16/9 to show the car at full flank without cropping the hood/trunk.
- Sticky right panel: fixed width 320px on desktop (matches existing Step3Vehicle.tsx `gridTemplateColumns: '1fr 320px'`).
- Map area within sticky panel: height 220px on desktop, hidden on mobile (D-10).

---

## Typography

All text uses `var(--font-montserrat)` (Montserrat) unless specified as display/Cormorant.

| Role              | Font               | Size | Weight | Line Height | Letter Spacing | Usage                                                                                   |
|-------------------|--------------------|------|--------|-------------|----------------|-----------------------------------------------------------------------------------------|
| Label/Meta        | Montserrat         | 11px | 400    | 1.4         | 0.25–0.35em    | Field labels (PICKUP, DATE, TIME), tab text, icon captions, CZK conversion, discount badge, map time labels |
| Body              | Montserrat         | 14px | 400    | 1.75        | 0.03em         | Card description text, "What's included" items, secondary notes                        |
| Heading/Price     | Montserrat         | 20px | 600    | 1.2         | 0.03em–0.06em  | Card class name (weight 400 to distinguish from price), section titles; primary price display (weight 600) |
| Display           | Cormorant Garamond | 28px | 600    | 1.1         | 0.03em         | Section headline above Entry Bar if present                                             |

**Weight usage rules:**
- `font-weight: 400` — default weight for all body, label/meta, and heading (class name) text
- `font-weight: 600` — reserved for price values and display headline only; makes numeric figures stand out clearly from surrounding text

> Source: globals.css `.label` (11px/0.28em), `.body-text` (14px), `.display` (Cormorant), VehicleCard.tsx price, globals.css `.hourly-strip-price` (28px display).
> Consolidated from 6 sizes to 4. 12px (Meta/Caption) merged into 11px (Label/Meta). 16px (Heading) merged into 20px (Heading/Price) — weight 400 vs 600 provides visual distinction within that size.

---

## Color

60/30/10 split — light theme values:

| Role            | Value     | Usage                                                                         |
|-----------------|-----------|-------------------------------------------------------------------------------|
| Dominant (60%)  | `#F7F4EF` | Page background, Entry Bar surface, card backgrounds                         |
| Secondary (30%) | `#EFEAE2` | Input field backgrounds, sticky panel background, time-slot dropdown bg       |
| Accent (10%)    | `#8A5A2E` | (see reserved list below)                                                     |
| Destructive     | `#C0392B` | Form validation errors on required fields (already used in BookingWidget.tsx) |

**Accent (`#8A5A2E` = light-theme copper) is reserved ONLY for:**
1. Selected vehicle card border (2px solid)
2. Active trip-type tab: background fill and text
3. CTA button "Посмотреть варианты" — copper fill, offwhite text
4. CTA button "Select [Class]" in sticky panel — copper fill, offwhite text
5. Time-slot selected-item left border (4px) and active highlight
6. Required field asterisk (`*`)
7. Discount badge background (rgba copper at 12% opacity)
8. Map marker accent ring / dot color for the animated moving point
9. focus-visible outline (2px solid copper, 4px offset — from globals.css)

Accent is NOT used on: inactive tabs, unselected cards, placeholder text, dividers, secondary labels.

---

## Layout Contract

### Entry Bar (replaces Step1 + Step2)

Single-screen form on `/book` page, rendered above the fold.

**Structure (top to bottom):**
1. TripTypeTabs — "Transfer" | "Hourly" tabs. "Transfer" active by default. No "Multi-day" tab in the redesigned bar (not in scope per D-02). Pills with 999px border-radius, 44px min-height, 8px/20px padding (top/bottom 8px, left/right 20px — both multiples of 4).
2. Unified field grid — responsive:
   - Desktop: 4-column grid — [From] [To] [Date] [Time] + conditional [Flight №]
   - Mobile: single column, fields stacked vertically with 16px gap
3. Conditional "Flight number" field: appears inline in the row when `isAirportPlace(origin)` is true (D-06). Positioned after the Time field in the desktop grid row (5th column, flexible width). On mobile: beneath Time field.
4. "Add return" checkbox row: below the field grid. When checked, return [Date] and [Time] fields expand with 300ms ease transition (max-height animation or simple conditional render with step-enter animation).
5. CTA button "ПОСМОТРЕТЬ ВАРИАНТЫ": full-width, copper fill, 44px min-height, 0.35em letter-spacing, uppercase Montserrat 11px.

**Hourly mode (D-02):** "To" field is replaced by DurationSelector (existing component, keep). "Add return" checkbox is hidden.

**Validation:** Required fields (From, To/Duration, Date, Time) — on submit, invalid fields get red border (`#C0392B`, 1px). Single live-region `<p role="alert">` below the fields: "Please fill in all required fields before continuing." (same pattern as BookingWidget.tsx).

### Vehicle Selection Screen (Step3 redesign)

**Desktop layout:** CSS Grid `gridTemplateColumns: '1fr 320px'`, gap 32px.

**Left panel:** 3 vehicle class cards in a column (stacked vertically — NOT a 3-column grid). Each card is full-width within the left column. Cards are separated by 16px gap.

> Rationale: Blacklane reference shows 3 horizontal cards in a row, but Prestigo's 320px right panel leaves ~calc(100% - 352px) for cards. At typical 1280px viewport this is ~928px → 3 cards at ~288px each. The executor may implement either 3-column card grid OR vertical stack depending on breakpoint. This spec mandates: **desktop = 3 cards in a horizontal row** (matching Blacklane reference), **mobile = vertical single-column**. Card min-width: 200px.

**Right sticky panel (desktop only, hidden on mobile per D-10):**
- `position: sticky`, `top: 24px`
- Width: 320px
- Background: `var(--anthracite-mid)` (#EFEAE2 in light theme)
- Border: 1px solid `var(--anthracite-light)`
- Padding: 24px
- **Contents (top to bottom):**
  1. Animated route map — 220px height, Google Maps JS SDK, polyline + animated dot
  2. Divider line (1px, `var(--anthracite-light)`)
  3. Selected class name — 11px Montserrat weight 400, 0.25em letter-spacing, uppercase
  4. Price — 20px Montserrat weight 600, copper color
  5. "All fees included" note — 11px Montserrat weight 400, warmgrey
  6. CTA "SELECT [CLASS]" — full-width, copper fill, 44px min-height

**Vehicle card structure (each card):**
1. Exterior photo — aspect-ratio 3/2, `object-fit: cover`, border-radius 4px. Source: `/public/vehicles/{class}-exterior.jpg` (Higgsfield AI, D-14/D-17).
2. Class name — 11px Montserrat weight 400, 0.4em letter-spacing, uppercase, dark text
3. Capacity row — pax icon + count, luggage icon + count, 16px gap, 16px icon size, warmgrey
4. Price display — 20px Montserrat weight 600, dark text (one-way EUR; CZK conversion below at 11px)
5. "What's included" list — always visible (not accordion). 6 items, 14px Montserrat weight 400, warmgrey, 16px icon per item, 8px gap.
6. Selection state — selected: 2px copper border, `rgba(138, 90, 46, 0.08)` background fill; default: 1px `var(--anthracite-light)` border.

**"What's included" items (D-16, exact copy):**
1. Fixed price guaranteed (icon: tag/checkmark)
2. Phone charging (icon: lightning bolt)
3. 60 min free waiting (icon: clock)
4. Wi-Fi (icon: wifi)
5. Water on board (icon: droplet)
6. Meet & greet (icon: hand-wave / person) — shown for airport transfers only, greyed-out or hidden for non-airport

**Interior slideshow block** (D-15): below the 3 cards, full-width. Auto-plays with 4-second interval. Manual prev/next arrows (copper, 32px hit area). Pause on hover. Source images: `/public/vehicles/{class}-interior-{n}.jpg`. 3–5 slides per class. Shown for the currently selected class only. If no class selected: shows Business class slides.

---

## Animated Route Map Contract (D-11, D-12)

**Library:** Google Maps JS SDK (loaded via existing `NEXT_PUBLIC_GOOGLE_MAPS_KEY`). No Static Maps API.

**Initialization:** Map loads when Step3 mounts. Polyline drawn from origin `LatLng` to destination `LatLng` using `google.maps.DirectionsService` or a simple straight-line polyline if Directions fails (fallback).

**Animation:** A circle marker (`radius: 6px`, fill: copper `#8A5A2E`) moves along the polyline. Animation plays once on load (not looping) — one pass from pickup to drop-off in 3 seconds. Respects `prefers-reduced-motion`: if reduced-motion, marker is placed at midpoint of polyline, no animation.

**Map labels (D-12):** Two `google.maps.InfoWindow` or custom overlay `<div>` elements:
- At origin marker: pickup time (e.g., "10:30 AM"), 11px Montserrat, warmgrey bg with 4px padding
- At destination marker: drop-off time (estimated from API `distanceKm` or a static "+duration" label), same style

**Map style:** Minimal/greyscale map tiles using `google.maps.MapTypeId.ROADMAP` with a custom JSON style array (greyscale, hide POI labels, hide transit). Background `#F7F4EF` matching light theme.

**Empty/error state:** If route cannot be computed (no origin/destination in store): show a placeholder `<div>` with warmgrey background `#EFEAE2` and centered text "Route unavailable — you can still select a vehicle class." (11px Montserrat weight 400, warmgrey). Same 220px height.

---

## Copywriting Contract

| Element                              | Copy                                                                        |
|--------------------------------------|-----------------------------------------------------------------------------|
| Entry Bar CTA (primary)             | ПОСМОТРЕТЬ ВАРИАНТЫ                                                         |
| Entry Bar CTA (Hourly mode)         | ПОСМОТРЕТЬ ВАРИАНТЫ (same — the tab makes context clear)                    |
| Tab: Transfer                        | TRANSFER                                                                     |
| Tab: Hourly                          | HOURLY                                                                       |
| Field label: From                    | PICKUP LOCATION                                                              |
| Field label: To                      | DESTINATION                                                                  |
| Field label: Date                    | DATE                                                                         |
| Field label: Time                    | TIME                                                                         |
| Field label: Flight number           | FLIGHT NUMBER (optional)                                                     |
| Flight field placeholder             | e.g. BA 256                                                                  |
| "Add return" checkbox label          | Add return journey                                                           |
| Return Date label                    | RETURN DATE                                                                  |
| Return Time label                    | RETURN TIME                                                                  |
| Entry Bar validation error          | Please fill in all required fields before continuing.                        |
| Step3 section heading                | Choose your experience                                                       |
| Vehicle classes                      | Business (E-Class W213) / First Class (S-Class W223) / Business Van (V-Class W447) |
| "What's included" section label      | What's included                                                              |
| Sticky panel price note             | All fees included                                                            |
| Sticky panel CTA (no selection)     | SELECT A CLASS                                                               |
| Sticky panel CTA (class selected)   | SELECT [CLASS NAME] — e.g. "SELECT BUSINESS"                                |
| Map empty state                      | Route unavailable — you can still select a vehicle class.                   |
| Slideshow caption                    | [Class name] interior (e.g. "Business interior")                            |
| Pricing fetch error                  | Pricing unavailable. Your selection has been saved — continue to request a quote. |
| Price loading state                  | skeleton-shimmer (existing class, no text)                                   |
| Return time ordering error          | Return must be after pickup                                                  |

**Destructive actions in this phase:** none. No confirm dialogs required.

---

## Interaction States

### Vehicle Card

| State    | Visual                                                    |
|----------|-----------------------------------------------------------|
| Default  | 1px border `var(--anthracite-light)`, bg `var(--anthracite-mid)` |
| Hover    | border-color transitions to `rgba(138,90,46,0.5)`, bg lightens subtly |
| Selected | 2px border `var(--copper)` (#8A5A2E light-theme), bg `rgba(138,90,46,0.08)` |
| Loading  | skeleton-shimmer on price area (existing class)           |

Transition: `border-color 0.2s ease, background-color 0.2s ease` (existing pattern from VehicleCard.tsx)

### Trip Type Tabs

| State    | Visual                                                    |
|----------|-----------------------------------------------------------|
| Inactive | border `var(--anthracite-light)`, text warmgrey, bg transparent |
| Hover    | text transitions to `var(--offwhite)` (light: `#211F1C`) |
| Active   | border + bg `var(--offwhite)` (#211F1C border, #211F1C bg), text `var(--anthracite)` (#F7F4EF) |

> Note: In dark theme offwhite is `#F5F2EE`; in light theme the token inverts. TripTypeTabs already uses `var(--offwhite)` so the inversion is automatic.

### Time Slot List

| State    | Visual                                                              |
|----------|---------------------------------------------------------------------|
| Default  | text warmgrey, bg transparent, left border 3px transparent          |
| Selected | text dark (`var(--offwhite)`), bg `var(--anthracite-mid)`, left border 3px copper |
| Hover    | text transitions to dark                                            |

Min-height per slot: 44px (touch target).

### CTA Buttons

| State  | Visual                                                              |
|--------|---------------------------------------------------------------------|
| Default| copper bg, offwhite text (white in light context), 1px copper border |
| Hover  | bg lightens slightly (copper-light), scale 1.0                     |
| Active | `transform: scale(0.97)` (0.15s ease-out) — from btn-primary class |
| Focus  | 2px copper outline, 4px offset — from globals.css `:focus-visible` |

### Input Fields

| State     | Visual                                                         |
|-----------|----------------------------------------------------------------|
| Default   | bg `var(--anthracite-mid)`, border 1px `var(--anthracite-light)` |
| Focus     | No border-color change (existing pattern); focus-visible outline |
| Error     | border 1px `#C0392B` (red)                                     |
| Filled    | text `var(--offwhite)` (#211F1C in light)                      |
| Placeholder | text `var(--warmgrey)` (#6E6962 in light)                   |

---

## Accessibility Contract

- All interactive elements min 44×44px touch target (WCAG 2.5.5)
- `role="tablist"` / `role="tab"` / `aria-selected` on TripTypeTabs (existing)
- Time-slot list: `role="listbox"` / `role="option"` / `aria-selected` (existing pattern)
- Vehicle cards: `<button>` elements with `aria-pressed` state (existing pattern)
- Validation: `<p role="alert" aria-live="polite">` for field errors
- Map container: `aria-label="Route map from [origin] to [destination]"` on the map div; `role="img"` on the animated overlay; static time labels in accessible text (not only visual)
- Image alt text: `"Prestigo [Class] — [Model] exterior"` for vehicle photos, `"[Class] interior view [n]"` for slideshow
- Sticky panel CTA: `aria-label` includes price when available — e.g. `"Select Business class — €84"`
- `prefers-reduced-motion`: map animation disabled, slideshow auto-play disabled, step-enter animation skipped (existing `.animate-step-enter` already respects this via globals.css)
- VehicleSlideshow.tsx prev/next navigation arrows: `aria-label="Previous slide"` on the left arrow button, `aria-label="Next slide"` on the right arrow button

---

## Animation Contract

| Animation               | Duration | Easing                       | Trigger                              | Reduced Motion        |
|-------------------------|----------|------------------------------|--------------------------------------|-----------------------|
| Step enter (page)       | 300ms    | cubic-bezier(0.22, 1, 0.36, 1) | Step component mounts              | disabled (opacity 1)  |
| Map dot travel          | 3000ms   | linear                       | Step3 mounts, once (not looping)     | disabled (dot at midpoint) |
| "Add return" expand     | 300ms    | ease                         | Checkbox checked                     | instant show/hide     |
| Card selection          | 200ms    | ease                         | Card clicked                         | instant               |
| Tab switch              | 200ms    | ease                         | Tab clicked                          | instant               |
| Slideshow auto-advance  | 4000ms   | ease-in-out (cross-fade)     | Auto on load                         | disabled (show first slide only) |
| Skeleton shimmer        | 1200ms   | linear (infinite)            | Price loading                        | disabled (static fill) |

---

## Image Asset Contract (D-14, D-15, D-17)

Images are generated by Higgsfield AI in a Phase 59 sub-task and committed to `/public/vehicles/` before component work begins.

| File path                                | Dimensions | Description                                               |
|------------------------------------------|------------|-----------------------------------------------------------|
| `/public/vehicles/business-exterior.jpg` | 900×600px  | E-Class W213, neutral/studio bg, 3/4 front angle         |
| `/public/vehicles/first-exterior.jpg`   | 900×600px  | S-Class W223, neutral/studio bg, 3/4 front angle         |
| `/public/vehicles/van-exterior.jpg`     | 900×600px  | V-Class W447, neutral/studio bg, 3/4 front angle         |
| `/public/vehicles/business-int-1.jpg`  | 1200×800px | Business cabin interior, front-looking perspective        |
| `/public/vehicles/business-int-2.jpg`  | 1200×800px | Business rear seat detail, leather + ambient lighting     |
| `/public/vehicles/business-int-3.jpg`  | 1200×800px | Business luggage boot, organized luggage visual           |
| `/public/vehicles/first-int-1.jpg`     | 1200×800px | First Class rear seat panorama                            |
| `/public/vehicles/first-int-2.jpg`     | 1200×800px | First Class center console / champagne detail             |
| `/public/vehicles/first-int-3.jpg`     | 1200×800px | First Class boot with luxury luggage                      |
| `/public/vehicles/van-int-1.jpg`       | 1200×800px | V-Class cabin, 6-seat layout                              |
| `/public/vehicles/van-int-2.jpg`       | 1200×800px | V-Class luggage capacity (loaded boot)                    |
| `/public/vehicles/van-int-3.jpg`       | 1200×800px | V-Class individual seat detail                            |

All images: 2025+ model year, photorealistic, no city backdrop, clean neutral/studio background. JPEG quality ≥90. Use `next/image` with `sizes` prop for responsive delivery.

---

## Component Inventory (new and modified)

| Component                              | Status      | Notes                                                    |
|----------------------------------------|-------------|----------------------------------------------------------|
| `components/booking/EntryBar.tsx`     | NEW         | Replaces Step1TripType + Step2DateTime logic             |
| `components/booking/TripTypeTabs.tsx` | MODIFY      | Add light-theme compatibility (tokens already invert via `.theme-light`); remove Multi-Day tab from entry bar context if desired |
| `components/booking/steps/Step3Vehicle.tsx` | MODIFY | New layout: left cards column + right sticky panel       |
| `components/booking/VehicleCard.tsx`  | MODIFY      | New visual shell: exterior photo 3/2, "What's included" list, light-theme card bg |
| `components/booking/RouteMap.tsx`     | NEW         | Google Maps JS SDK wrapper, animated dot, time labels    |
| `components/booking/VehicleSlideshow.tsx` | NEW    | Interior + luggage photo slideshow with auto-play        |
| `components/booking/StickyBookingPanel.tsx` | NEW  | Right sticky panel: map + summary + CTA                  |
| `components/booking/BookingWizard.tsx`| MODIFY      | ProgressBar step renumbering, EntryBar plug-in at step 1 |
| `components/booking/ProgressBar.tsx`  | MODIFY      | Steps 1+2 merged to 1 unified step                       |

---

## Analytics Event Placement (D-18, D-19)

| Event              | Trigger moment                                   | Component                  |
|--------------------|--------------------------------------------------|----------------------------|
| `form_start`       | EntryBar mounts (useEffect once)                 | EntryBar.tsx               |
| `checkout_progress`| User submits Entry Bar → advances to Step3       | EntryBar.tsx handleSubmit  |
| `view_item_list`   | Step3 mounts (after price fetch completes)       | Step3Vehicle.tsx           |
| `view_item`        | Individual VehicleCard becomes visible (IntersectionObserver or on Step3 mount) | Step3Vehicle.tsx |
| `begin_checkout`   | User clicks "Select [Class]" in sticky panel     | StickyBookingPanel.tsx     |
| `select_item`      | User clicks any VehicleCard                      | Step3Vehicle.tsx (existing pushVehicleSelect) |

Meta Pixel `InitiateCheckout` fires at the same moment as `begin_checkout` (existing pattern in BookingWizard.tsx — preserve the trigger point).

---

## Registry Safety

| Registry       | Blocks Used | Safety Gate                  |
|----------------|-------------|------------------------------|
| shadcn official| none        | not applicable (no shadcn)   |
| third-party    | none        | not applicable               |

No third-party component registries used. All components are custom-built with Tailwind utilities and project CSS vars.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

*Phase: 59-booking-flow-redesign-blacklane*
*UI-SPEC created: 2026-06-17*
*Decisions sourced: CONTEXT.md (D-01..D-20), globals.css, BookingWidget.tsx, VehicleCard.tsx, Step3Vehicle.tsx, TripTypeTabs.tsx, Step1TripType.tsx*
