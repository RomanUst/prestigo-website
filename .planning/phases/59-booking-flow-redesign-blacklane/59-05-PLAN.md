---
phase: 59-booking-flow-redesign-blacklane
plan: 05
type: execute
wave: 3
depends_on: ["59-01", "59-02", "59-04"]
files_modified:
  - components/booking/VehicleCard.tsx
  - components/booking/VehicleSlideshow.tsx
  - components/booking/steps/Step3Vehicle.tsx
  - tests/VehicleSlideshow.test.tsx
  - tests/VehicleCard.test.tsx
  - tests/Step3Vehicle.test.tsx
autonomous: true
requirements: [BOOK-04, BOOK-05, TRACK-01]
must_haves:
  truths:
    - "Each vehicle card shows the new exterior photo (3/2), capacity icons, price, and the 6-item 'What's included' list"
    - "An auto-playing interior/luggage slideshow appears below the cards for the selected class"
    - "The vehicle step uses the two-column layout: cards (+slideshow) on the left, StickyBookingPanel on the right (desktop); single column on mobile with no map"
    - "view_item_list / view_item / select_item analytics still fire on the redesigned vehicle step"
  artifacts:
    - path: "components/booking/VehicleCard.tsx"
      provides: "Redesigned card shell: 3/2 photo + What's included list"
      contains: "Фиксированная цена"
    - path: "components/booking/VehicleSlideshow.tsx"
      provides: "Interior/luggage auto-play slideshow"
      min_lines: 50
    - path: "components/booking/steps/Step3Vehicle.tsx"
      provides: "Two-column vehicle step with StickyBookingPanel + slideshow"
      contains: "StickyBookingPanel"
  key_links:
    - from: "components/booking/steps/Step3Vehicle.tsx"
      to: "components/booking/StickyBookingPanel.tsx"
      via: "import default"
      pattern: "from '@/components/booking/StickyBookingPanel'"
    - from: "components/booking/steps/Step3Vehicle.tsx"
      to: "components/booking/VehicleSlideshow.tsx"
      via: "import default"
      pattern: "from '@/components/booking/VehicleSlideshow'"
    - from: "components/booking/VehicleCard.tsx"
      to: "public/vehicles"
      via: "config.image via next/image"
      pattern: "config.image"
---

<objective>
Finish the vehicle-selection redesign: rebuild the VehicleCard visual shell (3/2 exterior photo + always-visible 6-item "What's included" list, light theme), add the auto-playing interior/luggage VehicleSlideshow below the cards, and restructure Step3Vehicle into the two-column Blacklane layout (cards + slideshow left, StickyBookingPanel right on desktop; single column, no map, on mobile). All existing price logic, the fetchPrice flow, the Business auto-select, the round-trip block, and the view_item_list / view_item / select_item analytics are preserved (D-18, TRACK-01).

Purpose: BOOK-05 delivers the richer cards + capacity + What's-included; BOOK-04 completes by mounting the map-bearing StickyBookingPanel; TRACK-01 keeps the vehicle-step funnel events firing.
Output: redesigned VehicleCard, new VehicleSlideshow, restructured Step3Vehicle, GREEN VehicleSlideshow + VehicleCard + Step3Vehicle tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-booking-flow-redesign-blacklane/59-CONTEXT.md
@.planning/phases/59-booking-flow-redesign-blacklane/59-RESEARCH.md
@.planning/phases/59-booking-flow-redesign-blacklane/59-PATTERNS.md
@.planning/phases/59-booking-flow-redesign-blacklane/59-UI-SPEC.md
@components/booking/VehicleCard.tsx
@components/booking/steps/Step3Vehicle.tsx
@.planning/phases/59-booking-flow-redesign-blacklane/59-04-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols/files: `VehicleSlideshow` (default export, components/booking/VehicleSlideshow.tsx); module-local `INCLUDED_ITEMS`, `SLIDES_BY_CLASS` (slideshow image manifest). Modified symbols: `VehicleCard` (visual shell — 3/2 photo, What's-included list; props interface UNCHANGED); `Step3Vehicle` layout (left column + StickyBookingPanel; PriceSummary desktop block replaced).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build VehicleSlideshow.tsx (interior/luggage auto-play)</name>
  <files>components/booking/VehicleSlideshow.tsx, tests/VehicleSlideshow.test.tsx</files>
  <behavior>
    - Renders the first interior slide for the active class
    - Auto-advances every 4000ms (cross-fade), cycling through that class's slides
    - prev/next buttons with aria-label "Previous slide" / "Next slide"
    - Pauses auto-play on hover; respects prefers-reduced-motion (no auto-play, first slide only)
    - Defaults to Business slides when no class is selected
  </behavior>
  <read_first>
    - components/booking/VehicleCard.tsx (next/image fill pattern, onError display:none, useState/useEffect imports)
    - .planning/phases/59-booking-flow-redesign-blacklane/59-PATTERNS.md (VehicleSlideshow pattern block: setInterval auto-play + cleanup, image render pattern)
    - .planning/phases/59-booking-flow-redesign-blacklane/59-UI-SPEC.md (Interior slideshow block contract: 4s interval, prev/next 32px copper, pause on hover, image paths /public/vehicles/{class}-int-{n}.jpg, caption "[Class] interior", aria-labels)
    - tests/VehicleSlideshow.test.tsx (the RED contract — fake timers, 4000ms advance, prev/next)
    - types/booking.ts (VehicleClass union for the active-class prop)
  </read_first>
  <action>
    Create components/booking/VehicleSlideshow.tsx as a 'use client' component. Prop: activeClass: VehicleClass | null. Define a SLIDES_BY_CLASS manifest mapping each class to its 3 interior image paths from plan 59-01: business -> ['/vehicles/business-int-1.jpg','/vehicles/business-int-2.jpg','/vehicles/business-int-3.jpg']; first_class -> ['/vehicles/first-int-1.jpg', ...-2, ...-3]; business_van -> ['/vehicles/van-int-1.jpg', ...-2, ...-3]. The active slide set is SLIDES_BY_CLASS[activeClass ?? 'business']. State activeIndex; auto-advance with setInterval(4000ms) using the cleanup pattern from PATTERNS.md (clear on unmount and when slides change). Pause on hover (clear interval on mouseenter, restart on mouseleave). Check prefers-reduced-motion once: if set, do not auto-play (show first slide only). Render the active image with next/image fill (aspect 3/2) and the onError display:none guard, plus prev/next buttons (aria-label "Previous slide" / "Next slide", copper, 32px hit area) that decrement/increment activeIndex modulo slides.length. Caption: `{ClassLabel} interior`. Use only var(--...) tokens. Update tests/VehicleSlideshow.test.tsx only to correct fixtures if needed; do not weaken assertions.
  </action>
  <verify>
    <automated>npx vitest run tests/VehicleSlideshow.test.tsx 2>&1 | tail -5 | grep -qiE "passed|✓" && grep -q "Previous slide" components/booking/VehicleSlideshow.tsx && grep -q "/vehicles/business-int-1.jpg" components/booking/VehicleSlideshow.tsx && grep -q "clearInterval" components/booking/VehicleSlideshow.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - components/booking/VehicleSlideshow.tsx exists, starts with 'use client'
    - SLIDES_BY_CLASS references the 9 interior image paths from plan 59-01
    - Auto-play via setInterval(...,4000) with clearInterval cleanup; pause on hover; reduced-motion guard
    - prev/next buttons carry aria-label "Previous slide" / "Next slide"
    - `npx vitest run tests/VehicleSlideshow.test.tsx` passes (GREEN)
  </acceptance_criteria>
  <done>VehicleSlideshow auto-plays interior/luggage slides per class with accessible controls; tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Redesign VehicleCard.tsx shell (3/2 photo + What's included)</name>
  <files>components/booking/VehicleCard.tsx, tests/VehicleCard.test.tsx</files>
  <behavior>
    - Exterior photo renders at aspect-ratio 3/2 with alt "Prestigo [Class] — exterior"
    - All 6 "What's included" items render on every card (always visible, not accordion)
    - One Way / Round Trip price buttons and CZK conversion are unchanged
    - Selection state (2px copper border, tinted bg) preserved; props interface unchanged
  </behavior>
  <read_first>
    - components/booking/VehicleCard.tsx (full current file: props interface lines 8-20, selection/bg lines 38-67, photo block line 70, capacity row 96-112, price buttons 114-199 — KEEP these)
    - .planning/phases/59-booking-flow-redesign-blacklane/59-PATTERNS.md (VehicleCard modify block: aspect 16/9 -> 3/2, INCLUDED_ITEMS array, capacity/price kept)
    - .planning/phases/59-booking-flow-redesign-blacklane/59-UI-SPEC.md (Vehicle card structure; "What's included" items D-16 exact copy + icons; alt text; selection visuals)
    - .planning/phases/59-booking-flow-redesign-blacklane/59-CONTEXT.md (D-16 exact 6-item Russian copy)
    - tests/Step3Vehicle.test.tsx (existing VehicleCard tests — One Way/Round Trip button assertions that must still pass)
    - tests/VehicleCard.test.tsx (if it exists; otherwise this task adds What's-included assertions to the relevant existing card test file)
  </read_first>
  <action>
    Modify components/booking/VehicleCard.tsx WITHOUT changing the VehicleCardProps interface or the price-button logic. Change the exterior photo wrapper aspect-ratio from '16/9' to '3/2' (D-14) and set the Image alt to `Prestigo ${config.label} — exterior`. Add an always-visible "What's included" list after the capacity row and before the price buttons, using the exact 6 Russian items from D-16: 'Фиксированная цена', 'Зарядка для телефонов', '60 минут бесплатного ожидания', 'Wi-Fi', 'Вода на борту', 'Meet & greet' — each as an <li> with a lucide-react Check icon (size 12, color var(--copper), aria-hidden), 14px Montserrat warmgrey text, per the PATTERNS.md INCLUDED_ITEMS block. Keep the selection border/bg logic, the capacity icon row, the One Way / Round Trip buttons, the quoteMode branch, and the skeleton-shimmer loading state exactly as-is. Use only var(--...) tokens (the card sits inside the theme-light wrapper). If tests/VehicleCard.test.tsx does not exist, add the What's-included assertions to the existing card-focused test file (tests/Step3Vehicle.test.tsx currently imports VehicleCard) — assert all 6 items render and the photo alt contains the class label; do not weaken the existing One Way/Round Trip assertions.
  </action>
  <verify>
    <automated>npx vitest run tests/Step3Vehicle.test.tsx 2>&1 | tail -5 | grep -qiE "passed|✓" && grep -q "aspectRatio: '3/2'" components/booking/VehicleCard.tsx && grep -q "Фиксированная цена" components/booking/VehicleCard.tsx && grep -c "Meet & greet" components/booking/VehicleCard.tsx | grep -qv 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - VehicleCard.tsx photo wrapper uses aspectRatio: '3/2'
    - All 6 D-16 items present: `grep -c "Фиксированная цена\|Зарядка для телефонов\|60 минут бесплатного ожидания\|Wi-Fi\|Вода на борту\|Meet & greet" components/booking/VehicleCard.tsx` returns 6
    - VehicleCardProps interface unchanged (same 11 props)
    - Image alt contains the class label
    - One Way / Round Trip price-button tests still pass (GREEN)
  </acceptance_criteria>
  <done>VehicleCard shows the new 3/2 photo and the 6-item What's-included list with price logic intact; card tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Restructure Step3Vehicle two-column layout + slideshow + StickyBookingPanel</name>
  <files>components/booking/steps/Step3Vehicle.tsx, tests/Step3Vehicle.test.tsx</files>
  <read_first>
    - components/booking/steps/Step3Vehicle.tsx (full current file: fetchPrice useCallback lines 99-141, Business auto-select 154-158, return-time re-fetch 161-167, cards map 193-222, desktop grid 232-238 with PriceSummary, mobile grid 242-244, round-trip block 250-346, mobile PriceSummary line 349)
    - components/booking/StickyBookingPanel.tsx (default export from plan 59-04 — replaces the desktop PriceSummary block)
    - components/booking/VehicleSlideshow.tsx (default export from Task 1 — mounts below the cards)
    - components/booking/PriceSummary.tsx (mobile bar — keep the mobileOnly instance for the mobile CTA path)
    - .planning/phases/59-booking-flow-redesign-blacklane/59-PATTERNS.md (Step3Vehicle modify block: new 2-col layout, theme-light, replace PriceSummary with StickyBookingPanel, add VehicleSlideshow, preserve fetchPrice + select_item)
    - .planning/phases/59-booking-flow-redesign-blacklane/59-UI-SPEC.md (Vehicle Selection Screen layout: 1fr 320px grid gap 32, cards row desktop, mobile single column no map, section heading "Choose your experience")
    - tests/Step3Vehicle.test.tsx (assertions to keep + the new layout assertion this task adds)
  </read_first>
  <action>
    Modify components/booking/steps/Step3Vehicle.tsx. Preserve ALL existing logic unchanged: the fetchPrice useCallback and its mount useEffect, the Business auto-select effect, the return-time re-fetch effect, the cards array (VehicleCard mapping with onSelectOneWay/onSelectRoundTrip calling pushVehicleSelect), the round-trip return date/time block, and the fetchError message. Wrap the rendered output's outer container in className="theme-light" (D-20). Replace the desktop two-column block: keep `gridTemplateColumns: '1fr 320px', gap: 32`, but on the LEFT put a wrapper div containing the three cards (desktop horizontal row per UI-SPEC) followed by <VehicleSlideshow activeClass={vehicleClass} /> below them; on the RIGHT render <StickyBookingPanel /> instead of <PriceSummary desktopOnly />. Keep the mobile single-column cards grid and the mobile <PriceSummary mobileOnly /> bar exactly as-is (no map on mobile, D-10). Add the section heading "Choose your experience" above the grid (per UI-SPEC copy). Import StickyBookingPanel and VehicleSlideshow; remove the desktop PriceSummary import usage only if PriceSummary is no longer used on desktop (keep the mobileOnly instance + import). Do NOT touch the analytics (view_item_list/view_item live in BookingWizard at step 2 post-renumber; select_item stays in pushVehicleSelect here). Update tests/Step3Vehicle.test.tsx: add an assertion that StickyBookingPanel and VehicleSlideshow mount in the desktop layout (mock both with stubs to avoid Google Maps), and keep all existing card/price assertions GREEN.
  </action>
  <verify>
    <automated>npx vitest run tests/Step3Vehicle.test.tsx 2>&1 | tail -5 | grep -qiE "passed|✓" && grep -q "from '@/components/booking/StickyBookingPanel'" components/booking/steps/Step3Vehicle.tsx && grep -q "from '@/components/booking/VehicleSlideshow'" components/booking/steps/Step3Vehicle.tsx && grep -q "Choose your experience" components/booking/steps/Step3Vehicle.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Step3Vehicle.tsx imports and renders StickyBookingPanel (desktop right) and VehicleSlideshow (left, below cards)
    - The fetchPrice useCallback, Business auto-select, and round-trip block are unchanged (`grep -q "calculate-price" components/booking/steps/Step3Vehicle.tsx`)
    - pushVehicleSelect / select_item still present
    - Section heading "Choose your experience" present
    - Mobile keeps single-column cards + PriceSummary mobileOnly, no map
    - `npx vitest run tests/Step3Vehicle.test.tsx` passes (GREEN)
  </acceptance_criteria>
  <done>Vehicle step uses the two-column Blacklane layout with slideshow + sticky panel; pricing/analytics logic preserved; tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN/static → Browser | Vehicle images served from /public/vehicles via next/image |
| Browser → /api/calculate-price | Price fetch on vehicle-step mount (unchanged logic) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-10 | Tampering | next/image src paths | accept | Image paths are static string literals in a manifest; no user input flows into the src (no path traversal) |
| T-59-11 | Denial of Service | slideshow setInterval | mitigate | Interval cleared on unmount and on slide change; paused on hover; disabled under reduced-motion |
| T-59-12 | Information Disclosure | vehicle images | accept | Public marketing assets; no PII |
</threat_model>

<verification>
- Cards show 3/2 photo + 6-item What's-included; slideshow auto-plays per class
- Step3Vehicle two-column layout mounts StickyBookingPanel + VehicleSlideshow on desktop; mobile single column, no map
- fetchPrice / Business auto-select / round-trip block / select_item preserved
- VehicleSlideshow + VehicleCard + Step3Vehicle tests GREEN
</verification>

<success_criteria>
- BOOK-05: cards show capacity + "What's included" (6 items) + new exterior photo; interior slideshow present
- BOOK-04: StickyBookingPanel (with RouteMap) mounted on the vehicle step (desktop)
- TRACK-01: view_item_list / view_item / select_item still fire on the redesigned vehicle step
</success_criteria>

<output>
Create `.planning/phases/59-booking-flow-redesign-blacklane/59-05-SUMMARY.md` when done
</output>
