---
phase: 69-string-externalization-ui-chrome
plan: 05
type: execute
wave: 5
depends_on: [69-04]
files_modified:
  - components/Testimonials.tsx
  - components/TestimonialsCarousel.tsx
  - components/CookieBanner.tsx
  - messages/en.json
  - messages/ru.json
  - messages/es.json
  - messages/fr.json
  - messages/ar.json
  - messages/hi.json
  - messages/zh.json
autonomous: true
requirements: [STR-01]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Testimonials (async server, getTranslations) and TestimonialsCarousel (client, useTranslations) render all chrome labels + aria-labels through the shared Testimonials namespace; user-generated review content (quote/name/role/sourceLabel) is NOT externalized (STR-01)"
    - "CookieBanner renders every visible string, tab label, category/service row, button, and sr-only toggle label through useTranslations('CookieBanner'); the Privacy Policy / Legal Notice inline links render via t.rich as localized Link, byte-identical incl. the ' · ' separator"
    - "Third-party service provider names (Stripe, Supabase, Google Maps, Google Analytics 4, Meta Pixel) stay proper nouns in the component; the consent storage/gtag/CustomEvent logic is untouched"
    - "EN output byte-for-byte unchanged; Testimonials + CookieBanner namespaces present in all 7 locale files; /ru build prerenders both with no MISSING_MESSAGE"
    - "This wave completes STR-01: no hardcoded user-facing UI-chrome copy remains in any of the phase's target components"
  artifacts:
    - "messages/en.json Testimonials + CookieBanner namespaces synced to 6 stubs (final catalog)"
  key_links:
    - "Testimonials getTranslations('Testimonials') (async) + TestimonialsCarousel useTranslations('Testimonials') (client) share one namespace"
    - "CookieBanner t.rich('consentBody', { privacy, terms }) -> Link from @/i18n/routing"
---

<objective>
Externalize the final two component clusters: Testimonials (+ its Carousel) and the CookieBanner. This is the last wave — it closes STR-01 for UI chrome.

Purpose: Exercise the two remaining next-intl shapes not yet proven — `getTranslations` in an already-async Server Component (Testimonials), and `t.rich` with embedded localized links inside a client component (CookieBanner) — while leaving user-generated review content and the consent-mechanics logic untouched.
Output: Testimonials + TestimonialsCarousel + CookieBanner consuming their namespaces; the EN catalog is complete; 6 stubs re-synced to the final catalog.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-string-externalization-ui-chrome/69-RESEARCH.md
@.planning/phases/69-string-externalization-ui-chrome/69-VALIDATION.md
@.planning/phases/69-string-externalization-ui-chrome/69-01-SUMMARY.md
</context>

<artifacts>
## Artifacts this plan produces
- `messages/en.json` gains `Testimonials` (label, heading lines, body, carousel aria/label keys) and `CookieBanner` (heading, rich consentBody, tabs, category names+descriptions, service descriptions, buttons, toggle labels) namespaces. Synced to 6 stubs — the completed EN catalog for the phase.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Externalize Testimonials (getTranslations) + TestimonialsCarousel (useTranslations)</name>
  <files>components/Testimonials.tsx, components/TestimonialsCarousel.tsx, messages/en.json, messages/ru.json, messages/es.json, messages/fr.json, messages/ar.json, messages/hi.json, messages/zh.json</files>
  <read_first>
    - components/Testimonials.tsx (current — async Server Component that awaits getReviews(); label "Testimonials", heading "Trusted by those"/"who value their time.", privacy body)
    - components/TestimonialsCarousel.tsx (current — 'use client'; StarBadge "{stars} out of 5 stars" aria + "Google Review"; region aria "Testimonials"; "Testimonial pagination"; "Go to slide {n}"; review data quote/name/role/sourceLabel is user content)
    - messages/en.json (convention + prior namespaces)
    - .planning/phases/69-string-externalization-ui-chrome/69-RESEARCH.md (Pattern 4 — async Server Component MUST use getTranslations, not the hook, after an await)
  </read_first>
  <action>
    1. Add a `Testimonials` namespace: `label` ("Testimonials"), `headingLine1` ("Trusted by those"), `headingLine2` ("who value their time."), `body` ("Published with permission. Names abbreviated to protect passenger privacy — part of our discretion policy."), and the carousel chrome: `starsAria` ("{stars} out of 5 stars"), `googleReview` ("Google Review"), `carouselAria` ("Testimonials"), `paginationAria` ("Testimonial pagination"), `goToSlide` ("Go to slide {n}").
    2. In Testimonials.tsx use `const t = await getTranslations('Testimonials')` (from 'next-intl/server') — it is already async (awaits getReviews()), so it MUST use getTranslations, not useTranslations, after the await boundary. Replace the label, the two heading lines (keep `<br />` + display-italic span), and the privacy body.
    3. In TestimonialsCarousel.tsx add `const t = useTranslations('Testimonials')`. Replace: StarBadge `aria-label={t('starsAria', { stars })}` and the "Google Review" label with `t('googleReview')`; the region `aria-label={t('carouselAria')}`; the pagination `aria-label={t('paginationAria')}`; each dot `aria-label={t('goToSlide', { n: i + 1 })}`. Do NOT touch `review.quote`/`name`/`role`/`sourceLabel`/`relativeTime` — that is user-generated review content, out of scope.
    4. Re-sync the 6 stub files from en.json.
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('components/Testimonials.tsx','utf8'); if(!/getTranslations\('Testimonials'\)/.test(f)){console.error('Testimonials must use getTranslations (async)');process.exit(1)}" && npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "getTranslations('Testimonials')" components/Testimonials.tsx` matches (async server); `grep -n "useTranslations('Testimonials')" components/TestimonialsCarousel.tsx` matches (client).
    - Review content stays hardcoded: `review.quote`/`review.name`/`review.role` references remain in TestimonialsCarousel.tsx (not externalized).
    - `messages/en.json` `Testimonials.starsAria` contains `{stars}`, `Testimonials.goToSlide` contains `{n}`; all 6 stubs byte-identical (`cmp -s` exits 0).
    - `npx vitest run` exits 0 and `npm run build` exits 0.
  </acceptance_criteria>
  <done>Testimonials uses getTranslations (async) and the Carousel uses useTranslations for all chrome labels; review content untouched; EN byte-identical; stubs synced; suite + build green.</done>
</task>

<task type="auto">
  <name>Task 2: Externalize CookieBanner (t.rich links + full consent copy)</name>
  <files>components/CookieBanner.tsx, messages/en.json, messages/ru.json, messages/es.json, messages/fr.json, messages/ar.json, messages/hi.json, messages/zh.json</files>
  <read_first>
    - components/CookieBanner.tsx (current — 'use client'; wordmark aria "PRESTIGO"; "Welcome aboard." heading; consent paragraph with inline Privacy Policy / Legal Notice <a> links + " · " separator; tabs "Categories"/"Services"; CategoryRow x3 Essential/Analytics/Marketing; ServiceRow x5 Stripe/Supabase/Google Maps/Google Analytics 4/Meta Pixel; buttons "Accept all"/"Save settings"/"More information"; Toggle sr-only "Enabled"/"Disabled")
    - messages/en.json (convention + prior namespaces)
    - i18n/routing.ts (Link export)
    - .planning/phases/69-string-externalization-ui-chrome/69-RESEARCH.md (Pattern 5 — t.rich for the embedded Privacy/Legal links; Pitfall 5 — do NOT touch consent storage/gtag logic)
  </read_first>
  <action>
    1. Add a `CookieBanner` namespace: `wordmarkAria` ("PRESTIGO"), `heading` ("Welcome aboard."), `consentBody` (rich string: "Before your journey begins, a quick word on privacy. We use third-party technologies to run the booking flow and refine the experience. You may revoke or change your choice at any time. <privacy>Privacy Policy</privacy> · <terms>Legal Notice</terms>" — verbatim, keeping the ' · ' separator inside the message), `tabCategories` ("Categories"), `tabServices` ("Services"), `catEssential`/`catAnalytics`/`catMarketing` (the category NAMES, reused by both CategoryRow titles and ServiceRow badges), `catEssentialDesc`/`catAnalyticsDesc`/`catMarketingDesc` (the 3 CategoryRow descriptions verbatim), `svcStripeDesc`/`svcSupabaseDesc`/`svcMapsDesc`/`svcGa4Desc`/`svcMetaDesc` (the 5 ServiceRow descriptions verbatim), `acceptAll` ("Accept all"), `saveSettings` ("Save settings"), `moreInfo` ("More information"), `toggleEnabled` ("Enabled"), `toggleDisabled` ("Disabled").
    2. Add `const t = useTranslations('CookieBanner')`. Render the consent paragraph with `t.rich('consentBody', { privacy: (chunks) => <Link href="/privacy" className="text-copper-light hover:text-copper underline underline-offset-2">{chunks}</Link>, terms: (chunks) => <Link href="/terms" className="text-copper-light hover:text-copper underline underline-offset-2">{chunks}</Link> })` — import `Link` from `@/i18n/routing`, replacing the two raw anchors (the Privacy Policy link to the privacy route and the Legal Notice link to the terms route), preserving the two classNames verbatim. The ' · ' separator lives inside the message, so the surrounding `{' · '}` JSX is removed.
    3. Replace all remaining hardcoded strings with `t(...)`: wordmark aria, heading, tab labels, the 3 CategoryRow title/description pairs (pass `catEssential`+`catEssentialDesc` etc. as props), the 5 ServiceRow `category` badges (from `catEssential`/`catAnalytics`/`catMarketing`) and `description` props (the svc*Desc keys), the 3 footer buttons, and the Toggle `sr-only` label (`checked ? t('toggleEnabled') : t('toggleDisabled')`).
    4. KEEP structural/untouched: the 5 service provider TITLE names (Stripe, Supabase, Google Maps, Google Analytics 4, Meta Pixel — proper nouns) stay hardcoded in the component; the entire consent-storage / gtag consent-update / `prestigo:consent-granted` CustomEvent / localStorage-migration logic is unchanged.
    5. Re-sync the 6 stub files from en.json.
  </action>
  <verify>
    <automated>npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "useTranslations('CookieBanner')" components/CookieBanner.tsx` matches; `grep -n "t.rich('consentBody'" components/CookieBanner.tsx` matches; `grep -n "from '@/i18n/routing'" components/CookieBanner.tsx` matches (Link imported).
    - The Privacy/Legal links are localized `Link`s inside t.rich, no raw internal anchor remains: `grep -cE '<a[[:space:]]+href="/' components/CookieBanner.tsx` returns 0.
    - `messages/en.json` `CookieBanner.consentBody` contains `<privacy>` and `<terms>` tags and the ` · ` separator; the 5 service provider titles are NOT standalone translatable keys (they stay in the component).
    - All 6 stubs byte-identical to en.json (`cmp -s` exits 0); `npx vitest run` exits 0; `npm run build` exits 0.
  </acceptance_criteria>
  <done>CookieBanner renders all consent copy via useTranslations('CookieBanner') with the Privacy/Legal links through t.rich + localized Link; provider names + consent mechanics untouched; EN byte-identical; stubs synced; suite + build green. STR-01 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time author -> message catalog | Testimonials/CookieBanner chrome strings are developer-authored static JSON |
| user-generated review content -> render | Google review text/name/role is NOT externalized and NOT altered by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-10 | Tampering | CookieBanner t.rich consentBody | low | mitigate | `t.rich('consentBody', ...)` takes React-component renderers for the `<privacy>`/`<terms>` tags — it never injects raw HTML. The message value is static author-controlled JSON, never user input, so no XSS surface (RESEARCH Security Domain). |
| T-69-11 | Tampering | CookieBanner consent mechanics | medium | mitigate | The localStorage consent-storage, gtag consent-update, and `prestigo:consent-granted` CustomEvent logic MUST stay byte-identical — only display strings change. Verified by the full `npx vitest run` (consent-related tests) staying green. |
| T-69-12 | Elevation of Privilege | CookieBanner Privacy/Legal links | low | mitigate | The two links become `createNavigation`'s `Link` (internal pathnames only) — no open-redirect surface. |
</threat_model>

<verification>
- `npx vitest run` — full suite green (Testimonials, carousel, consent tests, and the Phase-68 middleware regression suite).
- `npm run build` — every locale subpath prerenders all target components with no MISSING_MESSAGE.
- Phase gate (per 69-VALIDATION.md): full suite green + build green + a live `npm run dev` spot-check — EN pages (/, section anchors) byte-identical to pre-phase, and internal links from `/ru/` (Nav, Footer, Fleet, Services, CookieBanner) keep the `/ru/` prefix — before `/gsd-verify-work`.
</verification>

<success_criteria>
- Testimonials (getTranslations), TestimonialsCarousel (useTranslations), and CookieBanner (useTranslations + t.rich) fully externalized; review content + consent mechanics + provider names untouched; EN byte-identical; 6 stubs synced to the final catalog; suite + build green.
- STR-01 is complete: no hardcoded user-facing UI-chrome copy remains across Nav, Footer, Hero(+subs), Services, Fleet, HowItWorks, Testimonials(+Carousel), CookieBanner, FeatureStrip.
</success_criteria>

<output>
Create `.planning/phases/69-string-externalization-ui-chrome/69-05-SUMMARY.md` when done.
</output>