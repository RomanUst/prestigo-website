---
phase: 69-string-externalization-ui-chrome
plan: 04
type: execute
wave: 4
depends_on: [69-03]
files_modified:
  - components/Services.tsx
  - components/Fleet.tsx
  - messages/en.json
  - messages/ru.json
  - messages/es.json
  - messages/fr.json
  - messages/ar.json
  - messages/hi.json
  - messages/zh.json
autonomous: true
requirements: [STR-01]
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Services and Fleet render every visible string, price callout, and alt through useTranslations('Services'|'Fleet') — no hardcoded user-facing copy remains (STR-01)"
    - "Service price callouts (From €{price}, From €{price}/hr) keep their DB-driven values (airportPrice/hourlyFrom/cheapestIntercity props) via named ICU args, byte-identical"
    - "Vehicle model names (Mercedes-Benz E/S/V-Class) stay proper nouns in the component config (NOT translatable catalog values) and are interpolated into the alt template; Services 'Learn more' and Fleet 'See pricing →' go through the localized Link"
    - "The #book 'Book a transfer' fragment anchor stays a raw <a> (same-page fragment, not an internal route)"
    - "EN output byte-for-byte unchanged; Services/Fleet namespaces present in all 7 locale files; /ru build prerenders both with no MISSING_MESSAGE"
  artifacts:
    - "messages/en.json Services + Fleet namespaces (cards array, price-callout ICU keys, vehicle array, alt template) synced to 6 stubs"
  key_links:
    - "Services priceCallouts keyed by structural id (not translated label) -> t('fromPrice', { price }) / t('fromPriceHourly', { price })"
    - "Fleet alt template t('vehicles') zipped with structural {model, photo} config -> {model} interpolated"
    - "Services 'Learn more' + Fleet 'See pricing →' -> Link from @/i18n/routing"
---

<objective>
Externalize the Services grid and the Fleet cards. Both carry price/data interpolation and internal-link conversions; both must keep proper-noun vehicle model names and DB-driven prices out of the translatable catalog while translating everything else.

Purpose: Establish the "structural config stays in the component, display strings move to a catalog array zipped by a stable id/index" pattern for data-driven card grids — the shape Phases 70/71 reuse for booking vehicle cards and route pages.
Output: Services + Fleet consuming their namespaces; internal links locale-aware; 6 stubs re-synced.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-string-externalization-ui-chrome/69-RESEARCH.md
@.planning/phases/69-string-externalization-ui-chrome/69-01-SUMMARY.md
</context>

<artifacts>
## Artifacts this plan produces
- `messages/en.json` gains `Services` (heading lines, `cards` array of 8 {label,title,body,detail}, `fromPrice`/`fromPriceHourly` ICU keys, `learnMore`, `bookTransfer`) and `Fleet` (heading lines, `vehicles` array of 3 {category,passengers,bags,features[],altTemplate}, `seePricing`) namespaces. Synced to 6 stubs.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Externalize Services.tsx (grid + price callouts + Link swap)</name>
  <files>components/Services.tsx, messages/en.json, messages/ru.json, messages/es.json, messages/fr.json, messages/ar.json, messages/hi.json, messages/zh.json</files>
  <read_first>
    - components/Services.tsx (current — `services` array of 8 {label,title,body,detail,href,isNew}; heading "Our services" / "Every journey," / "covered."; priceCallouts keyed by label; "Learn more" Link (next/link); "Book a transfer" #book anchor)
    - messages/en.json (convention + prior namespaces)
    - i18n/routing.ts (Link export)
    - .planning/phases/69-string-externalization-ui-chrome/69-RESEARCH.md (Pitfall 4 ICU price interpolation, Pitfall 1 link scope)
  </read_first>
  <action>
    1. Add a `Services` namespace: `label` ("Our services"), `headingLine1` ("Every journey,"), `headingLine2` ("covered."), `cards` (array of 8 objects `{ label, title, body, detail }` verbatim from the current `services` array, in the same order), `fromPrice` ("From €{price}"), `fromPriceHourly` ("From €{price}/hr"), `learnMore` ("Learn more"), `bookTransfer` ("Book a transfer").
    2. In Services.tsx, keep the `services` config array but reduce each element to structural fields `{ id, href, isNew }` where `id` = the current label string ("Airport", "Intercity", ... — used as the React key AND the priceCallouts lookup key, NOT rendered). Render display fields from `t.raw('cards')` zipped by index: `.label`, `.title`, `.body`, and the `.detail` fallback.
    3. Rewrite `priceCallouts` to build from ICU keys keyed by the structural `id`: Airport -> `t('fromPrice', { price: airportPrice })`, City -> `t('fromPriceHourly', { price: hourlyFrom })`, Intercity -> `t('fromPrice', { price: cheapestIntercity })`. Keep the DB-driven prop values; the fallback to `cards[i].detail` when no callout exists stays.
    4. Swap the per-service "Learn more" `Link` import to `@/i18n/routing` (away from the framework navigation module it currently uses); render `t('learnMore')`. The "Book a transfer" button stays a same-page fragment anchor (`#book`) and renders `t('bookTransfer')`.
    5. Re-sync the 6 stub files from en.json.
  </action>
  <verify>
    <automated>npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "useTranslations('Services')" components/Services.tsx` matches; `grep -n "from '@/i18n/routing'" components/Services.tsx` matches (localized Link imported); the "Learn more" element renders a `Link` (no framework-navigation Link import remains — `grep -cE "from ['\"]next/link['\"]" components/Services.tsx` returns 0).
    - `messages/en.json` `Services.cards` is an 8-element array; `Services.fromPrice` contains `{price}`; all 6 stubs byte-identical (`cmp -s` exits 0).
    - `npx vitest run` exits 0 and `npm run build` exits 0 (price callouts render DB values; no MISSING_MESSAGE on / or /ru).
  </acceptance_criteria>
  <done>Services renders all copy + DB-driven price callouts via useTranslations('Services'); "Learn more" is locale-aware; "#book" fragment anchor stays raw; EN byte-identical; stubs synced; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Externalize Fleet.tsx (vehicle cards + raw <a> -> Link, model as proper noun)</name>
  <files>components/Fleet.tsx, messages/en.json, messages/ru.json, messages/es.json, messages/fr.json, messages/ar.json, messages/hi.json, messages/zh.json</files>
  <read_first>
    - components/Fleet.tsx (current — `vehicles` array of 3 {model,category,passengers,bags,features[],photo,photoAlt}; heading "The fleet" / "Mercedes only." / "Always."; "See pricing →" raw <a href="/book">)
    - messages/en.json (Services namespace + convention)
    - .planning/phases/69-string-externalization-ui-chrome/69-RESEARCH.md (Pitfall 5 — vehicle model names are proper nouns, out of scope for translation)
  </read_first>
  <action>
    1. Add a `Fleet` namespace: `label` ("The fleet"), `headingLine1` ("Mercedes only."), `headingLine2` ("Always."), `vehicles` (array of 3 objects `{ category, passengers, bags, features, altTemplate }` verbatim from the current data — `features` is the 4-string array; `altTemplate` = "{model} — Prague chauffeur service"), `seePricing` ("See pricing →").
    2. In Fleet.tsx, keep the `vehicles` config array reduced to structural fields `{ model, photo }` (model = the proper-noun "Mercedes-Benz E/S/V-Class" — NOT translated, stays in code as both React key and alt interpolation value). Add `const t = useTranslations('Fleet')`. Render display fields from `t.raw('vehicles')` zipped by index: `.category`, `.passengers`, `.bags`, `.features` (map the array), and `alt={t('vehicles.'+i+'.altTemplate', ...)}` — OR render the alt from the zipped array's `altTemplate` with the model interpolated (`altTemplate.replace('{model}', model)` is NOT allowed; use next-intl interpolation via a per-index message key, or keep altTemplate as a t() key with `{model}` arg). Render the vehicle `model` heading directly from the structural config (proper noun, not translated).
    3. Convert the "See pricing →" raw anchor (currently pointing at the booking route) to `Link` from `@/i18n/routing`, rendering `t('seePricing')`.
    4. Re-sync the 6 stub files from en.json.
  </action>
  <verify>
    <automated>npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "useTranslations('Fleet')" components/Fleet.tsx` matches; `grep -n "from '@/i18n/routing'" components/Fleet.tsx` matches; the "See pricing" element is a localized `Link` (no raw internal anchor remains — `grep -cE '<a[[:space:]]+href="/' components/Fleet.tsx` returns 0).
    - The three "Mercedes-Benz …-Class" model strings still appear in components/Fleet.tsx (proper nouns kept in code, `grep -c "Mercedes-Benz" components/Fleet.tsx` >= 3) and are NOT present as standalone translatable keys in messages/en.json.
    - `messages/en.json` `Fleet.vehicles` is a 3-element array with `features` arrays; all 6 stubs byte-identical (`cmp -s` exits 0).
    - `npx vitest run` exits 0 and `npm run build` exits 0.
  </acceptance_criteria>
  <done>Fleet renders category/passengers/bags/features/alt via useTranslations('Fleet'); model names stay proper nouns in code; "See pricing →" is locale-aware; EN byte-identical; stubs synced; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time author -> message catalog | Services/Fleet strings + prices are developer/DB-sourced static values; no untrusted user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-08 | Tampering | Services price callouts | low | mitigate | Prices are injected as numeric named ICU args from server props (DB-driven), rendered by plain `t()` — no raw HTML, no user input, no `t.rich`. |
| T-69-09 | Elevation of Privilege | Services/Fleet internal links | low | mitigate | "Learn more" / "See pricing →" use `createNavigation`'s `Link` (internal pathnames only); the #book fragment stays raw. No open-redirect surface. |
</threat_model>

<verification>
- `npm run build` — / and /ru prerender Services + Fleet with the new namespaces, DB price callouts intact, no MISSING_MESSAGE.
- `npx vitest run` — full suite green.
- Manual dev spot-check: Services/Fleet text + price callouts byte-identical on /; "Learn more"/"See pricing →" keep the /ru prefix from /ru.
</verification>

<success_criteria>
- Services + Fleet fully externalized; DB prices + proper-noun model names preserved; internal links locale-aware; EN byte-identical; 6 stubs synced; suite + build green.
</success_criteria>

<output>
Create `.planning/phases/69-string-externalization-ui-chrome/69-04-SUMMARY.md` when done.
</output>
