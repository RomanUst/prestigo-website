---
phase: 69-string-externalization-ui-chrome
plan: 03
type: execute
wave: 3
depends_on: [69-02]
files_modified:
  - components/Hero.tsx
  - components/HeroRating.tsx
  - components/HeroTypewriter.tsx
  - components/HeroWhatsApp.tsx
  - components/HeroBackground.tsx
  - messages/en.json
  - messages/ru.json
  - messages/es.json
  - messages/fr.json
  - messages/ar.json
  - messages/hi.json
  - messages/zh.json
autonomous: true
requirements: [STR-01]
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Hero and its 4 sub-parts (HeroTypewriter, HeroRating, HeroWhatsApp, HeroBackground) render every visible string, aria-label, and alt through the shared useTranslations('Hero') namespace — no hardcoded user-facing copy remains (STR-01)"
    - "The 5 HeroTypewriter rotating phrases are a JSON array (t.raw('words')), NOT a comma-joined string, so a later phase can reorder per-locale without touching code; words[0] stays the widest phrase for LCP"
    - "The Hero price anchor keeps its inline copper €{amount} span via t.rich, byte-identical; the {price} value stays DB-driven (airportPrice prop)"
    - "Hero server parts use useTranslations synchronously (Hero stays a sync Server Component — no await introduced); the 3 client sub-parts read from the NextIntlClientProvider set in SiteChrome"
    - "EN output is byte-for-byte unchanged (headline whitespace, <br> breaks, HERO_ALT, rating aria-label); Hero namespace present in all 7 locale files; build prerenders /ru Hero with no MISSING_MESSAGE"
  artifacts:
    - "messages/en.json Hero namespace (words array, price-anchor rich key, rating aria) synced to 6 stubs"
  key_links:
    - "HeroTypewriter/HeroWhatsApp/HeroBackground (client) -> NextIntlClientProvider (SiteChrome) -> useTranslations('Hero')"
    - "Hero.tsx t.rich('priceAnchor', { amount, price }) -> inline copper span preserved"
---

<objective>
Externalize the LCP-critical Hero and its four sub-parts into the shared `Hero` namespace. Hero is the most visually prominent surface in the phase — byte-for-byte fidelity and the LCP-safe typewriter behavior are the correctness bar.

Purpose: Prove the namespace pattern holds for a mixed server/client component cluster (sync Server Components + 'use client' children) sharing one namespace across the provider boundary, including a t.rich interpolation and an array-valued key.
Output: Hero + HeroTypewriter + HeroRating + HeroWhatsApp + HeroBackground consuming useTranslations('Hero'); 6 stubs re-synced.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-string-externalization-ui-chrome/69-RESEARCH.md
@.planning/phases/69-string-externalization-ui-chrome/69-01-SUMMARY.md
</context>

<artifacts>
## Artifacts this plan produces
- `messages/en.json` gains the `Hero` namespace: `label`, `headlineSuffix`, `headlineItalic`, `subhead1`/`subhead2`, `bookRide`, `priceAnchor` (rich, `<price>€{amount}</price>`), `scroll`, `ratingAria` (`{rating}`/`{count}` args), `googleReviews`, `words` (5-item array), `bookViaWhatsapp`, `heroAlt`. Synced to 6 stubs.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Externalize Hero.tsx + HeroRating.tsx (server parts)</name>
  <files>components/Hero.tsx, components/HeroRating.tsx, messages/en.json, messages/ru.json, messages/es.json, messages/fr.json, messages/ar.json, messages/hi.json, messages/zh.json</files>
  <read_first>
    - components/Hero.tsx (current — sync Server Component; label, headline suffix " in Prague,", italic "as it should be.", subhead lines, "Book a ride" #book anchor, price anchor with inline copper span, "Scroll" hint; renders HeroTypewriter/HeroWhatsApp/HeroRating/HeroBackground)
    - components/HeroRating.tsx (current — aria-label "Rated {x} out of 5 from {n} Google reviews", "Google reviews" label; ratingValue.toFixed(1) is data)
    - messages/en.json (convention + prior namespaces)
    - .planning/phases/69-string-externalization-ui-chrome/69-RESEARCH.md (Pattern 4 sync-server, Pitfall 4 ICU interpolation, Pitfall 5 scope; Open Question #2 — keep Hero synchronous)
  </read_first>
  <action>
    1. Add a `Hero` namespace to messages/en.json with verbatim strings: `label` ("Prague · Premium Chauffeur"), `headlineSuffix` ("in Prague,"), `headlineItalic` ("as it should be."), `subhead1` ("Your chauffeur is already tracking your flight."), `subhead2` ("You simply arrive."), `bookRide` ("Book a ride"), `priceAnchor` (rich string "Airport transfers from <price>€{amount}</price> — fixed price, no surcharges"), `scroll` ("Scroll"), `ratingAria` ("Rated {rating} out of 5 from {count} Google reviews"), `googleReviews` ("Google reviews").
    2. In Hero.tsx add `const t = useTranslations('Hero')` (from 'next-intl', the synchronous hook — keep Hero a synchronous Server Component; do NOT introduce an await or switch it to the async server translation API; RESEARCH Open Question #2). Replace: the label, the `<HeroTypewriter /> {headlineSuffix}` (preserve the exact single space and the "in Prague," text), the italic line, the two subhead lines (keep the `<br />`), the "Book a ride" text (the `#book` same-page fragment anchor STAYS a raw anchor — not an internal route), and "Scroll".
    3. Render the price anchor with `t.rich('priceAnchor', { amount: airportPrice, price: (chunks) => <span style={{ color: 'var(--copper)' }}>{chunks}</span> })` so the inline copper €{amount} span is preserved byte-identically and the value stays the DB-driven `airportPrice` prop.
    4. In HeroRating.tsx add `const t = useTranslations('Hero')`; render `aria-label={t('ratingAria', { rating: ratingValue, count: reviewCount })}` and replace the "Google reviews" label with `t('googleReviews')`. Keep the star-rendering math and `ratingValue.toFixed(1)` (numeric data) unchanged.
    5. Re-sync the 6 stub files from en.json.
  </action>
  <verify>
    <automated>npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "useTranslations('Hero')" components/Hero.tsx` and `grep -n "useTranslations('Hero')" components/HeroRating.tsx` both match.
    - Hero stays a synchronous Server Component: `grep -cE 'async function Hero' components/Hero.tsx` returns 0.
    - `messages/en.json` `Hero.priceAnchor` contains `<price>` and `{amount}`; `Hero.ratingAria` contains `{rating}` and `{count}`; all 6 stubs byte-identical (`cmp -s` exits 0).
    - `npx vitest run` exits 0 and `npm run build` exits 0.
  </acceptance_criteria>
  <done>Hero + HeroRating render via useTranslations('Hero'); price anchor keeps its inline copper span through t.rich; Hero stays synchronous; #book fragment anchor untouched; EN byte-identical; stubs synced; suite + build green.</done>
</task>

<task type="auto">
  <name>Task 2: Externalize the 3 client sub-parts (HeroTypewriter, HeroWhatsApp, HeroBackground)</name>
  <files>components/HeroTypewriter.tsx, components/HeroWhatsApp.tsx, components/HeroBackground.tsx, messages/en.json, messages/ru.json, messages/es.json, messages/fr.json, messages/ar.json, messages/hi.json, messages/zh.json</files>
  <read_first>
    - components/HeroTypewriter.tsx (current — 'use client'; `words` array of 5 phrases with the LCP comment about words[0] being widest)
    - components/HeroWhatsApp.tsx (current — 'use client'; visible text "Book via WhatsApp"; the wa.me URL + prefilled text query stay)
    - components/HeroBackground.tsx (current — 'use client'; HERO_ALT used in 3 places: desktop getImageProps, mobile getImageProps, and the <img> alt)
    - messages/en.json (Hero namespace from Task 1)
  </read_first>
  <action>
    1. Append to the `Hero` namespace: `words` (array of the 5 rotating phrases verbatim, order preserved — "Chauffeur Service", "Airport Transfer", "Private Driver", "Luxury Transport", "Business Travel"), `bookViaWhatsapp` ("Book via WhatsApp"), `heroAlt` ("Chauffeur driving a Mercedes-Benz S-Class toward the Prague skyline at sunset").
    2. HeroTypewriter: add `const t = useTranslations('Hero')`; replace the module-level `words` const usage with `t.raw('words') as string[]` inside the component. Keep the LCP comment (adapt wording to note the array's first item must stay the widest phrase) and every timing/opacity/reduced-motion behavior unchanged.
    3. HeroWhatsApp: add `const t = useTranslations('Hero')`; replace the visible "Book via WhatsApp" text with `t('bookViaWhatsapp')`. Keep the wa.me href, the prefilled text query, and the trackMetaEvent onClick unchanged.
    4. HeroBackground: add `const t = useTranslations('Hero')`; replace the `HERO_ALT` const with `t('heroAlt')` at all 3 usage sites (both getImageProps alt props and the `<img alt>`). Keep the picture/preload/motion logic unchanged.
    5. Re-sync the 6 stub files from en.json.
  </action>
  <verify>
    <automated>npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslations('Hero')" components/HeroTypewriter.tsx components/HeroWhatsApp.tsx components/HeroBackground.tsx` shows a match in each file.
    - `messages/en.json` `Hero.words` is a 5-element array; `Hero.heroAlt` and `Hero.bookViaWhatsapp` present; all 6 stubs byte-identical (`cmp -s` exits 0).
    - `npx vitest run` exits 0 and `npm run build` exits 0.
  </acceptance_criteria>
  <done>HeroTypewriter reads its rotating phrases from the words array; HeroWhatsApp and HeroBackground render their string/alt via useTranslations('Hero'); client sub-parts resolve through the SiteChrome provider; EN byte-identical; stubs synced; suite + build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time author -> message catalog | Hero strings are developer-authored static JSON; no user input |
| SiteChrome provider -> Hero client sub-parts | client components read messages from the provider context set in Plan 01; no new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-06 | Tampering | Hero t.rich price anchor | low | mitigate | `t.rich('priceAnchor', ...)` takes a React-component renderer for the `<price>` tag and injects the numeric `airportPrice` via a named ICU arg — never raw HTML, never user input. Structurally XSS-safe. |
| T-69-07 | Tampering | Hero client sub-parts | low | accept | No auth/middleware/CSP surface touched; strings are static catalog values. |
</threat_model>

<verification>
- `npm run build` — / and /ru prerender the full Hero with the new namespace, no MISSING_MESSAGE, no async-Hero regression.
- `npx vitest run` — full suite green.
- Manual dev spot-check: Hero headline/subhead/price anchor byte-identical on /; typewriter rotation still LCP-safe (only after first interaction); HERO_ALT unchanged.
</verification>

<success_criteria>
- Hero + 4 sub-parts fully externalized under one Hero namespace; words array + t.rich price anchor + rating aria interpolation correct; Hero stays synchronous; EN byte-identical; 6 stubs synced; suite + build green.
</success_criteria>

<output>
Create `.planning/phases/69-string-externalization-ui-chrome/69-03-SUMMARY.md` when done.
</output>
