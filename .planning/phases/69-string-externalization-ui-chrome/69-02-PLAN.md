---
phase: 69-string-externalization-ui-chrome
plan: 02
type: execute
wave: 2
depends_on: [69-01]
files_modified:
  - components/Footer.tsx
  - components/FeatureStrip.tsx
  - components/HowItWorks.tsx
  - messages/en.json
  - messages/ru.json
  - messages/es.json
  - messages/fr.json
  - messages/ar.json
  - messages/hi.json
  - messages/zh.json
autonomous: true
requirements: [STR-01]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Footer, FeatureStrip, and HowItWorks render every visible string + aria-label + alt text through useTranslations('Footer'|'FeatureStrip'|'HowItWorks') — no hardcoded user-facing copy remains in those files (STR-01)"
    - "Every internal Footer link (/book, the 8 service items, the 5 route items, /routes, the 3 blog items, /blog, /privacy, /terms) goes through the localized Link from @/i18n/routing; tel:/mailto:/wa.me/instagram/facebook stay raw <a>"
    - "EN output of all three components is byte-for-byte unchanged (text, <br> line breaks, punctuation, arrows, aria-labels, alt text, the © {year} interpolation)"
    - "The chelautotrans legal address block, phone number, and email stay hardcoded EN (out of scope per RESEARCH Pitfall 5)"
    - "The Footer/FeatureStrip/HowItWorks namespaces are present in all 7 locale files; /ru build prerenders these components with no MISSING_MESSAGE"
  artifacts:
    - "messages/en.json Footer, FeatureStrip, HowItWorks namespaces (+ synced to 6 stubs)"
  key_links:
    - "Footer internal links -> Link from @/i18n/routing (locale-prefix retention)"
    - "Footer.services / routes / blog / FeatureStrip.pillars / HowItWorks.steps -> catalog arrays zipped with structural hrefs/numbers/icons in the component"
---

<objective>
Externalize the three remaining server-rendered layout components: Footer (the largest raw-<a>-to-Link conversion in the phase), FeatureStrip, and HowItWorks. Apply the Plan 01 namespace convention; keep EN byte-for-byte unchanged.

Purpose: Footer carries the bulk of Phase 68's IN-01 link-sweep debt (RESEARCH Pitfall 1) — a /ru visitor clicking any footer link currently drops to EN root. This plan closes that for the footer and finishes the simple server components.
Output: Footer, FeatureStrip, HowItWorks consuming their namespaces; 6 stubs re-synced.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-string-externalization-ui-chrome/69-RESEARCH.md
@.planning/phases/69-string-externalization-ui-chrome/69-01-SUMMARY.md
</context>

<artifacts>
## Artifacts this plan produces
- `messages/en.json` gains `Footer`, `FeatureStrip`, `HowItWorks` namespaces (arrays for the service/route/blog link groups, the 4 feature pillars, the 3 how-it-works steps), synced into all 6 stubs.
- Reusable pattern: catalog arrays (`t.raw('services')` etc.) zipped with structural href/number/icon config kept in the component.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Externalize Footer + convert every internal link to the localized Link</name>
  <files>components/Footer.tsx, messages/en.json, messages/ru.json, messages/es.json, messages/fr.json, messages/ar.json, messages/hi.json, messages/zh.json</files>
  <read_first>
    - components/Footer.tsx (current — all hardcoded strings; raw <a> internal links; the legal address block; tel/mailto/social <a>)
    - messages/en.json (Nav namespace + convention from Plan 01)
    - i18n/routing.ts (Link export from createNavigation)
    - .planning/phases/69-string-externalization-ui-chrome/69-RESEARCH.md (Pitfall 1 punch list — the exact raw-<a> lines; Pitfall 5 out-of-scope boundary)
    - .planning/phases/69-string-externalization-ui-chrome/69-01-SUMMARY.md (the locked convention)
  </read_first>
  <action>
    1. Add a `Footer` namespace to messages/en.json with verbatim current strings: `ctaLabel` ("Ready to travel?"), `ctaHeadlineLine1` ("Book your transfer."), `ctaHeadlineLine2` ("We handle the rest."), `bookNow` ("Book now"), `brandTagline1`/`brandTagline2` ("Premium chauffeur service." / "Prague and Central Europe."), `servicesHeading` ("Services"), `services` (array of the 8 labels: Airport Transfer, Intercity Routes, Multi-day Hire, Corporate Accounts, VIP & Events, Group Transfer, About PRESTIGO, FAQ), `routesHeading` ("Popular routes"), `routes` (array of the 5 labels incl. the "→" arrows), `viewAllRoutes` ("View all routes →"), `blogHeading` ("Blog"), `blog` (array of the 3 labels), `viewAllPosts` ("View all posts →"), `contactHeading` ("Contact"), `whatsapp` ("WhatsApp"), `availability` ("24 / 7 availability"), `copyright` ("© {year} PRESTIGO. All rights reserved." — named ICU arg {year}), `privacy` ("Privacy Policy"), `terms` ("Terms"), `instagramAria` ("Instagram"), `facebookAria` ("Facebook"), `tagline` ("Prestige in every mile.").
    2. In Footer.tsx add `const t = useTranslations('Footer')` (from 'next-intl'). Keep the service/route/blog list objects' `href` (and `isNew`) in the component (structural), and render each label from the catalog array via `t.raw('services')`/`t.raw('routes')`/`t.raw('blog')` zipped by index — do not move hrefs into the catalog.
    3. Convert every INTERNAL link to `Link` from `@/i18n/routing`: the /book CTA, all 8 service `href`s, all 5 route `href`s, /routes, all 3 blog `href`s, /blog, /privacy, /terms. Keep `tel:+420...`, `mailto:...`, `https://wa.me/...`, the Instagram and Facebook `https://` links as raw `<a>` (external/protocol links — do NOT convert). Keep the `#`-fragment behavior nowhere applicable here.
    4. Render `t('copyright', { year: new Date().getFullYear() })` — keep the dynamic year via the ICU arg, byte-identical output.
    5. KEEP hardcoded (out of scope): the chelautotrans s.r.o. / IČO / Spojovací address block, the phone number text, the email text.
    6. Re-sync the 6 stub files from en.json (`cp messages/en.json messages/{ru,es,fr,ar,hi,zh}.json`).
  </action>
  <verify>
    <automated>npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "useTranslations('Footer')" components/Footer.tsx` matches; `grep -n "from '@/i18n/routing'" components/Footer.tsx` matches (localized Link imported).
    - Every internal footer link is a localized `Link`, no raw internal anchor remains: `grep -cE '<a[[:space:]]+href="/' components/Footer.tsx` returns 0. The external/protocol anchors (`tel:`, `mailto:`, `wa.me`, the two social `https://` links) remain — `grep -cE '<a[[:space:]]+href="(tel:|mailto:|https://)' components/Footer.tsx` returns a value >= 5.
    - `messages/en.json` has a `Footer` namespace with `services`/`routes`/`blog` arrays and a `copyright` value containing `{year}`.
    - All 6 stubs are byte-identical to en.json (`cmp -s` exits 0) and contain the `Footer` namespace.
    - `npx vitest run` exits 0 and `npm run build` exits 0 (/ and /ru prerender the footer with no MISSING_MESSAGE).
  </acceptance_criteria>
  <done>Footer renders all copy via useTranslations('Footer'); every internal link is locale-aware; legal/contact data stays hardcoded; EN byte-identical; stubs synced; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Externalize FeatureStrip + HowItWorks</name>
  <files>components/FeatureStrip.tsx, components/HowItWorks.tsx, messages/en.json, messages/ru.json, messages/es.json, messages/fr.json, messages/ar.json, messages/hi.json, messages/zh.json</files>
  <read_first>
    - components/FeatureStrip.tsx (current — aria-label "Why PRESTIGO"; 4 pillars {title, sub}; icon components stay)
    - components/HowItWorks.tsx (current — "How it works" label; heading lines; 3 steps {number, title, body, photoAlt}; "Step {number}")
    - messages/en.json (convention + Footer namespace)
  </read_first>
  <action>
    1. Add a `FeatureStrip` namespace: `sectionAria` ("Why PRESTIGO"), `pillars` (array of 4 objects `{ title, sub }` verbatim: "Professional chauffeurs"/"English-speaking & discreet", "Premium Mercedes fleet"/"E-, S- & V-Class, latest models", "Fixed prices"/"No hidden fees, flight tracking", "24/7 support"/"Always on time, every day"). Keep the `Icon` component references in the component config (structural, non-translatable) and zip with the catalog array by index. Add `const t = useTranslations('FeatureStrip')`.
    2. Add a `HowItWorks` namespace: `label` ("How it works"), `headingLine1` ("Three steps."), `headingLine2` ("Zero surprises."), `stepPrefix` ("Step"), `steps` (array of 3 objects `{ title, body, photoAlt }` verbatim from the current `steps` array). Keep each step's `number` ('01'/'02'/'03') and `photo` path in the component config; render `{t('stepPrefix')} {step.number}`, `t.raw('steps')[i].title`, `.body`, and `alt={t.raw('steps')[i].photoAlt}`. Add `const t = useTranslations('HowItWorks')`.
    3. Preserve the `<br />` line breaks and the `display-italic` span in HowItWorks' heading exactly (headingLine1 + <br> + italic headingLine2), matching Footer's CTA-headline pattern.
    4. Re-sync the 6 stub files from en.json.
  </action>
  <verify>
    <automated>npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "useTranslations('FeatureStrip')" components/FeatureStrip.tsx` and `grep -n "useTranslations('HowItWorks')" components/HowItWorks.tsx` both match.
    - `messages/en.json` has `FeatureStrip.pillars` (4 items) and `HowItWorks.steps` (3 items) arrays; all 6 stubs byte-identical (`cmp -s` exits 0).
    - `npx vitest run` exits 0 (full suite; no regression) and `npm run build` exits 0.
  </acceptance_criteria>
  <done>FeatureStrip + HowItWorks render all copy, aria-labels, and alt text via their namespaces; icons/step-numbers/photo paths stay structural; EN byte-identical; stubs synced; suite + build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time author -> message catalog | Footer/FeatureStrip/HowItWorks strings are developer-authored static JSON; no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-04 | Tampering | Footer/FeatureStrip/HowItWorks | low | accept | All externalized strings are static author-controlled catalog values rendered with plain `t()`/`t.raw()` (no `t.rich`, no user input). No auth/middleware/CSP surface is touched. |
| T-69-05 | Elevation of Privilege | Footer internal links | low | mitigate | Link swaps use `createNavigation`'s `Link`, which only resolves internal pathnames through `routing` — external/protocol links (tel/mailto/wa.me/social) are deliberately left as raw `<a>`, so no open-redirect surface is introduced. |
</threat_model>

<verification>
- `npm run build` — / and /ru prerender Footer/FeatureStrip/HowItWorks with the new namespaces, no MISSING_MESSAGE.
- `npx vitest run` — full suite green.
- Manual dev spot-check: footer text byte-identical on /; every footer internal link keeps the /ru prefix when clicked from /ru.
</verification>

<success_criteria>
- Footer, FeatureStrip, HowItWorks fully externalized; Footer internal links locale-aware; legal/contact data untouched; EN byte-identical; 6 stubs synced; suite + build green.
</success_criteria>

<output>
Create `.planning/phases/69-string-externalization-ui-chrome/69-02-SUMMARY.md` when done.
</output>
