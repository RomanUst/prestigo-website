---
phase: 75-e2e-verification-launch
plan: 15
type: execute
wave: 1
depends_on: []
files_modified:
  - app/[locale]/login/actions.ts
  - app/[locale]/login/auth-helpers.ts
  - lib/supabase/middleware.ts
  - components/auth/OAuthButtons.tsx
  - app/[locale]/account/page.tsx
  - app/[locale]/account/trips/page.tsx
  - tests/login-actions.test.ts
  - tests/middleware-customer.test.ts
  - tests/account-trips.test.tsx
autonomous: true
requirements: [VER-01]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A customer who signs in with a password at /ru/login (no return-to param) is redirected to /ru/account, not the EN /account; the same holds for ar and every other locale (D-04 account path in RU and AR)."
    - "An unauthenticated request to /ru/account/* is redirected by the middleware gate to /ru/login?return-to=<encoded /ru/...> (localized login, return-to preserved); /account/* (EN) still goes to /login as today."
    - "Magic-link and sign-up confirmation emails and the OAuth buttons always carry a localized return-to (the provided return-to, or the locale's /account path), so the locale-agnostic /auth/callback lands the user back in their locale."
    - "Signing out from any localized page redirects to that locale's home (e.g. /ar), using the request locale inside the server action."
    - "The account dashboard links (My trips, Profile) and the trips page links/redirect use the i18n Link / locale-aware redirect with the return-to parameter name the login page actually reads (the legacy 'next' parameter is replaced)."
    - "safeReturnTo still rejects absolute URLs, protocol-relative '//' and backslash tricks; the only change is that its default is the caller-supplied localized account path (open-redirect guard unchanged — v2.0 57-02)."
    - "empty edge: safeReturnTo(null) and safeReturnTo('') with a localized fallback return that fallback; with no fallback argument they return '/account' exactly as before."
  artifacts:
    - "safeReturnTo(raw, fallback?) (app/[locale]/login/auth-helpers.ts)"
    - "localized login redirect in lib/supabase/middleware.ts"
  key_links:
    - "signInWithPassword(locale, ...) -> safeReturnTo(return-to, getPathname({ locale, href: '/account' })) -> redirect"
    - "middleware /account gate: raw pathname locale segment -> getPathname({ locale, href: '/login' }) + return-to"
    - "sendMagicLink / signUpWithPassword / OAuthButtons -> /auth/callback?return-to=<localized> -> callback safeReturnTo -> localized page"
  prohibitions:
    - "MUST NOT weaken safeReturnTo's relative-only / no '//' / no backslash rules (open-redirect guard)."
    - "MUST NOT treat the NEXT_LOCALE cookie's presence as a locale choice (middleware sets it on every response)."
    - "MUST NOT change the /api or /auth route trees' locations (they stay outside [locale])."
---

<objective>
Make the sign-in and account ("My trips") path locale-continuous so D-04's RU and AR account verification can pass: localized post-login destination, localized middleware login redirect, localized return-to on magic link / sign-up / OAuth, localized sign-out, and locale-aware account links.

Purpose: planning-time discovery found that signing in from `/ru/login` lands on the English `/account`, the /account gate sends localized users to the English `/login`, and the trips page redirect uses a parameter name the login page ignores — the RU/AR account E2E would fail on navigation, not translation.
Output: locale-aware redirects/links across 6 auth/account files with tests; safeReturnTo guard unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-e2e-verification-launch/75-CONTEXT.md
@.planning/phases/70-string-externalization-booking-account/70-08-SUMMARY.md
</context>

## Artifacts this phase produces

- `safeReturnTo(raw: string | null, fallback?: string): string` — optional localized fallback (default `'/account'` unchanged).
- Localized defaults in `signInWithPassword`, `sendMagicLink`, `signUpWithPassword` (return-to on `emailRedirectTo`), `customerSignOut` (locale from the request) in `app/[locale]/login/actions.ts`.
- Localized login redirect for the `/account` gate in `lib/supabase/middleware.ts`.
- Localized return-to default in `components/auth/OAuthButtons.tsx`; i18n Link / locale-aware redirect in `app/[locale]/account/page.tsx` and `app/[locale]/account/trips/page.tsx`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — /ru/login password sign-in lands on /ru/account (safeReturnTo fallback + action)</name>
  <files>app/[locale]/login/auth-helpers.ts, app/[locale]/login/actions.ts, tests/login-actions.test.ts</files>
  <read_first>
    - app/[locale]/login/auth-helpers.ts (safeReturnTo — relative-only guard, default '/account')
    - app/[locale]/login/actions.ts (signInWithPassword(locale, prevState, formData) -> redirect(returnTo); sendMagicLink / signUpWithPassword emailRedirectTo; customerSignOut redirect to root)
    - i18n/routing.ts (getPathname)
    - tests/login-actions.test.ts (existing mocks for supabase client, redirect, rate limit, getTranslations)
    - .planning/phases/70-string-externalization-booking-account/70-08-SUMMARY.md (Pattern E: actions receive locale as first bound argument)
  </read_first>
  <behavior>
    - signInWithPassword('ru', null, form without return-to) -> redirect called with '/ru/account'; with return-to '/ru/book' -> '/ru/book'; with return-to 'https://evil.com' -> '/ru/account'; with '//evil.com' -> '/ru/account'; locale 'en' without return-to -> '/account'.
    - safeReturnTo(null) === '/account'; safeReturnTo('', '/ar/account') === '/ar/account'; safeReturnTo('/\\evil', '/ar/account') === '/ar/account'.
  </behavior>
  <action>Give safeReturnTo an optional fallback parameter used instead of the hardcoded default when provided (guard rules untouched). In signInWithPassword compute the fallback with getPathname({ locale, href: '/account' }) and pass it to safeReturnTo. Extend tests/login-actions.test.ts (and any auth-helpers unit test present) with the behavior cases. Commit with the security: prefix (redirect-guard-adjacent change).</action>
  <verify>
    <automated>npx vitest run tests/login-actions.test.ts tests/auth-callback.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests pass including every open-redirect case.
    - `git diff app/[locale]/login/auth-helpers.ts` shows the startsWith('/') / '//' / backslash checks unchanged.
    - The commit subject starts with `security:`.
  </acceptance_criteria>
  <done>Password sign-in from a localized login page returns the user to their locale's account, with the open-redirect guard intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Middleware gate, magic link / sign-up / OAuth return-to, sign-out, account + trips links</name>
  <files>app/[locale]/login/actions.ts, lib/supabase/middleware.ts, components/auth/OAuthButtons.tsx, app/[locale]/account/page.tsx, app/[locale]/account/trips/page.tsx, tests/login-actions.test.ts, tests/middleware-customer.test.ts, tests/account-trips.test.tsx</files>
  <read_first>
    - lib/supabase/middleware.ts lines ~93-104 (/account gate using gatedPathname vs raw pathname)
    - middleware.ts and tests/middleware-customer.test.ts (how the gate is exercised; T-68-04 raw-pathname discipline)
    - components/auth/OAuthButtons.tsx (return-to query on the OAuth start URL)
    - app/[locale]/account/page.tsx (next/link to /account/trips and /account/profile)
    - app/[locale]/account/trips/page.tsx (server redirect to login with a 'next' param; next/link to /book)
    - app/auth/callback/route.ts (reads return-to via safeReturnTo; stays outside [locale], unchanged)
  </read_first>
  <behavior>
    - Middleware: unauthenticated GET /ru/account/trips -> 3xx to /ru/login?return-to=%2Fru%2Faccount%2Ftrips; GET /account -> /login?return-to=%2Faccount (unchanged); GET /ar/account -> /ar/login?return-to=%2Far%2Faccount.
    - sendMagicLink('ru', ...) and signUpWithPassword('ru', ...) pass an emailRedirectTo whose return-to decodes to the provided return-to or '/ru/account'.
    - customerSignOut with the request locale mocked to 'ar' redirects to '/ar'; with 'en' to '/'.
    - account page rendered for ru links to /ru/account/trips and /ru/account/profile; trips page unauthenticated redirects to the ru login with return-to (not 'next').
  </behavior>
  <action>Middleware gate: derive the locale from the raw pathname's first segment against routing.locales (non-en only) and set the redirect pathname with getPathname({ locale, href: '/login' }); keep the raw-pathname return-to and the gatedPathname check. sendMagicLink and signUpWithPassword: append a return-to query to emailRedirectTo using safeReturnTo(formData return-to, getPathname({ locale, href: '/account' })), URL-encoded. OAuthButtons: always set return-to — the provided prop, else the active locale's /account path via getPathname with useLocale(). customerSignOut: read the request locale with getLocale() from next-intl/server (dynamic server-action request — not the static-render case of WINDOWS #7) and redirect to getPathname({ locale, href: '/' }); keep revalidatePath. Account page: use the i18n Link for trips/profile. Trips page: replace the next/navigation redirect with the next-intl redirect from @/i18n/routing to the localized login with a return-to of the localized trips path, and use the i18n Link for /book. Extend tests/middleware-customer.test.ts, tests/login-actions.test.ts and tests/account-trips.test.tsx with the behavior cases. Commits touching the middleware gate use the security: prefix.</action>
  <verify>
    <automated>npx vitest run tests/middleware-customer.test.ts tests/middleware-i18n.test.ts tests/middleware-matcher.test.ts tests/login-actions.test.ts tests/account-trips.test.tsx tests/nav-auth.test.tsx tests/auth-callback.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All listed tests pass (middleware matcher/i18n suites unchanged and green).
    - `grep -n "next=" app/[locale]/account/trips/page.tsx` returns nothing; `grep -n "from 'next/link'" app/[locale]/account/page.tsx app/[locale]/account/trips/page.tsx` returns nothing.
    - `grep -n "return-to" app/[locale]/login/actions.ts` shows the parameter on both emailRedirectTo values.
    - Middleware-gate commit subject starts with `security:`.
  </acceptance_criteria>
  <done>The whole sign-in / account / sign-out path keeps the visitor's locale end-to-end, with redirect guards unchanged and tests proving each hop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> login server actions | return-to supplied by the client |
| Middleware -> redirect | Location header built from the request path |
| Email link -> /auth/callback | return-to travels through Supabase email links |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-28 | Spoofing (open redirect) | safeReturnTo / callback | high | mitigate | Guard rules unchanged (relative-only, no //, no backslash); localized fallback computed server-side via getPathname; tests cover https://, //, backslash inputs |
| T-75-29 | Tampering | middleware login redirect | medium | mitigate | Locale derived only from an exact routing.locales match on the raw first segment; path built with getPathname; T-68-04 raw-pathname discipline kept; middleware suites green |
| T-75-30 | Spoofing | sign-out redirect | low | mitigate | Target is getPathname of a fixed '/' for an allow-listed locale |
| T-75-SC | Tampering | package installs | low | accept | No installs |
</threat_model>

<verification>
- `npx vitest run tests/middleware-customer.test.ts tests/middleware-i18n.test.ts tests/middleware-matcher.test.ts tests/login-actions.test.ts tests/account-trips.test.tsx tests/nav-auth.test.tsx tests/auth-callback.test.ts` green; `npx tsc --noEmit` clean.
</verification>

<success_criteria>
RU/AR (and every locale) users can sign in, reach "My trips" and sign out without ever being dropped onto English routes, and the open-redirect protections are provably unchanged.
</success_criteria>

<output>
Create `.planning/phases/75-e2e-verification-launch/75-15-SUMMARY.md` when done
</output>
