---
phase: 75-e2e-verification-launch
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - app/[locale]/fleet/page.tsx
  - content/pages/en/fleet.json
  - content/pages/ru/fleet.json
  - content/pages/es/fleet.json
  - content/pages/fr/fleet.json
  - content/pages/ar/fleet.json
  - content/pages/hi/fleet.json
  - content/pages/zh/fleet.json
  - tests/fleet-page-render.test.tsx
  - tests/__snapshots__/fleet-page-render.test.tsx.snap
  - .planning/phases/75-e2e-verification-launch/freeze/75-08.freeze
autonomous: true
requirements: [VER-01]
estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every visible English string on /fleet (hero, intro, the per-vehicle descriptive text, 'Every vehicle, every time' standards, 'One marque. Three silhouettes.' section, 'Maintenance & Safety', FAQ, CTA, image alt texts) and the metadata title/description/OG come from content/pages/<locale>/fleet.json via getPageContent('fleet', locale) with a params-derived locale (D-06, D-07, D-08; Phase 74 deferred 'Localize the /fleet page')."
    - "The EN /fleet render is byte-for-byte identical to the pre-change golden snapshot; model names (Mercedes-Benz E-Class / S-Class / V-Class), capacities and prices stay DNT/structural."
    - "The /ru/fleet render and its metadata title are Russian: the rendered output contains no 'Part of the experience' / 'Every vehicle, every time' and the title is no longer 'Our Fleet — Mercedes Chauffeur Cars Prague'."
    - "The FAQPage JSON-LD on /fleet is built from the localized FAQ items as plain strings (visible FAQ and structured FAQ always match per locale — Rich Results policy); other Service/ItemList/Breadcrumb names stay English by design (Phase 74 D-09)."
    - "All 6 non-EN locale files match the EN key set, types and array lengths (same vehicle count, same FAQ count); DNT tokens and numbers reappear verbatim (D-08)."
    - "getAlternates receives content: { kind: 'page', key: 'fleet' }; the two booking/contact links on the page resolve through getPathname({ locale, href })."
  artifacts:
    - "content/pages/{en,ru,es,fr,ar,hi,zh}/fleet.json"
    - "tests/fleet-page-render.test.tsx + snapshot"
    - ".planning/phases/75-e2e-verification-launch/freeze/75-08.freeze"
  key_links:
    - "app/[locale]/fleet/page.tsx -> getPageContent('fleet', locale) -> content/pages/<locale>/fleet.json"
    - "content.faq.items -> visible FAQ AND FAQPage mainEntity (one source, cannot diverge)"
  prohibitions:
    - "MUST NOT run node scripts/i18n-translate.mjs --dry-run (or any non --check mode) on the repo tree (D-08)."
    - "MUST NOT translate vehicle model names, capacities or prices."
---

<objective>
Localize the `/fleet` page end-to-end through the content model (flagged and deferred by Phase 74 as "Localize the /fleet page"), with EN moved verbatim and in-session translations for all 6 locales, keeping visible and structured FAQ in sync per locale (D-06/D-07/D-08).

Purpose: `/ru/fleet` etc. currently render the whole English fleet page with an English title while hreflang declares them localized.
Output: 7 `fleet.json` files, a content-driven fleet page, EN byte-parity + RU render test, freeze patterns.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-e2e-verification-launch/75-CONTEXT.md
@.planning/phases/75-e2e-verification-launch/75-RESEARCH.md
@.planning/phases/75-e2e-verification-launch/75-PATTERNS.md
</context>

## Translation rules (D-08) — apply to every in-session translation in this plan

1. You are the translator (Anthropic credit is empty; the i18n Translate workflow is disabled). Never run `node scripts/i18n-translate.mjs` in any mode other than `--check`.
2. Keep verbatim (i18n/glossary.json doNotTranslate): Prestigo, PRESTIGO, chelautotrans s.r.o., Mercedes-Benz, E-Class, S-Class, V-Class, rideprestigo.com, phone numbers, € / Kč amounts, times, and every `{placeholder}`.
3. Place names: use the exonym the locale already uses in `content/routes/<locale>/*.json`.
4. Register: premium, plain, short sentences; zh = Simplified Chinese; ar = Modern Standard Arabic; hi = Devanagari Hindi with brand/model tokens kept in Latin inside the sentence.
5. Structure: identical key set, value types, array lengths and order to EN; nothing left in English except DNT-only values.
6. EN strings are moved verbatim; EN rendered output must stay byte-identical.
7. Freeze file format: one pattern per line (`<sourceKey>` or `<sourceKey>::<dotPathPrefix>`), `#` comments; applied by plan 75-18.

## Artifacts this phase produces

- `content/pages/<locale>/fleet.json` (7 files) — sections `hero`, `intro`, `vehicles[{...translatable fields only}]`, `standards`, `marque`, `maintenance`, `faq{heading,items[{q,a}]}`, `cta`, `imageAlts`, `metadata{title,description,ogTitle,ogDescription}` (sub-keys follow the actual page strings; model names, photos, capacities stay in code).
- `tests/fleet-page-render.test.tsx` + snapshot.
- `.planning/phases/75-e2e-verification-launch/freeze/75-08.freeze` containing `content/pages/en/fleet.json`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — EN golden snapshot, fleet content model and ru translation proven by render</name>
  <files>tests/fleet-page-render.test.tsx, tests/__snapshots__/fleet-page-render.test.tsx.snap, content/pages/en/fleet.json, content/pages/ru/fleet.json, app/[locale]/fleet/page.tsx</files>
  <read_first>
    - app/[locale]/fleet/page.tsx (full file, 507 lines: generateMetadata line ~19-30, vehicles data, fleetFaqs + FAQPage JSON-LD ~line 200-220, sections from ~line 230)
    - app/[locale]/about/page.tsx (params-locale + getPageContent + getAlternates content-ref reference)
    - lib/page-content.ts
    - tests/route-page-render.test.tsx (render + mocks + priceValidUntil normalization)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-CONTEXT.md (FAQPage acceptedAnswer.text must be a plain string, never a ReactNode)
  </read_first>
  <behavior>
    - The unchanged page rendered for 'en' (Nav, Footer, Reveal/Divider as pass-through, next/image mocked) produces the golden snapshot, committed first.
    - After refactor the EN render equals the golden snapshot byte-for-byte.
    - The 'ru' render contains the ru hero italic line and the ru first FAQ question, and contains neither 'Part of the experience' nor 'Every vehicle, every time'; the ru FAQPage JSON-LD first question equals the ru visible first question.
    - generateMetadata for 'ru' returns the ru title.
  </behavior>
  <action>Create tests/fleet-page-render.test.tsx per tests/route-page-render.test.tsx (mock Nav, Footer, next/image; keep Reveal/Divider rendering children), render the UNCHANGED page for 'en', write and commit the golden snapshot alone. Create content/pages/en/fleet.json with every visible English string and the metadata strings verbatim; keep model names, photo paths, capacities, bag counts and prices in code as structural data zipped by index (Phase 69 convention). Refactor the page: generateMetadata and the page body use const { locale } = await params and getPageContent('fleet', locale) narrowed to a local FleetContent type; metadata from content.metadata; getAlternates('/fleet', { indexable: true, content: { kind: 'page', key: 'fleet' }, locale }); FAQPage mainEntity mapped from content.faq.items with acceptedAnswer.text as the plain answer string; Service/ItemList/Breadcrumb names untouched (English by design, Phase 74 D-09); both internal links via getPathname({ locale, href }). Write content/pages/ru/fleet.json per the Translation rules; add the ru assertions (visible, JSON-LD and metadata).</action>
  <verify>
    <automated>npx vitest run tests/fleet-page-render.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Golden snapshot commit precedes the refactor commit.
    - `npx vitest run tests/fleet-page-render.test.tsx` passes (EN unchanged, ru visible + JSON-LD + metadata localized).
    - `grep -c "Part of the experience" app/[locale]/fleet/page.tsx` is 0 and the string exists in `content/pages/en/fleet.json`.
    - `grep -n "key: 'fleet'" app/[locale]/fleet/page.tsx` matches.
  </acceptance_criteria>
  <done>/fleet renders from the content model, EN byte-identical, Russian visible text + FAQ schema + metadata proven.</done>
</task>

<task type="auto">
  <name>Task 2: es/fr/ar/hi/zh translations, parity test and freeze patterns</name>
  <files>content/pages/es/fleet.json, content/pages/fr/fleet.json, content/pages/ar/fleet.json, content/pages/hi/fleet.json, content/pages/zh/fleet.json, tests/fleet-page-render.test.tsx, .planning/phases/75-e2e-verification-launch/freeze/75-08.freeze</files>
  <read_first>
    - content/pages/en/fleet.json and content/pages/ru/fleet.json (Task 1)
    - i18n/glossary.json
    - messages/{es,fr,ar,hi,zh}.json Fleet namespace (existing homepage fleet-card wording per locale — keep terminology consistent)
  </read_first>
  <action>Translate the EN file in-session into es, fr, ar, hi, zh per the Translation rules, keeping vehicle/tier terminology consistent with each catalog's existing Fleet namespace. Extend the test with the 6-locale parity block (key set, types, array lengths, no non-DNT value identical to EN, DNT/number reappearance) and render assertions for 'ar' and 'zh' (hero italic line present, EN hero italic absent, FAQPage first question equals visible first question). Create freeze/75-08.freeze with content/pages/en/fleet.json.</action>
  <verify>
    <automated>npx vitest run tests/fleet-page-render.test.tsx && test -s .planning/phases/75-e2e-verification-launch/freeze/75-08.freeze</automated>
  </verify>
  <acceptance_criteria>
    - All 7 locale files exist and parse; parity passes; ar and zh render assertions pass.
    - `freeze/75-08.freeze` contains `content/pages/en/fleet.json`.
  </acceptance_criteria>
  <done>/fleet is fully translated in all locales with parity tested and queued for the manifest freeze.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Content JSON -> rendered page / FAQPage JSON-LD | In-session translations rendered and serialized |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-17 | Tampering (misinformation) | model names / capacities / prices | medium | mitigate | These stay structural in code; parity test asserts DNT tokens and numbers reappear verbatim |
| T-75-20 | Repudiation (rich-result policy) | FAQPage vs visible FAQ | low | mitigate | Both built from the same content.faq.items per locale; test asserts equality |
| T-75-SC | Tampering | package installs | low | accept | No installs |
</threat_model>

<verification>
- `npx vitest run tests/fleet-page-render.test.tsx` green; `npx tsc --noEmit` clean.
</verification>

<success_criteria>
/fleet is localized in every locale (visible, FAQ schema, metadata), EN byte-identical, translations structurally consistent.
</success_criteria>

<output>
Create `.planning/phases/75-e2e-verification-launch/75-08-SUMMARY.md` when done
</output>
