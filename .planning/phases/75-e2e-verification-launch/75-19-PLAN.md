---
phase: 75-e2e-verification-launch
plan: 19
type: execute
wave: 6
depends_on: [75-17, 75-18]
files_modified:
  - .planning/phases/75-e2e-verification-launch/75-QA-RESULTS.md
autonomous: false
requirements: [VER-01]
user_setup:
  - service: google-analytics-4
    why: "Register the site_locale event parameter as an event-scoped custom dimension (no Admin API write credential exists — D-10 manual fallback)"
    dashboard_config:
      - task: "Create custom dimension: name Site Locale, scope Event, event parameter site_locale"
        location: "GA4 -> Admin -> Property (530321354) -> Data display -> Custom definitions -> Create custom dimension"
  - service: supabase-auth-test-account
    why: "D-04 RU/AR account-path E2E needs a signed-in customer; no service-role key exists locally"
    dashboard_config:
      - task: "Create a customer account on rideprestigo.com and save its credentials to scripts/qa/.e2e-account.json (git-ignored)"
        location: "https://rideprestigo.com/ru/login -> create account tab"
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The human explicitly approves the production deploy (push of main to origin, which Vercel auto-deploys and promotes in ~90 s) before anything is pushed."
    - "Before pushing, the outgoing commits are scanned for secrets (Stripe live/secret keys, Supabase service-role/JWT secrets, private keys, .env files) and the push is blocked on any hit."
    - "After the push, production serves the new build: /ru/fleet has a Russian title, /ru/routes/prague-berlin links to /ru/book, the GA init script on / contains site_locale, and csp_regression.py --compare shows no drift (or any drift is recorded for investigation) — one smoke check per changed layer."
    - "If plan 75-17 chose the column, the MCP verification of migration 062 is recorded before the push (precondition), so booking capture cannot hit an unknown column."
    - "GA4 has an event-scoped custom dimension on parameter site_locale (registered right after deploy so reporting starts immediately) (D-10), and scripts/qa/.e2e-account.json exists for the account-path E2E (D-04) — both confirmed by the human."
  artifacts:
    - "75-QA-RESULTS.md section 'Deploy & smoke'"
  key_links:
    - "git push origin main -> Vercel production deploy (project prestigo-site) -> rideprestigo.com"
    - "GA4 custom dimension site_locale <- events carrying site_locale (plans 75-04/75-05)"
  prohibitions:
    - "MUST NOT push or merge to main without the human's explicit deploy approval in this plan."
    - "MUST NOT push any commit that contains a secret or an env file."
    - "MUST NOT fire real Meta CAPI or GA4 Measurement Protocol events as a smoke test."
---

<objective>
Ship phase 75 to production with explicit human approval, prove the deploy with one smoke check per changed layer, and complete the two human-only setup steps (GA4 custom dimension registration per D-10's manual fallback; the E2E test account for D-04).

Purpose: D-02 verifies on production, so every fix must be live before the full QA run; production pushes are outward-facing and require the user's go-ahead.
Output: deployed build, smoke results in `75-QA-RESULTS.md`, GA4 dimension registered, E2E account credentials in place.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/75-e2e-verification-launch/75-CONTEXT.md
@.planning/phases/75-e2e-verification-launch/75-17-SUMMARY.md
@.planning/phases/75-e2e-verification-launch/75-18-SUMMARY.md
</context>

## Artifacts this phase produces

- Production deployment of all phase-75 commits.
- `75-QA-RESULTS.md` section "Deploy & smoke" (commit SHA deployed, timestamps, smoke results, CSP compare output).
- External: GA4 event-scoped custom dimension `site_locale`; local git-ignored `scripts/qa/.e2e-account.json`.

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Approve the production deploy</name>
  <decision>Deploy phase 75 to production now?</decision>
  <context>Plan 75-18's gate is green and recorded in 75-QA-RESULTS.md. Deploying = pushing local main to origin/main; Vercel (project prestigo-site) auto-deploys and promotes to rideprestigo.com in ~90 s (Preview builds are irrelevant). The deploy contains: localized /book, /book/multi-day, /fleet, /routes, forms, shared components; locale-preserving links site-wide; site_locale analytics; Stripe/Places locale; coded error messages; locale-aware auth redirects; and (if chosen in 75-17) code writing bookings.locale — whose migration must already be applied. Rollback if needed: Vercel instant rollback or git revert of the phase commits.</context>
  <options>
    <option id="deploy-now">
      <name>Deploy now (push main to origin)</name>
      <pros>Production gets every fix; full QA (75-20) can run immediately</pros>
      <cons>Public change; any regression is live until rolled back</cons>
    </option>
    <option id="deploy-via-pr">
      <name>Deploy via PR (gh pr create from main's phase-75 commits, then merge)</name>
      <pros>Leaves a reviewable PR record on GitHub before merge</pros>
      <cons>Extra step; Preview check on the PR will fail as usual (non-blocking)</cons>
    </option>
    <option id="hold">
      <name>Hold</name>
      <pros>Nothing public changes</pros>
      <cons>Plans 75-20/75-21 cannot run (D-02 needs production)</cons>
    </option>
  </options>
  <read_first>
    - .planning/phases/75-e2e-verification-launch/75-QA-RESULTS.md "Pre-deploy gate" section
    - .planning/phases/75-e2e-verification-launch/75-17-SUMMARY.md (migration decision/verification)
  </read_first>
  <acceptance_criteria>
    - The human's reply (deploy-now, deploy-via-pr or hold) is recorded in the SUMMARY before Task 2.
  </acceptance_criteria>
  <resume-signal>Reply "deploy-now", "deploy-via-pr" or "hold"</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: Secret-scan, push, wait for production, smoke one path per changed layer</name>
  <precondition>If .planning/phases/75-e2e-verification-launch/75-17-SUMMARY.md records option a, it also records the Supabase MCP verification rows for bookings.locale and bookings_locale_format_check; if not recorded, halt.</precondition>
  <files>.planning/phases/75-e2e-verification-launch/75-QA-RESULTS.md</files>
  <read_first>
    - .planning/phases/75-e2e-verification-launch/75-17-SUMMARY.md (decision + MCP verification record)
    - .planning/phases/75-e2e-verification-launch/75-QA-RESULTS.md (pre-deploy gate section)
    - scripts/qa/csp_regression.py (compare mode)
  </read_first>
  <action>Skip with "hold recorded" if Task 1 chose hold. Otherwise: list outgoing commits (git log origin/main..HEAD) and scan their full diff (git diff origin/main..HEAD) for secret patterns — sk_live_, rk_live_, whsec_, service_role, SUPABASE_SERVICE_ROLE_KEY=, BEGIN PRIVATE KEY, eyJhbGciOi (JWT-looking literals outside tests/fixtures), any .env file path — and abort on a real hit. Do not stage untracked files (the repo has unrelated untracked scripts and .gsd/). Deploy per the decision: deploy-now pushes main to origin; deploy-via-pr pushes a branch pointing at the same commits, opens a PR with gh (body ending with the attribution line) and merges it after the human confirms in chat. Poll production every 20 s for up to 10 minutes until curl of https://rideprestigo.com/ru/fleet returns a title that is no longer the English fleet title. Then smoke: (1) /ru/fleet title Russian; (2) /ru/routes/prague-berlin HTML contains href="/ru/book" and no href="/book"; (3) the HTML of / contains the ga-init script with site_locale; (4) python3 scripts/qa/csp_regression.py --compare https://rideprestigo.com — record exit code and any diff (drift is recorded for plan 75-20 to investigate, not fixed by loosening CSP); (5) python3 scripts/qa/render_audit.py https://rideprestigo.com --locales ar exits 0. Do not POST to /api/meta-capi or trigger any purchase. Record the deployed SHA, timings and each smoke result in a "Deploy & smoke" section of 75-QA-RESULTS.md. On any smoke failure, stop and report with the rollback options (Vercel instant rollback / git revert) instead of pushing more changes.</action>
  <verify>
    <automated>curl -s https://rideprestigo.com/ru/fleet | grep -o "<title>[^<]*</title>" && curl -s https://rideprestigo.com/ru/routes/prague-berlin | grep -c 'href="/ru/book"' && python3 scripts/qa/render_audit.py https://rideprestigo.com --locales ar</automated>
  </verify>
  <acceptance_criteria>
    - `git status -sb` shows main not ahead of origin/main (deploy-now) or the PR is merged (deploy-via-pr).
    - The /ru/fleet title is not "Our Fleet — Mercedes Chauffeur Cars Prague | PRESTIGO".
    - `/ru/routes/prague-berlin` contains at least one `href="/ru/book"`.
    - `75-QA-RESULTS.md` has a "Deploy & smoke" section with the SHA and all five smoke results.
  </acceptance_criteria>
  <done>Phase 75 is live on production and each changed layer is smoke-verified.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Register the GA4 site_locale dimension and provision the E2E test account</name>
  <action>Two human-only setup steps (no API credentials exist for either)</action>
  <instructions>
    Claude has deployed site_locale on every GA4 event (client gtag set + server Measurement Protocol purchase).
    A) GA4 custom dimension (D-10): GA4 -> Admin -> Property 530321354 -> Data display -> Custom definitions -> Create custom dimension -> Dimension name "Site Locale", Scope "Event", Event parameter "site_locale" -> Save. (Custom dimensions only populate from registration onward, so do this now.)
    B) E2E test account (D-04): open https://rideprestigo.com/ru/login, choose the create-account tab, sign up with an email address you control (Gmail plus-addressing is fine) and a throwaway password, confirm the email if asked, and set the profile name to first name "E2E", last name "TEST". Then create the file scripts/qa/.e2e-account.json in the repo with the JSON object {"email": "...", "password": "..."} — it is git-ignored and never committed or printed. Tell Claude whether this account should be deleted after the QA run.
  </instructions>
  <verification>Claude checks that scripts/qa/.e2e-account.json exists and parses with email/password keys (without printing them) and that git check-ignore reports it ignored; the GA4 registration is confirmed by the user's reply and recorded in 75-QA-RESULTS.md.</verification>
  <read_first>
    - .gitignore (scripts/qa/.e2e-account.json must be ignored — plan 75-01)
  </read_first>
  <acceptance_criteria>
    - `python3 -c "import json;d=json.load(open('scripts/qa/.e2e-account.json'));assert d['email'] and d['password']"` exits 0 (nothing printed) and `git check-ignore scripts/qa/.e2e-account.json` prints the path.
    - The GA4 registration confirmation and the delete-account preference are recorded in 75-QA-RESULTS.md.
  </acceptance_criteria>
  <resume-signal>Reply "done" with "delete account after QA: yes/no"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo -> origin/main -> Vercel production | Code becomes public on push |
| Local credentials file -> repo | Test-account password on disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-39 | Information Disclosure | secrets in pushed commits | high | mitigate | Pre-push diff scan for key/secret/JWT/private-key patterns and env files; untracked files never staged |
| T-75-40 | Denial of Service | production regression | high | mitigate | Human deploy approval; per-layer smoke; documented instant rollback; stop on first failure |
| T-75-34 | Denial of Service | booking capture vs schema | high | mitigate | Precondition requires recorded migration 062 verification when the column option was chosen |
| T-75-08 | Information Disclosure | .e2e-account.json | medium | mitigate | Git-ignored (plan 75-01), existence checked without printing contents |
| T-75-SC | Tampering | package installs | low | accept | No installs |
</threat_model>

<verification>
- Deploy approved and executed; production smoke checks pass; GA4 dimension and E2E account confirmed.
</verification>

<success_criteria>
All phase-75 fixes are live on rideprestigo.com with human approval, smoke-verified per layer, and the GA4 dimension plus E2E account are in place for the full production QA run.
</success_criteria>

<output>
Create `.planning/phases/75-e2e-verification-launch/75-19-SUMMARY.md` when done
</output>
