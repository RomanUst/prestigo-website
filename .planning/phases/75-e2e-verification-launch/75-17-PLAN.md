---
phase: 75-e2e-verification-launch
plan: 17
type: execute
wave: 3
depends_on: [75-05]
files_modified:
  - supabase/migrations/062_bookings_locale.sql
  - lib/supabase.ts
  - types/database.types.ts
  - components/admin/BookingsTable.tsx
  - tests/supabase.test.ts
  - tests/BookingsTable.test.tsx
autonomous: false
requirements: [VER-01]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The human chooses, before any schema change, between (a) adding a nullable bookings.locale column (booking language visible in admin/dispatch, queryable revenue split) and (b) PaymentIntent-metadata-only persistence; the choice and rationale are recorded in the SUMMARY (D-11 Claude's-discretion storage decision, one-way door)."
    - "If (a): migration 062_bookings_locale.sql adds bookings.locale text NULL with a format CHECK (NULL or a 2-letter code with an optional script/region suffix), is applied to the live database via the Supabase MCP before any code that writes it is deployed, and information_schema confirms the column and constraint exist (BLOCKING schema push)."
    - "If (a): buildBookingRow and buildBookingRows write locale from meta.locale (already normalized by plan 75-05) for unpaid capture, confirmed inserts and both round-trip legs; admin-created bookings keep locale NULL."
    - "If (a): the admin BookingsTable expanded row shows the booking language (e.g. 'RU') or an em dash when NULL; admin_search_bookings already returns b.* so no RPC is dropped/recreated (no PUBLIC EXECUTE re-grant risk)."
    - "If (b): no schema/code change is made in this plan and the webhook's payment-link GA4 path keeps its 'en' fallback; the decision is recorded as the D-11 storage outcome."
    - "empty edge: a booking row built from meta without a locale key stores NULL (never an empty string, never a crash)."
  artifacts:
    - "supabase/migrations/062_bookings_locale.sql (option a)"
    - "lib/supabase.ts buildBookingRow/buildBookingRows locale field (option a)"
    - "types/database.types.ts bookings.locale (option a)"
  key_links:
    - "PaymentIntent metadata.locale (plan 75-05) -> buildBookingRow(meta) -> bookings.locale -> admin BookingsTable + webhook payment-link siteLocale"
    - "Schema applied (checkpoint Task 3) BEFORE plan 75-19 deploys code that writes the column — otherwise every booking capture insert fails on an unknown column"
  prohibitions:
    - "MUST NOT deploy code that writes bookings.locale before migration 062 is applied to the live database."
    - "MUST NOT drop/recreate any SECURITY DEFINER RPC in this migration (if one ever is, REVOKE EXECUTE FROM PUBLIC, anon, authenticated again)."
---

<objective>
Resolve D-11's storage choice (PaymentIntent metadata vs a `bookings.locale` column — Claude's discretion, but a one-way door) with the human, and if the column is chosen: write migration 062, persist the booking language on every booking row, show it in the admin bookings table, and apply the migration to production before any dependent deploy.

Purpose: D-11's stated benefit is "revenue splittable by language and booking language visible in admin"; metadata alone satisfies GA4 forwarding (done in 75-05) but not admin visibility — so the column is recommended, but a production schema change is gated.
Output: decision record; if (a) the migration file, applied schema, builder + admin + type changes with tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-e2e-verification-launch/75-CONTEXT.md
@.planning/phases/75-e2e-verification-launch/75-RESEARCH.md
@.planning/phases/75-e2e-verification-launch/75-05-SUMMARY.md
</context>

## Artifacts this phase produces

- Decision record (SUMMARY): D-11 storage = column + metadata, or metadata only.
- Option (a) only: `supabase/migrations/062_bookings_locale.sql` (column `bookings.locale text NULL`, constraint `bookings_locale_format_check`); `locale` field in `buildBookingRow` / `buildBookingRows` (`lib/supabase.ts`); `locale: string | null` in `types/database.types.ts` bookings Row/Insert/Update; "Language" line in `components/admin/BookingsTable.tsx`.

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Decide D-11 booking-locale storage (column + metadata vs metadata only)</name>
  <decision>How should the booking locale be persisted (D-11)?</decision>
  <context>Plan 75-05 already writes metadata.locale on every Stripe PaymentIntent and forwards it to the server GA4 purchase. The open part of D-11 is "shows booking language in admin". Adding a column is a production schema change on the bookings table (one-way per D-11: undoing it means a DROP COLUMN and losing the recorded languages). The column is additive, nullable and needs no RPC change (admin_search_bookings returns b.*).</context>
  <options>
    <option id="option-a">
      <name>Column + metadata (recommended)</name>
      <pros>Booking language visible in admin/dispatch (driver can prepare), queryable in SQL for revenue-by-language, feeds the payment-link GA4 path; additive nullable column, no RPC touched</pros>
      <cons>Production migration (one-way); must be applied before the deploy in plan 75-19</cons>
    </option>
    <option id="option-b">
      <name>Metadata only</name>
      <pros>No schema change; fully reversible; GA4/CAPI already carry the locale</pros>
      <cons>Language not visible in the admin bookings list (only in the Stripe dashboard); payment-link purchases stay 'en' in GA4</cons>
    </option>
  </options>
  <read_first>
    - .planning/phases/75-e2e-verification-launch/75-CONTEXT.md D-11 (storage is Claude's discretion; one-way if a column is added)
    - .planning/phases/75-e2e-verification-launch/75-05-SUMMARY.md (metadata.locale already persisted)
  </read_first>
  <acceptance_criteria>
    - The human's choice (option-a or option-b) is recorded verbatim in the plan SUMMARY before Task 2 starts.
  </acceptance_criteria>
  <resume-signal>Reply "option-a" or "option-b"</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2 (option a only): Tracer — migration file + booking row locale + admin display, proven by unit tests</name>
  <reversibility rating="one-way">Adds bookings.locale on the production bookings table; undo requires a DROP COLUMN migration and loses every recorded booking language.</reversibility>
  <files>supabase/migrations/062_bookings_locale.sql, lib/supabase.ts, types/database.types.ts, components/admin/BookingsTable.tsx, tests/supabase.test.ts, tests/BookingsTable.test.tsx</files>
  <read_first>
    - supabase/migrations/061_driver_assignments_trip_progress.sql (header comment + ALTER style; latest number is 061)
    - lib/supabase.ts buildBookingRow (~line 62-112), buildBookingRows (~line 346), captureUnpaidBooking (~line 212)
    - types/database.types.ts bookings Row/Insert/Update
    - components/admin/BookingsTable.tsx (Booking interfaces ~lines 31-180 and the expanded-row detail layout)
    - tests/supabase.test.ts, tests/BookingsTable.test.tsx
  </read_first>
  <behavior>
    - buildBookingRow({..., locale: 'ru'}, piId, 'unpaid').locale === 'ru'; with no locale key -> null; buildBookingRows for a round trip sets the same locale on both legs.
    - BookingsTable expanded row renders 'RU' for locale 'ru' and an em dash for null.
  </behavior>
  <action>Skip this task entirely (record "option-b chosen, no schema change") if the decision was option-b. Otherwise: create supabase/migrations/062_bookings_locale.sql with the project header comment (File / Description / Date 2026-09-25), ALTER TABLE public.bookings ADD COLUMN IF NOT EXISTS locale text NULL, and a separate ALTER TABLE ... ADD CONSTRAINT bookings_locale_format_check CHECK (locale IS NULL OR locale ~ '^[a-z]{2}(-[A-Za-z]{2,4})?$'); no RPC, no GRANT changes. In lib/supabase.ts add locale: meta.locale || null to buildBookingRow's returned row and to both legs in buildBookingRows. Add locale: string | null (Row) and locale?: string | null (Insert/Update) to types/database.types.ts bookings. In components/admin/BookingsTable.tsx add locale?: string | null to the booking interfaces and a "Language" detail line (uppercase code or an em dash) in the expanded row. Extend tests/supabase.test.ts and tests/BookingsTable.test.tsx per the behavior list. This code must not be deployed until Task 3 has applied the migration (plan 75-19 has a precondition for it).</action>
  <verify>
    <automated>npx vitest run tests/supabase.test.ts tests/BookingsTable.test.tsx tests/webhooks-stripe.test.ts tests/create-payment-intent.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - (option a) `supabase/migrations/062_bookings_locale.sql` exists with ADD COLUMN IF NOT EXISTS locale and the named CHECK constraint, and contains no DROP, CREATE FUNCTION or GRANT statement.
    - (option a) Tests pass including the empty-locale -> null case and the round-trip both-legs case.
    - (option b) No file in this plan's list is modified and the SUMMARY records the decision.
  </acceptance_criteria>
  <done>(a) The booking language is written on every site-created booking row and shown in admin, pending the live schema push; (b) decision recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: [BLOCKING] Apply migration 062 to the live database via Supabase MCP (option a only)</name>
  <action>[BLOCKING] (option a only) Orchestrator applies migration 062 to the live Supabase database via the Supabase MCP and verifies it</action>
  <instructions>
    Executor subagents have no Supabase MCP access, so the orchestrator performs this step (skip entirely if option-b was chosen).
    1. Call the Supabase MCP apply_migration with name "062_bookings_locale" and the exact SQL body of supabase/migrations/062_bookings_locale.sql.
    2. Verify with execute_sql: select column_name, data_type, is_nullable from information_schema.columns where table_schema='public' and table_name='bookings' and column_name='locale' — expect one row: text, YES.
    3. Verify the constraint: select conname from pg_constraint where conname='bookings_locale_format_check' — expect one row.
    4. Confirm no function grants changed (the migration touches no RPC): select count(*) from information_schema.routine_privileges where routine_name='admin_search_bookings' and grantee in ('PUBLIC','anon','authenticated') — expect 0.
    5. Paste the three query results back.
  </instructions>
  <verification>The executor records the pasted results in the SUMMARY; plan 75-19's precondition checks this record before deploying.</verification>
  <read_first>
    - supabase/migrations/062_bookings_locale.sql (exact SQL to apply)
  </read_first>
  <acceptance_criteria>
    - (option a) The pasted results show the locale column (text, nullable), the bookings_locale_format_check constraint, and 0 PUBLIC/anon/authenticated grants on admin_search_bookings; they are recorded in the SUMMARY.
    - (option b) The SUMMARY records "skipped — option-b".
  </acceptance_criteria>
  <resume-signal>Paste the verification query results, or "skipped — option-b"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application -> production database | Schema change on the live bookings table |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-33 | Tampering | bookings table schema (one-way) | high | mitigate | Human decision checkpoint before any change; additive nullable column with IF NOT EXISTS; format CHECK constraint; applied and verified before deploy |
| T-75-34 | Denial of Service | booking capture after deploy | high | mitigate | Code writing the column is deployed only after the MCP apply is verified (plan 75-19 precondition) — prevents unknown-column insert failures |
| T-75-35 | Elevation of Privilege | RPC grants | low | accept | No RPC is dropped/recreated (admin_search_bookings returns b.*); grant check query confirms no PUBLIC/anon/authenticated EXECUTE |
| T-75-SC | Tampering | package installs | low | accept | No installs |
</threat_model>

<verification>
- (a) Unit tests green; MCP verification results recorded. (b) Decision recorded, no changes.
</verification>

<success_criteria>
D-11's storage decision is made by the human and, if the column is chosen, the booking language is persisted on every site booking, visible in admin, and live in the production schema before any dependent code ships.
</success_criteria>

<output>
Create `.planning/phases/75-e2e-verification-launch/75-17-SUMMARY.md` when done
</output>
