---
phase: 75-e2e-verification-launch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/qa/en_leak_static.mjs
  - scripts/qa/en_leak_allowlist.json
  - scripts/qa/en_leak_rendered.py
  - tests/en-leak-static.test.ts
  - tests/fixtures/en-leak/leaky.tsx
  - tests/fixtures/en-leak/clean.tsx
  - .planning/phases/75-e2e-verification-launch/75-EN-LEAK-AUDIT.md
autonomous: true
requirements: [VER-01]
estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "`node scripts/qa/en_leak_static.mjs` parses every .tsx under app/[locale]/** and components/** (excluding app/admin and allowlisted files) with the TypeScript compiler API and reports, with file:line, (R1) JSX text containing 2+ Latin words that are not DNT terms, (R2) string-literal placeholder / aria-label / alt / title / label attributes with English text, (R3) locale-dropping navigation: an anchor whose href is a root-relative string or template literal, a default import from next/link, a useRouter imported from next/navigation, or a redirect() call from next/navigation with a root-relative string-literal argument; it exits 1 when any non-allowlisted finding exists (D-06 static layer, D-07 surfaces)."
    - "tests/en-leak-static.test.ts proves the scanner flags every rule on tests/fixtures/en-leak/leaky.tsx and reports zero findings on tests/fixtures/en-leak/clean.tsx (useTranslations output, DNT-only text, i18n Link/getPathname hrefs, external/mailto/tel hrefs)."
    - "`python3 scripts/qa/en_leak_rendered.py https://rideprestigo.com` loads every non-EN locale x (21 key pages + extras) after hydration and flags: on ru/ar/hi/zh any visible text node, placeholder, aria-label, alt, title, html title, meta description, og:title/og:description, FAQPage Q/A or route Service name/description with a run of 2+ Latin words not in the allowlist; on es/fr any such string of 3+ words that is byte-identical to the same page's EN rendering; on every non-EN locale any internal anchor whose href drops the locale prefix (D-06 rendered layer, D-07)."
    - "scripts/qa/en_leak_allowlist.json encodes intentional EN as an explicit allowlist with a reason per entry: glossary DNT terms (i18n/glossary.json brand + vehicleClasses + domains), destination/place names, vehicle tier names accepted in EN, D-09 EN-fallback pages (the 3 EN-only JSX posts + any MDX post lacking content/blog/<locale>/<slug>.mdx), D-05 recorded-as-is post-booking surface (/book/confirmation), and Phase 74 D-09 by-design English JSON-LD (BreadcrumbList names, non-route Service nodes) (D-09)."
    - "75-EN-LEAK-AUDIT.md records the pre-fix inventory from both layers (per file / per locale-page counts and concrete strings), mapped to the fixing plan (75-06..75-16) so every leak has an owner."
    - "empty edge: a page with zero text findings still appears in the rendered JSON with an empty list; a locale file-set with no findings exits 0 (never a crash on an empty page or a missing optional attribute)."
    - "adjacency edge: a localized string byte-identical to EN is a leak unless it is DNT/place-name/tier-name allowlisted (e.g. fr 'Prague → Berlin' passes via place names; es 'First Class' passes via tier names) — identical is never silently accepted."
  artifacts:
    - "scripts/qa/en_leak_static.mjs (exports scanSource(code, filename) + CLI)"
    - "scripts/qa/en_leak_rendered.py"
    - "scripts/qa/en_leak_allowlist.json"
    - "tests/en-leak-static.test.ts + tests/fixtures/en-leak/{leaky,clean}.tsx"
    - ".planning/phases/75-e2e-verification-launch/75-EN-LEAK-AUDIT.md"
  key_links:
    - "i18n/glossary.json doNotTranslate -> en_leak_allowlist.json dnt seed (never hand-maintain a divergent brand list)"
    - "en_leak_static.mjs scanSource() R3 rule is imported by tests/locale-links-backstop.test.ts in plan 75-14 — one navigation-leak detector, two consumers"
    - "content/blog/<locale>/ file presence decides which /<locale>/blog/<slug> pages are D-09 EN-fallback (same rule as Phase 74 D-07 hreflang gating)"
  prohibitions:
    - "MUST NOT allowlist a string merely because it is currently English — every allowlist entry carries a reason tied to DNT, place/tier names, D-05, D-09 or Phase 74 D-09."
    - "MUST NOT send analytics hits from the rendered scan (GA4/Meta requests aborted)."
---

<objective>
Build the two-layer EN-leak audit locked by D-06 — a static AST scan of hardcoded English in JSX/props (plus locale-dropping navigation) and a rendered production scan (Latin-script detection for ru/ar/hi/zh, diff-against-EN for es/fr, internal-link locale check) — with the D-09 intentional-EN allowlist, and record the pre-fix inventory.

Purpose: VER-01 "no EN leakage" must be proven systematically, not by the five known strings. Planning-time discovery already found whole EN pages (/fleet, /routes, /book, /book/multi-day), untranslated forms/components and ~210 links that drop the locale prefix; this plan makes those findings reproducible and gives every fix plan (75-06..75-16) a machine check to go green against.
Output: `en_leak_static.mjs` (+ unit test and fixtures), `en_leak_rendered.py`, the allowlist JSON and `75-EN-LEAK-AUDIT.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-e2e-verification-launch/75-CONTEXT.md
@.planning/phases/75-e2e-verification-launch/75-RESEARCH.md
@.planning/phases/75-e2e-verification-launch/75-PATTERNS.md
@scripts/qa/overflow_audit.py
@i18n/glossary.json
</context>

## Artifacts this phase produces

- `scripts/qa/en_leak_static.mjs` — exports `scanSource(code: string, filename: string): Finding[]` (Finding = `{ file, line, rule: 'R1'|'R2'|'R3', text }`) and `scanTree(rootDir): Finding[]`; CLI `node scripts/qa/en_leak_static.mjs [--json out.json] [--rule R3]`.
- `scripts/qa/en_leak_allowlist.json` — keys `dnt`, `placeNames`, `tierNames`, `enFallbackPaths`, `recordedAsIs`, `jsonLdByDesign`, `staticIgnoreFiles`, `linkExempt`; every entry `{ value, reason }`.
- `scripts/qa/en_leak_rendered.py` — CLI `python3 scripts/qa/en_leak_rendered.py [base_url] [--locales ...]`, output `scripts/qa/out/en_leak_rendered.json`.
- `tests/en-leak-static.test.ts`, `tests/fixtures/en-leak/leaky.tsx`, `tests/fixtures/en-leak/clean.tsx`.
- `.planning/phases/75-e2e-verification-launch/75-EN-LEAK-AUDIT.md`.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — AST static scanner + allowlist + fixture tests, run on the repo into the audit inventory</name>
  <files>scripts/qa/en_leak_static.mjs, scripts/qa/en_leak_allowlist.json, tests/en-leak-static.test.ts, tests/fixtures/en-leak/leaky.tsx, tests/fixtures/en-leak/clean.tsx, .planning/phases/75-e2e-verification-launch/75-EN-LEAK-AUDIT.md</files>
  <read_first>
    - i18n/glossary.json (doNotTranslate.brand / vehicleClasses / structural.domains — the DNT seed)
    - lib/routes.ts (destination city names for the placeNames allowlist)
    - scripts/lib/i18n-qa-report.mjs checkNoEnglishLeakage (Phase 72 DNT-exclusion shape to mirror — RESEARCH "Don't Hand-Roll")
    - i18n/routing.ts (Link / redirect / useRouter / getPathname exports — the locale-aware navigation bridge the R3 rule treats as clean)
    - components/Footer.tsx and components/Services.tsx (examples of clean i18n Link usage)
    - app/[locale]/routes/prague-berlin/page.tsx (example of R2 alt literal and R3 raw anchor hrefs to be flagged)
    - .planning/phases/75-e2e-verification-launch/75-RESEARCH.md § Pitfall 6 (walk JSX text nodes — exact-string grep under-reports split headings)
  </read_first>
  <action>Create scripts/qa/en_leak_static.mjs as an ES module using the repo's installed typescript package (require via createRequire; no new dependency). scanSource parses TSX with ts.createSourceFile(ScriptKind.TSX) and walks the AST: R1 = JsxText whose trimmed text, after removing allowlisted DNT/place/tier tokens, still contains 2+ Latin words of 2+ letters and at least one lowercase letter; R2 = JsxAttribute named placeholder, aria-label, alt, title or label with a StringLiteral initializer passing the same English test; R3 = (a) a JsxAttribute href on an intrinsic anchor element whose initializer is a StringLiteral or NoSubstitution/Template literal starting with a single slash (not a double slash, not an asset path with a file extension, not /api/), (b) an ImportDeclaration default-importing from next/link, (c) an import of useRouter from next/navigation, (d) a CallExpression of an identifier imported as redirect from next/navigation whose first argument is a string literal starting with a slash. Object-literal props (title/body/q/a/name/description inside arrays) are reported as R4 severity review only when they are not inside a variable or property whose name matches schema, jsonLd, ld, graph or metadata and not inside generateMetadata — R4 never fails the run, it feeds the inventory. scanTree walks app/[locale] and components recursively (skip app/admin and every file listed in staticIgnoreFiles), returns findings sorted by file then line, and the CLI prints a per-file count table, writes JSON when --json is given, supports --rule to filter, and exits 1 when any R1/R2/R3 finding remains after allowlisting (0 otherwise). Create scripts/qa/en_leak_allowlist.json: dnt seeded from i18n/glossary.json doNotTranslate (brand, vehicleClasses, domains) plus Meta Pixel, WhatsApp, Google, Stripe, Apple; placeNames from lib/routes.ts destination names plus Prague, Czech Republic, Central Europe proper nouns; tierNames First Class, Business Van, PRG · VIE · BER · MUC, EUR — €, CZK — Kč, DIČ / VAT ID; enFallbackPaths the 3 EN-only JSX posts (/blog/prague-vienna-transfer-vs-train, /blog/prague-airport-to-city-center, /blog/prague-airport-taxi-vs-chauffeur) with reason D-09; recordedAsIs /book/confirmation with reason D-05; jsonLdByDesign BreadcrumbList.name and non-route Service.name/description with reason Phase 74 D-09; staticIgnoreFiles the 3 EN-only JSX post page.tsx files (D-09) and app/[locale]/book/confirmation/page.tsx for R1/R2 only (D-05 text recorded as-is; its R3 navigation is still checked); linkExempt anchors carrying an hreflang attribute, the locale switcher menu, the first-visit locale banner, mailto:, tel:, external https URLs, hash-only links, /api/, /_next/ and static file extensions. Every entry has {value, reason}. Write tests/fixtures/en-leak/leaky.tsx (one instance of each R1/R2/R3 variant) and clean.tsx (useTranslations output, DNT-only text like PRESTIGO, an i18n Link, a getPathname href, an external href, a mailto link) and tests/en-leak-static.test.ts asserting scanSource(leaky) returns at least one finding per rule variant with correct line numbers and scanSource(clean) returns none. Finally run the CLI over the repo and create 75-EN-LEAK-AUDIT.md with a "Static layer (pre-fix baseline)" table: file, R1/R2/R3/R4 counts, sample strings, and the owning fix plan (75-06 book page, 75-07 multi-day, 75-08 fleet, 75-09 routes hub, 75-10 route pages, 75-11 service/static pages, 75-12 forms, 75-13 shared components/small pages, 75-14 booking-flow navigation, 75-15 account/auth navigation, 75-16 error localization); any file with findings that maps to no plan is listed under an explicit "UNOWNED" heading.</action>
  <verify>
    <automated>npx vitest run tests/en-leak-static.test.ts && node scripts/qa/en_leak_static.mjs --json scripts/qa/out/en_leak_static.json; test $? -le 1 && test -s scripts/qa/out/en_leak_static.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/en-leak-static.test.ts` passes: every rule variant is detected in leaky.tsx and clean.tsx yields zero findings.
    - `node scripts/qa/en_leak_static.mjs` over the repo exits 1 (pre-fix leaks exist) and flags, among others, `app/[locale]/fleet/page.tsx`, `app/[locale]/book/page.tsx`, `app/[locale]/corporate/CorporateForm.tsx` and at least one `app/[locale]/routes/prague-*/page.tsx` R3 anchor.
    - `scripts/qa/en_leak_allowlist.json` parses as JSON and every entry in every list has non-empty `value` and `reason`.
    - `75-EN-LEAK-AUDIT.md` contains the static table with an owning plan for every flagged file (or an explicit UNOWNED heading).
    - No new package was added to package.json (the scanner uses the already-installed typescript package).
  </acceptance_criteria>
  <done>A tested AST scanner with an explicit reasoned allowlist exists and the full pre-fix static inventory is recorded with plan ownership.</done>
</task>

<task type="auto">
  <name>Task 2: Rendered production scan (Latin detection, es/fr diff, metadata/JSON-LD, internal-link locale) + baseline</name>
  <files>scripts/qa/en_leak_rendered.py, .planning/phases/75-e2e-verification-launch/75-EN-LEAK-AUDIT.md</files>
  <read_first>
    - scripts/qa/overflow_audit.py (browser/consent skeleton, PAGES/LOCS)
    - scripts/qa/en_leak_allowlist.json (Task 1 — the single allowlist both layers read)
    - components/LocaleSwitcher.tsx and components/CookieBanner.tsx (switcher menu + first-visit banner links intentionally point at other locales — linkExempt)
    - content/blog (which slugs exist per locale — D-09 EN-fallback detection)
    - .planning/phases/75-e2e-verification-launch/75-CONTEXT.md D-06, D-07, D-09
  </read_first>
  <action>Create scripts/qa/en_leak_rendered.py (Playwright; overflow_audit.py skeleton; argparse positional base_url, --locales default ru,es,fr,ar,hi,zh; analytics requests aborted as in plan 75-01). Page set: overflow_audit.py PAGES plus /authors/roman-ustyugov, one translated MDX blog post that exists for every locale under content/blog, one D-09 EN-only JSX post, a deliberately missing URL (/this-page-does-not-exist) and /book/confirmation. For each non-EN locale and page: load, wait for hydration (load + 1500 ms), and collect (a) visible text nodes (skip script/style/noscript, hidden elements, the switcher menu), (b) placeholder, aria-label, alt, title attributes, (c) document title, meta description, og:title, og:description, (d) ld+json FAQPage question/answer text and route Service name/description. For ru/ar/hi/zh flag any string containing a run of 2+ Latin words (3+ letters) left after removing allowlisted dnt/placeNames/tierNames tokens. For es/fr also load the EN page once and flag any collected string of 3+ words byte-identical to an EN-page string, minus allowlist. Link check (all non-EN locales): every anchor whose href starts with a single slash must start with /{locale}/ or equal /{locale}, unless linkExempt. Pages in enFallbackPaths (resolved per locale by checking content/blog/<locale>/<slug>.mdx existence for MDX posts) and recordedAsIs are scanned but their findings are labelled allowlisted with the reason, never counted as leaks. Output scripts/qa/out/en_leak_rendered.json keyed locale -> page -> {leaks, allowlisted, linkLeaks} in deterministic order; stdout summary per locale; exit 1 when any non-allowlisted leak or link leak exists. Run it against production and append a "Rendered layer (pre-fix baseline)" section to 75-EN-LEAK-AUDIT.md: per-locale and per-page counts, representative strings, link-leak counts, and the owning fix plan for each page; note separately what is verified only at the booking E2E layer (Stripe Elements locale, Google Places language — plan 75-03/75-05).</action>
  <verify>
    <automated>python3 scripts/qa/en_leak_rendered.py https://rideprestigo.com --locales ru,fr; test $? -le 1 && python3 -c "import json;d=json.load(open('scripts/qa/out/en_leak_rendered.json'));assert 'ru' in d and 'fr' in d;print('rendered scan OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/qa/out/en_leak_rendered.json` contains `ru` and `fr` entries covering all PAGES plus the extras, each page carrying `leaks`, `allowlisted` and `linkLeaks` lists (possibly empty).
    - Against current production the ru scan reports leaks on `/fleet` and `/book` and link leaks on `/routes/prague-vienna` (pre-fix state confirmed at planning time) — proving the detector works.
    - D-09 EN-only JSX post findings appear under `allowlisted` with reason D-09, not under `leaks`.
    - `75-EN-LEAK-AUDIT.md` has the rendered-layer section for all 6 non-EN locales with owning plans.
  </acceptance_criteria>
  <done>The rendered-layer scanner reproduces the known production leaks, honors the D-09/D-05 allowlist, and the full pre-fix inventory is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QA runner -> production | Rendered scan loads production pages with third-party tags |
| Allowlist file -> verification verdict | A permissive allowlist can hide real leaks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-04 | Repudiation | en_leak_allowlist.json | medium | mitigate | Every entry requires a reason tied to DNT/D-05/D-09/74 D-09; fixture tests prove detection still fires on leaky input |
| T-75-05 | Tampering | production analytics | medium | mitigate | GA4/Meta request abort route in the rendered scan (same as plan 75-01) |
| T-75-SC | Tampering | package installs | low | accept | No installs — TypeScript compiler API from the existing devDependency, Playwright already installed |
</threat_model>

<verification>
- `npx vitest run tests/en-leak-static.test.ts` green.
- Both scanners run against the repo / production and reproduce the known leaks.
- `75-EN-LEAK-AUDIT.md` lists every finding with an owner plan.
</verification>

<success_criteria>
D-06's two-layer audit exists as repeatable scripts with a reasoned D-09 allowlist, and the complete pre-fix leak inventory (text, attributes, metadata/JSON-LD, internal-link locale) is recorded and mapped to fixing plans.
</success_criteria>

<output>
Create `.planning/phases/75-e2e-verification-launch/75-02-SUMMARY.md` when done
</output>
