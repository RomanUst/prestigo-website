---
phase: 75-e2e-verification-launch
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - app/[locale]/routes/page.tsx
  - content/pages/en/routes.json
  - content/pages/ru/routes.json
  - content/pages/es/routes.json
  - content/pages/fr/routes.json
  - content/pages/ar/routes.json
  - content/pages/hi/routes.json
  - content/pages/zh/routes.json
  - tests/routes-hub-render.test.tsx
  - tests/__snapshots__/routes-hub-render.test.tsx.snap
  - .planning/phases/75-e2e-verification-launch/freeze/75-09.freeze
autonomous: true
requirements: [VER-01]
estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every visible English string on the /routes hub (hero, 'Most popular routes' cards chrome, 'Why a private transfer beats the train', country grouping headings and labels, how-it-works, 'Borders, tolls & paperwork', 'Luggage, pets & children', long-distance 'Quote on' block, FAQ, CTA buttons, hero alt) and the metadata title/description come from content/pages/<locale>/routes.json via getPageContent('routes', locale) with a params-derived locale (D-06, D-07, D-08)."
    - "Destination and country names shown on the hub come from destinationNames (slug -> name) and countryNames (EN country -> name) maps in routes.json whose EN values equal today's lib/routes.ts values, so EN output is unchanged while each locale shows its own exonyms (consistent with content/routes/<locale>/<slug>.json hero.label)."
    - "The EN /routes render is byte-for-byte identical to the pre-change golden snapshot (prices come from the same fixture in the test)."
    - "The /ru/routes render and metadata title are Russian (no 'beats the train', no 'Most popular routes'), and every route card / CTA link on /ru/routes starts with /ru/ (getPathname), including the template-literal /routes/<slug> card links."
    - "The FAQPage JSON-LD is built from the localized FAQ items (plain strings) and equals the visible FAQ per locale; Service/Breadcrumb names stay English by design (Phase 74 D-09)."
    - "All 6 non-EN locale files match the EN key set, types and array lengths (30 destinationNames entries, same FAQ count) (D-08)."
  artifacts:
    - "content/pages/{en,ru,es,fr,ar,hi,zh}/routes.json"
    - "tests/routes-hub-render.test.tsx + snapshot"
    - ".planning/phases/75-e2e-verification-launch/freeze/75-09.freeze"
  key_links:
    - "app/[locale]/routes/page.tsx -> getPageContent('routes', locale) -> content/pages/<locale>/routes.json"
    - "routes.json destinationNames[slug] must agree with content/routes/<locale>/<slug>.json hero.label destination exonym (single naming per locale)"
  prohibitions:
    - "MUST NOT run node scripts/i18n-translate.mjs --dry-run (or any non --check mode) on the repo tree (D-08)."
    - "MUST NOT change prices, distances, durations or slugs (locale-invariant data stays in lib/routes.ts / route_prices — 71 D-02)."
---

<objective>
Localize the `/routes` hub page through the content model — prose, FAQ, CTAs, metadata, and per-locale destination/country names — with every card/CTA link kept in the visitor's locale (D-06/D-07/D-08).

Purpose: the hub is the entry point to all 30 intercity routes; today `/ru/routes` etc. are fully English (title included) and every card link drops the locale prefix, sending localized visitors to English route pages.
Output: 7 `routes.json` files, a content-driven hub with locale-aware links, EN byte-parity + RU render test, freeze patterns.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-e2e-verification-launch/75-CONTEXT.md
@.planning/phases/75-e2e-verification-launch/75-RESEARCH.md
@.planning/phases/75-e2e-verification-launch/75-PATTERNS.md
</context>

## Translation rules (D-08) — apply to every in-session translation in this plan

1. You are the translator (Anthropic credit is empty; the i18n Translate workflow is disabled). Never run `node scripts/i18n-translate.mjs` in any mode other than `--check`.
2. Keep verbatim (i18n/glossary.json doNotTranslate): Prestigo, PRESTIGO, chelautotrans s.r.o., Mercedes-Benz, E-Class, S-Class, V-Class, rideprestigo.com, phone numbers, € / Kč amounts, times, and every `{placeholder}`.
3. Destination names: copy the exonym from `content/routes/<locale>/<slug>.json` `hero.label` (the part after the arrow) so hub and route page agree; country names in the locale's standard form.
4. Register: premium, plain, short sentences; zh = Simplified Chinese; ar = Modern Standard Arabic; hi = Devanagari Hindi with brand tokens kept in Latin.
5. Structure: identical key set, value types, array lengths and order to EN; nothing left in English except DNT-only values.
6. EN strings are moved verbatim; EN rendered output must stay byte-identical.
7. Freeze file format: one pattern per line (`<sourceKey>` or `<sourceKey>::<dotPathPrefix>`), `#` comments; applied by plan 75-18.

## Artifacts this phase produces

- `content/pages/<locale>/routes.json` (7 files) — sections `hero`, `popular{label,headingLine1,headingItalic,fromLabels{eClass,vClass,sClass},viewRoute}`, `whyPrivate`, `countries{label,intro,...}`, `destinationNames{<slug>: string}` (30), `countryNames{<EN country>: string}`, `howItWorks`, `borders`, `luggage`, `longDistance`, `faq{heading,items[{q,a}]}`, `cta`, `imageAlt`, `metadata`.
- `tests/routes-hub-render.test.tsx` + snapshot.
- `.planning/phases/75-e2e-verification-launch/freeze/75-09.freeze` containing `content/pages/en/routes.json`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — EN golden snapshot, hub content model with localized names + locale-aware links, ru proven by render</name>
  <files>tests/routes-hub-render.test.tsx, tests/__snapshots__/routes-hub-render.test.tsx.snap, content/pages/en/routes.json, content/pages/ru/routes.json, app/[locale]/routes/page.tsx</files>
  <read_first>
    - app/[locale]/routes/page.tsx (full file, 490 lines: getLocale() usage, getAllRoutes() prices, ROUTES grouping by country, cards with the Prague arrow label, FAQ + FAQPage JSON-LD ~line 80-95, CTA anchors, card links built with a template literal)
    - lib/routes.ts (ROUTES slug/toLabel/country fields)
    - tests/helpers/route-content-parity.ts (FIXTURE_PRICES to mock getAllRoutes deterministically)
    - tests/route-page-render.test.tsx (render + mocks + priceValidUntil normalization)
    - content/routes/ru/prague-berlin.json (hero.label exonym format)
    - i18n/routing.ts (getPathname)
  </read_first>
  <behavior>
    - The unchanged hub rendered for 'en' (Nav, Footer, next/image mocked, getAllRoutes mocked with fixture prices) produces the golden snapshot, committed first.
    - After refactor the EN render equals the golden snapshot byte-for-byte.
    - The 'ru' render contains the ru hero headline, the ru name for Berlin from destinationNames and the ru first FAQ question; it contains neither 'beats the train' nor 'Most popular routes'; every href beginning with a single slash in the ru render starts with /ru/.
    - ru FAQPage first question equals the ru visible first question; generateMetadata('ru') returns the ru title.
  </behavior>
  <action>Create tests/routes-hub-render.test.tsx per tests/route-page-render.test.tsx (mock Nav, Footer, next/image, and @/lib/route-prices getAllRoutes with FIXTURE_PRICES; keep Reveal/Divider pass-through), render the UNCHANGED hub for 'en', commit the golden snapshot alone. Create content/pages/en/routes.json with every visible English string plus metadata, verbatim, and destinationNames (slug -> current toLabel) and countryNames (current country -> same string) maps; prices, distances, durations, slugs, grouping order and images stay in code. Refactor: replace the parameterless next-intl server locale lookup with const { locale } = await params in generateMetadata and the page (static-safe WINDOWS #7 pattern); load getPageContent('routes', locale) narrowed to a local type; metadata from content; getAlternates('/routes', { indexable: true, content: { kind: 'page', key: 'routes' }, locale }); card labels and country headings from destinationNames/countryNames (EN values identical, so EN parity holds); FAQPage from content.faq.items as plain strings; every internal href (including template-literal /routes/<slug> card links and the CTA anchors) through getPathname({ locale, href }). Write content/pages/ru/routes.json per the Translation rules; add the ru assertions including the href-prefix check.</action>
  <verify>
    <automated>npx vitest run tests/routes-hub-render.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Golden snapshot commit precedes the refactor commit.
    - `npx vitest run tests/routes-hub-render.test.tsx` passes (EN unchanged; ru text, names, FAQ schema, metadata and hrefs localized).
    - `grep -c "getLocale()" app/[locale]/routes/page.tsx` is 0 and `grep -n "key: 'routes'" app/[locale]/routes/page.tsx` matches.
    - `grep -c "beats the train" app/[locale]/routes/page.tsx` is 0.
  </acceptance_criteria>
  <done>The routes hub renders from the content model with localized names and locale-preserving links, EN byte-identical, Russian proven.</done>
</task>

<task type="auto">
  <name>Task 2: es/fr/ar/hi/zh translations, parity test and freeze patterns</name>
  <files>content/pages/es/routes.json, content/pages/fr/routes.json, content/pages/ar/routes.json, content/pages/hi/routes.json, content/pages/zh/routes.json, tests/routes-hub-render.test.tsx, .planning/phases/75-e2e-verification-launch/freeze/75-09.freeze</files>
  <read_first>
    - content/pages/en/routes.json and content/pages/ru/routes.json (Task 1)
    - content/routes/{es,fr,ar,hi,zh}/*.json hero.label (destination exonyms per locale)
    - i18n/glossary.json
  </read_first>
  <action>Translate in-session into es, fr, ar, hi, zh per the Translation rules, taking every destinationNames value from the matching content/routes/<locale>/<slug>.json hero.label. Extend the test: 6-locale parity block (key set, types, 30 destinationNames, array lengths, no non-DNT/non-place value identical to EN, DNT/€ reappearance), a consistency check that each locale's destinationNames[slug] occurs inside that locale's route file hero.label, and ar/zh render assertions (hero present, EN hero absent, all single-slash hrefs prefixed with the locale). Create freeze/75-09.freeze with content/pages/en/routes.json.</action>
  <verify>
    <automated>npx vitest run tests/routes-hub-render.test.tsx && test -s .planning/phases/75-e2e-verification-launch/freeze/75-09.freeze</automated>
  </verify>
  <acceptance_criteria>
    - All 7 locale files exist and parse; parity + name-consistency pass; ar and zh render assertions pass.
    - `freeze/75-09.freeze` contains `content/pages/en/routes.json`.
  </acceptance_criteria>
  <done>The routes hub is fully translated with names consistent with each route page and queued for the manifest freeze.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Content JSON -> rendered page / FAQPage JSON-LD | In-session translations rendered and serialized |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-17 | Tampering (misinformation) | prices / durations on route cards | medium | mitigate | Prices/distances stay in code + route_prices; parity test asserts DNT/€ reappearance in translated prose |
| T-75-21 | Tampering (navigation) | card/CTA links | low | mitigate | All internal hrefs built with getPathname; test asserts locale prefix |
| T-75-SC | Tampering | package installs | low | accept | No installs |
</threat_model>

<verification>
- `npx vitest run tests/routes-hub-render.test.tsx` green; `npx tsc --noEmit` clean.
</verification>

<success_criteria>
/routes is localized in every locale with locale-preserving links and consistent destination names, EN byte-identical.
</success_criteria>

<output>
Create `.planning/phases/75-e2e-verification-launch/75-09-SUMMARY.md` when done
</output>
