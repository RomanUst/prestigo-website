No external API integration: Phase 75 only threads a site-locale value through already-integrated services (GA4 gtag + Measurement Protocol, Meta Pixel + CAPI, Stripe Elements / PaymentIntent metadata, Google Places autocomplete) and runs read-only verification scripts (Playwright against production, GSC URL Inspection with the existing read-only token); Metricool drafts are created through the already-connected Metricool MCP — no new API surface is added.
