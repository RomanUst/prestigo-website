---
phase: 75-e2e-verification-launch
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - app/[locale]/corporate/CorporateForm.tsx
  - components/ContactForm.tsx
  - app/[locale]/contact/page.tsx
  - content/pages/en/contact.json
  - content/pages/ru/contact.json
  - content/pages/es/contact.json
  - content/pages/fr/contact.json
  - content/pages/ar/contact.json
  - content/pages/hi/contact.json
  - content/pages/zh/contact.json
  - messages/en.json
  - messages/ru.json
  - messages/es.json
  - messages/fr.json
  - messages/ar.json
  - messages/hi.json
  - messages/zh.json
  - tests/corporate-form.test.tsx
  - tests/contact-form.test.tsx
  - .planning/phases/75-e2e-verification-launch/freeze/75-12.freeze
autonomous: true
requirements: [VER-01]
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CorporateForm is fully externalized into a new Corporate.form namespace (every label, placeholder, select option incl. an ICU '{count}' trips-per-month template, submit/sending text, success heading/body/link, and all three client error messages: rate-limited, invalid input, generic) — rendered through useTranslations, zero hardcoded English left (D-07/D-08; RESEARCH Pitfall 5: the whole form, not just the notes placeholder)."
    - "ContactForm is fully externalized into a ContactForm namespace (heading, response-time note, labels, required markers' text, placeholders, submit/sending, success and error states) while its GA4 form_id/form_name analytics identifiers stay byte-identical."
    - "The /contact page's detail labels (Phone, Email, Location, Availability, Legal entity) and location/availability lines come from content/pages/<locale>/contact.json; the legal-entity block (chelautotrans s.r.o., IČO, address) and phone/email stay verbatim DNT; internal links keep the locale (getPathname)."
    - "messages/{ru,es,fr,ar,hi,zh}.json contain every new key with a translated value (node scripts/i18n-translate.mjs --check passes after commit — completeness + value-type parity)."
    - "Rendering CorporateForm and ContactForm with ru messages shows no English label, placeholder, button or error text; a 429 response in CorporateForm shows the translated rate-limit message."
    - "EN rendering of both forms and /contact is text-identical to before (strings moved verbatim)."
  artifacts:
    - "messages/*.json namespaces Corporate.form and ContactForm"
    - "content/pages/<locale>/contact.json details labels"
    - "tests/corporate-form.test.tsx, tests/contact-form.test.tsx"
    - ".planning/phases/75-e2e-verification-launch/freeze/75-12.freeze"
  key_links:
    - "app/[locale]/corporate/CorporateForm.tsx useTranslations('Corporate.form') -> messages/<locale>.json Corporate.form"
    - "components/ContactForm.tsx useTranslations('ContactForm') -> messages/<locale>.json ContactForm"
    - "messages/*.json edits are serialized across plans 75-12 -> 75-13 -> 75-16 (different waves) to avoid concurrent writes"
  prohibitions:
    - "MUST NOT run node scripts/i18n-translate.mjs --dry-run on the repo tree (D-08)."
    - "MUST NOT rename or translate GA4/Meta analytics identifiers (form_id, form_name, event names)."
---

<objective>
Externalize and translate the two lead-capture forms (CorporateForm, ContactForm) and the /contact detail labels (D-07/D-08) — the first of three sequential `messages/*.json` plans.

Purpose: CorporateForm has zero i18n wiring (every label, option, error and success string is English) and ContactForm/contact labels are English on every locale — both are conversion surfaces, and their error states are user-visible leaks the rendered scan cannot trigger.
Output: `Corporate.form` and `ContactForm` namespaces in all 7 catalogs, contact.json labels, component tests, freeze patterns.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-e2e-verification-launch/75-CONTEXT.md
@.planning/phases/75-e2e-verification-launch/75-RESEARCH.md
@.planning/phases/75-e2e-verification-launch/75-PATTERNS.md
@.planning/phases/69-string-externalization-ui-chrome/69-01-SUMMARY.md
</context>

## Translation rules (D-08) — apply to every in-session translation in this plan

1. You are the translator (Anthropic credit is empty; the i18n Translate workflow is disabled). Never run `node scripts/i18n-translate.mjs` in any mode other than `--check`.
2. Keep verbatim: Prestigo, PRESTIGO, chelautotrans s.r.o., IČO values, addresses, rideprestigo.com, info@rideprestigo.com, phone numbers, WhatsApp, and every ICU `{placeholder}` (plural/select syntax unchanged).
3. Register: premium, plain, short; zh = Simplified; ar = MSA; hi = Devanagari.
4. Namespace convention (Phase 69 lock): PascalCase component namespaces, camelCase semantic keys, JSON arrays for ordered lists, named ICU args.
5. EN strings are moved verbatim.
6. Freeze file format: one pattern per line (`<sourceKey>` or `<sourceKey>::<dotPathPrefix>`), `#` comments; applied by plan 75-18.

## Artifacts this phase produces

- `messages/<locale>.json` namespace `Corporate.form` (~20 keys incl. `tripsPerMonth` ICU with `{count}`, `errors.rateLimited|invalidInput|generic`, `success.*`).
- `messages/<locale>.json` namespace `ContactForm` (~15 keys incl. `errors.*`, `success.*`).
- `content/pages/<locale>/contact.json` `details{phoneLabel,emailLabel,locationLabel,locationLines[],availabilityLabel,availability,legalEntityLabel}`.
- `tests/corporate-form.test.tsx`, `tests/contact-form.test.tsx`.
- `.planning/phases/75-e2e-verification-launch/freeze/75-12.freeze`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — CorporateForm fully externalized (7 catalogs), proven by ru render incl. error path</name>
  <files>app/[locale]/corporate/CorporateForm.tsx, messages/en.json, messages/ru.json, messages/es.json, messages/fr.json, messages/ar.json, messages/hi.json, messages/zh.json, tests/corporate-form.test.tsx</files>
  <read_first>
    - app/[locale]/corporate/CorporateForm.tsx (full file, 108 lines — every hardcoded string incl. the three setErrorMessage branches)
    - tests/helpers/renderWithIntl.tsx (renderWithIntl(ui, { locale, messages }))
    - messages/en.json top-level namespace layout (add Corporate as a new top-level namespace with a form child)
    - tests/i18n-catalog-array-types.test.ts (array/type parity rules the catalogs must satisfy)
  </read_first>
  <behavior>
    - renderWithIntl(CorporateForm) with en messages renders the same visible strings as before (labels, placeholders, options, button).
    - With ru messages (import messages/ru.json) no English label/placeholder/button text is present (compare against the EN catalog values).
    - fetch mocked to 429 -> the ru rate-limit message is shown; 400 -> ru invalid-input message; network throw -> ru generic message.
    - The trips-per-month option labels render via ICU with the numeric bucket values unchanged.
  </behavior>
  <action>Add useTranslations('Corporate.form') to CorporateForm and replace every user-visible literal (labels, placeholders, select option labels, the per-month template as an ICU string with a named count argument, submit and sending states, success block, and the three error strings mapped by status 429/400/other and the catch branch) with t() calls; option values sent to the API stay unchanged. Add the Corporate.form namespace to messages/en.json with the exact current English strings, and to the other 6 catalogs translated per the Translation rules (same keys, same types). Write tests/corporate-form.test.tsx covering the behavior list (mock global fetch per case).</action>
  <verify>
    <automated>npx vitest run tests/corporate-form.test.tsx tests/i18n-catalog-array-types.test.ts tests/corporate-contact.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/corporate-form.test.tsx` passes all behavior cases; catalog array-type and corporate-contact API tests still pass.
    - `grep -c "useTranslations('Corporate.form')" app/[locale]/corporate/CorporateForm.tsx` is 1.
    - `node -e "for (const l of ['ru','es','fr','ar','hi','zh']) { const m=require('./messages/'+l+'.json'); if(!m.Corporate||!m.Corporate.form) throw l }"` exits 0.
  </acceptance_criteria>
  <done>CorporateForm renders entirely from the catalogs in every locale, including its error and success states, proven by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ContactForm namespace + /contact detail labels + locale links + freeze patterns</name>
  <files>components/ContactForm.tsx, app/[locale]/contact/page.tsx, content/pages/en/contact.json, content/pages/ru/contact.json, content/pages/es/contact.json, content/pages/fr/contact.json, content/pages/ar/contact.json, content/pages/hi/contact.json, content/pages/zh/contact.json, messages/en.json, messages/ru.json, messages/es.json, messages/fr.json, messages/ar.json, messages/hi.json, messages/zh.json, tests/contact-form.test.tsx, .planning/phases/75-e2e-verification-launch/freeze/75-12.freeze</files>
  <read_first>
    - components/ContactForm.tsx (full file; pushGA4Event form_start/form_submit/generate_lead identifiers to keep)
    - app/[locale]/contact/page.tsx (details block ~line 113-150, internal anchors, how content is loaded)
    - content/pages/en/contact.json (existing sections: metadata, hero, whatsapp, whatHappensNext, commonEnquiries, alsoUseful)
    - lib/localized-href.ts if present (plan 75-11) — otherwise use getPathname from @/i18n/routing directly
  </read_first>
  <behavior>
    - ContactForm with ru messages shows no English heading/label/placeholder/button/success/error text; GA4 form_name remains the English identifier.
    - The /contact page rendered for ru shows ru detail labels; the legal-entity block and phone/email are unchanged; every single-slash href starts with /ru/.
  </behavior>
  <action>Externalize ContactForm into a ContactForm namespace (all user-visible strings; required-field asterisk markup stays structural) and add it to all 7 catalogs (EN verbatim, 6 translated). Add a details section to the 7 contact.json files for the Phone/Email/Location/Availability/Legal entity labels and the location/availability lines (legal-entity text, phone and email stay literal DNT in code), and render them from content in the contact page; route every internal href on the page through getPathname (or localizedHref when available). Write tests/contact-form.test.tsx (ContactForm under en/ru with fetch mocked for success/error) plus a contact-page render assertion for ru labels and href prefixes. Create freeze/75-12.freeze with: messages/en.json::Corporate.form, messages/en.json::ContactForm, content/pages/en/contact.json::details.</action>
  <verify>
    <automated>npx vitest run tests/contact-form.test.tsx tests/corporate-form.test.tsx tests/pages-a.test.ts tests/i18n-catalog-array-types.test.ts && npx tsc --noEmit && test $(grep -c "::" .planning/phases/75-e2e-verification-launch/freeze/75-12.freeze) -ge 3</automated>
  </verify>
  <acceptance_criteria>
    - All listed tests pass; `npx tsc --noEmit` clean.
    - `grep -c "useTranslations('ContactForm')" components/ContactForm.tsx` is 1 and `grep -n "form_name" components/ContactForm.tsx` still shows the unchanged English identifier.
    - `freeze/75-12.freeze` contains the 3 patterns.
  </acceptance_criteria>
  <done>Both forms and the /contact details are localized in all 7 locales with analytics identifiers intact, tested, and queued for the manifest freeze.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form -> /api/corporate-contact, /api/contact | Unchanged request contracts; only UI text changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-24 | Tampering | form submission payload | low | mitigate | Option values and field names posted to the APIs are unchanged (only labels move to catalogs); existing corporate-contact API test must still pass |
| T-75-25 | Repudiation | analytics identifiers | low | mitigate | GA4 form_id/form_name kept verbatim; asserted by test/grep |
| T-75-SC | Tampering | package installs | low | accept | No installs |
</threat_model>

<verification>
- `npx vitest run tests/corporate-form.test.tsx tests/contact-form.test.tsx tests/corporate-contact.test.ts tests/pages-a.test.ts tests/i18n-catalog-array-types.test.ts` green; `npx tsc --noEmit` clean.
</verification>

<success_criteria>
The corporate and contact forms (incl. error/success states) and contact details show no English on localized pages, EN unchanged, analytics untouched.
</success_criteria>

<output>
Create `.planning/phases/75-e2e-verification-launch/75-12-SUMMARY.md` when done
</output>
