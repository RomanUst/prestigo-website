---
phase: 75-e2e-verification-launch
plan: 16
type: execute
wave: 3
depends_on: [75-05, 75-13]
files_modified:
  - app/api/create-payment-intent/route.ts
  - app/api/validate-promo/route.ts
  - components/booking/steps/Step6Payment.tsx
  - components/booking/steps/Step3Auth.tsx
  - app/[locale]/account/reset-password/page.tsx
  - lib/auth-error-code.ts
  - messages/en.json
  - messages/ru.json
  - messages/es.json
  - messages/fr.json
  - messages/ar.json
  - messages/hi.json
  - messages/zh.json
  - tests/create-payment-intent.test.ts
  - tests/validate-promo.test.ts
  - tests/Step6Payment.test.tsx
  - tests/Step3Auth.test.tsx
  - tests/auth-error-code.test.ts
  - tests/reset-password-locale.test.tsx
  - .planning/phases/75-e2e-verification-launch/freeze/75-16.freeze
autonomous: true
requirements: [VER-01]
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every user-visible error returned by /api/create-payment-intent (rate limit, invalid request/trip type/vehicle, too many passengers, custom-quote route, 12-hour lead time, round-trip date rules, promo invalid, pricing unavailable, internal) carries a stable machine code next to the existing English error string, and Step6Payment shows t('errors.<code>') from Booking.step6.errors — never the raw English server text on any locale (D-07 'user-visible API error messages')."
    - "/api/validate-promo returns codes (rate limited, no code, invalid, internal) and Step6Payment's promo branch renders the translated message for each."
    - "empty edge: an error response with no code or an unknown code renders the translated generic fallback (paymentInitFailed / promoInvalid), never an empty message and never the raw server string."
    - "Supabase auth errors surfaced in Step3Auth registration and the reset-password page are mapped by lib/auth-error-code.ts (known AuthError codes -> Errors.auth.<key>, unknown -> generic) and rendered translated; the raw GoTrue English message is no longer interpolated into the UI."
    - "Step3Auth's magic-link emailRedirectTo return-to and the reset-password success navigation keep the locale (getPathname / i18n router)."
    - "The HTTP status codes and the English error field of both APIs are unchanged (backward compatible for logs and existing tests)."
    - "All 7 catalogs contain the new Booking.step6.errors and Errors.auth keys translated (--check completeness + type parity)."
  artifacts:
    - "code field on create-payment-intent and validate-promo error responses"
    - "lib/auth-error-code.ts (authErrorKey(code))"
    - "messages/*.json Booking.step6.errors.*, Errors.auth.*"
    - ".planning/phases/75-e2e-verification-launch/freeze/75-16.freeze"
  key_links:
    - "create-payment-intent JSON { error, code } -> Step6Payment t(`errors.${code}`) with generic fallback"
    - "Supabase AuthError.code -> authErrorKey() -> Errors.auth.<key>"
  prohibitions:
    - "MUST NOT change API status codes or remove the English error field (clients and tests rely on them)."
    - "MUST NOT echo raw server/GoTrue error text into localized UI."
    - "MUST NOT run node scripts/i18n-translate.mjs --dry-run on the repo tree (D-08)."
---

<objective>
Stop raw English server/auth error text from reaching localized users (D-07 "user-visible API error messages"): add stable error codes to the booking APIs and map them — and Supabase auth error codes — to translated catalog messages; also keep the locale on the Step3Auth magic-link return and the reset-password redirect. Third and last sequential `messages/*.json` plan.

Purpose: a Russian customer who books less than 12 hours ahead or enters an expired promo code today sees the English server sentence verbatim inside the payment step.
Output: coded API errors, mapped client rendering in 7 locales, auth error mapper, tests, freeze patterns.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-e2e-verification-launch/75-CONTEXT.md
@.planning/phases/75-e2e-verification-launch/75-05-SUMMARY.md
</context>

## Translation rules (D-08) — apply to every in-session translation in this plan

1. You are the translator (Anthropic credit is empty; the i18n Translate workflow is disabled). Never run `node scripts/i18n-translate.mjs` in any mode other than `--check`.
2. Keep verbatim numbers ("12 hours" stays the numeral 12), brand tokens and every ICU `{placeholder}`.
3. Register: short, calm, actionable; zh = Simplified; ar = MSA; hi = Devanagari.
4. EN catalog values reuse the current English server sentences verbatim where one exists.
5. Freeze file format: one pattern per line (`<sourceKey>` or `<sourceKey>::<dotPathPrefix>`), `#` comments; applied by plan 75-18.

## Artifacts this phase produces

- Error `code` values: `RATE_LIMITED`, `INVALID_REQUEST`, `TOO_MANY_PASSENGERS`, `CUSTOM_QUOTE_REQUIRED`, `LEAD_TIME`, `ROUND_TRIP_DATES`, `RETURN_BEFORE_PICKUP`, `PROMO_INVALID`, `PRICING_UNAVAILABLE`, `INTERNAL` (create-payment-intent); `RATE_LIMITED`, `NO_CODE`, `PROMO_INVALID`, `INTERNAL` (validate-promo).
- `lib/auth-error-code.ts` — `authErrorKey(code: string | undefined): 'userExists' | 'weakPassword' | 'emailRateLimited' | 'invalidEmail' | 'signupDisabled' | 'sessionExpired' | 'generic'`.
- Catalog keys `Booking.step6.errors.*` and `Errors.auth.*` in all 7 locales.
- Tests: `tests/auth-error-code.test.ts`, `tests/reset-password-locale.test.tsx` (new); extended create-payment-intent, validate-promo, Step6Payment, Step3Auth tests.
- `.planning/phases/75-e2e-verification-launch/freeze/75-16.freeze`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — LEAD_TIME error end-to-end: API code -> Step6Payment translated message, then all booking API codes</name>
  <files>app/api/create-payment-intent/route.ts, app/api/validate-promo/route.ts, components/booking/steps/Step6Payment.tsx, messages/en.json, messages/ru.json, messages/es.json, messages/fr.json, messages/ar.json, messages/hi.json, messages/zh.json, tests/create-payment-intent.test.ts, tests/validate-promo.test.ts, tests/Step6Payment.test.tsx</files>
  <read_first>
    - app/api/create-payment-intent/route.ts (every NextResponse.json error branch: 429, 400 variants, lead-time, promo, 503, 500)
    - app/api/validate-promo/route.ts (four error branches)
    - components/booking/steps/Step6Payment.tsx (setPaymentError with the server error text; setPromoError with the server error text; t = useTranslations('Booking.step6'))
    - tests/create-payment-intent.test.ts, tests/validate-promo.test.ts, tests/Step6Payment.test.tsx
  </read_first>
  <behavior>
    - create-payment-intent returns { error: <unchanged English>, code: 'LEAD_TIME' } with the same status for a pickup < 12 h ahead; each other branch returns its code with unchanged status and error text.
    - validate-promo returns { valid: false, error: <unchanged>, code } for each failure branch.
    - Step6Payment rendered under ru with fetch returning { error: 'Bookings must be made at least 12 hours in advance.', code: 'LEAD_TIME' } shows the ru LEAD_TIME message and not the English sentence; with no code -> ru paymentInitFailed; with code 'SOMETHING_NEW' -> ru paymentInitFailed.
    - Promo apply under ru with code PROMO_INVALID shows the ru promo-invalid message; unknown code -> ru promoInvalid.
  </behavior>
  <action>Add a code field to every error response of both routes (status codes and English error strings unchanged) using the code list in Artifacts. In Step6Payment, replace the two places that display the server error text with a lookup: when data.code is a known key under Booking.step6.errors use t('errors.' + code), otherwise the existing generic key (paymentInitFailed for the payment-intent branch, promoInvalid for promo); keep console.error logging of the raw server text for debugging. Add Booking.step6.errors to all 7 catalogs (EN = the current server sentences verbatim; 6 translated). Extend the three test files with the behavior cases.</action>
  <verify>
    <automated>npx vitest run tests/create-payment-intent.test.ts tests/validate-promo.test.ts tests/Step6Payment.test.tsx tests/i18n-catalog-array-types.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests pass with every behavior case.
    - `grep -n "setPaymentError(data.error" components/booking/steps/Step6Payment.tsx` and `grep -n "setPromoError(data.error" components/booking/steps/Step6Payment.tsx` return nothing.
    - `grep -c "code:" app/api/create-payment-intent/route.ts` is at least 10.
  </acceptance_criteria>
  <done>Booking API errors are shown translated in every locale via stable codes, with API contracts backward compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Supabase auth error mapping (Step3Auth, reset-password), locale-keeping auth navigation, freeze patterns</name>
  <files>lib/auth-error-code.ts, components/booking/steps/Step3Auth.tsx, app/[locale]/account/reset-password/page.tsx, messages/en.json, messages/ru.json, messages/es.json, messages/fr.json, messages/ar.json, messages/hi.json, messages/zh.json, tests/auth-error-code.test.ts, tests/Step3Auth.test.tsx, tests/reset-password-locale.test.tsx, .planning/phases/75-e2e-verification-launch/freeze/75-16.freeze</files>
  <read_first>
    - components/booking/steps/Step3Auth.tsx (registerError interpolating the GoTrue message; magic-link emailRedirectTo with return-to /book)
    - app/[locale]/account/reset-password/page.tsx (setErrorMsg with the GoTrue message; next/navigation router push to /account)
    - messages/en.json Errors and Auth namespaces (existing keys to reuse, e.g. genericRetry)
    - tests/Step3Auth.test.tsx
  </read_first>
  <behavior>
    - authErrorKey('user_already_exists') = 'userExists'; ('weak_password') = 'weakPassword'; ('over_email_send_rate_limit') = 'emailRateLimited'; ('email_address_invalid') = 'invalidEmail'; ('signup_disabled') = 'signupDisabled'; ('session_expired' or 'refresh_token_not_found') = 'sessionExpired'; (undefined / '' / 'anything_else') = 'generic'.
    - Step3Auth under ru with a mocked AuthError { code: 'weak_password', message: 'Password should be...' } shows the ru weakPassword message and not the English GoTrue text; the magic-link emailRedirectTo return-to is '/ru/book'.
    - reset-password under ar with an update error shows the ar mapped message; on success it navigates via the i18n router to '/account' (rendered as /ar/account).
  </behavior>
  <action>Create lib/auth-error-code.ts exporting authErrorKey per the behavior list. Add Errors.auth.{userExists,weakPassword,emailRateLimited,invalidEmail,signupDisabled,sessionExpired,generic} to all 7 catalogs (EN written plainly; 6 translated). Step3Auth: render the mapped Errors.auth message instead of interpolating error.message (keep console logging), and build the magic-link return-to with getPathname({ locale, href: '/book' }) using useLocale(). reset-password: map the update error through authErrorKey, and switch the post-success navigation to useRouter from @/i18n/routing. Write tests/auth-error-code.test.ts, extend tests/Step3Auth.test.tsx and create tests/reset-password-locale.test.tsx per the behavior list. Create freeze/75-16.freeze with messages/en.json::Booking.step6.errors and messages/en.json::Errors.auth.</action>
  <verify>
    <automated>npx vitest run tests/auth-error-code.test.ts tests/Step3Auth.test.tsx tests/reset-password-locale.test.tsx tests/i18n-catalog-array-types.test.ts && npx tsc --noEmit && test $(grep -c "::" .planning/phases/75-e2e-verification-launch/freeze/75-16.freeze) -ge 2</automated>
  </verify>
  <acceptance_criteria>
    - Tests pass; `npx tsc --noEmit` clean.
    - `grep -n "message: error.message" components/booking/steps/Step3Auth.tsx` returns nothing and `grep -n "setErrorMsg(error.message" app/[locale]/account/reset-password/page.tsx` returns nothing.
    - `grep -n "from 'next/navigation'" app/[locale]/account/reset-password/page.tsx` shows no useRouter import.
    - `freeze/75-16.freeze` contains both patterns.
  </acceptance_criteria>
  <done>Auth errors are translated in every locale and the auth-adjacent navigation in the wizard and reset flow keeps the locale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> browser error rendering | Server-controlled error strings reach the UI |
| Supabase GoTrue -> browser | Third-party auth error messages |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-31 | Information Disclosure | raw server/auth error text in UI | low | mitigate | UI renders only catalog messages keyed by a fixed code allow-list; unknown codes -> generic message; raw text only in console logs |
| T-75-32 | Tampering (API contract) | status codes / error field | low | mitigate | Status and English error unchanged; existing API tests must still pass |
| T-75-SC | Tampering | package installs | low | accept | No installs |
</threat_model>

<verification>
- `npx vitest run tests/create-payment-intent.test.ts tests/validate-promo.test.ts tests/Step6Payment.test.tsx tests/auth-error-code.test.ts tests/Step3Auth.test.tsx tests/reset-password-locale.test.tsx tests/i18n-catalog-array-types.test.ts` green; `npx tsc --noEmit` clean.
</verification>

<success_criteria>
No raw English API or auth error text is displayed on a localized page; errors are translated via stable codes, APIs stay backward compatible, and auth-adjacent navigation keeps the locale.
</success_criteria>

<output>
Create `.planning/phases/75-e2e-verification-launch/75-16-SUMMARY.md` when done
</output>
