---
phase: 75-e2e-verification-launch
plan: 18
type: execute
wave: 5
depends_on: [75-01, 75-02, 75-03, 75-04, 75-05, 75-06, 75-07, 75-08, 75-09, 75-10, 75-11, 75-12, 75-13, 75-14, 75-15, 75-16, 75-17]
files_modified:
  - scripts/i18n-freeze-manifest.mjs
  - tests/i18n-freeze-manifest.test.ts
  - i18n/translation-manifest.json
  - lib/content/metricool.ts
  - tests/content-metricool.test.ts
  - tests/content-locale-parity.test.ts
  - scripts/qa/en_leak_allowlist.json
  - .planning/phases/75-e2e-verification-launch/75-QA-RESULTS.md
autonomous: true
requirements: [VER-01]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "scripts/i18n-freeze-manifest.mjs reads every .planning/phases/75-e2e-verification-launch/freeze/*.freeze pattern, selects the matching EN units via flattenEnSource, and writes { enHash: sha256(EN value), lastTranslatedAt } into i18n/translation-manifest.json — so a future pipeline run neither overwrites nor re-spends tokens on the hand-translated units (D-08)."
    - "The freeze tool refuses (non-zero exit, unit listed) to freeze any unit whose value in any of the 6 locale files is missing or byte-identical to EN unless the value is DNT-only — an untranslated stub can never be frozen as translated; --verify exits 0 only when every selected unit's manifest hash equals sha256 of the current EN value."
    - "createMetricoolPost/createMetricoolPosts accept draft?: boolean; draft: true sends draft true and autoPublish false to Metricool, while omitting draft keeps today's body byte-identical (existing publish pipeline unchanged) — the announcement can never publish live by accident (D-14, RESEARCH Pitfall 9)."
    - "tests/content-locale-parity.test.ts proves every content/pages/en/**/*.json and content/routes/en/*.json has all 6 locale siblings with the identical flattened key set and value types (arrays equal length)."
    - "Pre-deploy gate is green and recorded in 75-QA-RESULTS.md: full npx vitest run (every failing test in a file touched since tag v2.2 fixed or justifiably removed; failures in untouched files listed, not fixed — D-15), node scripts/i18n-translate.mjs --check, npx tsc --noEmit, npm run lint, node scripts/qa/en_leak_static.mjs exit 0, tests/locale-links-backstop.test.ts green."
  artifacts:
    - "scripts/i18n-freeze-manifest.mjs + tests/i18n-freeze-manifest.test.ts"
    - "i18n/translation-manifest.json (frozen phase-75 units)"
    - "lib/content/metricool.ts CreatePostInput.draft"
    - "tests/content-locale-parity.test.ts"
    - ".planning/phases/75-e2e-verification-launch/75-QA-RESULTS.md (pre-deploy gate section)"
  key_links:
    - "freeze/75-NN.freeze (plans 75-06..75-16) -> scripts/i18n-freeze-manifest.mjs -> i18n/translation-manifest.json -> scripts/i18n-translate.mjs unitsNeedingTranslation() skips them"
    - "lib/content/metricool.ts buildBody draft flag -> Metricool POST body (draft/autoPublish)"
  prohibitions:
    - "MUST NOT run node scripts/i18n-translate.mjs --dry-run or any translating mode — only --check (D-08)."
    - "MUST NOT freeze a unit whose locale value is missing or identical to EN (non-DNT)."
    - "MUST NOT delete or skip a failing test in a v3.0-touched file without a written justification in 75-QA-RESULTS.md (D-15)."
---

<objective>
Make the tree deploy-ready: freeze the translation manifest for every hand-translated unit (D-08), add draft support to the Metricool client before any announcement (D-14, Pitfall 9), prove content locale parity, and run the full pre-deploy gate including D-15's vitest baseline rule.

Purpose: the deploy in plan 75-19 must ship a green, frozen, leak-free tree; without the freeze a future pipeline run would overwrite every in-session translation from this phase.
Output: freeze tool + frozen manifest, Metricool draft flag, content parity test, and the gate results recorded in `75-QA-RESULTS.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-e2e-verification-launch/75-CONTEXT.md
@.planning/phases/75-e2e-verification-launch/75-RESEARCH.md
@scripts/lib/i18n-manifest.mjs
</context>

## Artifacts this phase produces

- `scripts/i18n-freeze-manifest.mjs` — CLI `node scripts/i18n-freeze-manifest.mjs [--dir .planning/phases/75-e2e-verification-launch/freeze] [--verify]`; exports `selectUnits(patterns, rootDir)` and `freezeUnits(units, manifest, rootDir)`.
- Frozen entries in `i18n/translation-manifest.json` for every phase-75 pattern.
- `CreatePostInput.draft?: boolean` in `lib/content/metricool.ts`.
- `tests/i18n-freeze-manifest.test.ts`, `tests/content-locale-parity.test.ts`; extended `tests/content-metricool.test.ts`.
- `.planning/phases/75-e2e-verification-launch/75-QA-RESULTS.md` (section "Pre-deploy gate").

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — manifest freeze tool end-to-end (fixture test, then apply every phase-75 freeze file)</name>
  <files>scripts/i18n-freeze-manifest.mjs, tests/i18n-freeze-manifest.test.ts, i18n/translation-manifest.json</files>
  <read_first>
    - scripts/lib/i18n-manifest.mjs (sha256, flattenEnSource(obj, sourcePath) unitKey format '<sourceKey>::<dotPath>', loadManifest, writeManifest)
    - scripts/lib/i18n-surfaces.mjs enumerateEnSources (sourceKey -> targetPathFor(locale) mapping for messages and content)
    - scripts/lib/i18n-qa-report.mjs checkNoEnglishLeakage (DNT-only detection to reuse for the identical-value exception)
    - i18n/glossary.json
    - .planning/phases/75-e2e-verification-launch/freeze/*.freeze (inputs written by plans 75-06..75-16)
  </read_first>
  <behavior>
    - Fixture: EN source with units a.b = 'Hello world', a.c = 'PRESTIGO', and 6 locale files where a.b is translated and a.c identical -> freezing pattern '<src>' writes both (a.c allowed as DNT-only).
    - A locale file where a.b equals the EN value -> the tool exits non-zero and names the unit/locale; nothing is written.
    - A locale file missing a.b -> non-zero exit naming the unit.
    - Pattern '<src>::a' selects a.b and a.c; '<src>::a.b' selects only a.b; '<src>::a.bx' selects nothing (prefix matches whole path segments only).
    - --verify exits 0 after a freeze and 1 after the EN value of a frozen unit changes.
  </behavior>
  <action>Create scripts/i18n-freeze-manifest.mjs (ES module, isMain-guarded CLI): parse pattern lines (ignore blanks and # comments), resolve each sourceKey to its EN JSON file and its 6 locale target files the same way enumerateEnSources maps them, select units by exact sourceKey (all units) or sourceKey::dotPrefix (unit dotPath equals the prefix or starts with prefix + '.'), check every selected unit exists in all 6 locale files and is not byte-identical to EN unless DNT-only (glossary-based check reused from checkNoEnglishLeakage's approach), then write the manifest entries with a single ISO timestamp via writeManifest; print counts per pattern; --verify mode performs the hash comparison without writing. Never call any translator and never import the translation pipeline's entry script (only the scripts/lib helpers). Write tests/i18n-freeze-manifest.test.ts against a temp-dir fixture for every behavior case. Then run the CLI over the phase freeze directory and commit the updated i18n/translation-manifest.json; run --verify.</action>
  <verify>
    <automated>npx vitest run tests/i18n-freeze-manifest.test.ts tests/i18n-translate-manifest.test.ts && node scripts/i18n-freeze-manifest.mjs && node scripts/i18n-freeze-manifest.mjs --verify</automated>
  </verify>
  <acceptance_criteria>
    - The fixture test passes every behavior case.
    - `node scripts/i18n-freeze-manifest.mjs --verify` exits 0 on the real repo after the freeze.
    - `git diff --stat i18n/translation-manifest.json` shows added/updated entries only (no removals).
    - `grep -cE "import .*i18n-translate[.]mjs" scripts/i18n-freeze-manifest.mjs` is 0 (the tool never imports the translating pipeline).
  </acceptance_criteria>
  <done>Every hand-translated phase-75 unit is frozen in the manifest by a tested tool that refuses to freeze untranslated stubs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Metricool draft support (before any announcement call)</name>
  <files>lib/content/metricool.ts, tests/content-metricool.test.ts</files>
  <read_first>
    - lib/content/metricool.ts (CreatePostInput type ~line 67; buildBody ~line 80 with autoPublish/draft literals; createMetricoolPost / createMetricoolPosts)
    - tests/content-metricool.test.ts (fetch mock + body assertions)
    - lib/content/publish.ts (existing caller — must stay byte-identical in behavior)
  </read_first>
  <behavior>
    - createMetricoolPost({ ...input, draft: true }) sends a body with draft true and autoPublish false.
    - createMetricoolPost(input) without draft sends autoPublish true and draft false exactly as today.
    - createMetricoolPosts passes draft through to every per-channel call.
  </behavior>
  <action>Add draft?: boolean to CreatePostInput (and to the createMetricoolPosts input it fans out); in buildBody set autoPublish to the negation of input.draft and draft to input.draft === true, so an absent flag reproduces today's values. Extend tests/content-metricool.test.ts with the behavior cases; tests/content-publish.test.ts must still pass unchanged.</action>
  <verify>
    <automated>npx vitest run tests/content-metricool.test.ts tests/content-publish.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both test files pass; the draft:true case asserts `draft: true` and `autoPublish: false` in the posted JSON.
    - `git diff lib/content/publish.ts` is empty.
  </acceptance_criteria>
  <done>The shared Metricool client can create drafts and cannot publish an announcement by default-path accident.</done>
</task>

<task type="auto">
  <name>Task 3: Content parity test + full pre-deploy gate (D-15) recorded in 75-QA-RESULTS.md</name>
  <files>tests/content-locale-parity.test.ts, scripts/qa/en_leak_allowlist.json, .planning/phases/75-e2e-verification-launch/75-QA-RESULTS.md</files>
  <read_first>
    - scripts/lib/i18n-manifest.mjs flattenEnSource (flatten helper to reuse in the parity test)
    - scripts/qa/en_leak_allowlist.json (plan 75-02 — only add an entry here with a reason if a gate finding is a justified exception, e.g. an unlocalizable 404 metadata title recorded by plan 75-13)
    - .planning/phases/63-*/deferred-items.md and STATE.md Blockers (pre-existing tests/admin-bookings.test.ts note — research found it green; re-check)
    - package.json scripts (no test script; lint exists)
  </read_first>
  <action>Create tests/content-locale-parity.test.ts: for every JSON under content/pages/en (recursive) and content/routes/en, assert each of ru/es/fr/ar/hi/zh has the sibling file and an identical flattened key set with equal value types and equal array lengths. Then run, in order, and record each command, exit code and totals in a "Pre-deploy gate" section of 75-QA-RESULTS.md: npx vitest run (full); for any failing test file, check git diff --name-only v2.2..HEAD — if the failing test file or the module it tests changed since v2.2 it is v3.0-touched and must be fixed (or removed only with a written justification) before continuing; failures in untouched files are listed with a one-line cause and left alone (D-15); node scripts/i18n-translate.mjs --check (after committing, since it asserts EN sources are clean per git diff); npx tsc --noEmit; npm run lint; node scripts/qa/en_leak_static.mjs (must exit 0 — any remaining R1/R2/R3 finding is fixed in its file as a Rule-1 deviation or, if intentional, allowlisted with an explicit reason); npx vitest run tests/locale-links-backstop.test.ts. Also attempt npx next build and record the outcome (a failure caused only by missing server-only env vars locally is recorded as environment-limited; a code/type/route-export failure must be fixed). Record the final vitest totals, the list of pre-existing untouched failures (or 'none') and the v3.0-touched fixes.</action>
  <verify>
    <automated>npx vitest run && node scripts/i18n-translate.mjs --check && npx tsc --noEmit && node scripts/qa/en_leak_static.mjs && grep -n "Pre-deploy gate" .planning/phases/75-e2e-verification-launch/75-QA-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 (or every remaining failure is in a file untouched since v2.2 and listed in 75-QA-RESULTS.md with its cause).
    - `node scripts/i18n-translate.mjs --check`, `npx tsc --noEmit`, `npm run lint` and `node scripts/qa/en_leak_static.mjs` all exit 0.
    - `75-QA-RESULTS.md` contains the "Pre-deploy gate" section with every command, exit code and the D-15 lists.
    - Any change to `scripts/qa/en_leak_allowlist.json` adds entries with a non-empty `reason` only.
  </acceptance_criteria>
  <done>The tree is frozen, parity-proven, leak-free by static scan and green on the full gate, with the D-15 baseline recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Freeze tool -> translation manifest | Marks units as translated for all future pipeline runs |
| Metricool client -> public social accounts | A body flag decides draft vs live publication |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-36 | Tampering | i18n/translation-manifest.json | medium | mitigate | Refuses to freeze missing or EN-identical (non-DNT) locale values; --verify hash check; fixture tests |
| T-75-37 | Tampering (public brand content) | Metricool body | high | mitigate | draft flag maps to draft true / autoPublish false; tested; announcement in plan 75-21 additionally goes through the Metricool MCP with draft true behind a blocking checkpoint |
| T-75-38 | Repudiation | D-15 test baseline | low | mitigate | Every failing test classified by git diff against v2.2 and recorded with cause |
| T-75-SC | Tampering | package installs | low | accept | No installs |
</threat_model>

<verification>
- Freeze tool tests + --verify green; Metricool tests green; full gate commands green and recorded.
</verification>

<success_criteria>
The phase's translations are protected from future overwrites, the Metricool client is draft-capable, and a fully green, leak-free, recorded pre-deploy gate exists (D-08, D-14 prerequisite, D-15).
</success_criteria>

<output>
Create `.planning/phases/75-e2e-verification-launch/75-18-SUMMARY.md` when done
</output>
