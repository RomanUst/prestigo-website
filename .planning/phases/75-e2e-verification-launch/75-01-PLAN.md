---
phase: 75-e2e-verification-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/qa/render_audit.py
  - scripts/qa/switcher_audit.py
  - scripts/qa/hreflang_reciprocity.py
  - scripts/qa/jsonld_audit.py
  - scripts/qa/csp_regression.py
  - scripts/qa/baselines/csp_baseline.json
  - .gitignore
  - .planning/phases/75-e2e-verification-launch/75-QA-BASELINE.md
autonomous: true
requirements: [VER-01]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "`python3 scripts/qa/render_audit.py https://rideprestigo.com` visits every locale x key page (7 x 21 = 147 URLs from overflow_audit.py's PAGES/LOCS) and, per URL, records HTTP status, html lang, html dir, canonical presence and every securitypolicyviolation event; it exits 1 when any URL is non-200, has the wrong lang (en root = en, prefixed locales = their code), has dir=rtl anywhere except ar (or ltr on ar), or fired a CSP violation (VER-01 render + no-CSP-regression, D-13)."
    - "`python3 scripts/qa/switcher_audit.py https://rideprestigo.com` opens the LocaleSwitcher and proves every source locale can switch to every target locale on a deep page (/routes/prague-vienna) and lands on the SAME page in the target locale with the target html lang (VER-01 switcher, D-13)."
    - "`python3 scripts/qa/hreflang_reciprocity.py https://rideprestigo.com` parses /sitemap.xml, fails on any non-reciprocal alternate pair, any alternate URL not returning 200, or an x-default that is not the EN root form, and spot-checks that sampled pages' own hreflang link tags equal their sitemap cluster (D-14 hreflang reciprocity validator)."
    - "`python3 scripts/qa/jsonld_audit.py https://rideprestigo.com` parses every ld+json block on sampled locale pages and fails on invalid JSON, a missing @context, a FAQPage acceptedAnswer.text that is not a plain string, or a route Service node whose inLanguage is not the locale's BCP-47 tag (zh -> zh-Hans) (D-07 metadata/JSON-LD regression surface, D-14 Rich Results pre-check)."
    - "`python3 scripts/qa/csp_regression.py --compare https://rideprestigo.com` diffs the Content-Security-Policy header of every route class (nonce value normalized) against scripts/qa/baselines/csp_baseline.json captured from pre-change production in this plan, and exits 1 on any drift (VER-01 no CSP regression)."
    - "The pre-change production baseline of all five scripts is recorded in 75-QA-BASELINE.md (per-script pass/fail counts + the concrete failures), so the post-deploy run in plan 75-20 has a before/after comparison."
    - "Every script takes the base URL as its first positional argument (default https://rideprestigo.com), accepts --locales to restrict the run, writes machine-readable JSON to scripts/qa/out/ and never sends analytics hits (GA4/Meta requests are aborted at the network layer) (D-13 style, D-02 production target)."
    - statement: "QA script JSON output lists findings in a deterministic order (routing.locales order en, ru, es, fr, ar, hi, zh, then the PAGES order, then finding text) so a baseline run and a post-fix run diff cleanly line-by-line"
      verification: backstop
  artifacts:
    - "scripts/qa/render_audit.py, switcher_audit.py, hreflang_reciprocity.py, jsonld_audit.py, csp_regression.py (new, overflow_audit.py style)"
    - "scripts/qa/baselines/csp_baseline.json (golden pre-change CSP per route class)"
    - ".planning/phases/75-e2e-verification-launch/75-QA-BASELINE.md"
  key_links:
    - "scripts/qa/overflow_audit.py PAGES/LOCS lists are copied verbatim into each Playwright script (coverage parity with the existing overflow regression check)"
    - "csp_regression.py baseline (captured here, pre-change) -> compared again post-deploy in plan 75-20 — the only way a CSP change caused by this phase is detectable"
    - "sitemap.xml alternates and page-level hreflang both come from lib/seo.ts getAlternates() — the reciprocity script checks both surfaces agree"
  prohibitions:
    - "MUST NOT send GA4 or Meta analytics hits from a QA run to production analytics (requests to google-analytics.com, googletagmanager.com collect endpoints and facebook.com/tr are aborted by the script)."
    - "MUST NOT loosen or rewrite the CSP to make the regression check pass — a CSP diff is investigated as a regression, never 'fixed' by widening the policy (RESEARCH Pitfall 8)."
  assumptions:
    - "[ASSUMPTION · VER-01 · edge:empty] A locale with zero failures still writes a JSON entry with an empty findings list (never omits the locale), so 'no findings' and 'not run' are distinguishable."
---

<objective>
Build the repeatable production QA harness for VER-01 (per D-13: Python Playwright scripts in `scripts/qa/`, same style as `overflow_audit.py`, base-URL argument, not wired into CI) covering render, locale switcher, hreflang reciprocity, JSON-LD and CSP — and record the pre-change production baseline (D-02: production is the target).

Purpose: every later plan's fixes are proven against production by re-running these exact scripts in plan 75-20; the CSP golden baseline MUST be captured now, before any phase-75 code is deployed, or a CSP regression becomes undetectable.
Output: five QA scripts, a golden CSP baseline JSON, a `.gitignore` entry for QA outputs and the E2E account file, and `75-QA-BASELINE.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-e2e-verification-launch/75-CONTEXT.md
@.planning/phases/75-e2e-verification-launch/75-RESEARCH.md
@.planning/phases/75-e2e-verification-launch/75-PATTERNS.md
@scripts/qa/overflow_audit.py
</context>

## Artifacts this phase produces

- `scripts/qa/render_audit.py` — CLI `python3 scripts/qa/render_audit.py [base_url] [--locales en,ru,...]`, output `scripts/qa/out/render_audit.json`.
- `scripts/qa/switcher_audit.py` — output `scripts/qa/out/switcher_audit.json`.
- `scripts/qa/hreflang_reciprocity.py` — output `scripts/qa/out/hreflang_reciprocity.json`.
- `scripts/qa/jsonld_audit.py` — output `scripts/qa/out/jsonld_audit.json`.
- `scripts/qa/csp_regression.py` — modes `--capture` (writes baseline) / `--compare` (diffs), output `scripts/qa/out/csp_regression.json`.
- `scripts/qa/baselines/csp_baseline.json` — golden pre-change CSP per route class.
- `.gitignore` entries: `scripts/qa/out/`, `scripts/qa/.e2e-account.json`.
- `.planning/phases/75-e2e-verification-launch/75-QA-BASELINE.md`.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — render_audit.py end-to-end against production (7 locales x 21 pages) + QA output ignore rules</name>
  <files>scripts/qa/render_audit.py, .gitignore, .planning/phases/75-e2e-verification-launch/75-QA-BASELINE.md</files>
  <read_first>
    - scripts/qa/overflow_audit.py (the D-13 style template: CLI arg, PAGES/LOCS lists, consent init script, URL building for en root vs prefixed locales, JSON dump + stdout summary)
    - i18n/locales.ts (locale order en, ru, es, fr, ar, hi, zh; rtlLocales = ar)
    - .planning/phases/75-e2e-verification-launch/75-RESEARCH.md § "Recommended Project Structure" and § Pitfall 8 (CSP)
    - .gitignore (current ignore rules; append only)
  </read_first>
  <action>Create scripts/qa/render_audit.py in the overflow_audit.py style (module docstring with usage, top-level PAGES and LOCS copied verbatim from overflow_audit.py, identical en-root/prefix URL building) but using argparse: positional base_url (default https://rideprestigo.com), optional --locales (comma list, default all 7 in routing order). Browser context: Chromium, desktop viewport 1280x900, locale en-US, add_init_script that (a) sets the prestigo_consent_v2 localStorage key to analytics false / marketing false exactly as overflow_audit.py does and (b) registers a document-level securitypolicyviolation listener that pushes violatedDirective, blockedURI and sourceFile into a window array. Register a context-level route that aborts every request whose URL contains google-analytics.com, googletagmanager.com/gtag, /g/collect, facebook.com/tr or connect.facebook.net so a QA run never sends analytics hits. For each locale x page (iterate LOCS in routing order, then PAGES order): goto with wait_until load and a 90s timeout, wait 1200 ms, record response status, html lang, html dir, whether a canonical link exists, the count of hreflang alternate links, and the collected CSP violations. Failure rules: status not 200; lang not equal to the locale code (en for the root); dir rtl on any locale other than ar or not rtl on ar; any CSP violation. Write scripts/qa/out/render_audit.json (create the out dir; keys sorted; findings per URL in deterministic order; a locale with no findings still has an entry) and print one summary line per failing URL plus a final totals line. Exit 0 when clean, 1 when any failure, 2 on an infrastructure error (browser launch/network). Append two lines to .gitignore: scripts/qa/out/ and scripts/qa/.e2e-account.json (the latter is consumed by the booking E2E account path in plans 75-03/75-20 and must never be committed). Run the script against production and create 75-QA-BASELINE.md with a "render_audit (pre-change baseline)" section: run date, totals, and every failing URL with its reason (a non-zero baseline is expected and is recorded, not fixed, here).</action>
  <verify>
    <automated>python3 scripts/qa/render_audit.py https://rideprestigo.com --locales en,ar; test $? -le 1 && python3 -c "import json;s=json.dumps(json.load(open('scripts/qa/out/render_audit.json')));assert '/ar' in s;print('render_audit OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/qa/render_audit.py` exists, is argparse-driven (positional base_url, `--locales`) and contains the 21-entry PAGES list and the 7-entry LOCS list identical to `scripts/qa/overflow_audit.py`.
    - Running it with `--locales en,ar` against production produces `scripts/qa/out/render_audit.json` containing entries for both `en` and `ar` URLs, and the exit code is 0 or 1 (never 2).
    - The script source contains a network route that aborts google-analytics / googletagmanager / facebook.com/tr requests and a securitypolicyviolation listener.
    - `.gitignore` contains the lines `scripts/qa/out/` and `scripts/qa/.e2e-account.json`; `git check-ignore scripts/qa/out/x.json scripts/qa/.e2e-account.json` prints both paths.
    - `75-QA-BASELINE.md` has a render_audit section with totals from a full 7-locale production run.
  </acceptance_criteria>
  <done>render_audit.py runs end-to-end against production for all 7 locales, writes deterministic JSON, and its baseline is recorded; QA outputs and the E2E account file are git-ignored.</done>
</task>

<task type="auto">
  <name>Task 2: switcher_audit.py, hreflang_reciprocity.py, jsonld_audit.py + baseline</name>
  <files>scripts/qa/switcher_audit.py, scripts/qa/hreflang_reciprocity.py, scripts/qa/jsonld_audit.py, .planning/phases/75-e2e-verification-launch/75-QA-BASELINE.md</files>
  <read_first>
    - scripts/qa/render_audit.py (Task 1 — reuse its argparse/consent/abort/JSON-output conventions verbatim)
    - components/LocaleSwitcher.tsx (trigger button with aria-haspopup, role=menu, role=menuitem items labelled with LOCALE_ENDONYMS; router.replace to the same pathname)
    - i18n/locales.ts (LOCALE_ENDONYMS and BCP47_TAG maps — zh is zh-Hans for hreflang/inLanguage)
    - lib/seo.ts (getAlternates — single source of both page hreflang and sitemap alternates)
    - .planning/phases/75-e2e-verification-launch/75-RESEARCH.md § Code Examples "hreflang reciprocity check against the live sitemap" (copy that XML-parsing core)
    - lib/jsonld.ts (route Service node shape with inLanguage; FAQPage acceptedAnswer.text plain-string rule from Phase 73)
  </read_first>
  <action>switcher_audit.py (Playwright, same conventions as render_audit.py): for every ordered (source, target) locale pair on /routes/prague-vienna (42 pairs) plus, for each source locale, one rotating target on /, /book and /fleet: load the source URL, click the switcher trigger (button with aria-haspopup inside the nav; if two instances exist use the visible one), click the role=menuitem whose text equals LOCALE_ENDONYMS[target], wait for navigation, and assert the final pathname equals the same page in the target locale (en = unprefixed root form, others = /target prefix) and html lang equals the target. Record the NEXT_LOCALE cookie value for information only (the middleware sets it on every response — never treat its presence as a pass signal). hreflang_reciprocity.py (no browser; urllib): implement the RESEARCH Code Example core (parse /sitemap.xml with the sitemap and xhtml namespaces into loc -> {hreflang: href}); fail on a non-reciprocal pair, an alternate target missing from the sitemap, an x-default that is not the EN root form of the cluster; then GET every distinct alternate URL (sequential, 20s timeout, 1 retry) and fail on non-200; then for 10 sampled clusters (home, /fleet, /book, /routes, /routes/prague-vienna, /services/airport-transfer, /faq, /about, /contact, /blog) fetch the EN page HTML and assert its alternate link tags equal the sitemap cluster. jsonld_audit.py (urllib, SSR HTML): for each locale on /, /routes/prague-vienna, /faq, /fleet, /routes, /services/airport-transfer, /book extract every ld+json script body; fail on JSON parse error, missing @context, any FAQPage mainEntity acceptedAnswer.text that is not a string, and any Service node on a /routes/prague-* page whose inLanguage is not BCP47_TAG[locale]; record (do not fail) Service/BreadcrumbList names that are English on non-EN locales as by-design per Phase 74 D-09. All three write JSON to scripts/qa/out/ with deterministic ordering and use the 0/1/2 exit-code convention. Run all three against production and append their baseline sections (totals + concrete failures) to 75-QA-BASELINE.md.</action>
  <verify>
    <automated>python3 scripts/qa/hreflang_reciprocity.py https://rideprestigo.com; test $? -le 1 && python3 scripts/qa/jsonld_audit.py https://rideprestigo.com --locales en,ru; test $? -le 1 && python3 scripts/qa/switcher_audit.py https://rideprestigo.com --locales ru; test $? -le 1 && ls scripts/qa/out/hreflang_reciprocity.json scripts/qa/out/jsonld_audit.json scripts/qa/out/switcher_audit.json</automated>
  </verify>
  <acceptance_criteria>
    - All three scripts exist, accept a positional base_url, and exit 0/1 (never 2) against production.
    - `scripts/qa/out/hreflang_reciprocity.json` reports the number of sitemap URLs checked and the number of reciprocity errors.
    - `scripts/qa/out/switcher_audit.json` contains one entry per tested (source, target, page) triple with the observed final pathname and html lang.
    - `scripts/qa/out/jsonld_audit.json` contains per-(locale, page) block counts and failures.
    - `75-QA-BASELINE.md` has switcher, hreflang and jsonld sections with production totals.
  </acceptance_criteria>
  <done>Switcher, hreflang-reciprocity and JSON-LD scripts run against production and their pre-change baselines are recorded.</done>
</task>

<task type="auto">
  <name>Task 3: csp_regression.py + golden pre-change CSP baseline</name>
  <files>scripts/qa/csp_regression.py, scripts/qa/baselines/csp_baseline.json, .planning/phases/75-e2e-verification-launch/75-QA-BASELINE.md</files>
  <read_first>
    - middleware.ts (buildCsp nonce policy for dynamic routes vs buildCspStatic for static marketing routes; which route classes get which)
    - .planning/phases/75-e2e-verification-launch/75-RESEARCH.md § Pitfall 8 (CSP should stay byte-identical — snapshot/diff, never rewrite)
    - scripts/qa/hreflang_reciprocity.py (Task 2 — urllib fetch conventions)
  </read_first>
  <action>Create csp_regression.py (urllib, no browser, redirects NOT followed so the first response's headers are captured) with two modes: --capture writes scripts/qa/baselines/csp_baseline.json; --compare (default) diffs against it. Route classes (one URL each, in this fixed order): /, /ru, /ru/fleet, /routes/prague-vienna, /ar/routes/prague-vienna, /book, /ru/book, /login, /ru/login, /ru/account, /api/health. For each capture the content-security-policy and content-security-policy-report-only header values (empty string when absent) and the status code; normalize every nonce token to a fixed placeholder with a regex over the 'nonce-...' form so the baseline is stable across requests. The baseline JSON maps path -> {status, csp, cspReportOnly}. Compare mode prints a unified diff per changed route and exits 1 on any header drift (status changes are reported but only CSP header drift fails). Capture the baseline from production NOW (this is the pre-change golden reference — no phase-75 code is deployed yet), then immediately run --compare against production to prove the script is self-consistent (must exit 0). Append a CSP section to 75-QA-BASELINE.md listing each route class, its status and whether it uses the nonce or the static policy.</action>
  <verify>
    <automated>python3 scripts/qa/csp_regression.py --capture https://rideprestigo.com && python3 scripts/qa/csp_regression.py --compare https://rideprestigo.com && python3 -c "import json;d=json.load(open('scripts/qa/baselines/csp_baseline.json'));assert len(d)==11 and all('csp' in v for v in d.values());print('csp baseline OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/qa/baselines/csp_baseline.json` exists with exactly 11 route-class entries, each carrying `status`, `csp`, `cspReportOnly`, and no raw nonce value (only the normalized placeholder).
    - `python3 scripts/qa/csp_regression.py --compare https://rideprestigo.com` exits 0 immediately after capture.
    - `75-QA-BASELINE.md` has a CSP section enumerating the 11 route classes.
  </acceptance_criteria>
  <done>A golden, nonce-normalized CSP baseline of pre-change production is committed and the compare mode is proven self-consistent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QA runner -> production site | Scripts issue real requests to rideprestigo.com and load real third-party tags |
| QA runner -> third-party analytics | Page scripts would send GA4/Meta hits unless blocked |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-01 | Tampering | production GA4/Meta data | medium | mitigate | Context-level route aborts google-analytics.com, googletagmanager.com collect/gtag, facebook.com/tr and connect.facebook.net requests in every Playwright script |
| T-75-02 | Denial of Service | production site under QA load | low | mitigate | Sequential requests only, per-request timeouts, single retry; no parallel browser pages |
| T-75-03 | Repudiation | CSP regression evidence | medium | mitigate | Golden baseline captured from pre-change production and committed, so post-deploy drift is provable against a fixed artifact |
| T-75-SC | Tampering | package installs | low | accept | No npm/pip installs in this plan — Playwright 1.58.0 and stdlib only (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- All five scripts exit 0/1 (never 2) against production and write JSON to `scripts/qa/out/`.
- `scripts/qa/baselines/csp_baseline.json` committed; compare mode self-consistent.
- `75-QA-BASELINE.md` contains render, switcher, hreflang, jsonld and CSP sections.
</verification>

<success_criteria>
The VER-01 production QA harness (render, switcher, hreflang reciprocity, JSON-LD, CSP) exists as repeatable `scripts/qa/` scripts (D-13) and its pre-change production baseline, including the golden CSP snapshot, is recorded for the before/after comparison in plan 75-20.
</success_criteria>

<output>
Create `.planning/phases/75-e2e-verification-launch/75-01-SUMMARY.md` when done
</output>
