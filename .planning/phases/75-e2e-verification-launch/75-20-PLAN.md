---
phase: 75-e2e-verification-launch
plan: 20
type: execute
wave: 7
depends_on: [75-19]
files_modified:
  - .planning/phases/75-e2e-verification-launch/75-QA-RESULTS.md
  - .planning/phases/75-e2e-verification-launch/75-EN-LEAK-AUDIT.md
  - .planning/phases/75-e2e-verification-launch/deferred-items.md
autonomous: false
requirements: [VER-01]
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every locale renders: render_audit.py over 7 locales x 21 pages exits 0 on production (200, correct html lang, dir=rtl only on ar, zero CSP violations) (VER-01 render)."
    - "The switcher works: switcher_audit.py exits 0 on production for all 42 ordered locale pairs on /routes/prague-vienna plus the rotating sample pages (VER-01 switcher)."
    - "Guest booking reaches a rendered Stripe payment form through all 6 steps in all 7 locales incl. /ar RTL, with the Stripe Elements locale equal to the site locale (hi = auto) and the Places languageCode equal to the locale (zh = zh-CN); the RU and AR sign-in + My trips + signed-in booking path passes; no payment is submitted (D-01, D-04, D-07)."
    - "No EN leakage: en_leak_static.mjs exits 0 and en_leak_rendered.py exits 0 for ru/es/fr/ar/hi/zh on production (text, attributes, metadata/JSON-LD, internal-link locale), with only reasoned allowlist entries (D-06, D-07, D-09)."
    - "GA4 and Meta carry site_locale: analytics_locale_audit.py exits 0 for all 7 locales (captured-and-aborted hits show ep.site_locale and cd[site_locale] = locale) (D-10, D-12)."
    - "hreflang clusters are reciprocal (hreflang_reciprocity.py exit 0), JSON-LD passes jsonld_audit.py, csp_regression.py --compare shows no drift vs the pre-change golden baseline, and overflow_audit.py reports 0 issues at 320/375/768/1024/1280 (VER-01 CSP, D-13 overflow regression, D-14 hreflang validator)."
    - "D-05 post-booking surfaces are recorded as-is from code/templates: the confirmation page copy language and the client email template language (lib/email*.ts) — anything EN-only is logged in deferred-items.md as the deferred localized-emails / confirmation-copy capability, not fixed."
    - "All results (per script, per locale, pre-change baseline vs post-deploy) are recorded in 75-QA-RESULTS.md, which the phase verifier embeds into 75-VERIFICATION.md (D-13)."
    - "After the run, every E2E row is removed from production Supabase by the strict marker (client_email LIKE 'e2e+%@rideprestigo.com' AND first name 'E2E' AND last name 'TEST' AND status 'unpaid'), cross-checked against scripts/qa/out/booking_e2e_refs.json, with zero marker rows remaining (D-03)."
  artifacts:
    - ".planning/phases/75-e2e-verification-launch/75-QA-RESULTS.md (production sections)"
    - ".planning/phases/75-e2e-verification-launch/75-EN-LEAK-AUDIT.md (post-fix section)"
    - ".planning/phases/75-e2e-verification-launch/deferred-items.md"
  key_links:
    - "75-QA-BASELINE.md (plan 75-01, pre-change) vs 75-QA-RESULTS.md (post-deploy) — before/after evidence per script"
    - "scripts/qa/out/booking_e2e_refs.json -> strict-marker DELETE cross-check (D-03)"
  prohibitions:
    - "MUST NOT delete any booking row that does not carry the full E2E marker, and MUST NOT use a date-range or status-only filter (D-03)."
    - "MUST NOT leave E2E test bookings in production Supabase after the run."
    - "MUST NOT submit a payment or type card data during the booking E2E (D-01)."
    - "MUST NOT deploy from this plan — failures are recorded as gaps for /gsd-verify-work gap closure."
  assumptions:
    - "[ASSUMPTION · VER-01 · edge:ordering] Script outputs are compared baseline-vs-post-deploy line by line; this relies on the deterministic ordering backstop in plan 75-01."
---

<objective>
Run the complete VER-01 production QA — render, switcher, two-layer EN-leak, hreflang reciprocity, JSON-LD, CSP regression, analytics locale capture, overflow regression and the per-locale booking E2E (guest x 7 + RU/AR account path) — record everything in `75-QA-RESULTS.md` against the pre-change baseline, record D-05 post-booking surfaces as-is, and remove every E2E booking row by strict marker.

Purpose: this is the proof run for all 7 roadmap success criteria that are verifiable by script; the cleanup keeps the admin/dispatch list clean (D-03).
Output: production QA results, post-fix leak audit, deferred-items record, verified cleanup.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-e2e-verification-launch/75-CONTEXT.md
@.planning/phases/75-e2e-verification-launch/75-QA-BASELINE.md
@.planning/phases/75-e2e-verification-launch/75-EN-LEAK-AUDIT.md
@.planning/phases/75-e2e-verification-launch/75-QA-RESULTS.md
@.planning/phases/75-e2e-verification-launch/75-19-SUMMARY.md
</context>

## Artifacts this phase produces

- `75-QA-RESULTS.md` sections: "Production QA — ar tracer", "Production QA — all locales" (one row per script x locale with exit code and counts, baseline vs now), "Booking E2E" (per locale/path: reachedStripe, Stripe locale, Places language, bookingReference), "D-05 post-booking surfaces", "E2E cleanup".
- `75-EN-LEAK-AUDIT.md` section "Post-fix production result".
- `deferred-items.md` (phase 75).

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — full script sweep for /ar (RTL) on production, recorded</name>
  <precondition>75-19-SUMMARY.md records a completed deploy (not "hold") with passing smoke checks.</precondition>
  <files>.planning/phases/75-e2e-verification-launch/75-QA-RESULTS.md</files>
  <read_first>
    - .planning/phases/75-e2e-verification-launch/75-QA-BASELINE.md (pre-change numbers to compare)
    - scripts/qa/*.py and scripts/qa/en_leak_static.mjs usage docstrings
    - .planning/phases/75-e2e-verification-launch/75-19-SUMMARY.md
  </read_first>
  <action>Against https://rideprestigo.com run for ar: render_audit.py --locales ar, switcher_audit.py --locales ar, en_leak_rendered.py --locales ar, jsonld_audit.py --locales ar, analytics_locale_audit.py --locales ar, booking_e2e.py --locales ar (guest), plus the locale-independent hreflang_reciprocity.py and csp_regression.py --compare. Record each command, exit code, key counts and the baseline-vs-now delta under "Production QA — ar tracer" in 75-QA-RESULTS.md. A failure is recorded with its concrete finding (it becomes a verification gap) — do not patch and redeploy from this plan.</action>
  <verify>
    <automated>python3 scripts/qa/render_audit.py https://rideprestigo.com --locales ar && python3 scripts/qa/booking_e2e.py https://rideprestigo.com --locales ar && grep -n "Production QA — ar tracer" .planning/phases/75-e2e-verification-launch/75-QA-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Every script ran for ar and its exit code is recorded; the ar guest booking run shows reachedStripe true with Stripe locale ar and htmlDir rtl.
    - The section compares against the baseline numbers from 75-QA-BASELINE.md.
  </acceptance_criteria>
  <done>The RTL locale is proven end-to-end across every QA script on production.</done>
</task>

<task type="auto">
  <name>Task 2: All locales, booking E2E x 7 + RU/AR account, overflow x 5 widths, D-05 record, full results</name>
  <files>.planning/phases/75-e2e-verification-launch/75-QA-RESULTS.md, .planning/phases/75-e2e-verification-launch/75-EN-LEAK-AUDIT.md, .planning/phases/75-e2e-verification-launch/deferred-items.md</files>
  <read_first>
    - .planning/phases/75-e2e-verification-launch/75-QA-RESULTS.md (Task 1 section, gate + deploy sections)
    - scripts/qa/overflow_audit.py (writes overflow_<width>.json to the cwd — run it from scripts/qa/out)
    - app/[locale]/book/confirmation/page.tsx and lib/email.ts, lib/email-corporate.ts, lib/email-bespoke.ts (D-05: determine confirmation copy and client email language from code)
    - .planning/phases/75-e2e-verification-launch/75-CONTEXT.md D-05 and Deferred Ideas
  </read_first>
  <action>Run every script for all locales on production: render_audit.py, switcher_audit.py, en_leak_rendered.py (ru,es,fr,ar,hi,zh), jsonld_audit.py, analytics_locale_audit.py, hreflang_reciprocity.py, csp_regression.py --compare, and node scripts/qa/en_leak_static.mjs on the repo. Run booking_e2e.py --locales en,ru,es,fr,ar,hi,zh (guest) and then --account ru,ar (uses scripts/qa/.e2e-account.json); the script's pacing and 429 back-off apply. From scripts/qa/out run python3 ../overflow_audit.py https://rideprestigo.com W for W in 320, 375, 768, 1024, 1280 and record the issue totals (must be 0; if new translated strings overflow, record page/locale/element as a gap). D-05: read the confirmation page and the client email builders and record which language each renders for a non-EN booking (expected: English copy on both); add deferred-items.md entries for "localized client emails (D-05 deferred capability)" and "confirmation page copy is English-only (D-05 recorded as-is; navigation already locale-preserving)", plus "Playwright E2E in CI (deferred until Preview deploys work)". Write "Production QA — all locales" (table: script x locale, exit code, counts, baseline vs now), "Booking E2E" (per locale/path: reachedStripe, stripe locale, places language, html lang/dir, bookingReference) and "D-05 post-booking surfaces" sections into 75-QA-RESULTS.md, a VER-01 facet summary (render / switcher / booking incl. RTL / guest checkout / analytics locale / CSP / EN leakage -> PASS or GAP with evidence), and a "Post-fix production result" section in 75-EN-LEAK-AUDIT.md.</action>
  <verify>
    <automated>node scripts/qa/en_leak_static.mjs && python3 scripts/qa/en_leak_rendered.py https://rideprestigo.com && python3 scripts/qa/analytics_locale_audit.py https://rideprestigo.com && python3 scripts/qa/hreflang_reciprocity.py https://rideprestigo.com && python3 scripts/qa/csp_regression.py --compare https://rideprestigo.com && grep -n "VER-01" .planning/phases/75-e2e-verification-launch/75-QA-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - 75-QA-RESULTS.md contains all-locale results for every script, 7 guest booking rows + 2 account rows, 5 overflow widths, the D-05 section and the VER-01 facet summary.
    - Every VER-01 facet is PASS, or GAP with concrete evidence (no facet left unassessed).
    - `deferred-items.md` exists with the D-05 and CI entries.
    - `scripts/qa/out/booking_e2e_refs.json` lists every reference created across all E2E runs in this phase.
  </acceptance_criteria>
  <done>The full VER-01 production proof is recorded per locale with before/after evidence and D-05 surfaces documented.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Orchestrator removes E2E rows by strict marker (and the test account if requested)</name>
  <reversibility rating="one-way">Deleted production booking rows cannot be restored; the strict marker plus reference cross-check is the guardrail (D-03 already decided — no re-ask).</reversibility>
  <action>Orchestrator performs the D-03 cleanup through the Supabase MCP (executors have no MCP access)</action>
  <instructions>
    Claude recorded every created booking reference in scripts/qa/out/booking_e2e_refs.json and listed them in 75-QA-RESULTS.md "Booking E2E".
    1. execute_sql: select booking_reference, client_email, client_first_name, client_last_name, status, created_at from bookings where client_email like 'e2e+%@rideprestigo.com' and client_first_name = 'E2E' and client_last_name = 'TEST' and status = 'unpaid' order by created_at;
    2. Compare with the recorded references: every recorded ref must appear; any extra row must also carry the full marker (earlier E2E runs) — list extras before deleting; if any row lacks part of the marker, STOP and ask the user.
    3. execute_sql: delete from bookings where client_email like 'e2e+%@rideprestigo.com' and client_first_name = 'E2E' and client_last_name = 'TEST' and status = 'unpaid' returning booking_reference; — the returned set must equal the rows from step 1.
    4. execute_sql: the step-1 select again must return 0 rows.
    5. If the user answered "delete account after QA: yes" in plan 75-19, delete that auth user (delete from auth.users where email = '<account email>') after step 4 and confirm 0 rows remain for that email.
    6. Paste the step-1 count, the deleted references, the step-4 zero result and (if done) the account deletion result.
  </instructions>
  <verification>Claude records the counts and references in the "E2E cleanup" section of 75-QA-RESULTS.md and checks: deleted set == pre-delete set ⊇ recorded refs, post-delete count 0.</verification>
  <read_first>
    - scripts/qa/out/booking_e2e_refs.json (every recorded E2E reference)
    - .planning/phases/75-e2e-verification-launch/75-19-SUMMARY.md (delete-account preference)
  </read_first>
  <acceptance_criteria>
    - 75-QA-RESULTS.md "E2E cleanup" records: pre-delete marker count, deleted references (equal to the pre-delete set and containing every recorded ref), and a post-delete marker count of 0.
    - If account deletion was requested, 0 auth users remain for that email.
  </acceptance_criteria>
  <resume-signal>Paste the cleanup query results</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QA runner -> production | Real requests, real booking captures |
| Orchestrator (Supabase MCP) -> production database | Row deletion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-41 | Tampering | production bookings deletion | high | mitigate | Delete only rows matching the full marker (email pattern AND first/last name AND unpaid); pre-select, reference cross-check, RETURNING set equality, post-delete zero check; stop on any partial-marker row |
| T-75-42 | Tampering | leftover test data in dispatch/admin | medium | mitigate | Post-delete count must be 0; recorded in results |
| T-75-07 | Elevation of Privilege | payment | high | mitigate | booking_e2e never submits payment (plan 75-03 design) |
| T-75-01 | Tampering | production analytics | medium | mitigate | All QA scripts abort GA4/Meta hits |
| T-75-SC | Tampering | package installs | low | accept | No installs |
</threat_model>

<verification>
- All scripts run on production with results recorded; VER-01 facets assessed; cleanup verified with zero remaining marker rows.
</verification>

<success_criteria>
VER-01 is proven (or its gaps precisely evidenced) on production for all 7 locales, D-05 surfaces are recorded, and no E2E data remains in production.
</success_criteria>

<output>
Create `.planning/phases/75-e2e-verification-launch/75-20-SUMMARY.md` when done
</output>
