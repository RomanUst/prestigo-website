---
phase: 75-e2e-verification-launch
plan: 14
type: execute
wave: 4
depends_on: [75-02, 75-04, 75-06, 75-07, 75-08, 75-09, 75-10, 75-11, 75-12, 75-13, 75-15, 75-16]
files_modified:
  - components/booking/TripTypeTabs.tsx
  - components/booking/BookingWizard.tsx
  - components/booking/BookingWidget.tsx
  - components/ArticleByline.tsx
  - app/[locale]/book/confirmation/page.tsx
  - tests/TripTypeTabs.test.tsx
  - tests/BookingWidget.test.tsx
  - tests/BookingWizard.test.tsx
  - tests/locale-links-backstop.test.ts
autonomous: true
requirements: [VER-01]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Inside the booking flow every client-side navigation keeps the locale: the TripTypeTabs multi-day tab, the BookingWizard quote-flow confirmation push and the BookingWidget hand-off to /book use the locale-aware router from @/i18n/routing, so /ru/book -> multi-day tab lands on /ru/book/multi-day (not the EN page) (VER-01 booking per-locale, D-01)."
    - "ArticleByline and the confirmation page's links use the i18n Link, so blog bylines and the post-booking 'book again' link stay in the locale (the confirmation page's English copy itself is recorded as-is per D-05, not translated here)."
    - "tests/locale-links-backstop.test.ts runs the plan-75-02 scanner's R3 rule over app/[locale]/** and components/** and fails on any root-relative anchor href literal/template, next/link default import, useRouter from next/navigation, or root-relative string-literal redirect() from next/navigation outside the reasoned allowlist — it passes with zero findings after all phase-75 fix plans (a permanent regression guard for locale continuity)."
    - "Existing booking tests (TripTypeTabs, BookingWidget, BookingWizard, confirmation-page) pass with their router mocks retargeted to @/i18n/routing."
  artifacts:
    - "tests/locale-links-backstop.test.ts"
  key_links:
    - "tests/locale-links-backstop.test.ts -> scripts/qa/en_leak_static.mjs scanTree() R3 + scripts/qa/en_leak_allowlist.json (single detector, two consumers)"
    - "useRouter from @/i18n/routing -> next-intl locale-aware push/replace (adds the active locale prefix)"
  prohibitions:
    - "MUST NOT add an allowlist entry to make the backstop pass without a D-09/D-05/external-link reason."
    - "MUST NOT translate the confirmation page copy in this phase (D-05: recorded as-is; logged as deferred)."
---

<objective>
Finish locale continuity inside the booking flow (tab switch to multi-day, quote-flow confirmation push, widget hand-off, byline and confirmation links) and lock the whole phase's link work behind a permanent backstop test that reuses the static scanner's navigation rule.

Purpose: VER-01 "booking completes per-locale" fails if a Russian user clicking the multi-day tab or submitting a quote is pushed onto English routes; the backstop makes the ~210 link fixes of plans 75-06..75-16 impossible to silently regress.
Output: locale-aware routers/links in 5 files and `tests/locale-links-backstop.test.ts` (green only when every fix plan has landed).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-e2e-verification-launch/75-CONTEXT.md
@.planning/phases/75-e2e-verification-launch/75-02-SUMMARY.md
</context>

## Artifacts this phase produces

- `tests/locale-links-backstop.test.ts` — imports `scanTree` from `scripts/qa/en_leak_static.mjs`, filters rule R3, asserts zero non-allowlisted findings.
- Locale-aware navigation in `components/booking/TripTypeTabs.tsx`, `components/booking/BookingWizard.tsx`, `components/booking/BookingWidget.tsx`, `components/ArticleByline.tsx`, `app/[locale]/book/confirmation/page.tsx`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — TripTypeTabs multi-day tab keeps the locale (i18n router), proven by test</name>
  <files>components/booking/TripTypeTabs.tsx, tests/TripTypeTabs.test.tsx</files>
  <read_first>
    - components/booking/TripTypeTabs.tsx (navigate tab kind with href /book/multi-day; router.push)
    - i18n/routing.ts (useRouter export from createNavigation)
    - tests/TripTypeTabs.test.tsx (current next/navigation router mock)
  </read_first>
  <behavior>
    - Clicking the multi-day tab calls the @/i18n/routing router's push with '/book/multi-day' (the i18n router adds the active locale prefix); next/navigation's router is no longer imported.
  </behavior>
  <action>Switch TripTypeTabs' useRouter import to @/i18n/routing (hrefs stay unprefixed — the i18n router applies the locale). Retarget tests/TripTypeTabs.test.tsx's router mock to @/i18n/routing and assert the behavior; render under ru via renderWithIntl to prove it still works under a non-EN locale.</action>
  <verify>
    <automated>npx vitest run tests/TripTypeTabs.test.tsx tests/Step1TripType.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tests pass; `grep -n "from 'next/navigation'" components/booking/TripTypeTabs.tsx` shows no useRouter import.
  </acceptance_criteria>
  <done>The booking wizard's multi-day tab keeps the visitor's locale.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BookingWizard, BookingWidget, ArticleByline, confirmation links + the locale-links backstop test</name>
  <files>components/booking/BookingWizard.tsx, components/booking/BookingWidget.tsx, components/ArticleByline.tsx, app/[locale]/book/confirmation/page.tsx, tests/BookingWidget.test.tsx, tests/BookingWizard.test.tsx, tests/locale-links-backstop.test.ts</files>
  <read_first>
    - components/booking/BookingWizard.tsx (quote flow router.push to the confirmation path with type=quote)
    - components/booking/BookingWidget.tsx (router.push('/book') hand-off)
    - components/ArticleByline.tsx (next/link to the author page)
    - app/[locale]/book/confirmation/page.tsx (next/link 'book now' link; D-05: text stays)
    - scripts/qa/en_leak_static.mjs and scripts/qa/en_leak_allowlist.json (plan 75-02: scanTree, R3, staticIgnoreFiles / reasons)
    - tests/BookingWidget.test.tsx, tests/BookingWizard.test.tsx, tests/confirmation-page.test.tsx (router/link mocks to retarget)
  </read_first>
  <behavior>
    - BookingWizard quote submission pushes the confirmation path via the i18n router; BookingWidget pushes '/book' via the i18n router.
    - ArticleByline and the confirmation page render the i18n Link (href unprefixed in source, prefixed at runtime by next-intl).
    - tests/locale-links-backstop.test.ts: scanTree over app/[locale] and components, rule R3, minus allowlisted files/entries, yields zero findings.
  </behavior>
  <action>Switch BookingWizard and BookingWidget to useRouter from @/i18n/routing; switch ArticleByline and the confirmation page's next/link imports to the i18n Link (confirmation copy untouched per D-05). Update the router/link mocks in tests/BookingWidget.test.tsx and tests/BookingWizard.test.tsx (and tests/confirmation-page.test.tsx only if its mock targets next/link — keep the D-05 copy assertions as they are). Create tests/locale-links-backstop.test.ts that imports scanTree from ../scripts/qa/en_leak_static.mjs, runs it over the repo root, keeps only R3 findings not covered by scripts/qa/en_leak_allowlist.json, and expects an empty list — on failure the assertion message lists file:line so the offending link is obvious. If findings remain in files owned by earlier plans, fix them here only when trivially mechanical (swap to getPathname/localizedHref/i18n Link) and list each in the SUMMARY as a Rule-1 deviation; never widen the allowlist to pass.</action>
  <verify>
    <automated>npx vitest run tests/locale-links-backstop.test.ts tests/BookingWidget.test.tsx tests/BookingWizard.test.tsx tests/confirmation-page.test.tsx tests/TripTypeTabs.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All listed tests pass; `tests/locale-links-backstop.test.ts` reports zero R3 findings.
    - `grep -rn "from 'next/link'" components app/[locale] --include=*.tsx` returns only allowlisted D-09 files (or nothing).
    - `git diff scripts/qa/en_leak_allowlist.json` is empty in this plan (allowlist not widened).
  </acceptance_criteria>
  <done>Every navigation in the booking flow keeps the locale and a permanent backstop guards all phase-75 link work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client router -> next route | Router choice decides the locale of the next page |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-21 | Tampering (navigation) | booking-flow routers/links | low | mitigate | i18n router/Link only; backstop test fails on any regression |
| T-75-27 | Repudiation | backstop allowlist | medium | mitigate | Allowlist must not change in this plan (git diff gate); every entry carries a reason from plan 75-02 |
| T-75-SC | Tampering | package installs | low | accept | No installs |
</threat_model>

<verification>
- `npx vitest run tests/locale-links-backstop.test.ts tests/TripTypeTabs.test.tsx tests/BookingWidget.test.tsx tests/BookingWizard.test.tsx tests/confirmation-page.test.tsx` green.
</verification>

<success_criteria>
The booking flow never drops the visitor's locale and a permanent backstop test enforces locale continuity across app/[locale] and components.
</success_criteria>

<output>
Create `.planning/phases/75-e2e-verification-launch/75-14-SUMMARY.md` when done
</output>
