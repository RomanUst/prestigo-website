---
phase: 75-e2e-verification-launch
plan: 21
type: execute
wave: 8
depends_on: [75-18, 75-20]
files_modified:
  - scripts/qa/gsc_indexing_baseline.py
  - .planning/phases/75-e2e-verification-launch/75-LAUNCH.md
  - .planning/phases/75-e2e-verification-launch/75-QA-RESULTS.md
autonomous: false
requirements: [VER-01]
user_setup:
  - service: google-search-console
    why: "Sitemap resubmit and Request Indexing need write access; the existing OAuth token is webmasters.readonly (RESEARCH Pitfall 7)"
    dashboard_config:
      - task: "Resubmit https://rideprestigo.com/sitemap.xml and request indexing for the listed locale URLs"
        location: "Search Console -> property sc-domain:rideprestigo.com -> Sitemaps / URL Inspection"
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "scripts/qa/gsc_indexing_baseline.py inspects 7 locales x 6 key pages (/, /book, /fleet, /routes, /routes/prague-vienna, /services/airport-transfer) with the existing read-only GSC token and records coverage state, indexing state, last crawl time and Google-selected canonical per URL into 75-LAUNCH.md as the per-locale indexing baseline; an expired token exits with a clear re-auth instruction instead of a traceback (D-14)."
    - "The sitemap is resubmitted in GSC and indexing is requested for up to 10 key locale URLs listed in 75-LAUNCH.md; the Rich Results Test passes on the sampled locale URLs; both results are recorded (D-14)."
    - "The 7-language announcement exists only as Metricool DRAFTS for Instagram and Facebook on analytics-picked, distinct day/hour slots (best-time data + free slots), created through the Metricool MCP with draft true and verified as drafts after creation — nothing is published (D-14)."
    - "The announcement copy uses plain words, opens with the traveller's pain and its fix, lists the 7 languages, contains no price or currency amount and no ride-hailing comparison, and uses an existing on-site photo of the current Mercedes fleet (no gloves/cap on any chauffeur, standard doors)."
    - "75-LAUNCH.md ends with the milestone-close handoff: after /gsd-verify-work 75 passes, run /gsd-audit-milestone then /gsd-complete-milestone v3.0 (creates tag v3.0) — these are workflow-level commands that cannot run inside this phase's execution (D-14 milestone close)."
    - "empty edge: a GSC inspection returning no data for a URL (never crawled) is recorded as 'not yet known to Google', not dropped."
  artifacts:
    - "scripts/qa/gsc_indexing_baseline.py"
    - ".planning/phases/75-e2e-verification-launch/75-LAUNCH.md"
  key_links:
    - "~/.config/google-ads/gsc_token.json (webmasters.readonly) -> urlInspection().index().inspect() -> 75-LAUNCH.md baseline"
    - "75-LAUNCH.md announcement copy -> Metricool MCP createScheduledPost (draft true) -> Metricool planner (user un-drafts manually)"
  prohibitions:
    - "MUST NOT publish any Metricool post live or flip a draft to scheduled/published — drafts only; the user un-drafts in the Metricool planner."
    - "MUST NOT state a price, 'from EUR X', rate or discount in announcement copy."
    - "MUST NOT compare PRESTIGO to Uber or any ride-hailing app in announcement copy."
    - "MUST NOT print or commit OAuth token contents."
---

<objective>
Execute the D-14 launch steps: record a per-locale GSC indexing baseline, have the sitemap resubmitted and key locale URLs submitted for indexing, confirm Rich Results on sampled locale URLs, create the 7-language announcement as Metricool drafts only, and hand off the milestone close.

Purpose: "launch" in this phase means discoverability + validation + announcement drafts + milestone close — the site is already live.
Output: `gsc_indexing_baseline.py`, `75-LAUNCH.md` (baseline, URLs, copy, results, handoff), Metricool drafts, results in `75-QA-RESULTS.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-e2e-verification-launch/75-CONTEXT.md
@.planning/phases/75-e2e-verification-launch/75-RESEARCH.md
@.planning/phases/75-e2e-verification-launch/75-QA-RESULTS.md
</context>

## Artifacts this phase produces

- `scripts/qa/gsc_indexing_baseline.py` — CLI `python3 scripts/qa/gsc_indexing_baseline.py [--token ~/.config/google-ads/gsc_token.json] [--site sc-domain:rideprestigo.com]`, output `scripts/qa/out/gsc_indexing_baseline.json`; exit 3 on expired/invalid token.
- `.planning/phases/75-e2e-verification-launch/75-LAUNCH.md` — indexing baseline table, Request-Indexing URL list, Rich Results sample list + results, announcement copy (IG + FB), chosen image URL, Metricool draft IDs/slots, milestone-close handoff.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — GSC indexing baseline script run end-to-end + launch brief (URLs, RRT sample, announcement copy)</name>
  <files>scripts/qa/gsc_indexing_baseline.py, .planning/phases/75-e2e-verification-launch/75-LAUNCH.md</files>
  <read_first>
    - ~/Desktop/founder prestigo/gsc_request_indexing.py and gsc_now.py (existing OAuth + urlInspection usage pattern; token path; never print token contents)
    - .planning/phases/75-e2e-verification-launch/75-RESEARCH.md § Pitfall 7 (read-only scope: inspection works, submit does not)
    - app/[locale]/fleet/page.tsx hero/vehicle photos and public/ (existing images to choose the announcement visual from)
    - .planning/phases/75-e2e-verification-launch/75-CONTEXT.md D-14 and Specific Ideas (copy rules)
  </read_first>
  <action>Create scripts/qa/gsc_indexing_baseline.py using google-api-python-client and google-auth (already installed): load the authorized-user token from --token (default ~/.config/google-ads/gsc_token.json), refresh if needed; on invalid_grant or missing token print "GSC token expired — re-auth: python3 ~/Desktop/founder\ prestigo/gsc_auth_setup.py" and exit 3 (the orchestrator then treats it as an auth gate for the user). For the 42 URLs (7 locales x /, /book, /fleet, /routes, /routes/prague-vienna, /services/airport-transfer; EN unprefixed) call searchconsole urlInspection.index.inspect sequentially with siteUrl sc-domain:rideprestigo.com and record verdict, coverageState, indexingState, lastCrawlTime, googleCanonical, userCanonical; an empty result is recorded as "not yet known to Google". Write scripts/qa/out/gsc_indexing_baseline.json (deterministic order) and never print token data. Run it. Create 75-LAUNCH.md with: the baseline table grouped by locale with per-locale indexed counts; a Request-Indexing list of 10 URLs (the 6 non-EN homepages plus /ru/book, /ar/book, /es/fleet, /zh/routes); a Rich Results sample list (/ru/routes/prague-vienna, /ar/faq, /es/fleet, /zh/routes, /hi/services/airport-transfer) — run python3 scripts/qa/jsonld_audit.py for these first and note its result; a section headed exactly "## Announcement copy" holding only the Instagram and Facebook captions in English (primary audience) with one line naming all 7 languages by endonym, opening with the traveller's pain (booking a driver abroad in a language you do not read) and its fix (book, read and pay in your own language; same door-to-door chauffeur, fixed quote at rideprestigo.com), with no price/currency amount and no ride-hailing comparison; and the chosen visual: an existing public image URL on rideprestigo.com showing the current Mercedes fleet — open it with the Read tool and confirm current-generation E/S/V-Class, standard front-hinged doors, and no chauffeur wearing gloves or a cap; TikTok is excluded because it requires a video asset (noted in the file).</action>
  <verify>
    <automated>python3 scripts/qa/gsc_indexing_baseline.py; test $? -eq 0 -o $? -eq 3 && grep -n "Request-Indexing\|Rich Results\|Instagram\|Facebook" .planning/phases/75-e2e-verification-launch/75-LAUNCH.md && ! awk '/^## Announcement copy/{f=1;next}/^## /{f=0}f' .planning/phases/75-e2e-verification-launch/75-LAUNCH.md | grep -niE "€|eur [0-9]|[0-9]+ ?eur|uber"</automated>
  </verify>
  <acceptance_criteria>
    - The script exits 0 with 42 recorded URLs, or 3 with the re-auth message (then the baseline is completed after the user re-auths).
    - 75-LAUNCH.md contains the baseline table, the 10-URL Request-Indexing list, the 5-URL Rich Results sample, IG + FB copy and the verified image URL.
    - The "## Announcement copy" section of 75-LAUNCH.md contains no currency amount and no ride-hailing brand (checked by the awk-scoped grep in verify).
  </acceptance_criteria>
  <done>The per-locale indexing baseline is recorded and the launch brief (URLs, RRT sample, compliant copy and visual) is ready.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Orchestrator creates the announcement as Metricool DRAFTS via the Metricool MCP</name>
  <action>Create Instagram and Facebook announcement drafts through the Metricool MCP (executor subagents have no MCP access)</action>
  <instructions>
    Claude wrote the copy and the image URL into 75-LAUNCH.md and the shared client is draft-capable (plan 75-18). Using the Metricool MCP for brand blogId 6581736, timezone Europe/Prague:
    1. getBestTimeToPostByNetwork for instagram and facebook, and getScheduledPosts for the next 14 days to avoid taken slots.
    2. Pick a different day AND hour for the Instagram and the Facebook post, each on a best-time slot that is free.
    3. createScheduledPost once per network with the 75-LAUNCH.md copy and image URL and info.draft = true (Instagram type POST, Facebook type POST). Never set autoPublish/publish.
    4. getScheduledPosts again and confirm both new posts are drafts.
    5. Paste the post IDs, networks, slots and the draft flag values.
  </instructions>
  <verification>Claude records the IDs, slots and draft=true confirmation in 75-LAUNCH.md; any post not in draft state is a blocking failure reported to the user immediately.</verification>
  <read_first>
    - .planning/phases/75-e2e-verification-launch/75-LAUNCH.md "## Announcement copy" section and chosen image URL
  </read_first>
  <acceptance_criteria>
    - 75-LAUNCH.md records one Instagram and one Facebook Metricool post ID, each on a different day and hour, each with draft = true confirmed by a post-creation read.
  </acceptance_criteria>
  <resume-signal>Paste the Metricool draft IDs, slots and draft flags</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: User resubmits the sitemap, requests indexing and runs the Rich Results Test</name>
  <action>GSC sitemap resubmit + Request Indexing + Rich Results Test (web-UI-only with the read-only token)</action>
  <instructions>
    Claude recorded the indexing baseline and listed the URLs in 75-LAUNCH.md.
    1. Search Console (sc-domain:rideprestigo.com) -> Sitemaps -> submit https://rideprestigo.com/sitemap.xml.
    2. URL Inspection -> Request indexing for each of the 10 URLs in the Request-Indexing list (the daily quota is about 10).
    3. https://search.google.com/test/rich-results -> test each of the 5 sampled URLs; note "valid items detected" or the errors.
    4. Reply with: sitemap status, which URLs were accepted for indexing, and the Rich Results outcome per URL.
  </instructions>
  <verification>Claude records the replies in 75-LAUNCH.md and 75-QA-RESULTS.md (D-14 section), then appends the milestone-close handoff: after /gsd-verify-work 75 passes -> /gsd-audit-milestone -> /gsd-complete-milestone v3.0 (tag v3.0).</verification>
  <read_first>
    - .planning/phases/75-e2e-verification-launch/75-LAUNCH.md (Request-Indexing list and Rich Results sample)
  </read_first>
  <acceptance_criteria>
    - 75-LAUNCH.md and 75-QA-RESULTS.md record the sitemap submission status, the indexing-request outcome per URL and the Rich Results outcome per sampled URL.
    - 75-LAUNCH.md ends with the milestone-close handoff (/gsd-verify-work 75 -> /gsd-audit-milestone -> /gsd-complete-milestone v3.0).
  </acceptance_criteria>
  <resume-signal>Paste the sitemap status, indexing requests and Rich Results outcomes</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local OAuth token -> Google APIs | Read-only Search Console access |
| Orchestrator (Metricool MCP) -> public social accounts | Posts created on real brand accounts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-43 | Tampering (public brand content) | Metricool announcement | high | mitigate | Drafts only via MCP with info.draft true, verified draft state after creation, blocking checkpoint; copy lint for prices and ride-hailing comparisons |
| T-75-44 | Information Disclosure | GSC OAuth token | medium | mitigate | Token read from outside the repo, never printed or committed; read-only scope; expired token -> explicit re-auth exit code |
| T-75-45 | Repudiation | launch evidence | low | mitigate | Baseline JSON + 75-LAUNCH.md record every URL, slot and result |
| T-75-SC | Tampering | package installs | low | accept | No installs — google-api-python-client already installed |
</threat_model>

<verification>
- Indexing baseline recorded; sitemap resubmitted; indexing requested; Rich Results recorded; Metricool drafts verified as drafts; milestone-close handoff written.
</verification>

<success_criteria>
D-14 launch is complete: GSC baseline + resubmit + indexing requests, Rich Results confirmed on sampled locale URLs, announcement exists only as compliant Metricool drafts, and the milestone close is handed off to /gsd-audit-milestone -> /gsd-complete-milestone.
</success_criteria>

<output>
Create `.planning/phases/75-e2e-verification-launch/75-21-SUMMARY.md` when done
</output>
