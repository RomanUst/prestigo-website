---
phase: 75-e2e-verification-launch
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/qa/booking_e2e.py
autonomous: true
requirements: [VER-01]
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "`python3 scripts/qa/booking_e2e.py https://rideprestigo.com --locales en` drives a guest booking through all 6 wizard steps (route/date -> vehicle -> guest auth choice -> extras -> passenger -> payment) on production and stops when the Stripe Payment Element iframe is rendered and visible — it never clicks the pay button and never submits card data (D-01, D-02)."
    - "Every E2E booking carries the D-03 marker: passenger first name 'E2E', last name 'TEST', email e2e+{locale}@rideprestigo.com for guests and e2e+account-{locale}@rideprestigo.com for the account path; the bookingReference returned by /api/create-payment-intent is appended (deduplicated) to scripts/qa/out/booking_e2e_refs.json so cleanup can cross-check exact rows (D-03)."
    - "With --locales en,ru,es,fr,ar,hi,zh the script runs the guest path in every locale using button/label text resolved from messages/<locale>.json (a label still in English on a localized page makes the locator fail — the flow itself is a leak detector), asserts html dir=rtl on ar, records the Stripe Payment Element locale (expected en/ru/es/fr/ar/zh = same code; hi = auto semantics, never forced en) and the Google Places autocomplete request languageCode (expected = locale, zh = zh-CN) (D-01, D-07)."
    - "With --account ru,ar the script signs in at /<locale>/login using credentials from the git-ignored scripts/qa/.e2e-account.json, verifies /<locale>/account/trips (My trips) renders in that locale (html lang, no Latin leak outside allowlist), then drives the signed-in booking to the rendered Stripe form (D-04)."
    - "The script paces runs (>= 20 s between create-payment-intent calls) and on HTTP 429 waits 65 s and retries once, so the production rate limiter is respected; GA4/Meta hits are aborted so no E2E funnel events reach analytics."
    - "empty edge: when scripts/qa/.e2e-account.json is absent, --account runs are skipped with an explicit 'account credentials missing' result entry (not a crash, not a silent pass)."
  artifacts:
    - "scripts/qa/booking_e2e.py"
  key_links:
    - "scripts/qa/booking_e2e.py -> /api/create-payment-intent response bookingReference -> scripts/qa/out/booking_e2e_refs.json -> strict marker cleanup in plan 75-20 (D-03)"
    - "messages/<locale>.json Booking.* labels -> Playwright locators (locale-aware selectors, no data-testid code change needed)"
  prohibitions:
    - "MUST NOT submit a real payment or type card data — the run stops at the rendered Stripe Payment Element (D-01)."
    - "MUST NOT use any passenger identity other than the E2E TEST marker, so every production row it creates is removable by the strict marker filter (D-03)."
    - "MUST NOT print or commit the E2E account password (scripts/qa/.e2e-account.json is git-ignored by plan 75-01)."
---

<objective>
Author the per-locale booking E2E driver locked by D-01/D-02/D-03/D-04: guest checkout through all 6 wizard steps up to a rendered, localized Stripe payment form on production in all 7 locales (incl. RTL `/ar`), plus the sign-in + "My trips" account path in RU and AR — without ever paying — with every created row carrying the `E2E TEST` marker and its reference recorded for strict cleanup.

Purpose: VER-01 "booking completes per-locale (incl. RTL), guest checkout intact" is only provable by driving the real wizard; this script is also the only automated check of the Stripe Elements locale and Google Places language (D-07 surface 4).
Output: `scripts/qa/booking_e2e.py` (tracer-proven on EN against current production; full post-fix run happens in plan 75-20).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-e2e-verification-launch/75-CONTEXT.md
@.planning/phases/75-e2e-verification-launch/75-RESEARCH.md
@.planning/phases/75-e2e-verification-launch/75-PATTERNS.md
@scripts/qa/overflow_audit.py
</context>

## Artifacts this phase produces

- `scripts/qa/booking_e2e.py` — CLI `python3 scripts/qa/booking_e2e.py [base_url] [--locales en,...] [--account ru,ar] [--headed]`; outputs `scripts/qa/out/booking_e2e.json` (per-run result) and `scripts/qa/out/booking_e2e_refs.json` (deduplicated list of `{ref, locale, path: guest|account, createdAt}`); screenshots `scripts/qa/out/booking_e2e_<locale>_<path>.png`.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — EN guest booking on production through 6 steps to a rendered Stripe Payment Element (no payment)</name>
  <files>scripts/qa/booking_e2e.py</files>
  <read_first>
    - scripts/qa/overflow_audit.py (browser/consent skeleton to copy)
    - components/booking/BookingWizard.tsx (step order: 1 EntryBar, 2 Step3Vehicle, 3 Step3Auth, 4 Step4Extras, 5 Step5Passenger, 6 Step6Payment; step-2 advance lives in StickyBookingPanel)
    - components/booking/EntryBar.tsx and components/booking/AddressInputNew.tsx (origin/destination autocomplete inputs, date/time pickers)
    - components/booking/steps/Step3Auth.tsx (guest continuation control)
    - components/booking/steps/Step5Passenger.tsx (first/last/email/phone fields and validation)
    - components/booking/steps/Step6Payment.tsx (POST /api/create-payment-intent -> bookingReference; Elements + PaymentElement render)
    - app/api/create-payment-intent/route.ts (12-hour lead-time rule, rate limit per IP per 60 s)
    - messages/en.json Booking namespace (button/label text used as locators)
  </read_first>
  <action>Create scripts/qa/booking_e2e.py (argparse: positional base_url default https://rideprestigo.com, --locales default en, --account default empty, --headed flag). Browser: Chromium desktop 1280x900, consent localStorage pre-set as in overflow_audit.py, context route aborting GA4/Meta requests (google-analytics.com, googletagmanager.com collect, facebook.com/tr, connect.facebook.net). A response listener on /api/create-payment-intent captures the JSON body's bookingReference (and returnBookingReference if present) and the HTTP status. Build a label resolver that loads messages/<locale>.json and returns the string at a dotted Booking.* key so every locator uses the locale's own text (for EN it simply reads messages/en.json). Guest flow for one locale: open /book (prefixed for non-EN); step 1 — type a non-airport origin (Old Town Square, Prague) and destination (Prague Castle) into the address inputs, pick the first autocomplete suggestion each time, choose a pickup date at least 3 days ahead and a daytime slot, advance; step 2 — select the first available vehicle and advance via the sticky panel control; step 3 — choose the guest path; step 4 — keep default extras and advance; step 5 — fill first name E2E, last name TEST, email e2e+<locale>@rideprestigo.com, phone +420 700 000 000 (adjust only the digits if client validation rejects it), advance; step 6 — wait (max 60 s) for the Stripe Payment Element iframe (frame whose src contains js.stripe.com and elements-inner) to be attached and visible, then STOP: take a screenshot, read the iframe src and record any locale parameter found in its query or hash. Never click the pay control, never type into Stripe fields. Append the captured bookingReference to scripts/qa/out/booking_e2e_refs.json (create if missing; list of objects; deduplicate by ref). Result per run: {locale, path, reachedStripe, bookingReference, stripeLocaleParam, htmlLang, htmlDir, durationMs, error}. Write scripts/qa/out/booking_e2e.json as an object {"runs": [ ...one result object per run... ]} in deterministic order; exit 1 if any run failed to reach the Stripe form. Discover real selectors on the live DOM during this task (run --headed locally if needed); prefer role/label locators resolved through the label resolver over CSS classes. Run once against production for en.</action>
  <verify>
    <automated>python3 scripts/qa/booking_e2e.py https://rideprestigo.com --locales en && python3 -c "import json;r=json.load(open('scripts/qa/out/booking_e2e.json'));assert all(x['reachedStripe'] for x in r['runs']);refs=json.load(open('scripts/qa/out/booking_e2e_refs.json'));assert len(refs)>=1;print('EN guest reached Stripe; refs recorded:',len(refs))"</automated>
  </verify>
  <acceptance_criteria>
    - The EN production run exits 0, `scripts/qa/out/booking_e2e.json` shows `reachedStripe: true` for en/guest and a non-empty `bookingReference` matching `^(PRG|QR)-`.
    - `scripts/qa/out/booking_e2e_refs.json` contains that reference with `locale: "en"` and `path: "guest"`.
    - The script source contains no call that clicks a submit/pay control inside step 6 and no interaction with Stripe iframe inputs (only presence/visibility + src read).
    - The screenshot `scripts/qa/out/booking_e2e_en_guest.png` shows the payment form.
  </acceptance_criteria>
  <done>One real guest booking path runs end-to-end on production to the rendered Stripe form with the E2E marker, and its reference is recorded for cleanup.</done>
</task>

<task type="auto">
  <name>Task 2: All 7 locales + RTL + Stripe/Places locale capture + RU/AR account path</name>
  <files>scripts/qa/booking_e2e.py</files>
  <read_first>
    - scripts/qa/booking_e2e.py (Task 1)
    - messages/ru.json, messages/ar.json, messages/zh.json (the same Booking.* keys the resolver reads)
    - app/[locale]/login/page.tsx (password tab control, email/password inputs, return-to handling)
    - app/[locale]/account/trips/page.tsx (My trips page)
    - components/booking/steps/Step3Auth.tsx (signed-in state shows prefilled passenger / skip)
    - .planning/phases/75-e2e-verification-launch/75-RESEARCH.md § Pitfall 1 (Stripe has no hi locale -> auto) and Summary item 2 (Places supports all 7)
  </read_first>
  <action>Generalize the flow over --locales (routing order) with pacing: wait at least 20 s between runs and, on a 429 from create-payment-intent, wait 65 s and retry that run once. Per run additionally record (result keys htmlLang, htmlDir, placesLanguage, stripeLocaleParam, stripeFieldLabel, expectedStripeLocale, expectedPlacesLanguage, localeChecksPassed): html lang/dir (assert dir rtl for ar, ltr otherwise); the languageCode found in the JSON body of the first request to places.googleapis.com autocomplete (capture via request listener; also record a language query param if the legacy maps endpoint is used); and the Stripe Payment Element locale — read from the elements iframe src locale parameter, and if Stripe does not expose it there, read the visible card-number field label through frame_locator and record it as stripeFieldLabel. Expected values: stripe locale = locale for en/ru/es/fr/ar/zh and auto (or no forced en) for hi; places languageCode = locale, zh = zh-CN. Mismatches are recorded as failures in the result (expected pre-fix: non-EN runs fail these two checks — that is the baseline plan 75-05 fixes). Account path (--account list): read scripts/qa/.e2e-account.json ({email, password}); if missing, emit a result {path: account, skipped: true, reason: 'account credentials missing'} per requested locale and continue. Otherwise: open /<locale>/login, switch to the password tab via its localized label, sign in, open /<locale>/account/trips, record html lang and whether any visible text has a run of 2+ Latin words outside scripts/qa/en_leak_allowlist.json when present (skip that sub-check with a note if the allowlist file does not exist yet), then run the booking flow; at step 3 the signed-in state must not force the guest path; at step 5 overwrite passenger fields with the marker (E2E / TEST / e2e+account-<locale>@rideprestigo.com) so the row matches the cleanup filter. Never log the password. Run the guest path against production for ru (baseline; expected to reach Stripe but with an English Stripe/Places locale) and record the outcome in the SUMMARY.</action>
  <verify>
    <automated>python3 scripts/qa/booking_e2e.py https://rideprestigo.com --locales ru; test $? -le 1 && python3 -c "import json;r=json.load(open('scripts/qa/out/booking_e2e.json'))['runs'];ru=[x for x in r if x['locale']=='ru'][0];assert 'stripeLocaleParam' in ru and 'placesLanguage' in ru and ru['htmlDir']=='ltr';print('ru baseline recorded, reachedStripe=',ru['reachedStripe'])"</automated>
  </verify>
  <acceptance_criteria>
    - Running with `--locales ru` against production records `stripeLocaleParam` (or `stripeFieldLabel`), `placesLanguage`, `htmlLang`, `htmlDir` for the ru run.
    - Running with `--account ru` while `scripts/qa/.e2e-account.json` is absent produces a `skipped: true` result with reason `account credentials missing` and exit code 0 or 1 (never an exception traceback).
    - The script contains pacing (>= 20 s) and single 429 retry after 65 s.
    - `grep -n "password" scripts/qa/booking_e2e.py` shows no print/log statement that outputs the password value.
  </acceptance_criteria>
  <done>The booking E2E covers all 7 locales, RTL, Stripe/Places locale capture and the RU/AR account path, and has recorded the pre-fix ru baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E runner -> production booking API | Real create-payment-intent calls create live PaymentIntents and unpaid booking rows |
| Local credentials file -> git | Test-account password lives on disk next to the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-06 | Tampering | production bookings table | high | mitigate | Every run uses the E2E/TEST/e2e+ marker and records its bookingReference; plan 75-20 deletes strictly by marker cross-checked against recorded refs (D-03) |
| T-75-07 | Elevation of Privilege | payment | high | mitigate | Script never clicks pay and never touches Stripe inputs; PaymentIntents stay requires_payment_method (no charge) |
| T-75-08 | Information Disclosure | scripts/qa/.e2e-account.json | medium | mitigate | File git-ignored (plan 75-01), password never printed, account path skips cleanly when the file is absent |
| T-75-09 | Denial of Service | create-payment-intent rate limiter | low | mitigate | >= 20 s pacing and a single 65 s back-off retry on 429 |
| T-75-SC | Tampering | package installs | low | accept | No installs — Playwright already present |
</threat_model>

<verification>
- EN guest tracer reaches the Stripe form on production; ref recorded.
- ru baseline run records Stripe/Places locale values.
- Account path skips cleanly without credentials.
</verification>

<success_criteria>
A repeatable, marker-safe booking E2E (D-01..D-04) exists and is proven on production; it measures the Stripe Elements locale and Places language per locale and records every created booking reference for strict cleanup.
</success_criteria>

<output>
Create `.planning/phases/75-e2e-verification-launch/75-03-SUMMARY.md` when done
</output>
