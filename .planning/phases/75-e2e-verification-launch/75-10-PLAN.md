---
phase: 75-e2e-verification-launch
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/route-content.ts
  - app/[locale]/routes/prague-berlin/page.tsx
  - app/[locale]/routes/prague-bratislava/page.tsx
  - app/[locale]/routes/prague-brno/page.tsx
  - app/[locale]/routes/prague-budapest/page.tsx
  - app/[locale]/routes/prague-ceske-budejovice/page.tsx
  - app/[locale]/routes/prague-cesky-krumlov/page.tsx
  - app/[locale]/routes/prague-dresden/page.tsx
  - app/[locale]/routes/prague-frantiskovy-lazne/page.tsx
  - app/[locale]/routes/prague-graz/page.tsx
  - app/[locale]/routes/prague-hradec-kralove/page.tsx
  - app/[locale]/routes/prague-karlovy-vary/page.tsx
  - app/[locale]/routes/prague-krakow/page.tsx
  - app/[locale]/routes/prague-kutna-hora/page.tsx
  - app/[locale]/routes/prague-leipzig/page.tsx
  - app/[locale]/routes/prague-liberec/page.tsx
  - app/[locale]/routes/prague-linz/page.tsx
  - app/[locale]/routes/prague-marianske-lazne/page.tsx
  - app/[locale]/routes/prague-munich/page.tsx
  - app/[locale]/routes/prague-nuremberg/page.tsx
  - app/[locale]/routes/prague-olomouc/page.tsx
  - app/[locale]/routes/prague-ostrava/page.tsx
  - app/[locale]/routes/prague-pardubice/page.tsx
  - app/[locale]/routes/prague-passau/page.tsx
  - app/[locale]/routes/prague-plzen/page.tsx
  - app/[locale]/routes/prague-regensburg/page.tsx
  - app/[locale]/routes/prague-salzburg/page.tsx
  - app/[locale]/routes/prague-vienna/page.tsx
  - app/[locale]/routes/prague-warsaw/page.tsx
  - app/[locale]/routes/prague-wroclaw/page.tsx
  - app/[locale]/routes/prague-zlin/page.tsx
  - content/routes/en/prague-*.json
  - content/routes/ru/prague-*.json
  - content/routes/es/prague-*.json
  - content/routes/fr/prague-*.json
  - content/routes/ar/prague-*.json
  - content/routes/hi/prague-*.json
  - content/routes/zh/prague-*.json
  - tests/route-locale-links.test.tsx
  - tests/route-hero-alt.test.ts
  - .planning/phases/75-e2e-verification-launch/freeze/75-10.freeze
autonomous: true
requirements: [VER-01]
estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "All 30 route pages take their hero image alt text from content hero.imageAlt: EN values are byte-identical to today's hardcoded alts and all 6 other locales carry a translated alt (D-07 alt surface, D-08)."
    - "Every internal link on all 30 route pages (hero CTAs, bottom CTAs, related-route cards built with a template literal, author/contact links) resolves through getPathname({ locale, href }) so /ru/routes/prague-berlin links to /ru/book, /ru/contact and /ru/routes/<slug> instead of the EN root (locale continuity — planning-time discovery: 161 raw links across the route pages)."
    - "The prague-vienna EN golden byte-parity snapshot in tests/route-page-render.test.tsx is unchanged (EN hrefs and alts identical after the refactor)."
    - "The 9 hi route files whose whyBook.headingLine1 fell back to English ('Why book with Prestigo') now carry a Hindi heading that keeps the Prestigo token verbatim — WINDOWS #6 closed (D-09)."
    - "RouteContent.hero.imageAlt is a required string once all 210 content files (30 routes x 7 locales) carry it; the loader still validates slug/locale before touching the filesystem."
    - "empty edge: no route content file has an empty or whitespace-only hero.imageAlt, and no ru/ar/hi/zh imageAlt contains the English phrase 'private chauffeur transfer'."
  artifacts:
    - "hero.imageAlt in content/routes/<locale>/prague-*.json (210 files)"
    - "lib/route-content.ts RouteContent.hero.imageAlt"
    - "tests/route-locale-links.test.tsx, tests/route-hero-alt.test.ts"
    - ".planning/phases/75-e2e-verification-launch/freeze/75-10.freeze"
  key_links:
    - "content/routes/<locale>/<slug>.json hero.imageAlt -> route page hero image alt"
    - "route page locale (already in scope for getRouteContent) -> getPathname({ locale, href }) for every internal anchor"
    - "freeze/75-10.freeze -> plan 75-18 manifest freeze of the 30 hero.imageAlt units and the 9 whyBook.headingLine1 units"
  prohibitions:
    - "MUST NOT run node scripts/i18n-translate.mjs --dry-run (or any non --check mode) on the repo tree (D-08)."
    - "MUST NOT string-join a locale code and a path to build a link — always the i18n/routing getPathname bridge (Phase 74 prohibition)."
    - "MUST NOT change EN route page output (alts and hrefs render identically for en)."
---

<objective>
Close the remaining EN leaks on the 30 intercity route pages: the hardcoded English hero `alt`, the ~161 internal links that drop the locale prefix, and the WINDOWS #6 `hi` headings that fell back to English — with EN output byte-identical (D-07/D-08/D-09).

Purpose: route pages are the highest-traffic SEO surface; every localized visitor currently gets bounced to the English booking/contact/related-route pages on click, and hi shows English "Why book with Prestigo" headings.
Output: `hero.imageAlt` in all 210 route content files, locale-aware links in all 30 pages, 9 hi headings translated, tests, freeze patterns.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-e2e-verification-launch/75-CONTEXT.md
@.planning/phases/75-e2e-verification-launch/75-RESEARCH.md
@.planning/WINDOWS.md
</context>

## Translation rules (D-08) — apply to every in-session translation in this plan

1. You are the translator (Anthropic credit is empty; the i18n Translate workflow is disabled). Never run `node scripts/i18n-translate.mjs` in any mode other than `--check`.
2. Keep verbatim: Prestigo, PRESTIGO, Mercedes-Benz, E-Class, S-Class, V-Class, rideprestigo.com and every `{placeholder}`.
3. City names: use the exonym already in the same file's `hero.label` (after the arrow) and "Prague" as that file writes it.
4. hi: Devanagari with the Prestigo token kept in Latin inside the sentence (never dropped — the WINDOWS #6 failure mode); zh = Simplified; ar = MSA.
5. The alt template meaning is "<City> — private chauffeur transfer from Prague to <City>"; render it naturally per locale, same meaning for all 30 routes.
6. EN strings stay byte-identical.
7. Freeze file format: one pattern per line (`<sourceKey>` or `<sourceKey>::<dotPathPrefix>`), `#` comments; applied by plan 75-18.

## Artifacts this phase produces

- `hero.imageAlt: string` in `content/routes/<locale>/prague-*.json` (30 routes x 7 locales) and in the `RouteContent` type (`lib/route-content.ts`).
- Locale-aware internal links (getPathname) in all 30 `app/[locale]/routes/prague-*/page.tsx`.
- Translated `whyBook.headingLine1` in 9 `content/routes/hi/*.json` files (ceske-budejovice, wroclaw, frantiskovy-lazne, olomouc, hradec-kralove, marianske-lazne, zlin, plzen, pardubice).
- `tests/route-locale-links.test.tsx`, `tests/route-hero-alt.test.ts`.
- `.planning/phases/75-e2e-verification-launch/freeze/75-10.freeze`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — prague-vienna: content-driven alt + locale-aware links, EN snapshot unchanged, ru proven</name>
  <files>lib/route-content.ts, app/[locale]/routes/prague-vienna/page.tsx, content/routes/en/prague-vienna.json, content/routes/ru/prague-vienna.json, content/routes/es/prague-vienna.json, content/routes/fr/prague-vienna.json, content/routes/ar/prague-vienna.json, content/routes/hi/prague-vienna.json, content/routes/zh/prague-vienna.json, tests/route-locale-links.test.tsx</files>
  <read_first>
    - app/[locale]/routes/prague-vienna/page.tsx (hero Image alt ~line 112; anchors ~119-120, related-route template-literal card links ~316, bottom CTA ~337; locale variable already in scope for getRouteContent)
    - lib/route-content.ts (RouteContent.hero type; loader validation)
    - tests/route-page-render.test.tsx (EN golden byte-parity snapshot of prague-vienna — must stay green unchanged; how next-intl/server is mocked)
    - i18n/routing.ts (getPathname)
  </read_first>
  <behavior>
    - tests/route-page-render.test.tsx passes with its existing EN snapshot untouched.
    - Rendering prague-vienna with locale 'ru' (mocked getLocale -> 'ru', ru messages/content) yields a hero image alt equal to content/routes/ru/prague-vienna.json hero.imageAlt and every href beginning with a single slash starts with /ru/.
    - Rendering with 'en' yields hrefs /book, /contact and /routes/<slug> exactly as before.
  </behavior>
  <action>Add an optional imageAlt string to RouteContent.hero in lib/route-content.ts (made required in Task 2). Add hero.imageAlt to all 7 prague-vienna content files (EN = the exact current alt text; others translated per the rules). In the page, render the hero Image alt from content.hero.imageAlt and route every internal href — literal CTAs and the template-literal related-route links — through getPathname({ locale, href }) imported from @/i18n/routing, using the locale value the page already resolves for getRouteContent. Create tests/route-locale-links.test.tsx reusing the mocking approach of tests/route-page-render.test.tsx but parameterized by locale, asserting the behavior list for prague-vienna.</action>
  <verify>
    <automated>npx vitest run tests/route-page-render.test.tsx tests/route-locale-links.test.tsx tests/route-content.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/route-page-render.test.tsx` passes and `git diff --stat tests/__snapshots__/route-page-render.test.tsx.snap` is empty.
    - `tests/route-locale-links.test.tsx` passes for en and ru on prague-vienna.
    - `grep -c "getPathname" app/[locale]/routes/prague-vienna/page.tsx` is at least 1 and the page has no hardcoded alt text for the hero image.
  </acceptance_criteria>
  <done>One route page proves the pattern end-to-end: content-driven translated alt and locale-preserving links, EN byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Remaining 29 route pages + 203 content alts + 9 hi headings, required type, tests, freeze</name>
  <files>app/[locale]/routes/prague-*/page.tsx (29 remaining), content/routes/*/prague-*.json, lib/route-content.ts, tests/route-locale-links.test.tsx, tests/route-hero-alt.test.ts, .planning/phases/75-e2e-verification-launch/freeze/75-10.freeze</files>
  <read_first>
    - app/[locale]/routes/prague-vienna/page.tsx (Task 1 — the exact transformation to repeat)
    - content/routes/hi/prague-plzen.json and content/routes/en/prague-plzen.json (whyBook.headingLine1 EN fallback example)
    - .planning/WINDOWS.md entry #6
  </read_first>
  <action>Apply the Task 1 transformation to the other 29 route pages. A throwaway codemod script may live in the session scratchpad (not committed); every page must end up importing getPathname and routing all internal hrefs through it, and rendering the hero alt from content. Insert hero.imageAlt into the remaining 29 x 7 content files: EN = each page's current alt verbatim; the 6 translations authored in-session per the rules (programmatic insertion from a translation table you write is fine; preserve each file's existing key order and 2-space JSON formatting, placing imageAlt as the last hero key). Translate whyBook.headingLine1 in the 9 hi files listed in Artifacts (keep Prestigo in Latin). Make RouteContent.hero.imageAlt required. Create tests/route-hero-alt.test.ts: for all 30 slugs x 7 locales, getRouteContent returns a non-empty imageAlt; en imageAlt equals the pre-change alt recorded in the test as a fixture list; ru/ar/hi/zh imageAlt never contains 'private chauffeur transfer'; no hi whyBook.headingLine1 equals its EN value; every hi whyBook.headingLine1 in the 9 files contains Prestigo. Extend tests/route-locale-links.test.tsx to loop all 30 slugs for 'ar' (every single-slash href starts with /ar/). Create freeze/75-10.freeze with one line per route: content/routes/en/<slug>.json::hero.imageAlt (30 lines) plus the 9 lines content/routes/en/<slug>.json::whyBook.headingLine1.</action>
  <verify>
    <automated>npx vitest run tests/route-page-render.test.tsx tests/route-locale-links.test.tsx tests/route-hero-alt.test.ts tests/routes-a.test.ts tests/routes-b.test.ts tests/routes-c.test.ts tests/rtl-backstop.test.ts && npx tsc --noEmit && test $(grep -c "" .planning/phases/75-e2e-verification-launch/freeze/75-10.freeze) -ge 39</automated>
  </verify>
  <acceptance_criteria>
    - All listed tests pass; the prague-vienna EN snapshot is unchanged.
    - `grep -L "getPathname" app/[locale]/routes/prague-*/page.tsx` returns no file (every route page uses the bridge).
    - `node scripts/qa/en_leak_static.mjs --rule R3` (if plan 75-02 has landed) reports zero R3 findings under `app/[locale]/routes/prague-*`; otherwise the route-locale-links test for all 30 slugs is the gate.
    - `freeze/75-10.freeze` has >= 39 pattern lines.
  </acceptance_criteria>
  <done>All 30 route pages keep visitors in their locale, carry translated hero alts, the hi headings are fixed, and every change is tested and queued for the manifest freeze.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route page -> internal navigation | Links decide which locale the next page renders in |
| Content JSON -> rendered alt | In-session translations rendered as attributes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-21 | Tampering (navigation) | route page links | low | mitigate | getPathname only (no string-joined locale paths); per-locale href-prefix tests over all 30 pages |
| T-75-22 | Tampering (content integrity) | 210 content files edited programmatically | medium | mitigate | Codemod output verified by tests over all 30 x 7 files and the unchanged EN golden snapshot; codemod not committed |
| T-75-SC | Tampering | package installs | low | accept | No installs |
</threat_model>

<verification>
- Route test suite (route-page-render, route-locale-links, route-hero-alt, routes-a/b/c, rtl-backstop) green; `npx tsc --noEmit` clean.
</verification>

<success_criteria>
The 30 route pages have no hardcoded English alt, no locale-dropping link, and no hi English-fallback heading; EN output unchanged.
</success_criteria>

<output>
Create `.planning/phases/75-e2e-verification-launch/75-10-SUMMARY.md` when done
</output>
