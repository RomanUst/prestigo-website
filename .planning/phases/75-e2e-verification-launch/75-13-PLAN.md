---
phase: 75-e2e-verification-launch
plan: 13
type: execute
wave: 2
depends_on: [75-11, 75-12]
files_modified:
  - components/BookingSection.tsx
  - components/HourlyBookingSection.tsx
  - components/Routes.tsx
  - components/RoutesBento.tsx
  - components/RoutesMap.tsx
  - components/BlogCard.tsx
  - components/booking/steps/StepStub.tsx
  - app/[locale]/not-found.tsx
  - app/[locale]/book/loading.tsx
  - app/[locale]/blog/[slug]/page.tsx
  - app/[locale]/authors/roman-ustyugov/page.tsx
  - content/pages/en/authors/roman-ustyugov.json
  - content/pages/ru/authors/roman-ustyugov.json
  - content/pages/es/authors/roman-ustyugov.json
  - content/pages/fr/authors/roman-ustyugov.json
  - content/pages/ar/authors/roman-ustyugov.json
  - content/pages/hi/authors/roman-ustyugov.json
  - content/pages/zh/authors/roman-ustyugov.json
  - messages/en.json
  - messages/ru.json
  - messages/es.json
  - messages/fr.json
  - messages/ar.json
  - messages/hi.json
  - messages/zh.json
  - tests/shared-sections-i18n.test.tsx
  - tests/BlogCard.test.tsx
  - .planning/phases/75-e2e-verification-launch/freeze/75-13.freeze
autonomous: true
requirements: [VER-01]
estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "BookingSection (home, airport-transfer, concierge) renders its label, 'Book your chauffeur now' headline, subhead and 4 trust bullets from a BookingSection namespace in every locale (STATE known leak: airport-transfer booking block) (D-07/D-08)."
    - "HourlyBookingSection, the homepage Routes section (label, headline, intro), RoutesBento ('View route'), the RoutesMap aria-label, BlogCard ('Read article') and StepStub render only catalog text; their internal links use the i18n Link / getPathname so they keep the locale."
    - "The [locale] not-found page, the /book loading-state aria-label, the /blog/[slug] bottom CTA block ('Skip the taxi rank...', both buttons) and the /authors/roman-ustyugov page labels and visible bio sections render in the visitor's locale; their links keep the locale; JSON-LD Person/Breadcrumb stay English by design (Phase 74 D-09)."
    - "messages/hi.json Booking.entryBar.flightNumberAriaLabel is Hindi (it was byte-identical to EN at planning time)."
    - "All 7 catalogs contain every new key translated (--check completeness + type parity), EN strings are verbatim, and each component renders under ru without any of its EN strings."
    - "empty edge: the not-found page renders translated text even when reached without a matching route (unknown /<locale>/ path) — if Next.js cannot localize the static 404 metadata title, that single title is recorded as an allowlisted exception with its reason rather than left unexplained."
  artifacts:
    - "messages/*.json namespaces BookingSection, HourlyBookingSection, RoutesSection, BlogCard, StepStub, NotFound, BlogPost.cta, Common.loadingBooking"
    - "content/pages/<locale>/authors/roman-ustyugov.json"
    - "tests/shared-sections-i18n.test.tsx"
    - ".planning/phases/75-e2e-verification-launch/freeze/75-13.freeze"
  key_links:
    - "components (client) useTranslations(<namespace>) -> messages/<locale>.json; server components use useTranslations like components/Services.tsx (request locale) or getTranslations({ locale, namespace }) when the page passes locale"
    - "messages/*.json edits serialized after plan 75-12 and before plan 75-16"
  prohibitions:
    - "MUST NOT run node scripts/i18n-translate.mjs --dry-run on the repo tree (D-08)."
    - "MUST NOT localize JSON-LD Person/BreadcrumbList names (Phase 74 D-09 scope)."
---

<objective>
Externalize and translate the remaining shared components and small pages found by planning-time discovery — BookingSection (the airport-transfer "Book your chauffeur now" block), HourlyBookingSection, homepage Routes/RoutesBento/RoutesMap, BlogCard, StepStub, the [locale] 404 page, the /book loading aria, the blog-post bottom CTA and the author page — plus the one hi catalog aria value still in English (D-07/D-08). Second of three sequential `messages/*.json` plans.

Purpose: these components appear on the homepage and most marketing pages, so their English text leaks onto every locale.
Output: new catalog namespaces in all 7 locales, author content files, locale-aware links, render tests, freeze patterns.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-e2e-verification-launch/75-CONTEXT.md
@.planning/phases/75-e2e-verification-launch/75-RESEARCH.md
@.planning/phases/75-e2e-verification-launch/75-11-SUMMARY.md
</context>

## Translation rules (D-08) — apply to every in-session translation in this plan

1. You are the translator (Anthropic credit is empty; the i18n Translate workflow is disabled). Never run `node scripts/i18n-translate.mjs` in any mode other than `--check`.
2. Keep verbatim: Prestigo, PRESTIGO, Mercedes-Benz, E-Class, S-Class, V-Class, rideprestigo.com, phone numbers, times ("24/7"), and every ICU `{placeholder}`.
3. Register: premium, plain, short; zh = Simplified; ar = MSA; hi = Devanagari with brand tokens in Latin.
4. Namespace convention (Phase 69): PascalCase namespaces, camelCase keys, arrays for ordered lists; structural config stays in code zipped by index.
5. EN strings moved verbatim; EN rendered output unchanged.
6. Freeze file format: one pattern per line (`<sourceKey>` or `<sourceKey>::<dotPathPrefix>`), `#` comments; applied by plan 75-18.

## Artifacts this phase produces

- Catalog namespaces (all 7 locales): `BookingSection`, `HourlyBookingSection`, `RoutesSection` (Routes/RoutesBento/RoutesMap strings), `BlogCard`, `StepStub`, `NotFound`, `BlogPost.cta`, `Common.loadingBooking`.
- Fixed value: `Booking.entryBar.flightNumberAriaLabel` in `messages/hi.json`.
- `content/pages/<locale>/authors/roman-ustyugov.json` (7 files).
- `tests/shared-sections-i18n.test.tsx`; updated `tests/BlogCard.test.tsx`.
- `.planning/phases/75-e2e-verification-launch/freeze/75-13.freeze`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — BookingSection localized in all 7 catalogs, proven by ru render</name>
  <files>components/BookingSection.tsx, messages/en.json, messages/ru.json, messages/es.json, messages/fr.json, messages/ar.json, messages/hi.json, messages/zh.json, tests/shared-sections-i18n.test.tsx</files>
  <read_first>
    - components/BookingSection.tsx (client component: label, two-line headline, subhead, 4-item trust array, BookingWidget mount)
    - components/Services.tsx (namespace + structural-array-zip convention)
    - tests/helpers/renderWithIntl.tsx
  </read_first>
  <behavior>
    - renderWithIntl(BookingSection) (BookingWidget mocked) with en shows the same 4 trust bullets and headline as before.
    - With ru messages none of the EN BookingSection strings are present and the ru headline is.
  </behavior>
  <action>Add useTranslations('BookingSection') and move label, headline line + italic line, subhead and the 4 trust items (JSON array) to the catalog; add the namespace to all 7 catalogs (EN verbatim, 6 translated). Create tests/shared-sections-i18n.test.tsx with the behavior cases (mock @/components/booking/BookingWidget).</action>
  <verify>
    <automated>npx vitest run tests/shared-sections-i18n.test.tsx tests/i18n-catalog-array-types.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests pass; `grep -c "useTranslations('BookingSection')" components/BookingSection.tsx` is 1.
    - Every non-EN catalog has a `BookingSection` namespace whose `trust` array length equals EN's.
  </acceptance_criteria>
  <done>The booking block used on home/airport-transfer/concierge is localized end-to-end in all locales.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HourlyBookingSection, Routes/RoutesBento/RoutesMap, BlogCard, StepStub (+ hi aria fix)</name>
  <files>components/HourlyBookingSection.tsx, components/Routes.tsx, components/RoutesBento.tsx, components/RoutesMap.tsx, components/BlogCard.tsx, components/booking/steps/StepStub.tsx, messages/en.json, messages/ru.json, messages/es.json, messages/fr.json, messages/ar.json, messages/hi.json, messages/zh.json, tests/shared-sections-i18n.test.tsx, tests/BlogCard.test.tsx</files>
  <read_first>
    - components/HourlyBookingSection.tsx, components/Routes.tsx (server component on the homepage), components/RoutesBento.tsx, components/RoutesMap.tsx (aria-label ~line 219), components/BlogCard.tsx (server, used by the blog index), components/booking/steps/StepStub.tsx
    - components/Services.tsx (server component using useTranslations + i18n Link — the pattern for Routes.tsx and BlogCard)
    - tests/BlogCard.test.tsx (currently renders without an intl provider — migrate to renderWithIntl per the 70-01 Rule-1 precedent)
  </read_first>
  <behavior>
    - Each component rendered with ru messages contains no EN string of its own and its internal links start with /ru/ (i18n Link).
    - StepStub renders 'step {step} of 6' via ICU in each locale.
    - messages/hi.json Booking.entryBar.flightNumberAriaLabel differs from EN.
  </behavior>
  <action>Externalize each component: HourlyBookingSection (namespace HourlyBookingSection), Routes + RoutesBento + RoutesMap aria (shared RoutesSection namespace; RoutesMap aria-label via t()), BlogCard (BlogCard namespace; the read-article label; card link through the i18n Link), StepStub (StepStub namespace with an ICU step-of-6 string). Server components follow components/Services.tsx (useTranslations + Link from @/i18n/routing); client components use useTranslations; replace raw internal anchors and next/link with the i18n Link. Add every namespace to the 7 catalogs (EN verbatim, 6 translated) and translate messages/hi.json Booking.entryBar.flightNumberAriaLabel. Migrate tests/BlogCard.test.tsx to renderWithIntl and extend tests/shared-sections-i18n.test.tsx with the behavior cases.</action>
  <verify>
    <automated>npx vitest run tests/shared-sections-i18n.test.tsx tests/BlogCard.test.tsx tests/i18n-catalog-array-types.test.ts tests/blog.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All listed tests pass.
    - `grep -n "from 'next/link'" components/BlogCard.tsx components/RoutesBento.tsx components/Routes.tsx` returns nothing.
    - `node -e "const e=require('./messages/en.json'),h=require('./messages/hi.json');if(e.Booking.entryBar.flightNumberAriaLabel===h.Booking.entryBar.flightNumberAriaLabel)process.exit(1)"` exits 0.
  </acceptance_criteria>
  <done>All shared homepage/booking components render catalog text in every locale with locale-preserving links.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: not-found, /book loading aria, blog post CTA, author page + freeze patterns</name>
  <files>app/[locale]/not-found.tsx, app/[locale]/book/loading.tsx, app/[locale]/blog/[slug]/page.tsx, app/[locale]/authors/roman-ustyugov/page.tsx, content/pages/en/authors/roman-ustyugov.json, content/pages/ru/authors/roman-ustyugov.json, content/pages/es/authors/roman-ustyugov.json, content/pages/fr/authors/roman-ustyugov.json, content/pages/ar/authors/roman-ustyugov.json, content/pages/hi/authors/roman-ustyugov.json, content/pages/zh/authors/roman-ustyugov.json, messages/en.json, messages/ru.json, messages/es.json, messages/fr.json, messages/ar.json, messages/hi.json, messages/zh.json, tests/shared-sections-i18n.test.tsx, .planning/phases/75-e2e-verification-launch/freeze/75-13.freeze</files>
  <read_first>
    - app/[locale]/not-found.tsx (static metadata export, raw anchors + next/link)
    - app/[locale]/book/loading.tsx (aria-label)
    - app/[locale]/blog/[slug]/page.tsx lines ~170-195 (bottom CTA; locale from params already resolved in this file)
    - app/[locale]/authors/roman-ustyugov/page.tsx and lib/authors.ts (force-static page; visible labels 'Author profile', 'About', 'Areas of expertise' and bio text sourced from lib/authors.ts; JSON-LD Person built from lib/authors.ts)
    - lib/localized-href.ts (plan 75-11)
  </read_first>
  <behavior>
    - not-found rendered for ru shows ru heading/body/buttons and its links start with /ru/.
    - The /book loading aria-label is localized.
    - The blog post CTA rendered for ar shows ar text and both buttons link to /ar/book and /ar/services/airport-transfer.
    - The author page rendered for zh shows zh section labels and bio text; the JSON-LD Person block is unchanged from EN.
  </behavior>
  <action>not-found: use the NotFound namespace via next-intl (getTranslations in an async component or useTranslations per the next-intl App Router not-found pattern); replace raw anchors and next/link with the i18n Link; keep robots noindex; if Next.js cannot localize the static 404 title, keep it and record the reason (the rendered scan allowlist gets this single entry in plan 75-18's gate, not here). loading.tsx: localized aria-label from Common.loadingBooking. blog/[slug]: move the bottom CTA strings into BlogPost.cta and resolve both CTA hrefs with localizedHref(locale, ...). Author page: create content/pages/<locale>/authors/roman-ustyugov.json (7 files) for the visible section labels and the visible bio/expertise text (EN verbatim from the page/lib/authors.ts; 6 translated), render them from getPageContent('authors/roman-ustyugov', locale) with the params locale, keep lib/authors.ts and the Person/Breadcrumb JSON-LD English, and route internal links through localizedHref. Extend tests/shared-sections-i18n.test.tsx with the behavior cases. Create freeze/75-13.freeze with: messages/en.json::BookingSection, messages/en.json::HourlyBookingSection, messages/en.json::RoutesSection, messages/en.json::BlogCard, messages/en.json::StepStub, messages/en.json::NotFound, messages/en.json::BlogPost.cta, messages/en.json::Common.loadingBooking, messages/en.json::Booking.entryBar.flightNumberAriaLabel, content/pages/en/authors/roman-ustyugov.json.</action>
  <verify>
    <automated>npx vitest run tests/shared-sections-i18n.test.tsx tests/blog.test.ts tests/blog-jsonld.test.ts tests/i18n-catalog-array-types.test.ts && npx tsc --noEmit && test $(grep -c "::\|\.json$" .planning/phases/75-e2e-verification-launch/freeze/75-13.freeze) -ge 10</automated>
  </verify>
  <acceptance_criteria>
    - All listed tests pass; `npx tsc --noEmit` clean.
    - `grep -n "from 'next/link'" app/[locale]/not-found.tsx` returns nothing.
    - `blog-jsonld.test.ts` passes unchanged (JSON-LD untouched).
    - `freeze/75-13.freeze` lists the 10 patterns.
  </acceptance_criteria>
  <done>The 404, loading state, blog CTA and author page are localized with locale-preserving links, and every new unit is queued for the manifest freeze.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog/content text -> rendered components | In-session translations rendered as text/attributes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-21 | Tampering (navigation) | component/page links | low | mitigate | i18n Link / localizedHref only; ru/ar href-prefix assertions in tests |
| T-75-26 | Information Disclosure | 404 page | low | accept | noindex robots preserved; only static copy changes |
| T-75-SC | Tampering | package installs | low | accept | No installs |
</threat_model>

<verification>
- `npx vitest run tests/shared-sections-i18n.test.tsx tests/BlogCard.test.tsx tests/blog.test.ts tests/blog-jsonld.test.ts tests/i18n-catalog-array-types.test.ts` green; `npx tsc --noEmit` clean.
</verification>

<success_criteria>
No shared component, 404, loading state, blog CTA or author page shows English on a localized page; links keep the locale; EN unchanged.
</success_criteria>

<output>
Create `.planning/phases/75-e2e-verification-launch/75-13-SUMMARY.md` when done
</output>
