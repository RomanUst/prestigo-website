---
phase: 75-e2e-verification-launch
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/localized-href.ts
  - app/[locale]/services/page.tsx
  - app/[locale]/services/airport-transfer/page.tsx
  - app/[locale]/services/city-rides/page.tsx
  - app/[locale]/services/concierge/page.tsx
  - app/[locale]/services/corporate-accounts/page.tsx
  - app/[locale]/services/group-transfers/page.tsx
  - app/[locale]/services/intercity-routes/page.tsx
  - app/[locale]/services/vip-events/page.tsx
  - app/[locale]/about/page.tsx
  - app/[locale]/terms/page.tsx
  - app/[locale]/privacy/page.tsx
  - app/[locale]/faq/page.tsx
  - app/[locale]/blog/page.tsx
  - app/[locale]/data-deletion/page.tsx
  - content/pages/en/services/*.json
  - content/pages/ru/services/*.json
  - content/pages/es/services/*.json
  - content/pages/fr/services/*.json
  - content/pages/ar/services/*.json
  - content/pages/hi/services/*.json
  - content/pages/zh/services/*.json
  - content/pages/en/about.json
  - content/pages/ru/about.json
  - content/pages/es/about.json
  - content/pages/fr/about.json
  - content/pages/ar/about.json
  - content/pages/hi/about.json
  - content/pages/zh/about.json
  - content/pages/hi/corporate.json
  - content/pages/zh/corporate.json
  - content/pages/hi/data-deletion.json
  - tests/localized-href.test.ts
  - tests/static-pages-locale.test.tsx
  - .planning/phases/75-e2e-verification-launch/freeze/75-11.freeze
autonomous: true
requirements: [VER-01]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "lib/localized-href.ts exports localizedHref(locale, href): internal root-relative hrefs (single leading slash) resolve through getPathname({ locale, href }); external URLs, mailto:, tel:, hash-only and static asset paths are returned unchanged."
    - "empty edge: localizedHref(locale, '') and localizedHref(locale, undefined) return the input unchanged (no crash, no '/ru' fabricated from nothing)."
    - "Every internal link on the services hub, the 7 service pages, about, terms, privacy, faq, the blog index and data-deletion — literal hrefs, template literals (e.g. /routes/<slug>, /authors/<slug>) and content-provided hrefs (c.href, s.href, s.bookHref) — keeps the visitor's locale (planning-time discovery: /ru/services/airport-transfer and /ar/about link to EN /book, /services, /corporate)."
    - "The hero image alt of the 7 service pages and about comes from content hero.imageAlt (EN byte-identical to today's alt, 6 locales translated) (D-07 alt surface)."
    - "hi and zh corporate.json usagePatterns.headingItalic and hi data-deletion.json section3.facebookLinkLabel are translated (they were byte-identical to EN at planning time) (D-09 — WINDOWS #6 extended)."
    - "EN output of every touched page is unchanged (EN hrefs identical because getPathname returns the unprefixed path for en; EN alts moved verbatim)."
  artifacts:
    - "lib/localized-href.ts"
    - "hero.imageAlt in content/pages/<locale>/services/*.json and about.json"
    - "tests/localized-href.test.ts, tests/static-pages-locale.test.tsx"
    - ".planning/phases/75-e2e-verification-launch/freeze/75-11.freeze"
  key_links:
    - "localizedHref() is the one wrapper for content-provided hrefs; reused by plans 75-13 and 75-14"
    - "content/pages/<locale>/services/<page>.json hero.imageAlt -> service page hero alt"
  prohibitions:
    - "MUST NOT run node scripts/i18n-translate.mjs --dry-run (or any non --check mode) on the repo tree (D-08)."
    - "MUST NOT rewrite external links (WhatsApp, provider URLs) through the locale bridge."
---

<objective>
Make the services hub, the 7 service pages and the static pages (about, terms, privacy, faq, blog index, data-deletion) keep visitors in their locale, move their hardcoded English hero alts into the content model, and translate the three remaining English-identical content units found at planning time (D-07/D-08/D-09).

Purpose: these pages are already translated in body text (Phase 71/74) but every CTA and cross-link drops the locale, so a Russian visitor clicking "Book" lands on the English wizard.
Output: `lib/localized-href.ts`, locale-aware links on 14 pages, translated alts, three content fixes, tests, freeze patterns.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-e2e-verification-launch/75-CONTEXT.md
@.planning/phases/75-e2e-verification-launch/75-RESEARCH.md
</context>

## Translation rules (D-08) — apply to every in-session translation in this plan

1. You are the translator (Anthropic credit is empty; the i18n Translate workflow is disabled). Never run `node scripts/i18n-translate.mjs` in any mode other than `--check`.
2. Keep verbatim: Prestigo, PRESTIGO, chelautotrans s.r.o., Mercedes-Benz, E-Class, S-Class, V-Class, rideprestigo.com, Facebook (brand), phone numbers, € amounts and every `{placeholder}`.
3. Register: premium, plain; zh = Simplified; ar = MSA; hi = Devanagari with brand tokens kept in Latin inside the sentence.
4. Structure: identical key sets/types; EN strings byte-identical.
5. Freeze file format: one pattern per line (`<sourceKey>` or `<sourceKey>::<dotPathPrefix>`), `#` comments; applied by plan 75-18.

## Artifacts this phase produces

- `lib/localized-href.ts` — `localizedHref(locale: AppLocale | string, href: string | undefined): string | undefined`.
- `hero.imageAlt` in `content/pages/<locale>/services/{airport-transfer,city-rides,concierge,corporate-accounts,group-transfers,intercity-routes,vip-events}.json` and `content/pages/<locale>/about.json`.
- Translated `usagePatterns.headingItalic` (hi, zh corporate.json) and `section3.facebookLinkLabel` (hi data-deletion.json).
- `tests/localized-href.test.ts`, `tests/static-pages-locale.test.tsx`.
- `.planning/phases/75-e2e-verification-launch/freeze/75-11.freeze`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — localizedHref helper + /services/airport-transfer end-to-end (links + translated alt)</name>
  <files>lib/localized-href.ts, tests/localized-href.test.ts, app/[locale]/services/airport-transfer/page.tsx, content/pages/en/services/airport-transfer.json, content/pages/ru/services/airport-transfer.json, content/pages/es/services/airport-transfer.json, content/pages/fr/services/airport-transfer.json, content/pages/ar/services/airport-transfer.json, content/pages/hi/services/airport-transfer.json, content/pages/zh/services/airport-transfer.json, tests/static-pages-locale.test.tsx</files>
  <read_first>
    - app/[locale]/services/airport-transfer/page.tsx (hero Image alt ~line 142; raw anchors; how locale and content are resolved)
    - i18n/routing.ts (getPathname)
    - tests/route-page-render.test.tsx (async Server Component render + mocks pattern)
    - lib/page-content.ts
  </read_first>
  <behavior>
    - localizedHref('ru', '/book') = '/ru/book'; ('en', '/book') = '/book'; ('ar', '/routes/prague-vienna') = '/ar/routes/prague-vienna'; ('ru', 'https://wa.me/1') unchanged; ('ru', 'mailto:a@b.c') unchanged; ('ru', 'tel:+420') unchanged; ('ru', '#faq') unchanged; ('ru', '/hero.webp') unchanged; ('ru', '//evil.com') unchanged (never treated as internal); ('ru', '') and ('ru', undefined) unchanged.
    - The airport-transfer page rendered for 'ru' has a hero alt equal to content hero.imageAlt (ru) and every single-slash href starts with /ru/; for 'en' the output hrefs/alt equal today's.
  </behavior>
  <action>Create lib/localized-href.ts: when href is a string starting with exactly one slash and its last path segment has no file extension, return getPathname({ locale, href }) from @/i18n/routing; otherwise return href unchanged; document the rule. Write tests/localized-href.test.ts for every behavior case. In the airport-transfer page, wrap every internal href (literal and content-provided) with localizedHref(locale, ...), and render the hero alt from content.hero.imageAlt; add hero.imageAlt to the 7 airport-transfer content files (EN = current alt verbatim; others translated). Note: the page's "Book your chauffeur now" block is the BookingSection component, localized in plan 75-13 — do not edit components here. Create tests/static-pages-locale.test.tsx rendering the airport-transfer page for en and ru (mock Nav, Footer, BookingSection, next/image) and asserting the behavior list.</action>
  <verify>
    <automated>npx vitest run tests/localized-href.test.ts tests/static-pages-locale.test.tsx tests/services-a.test.ts tests/services-b.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/localized-href.test.ts` and `tests/static-pages-locale.test.tsx` pass; existing `tests/services-a.test.ts` and `tests/services-b.test.ts` still pass.
    - The airport-transfer page has no hardcoded hero alt text and imports localizedHref.
    - All 7 airport-transfer content files contain `hero.imageAlt`.
  </acceptance_criteria>
  <done>A reusable, tested locale-href helper exists and one service page is fully locale-preserving with a translated alt.</done>
</task>

<task type="auto">
  <name>Task 2: Remaining services + about/terms/privacy/faq/blog index/data-deletion links, alts, content fixes, tests, freeze</name>
  <files>app/[locale]/services/page.tsx, app/[locale]/services/city-rides/page.tsx, app/[locale]/services/concierge/page.tsx, app/[locale]/services/corporate-accounts/page.tsx, app/[locale]/services/group-transfers/page.tsx, app/[locale]/services/intercity-routes/page.tsx, app/[locale]/services/vip-events/page.tsx, app/[locale]/about/page.tsx, app/[locale]/terms/page.tsx, app/[locale]/privacy/page.tsx, app/[locale]/faq/page.tsx, app/[locale]/blog/page.tsx, app/[locale]/data-deletion/page.tsx, content/pages/*/services/*.json, content/pages/*/about.json, content/pages/hi/corporate.json, content/pages/zh/corporate.json, content/pages/hi/data-deletion.json, tests/static-pages-locale.test.tsx, .planning/phases/75-e2e-verification-launch/freeze/75-11.freeze</files>
  <read_first>
    - lib/localized-href.ts and app/[locale]/services/airport-transfer/page.tsx (Task 1 pattern)
    - app/[locale]/services/page.tsx (s.href / s.bookHref content links), app/[locale]/services/concierge/page.tsx (c.href), app/[locale]/services/intercity-routes/page.tsx (template-literal route links), app/[locale]/about/page.tsx (authors template link, hero alt ~line 127)
    - content/pages/en/corporate.json usagePatterns and content/pages/en/data-deletion.json section3 (EN sources of the three identical-to-EN units)
  </read_first>
  <action>Apply the Task 1 link treatment (localizedHref for every internal href incl. template literals and content-provided hrefs; external/mailto/tel untouched) to the other 13 pages. Move the hero alt of city-rides, concierge, corporate-accounts, group-transfers, intercity-routes, vip-events and about into hero.imageAlt of their content files (EN verbatim, 6 locales translated). Translate usagePatterns.headingItalic in content/pages/hi/corporate.json and content/pages/zh/corporate.json and section3.facebookLinkLabel in content/pages/hi/data-deletion.json (keep PRESTIGO / Facebook tokens). Extend tests/static-pages-locale.test.tsx: for 'ar' render each touched page (mock Nav/Footer/BookingSection/HourlyBookingSection/ContactForm/CorporateForm/next/image as needed) and assert every single-slash href starts with /ar/; assert en hrefs unchanged for services hub and about; assert the 8 imageAlt units exist in all 7 locales with EN equal to the pre-change alt; assert the three content units differ from EN in their locale files. Create freeze/75-11.freeze listing content/pages/en/services/<page>.json::hero.imageAlt for the 7 service pages, content/pages/en/about.json::hero.imageAlt, content/pages/en/corporate.json::usagePatterns.headingItalic and content/pages/en/data-deletion.json::section3.facebookLinkLabel.</action>
  <verify>
    <automated>npx vitest run tests/static-pages-locale.test.tsx tests/services-a.test.ts tests/services-b.test.ts tests/pages-a.test.ts tests/legal.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All listed tests pass; `npx tsc --noEmit` clean.
    - `grep -L "localizedHref\|getPathname" app/[locale]/services/*/page.tsx app/[locale]/services/page.tsx app/[locale]/about/page.tsx` returns no file.
    - `freeze/75-11.freeze` contains 10 pattern lines.
  </acceptance_criteria>
  <done>All service and static pages keep the visitor's locale on every link, alts are translated, and the last English-identical content units are fixed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Content-provided hrefs -> anchors | Content JSON decides link targets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-23 | Spoofing (open redirect via protocol-relative URL) | localizedHref | medium | mitigate | Only a single leading slash is treated as internal; '//host' is returned unchanged and never prefixed; unit test covers it |
| T-75-21 | Tampering (navigation) | page links | low | mitigate | getPathname bridge only; ar href-prefix tests over all touched pages |
| T-75-SC | Tampering | package installs | low | accept | No installs |
</threat_model>

<verification>
- `npx vitest run tests/localized-href.test.ts tests/static-pages-locale.test.tsx tests/services-a.test.ts tests/services-b.test.ts tests/pages-a.test.ts tests/legal.test.ts` green; `npx tsc --noEmit` clean.
</verification>

<success_criteria>
Service and static pages keep every visitor in their locale, carry translated alts, and no English-identical content unit remains in hi/zh.
</success_criteria>

<output>
Create `.planning/phases/75-e2e-verification-launch/75-11-SUMMARY.md` when done
</output>
