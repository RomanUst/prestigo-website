---
phase: 74-seo-hreflang-localized-metadata-sitemap-structured-data-swit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/seo.ts
  - i18n/locales.ts
  - lib/jsonld.ts
  - app/[locale]/routes/prague-vienna/page.tsx
  - app/sitemap.ts
  - tests/seo.test.ts
  - tests/sitemap.test.ts
  - tests/jsonld.test.ts
  - tests/__snapshots__/route-page-render.test.tsx.snap
autonomous: true
requirements: [SEO-01, SEO-03, SEO-04]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "getAlternates('/routes/prague-vienna', { indexable: true, content: { kind: 'route', key: 'prague-vienna' } }) returns languages keyed by BCP-47 hreflang tags for every locale that has a genuine translation file, plus x-default → EN root-form URL (SEO-01)"
    - "getAlternates called with indexable:false returns languages:{} (empty cluster) while keeping a self-canonical — no hreflang cluster is ever emitted for a noindex page (SEO-01, D-06)"
    - "A locale whose content file is missing is excluded from the cluster and that locale's page stays self-canonical → EN; a locale whose content merely equals EN by value is NOT special-cased — presence of the locale file is the gate, not value comparison (SEO-01, D-07)"
    - "For the zh locale the hreflang/inLanguage tag is the BCP-47 script subtag zh-Hans (not bare zh); en/ru/es/fr/ar/hi map to themselves (SEO-04)"
    - "app/sitemap.ts emits, per URL, an alternates.languages cluster produced by the same getAlternates() source of truth — the sitemap cluster for any URL is identical to that URL's page-level cluster (SEO-03)"
    - "buildRouteJsonLd emits inLanguage set to the active locale's BCP-47 tag on the Service node; for locale en the name/description strings are byte-for-byte the pre-phase English template, for non-en locales they are sourced from the content model (SEO-04, D-09)"
    - "The prague-vienna page renders end-to-end with the new helper: its <head> alternates come from getAlternates() and its JSON-LD carries inLanguage, proving the seo.ts → page → sitemap → jsonld spine on one path"
    - "{ statement: 'getAlternates languages object key order is deterministic — routing.locales order (en, ru, es, fr, ar, hi, zh) with x-default last — for stable snapshot/diff output', verification: backstop }"
    - "{ statement: 'A page passing a content ref where only the EN file exists yields a two-key cluster (en + x-default) only', verification: backstop }"
    - "{ statement: 'JSON-LD @graph node order is unchanged (businessNode first, Service second); only the inLanguage key is added', verification: backstop }"
    - "{ statement: 'sitemap() and getAlternates() remain synchronous — getPathname returns string in next-intl@4.14.2 — the existing synchronous tests/sitemap.test.ts sitemap() call is preserved', verification: backstop }"
    - "{ statement: 'BreadcrumbList `name` fields across the JSON-LD @graph (~31 files) remain hardcoded English by design — D-09 scopes JSON-LD localization to the Service node name/description only, not the breadcrumb trail; this is a recorded Phase 74 scope decision, not an oversight, and is not rediscovered as a gap in verification or Phase 75', verification: backstop }"
  artifacts:
    - lib/seo.ts (extended getAlternates + internal hasLocaleContent)
    - i18n/locales.ts (LOCALE_ENDONYMS + BCP47_TAG maps)
    - lib/jsonld.ts (buildRouteJsonLd with opts + inLanguage)
    - tests/seo.test.ts (new)
  key_links:
    - "getAlternates() is the single choke point read by both page generateMetadata and app/sitemap.ts — they cannot diverge"
    - "hasLocaleContent() fs.existsSync probe against content/{routes,pages,blog}/<locale>/ — the D-07 translation-availability gate"
    - "BCP47_TAG map in i18n/locales.ts feeds both hreflang keys (getAlternates) and JSON-LD inLanguage (buildRouteJsonLd)"
  prohibitions:
    - "Never emit an hreflang alternates cluster on a noindex page"
    - "Never claim a locale alternate for a page that has no genuine translation file for that locale"
    - "Never build a locale-prefixed URL by string-joining the locale code and the path — always resolve it through the i18n/routing navigation bridge"
    - "EN root output is unchanged apart from the intended SEO additions (the new hreflang cluster on translated EN pages and the JSON-LD inLanguage key); no visible copy, price, priceValidUntil, Place ID, rating, or metadata title/description value drifts"
    - "Never set routing.localeDetection to true"
    - "Never localize BreadcrumbList `name` fields in this phase — D-09 scopes JSON-LD localization to Service `name`/`description` only; BreadcrumbList text stays hardcoded English by design across this plan and Wave-2 (revisit only in a future phase if requirements change)"
---

<objective>
Prove the entire SEO plumbing spine end-to-end on ONE page (`/routes/prague-vienna`): a centralized, index-aware + translation-aware `getAlternates(path, opts)` in `lib/seo.ts`; the shared BCP-47 + endonym maps in `i18n/locales.ts`; localized JSON-LD `inLanguage` in `lib/jsonld.ts`; and the full `app/sitemap.ts` cluster — all reading from the one `getAlternates()` source of truth.

Purpose: `getAlternates()` is currently dead code (zero callers, RESEARCH Pitfall 4) and the ~56 call sites hand-roll `{ en, x-default }`. This tracer locks the helper signature that shapes all 55 remaining call sites and the sitemap, so the Wave-2 sweeps become mechanical against a proven contract. Because `sitemap.entry()` is a single choke point, completing the sitemap here makes SEO-03 done in this plan.

Output: extended `lib/seo.ts`, `i18n/locales.ts`, `lib/jsonld.ts`; `prague-vienna` wired to the helper end-to-end; full `app/sitemap.ts` cluster; new `tests/seo.test.ts`; regenerated JSON-LD snapshot.
</objective>

<assumption_delta_decision>
Noun now primary: **hreflang alternates cluster (plural, up to 7 BCP-47-tagged locales + x-default)** — promoted from the pre-phase singular `{ en, x-default }` pair.
Decision: **promote**. Rationale: the phase's entire purpose (SEO-01/03) is to replace the single-locale pair with a full multi-locale cluster site-wide; the plural cluster becomes the default output shape of `getAlternates()` and the sitemap. No alongside/no-change variant is warranted — the single-pair form is retired, not kept beside the cluster.
</assumption_delta_decision>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-seo-hreflang-localized-metadata-sitemap-structured-data-swit/74-RESEARCH.md
@.planning/phases/74-seo-hreflang-localized-metadata-sitemap-structured-data-swit/74-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols this plan creates (excluded from downstream drift verification):
- `getAlternates(canonicalPath, opts?)` — extended signature in `lib/seo.ts`; `opts = { indexable?: boolean; content?: { kind: 'route'|'page'|'blog'; key: string } }`
- `hasLocaleContent(ref, locale)` — new internal helper in `lib/seo.ts` (fs.existsSync probe)
- `LOCALE_ENDONYMS` and `BCP47_TAG` (`zh` → `zh-Hans`) — new `as const` exports in `i18n/locales.ts`
- `buildRouteJsonLd(route, slug, opts?)` — extended with `{ locale, name, description }` opts + `inLanguage` on the Service node in `lib/jsonld.ts`
- `tests/seo.test.ts` — new unit test file
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — tests/seo.test.ts pins the new getAlternates contract</name>
  <files>tests/seo.test.ts</files>
  <read_first>
    - lib/seo.ts (current single-arg stub, lines 32-43 — the shape being replaced)
    - lib/blog.ts (resolveLocalizedMdx/blogCanonical, ~lines 141-176 — the D-07 fs.existsSync + locale-validation pattern to mirror)
    - i18n/locales.ts (locales list order; the file that will gain BCP47_TAG)
    - tests/sitemap.test.ts (existing vitest style + synchronous sitemap() call convention)
    - .planning/phases/74-.../74-RESEARCH.md § "Validation Architecture" (SEO-01 test cases) and § "Architecture Patterns → Pattern 1"
  </read_first>
  <behavior>
    - Indexable + fully-translated route: cluster contains one key per locale (en, ru, es, fr, ar, hi, zh-Hans) whose URL is the locale-aware path, plus x-default → EN root-form URL.
    - indexable:false → languages is an empty object; canonical still present.
    - Partial translation (a content ref where a given locale file is absent): that locale key is omitted; en + x-default always present.
    - zh maps to the hreflang/inLanguage tag zh-Hans; en/ru/es/fr/ar/hi map to themselves.
    - Key ordering is deterministic (routing.locales order, x-default last).
    - No content ref supplied (chrome-only page) → all 7 locales included.
  </behavior>
  <action>Create tests/seo.test.ts as a failing Vitest suite that imports getAlternates from '@/lib/seo' and asserts each behavior above. Point the partial-translation case at a content key that genuinely has some but not all locale files under content/routes or content/pages (choose one by reading the tree so the test is grounded, not hypothetical). Assert x-default and canonical for the home ('' / '/') case. Assert the zh entry uses the zh-Hans tag. This suite MUST fail on the current single-argument stub — that failure is the RED gate before Task 2.</action>
  <verify>
    <automated>npx vitest run tests/seo.test.ts 2>&1 | grep -Eq "fail|FAIL|Error" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - tests/seo.test.ts exists and imports `getAlternates` from `@/lib/seo`
    - Running `npx vitest run tests/seo.test.ts` fails (RED) against the current stub
    - The suite includes at minimum: indexable+all-translated, indexable:false→empty, partial-translation-excludes-locale, zh→zh-Hans, deterministic-order cases
  </acceptance_criteria>
  <done>tests/seo.test.ts exists and fails against the current stub, pinning the target contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — extend getAlternates + locales maps + full sitemap cluster</name>
  <files>lib/seo.ts, i18n/locales.ts, app/sitemap.ts, tests/sitemap.test.ts</files>
  <read_first>
    - lib/seo.ts (stub to replace)
    - i18n/locales.ts (add maps here — the file must keep zero next-intl/next/React imports per its header comment)
    - i18n/routing.ts (getPathname export at line 42; confirmed synchronous `=> string` in 4.14.2 — no await needed)
    - lib/blog.ts (locale-validation-before-path.join pattern to mirror for the V5 guard)
    - lib/route-content.ts + lib/page-content.ts (existsSync fallback pattern, content roots)
    - app/sitemap.ts (current entry() helper, lines 21-28, and all entry() call sites)
    - tests/sitemap.test.ts (must keep passing; it calls sitemap() synchronously)
  </read_first>
  <action>In i18n/locales.ts add two `as const` exports: LOCALE_ENDONYMS mapping en→English, ru→Русский, es→Español, fr→Français, ar→العربية, hi→हिन्दी, zh→中文; and BCP47_TAG mapping every locale to itself except zh→zh-Hans. Keep the file free of next-intl/next/React imports (structural data only, per its header). In lib/seo.ts replace the stub getAlternates with a two-argument signature accepting an optional opts object carrying an indexable boolean and a content reference { kind: 'route'|'page'|'blog'; key }. Add an internal hasLocaleContent(ref, locale) that first rejects any locale not in routing.locales (mirror lib/blog.ts's guard) BEFORE touching the filesystem, then fs.existsSync-probes content/{routes|pages|blog}/<locale>/<key>.<json|mdx>. Behavior: when indexable is false, return languages as an empty object with the self-canonical preserved (D-06); otherwise compute the available locales as en plus every locale whose content file exists (or all locales when no content ref is given), and build languages keyed by BCP47_TAG[locale] with each URL resolved through the i18n/routing navigation bridge's getPathname({ locale, href }), always including x-default → the EN root-form URL. Preserve the existing call-with-target-path cross-canonical convention (canonical may point at a different path than the caller's own URL). In app/sitemap.ts widen entry() to accept the same opts and replace the hand-rolled { en, x-default } literal with the languages produced by getAlternates(urlPath, opts); pass a route content ref for route entries, a blog content ref for MDX blog entries (so D-07 EN-fallback blog posts collapse to en-only), no content ref for chrome-only hub pages, and indexable:false for any noindex URL. Keep sitemap() synchronous. Do NOT string-join the locale code and path anywhere.</action>
  <verify>
    <automated>npx vitest run tests/seo.test.ts tests/sitemap.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/seo.test.ts` passes (GREEN)
    - `npx vitest run tests/sitemap.test.ts` passes (existing assertions still green; sitemap() still called synchronously)
    - `grep -n "getAlternates" app/sitemap.ts` shows entry() delegating to getAlternates
    - `grep -Eic 'zh-Hans' i18n/locales.ts` ≥ 1 (BCP47_TAG present)
    - `grep -vn '^\s*\*' lib/seo.ts | grep -q "routing.locales"` — locale validated against the allowlist before any fs access
    - `grep -q "getPathname" lib/seo.ts` — locale URLs resolved via the routing bridge, not concatenation
  </acceptance_criteria>
  <reversibility rating="costly">The getAlternates signature shapes all 55 remaining call sites + the sitemap; a later signature change is git-revertible but forces re-touching every caller. Tracer-first is the de-risking mechanism — the contract is proven on one path before the sweep.</reversibility>
  <done>getAlternates is index- and translation-aware, feeds the full sitemap cluster, and both unit suites are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire prague-vienna end-to-end + JSON-LD inLanguage + snapshot regen</name>
  <files>lib/jsonld.ts, app/[locale]/routes/prague-vienna/page.tsx, tests/jsonld.test.ts, tests/__snapshots__/route-page-render.test.tsx.snap</files>
  <read_first>
    - lib/jsonld.ts (buildRouteJsonLd lines 62-115 — the EN template string that must stay byte-identical, and futureIsoDate(365) drift-normalization)
    - app/[locale]/routes/prague-vienna/page.tsx (generateMetadata + the buildRouteJsonLd caller spread + content.metadata.title/description already in scope)
    - tests/route-page-render.test.tsx (renders the page incl. JSON-LD; getLocale mocked to 'en'; snapshot at tests/__snapshots__/route-page-render.test.tsx.snap)
    - tests/jsonld.test.ts (existing buildRouteJsonLd assertions to extend)
    - lib/content-interpolate.ts (interpolate vs interpolateBidi — JSON-LD text uses plain interpolate, never bidi)
  </read_first>
  <action>Extend buildRouteJsonLd(route, slug) with an optional third opts param { locale, name, description }. Add inLanguage on the Service node set to BCP47_TAG[opts.locale] (defaulting to 'en'). Keep the EN name/description exactly as the current hardcoded template (Pitfall 7): only when opts.locale is not 'en' use opts.name/opts.description. Leave priceValidUntil = futureIsoDate(365) and every price/@id/url unchanged. In prague-vienna/page.tsx: replace the hand-rolled alternates block in generateMetadata with getAlternates('/routes/prague-vienna', { indexable: true, content: { kind: 'route', key: 'prague-vienna' } }); keep the single shared /og-image-style image reference as-is (D-08 — localize only title/description/alt); pass opts to buildRouteJsonLd with locale plus interpolate()'d content.metadata.title/description (plain interpolate, not bidi). Extend tests/jsonld.test.ts to assert inLanguage is present and equals the pre-phase English template values when locale is en, and equals passed name/description for a non-en locale. Regenerate tests/__snapshots__/route-page-render.test.tsx.snap (getLocale mock resolves 'en'), then prove the ONLY diff versus the prior snapshot is the added inLanguage key (a scratch diff check in the scratchpad, matching the Phase 73 discipline) — no other JSON-LD or body bytes changed. Scope note (D-09, RESEARCH Q1): BreadcrumbList `name` fields in the same @graph are OUT of scope for this phase and are not touched here or in any Wave-2 sweep — D-09 localizes the Service node's name/description only; the breadcrumb trail stays hardcoded English by design.</action>
  <verify>
    <automated>npx vitest run tests/jsonld.test.ts tests/route-page-render.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "inLanguage" lib/jsonld.ts` ≥ 1; buildRouteJsonLd accepts an opts arg
    - tests/jsonld.test.ts asserts EN name/description equal the pre-phase template (byte-identical) AND inLanguage present
    - `npx vitest run tests/jsonld.test.ts tests/route-page-render.test.tsx` passes with the regenerated snapshot
    - The snapshot diff versus the prior committed snapshot consists solely of added `inLanguage` keys (verified by the scratch diff) — no price, priceValidUntil, name, description, or body drift
    - prague-vienna generateMetadata calls getAlternates with the route content ref
  </acceptance_criteria>
  <reversibility rating="costly">Adding inLanguage to EN JSON-LD is a deliberate scoped byte-change to the published structured-data contract and its golden snapshot; reversible via git + snapshot regen, but any accidental drift beyond inLanguage is an EN ranking risk — hence the diff-is-only-inLanguage assertion.</reversibility>
  <done>prague-vienna emits its full hreflang cluster and inLanguage JSON-LD end-to-end; EN structured data is unchanged apart from inLanguage; all three suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| locale value → filesystem path | `hasLocaleContent()` composes `content/<kind>/<locale>/<key>` — the locale segment reaches `path.join`/`fs.existsSync` |
| build-time content tree → emitted hreflang/canonical | getAlternates output is served to crawlers as `<link rel="alternate">` and in the sitemap |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-01 | Tampering | lib/seo.ts hasLocaleContent | medium | mitigate | Validate `locale` against `routing.locales` (typed allowlist) BEFORE any `path.join`/`fs.existsSync`, mirroring lib/blog.ts/route-content.ts/page-content.ts — no path is built from an unrecognized locale |
| T-74-02 | Tampering | Emitted hreflang cluster correctness | medium | mitigate | D-06/D-07 gates in getAlternates ensure no cluster on noindex and no claimed-but-untranslated alternate — prevents contradictory crawl signals (SEO integrity) |
| T-74-SC | Tampering | package installs | low | accept | No new packages introduced this phase (RESEARCH Package Legitimacy Audit) — nothing to install |
</threat_model>

<verification>
- `npx vitest run tests/seo.test.ts tests/sitemap.test.ts tests/jsonld.test.ts tests/route-page-render.test.tsx` all green.
- Snapshot diff proves only `inLanguage` was added to EN JSON-LD.
- `grep -rn "getAlternates(" app/sitemap.ts` shows the sitemap consuming the single source of truth.
</verification>

<success_criteria>
The SEO spine (helper → page → sitemap → jsonld) works end-to-end on prague-vienna with an index-aware, translation-aware, BCP-47-correct hreflang cluster and localized JSON-LD, EN output unchanged apart from the intended additions.
</success_criteria>

<output>
Create `.planning/phases/74-seo-hreflang-localized-metadata-sitemap-structured-data-swit/74-01-SUMMARY.md` when done.
</output>
