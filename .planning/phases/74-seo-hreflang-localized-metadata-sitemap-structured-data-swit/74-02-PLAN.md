---
phase: 74-seo-hreflang-localized-metadata-sitemap-structured-data-swit
plan: 02
type: execute
wave: 2
depends_on: ["74-01"]
files_modified:
  - app/[locale]/routes/prague-berlin/page.tsx
  - app/[locale]/routes/prague-bratislava/page.tsx
  - app/[locale]/routes/prague-brno/page.tsx
  - app/[locale]/routes/prague-budapest/page.tsx
  - app/[locale]/routes/prague-ceske-budejovice/page.tsx
  - app/[locale]/routes/prague-cesky-krumlov/page.tsx
  - app/[locale]/routes/prague-dresden/page.tsx
  - app/[locale]/routes/prague-frantiskovy-lazne/page.tsx
  - app/[locale]/routes/prague-graz/page.tsx
  - app/[locale]/routes/prague-hradec-kralove/page.tsx
  - app/[locale]/routes/prague-karlovy-vary/page.tsx
  - app/[locale]/routes/prague-krakow/page.tsx
  - app/[locale]/routes/prague-kutna-hora/page.tsx
  - app/[locale]/routes/prague-leipzig/page.tsx
  - app/[locale]/routes/prague-liberec/page.tsx
  - app/[locale]/routes/prague-linz/page.tsx
  - app/[locale]/routes/prague-marianske-lazne/page.tsx
  - app/[locale]/routes/prague-munich/page.tsx
  - app/[locale]/routes/prague-nuremberg/page.tsx
  - app/[locale]/routes/prague-olomouc/page.tsx
  - app/[locale]/routes/prague-ostrava/page.tsx
  - app/[locale]/routes/prague-pardubice/page.tsx
  - app/[locale]/routes/prague-passau/page.tsx
  - app/[locale]/routes/prague-plzen/page.tsx
  - app/[locale]/routes/prague-regensburg/page.tsx
  - app/[locale]/routes/prague-salzburg/page.tsx
  - app/[locale]/routes/prague-warsaw/page.tsx
  - app/[locale]/routes/prague-wroclaw/page.tsx
  - app/[locale]/routes/prague-zlin/page.tsx
  - app/[locale]/routes/page.tsx
autonomous: true
requirements: [SEO-01, SEO-04]
estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every indexable route page (29 prague-* pages + the /routes hub) emits its full hreflang cluster via getAlternates() with the route content ref, replacing the hand-rolled { en, x-default } block — no route page retains a literal languages object (SEO-01)"
    - "Each route page's JSON-LD Service node carries inLanguage set to the active locale's BCP-47 tag, with EN name/description byte-identical to the pre-phase template and non-EN name/description sourced from that route's content model (SEO-04, D-09)"
    - "A route whose content exists for only some locales emits alternates only for the locales whose content file is present (D-07); when all 7 exist the cluster carries en, ru, es, fr, ar, hi, zh-Hans + x-default"
    - "{ statement: 'Every route page keeps its existing canonical path (/routes/<slug>) and single shared OG image — only title/description/alt are locale-sourced (D-08)', verification: backstop }"
    - "{ statement: 'Route pages remain ISR (revalidate=120) and their bare getLocale() resolution is unchanged — this sweep does not touch locale resolution, only the alternates and inLanguage wiring', verification: backstop }"
  artifacts:
    - "29 prague-* route page.tsx files wired to getAlternates + buildRouteJsonLd opts"
    - "app/[locale]/routes/page.tsx (hub) wired to getAlternates"
  key_links:
    - "Each route page imports getAlternates from @/lib/seo and passes { indexable:true, content:{kind:'route', key:<slug>} }"
    - "Each route page passes { locale, name, description } to buildRouteJsonLd from its already-in-scope getRouteContent metadata"
  prohibitions:
    - "Never emit a hand-rolled languages literal on a route page — the cluster comes only from getAlternates()"
    - "Never let a route page's EN JSON-LD name/description drift from the pre-phase template"
    - "Never build locale URLs by joining the locale code to the path"
---

<objective>
Sweep the 30 indexable route pages (29 remaining `prague-*` pages + the `/routes` hub) to consume the `getAlternates()` helper and emit localized JSON-LD `inLanguage`, matching the proven `prague-vienna` tracer shape.

Purpose: these are the highest-value indexable SEO pages; they share one uniform, mechanical shape (ISR, `getRouteContent`, `buildRouteJsonLd`), so a single sweep against the Wave-1 contract completes SEO-01/SEO-04 for the route surface. Scale matches Phase 73's 30-32-file mechanical sweeps (73-04/73-09/73-13).

Output: all 30 route pages emitting index/translation-aware hreflang clusters + `inLanguage` JSON-LD, EN structured data unchanged apart from `inLanguage`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-seo-hreflang-localized-metadata-sitemap-structured-data-swit/74-PATTERNS.md
@app/[locale]/routes/prague-vienna/page.tsx
</context>

<artifacts_this_phase_produces>
This plan creates no new symbols — it wires 30 existing route pages to the Wave-1 `getAlternates()` and extended `buildRouteJsonLd()`. (New symbols were produced in 74-01.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Alternates sweep across all 30 route pages</name>
  <files>app/[locale]/routes/*/page.tsx (29 prague-* pages), app/[locale]/routes/page.tsx</files>
  <read_first>
    - app/[locale]/routes/prague-vienna/page.tsx (the completed tracer reference — copy its generateMetadata alternates call shape)
    - lib/seo.ts (getAlternates signature from 74-01)
    - app/[locale]/routes/page.tsx (hub page — confirm whether it uses getRouteContent; the hub is a chrome/list page, so pass no content ref → all-locale cluster)
    - One representative prague-* page (e.g. prague-berlin/page.tsx) to confirm the hand-rolled alternates block shape being replaced
  </read_first>
  <action>For each of the 29 prague-* route pages, replace the hand-rolled alternates object inside generateMetadata with getAlternates('/routes/<slug>', { indexable: true, content: { kind: 'route', key: '<slug>' } }), using that page's own slug. Preserve each page's existing canonical path, openGraph.url, and single shared OG image reference (localize only title/description/alt per D-08). For the /routes hub page, replace its alternates block with getAlternates('/routes', { indexable: true }) with NO content ref (the hub is a translated chrome/list page, so all 7 locales are valid). Do not alter revalidate, getLocale, or any body content. Import getAlternates from @/lib/seo in each file. This is a homogeneous mechanical sweep against the Wave-1 tracer-proven contract (same shape as Phase 73's 30-32-file sweeps): if context pressure appears partway through, split the remaining files into a second mechanical batch (e.g., first 15 pages, then the remaining 14 + the hub) rather than truncating verification.</action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo && test "$(grep -rl "getAlternates(" app/[locale]/routes | wc -l | tr -d ' ')" -ge 30 && grep -rL "getAlternates(" app/[locale]/routes/*/page.tsx | grep -v prague-vienna | wc -l | grep -q '^0$' && echo SWEEP-OK</automated>
  </verify>
  <acceptance_criteria>
    - Every file under `app/[locale]/routes/` (30 pages incl. the hub) calls `getAlternates(` in generateMetadata
    - No route page.tsx retains a hand-rolled `'x-default':` literal inside generateMetadata (grep shows the literal only in lib/seo.ts) — `grep -rl "'x-default'" app/[locale]/routes | wc -l` returns 0
    - `npx vitest run tests/route-page-render.test.tsx` still green (prague-vienna + prague-berlin render assertions unaffected)
  </acceptance_criteria>
  <done>All 30 route pages emit their hreflang cluster from getAlternates(); no hand-rolled languages literals remain under routes/.</done>
</task>

<task type="auto">
  <name>Task 2: JSON-LD inLanguage on all 29 prague-* route pages</name>
  <files>app/[locale]/routes/*/page.tsx (29 prague-* pages)</files>
  <read_first>
    - app/[locale]/routes/prague-vienna/page.tsx (the completed buildRouteJsonLd opts caller shape)
    - lib/jsonld.ts (buildRouteJsonLd opts signature from 74-01 — { locale, name, description } + inLanguage)
    - lib/content-interpolate.ts (use plain interpolate for JSON-LD text, never interpolateBidi)
  </read_first>
  <action>For each of the 29 prague-* route pages, update the buildRouteJsonLd call in the page body to pass the third opts argument: the active locale plus the page's already-in-scope interpolate()'d content.metadata.title and content.metadata.description as name and description. Keep using plain interpolate (JSON-LD is JSON.stringify'd, never a ReactNode). Do not change any price, @id, url, priceValidUntil, or the breadcrumb block. The EN path stays byte-identical because buildRouteJsonLd only branches to the passed name/description when locale is not en. Same split-batch allowance as Task 1: if context pressure appears partway through, split into two mechanical batches rather than truncating verification.</action>
  <verify>
    <automated>npx vitest run tests/route-page-render.test.tsx tests/jsonld.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - At least 29 prague-* route pages call buildRouteJsonLd with a third opts argument carrying locale + name + description
    - `npx vitest run tests/route-page-render.test.tsx tests/jsonld.test.ts` green; prague-vienna snapshot unchanged (EN inLanguage already in the Wave-1 snapshot)
    - No price, priceValidUntil, @id, url, or breadcrumb change in any route page body (source review: only the buildRouteJsonLd call args changed)
  </acceptance_criteria>
  <done>All 29 prague-* route pages pass inLanguage-bearing opts to buildRouteJsonLd; EN JSON-LD unchanged apart from inLanguage; suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time content tree → emitted hreflang cluster | 30 route pages now emit multi-locale clusters read by crawlers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-02a | Tampering | route-page hreflang correctness | medium | mitigate | Clusters come only from the Wave-1 getAlternates() (D-06/D-07 gates centralized) — a per-page hand-rolled literal cannot reintroduce a contradictory or untranslated-claim cluster; grep gate proves no literals remain |
| T-74-SC | Tampering | package installs | low | accept | No new packages (RESEARCH audit) |
</threat_model>

<verification>
- `grep -rl "'x-default'" app/[locale]/routes | wc -l` returns 0 (all clusters centralized).
- `npx vitest run tests/route-page-render.test.tsx tests/jsonld.test.ts tests/seo.test.ts` green.
</verification>

<success_criteria>
All 30 indexable route pages emit index/translation-aware hreflang clusters and localized JSON-LD inLanguage, EN structured data unchanged apart from inLanguage.
</success_criteria>

<output>
Create `.planning/phases/74-seo-hreflang-localized-metadata-sitemap-structured-data-swit/74-02-SUMMARY.md` when done.
</output>