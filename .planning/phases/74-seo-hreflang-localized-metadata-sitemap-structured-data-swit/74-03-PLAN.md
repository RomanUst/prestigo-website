---
phase: 74-seo-hreflang-localized-metadata-sitemap-structured-data-swit
plan: 03
type: execute
wave: 2
depends_on: ["74-01"]
files_modified:
  - app/[locale]/services/airport-transfer/page.tsx
  - app/[locale]/services/city-rides/page.tsx
  - app/[locale]/services/concierge/page.tsx
  - app/[locale]/services/intercity-routes/page.tsx
  - app/[locale]/services/page.tsx
  - app/[locale]/fleet/page.tsx
  - app/[locale]/book/page.tsx
  - app/[locale]/book/multi-day/page.tsx
  - app/[locale]/page.tsx
  - app/[locale]/data-deletion/page.tsx
  - app/[locale]/blog/[slug]/page.tsx
  - app/[locale]/blog/prague-airport-taxi-vs-chauffeur/page.tsx
  - app/[locale]/blog/prague-airport-to-city-center/page.tsx
  - app/[locale]/blog/prague-vienna-transfer-vs-train/page.tsx
  - app/[locale]/authors/roman-ustyugov/page.tsx
  - lib/jsonld.ts
autonomous: true
requirements: [SEO-01, SEO-03, SEO-04]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every ISR non-route page (services airport-transfer/city-rides/concierge/intercity-routes/hub, /fleet, /book, /book/multi-day, home) emits its hreflang cluster via getAlternates() with no hand-rolled languages literal remaining (SEO-01)"
    - "The noindex /data-deletion page emits NO hreflang cluster (getAlternates indexable:false → empty languages) while staying self-canonical — closing the live D-06 violation (SEO-01, D-06)"
    - "services/intercity-routes keeps its cross-canonical → /routes and emits the /routes cluster from the same helper (canonical may target a different path than the page's own URL)"
    - "The blog /blog/[slug] page emits a locale alternate only where a localized MDX file exists (D-07); the 3 EN-only JSX blog posts and the author page stay self-canonical → EN with no non-EN alternate claimed (SEO-01, D-07/D-08)"
    - "buildAirportTransferJsonLd carries inLanguage (BCP-47) and the home JSON-LD carries inLanguage; EN JSON-LD text is unchanged apart from the inLanguage key (SEO-04, D-09)"
    - "{ statement: 'A locale with no content file for a swept page falls back to EN body and stays self-canonical → EN — the page renders, never 404s, for an untranslated locale', verification: backstop }"
    - "{ statement: 'Metadata title/description values are read from the content model with exact UTF-8 string equality (no normalization) — encoding of length/equality is byte/string level as produced by getPageContent', verification: backstop }"
    - "{ statement: 'Per-URL languages key order is deterministic across page and sitemap (routing.locales order, x-default last)', verification: backstop }"
  artifacts:
    - "10 ISR non-route page.tsx files wired to getAlternates"
    - "5 blog/author page.tsx files wired to getAlternates (D-07 content-aware)"
    - "lib/jsonld.ts buildAirportTransferJsonLd extended with inLanguage"
  key_links:
    - "getAlternates(indexable:false) closes the /data-deletion + (in 74-04) corporate-accounts live D-06 violations"
    - "blog/[slug] passes { kind:'blog', key:slug } so getAlternates fs-probes content/blog/<locale>/<slug>.mdx"
  prohibitions:
    - "Never emit an hreflang cluster on the noindex /data-deletion page"
    - "Never claim a non-EN alternate for an EN-only JSX blog post or the author page"
    - "Never build locale URLs by joining the locale code to the path"
    - "EN output is unchanged apart from the intended hreflang cluster and JSON-LD inLanguage additions"
---

<objective>
Sweep the ISR (already-correct-locale) non-route surface — service pages, `/fleet`, `/book`, `/book/multi-day`, home, and the blog/author surface — onto `getAlternates()`, close the live noindex D-06 violation on `/data-deletion`, apply the blog D-07 content-aware cluster, and add `inLanguage` to the airport-transfer + home JSON-LD.

Purpose: these pages already resolve locale correctly (no `getLocale` bug — that set is 74-04), so this is a pure alternates + JSON-LD-inLanguage sweep, plus the two D-06/D-07 correctness gates the phase exists to enforce.

Output: 15 non-route pages centralized on the helper; `/data-deletion` self-canonical with no cluster; blog content-aware; airport/home JSON-LD carrying `inLanguage`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-seo-hreflang-localized-metadata-sitemap-structured-data-swit/74-PATTERNS.md
@app/[locale]/routes/prague-vienna/page.tsx
</context>

<artifacts_this_phase_produces>
This plan wires 15 existing pages to the Wave-1 `getAlternates()` and extends `buildAirportTransferJsonLd` in `lib/jsonld.ts` with an `inLanguage` field (BCP-47). No new components.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Alternates sweep — ISR services + fleet + book + home + noindex data-deletion</name>
  <files>app/[locale]/services/airport-transfer/page.tsx, app/[locale]/services/city-rides/page.tsx, app/[locale]/services/concierge/page.tsx, app/[locale]/services/intercity-routes/page.tsx, app/[locale]/services/page.tsx, app/[locale]/fleet/page.tsx, app/[locale]/book/page.tsx, app/[locale]/book/multi-day/page.tsx, app/[locale]/page.tsx, app/[locale]/data-deletion/page.tsx</files>
  <read_first>
    - app/[locale]/routes/prague-vienna/page.tsx (getAlternates call shape reference)
    - lib/seo.ts (getAlternates signature)
    - app/[locale]/services/intercity-routes/page.tsx (existing cross-canonical → /routes block to preserve)
    - app/[locale]/data-deletion/page.tsx (current robots index:false + the alternates cluster it wrongly emits — the live D-06 violation)
    - app/[locale]/page.tsx (home generateMetadata + its bespoke JSON-LD block)
    - Each target page's current generateMetadata to identify the hand-rolled alternates block and whether it sources content via getPageContent (pass a page content ref where it does)
  </read_first>
  <action>Replace the hand-rolled alternates object in each page's generateMetadata with a getAlternates() call using that page's canonical path. For content-model pages pass { indexable:true, content:{ kind:'page', key:<page-key> } }; for the services hub / book / fleet / home treat as translated chrome pages (indexable:true, no content ref → all-locale cluster) unless the page genuinely has a content/pages/<locale>/<key>.json file, in which case pass the page content ref. For services/intercity-routes preserve its cross-canonical target (call getAlternates('/routes', { indexable:true }) so canonical stays /routes). For /data-deletion pass { indexable:false } so getAlternates returns an empty languages object — the page keeps its self-canonical and robots index:false and emits NO alternate cluster. Add inLanguage to the home page's bespoke top-level JSON-LD node (BCP-47 of the active locale) without changing any other schema value. Do not touch revalidate/getLocale/body content.</action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo && grep -q "getAlternates(" app/[locale]/data-deletion/page.tsx && grep -Eq "indexable: ?false" app/[locale]/data-deletion/page.tsx && test "$(grep -rl "x-default" app/[locale]/services app/[locale]/fleet app/[locale]/book app/[locale]/page.tsx app/[locale]/data-deletion 2>/dev/null | wc -l | tr -d ' ')" = "0" && echo ISR-SWEEP-OK</automated>
  </verify>
  <acceptance_criteria>
    - /data-deletion calls getAlternates with an indexable-false option and emits no hreflang cluster while keeping robots index:false + self-canonical
    - No hand-rolled x-default literal remains under services/, fleet, book, or home
    - services/intercity-routes still canonicalizes to /routes
    - `npx vitest run tests/sitemap.test.ts` green (sitemap uses the same helper; data-deletion is not in the sitemap regardless)
  </acceptance_criteria>
  <done>ISR non-route pages centralized on getAlternates; /data-deletion self-canonical with no cluster (D-06 violation closed).</done>
</task>

<task type="auto">
  <name>Task 2: Blog + author surface — D-07 content-aware alternates</name>
  <files>app/[locale]/blog/[slug]/page.tsx, app/[locale]/blog/prague-airport-taxi-vs-chauffeur/page.tsx, app/[locale]/blog/prague-airport-to-city-center/page.tsx, app/[locale]/blog/prague-vienna-transfer-vs-train/page.tsx, app/[locale]/authors/roman-ustyugov/page.tsx</files>
  <read_first>
    - lib/blog.ts (resolveLocalizedMdx/blogCanonical — the existing D-07 EN-fallback logic already consumed by blog/[slug])
    - app/[locale]/blog/[slug]/page.tsx (current generateMetadata + how it already computes canonical via blogCanonical)
    - lib/seo.ts (getAlternates blog content ref path)
    - One JSX blog post page (prague-airport-taxi-vs-chauffeur/page.tsx) and the author page — both EN-only by design (Phase 71 D-08)
  </read_first>
  <action>For app/[locale]/blog/[slug]/page.tsx replace the hand-rolled alternates block with getAlternates('/blog/' + slug, { indexable: true, content: { kind: 'blog', key: slug } }) so the cluster carries only locales whose content/blog/<locale>/<slug>.mdx exists (D-07); preserve the existing blogCanonical EN-fallback canonical behavior (a fallback post stays canonical → EN). For the 3 EN-only JSX blog posts and the author page, replace their alternates blocks with getAlternates('<path>', { indexable: true }) with NO content ref BUT confirm they emit only en + x-default (these paths have no non-EN content; if getAlternates with no content ref would include all locales, instead pass a content ref that resolves to en-only, or pass an explicit en-only cluster path) — the correct outcome is a self-canonical EN page with no claimed non-EN alternate. Choose the getAlternates invocation that yields exactly { en, x-default } for these 4 EN-only pages and document the choice in the SUMMARY.</action>
  <verify>
    <automated>npx vitest run tests/sitemap.test.ts tests/route-page-render.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - blog/[slug] passes a blog content ref to getAlternates; a slug with only an EN MDX file yields en + x-default only
    - The 3 JSX posts + author page emit exactly en + x-default (no non-EN alternate claimed)
    - No hand-rolled x-default literal remains under app/[locale]/blog or app/[locale]/authors
    - `grep -rl "x-default" app/[locale]/blog app/[locale]/authors | wc -l` returns 0
  </acceptance_criteria>
  <done>Blog and author surface centralized on getAlternates with correct D-07 content-awareness; EN-only pages stay self-canonical.</done>
</task>

<task type="auto">
  <name>Task 3: buildAirportTransferJsonLd inLanguage + airport page wiring</name>
  <files>lib/jsonld.ts, app/[locale]/services/airport-transfer/page.tsx</files>
  <read_first>
    - lib/jsonld.ts (buildAirportTransferJsonLd lines ~117-168 — add inLanguage the same way buildRouteJsonLd got it in 74-01; keep priceValidUntil = futureIsoDate(365))
    - app/[locale]/services/airport-transfer/page.tsx (the buildAirportTransferJsonLd caller + whether locale-aware content metadata is in scope to localize name/description)
    - tests/jsonld.test.ts (buildAirportTransferJsonLd assertions to extend)
  </read_first>
  <action>Extend buildAirportTransferJsonLd to accept an optional opts arg carrying the active locale (and optionally localized name/description if the airport page has content-model metadata in scope) and add inLanguage set to the BCP-47 tag of that locale, defaulting to en. Keep the EN name/description exactly as the current hardcoded English strings unless a genuine content-model source is available for non-en. Wire the airport-transfer page to pass the active locale. Extend tests/jsonld.test.ts to assert inLanguage is present and defaults to en. Do not change any price, priceValidUntil, or provider @id.</action>
  <verify>
    <automated>npx vitest run tests/jsonld.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "inLanguage" lib/jsonld.ts` ≥ 2 (route + airport builders)
    - tests/jsonld.test.ts asserts buildAirportTransferJsonLd emits inLanguage and EN price/name unchanged
    - `npx vitest run tests/jsonld.test.ts` green
  </acceptance_criteria>
  <done>Airport-transfer JSON-LD carries inLanguage; EN airport structured data unchanged apart from inLanguage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| noindex directive ↔ emitted alternates | /data-deletion must not contradict its own noindex with a hreflang cluster |
| blog content tree → claimed locale alternates | blog/[slug] must not claim a locale that only serves EN fallback |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-03a | Tampering | noindex/hreflang contradiction (data-deletion) | medium | mitigate | getAlternates(indexable:false) returns empty languages — the D-06 gate is centralized; grep proves no cluster remains |
| T-74-03b | Tampering | blog EN-fallback locale-claim | medium | mitigate | getAlternates blog content ref fs-probes per-locale MDX (D-07) — no fabricated non-EN alternate for an EN-fallback post |
| T-74-SC | Tampering | package installs | low | accept | No new packages (RESEARCH audit) |
</threat_model>

<verification>
- `grep -rl "x-default" app/[locale]/services app/[locale]/fleet app/[locale]/book app/[locale]/blog app/[locale]/authors app/[locale]/page.tsx app/[locale]/data-deletion | wc -l` returns 0.
- `npx vitest run tests/sitemap.test.ts tests/jsonld.test.ts tests/route-page-render.test.tsx` green.
</verification>

<success_criteria>
All ISR non-route + blog/author pages centralized on getAlternates with correct D-06/D-07 gates; airport/home JSON-LD carry inLanguage; EN output unchanged apart from the intended additions.
</success_criteria>

<output>
Create `.planning/phases/74-seo-hreflang-localized-metadata-sitemap-structured-data-swit/74-03-SUMMARY.md` when done.
</output>