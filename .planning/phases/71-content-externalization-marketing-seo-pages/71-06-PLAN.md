---
phase: 71-content-externalization-marketing-seo-pages
plan: 06
type: execute
wave: 2
depends_on: ["71-01"]
files_modified:
  - content/pages/en/services/airport-transfer.json
  - content/pages/en/services/corporate-accounts.json
  - content/pages/en/services/group-transfers.json
  - content/pages/en/services/intercity-routes.json
  - content/pages/en/services/vip-events.json
  - app/[locale]/services/airport-transfer/page.tsx
  - app/[locale]/services/corporate-accounts/page.tsx
  - app/[locale]/services/group-transfers/page.tsx
  - app/[locale]/services/intercity-routes/page.tsx
  - app/[locale]/services/vip-events/page.tsx
  - tests/services-b.test.ts
autonomous: true
requirements: [CNT-02]
user_setup: []

estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "airport-transfer, corporate-accounts, group-transfers, intercity-routes, and vip-events render their long-form bodies from content/pages/en/services/<slug>.json via getPageContent, with no user-facing body-copy literal remaining in the page"
    - { statement: "Rendered English output (visible copy + Service/FAQ JSON-LD + generateMetadata) is byte-for-byte unchanged for these 5 service pages at their current URLs", verification: backstop }
    - "getPageContent resolves under EN fallback so all 7 locales render EN content for these pages without 404"
    - "After this plan (with plan 05), all 8 service pages + hub are externalized (CNT-02 service coverage complete)"
    - "npx vitest run full suite is green"
  artifacts:
    - content/pages/en/services/airport-transfer.json (+4 sibling service JSONs)
    - tests/services-b.test.ts
  key_links:
    - "Each page imports getPageContent (Wave 1) + getLocale + interpolate; JSON-LD sources from the same loaded content object"
  prohibitions:
    - "No non-EN content authored (only content/pages/en/ written)"
    - "No shared page-body renderer introduced — each page keeps its bespoke JSX (D-04, Pitfall 4)"
    - "No new npm package added"
    - "content loader never imported into a client component"
    - "alternates/canonical blocks left byte-identical (Pitfall 6)"
---

<objective>
Externalize the remaining 5 bespoke service pages into `content/pages/en/services/` via the Wave 1 `getPageContent` loader. Byte-for-byte English output preserved; each page gets its own narrow content type (Pitfall 4).

Purpose: Completes CNT-02 service-page coverage (with plan 05).
Output: 5 content JSON files, 5 converted pages, parity assertions in tests/services-b.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/71-content-externalization-marketing-seo-pages/71-RESEARCH.md
@.planning/phases/71-content-externalization-marketing-seo-pages/71-PATTERNS.md
@.planning/phases/71-content-externalization-marketing-seo-pages/71-01-SUMMARY.md
@lib/page-content.ts
@lib/content-interpolate.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Externalize the airport-transfer service page</name>
  <files>content/pages/en/services/airport-transfer.json, app/[locale]/services/airport-transfer/page.tsx, tests/services-b.test.ts</files>
  <behavior>
    - `getPageContent('services/airport-transfer','en')` narrowed + interpolated reproduces every current literal byte-for-byte (Features, Meet & Greet, How it works, Journey times, Vehicle classes, FAQ, CTA, generateMetadata).
    - Service/FAQ JSON-LD (if present) is built from the same loaded content object.
  </behavior>
  <read_first>
    - app/[locale]/services/airport-transfer/page.tsx (FULL FILE, 368 lines — richest service page)
    - lib/page-content.ts (getPageContent signature, segment-wise guard, EN fallback)
    - content/routes/en/prague-vienna.json + its page (proven pattern to mirror)
    - .planning/phases/71-content-externalization-marketing-seo-pages/71-PATTERNS.md §Pitfall 4
  </read_first>
  <action>
    Create `content/pages/en/services/airport-transfer.json` holding the page's long-form/structured body copy (price sentences tokenized; verbatim otherwise). In `app/[locale]/services/airport-transfer/page.tsx`, add getLocale + getPageContent + interpolate imports, define a narrow local `type AirportTransferContent`, cast `getPageContent('services/airport-transfer', locale) as AirportTransferContent`, replace each body literal (`features`, `faqs`, section prose, etc.) with content reads (interpolated where a token exists), keep the bespoke JSX untouched (D-04). generateMetadata title/description/OG from content; `alternates` byte-identical. Source Service/FAQ JSON-LD from the same content object; apply the `</script>`-escaping helper to any NEW loader-derived JSON-LD written via dangerouslySetInnerHTML (T-71-02). Create `tests/services-b.test.ts` (node env) importing getPageContent: assert EN-fallback for this page + a segment-wise traversal-rejection case (`getPageContent('services/../x','en')` throws) + 3-string spot-check against verbatim originals.
  </action>
  <verify>
    <automated>npx vitest run tests/services-b.test.ts 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `content/pages/en/services/airport-transfer.json` parses; page renders from it.
    - `grep -nE "const (features|faqs) = \[" app/[locale]/services/airport-transfer/page.tsx` returns nothing.
    - `grep -c "use client" app/[locale]/services/airport-transfer/page.tsx` is 0.
    - `npx vitest run tests/services-b.test.ts` exits 0 including the traversal-rejection case.
  </acceptance_criteria>
  <done>airport-transfer renders from content with byte-identical EN output; parity + guard tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Externalize corporate-accounts and group-transfers</name>
  <files>content/pages/en/services/corporate-accounts.json, content/pages/en/services/group-transfers.json, app/[locale]/services/corporate-accounts/page.tsx, app/[locale]/services/group-transfers/page.tsx, tests/services-b.test.ts</files>
  <behavior>
    - For each page: narrowed + interpolated content reproduces every current literal byte-for-byte.
  </behavior>
  <read_first>
    - app/[locale]/services/corporate-accounts/page.tsx, group-transfers/page.tsx (FULL FILES)
    - content/pages/en/services/airport-transfer.json + its page (Task 1 reference pair)
    - lib/page-content.ts
  </read_first>
  <action>
    Convert both pages the same way as Task 1 (content JSON + narrowed getPageContent read with `services/<slug>` key + untouched bespoke JSX + generateMetadata from content + alternates untouched + JSON-LD from content). Extend tests/services-b.test.ts with EN-fallback + 3-string spot-check for each page.
  </action>
  <verify>
    <automated>npx vitest run tests/services-b.test.ts 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Both pages render from their content JSON; no body-copy literal remains (spot-grep returns nothing).
    - `npx vitest run tests/services-b.test.ts` exits 0.
  </acceptance_criteria>
  <done>corporate-accounts + group-transfers converted with byte-identical EN output; parity green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Externalize intercity-routes and vip-events</name>
  <files>content/pages/en/services/intercity-routes.json, content/pages/en/services/vip-events.json, app/[locale]/services/intercity-routes/page.tsx, app/[locale]/services/vip-events/page.tsx, tests/services-b.test.ts</files>
  <behavior>
    - For each page: narrowed + interpolated content reproduces every current literal byte-for-byte.
  </behavior>
  <read_first>
    - app/[locale]/services/intercity-routes/page.tsx, vip-events/page.tsx (FULL FILES)
    - content/pages/en/services/airport-transfer.json + its page (reference)
    - lib/page-content.ts
  </read_first>
  <action>
    Convert both pages the same way. Extend tests/services-b.test.ts with EN-fallback + 3-string spot-check for each, then run the FULL suite.
  </action>
  <verify>
    <automated>npx vitest run tests/services-b.test.ts && npx vitest run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Both pages render from their content JSON; no body-copy literal remains.
    - `npx vitest run` full suite green.
  </acceptance_criteria>
  <done>All 5 group-B service pages converted; with plan 05, all service pages externalized; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page key → filesystem | nested `services/<slug>` key flows into getPageContent → path.join + fs read |
| loaded content → JSON-LD dangerouslySetInnerHTML | Service/FAQ JSON-LD serialized into a script tag |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-01 | Tampering / Information Disclosure | getPageContent path construction | high | mitigate | Segment-wise `^[a-z0-9-]+$` validation + locale allowlist enforced in Wave 1; page keys are static literals; traversal-rejection asserted in tests/services-b.test.ts. |
| T-71-02 | Tampering (XSS via `</script>`) | any NEW loader-derived Service/FAQ JSON-LD | medium | mitigate | Apply `</script>`→`<\/script>` escaping (blog page helper) to any new JSON-LD written via dangerouslySetInnerHTML. Pre-existing inline JSON.stringify left as-is (out of scope). |

Package legitimacy: N/A — no npm installs.
</threat_model>

<artifacts_produced>
## Artifacts this plan produces
- content/pages/en/services/{airport-transfer,corporate-accounts,group-transfers,intercity-routes,vip-events}.json (5 files)
- tests/services-b.test.ts
</artifacts_produced>

<verification>
- `npx vitest run` full suite green.
- No user-facing body literal remains in the 5 converted service pages.
- getPageContent traversal-rejection case green.
</verification>

<success_criteria>
The 5 remaining service pages serve their bodies from the content model with byte-for-byte-unchanged English output and working EN fallback; combined with plan 05, all service pages + hub are externalized.
</success_criteria>

<output>
Create `.planning/phases/71-content-externalization-marketing-seo-pages/71-06-SUMMARY.md` when done.
</output>
