---
phase: 71-content-externalization-marketing-seo-pages
plan: 05
type: execute
wave: 2
depends_on: ["71-01"]
files_modified:
  - content/pages/en/services.json
  - content/pages/en/services/city-rides.json
  - content/pages/en/services/concierge.json
  - app/[locale]/services/page.tsx
  - app/[locale]/services/city-rides/page.tsx
  - app/[locale]/services/concierge/page.tsx
  - tests/services-a.test.ts
autonomous: true
requirements: [CNT-02]
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The services hub (app/[locale]/services/page.tsx), city-rides, and concierge pages render their long-form bodies from content/pages/en/<page>.json via getPageContent, with no user-facing body-copy literal remaining in the page"
    - { statement: "Rendered English output (visible copy + any Service/FAQ JSON-LD + generateMetadata title/description/OG) is byte-for-byte unchanged for these 3 pages at their current URLs", verification: backstop }
    - "getPageContent(page,'en') resolves under EN fallback so all 7 locales render EN content for these pages without 404"
    - "npx vitest run full suite is green"
  artifacts:
    - content/pages/en/services.json
    - content/pages/en/services/city-rides.json
    - content/pages/en/services/concierge.json
    - tests/services-a.test.ts
  key_links:
    - "Each page imports getPageContent (Wave 1 lib/page-content.ts) + getLocale + interpolate; JSON-LD sources from the same loaded content object (no duplicated FAQ literal)"
  prohibitions:
    - "No non-EN content authored (only content/pages/en/ written)"
    - "No shared page-body renderer introduced — each page keeps its existing bespoke JSX (D-04, Pitfall 4: no universal PageContent interface)"
    - "No new npm package added"
    - "content loader never imported into a client component"
    - "alternates/canonical blocks in generateMetadata left byte-identical (Pitfall 6)"
---

<objective>
Externalize the long-form bodies of the services hub page and two bespoke service subpages (city-rides, concierge) into the `content/pages/en/` model via the Wave 1 `getPageContent` loader — each page keeps its own bespoke JSX and gets a narrow per-page content type (Pitfall 4). Byte-for-byte English output preserved.

Purpose: CNT-02 coverage for the hub + first service subpages, proving the page-content loader on real bespoke pages.
Output: 3 content JSON files, 3 converted pages, parity assertions in tests/services-a.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/71-content-externalization-marketing-seo-pages/71-RESEARCH.md
@.planning/phases/71-content-externalization-marketing-seo-pages/71-PATTERNS.md
@.planning/phases/71-content-externalization-marketing-seo-pages/71-01-SUMMARY.md
@lib/page-content.ts
@lib/content-interpolate.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Externalize the services hub page (app/[locale]/services/page.tsx)</name>
  <files>content/pages/en/services.json, app/[locale]/services/page.tsx, tests/services-a.test.ts</files>
  <behavior>
    - `getPageContent('services','en')` returns an object that, once narrowed and interpolated, reproduces every current hub-page literal byte-for-byte (hero, section headings, service cards copy, any FAQ, CTA, generateMetadata title/description/OG).
    - Service/FAQ JSON-LD (if present) is built from the same loaded content object.
  </behavior>
  <read_first>
    - app/[locale]/services/page.tsx (FULL FILE, 420 lines — every literal const + generateMetadata + JSON-LD)
    - lib/page-content.ts (getPageContent signature + EN-fallback behavior)
    - content/routes/en/prague-vienna.json + app/[locale]/routes/prague-vienna/page.tsx (the proven route pattern to mirror for the page-content variant)
    - .planning/phases/71-content-externalization-marketing-seo-pages/71-PATTERNS.md §"services/<slug>/page.tsx ... bespoke", §Pitfall 4
  </read_first>
  <action>
    Create `content/pages/en/services.json` holding the hub page's long-form/structured body copy (any price-bearing sentence tokenized with `{...}` placeholders matching the page's own price variables; verbatim otherwise). In `app/[locale]/services/page.tsx`, add imports `getLocale` (next-intl/server), `getPageContent` (@/lib/page-content), and `interpolate` if the page interpolates prices; define a narrow local `type ServicesHubContent` for the exact shape this page reads and cast `getPageContent('services', locale) as ServicesHubContent`; replace each body-copy literal with the corresponding `content.*` read (interpolated where a token exists), keeping the existing bespoke JSX otherwise untouched (D-04). Source generateMetadata title/description/OG from the content object; leave `alternates`/canonical byte-identical. If the page builds Service/FAQ JSON-LD, source it from the same content object and — for any NEW loader-derived JSON-LD string written to `dangerouslySetInnerHTML` — apply the `</script>`-escaping helper pattern from app/[locale]/blog/[slug]/page.tsx (T-71-02). Create `tests/services-a.test.ts` (node env) importing getPageContent, asserting EN-fallback (`getPageContent('services','ru')` deep-equals `'en'`) and spot-checking 3 representative body strings against verbatim originals.
  </action>
  <verify>
    <automated>npx vitest run tests/services-a.test.ts 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `content/pages/en/services.json` parses; `getPageContent('services','en')` returns it.
    - No user-facing body-copy literal remains in the hub page (spot-grep of 3 removed sentences returns nothing in page.tsx).
    - `grep -c "use client" app/[locale]/services/page.tsx` is 0 (loader stays server-side).
    - `npx vitest run tests/services-a.test.ts` exits 0.
  </acceptance_criteria>
  <done>Hub page renders from content/pages/en/services.json with byte-identical EN output; EN-fallback + spot-check green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Externalize the city-rides service page</name>
  <files>content/pages/en/services/city-rides.json, app/[locale]/services/city-rides/page.tsx, tests/services-a.test.ts</files>
  <behavior>
    - `getPageContent('services/city-rides','en')` narrowed + interpolated reproduces every current literal byte-for-byte.
  </behavior>
  <read_first>
    - app/[locale]/services/city-rides/page.tsx (FULL FILE)
    - app/[locale]/services/page.tsx + content/pages/en/services.json (Task 1 reference pair, once done)
    - lib/page-content.ts
  </read_first>
  <action>
    Create `content/pages/en/services/city-rides.json` (nested under services/ per RESEARCH Open Q3) and convert `app/[locale]/services/city-rides/page.tsx` the same way as the hub: getLocale + `getPageContent('services/city-rides', locale)` narrowed to a local `type CityRidesContent`, body literals replaced with content reads, bespoke JSX untouched, generateMetadata from content, alternates untouched, JSON-LD from the same content object (escaping any new loader-derived JSON-LD). The Wave 1 `getPageContent` guard validates the key segment-wise (`services/city-rides` → both segments match `^[a-z0-9-]+$`), so the nested key is safe — do NOT weaken the guard. Extend tests/services-a.test.ts with EN-fallback + 3-string spot-check for this page.
  </action>
  <verify>
    <automated>npx vitest run tests/services-a.test.ts 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - city-rides page renders from its content JSON; no body-copy literal remains (spot-grep returns nothing).
    - The nested page key passes the Wave 1 segment-wise path-traversal guard (asserted: `getPageContent('services/../x','en')` throws).
    - `npx vitest run tests/services-a.test.ts` exits 0.
  </acceptance_criteria>
  <done>city-rides renders from content with byte-identical EN output; parity green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Externalize the concierge service page</name>
  <files>content/pages/en/services/concierge.json, app/[locale]/services/concierge/page.tsx, tests/services-a.test.ts</files>
  <behavior>
    - `getPageContent(<concierge key>,'en')` narrowed + interpolated reproduces every current literal byte-for-byte.
  </behavior>
  <read_first>
    - app/[locale]/services/concierge/page.tsx (FULL FILE)
    - content/pages/en/services/city-rides.json + its page (Task 2 reference)
    - lib/page-content.ts
  </read_first>
  <action>
    Convert `app/[locale]/services/concierge/page.tsx` the same way (content JSON + narrowed getPageContent read + untouched bespoke JSX + generateMetadata from content + alternates untouched + JSON-LD from content). Use the same nested-vs-flat key decision made in Task 2 for consistency. Extend tests/services-a.test.ts with EN-fallback + 3-string spot-check for concierge, then run the FULL suite.
  </action>
  <verify>
    <automated>npx vitest run tests/services-a.test.ts && npx vitest run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - concierge page renders from its content JSON; no body-copy literal remains.
    - `npx vitest run` full suite green.
  </acceptance_criteria>
  <done>All 3 group-A service pages converted with byte-identical EN output; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page key → filesystem | page key flows into getPageContent → path.join + fs read |
| loaded content → JSON-LD dangerouslySetInnerHTML | Service/FAQ JSON-LD serialized into a script tag |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-01 | Tampering / Information Disclosure | getPageContent path construction | high | mitigate | Path-traversal + locale allowlist enforced inside getPageContent (Wave 1). Page keys are static literals; nested keys are passed only in a form the Wave 1 guard validates (per Task 2 decision). No guard weakening. |
| T-71-02 | Tampering (XSS via `</script>`) | any NEW loader-derived Service/FAQ JSON-LD | medium | mitigate | Any new JSON-LD string built from loaded content and written via dangerouslySetInnerHTML applies the `</script>`→`<\/script>` escaping helper already used in app/[locale]/blog/[slug]/page.tsx. Pre-existing inline route/service JSON.stringify left as-is (out of scope). |

Package legitimacy: N/A — no npm installs.
</threat_model>

<artifacts_produced>
## Artifacts this plan produces
- content/pages/en/services.json
- content/pages/en/services/city-rides.json
- content/pages/en/services/concierge.json
- tests/services-a.test.ts
</artifacts_produced>

<verification>
- `npx vitest run` full suite green.
- No user-facing body literal remains in the 3 converted service pages.
- Loaders stay server-side; page keys pass the path-traversal guard.
</verification>

<success_criteria>
The services hub, city-rides, and concierge pages serve their bodies from the locale-aware content model with byte-for-byte-unchanged English output and working EN fallback for all locales.
</success_criteria>

<output>
Create `.planning/phases/71-content-externalization-marketing-seo-pages/71-05-SUMMARY.md` when done.
</output>
