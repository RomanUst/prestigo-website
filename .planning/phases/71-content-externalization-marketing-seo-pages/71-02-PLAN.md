---
phase: 71-content-externalization-marketing-seo-pages
plan: 02
type: execute
wave: 2
depends_on: ["71-01"]
files_modified:
  - content/routes/en/prague-berlin.json
  - content/routes/en/prague-bratislava.json
  - content/routes/en/prague-brno.json
  - content/routes/en/prague-budapest.json
  - content/routes/en/prague-ceske-budejovice.json
  - content/routes/en/prague-cesky-krumlov.json
  - content/routes/en/prague-dresden.json
  - content/routes/en/prague-frantiskovy-lazne.json
  - content/routes/en/prague-graz.json
  - content/routes/en/prague-hradec-kralove.json
  - app/[locale]/routes/prague-berlin/page.tsx
  - app/[locale]/routes/prague-bratislava/page.tsx
  - app/[locale]/routes/prague-brno/page.tsx
  - app/[locale]/routes/prague-budapest/page.tsx
  - app/[locale]/routes/prague-ceske-budejovice/page.tsx
  - app/[locale]/routes/prague-cesky-krumlov/page.tsx
  - app/[locale]/routes/prague-dresden/page.tsx
  - app/[locale]/routes/prague-frantiskovy-lazne/page.tsx
  - app/[locale]/routes/prague-graz/page.tsx
  - app/[locale]/routes/prague-hradec-kralove/page.tsx
  - tests/routes-a.test.ts
autonomous: true
requirements: [CNT-01]
user_setup: []

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each of the 10 group-A route pages renders from content/routes/en/<slug>.json via getRouteContent, with no route-body prose literal remaining in the page.tsx"
    - { statement: "Rendered English output (visible copy + FAQPage/BreadcrumbList JSON-LD + generateMetadata title/description/OG) is byte-for-byte unchanged for all 10 group-A routes at their current URLs", verification: backstop }
    - "The indexable vs noindex split for these routes is preserved (no change to robots/noindex handling in any of the 10 page.tsx)"
    - "prague-berlin keeps its own 'Trip Configurations' label and prague-hradec-kralove keeps its own FAQ heading in that route's content JSON (NOT the shared RoutePage namespace) — Pitfall 1"
    - "npx vitest run full suite is green"
  artifacts:
    - content/routes/en/prague-berlin.json (+9 sibling route JSONs)
    - tests/routes-a.test.ts
  key_links:
    - "Each page imports getRouteContent + interpolate + getLocale from Wave 1 (read-only consumers of lib/route-content.ts, lib/content-interpolate.ts, messages/en.json)"
  prohibitions:
    - "No non-EN content authored (only content/routes/en/ written)"
    - "No shared <RoutePageBody> renderer introduced — each page keeps its existing JSX (D-04)"
    - "Locale-invariant route data (prices/distanceKm/duration/tier/metadataTitle) NOT moved out of lib/routes.ts (D-02)"
    - "messages/en.json is NOT written by this plan (RoutePage namespace consumed read-only)"
    - "No new npm package added"
---

<objective>
Convert 10 of the remaining 29 route pages (group A) onto the content model established by the Wave 1 tracer, replicating the exact `prague-vienna` transformation. Mechanical, byte-for-byte-preserving in-place replacement of prose literals with `getRouteContent(slug, locale)` reads.

Purpose: CNT-01 coverage for group A. The architecture is already proven; this plan applies it at scale to disjoint route files so it can run in parallel with groups B and C.
Output: 10 `content/routes/en/<slug>.json` files, 10 converted `page.tsx` files, and per-slug parity assertions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/71-content-externalization-marketing-seo-pages/71-RESEARCH.md
@.planning/phases/71-content-externalization-marketing-seo-pages/71-PATTERNS.md
@.planning/phases/71-content-externalization-marketing-seo-pages/71-01-SUMMARY.md
@lib/route-content.ts
@lib/content-interpolate.ts
@app/[locale]/routes/prague-vienna/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert prague-berlin, prague-bratislava, prague-brno, prague-budapest</name>
  <files>content/routes/en/prague-berlin.json, content/routes/en/prague-bratislava.json, content/routes/en/prague-brno.json, content/routes/en/prague-budapest.json, app/[locale]/routes/prague-berlin/page.tsx, app/[locale]/routes/prague-bratislava/page.tsx, app/[locale]/routes/prague-brno/page.tsx, app/[locale]/routes/prague-budapest/page.tsx, tests/routes-a.test.ts</files>
  <behavior>
    - For each slug: `interpolate(getRouteContent(slug,'en').<field>, FIXTURE_PRICES)` reproduces every current literal byte-for-byte (hero, opening paragraphs, route narrative, inclusions, journey stops, good-to-know, day-trip config, chauffeur narrative, whyBook, faqs, related routes, highlights, cta, metadata).
    - prague-berlin's "Trip Configurations" section label is stored in prague-berlin.json (it is NOT the 30/30 shared "Day Trips from Prague" chrome string).
  </behavior>
  <read_first>
    - app/[locale]/routes/prague-vienna/page.tsx AND content/routes/en/prague-vienna.json (the proven reference pair — mirror it exactly)
    - app/[locale]/routes/prague-berlin/page.tsx, prague-bratislava/page.tsx, prague-brno/page.tsx, prague-budapest/page.tsx (FULL FILES — extract every literal)
    - tests/helpers/route-content-parity.ts (FIXTURE_PRICES, assertRouteContentShape, assertRouteParity)
    - .planning/phases/71-content-externalization-marketing-seo-pages/71-PATTERNS.md §"app/[locale]/routes/<slug>/page.tsx", §Non-uniformity guard (Pitfall 1)
  </read_first>
  <action>
    For each of the 4 slugs, replicate the Wave 1 tracer transformation exactly: create `content/routes/en/<slug>.json` from the page's literals (price sentences tokenized as `{ePrice}`/`{sPrice}`/`{vPrice}`; highlights + relatedRoutes captured verbatim); edit the `page.tsx` to add `getLocale`/`getRouteContent`/`interpolate`/`getTranslations` imports, read `content = getRouteContent('<slug>', locale)`, and replace each prose literal const with the corresponding `content.*` read mapped through `interpolate(..., prices)`, keeping identical variable names so the JSX is untouched (D-04). Move only the 30/30-verified chrome strings to `t('RoutePage....')`; for prague-berlin, the "Trip Configurations" label is page-specific — keep it in prague-berlin.json, NOT the namespace (Pitfall 1). Source generateMetadata title/description/OG from `content.metadata`; leave the `alternates` block byte-identical (Pitfall 6). FAQPage JSON-LD sources from `content.faqs` (no duplicated literal). Create `tests/routes-a.test.ts` (node env) importing the helpers from tests/helpers/route-content-parity.ts, and for these 4 slugs add: `assertRouteContentShape` + an EN-fallback deep-equal check + `assertRouteParity(slug, {...})` on 3 representative fields per slug captured verbatim from the original.
  </action>
  <verify>
    <automated>npx vitest run tests/routes-a.test.ts 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -REl "const (inclusions|dayTripConfigurations|whyBook|faqs|relatedRoutes) = \[" app/[locale]/routes/prague-berlin app/[locale]/routes/prague-bratislava app/[locale]/routes/prague-brno app/[locale]/routes/prague-budapest` returns nothing.
    - `grep -Rc "RoutePageBody" app/[locale]/routes/prague-berlin app/[locale]/routes/prague-bratislava app/[locale]/routes/prague-brno app/[locale]/routes/prague-budapest` is 0 for each.
    - `grep -F "Trip Configurations" content/routes/en/prague-berlin.json` matches (kept in content JSON, not the namespace).
    - `git diff messages/en.json` is empty (plan does not write it).
    - `npx vitest run tests/routes-a.test.ts` exits 0.
  </acceptance_criteria>
  <done>4 routes render from content JSON with byte-identical EN output; parity assertions green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert prague-ceske-budejovice, prague-cesky-krumlov, prague-dresden</name>
  <files>content/routes/en/prague-ceske-budejovice.json, content/routes/en/prague-cesky-krumlov.json, content/routes/en/prague-dresden.json, app/[locale]/routes/prague-ceske-budejovice/page.tsx, app/[locale]/routes/prague-cesky-krumlov/page.tsx, app/[locale]/routes/prague-dresden/page.tsx, tests/routes-a.test.ts</files>
  <behavior>
    - For each slug: interpolated content reproduces every current literal byte-for-byte.
    - Any section label not present in all 30 route files stays in that slug's content JSON, not messages/en.json (Pitfall 1).
  </behavior>
  <read_first>
    - content/routes/en/prague-vienna.json + app/[locale]/routes/prague-vienna/page.tsx (reference pair)
    - app/[locale]/routes/prague-ceske-budejovice/page.tsx, prague-cesky-krumlov/page.tsx, prague-dresden/page.tsx (FULL FILES)
    - tests/helpers/route-content-parity.ts
  </read_first>
  <action>
    Replicate the tracer transformation for these 3 slugs exactly as in Task 1 (content JSON + page edit + tokenized prices + untouched JSX + content-sourced JSON-LD + content.metadata for generateMetadata, alternates untouched). Before promoting any section label to `t('RoutePage....')`, confirm it is one of the Wave-1 30/30-verified strings; otherwise keep it in the slug's content JSON. Extend `tests/routes-a.test.ts` with a shape + EN-fallback + `assertRouteParity` block for each of these 3 slugs.
  </action>
  <verify>
    <automated>npx vitest run tests/routes-a.test.ts 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - No route-body prose literal remains in the 3 page.tsx (`grep -REl "const (inclusions|faqs|whyBook) = \[" ...` returns nothing).
    - `git diff messages/en.json` empty.
    - `npx vitest run tests/routes-a.test.ts` exits 0 with parity blocks for all 3 slugs.
  </acceptance_criteria>
  <done>3 routes converted with byte-identical EN output; parity green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Convert prague-frantiskovy-lazne, prague-graz, prague-hradec-kralove</name>
  <files>content/routes/en/prague-frantiskovy-lazne.json, content/routes/en/prague-graz.json, content/routes/en/prague-hradec-kralove.json, app/[locale]/routes/prague-frantiskovy-lazne/page.tsx, app/[locale]/routes/prague-graz/page.tsx, app/[locale]/routes/prague-hradec-kralove/page.tsx, tests/routes-a.test.ts</files>
  <behavior>
    - For each slug: interpolated content reproduces every current literal byte-for-byte.
    - prague-hradec-kralove's FAQ section heading (which differs from the 30/30 "Frequently asked questions") is stored in prague-hradec-kralove.json, NOT the RoutePage namespace (Pitfall 1).
  </behavior>
  <read_first>
    - content/routes/en/prague-vienna.json + app/[locale]/routes/prague-vienna/page.tsx (reference pair)
    - app/[locale]/routes/prague-frantiskovy-lazne/page.tsx, prague-graz/page.tsx, prague-hradec-kralove/page.tsx (FULL FILES)
    - tests/helpers/route-content-parity.ts
    - .planning/phases/71-content-externalization-marketing-seo-pages/71-PATTERNS.md §Non-uniformity guard
  </read_first>
  <action>
    Replicate the tracer transformation for these 3 slugs. For prague-hradec-kralove, the FAQ heading is page-specific — the JSX for that page reads its heading from the route's own content JSON (add a `faqHeading` field to that slug's JSON) rather than `t('RoutePage.sectionLabels.faqHeading')`; verify with `grep -Fl "Frequently asked questions" app/[locale]/routes/prague-hradec-kralove/page.tsx` that the shared string is NOT hardcoded back in. Extend `tests/routes-a.test.ts` with shape + EN-fallback + `assertRouteParity` for each slug. Then run the FULL suite to confirm no regression across the group.
  </action>
  <verify>
    <automated>npx vitest run tests/routes-a.test.ts && npx vitest run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - prague-hradec-kralove's distinct FAQ heading lives in content/routes/en/prague-hradec-kralove.json (grep confirms) and is read from content in its page.tsx.
    - No route-body prose literal remains in the 3 page.tsx.
    - `git diff messages/en.json` empty.
    - `npx vitest run` full suite green.
  </acceptance_criteria>
  <done>All 10 group-A routes converted; per-slug parity green; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dynamic route param → filesystem | slug flows into getRouteContent (guarded in Wave 1); this plan adds no new fs-path construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-01 | Tampering / Information Disclosure | getRouteContent (consumed, not modified) | high | mitigate | Path-traversal + locale allowlist already enforced inside getRouteContent (Wave 1, T-71-01). These pages pass only static literal slugs; no new path construction is introduced. |
| T-71-02 | Tampering (XSS via `</script>`) | route-page FAQPage JSON-LD | low | accept | Existing inline `JSON.stringify(pageSchema)` wiring preserved unchanged; FAQ text is relocated marketing copy, not user input. No new JSON-LD builder added. |

Package legitimacy: N/A — no npm installs.
</threat_model>

<artifacts_produced>
## Artifacts this plan produces
- content/routes/en/{prague-berlin,prague-bratislava,prague-brno,prague-budapest,prague-ceske-budejovice,prague-cesky-krumlov,prague-dresden,prague-frantiskovy-lazne,prague-graz,prague-hradec-kralove}.json (10 files)
- tests/routes-a.test.ts (per-slug parity assertions)
</artifacts_produced>

<verification>
- `npx vitest run` full suite green.
- No prose literal remains in any of the 10 group-A page.tsx.
- messages/en.json untouched by this plan.
</verification>

<success_criteria>
10 group-A route pages serve their bodies from the locale-aware content model with byte-for-byte-unchanged English output; the indexable/noindex split is preserved; parity assertions cover every slug.
</success_criteria>

<output>
Create `.planning/phases/71-content-externalization-marketing-seo-pages/71-02-SUMMARY.md` when done.
</output>
