---
phase: 71-content-externalization-marketing-seo-pages
plan: 08
type: execute
wave: 2
depends_on: ["71-01"]
files_modified:
  - content/pages/en/privacy.json
  - content/pages/en/terms.json
  - content/pages/en/data-deletion.json
  - app/[locale]/privacy/page.tsx
  - app/[locale]/terms/page.tsx
  - app/[locale]/data-deletion/page.tsx
  - tests/legal.test.ts
autonomous: true
requirements: [CNT-02]
user_setup: []

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The privacy, terms, and data-deletion pages render their clause-by-clause long-form bodies from content/pages/en/<page>.json via getPageContent, with no user-facing legal-copy literal remaining in the page"
    - { statement: "Rendered English output (every legal clause + generateMetadata) is byte-for-byte unchanged for these 3 legal pages at their current URLs", verification: backstop }
    - "getPageContent resolves under EN fallback so all 7 locales render EN legal content without 404"
    - "Legal entity details (chelautotrans s.r.o., IČO 05650801, Spojovací 685) are reproduced verbatim — no transcription drift"
    - "npx vitest run full suite is green"
  artifacts:
    - content/pages/en/privacy.json
    - content/pages/en/terms.json
    - content/pages/en/data-deletion.json
    - tests/legal.test.ts
  key_links:
    - "Each page imports getPageContent (Wave 1) + getLocale; JSON-LD/metadata unchanged for EN"
  prohibitions:
    - "No non-EN content authored (only content/pages/en/ written)"
    - "No shared page-body renderer introduced — each page keeps its bespoke JSX (D-04, Pitfall 4)"
    - "No new npm package added; loader never imported into a client component"
    - "alternates/canonical blocks left byte-identical (Pitfall 6)"
    - "Legal entity constants (name/IČO/address) are moved verbatim — not paraphrased or re-formatted"
---

<objective>
Externalize the long clause-by-clause bodies of the three legal pages (privacy, terms, data-deletion) into `content/pages/en/` via `getPageContent`. Byte-for-byte English output preserved — legal text carries the highest transcription-accuracy bar in the phase.

Purpose: CNT-02 coverage for the legal pages; completes CNT-02 (with plans 05-07).
Output: 3 content JSON files, 3 converted pages, parity assertions in tests/legal.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/71-content-externalization-marketing-seo-pages/71-RESEARCH.md
@.planning/phases/71-content-externalization-marketing-seo-pages/71-PATTERNS.md
@.planning/phases/71-content-externalization-marketing-seo-pages/71-01-SUMMARY.md
@lib/page-content.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Externalize the privacy page</name>
  <files>content/pages/en/privacy.json, app/[locale]/privacy/page.tsx, tests/legal.test.ts</files>
  <behavior>
    - `getPageContent('privacy','en')` narrowed reproduces every clause of the 400-line privacy page byte-for-byte, including legal entity details and any dates/version strings.
  </behavior>
  <read_first>
    - app/[locale]/privacy/page.tsx (FULL FILE, 400 lines — all clause prose + generateMetadata)
    - lib/page-content.ts
    - content/routes/en/prague-vienna.json + its page (proven pattern)
  </read_first>
  <action>
    Create `content/pages/en/privacy.json` holding the page's clause-by-clause legal prose (headings + paragraphs + lists), reproducing every string verbatim — including the legal entity constants (chelautotrans s.r.o., IČO 05650801, Spojovací 685) exactly, no reformatting. Convert app/[locale]/privacy/page.tsx: getLocale + getPageContent('privacy', locale) narrowed to a local `type PrivacyContent`, replace clause literals with content reads, keep bespoke JSX untouched (D-04). generateMetadata title/description from content; alternates untouched. Create `tests/legal.test.ts` (node env) importing getPageContent: EN-fallback for privacy + 4-string spot-check against verbatim originals (include the legal entity line to catch transcription drift).
  </action>
  <verify>
    <automated>npx vitest run tests/legal.test.ts 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - privacy page renders from content JSON; no clause prose literal remains (spot-grep of 3 clauses returns nothing in page.tsx).
    - `grep -F "chelautotrans s.r.o." content/pages/en/privacy.json` matches verbatim.
    - `grep -c "use client" app/[locale]/privacy/page.tsx` is 0.
    - `npx vitest run tests/legal.test.ts` exits 0 including the legal-entity spot-check.
  </acceptance_criteria>
  <done>privacy page renders from content with byte-identical EN output incl. verbatim entity details; parity green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Externalize the terms page</name>
  <files>content/pages/en/terms.json, app/[locale]/terms/page.tsx, tests/legal.test.ts</files>
  <behavior>
    - `getPageContent('terms','en')` narrowed reproduces every clause of the 311-line terms page byte-for-byte.
  </behavior>
  <read_first>
    - app/[locale]/terms/page.tsx (FULL FILE, 311 lines)
    - content/pages/en/privacy.json + its page (Task 1 reference)
    - lib/page-content.ts
  </read_first>
  <action>
    Create `content/pages/en/terms.json` (verbatim clause prose) and convert app/[locale]/terms/page.tsx the same way as Task 1 (narrowed getPageContent('terms', locale), clause literals → content reads, bespoke JSX untouched, generateMetadata from content, alternates untouched). Extend tests/legal.test.ts with EN-fallback + 4-string spot-check for terms.
  </action>
  <verify>
    <automated>npx vitest run tests/legal.test.ts 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - terms page renders from content JSON; no clause literal remains.
    - `npx vitest run tests/legal.test.ts` exits 0.
  </acceptance_criteria>
  <done>terms page renders from content with byte-identical EN output; parity green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Externalize the data-deletion page</name>
  <files>content/pages/en/data-deletion.json, app/[locale]/data-deletion/page.tsx, tests/legal.test.ts</files>
  <behavior>
    - `getPageContent('data-deletion','en')` narrowed reproduces every literal of the 168-line data-deletion page byte-for-byte.
  </behavior>
  <read_first>
    - app/[locale]/data-deletion/page.tsx (FULL FILE, 168 lines)
    - content/pages/en/privacy.json + its page (reference)
    - lib/page-content.ts
  </read_first>
  <action>
    Create `content/pages/en/data-deletion.json` (verbatim prose) and convert app/[locale]/data-deletion/page.tsx the same way. Extend tests/legal.test.ts with EN-fallback + 3-string spot-check for data-deletion, then run the FULL suite.
  </action>
  <verify>
    <automated>npx vitest run tests/legal.test.ts && npx vitest run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - data-deletion page renders from content JSON; no prose literal remains.
    - `npx vitest run` full suite green.
  </acceptance_criteria>
  <done>All 3 legal pages render from content with byte-identical EN output; CNT-02 complete (with plans 05-07); full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page key → filesystem | page key flows into getPageContent → path.join + fs read |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-01 | Tampering / Information Disclosure | getPageContent path construction | high | mitigate | Segment-wise `^[a-z0-9-]+$` validation + locale allowlist enforced in Wave 1; page keys are static literals. |
| T-71-02 | Tampering (XSS via `</script>`) | any legal-page JSON-LD | low | accept | Legal pages carry minimal/no JSON-LD; any pre-existing inline JSON.stringify is preserved unchanged. No new loader-derived JSON-LD added. |

Package legitimacy: N/A — no npm installs.
</threat_model>

<artifacts_produced>
## Artifacts this plan produces
- content/pages/en/{privacy,terms,data-deletion}.json (3 files)
- tests/legal.test.ts
</artifacts_produced>

<verification>
- `npx vitest run` full suite green.
- No legal-copy literal remains in the 3 converted pages.
- Legal entity details reproduced verbatim (spot-check).
</verification>

<success_criteria>
The privacy, terms, and data-deletion pages serve their clause-by-clause bodies from the content model with byte-for-byte-unchanged English output and working EN fallback; combined with plans 05-07, CNT-02 is complete.
</success_criteria>

<output>
Create `.planning/phases/71-content-externalization-marketing-seo-pages/71-08-SUMMARY.md` when done.
</output>
