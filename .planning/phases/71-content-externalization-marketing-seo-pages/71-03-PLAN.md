---
phase: 71-content-externalization-marketing-seo-pages
plan: 03
type: execute
wave: 2
depends_on: ["71-01"]
files_modified:
  - content/routes/en/prague-karlovy-vary.json
  - content/routes/en/prague-krakow.json
  - content/routes/en/prague-kutna-hora.json
  - content/routes/en/prague-leipzig.json
  - content/routes/en/prague-liberec.json
  - content/routes/en/prague-linz.json
  - content/routes/en/prague-marianske-lazne.json
  - content/routes/en/prague-munich.json
  - content/routes/en/prague-nuremberg.json
  - content/routes/en/prague-olomouc.json
  - app/[locale]/routes/prague-karlovy-vary/page.tsx
  - app/[locale]/routes/prague-krakow/page.tsx
  - app/[locale]/routes/prague-kutna-hora/page.tsx
  - app/[locale]/routes/prague-leipzig/page.tsx
  - app/[locale]/routes/prague-liberec/page.tsx
  - app/[locale]/routes/prague-linz/page.tsx
  - app/[locale]/routes/prague-marianske-lazne/page.tsx
  - app/[locale]/routes/prague-munich/page.tsx
  - app/[locale]/routes/prague-nuremberg/page.tsx
  - app/[locale]/routes/prague-olomouc/page.tsx
  - tests/routes-b.test.ts
autonomous: true
requirements: [CNT-01]
user_setup: []

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each of the 10 group-B route pages renders from content/routes/en/<slug>.json via getRouteContent, with no route-body prose literal remaining in the page.tsx"
    - { statement: "Rendered English output (visible copy + FAQPage/BreadcrumbList JSON-LD + generateMetadata) is byte-for-byte unchanged for all 10 group-B routes at their current URLs", verification: backstop }
    - "The indexable vs noindex split for these routes is preserved"
    - "Any section label not present in all 30 route files stays in that slug's content JSON, not the shared RoutePage namespace (Pitfall 1)"
    - "npx vitest run full suite is green"
  artifacts:
    - content/routes/en/prague-karlovy-vary.json (+9 sibling route JSONs)
    - tests/routes-b.test.ts
  key_links:
    - "Each page consumes getRouteContent + interpolate + getLocale + RoutePage namespace from Wave 1 (read-only)"
  prohibitions:
    - "No non-EN content authored (only content/routes/en/ written)"
    - "No shared <RoutePageBody> renderer introduced (D-04)"
    - "Locale-invariant route data NOT moved out of lib/routes.ts (D-02)"
    - "messages/en.json NOT written by this plan"
    - "No new npm package added"
---

<objective>
Convert group B (10 route pages) onto the content model, replicating the Wave 1 tracer transformation. Runs in parallel with groups A and C (disjoint files).

Purpose: CNT-01 coverage for group B.
Output: 10 content/routes/en/<slug>.json, 10 converted page.tsx, per-slug parity assertions in tests/routes-b.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/71-content-externalization-marketing-seo-pages/71-RESEARCH.md
@.planning/phases/71-content-externalization-marketing-seo-pages/71-PATTERNS.md
@.planning/phases/71-content-externalization-marketing-seo-pages/71-01-SUMMARY.md
@lib/route-content.ts
@lib/content-interpolate.ts
@app/[locale]/routes/prague-vienna/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert prague-karlovy-vary, prague-krakow, prague-kutna-hora, prague-leipzig</name>
  <files>content/routes/en/prague-karlovy-vary.json, content/routes/en/prague-krakow.json, content/routes/en/prague-kutna-hora.json, content/routes/en/prague-leipzig.json, app/[locale]/routes/prague-karlovy-vary/page.tsx, app/[locale]/routes/prague-krakow/page.tsx, app/[locale]/routes/prague-kutna-hora/page.tsx, app/[locale]/routes/prague-leipzig/page.tsx, tests/routes-b.test.ts</files>
  <behavior>
    - For each slug: `interpolate(getRouteContent(slug,'en').<field>, FIXTURE_PRICES)` reproduces every current literal byte-for-byte.
    - prague-kutna-hora is the 479-line cross-check page RESEARCH read in full — mirror its structure precisely.
  </behavior>
  <read_first>
    - content/routes/en/prague-vienna.json + app/[locale]/routes/prague-vienna/page.tsx (reference pair)
    - app/[locale]/routes/prague-karlovy-vary/page.tsx, prague-krakow/page.tsx, prague-kutna-hora/page.tsx, prague-leipzig/page.tsx (FULL FILES)
    - tests/helpers/route-content-parity.ts
    - .planning/phases/71-content-externalization-marketing-seo-pages/71-PATTERNS.md §"app/[locale]/routes/<slug>/page.tsx"
  </read_first>
  <action>
    Replicate the tracer transformation for these 4 slugs: create each `content/routes/en/<slug>.json` from the page literals (tokenized prices, verbatim highlights + relatedRoutes); edit each page.tsx to read `content = getRouteContent('<slug>', locale)`, replacing prose consts with `content.*` + `interpolate(..., prices)` under identical variable names (JSX untouched, D-04); promote ONLY the 30/30-verified chrome to `t('RoutePage....')` and keep any non-30/30 label in the slug's content JSON (Pitfall 1); source generateMetadata from `content.metadata` with `alternates` untouched; FAQPage JSON-LD from `content.faqs`. Create `tests/routes-b.test.ts` (node env) importing tests/helpers/route-content-parity.ts and adding shape + EN-fallback + `assertRouteParity(slug,{...})` (3 representative fields/slug verbatim) for these 4 slugs.
  </action>
  <verify>
    <automated>npx vitest run tests/routes-b.test.ts 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -REl "const (inclusions|dayTripConfigurations|whyBook|faqs|relatedRoutes) = \[" app/[locale]/routes/prague-karlovy-vary app/[locale]/routes/prague-krakow app/[locale]/routes/prague-kutna-hora app/[locale]/routes/prague-leipzig` returns nothing.
    - `git diff messages/en.json` empty.
    - `npx vitest run tests/routes-b.test.ts` exits 0.
  </acceptance_criteria>
  <done>4 routes converted with byte-identical EN output; parity green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert prague-liberec, prague-linz, prague-marianske-lazne</name>
  <files>content/routes/en/prague-liberec.json, content/routes/en/prague-linz.json, content/routes/en/prague-marianske-lazne.json, app/[locale]/routes/prague-liberec/page.tsx, app/[locale]/routes/prague-linz/page.tsx, app/[locale]/routes/prague-marianske-lazne/page.tsx, tests/routes-b.test.ts</files>
  <behavior>
    - For each slug: interpolated content reproduces every current literal byte-for-byte.
  </behavior>
  <read_first>
    - content/routes/en/prague-vienna.json + app/[locale]/routes/prague-vienna/page.tsx (reference pair)
    - app/[locale]/routes/prague-liberec/page.tsx, prague-linz/page.tsx, prague-marianske-lazne/page.tsx (FULL FILES)
    - tests/helpers/route-content-parity.ts
  </read_first>
  <action>
    Replicate the tracer transformation for these 3 slugs exactly as in Task 1. Extend `tests/routes-b.test.ts` with shape + EN-fallback + `assertRouteParity` blocks for each slug.
  </action>
  <verify>
    <automated>npx vitest run tests/routes-b.test.ts 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - No route-body prose literal remains in the 3 page.tsx.
    - `git diff messages/en.json` empty.
    - `npx vitest run tests/routes-b.test.ts` exits 0 with parity blocks for all 3 slugs.
  </acceptance_criteria>
  <done>3 routes converted with byte-identical EN output; parity green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Convert prague-munich, prague-nuremberg, prague-olomouc</name>
  <files>content/routes/en/prague-munich.json, content/routes/en/prague-nuremberg.json, content/routes/en/prague-olomouc.json, app/[locale]/routes/prague-munich/page.tsx, app/[locale]/routes/prague-nuremberg/page.tsx, app/[locale]/routes/prague-olomouc/page.tsx, tests/routes-b.test.ts</files>
  <behavior>
    - For each slug: interpolated content reproduces every current literal byte-for-byte.
  </behavior>
  <read_first>
    - content/routes/en/prague-vienna.json + app/[locale]/routes/prague-vienna/page.tsx (reference pair)
    - app/[locale]/routes/prague-munich/page.tsx, prague-nuremberg/page.tsx, prague-olomouc/page.tsx (FULL FILES)
    - tests/helpers/route-content-parity.ts
  </read_first>
  <action>
    Replicate the tracer transformation for these 3 slugs. Extend `tests/routes-b.test.ts` with shape + EN-fallback + `assertRouteParity` for each slug. Then run the FULL suite to confirm no regression across the group.
  </action>
  <verify>
    <automated>npx vitest run tests/routes-b.test.ts && npx vitest run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - No route-body prose literal remains in the 3 page.tsx.
    - `git diff messages/en.json` empty.
    - `npx vitest run` full suite green.
  </acceptance_criteria>
  <done>All 10 group-B routes converted; per-slug parity green; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dynamic route param → filesystem | slug flows into getRouteContent (guarded in Wave 1); this plan adds no new fs-path construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-01 | Tampering / Information Disclosure | getRouteContent (consumed) | high | mitigate | Path-traversal + locale allowlist enforced in Wave 1; pages pass only static literal slugs. |
| T-71-02 | Tampering (XSS via `</script>`) | route-page FAQPage JSON-LD | low | accept | Existing inline JSON.stringify wiring preserved; FAQ text is relocated copy, not user input. |

Package legitimacy: N/A — no npm installs.
</threat_model>

<artifacts_produced>
## Artifacts this plan produces
- content/routes/en/{prague-karlovy-vary,prague-krakow,prague-kutna-hora,prague-leipzig,prague-liberec,prague-linz,prague-marianske-lazne,prague-munich,prague-nuremberg,prague-olomouc}.json (10 files)
- tests/routes-b.test.ts
</artifacts_produced>

<verification>
- `npx vitest run` full suite green.
- No prose literal remains in any of the 10 group-B page.tsx.
- messages/en.json untouched by this plan.
</verification>

<success_criteria>
10 group-B route pages serve their bodies from the content model with byte-for-byte-unchanged English output; indexable/noindex split preserved; parity assertions cover every slug.
</success_criteria>

<output>
Create `.planning/phases/71-content-externalization-marketing-seo-pages/71-03-SUMMARY.md` when done.
</output>
