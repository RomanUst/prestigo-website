---
phase: 71-content-externalization-marketing-seo-pages
plan: 07
type: execute
wave: 2
depends_on: ["71-01"]
files_modified:
  - content/pages/en/home.json
  - content/pages/en/about.json
  - content/pages/en/faq.json
  - content/pages/en/contact.json
  - content/pages/en/corporate.json
  - app/[locale]/page.tsx
  - app/[locale]/about/page.tsx
  - app/[locale]/faq/page.tsx
  - app/[locale]/contact/page.tsx
  - app/[locale]/corporate/page.tsx
  - tests/marketing.test.ts
autonomous: true
requirements: [CNT-02]
user_setup: []

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Home long-form/SEO-description strings, about, faq, contact, and corporate render their long-form bodies from content/pages/en/<page>.json via getPageContent, with no user-facing body-copy literal remaining in those pages"
    - { statement: "Rendered English output (visible copy + AboutPage/FAQPage/AggregateRating/Organization JSON-LD + generateMetadata) is byte-for-byte unchanged for these 5 pages at their current URLs", verification: backstop }
    - "getPageContent resolves under EN fallback so all 7 locales render EN content for these pages without 404"
    - "FAQPage/AggregateRating structured data on the faq/about pages is built from the same loaded content object and stays byte-identical for EN"
    - "npx vitest run full suite is green"
  artifacts:
    - content/pages/en/home.json
    - content/pages/en/about.json
    - content/pages/en/faq.json
    - content/pages/en/contact.json
    - content/pages/en/corporate.json
    - tests/marketing.test.ts
  key_links:
    - "Each page imports getPageContent (Wave 1) + getLocale; JSON-LD (FAQPage/AggregateRating/AboutPage) sources from the same loaded content object — no duplicated FAQ/answer literal"
  prohibitions:
    - "No non-EN content authored (only content/pages/en/ written)"
    - "No shared page-body renderer introduced — each page keeps its bespoke JSX (D-04, Pitfall 4)"
    - "Home structural component config already externalized in Phase 69 is NOT re-touched — only remaining home long-form/SEO strings move (RESEARCH recommended structure)"
    - "No new npm package added; loader never imported into a client component"
    - "alternates/canonical blocks left byte-identical (Pitfall 6)"
---

<objective>
Externalize the remaining long-form bodies of the Home page (SEO/description strings only — its components were externalized in Phase 69), and the about / faq / contact / corporate marketing pages into `content/pages/en/` via `getPageContent`. Byte-for-byte English output preserved; FAQPage/AggregateRating JSON-LD sourced from the same content object.

Purpose: CNT-02 coverage for Home long-form + about/faq/contact/corporate.
Output: 5 content JSON files, 5 converted pages, parity assertions in tests/marketing.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/71-content-externalization-marketing-seo-pages/71-RESEARCH.md
@.planning/phases/71-content-externalization-marketing-seo-pages/71-PATTERNS.md
@.planning/phases/71-content-externalization-marketing-seo-pages/71-01-SUMMARY.md
@lib/page-content.ts
@lib/content-interpolate.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Externalize Home long-form/SEO strings and the contact page</name>
  <files>content/pages/en/home.json, content/pages/en/contact.json, app/[locale]/page.tsx, app/[locale]/contact/page.tsx, tests/marketing.test.ts</files>
  <behavior>
    - Home: `getPageContent('home','en')` reproduces the remaining hardcoded home strings byte-for-byte — generateMetadata title/description/OG and the description/name text inside localBusinessSchema/websiteSchema JSON-LD. Home component config already externalized in Phase 69 (messages/en.json) is NOT touched.
    - Contact: narrowed content reproduces the contact page's long-form prose byte-for-byte; form-field labels already in the Phase 69/70 catalog stay in the catalog (read-only), only long-form prose moves to content.
  </behavior>
  <read_first>
    - app/[locale]/page.tsx (FULL FILE, 245 lines — generateMetadata + localBusinessSchema + websiteSchema description/name strings; confirm which strings are still hardcoded vs already in messages/en.json)
    - app/[locale]/contact/page.tsx (FULL FILE, 241 lines)
    - lib/page-content.ts
    - content/routes/en/prague-vienna.json + its page (proven pattern)
  </read_first>
  <action>
    Create `content/pages/en/home.json` with ONLY the home strings still hardcoded in app/[locale]/page.tsx (generateMetadata title/description/OG + the description/name prose inside the JSON-LD schema objects). Do NOT move component config already externalized in Phase 69. Edit app/[locale]/page.tsx: add getLocale + getPageContent + interpolate imports, read `content = getPageContent('home', locale)` narrowed to a local `type HomeContent`, replace the still-hardcoded strings with content reads (interpolated where a price token exists — heroPrice/hourlyFrom already computed there), keep JSX/schema structure untouched; source the JSON-LD description/name from the content object. Create `content/pages/en/contact.json` and convert app/[locale]/contact/page.tsx: long-form prose → content, bespoke JSX untouched, generateMetadata from content, alternates untouched; any form-field labels already present in messages/en.json stay as `t(...)` reads (do not duplicate into content). Create `tests/marketing.test.ts` (node env) importing getPageContent: EN-fallback for home + contact, and 3-string spot-check each against verbatim originals.
  </action>
  <verify>
    <automated>npx vitest run tests/marketing.test.ts 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `getPageContent('home','en')` and `getPageContent('contact','en')` return the created JSON.
    - No still-hardcoded home SEO/schema prose remains in app/[locale]/page.tsx (spot-grep of 2 moved strings returns nothing).
    - No long-form prose literal remains in contact page.
    - `npx vitest run tests/marketing.test.ts` exits 0.
  </acceptance_criteria>
  <done>Home long-form/SEO strings + contact prose render from content with byte-identical EN output; parity green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Externalize the about page</name>
  <files>content/pages/en/about.json, app/[locale]/about/page.tsx, tests/marketing.test.ts</files>
  <behavior>
    - `getPageContent('about','en')` narrowed reproduces every current literal byte-for-byte (principles, requirements, founder bio prose, hero/section copy, generateMetadata).
    - AboutPage/AggregateRating JSON-LD is built from the same loaded content object; EN structured data stays byte-identical.
  </behavior>
  <read_first>
    - app/[locale]/about/page.tsx (FULL FILE, 394 lines — principles/requirements/founder/aboutPageSchemaGraph/getStaticAggregateRating)
    - lib/page-content.ts
    - content/pages/en/contact.json + its page (Task 1 reference)
  </read_first>
  <action>
    Create `content/pages/en/about.json` with the page's long-form/structured body copy (principles, requirements, founder bio prose, hero/section text). Convert app/[locale]/about/page.tsx: getLocale + getPageContent('about', locale) narrowed to a local `type AboutContent`, body literals replaced with content reads, bespoke JSX untouched (D-04). Keep the AggregateRating value source (getStaticAggregateRating) exactly as-is — only the prose text moves; the aboutPageSchemaGraph description/name text sources from the content object, numeric rating data unchanged. generateMetadata from content; alternates untouched. Extend tests/marketing.test.ts with EN-fallback + 3-string spot-check for about (include one JSON-LD-fed string to prove structured-data parity).
  </action>
  <verify>
    <automated>npx vitest run tests/marketing.test.ts 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "const (principles|requirements) = " app/[locale]/about/page.tsx` returns nothing.
    - about page renders from content JSON; AggregateRating numeric data unchanged (getStaticAggregateRating still called).
    - `npx vitest run tests/marketing.test.ts` exits 0.
  </acceptance_criteria>
  <done>about page renders from content with byte-identical EN output incl. AboutPage/AggregateRating JSON-LD; parity green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Externalize the faq and corporate pages</name>
  <files>content/pages/en/faq.json, content/pages/en/corporate.json, app/[locale]/faq/page.tsx, app/[locale]/corporate/page.tsx, tests/marketing.test.ts</files>
  <behavior>
    - faq: `getPageContent('faq','en')` narrowed reproduces the category-grouped `sections` + all Q&A byte-for-byte; the FAQPage JSON-LD (faqSchema) is built from the SAME loaded content object (no duplicated answer literal).
    - corporate: narrowed content reproduces the corporate page's long-form prose byte-for-byte; the CorporateForm component/labels are NOT moved (form is separate; only page prose moves).
  </behavior>
  <read_first>
    - app/[locale]/faq/page.tsx (FULL FILE, 233 lines — sections + faqSchema + breadcrumbSchema)
    - app/[locale]/corporate/page.tsx (FULL FILE, 361 lines)
    - lib/page-content.ts
    - content/pages/en/about.json + its page (Task 2 reference)
  </read_first>
  <action>
    Create `content/pages/en/faq.json` with the `sections` (category-grouped Q&A) and hero/section prose. Convert app/[locale]/faq/page.tsx: getLocale + getPageContent('faq', locale) narrowed to a local `type FaqContent`, replace `const sections` with a content read, keep bespoke JSX untouched; build `faqSchema.mainEntity` from the SAME content.sections Q&A (single source — the visible FAQ and the JSON-LD must not diverge); breadcrumbSchema unchanged (locale-invariant). Create `content/pages/en/corporate.json` and convert app/[locale]/corporate/page.tsx: long-form prose → content, bespoke JSX untouched, leave the CorporateForm import/usage untouched (form labels are out of scope for this content plan), generateMetadata from content, alternates untouched. Extend tests/marketing.test.ts with EN-fallback + 3-string spot-check for faq (include one JSON-LD-fed Q&A pair) and corporate, then run the FULL suite.
  </action>
  <verify>
    <automated>npx vitest run tests/marketing.test.ts && npx vitest run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "const sections = " app/[locale]/faq/page.tsx` returns nothing (sections sourced from content).
    - faqSchema mainEntity is derived from the same content.sections array (no second Q&A literal — grep shows Q&A text appears once).
    - corporate page renders from content; CorporateForm untouched.
    - `npx vitest run` full suite green.
  </acceptance_criteria>
  <done>faq + corporate render from content with byte-identical EN output; FAQPage JSON-LD single-sourced; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page key → filesystem | page key flows into getPageContent → path.join + fs read |
| loaded content → JSON-LD dangerouslySetInnerHTML | AboutPage/FAQPage/AggregateRating JSON-LD serialized into a script tag |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-01 | Tampering / Information Disclosure | getPageContent path construction | high | mitigate | Segment-wise `^[a-z0-9-]+$` validation + locale allowlist enforced in Wave 1; page keys are static literals. |
| T-71-02 | Tampering (XSS via `</script>`) | FAQPage/AboutPage JSON-LD built from loaded content | medium | mitigate | Apply `</script>`→`<\/script>` escaping (blog page helper) to any new JSON-LD written via dangerouslySetInnerHTML; FAQ answers are relocated copy, not user input. Pre-existing inline JSON.stringify left as-is. |

Package legitimacy: N/A — no npm installs.
</threat_model>

<artifacts_produced>
## Artifacts this plan produces
- content/pages/en/{home,about,faq,contact,corporate}.json (5 files)
- tests/marketing.test.ts
</artifacts_produced>

<verification>
- `npx vitest run` full suite green.
- No user-facing body literal remains in the 5 converted pages (spot-greps).
- FAQPage/AggregateRating structured data single-sourced from content, byte-identical for EN.
</verification>

<success_criteria>
Home long-form/SEO strings and the about/faq/contact/corporate pages serve their bodies from the content model with byte-for-byte-unchanged English output, EN fallback for all locales, and FAQPage/AggregateRating JSON-LD single-sourced from the loaded content.
</success_criteria>

<output>
Create `.planning/phases/71-content-externalization-marketing-seo-pages/71-07-SUMMARY.md` when done.
</output>
