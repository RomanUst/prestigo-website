---
phase: 71-content-externalization-marketing-seo-pages
plan: 04
type: execute
wave: 2
depends_on: ["71-01"]
files_modified:
  - content/routes/en/prague-ostrava.json
  - content/routes/en/prague-pardubice.json
  - content/routes/en/prague-passau.json
  - content/routes/en/prague-plzen.json
  - content/routes/en/prague-regensburg.json
  - content/routes/en/prague-salzburg.json
  - content/routes/en/prague-warsaw.json
  - content/routes/en/prague-wroclaw.json
  - content/routes/en/prague-zlin.json
  - app/[locale]/routes/prague-ostrava/page.tsx
  - app/[locale]/routes/prague-pardubice/page.tsx
  - app/[locale]/routes/prague-passau/page.tsx
  - app/[locale]/routes/prague-plzen/page.tsx
  - app/[locale]/routes/prague-regensburg/page.tsx
  - app/[locale]/routes/prague-salzburg/page.tsx
  - app/[locale]/routes/prague-warsaw/page.tsx
  - app/[locale]/routes/prague-wroclaw/page.tsx
  - app/[locale]/routes/prague-zlin/page.tsx
  - tests/routes-c.test.ts
autonomous: true
requirements: [CNT-01]
user_setup: []

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each of the 9 group-C route pages renders from content/routes/en/<slug>.json via getRouteContent, with no route-body prose literal remaining in the page.tsx"
    - { statement: "Rendered English output (visible copy + FAQPage/BreadcrumbList JSON-LD + generateMetadata) is byte-for-byte unchanged for all 9 group-C routes at their current URLs", verification: backstop }
    - "The indexable vs noindex split for these routes is preserved"
    - "Any section label not present in all 30 route files stays in that slug's content JSON, not the shared RoutePage namespace (Pitfall 1)"
    - "After this plan, no content-body prose literal remains in ANY of the 30 route pages (CNT-01 complete across all groups)"
    - "npx vitest run full suite is green"
  artifacts:
    - content/routes/en/prague-ostrava.json (+8 sibling route JSONs)
    - tests/routes-c.test.ts
  key_links:
    - "Each page consumes getRouteContent + interpolate + getLocale + RoutePage namespace from Wave 1 (read-only)"
  prohibitions:
    - "No non-EN content authored (only content/routes/en/ written)"
    - "No shared <RoutePageBody> renderer introduced (D-04)"
    - "Locale-invariant route data NOT moved out of lib/routes.ts (D-02)"
    - "messages/en.json NOT written by this plan"
    - "No new npm package added"
---

<objective>
Convert group C (9 route pages) onto the content model, replicating the Wave 1 tracer transformation. Runs in parallel with groups A and B (disjoint files). Completes CNT-01 across all 30 routes.

Purpose: CNT-01 coverage for group C + final route-body sweep.
Output: 9 content/routes/en/<slug>.json, 9 converted page.tsx, per-slug parity assertions in tests/routes-c.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/71-content-externalization-marketing-seo-pages/71-RESEARCH.md
@.planning/phases/71-content-externalization-marketing-seo-pages/71-PATTERNS.md
@.planning/phases/71-content-externalization-marketing-seo-pages/71-01-SUMMARY.md
@lib/route-content.ts
@lib/content-interpolate.ts
@app/[locale]/routes/prague-vienna/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert prague-ostrava, prague-pardubice, prague-passau</name>
  <files>content/routes/en/prague-ostrava.json, content/routes/en/prague-pardubice.json, content/routes/en/prague-passau.json, app/[locale]/routes/prague-ostrava/page.tsx, app/[locale]/routes/prague-pardubice/page.tsx, app/[locale]/routes/prague-passau/page.tsx, tests/routes-c.test.ts</files>
  <behavior>
    - For each slug: `interpolate(getRouteContent(slug,'en').<field>, FIXTURE_PRICES)` reproduces every current literal byte-for-byte.
  </behavior>
  <read_first>
    - content/routes/en/prague-vienna.json + app/[locale]/routes/prague-vienna/page.tsx (reference pair)
    - app/[locale]/routes/prague-ostrava/page.tsx, prague-pardubice/page.tsx, prague-passau/page.tsx (FULL FILES)
    - tests/helpers/route-content-parity.ts
    - .planning/phases/71-content-externalization-marketing-seo-pages/71-PATTERNS.md §"app/[locale]/routes/<slug>/page.tsx"
  </read_first>
  <action>
    Replicate the tracer transformation for these 3 slugs: create each `content/routes/en/<slug>.json` from the page literals (tokenized prices, verbatim highlights + relatedRoutes); edit each page.tsx to read `content = getRouteContent('<slug>', locale)`, replacing prose consts with `content.*` + `interpolate(..., prices)` under identical variable names (JSX untouched, D-04); promote ONLY 30/30-verified chrome to `t('RoutePage....')`, keep non-30/30 labels in the slug JSON (Pitfall 1); generateMetadata from `content.metadata`, `alternates` untouched; FAQPage JSON-LD from `content.faqs`. Create `tests/routes-c.test.ts` (node env) importing tests/helpers/route-content-parity.ts and adding shape + EN-fallback + `assertRouteParity(slug,{...})` for these 3 slugs.
  </action>
  <verify>
    <automated>npx vitest run tests/routes-c.test.ts 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -REl "const (inclusions|dayTripConfigurations|whyBook|faqs|relatedRoutes) = \[" app/[locale]/routes/prague-ostrava app/[locale]/routes/prague-pardubice app/[locale]/routes/prague-passau` returns nothing.
    - `git diff messages/en.json` empty.
    - `npx vitest run tests/routes-c.test.ts` exits 0.
  </acceptance_criteria>
  <done>3 routes converted with byte-identical EN output; parity green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert prague-plzen, prague-regensburg, prague-salzburg</name>
  <files>content/routes/en/prague-plzen.json, content/routes/en/prague-regensburg.json, content/routes/en/prague-salzburg.json, app/[locale]/routes/prague-plzen/page.tsx, app/[locale]/routes/prague-regensburg/page.tsx, app/[locale]/routes/prague-salzburg/page.tsx, tests/routes-c.test.ts</files>
  <behavior>
    - For each slug: interpolated content reproduces every current literal byte-for-byte.
  </behavior>
  <read_first>
    - content/routes/en/prague-vienna.json + app/[locale]/routes/prague-vienna/page.tsx (reference pair)
    - app/[locale]/routes/prague-plzen/page.tsx, prague-regensburg/page.tsx, prague-salzburg/page.tsx (FULL FILES)
    - tests/helpers/route-content-parity.ts
  </read_first>
  <action>
    Replicate the tracer transformation for these 3 slugs exactly as in Task 1. Extend `tests/routes-c.test.ts` with shape + EN-fallback + `assertRouteParity` blocks for each slug.
  </action>
  <verify>
    <automated>npx vitest run tests/routes-c.test.ts 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - No route-body prose literal remains in the 3 page.tsx.
    - `git diff messages/en.json` empty.
    - `npx vitest run tests/routes-c.test.ts` exits 0 with parity blocks for all 3 slugs.
  </acceptance_criteria>
  <done>3 routes converted with byte-identical EN output; parity green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Convert prague-warsaw, prague-wroclaw, prague-zlin + final route sweep</name>
  <files>content/routes/en/prague-warsaw.json, content/routes/en/prague-wroclaw.json, content/routes/en/prague-zlin.json, app/[locale]/routes/prague-warsaw/page.tsx, app/[locale]/routes/prague-wroclaw/page.tsx, app/[locale]/routes/prague-zlin/page.tsx, tests/routes-c.test.ts</files>
  <behavior>
    - For each slug: interpolated content reproduces every current literal byte-for-byte.
    - After this task, a repo-wide grep confirms zero route-body prose consts remain across all 30 route pages.
  </behavior>
  <read_first>
    - content/routes/en/prague-vienna.json + app/[locale]/routes/prague-vienna/page.tsx (reference pair)
    - app/[locale]/routes/prague-warsaw/page.tsx, prague-wroclaw/page.tsx, prague-zlin/page.tsx (FULL FILES)
    - tests/helpers/route-content-parity.ts
  </read_first>
  <action>
    Replicate the tracer transformation for these 3 slugs. Extend `tests/routes-c.test.ts` with shape + EN-fallback + `assertRouteParity` for each slug. Then run the FULL suite AND the cross-route sweep grep to confirm CNT-01 is complete across all 30 route pages.
  </action>
  <verify>
    <automated>npx vitest run tests/routes-c.test.ts && test $(grep -REl "const (inclusions|dayTripConfigurations|whyBook|faqs) = \[" app/[locale]/routes/ | wc -l | tr -d ' ') -eq 0 && npx vitest run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -REl "const (inclusions|dayTripConfigurations|whyBook|faqs) = \[" app/[locale]/routes/` returns nothing (all 30 routes swept).
    - `git diff messages/en.json` empty.
    - `npx vitest run` full suite green.
  </acceptance_criteria>
  <done>All 9 group-C routes converted; CNT-01 complete across all 30 routes; per-slug parity + full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dynamic route param → filesystem | slug flows into getRouteContent (guarded in Wave 1); this plan adds no new fs-path construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-01 | Tampering / Information Disclosure | getRouteContent (consumed) | high | mitigate | Path-traversal + locale allowlist enforced in Wave 1; pages pass only static literal slugs. |
| T-71-02 | Tampering (XSS via `</script>`) | route-page FAQPage JSON-LD | low | accept | Existing inline JSON.stringify wiring preserved; FAQ text is relocated copy, not user input. |

Package legitimacy: N/A — no npm installs.
</threat_model>

<artifacts_produced>
## Artifacts this plan produces
- content/routes/en/{prague-ostrava,prague-pardubice,prague-passau,prague-plzen,prague-regensburg,prague-salzburg,prague-warsaw,prague-wroclaw,prague-zlin}.json (9 files)
- tests/routes-c.test.ts
</artifacts_produced>

<verification>
- `npx vitest run` full suite green.
- Cross-route sweep grep confirms zero prose consts remain in any of the 30 route pages.
- messages/en.json untouched by this plan.
</verification>

<success_criteria>
9 group-C route pages serve their bodies from the content model with byte-for-byte-unchanged English output; combined with plans 01-03, CNT-01 is complete across all 30 routes; indexable/noindex split preserved.
</success_criteria>

<output>
Create `.planning/phases/71-content-externalization-marketing-seo-pages/71-04-SUMMARY.md` when done.
</output>
