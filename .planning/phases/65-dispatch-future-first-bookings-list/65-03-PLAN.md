---
phase: 65-dispatch-future-first-bookings-list
plan: 03
type: execute
wave: 2
depends_on: ["65-01"]
files_modified:
  - app/api/admin/settings/route.ts
  - tests/admin-settings.test.ts
  - components/admin/DispatchDefault.tsx
  - app/admin/(dashboard)/settings/page.tsx
autonomous: true
requirements: [DISP-02]
estimate:
  tokens: 52000
  raw_tokens: 27000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "PATCH /api/admin/settings persists dispatch_default_horizon and dispatch_horizon_days to pricing_globals id=1 and GET returns them — the setting round-trips and is what the bookings list reads on every subsequent visit (DISP-02, D-03)"
    - "An invalid dispatch_default_horizon (not future/last_n_days/all) is rejected with 400; dispatch_horizon_days is validated as an integer clamped to 1..365 (V5, DoS clamp)"
    - "The PATCH builds its .update() object only from fields present in the parsed body, so a horizon-only PATCH never clobbers notification_flags and vice-versa"
    - "E3 populated: the Dispatch Default widget renders the persisted horizon selected (copper radio dot); the Days field + helper text are visible only when Last N days is the active option"
    - "E3 partial: a cleared/invalid Days value clamps to the persisted/default 7 on blur and the widget never submits an invalid PATCH body"
    - "E3 error: a save failure shows 'Failed to save — try again' and the prior persisted value stays selected; success shows 'Saved' (verbatim reuse of NotificationToggles feedback copy)"
    - "E3 empty/loading/overflow: the widget always loads a persisted default (falls back to Future only if no row), hydrates via the existing NotificationToggles load pattern (inert until hydrated), and the Days helper text wraps to a second line at narrow widths"
  artifacts:
    - app/api/admin/settings/route.ts
    - tests/admin-settings.test.ts
    - components/admin/DispatchDefault.tsx
    - app/admin/(dashboard)/settings/page.tsx
  key_links:
    - "DispatchDefault PATCH → /api/admin/settings → pricing_globals id=1 → GET → settings/page.tsx server fetch → widget props (initialHorizon/initialDays)"
    - "The same GET is read by bookings/page.tsx in Plan 65-04 to seed BookingsTable's persisted default — this plan MUST return both new fields"
---

<objective>
Ship the persistent default-horizon setting (DISP-02, D-03): extend /api/admin/settings
to read/write dispatch_default_horizon + dispatch_horizon_days on pricing_globals (id=1),
and add a Dispatch Default widget to the admin Settings page — a sibling of
NotificationToggles with Future only / Last N days / All options and an editable N (Days).

Purpose: Give the operator a saved horizon that the bookings list applies on every visit
(read by Plan 65-04). Reuses the exact notification_flags settings pattern — no new table,
no new auth surface.
Output: extended settings GET/PATCH + schema, components/admin/DispatchDefault.tsx, mounted
in the Settings page.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-dispatch-future-first-bookings-list/65-CONTEXT.md
@.planning/phases/65-dispatch-future-first-bookings-list/65-RESEARCH.md
@.planning/phases/65-dispatch-future-first-bookings-list/65-PATTERNS.md
@.planning/phases/65-dispatch-future-first-bookings-list/65-UI-SPEC.md
@.planning/phases/65-dispatch-future-first-bookings-list/65-01-SUMMARY.md
@components/admin/NotificationToggles.tsx
@app/api/admin/settings/route.ts
</context>

## Artifacts this phase produces

Phase-wide artifact list is in 65-01-PLAN.md ("Artifacts this phase produces").
**This plan (65-03) produces:** the `dispatch_default_horizon` / `dispatch_horizon_days`
fields on the settings GET response + PATCH schema; the extended `settingsPatchSchema`
(`z.enum(['future','last_n_days','all'])` + `z.number().int().min(1).max(365)`); the new
client component **components/admin/DispatchDefault.tsx**; its mount in
`app/admin/(dashboard)/settings/page.tsx`; and round-trip/validation assertions in
`tests/admin-settings.test.ts`.

<assumption_delta_decision>
The assumption-delta scan flagged the term "choose" ("Admin can **choose** and save a
default horizon…"). This is NOT a smuggled second mechanism — it is exactly DISP-02's
single persisted setting (D-03), stored once in pricing_globals. **Decision: no-change** —
one setting, one storage location; no alternate/parallel horizon store is introduced.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend /api/admin/settings GET + PATCH for the horizon default</name>
  <files>app/api/admin/settings/route.ts, tests/admin-settings.test.ts</files>
  <read_first>
    - app/api/admin/settings/route.ts (whole file: auth-guard order L11-14/L32-34, single-field settingsPatchSchema L7-9, GET select L16-25, PATCH .update L45-51, enforceMaxBody L29)
    - tests/admin-settings.test.ts (existing GET/PATCH test shape + Supabase mock)
    - .planning/phases/65-dispatch-future-first-bookings-list/65-RESEARCH.md (Code Examples: extended settings schema; Security Domain V5 rows)
    - .planning/phases/65-dispatch-future-first-bookings-list/65-PATTERNS.md (settings/route.ts section)
  </read_first>
  <behavior>
    - PATCH { dispatch_default_horizon: 'last_n_days', dispatch_horizon_days: 14 } → 200, and a follow-up GET returns those values
    - PATCH { dispatch_default_horizon: 'future' } (horizon only) → 200 and does NOT overwrite the existing notification_flags
    - PATCH { dispatch_default_horizon: 'bogus' } → 400 (z.enum rejects)
    - PATCH { dispatch_horizon_days: 0 } → 400; PATCH { dispatch_horizon_days: 500 } → 400 (min 1 / max 365)
    - PATCH {} (no recognized field) → 400 (at-least-one-field refine)
    - GET returns notification_flags AND dispatch_default_horizon AND dispatch_horizon_days
  </behavior>
  <action>
    Extend settingsPatchSchema to an object with notification_flags optional (was required),
    dispatch_default_horizon: z.enum(['future','last_n_days','all']).optional(), and
    dispatch_horizon_days: z.number().int().min(1).max(365).optional(), plus a .refine that
    at least one of the three is present (400 otherwise). Keep the auth guard first and
    enforceMaxBody(request, 2_048) unchanged. In GET, extend .select() to
    'notification_flags, dispatch_default_horizon, dispatch_horizon_days' and return all
    three. In PATCH, build the .update() object field-by-field from parsed.data (only
    include keys that are present) so a horizon-only PATCH does not clobber notification_flags
    and a flags-only PATCH does not clobber the horizon. Preserve the 400-on-parse-fail shape
    with issues. Extend tests/admin-settings.test.ts to cover the <behavior> cases.
  </action>
  <verify>
    <automated>npx vitest run tests/admin-settings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - settingsPatchSchema accepts dispatch_default_horizon (enum future/last_n_days/all) and dispatch_horizon_days (int 1..365), both optional, with an at-least-one-field refine
    - GET response JSON includes dispatch_default_horizon and dispatch_horizon_days alongside notification_flags
    - PATCH .update() object is assembled only from present fields (a horizon-only PATCH leaves notification_flags untouched — assert via the mock update payload)
    - Invalid enum → 400; dispatch_horizon_days 0 or 500 → 400
    - `npx vitest run tests/admin-settings.test.ts` passes
  </acceptance_criteria>
  <reversibility rating="costly">Reads/writes the D-03 persisted-setting columns; changing the setting's shape later touches this route + the migration + the list loader.</reversibility>
  <done>The horizon default persists to pricing_globals and round-trips through GET, validated and non-clobbering, behind the admin guard.</done>
</task>

<task type="auto">
  <name>Task 2: Dispatch Default settings widget + mount</name>
  <files>components/admin/DispatchDefault.tsx, app/admin/(dashboard)/settings/page.tsx</files>
  <read_first>
    - components/admin/NotificationToggles.tsx (whole file: 'use client', initial* prop → useState, optimistic PATCH L40-56, card styling L67-83, Saved/Failed feedback copy, revert-on-error, copper/anthracite tokens)
    - app/admin/(dashboard)/settings/page.tsx (server fetch of /api/admin/settings + <NotificationToggles initialFlags={data.notification_flags} /> mount L49)
    - .planning/phases/65-dispatch-future-first-bookings-list/65-UI-SPEC.md (Copywriting Contract for the widget; E3 rows; Color correction — new code uses rgba(191,160,106,0.09) + var(--copper), never the stale rgb(184,115,51))
    - .planning/phases/65-dispatch-future-first-bookings-list/65-PATTERNS.md (Dispatch-horizon Settings widget section)
  </read_first>
  <action>
    Create components/admin/DispatchDefault.tsx ('use client') taking props initialHorizon:
    string and initialDays: number. Render a card matching NotificationToggles (background
    var(--anthracite-mid), border 1px var(--anthracite-light), radius 4, padding 24, an
    uppercase 11px letter-spaced 'DISPATCH DEFAULT' section header in var(--font-montserrat)).
    Three selectable radio-style options labelled exactly 'Future only' / 'Last N days' /
    'All' (active option marked with a copper dot / border using var(--copper) and the
    correct active tint rgba(191,160,106,0.09) — do NOT copy the stale rgba(184,115,51,0.09)
    literal used by older chips). When 'Last N days' is active, show a numeric 'Days' field
    (seeded from initialDays) and the helper text 'Shows bookings from the last N days, plus
    all upcoming trips.' On change, optimistically update local state then PATCH
    /api/admin/settings with only the changed field(s) (dispatch_default_horizon and/or
    dispatch_horizon_days); on success show 'Saved' for ~2s, on failure revert and show
    'Failed to save — try again' (reuse NotificationToggles' feedback pattern verbatim).
    On the Days field blur, if the value is empty/invalid, clamp back to the persisted/default
    7 and do not PATCH an invalid body. Mount <DispatchDefault initialHorizon={data.dispatch_default_horizon}
    initialDays={data.dispatch_horizon_days} /> in settings/page.tsx directly after
    <NotificationToggles>, reading the two fields from the same existing server fetch.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "rgba(191,160,106,0.09)" components/admin/DispatchDefault.tsx && grep -c "DispatchDefault" "app/admin/(dashboard)/settings/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - DispatchDefault.tsx is a 'use client' component with props initialHorizon + initialDays
    - It renders the three exact labels 'Future only', 'Last N days', 'All' and shows the 'Days' field + the D-06 helper text only when 'Last N days' is active
    - The active-state styling uses `rgba(191,160,106,0.09)` and `var(--copper)` (grep count >= 1); it does not introduce the stale `rgb(184,115,51)` literal in this new file
    - On save success it shows 'Saved'; on failure it shows 'Failed to save — try again' and reverts; a blur with empty/invalid Days clamps to 7 without PATCHing
    - settings/page.tsx mounts <DispatchDefault> with initialHorizon/initialDays from the existing /api/admin/settings fetch (grep count >= 1)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>The admin Settings page shows a Dispatch Default widget that persists the horizon choice and editable N, with the correct brand-copper active state and the notification-toggle save UX.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → PATCH /api/admin/settings | Untrusted dispatch_default_horizon / dispatch_horizon_days cross here |
| route → pricing_globals (id=1) | Global settings row updated via service-role client |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-65-02 | Spoofing/Elevation | PATCH /api/admin/settings | high | mitigate | Both GET and PATCH keep the getAdminUser() 401/403 guard first, before any body parse; the new fields sit behind it — no new auth surface |
| T-65-04 | Tampering | dispatch_default_horizon body field | high | mitigate | z.enum(['future','last_n_days','all']) rejects any other value with 400 (Zod); DB CHECK constraint (058) is defense-in-depth |
| T-65-03 | Denial of Service | dispatch_horizon_days body field | medium | mitigate | z.number().int().min(1).max(365) bounds N; enforceMaxBody(request, 2_048) caps body size |
| T-65-07 | Tampering | PATCH partial update | low | mitigate | .update() built only from present fields — a horizon PATCH cannot blank notification_flags (mass-assignment discipline) |
| T-65-SC | Tampering | package installs | low | accept | No packages installed this plan |
</threat_model>

<verification>
- `npx vitest run tests/admin-settings.test.ts` green (round-trip + enum/range 400s + non-clobber)
- `npx tsc --noEmit` passes (widget + page types)
- `npx vitest run` full suite green
- Manual/UAT: open admin Settings, change the Dispatch Default to Last N days = 14, reload — the choice persists
</verification>

<success_criteria>
- Horizon default persists in pricing_globals and round-trips through /api/admin/settings (DISP-02, D-03)
- Invalid horizon/days are rejected (V5); partial PATCH never clobbers notification_flags
- Dispatch Default widget renders with the correct brand-copper active state and NotificationToggles save UX (UI-SPEC E3)
</success_criteria>

<output>
Create `.planning/phases/65-dispatch-future-first-bookings-list/65-03-SUMMARY.md` when done
</output>
