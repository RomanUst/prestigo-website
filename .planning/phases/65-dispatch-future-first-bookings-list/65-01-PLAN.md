---
phase: 65-dispatch-future-first-bookings-list
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/prague-date.ts
  - tests/prague-date.test.ts
  - supabase/migrations/058_pricing_globals_dispatch_horizon.sql
  - supabase/migrations/059_admin_search_bookings_sort.sql
autonomous: false
requirements: [DISP-01, DISP-02]
user_setup:
  - service: supabase (live database)
    why: "Migrations in this repo are applied live by the operator (user runs the SQL against the live Supabase DB); this phase's RPC signature change + new columns are not auto-applied. See Task 3."
    dashboard_config:
      - task: "Apply migrations 058 + 059 to the live database (SQL editor or MCP apply_migration), then confirm bookings.pickup_date comparison + the new RPC signature return rows"
        location: "Supabase Dashboard → SQL Editor (or supabase apply_migration)"
estimate:
  tokens: 55000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "getPragueTodayISO() returns today's Europe/Prague calendar date as YYYY-MM-DD, correct across a UTC-day boundary (e.g. 23:30 UTC in summer resolves to the next Prague day); 'today' is server-computed, never client-derived (D-01)"
    - "shiftIsoDate(iso, days) does whole-day calendar arithmetic on a YYYY-MM-DD string (used for past cutoff today-1 and last-N-days today-N)"
    - "admin_search_bookings accepts a new p_sort text parameter (DEFAULT 'created_desc') and orders by pickup_date ASC when p_sort='pickup_asc', pickup_date DESC when p_sort='pickup_desc', created_at DESC otherwise — with the IDENTICAL CASE expression applied in BOTH the paged CTE ORDER BY and the jsonb_agg ORDER BY (D-02, Pitfall 1)"
    - "The p_sort ORDER BY is a static CASE expression, never string-concatenated dynamic SQL, so it is injection-proof inside the SECURITY DEFINER function (T-65-01)"
    - "pricing_globals gains dispatch_default_horizon (text NOT NULL DEFAULT 'future' CHECK IN future/last_n_days/all) and dispatch_horizon_days (integer NOT NULL DEFAULT 7 CHECK > 0) — the shipped default 'future' makes DISP-01 hold on first load (D-03)"
    - "Migrations 058 and 059 are applied LIVE by the operator (blocking hand-off), and the new 8-arg admin_search_bookings signature is re-GRANTed EXECUTE to service_role only (not PUBLIC/anon/authenticated, per 057)"
  artifacts:
    - lib/prague-date.ts
    - tests/prague-date.test.ts
    - supabase/migrations/058_pricing_globals_dispatch_horizon.sql
    - supabase/migrations/059_admin_search_bookings_sort.sql
  key_links:
    - "059 DROP FUNCTION must target the CURRENT live 7-arg signature (054's, with p_status), then CREATE the new 8-arg signature, then re-GRANT EXECUTE to service_role — omitting the re-GRANT breaks the admin service-role .rpc() call"
    - "058's DEFAULT 'future' is load-bearing for DISP-01 — without it the list would not default to future on a fresh row"
---

<objective>
Lay the schema + server-helper foundation for the future-first dispatch list: a
server-only Europe/Prague "today" helper (D-01), the pricing_globals persistence
columns for the default horizon (D-03), and the admin_search_bookings adaptive-sort
parameter (D-02). Migrations are applied live by the operator as a blocking hand-off.

This is the hard prerequisite wave: the end-to-end future-first tracer (Plan 65-02)
and the settings backend (Plan 65-03) both depend on the live schema landing here.

Purpose: Establish the data + time-boundary primitives every later plan calls, behind
the unavoidable live-migration operator gate, so downstream plans verify against a real
applied schema (build/type checks pass without the apply — a false-positive state — so
the apply is [BLOCKING]).
Output: lib/prague-date.ts (+ test), migrations 058 (columns) + 059 (RPC p_sort), applied live.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-dispatch-future-first-bookings-list/65-CONTEXT.md
@.planning/phases/65-dispatch-future-first-bookings-list/65-RESEARCH.md
@.planning/phases/65-dispatch-future-first-bookings-list/65-PATTERNS.md
@supabase/migrations/054_admin_search_bookings_status_filter.sql
@supabase/migrations/057_security_rls_hardening.sql
</context>

## Artifacts this phase produces

This phase (65) as a whole creates: migrations **058** (pricing_globals columns
`dispatch_default_horizon`, `dispatch_horizon_days`) and **059** (admin_search_bookings
`p_sort` param); helper **lib/prague-date.ts** (`getPragueTodayISO`, `shiftIsoDate`); the
GET `/api/admin/bookings` query params **`horizon`** (`future`/`past`/`all`/`last_n_days`)
and **`horizonDays`**; the settings fields `dispatch_default_horizon` / `dispatch_horizon_days`;
the new Settings widget **components/admin/DispatchDefault.tsx**; the BookingsTable
**Future/Past/All** segmented control; and tests `tests/prague-date.test.ts`,
`tests/admin-bookings-kpi-decoupling.test.tsx` (plus extensions to
`tests/admin-bookings.test.ts`, `tests/admin-settings.test.ts`, `tests/BookingsTable.test.ts`).

**This plan (65-01) produces:** `lib/prague-date.ts` (`getPragueTodayISO`, `shiftIsoDate`),
`tests/prague-date.test.ts`, migration `058_pricing_globals_dispatch_horizon.sql`
(columns `dispatch_default_horizon`, `dispatch_horizon_days`), migration
`059_admin_search_bookings_sort.sql` (param `p_sort`).

<reversibility_flags>
Per REVERSIBILITY_GATES: no one-way-door decisions in this phase, so no
`checkpoint:decision` is inserted. Two **costly** (reversible-but-expensive) items are
implemented in this plan and flagged, not gated:
- **D-02 (adaptive sort)** — Task 2 (059) changes the admin_search_bookings RPC signature
  (a live DB migration). Reversible by re-issuing the DROP/CREATE, but not free.
- **D-03 (persisted setting schema)** — Task 2 (058) adds two typed columns to
  pricing_globals read by the list on every visit; changing the shape later touches a
  migration + the settings API + the list loader.
</reversibility_flags>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Server-only Europe/Prague "today" helper</name>
  <files>lib/prague-date.ts, tests/prague-date.test.ts</files>
  <read_first>
    - .planning/phases/65-dispatch-future-first-bookings-list/65-RESEARCH.md (Pattern 1: server-computed today; Don't Hand-Roll — use built-in Intl, no date-fns-tz/luxon)
    - .planning/phases/65-dispatch-future-first-bookings-list/65-PATTERNS.md (lib/prague-date.ts section)
    - tests/admin-settings.test.ts (vi.hoisted Vitest conventions to mirror)
    - app/admin/(dashboard)/bookings/page.tsx (the inline UTC-anchored getMonday/toISOString().split('T')[0] this replaces server-side — do NOT reuse its client UTC approach)
  </read_first>
  <behavior>
    - getPragueTodayISO(new Date('2026-08-28T23:30:00Z')) === '2026-08-29' (summer CEST: 23:30 UTC is already next day in Prague)
    - getPragueTodayISO(new Date('2026-01-15T22:00:00Z')) === '2026-01-15' (winter CET: 22:00 UTC is still same day in Prague; boundary is 23:00 UTC)
    - getPragueTodayISO() with no arg uses new Date() (current time) and returns a YYYY-MM-DD string
    - shiftIsoDate('2026-08-29', -1) === '2026-08-28'; shiftIsoDate('2026-03-01', -7) === '2026-02-22' (whole-day arithmetic, month/DST-boundary safe because it operates on the calendar-date string anchored at UTC midnight)
  </behavior>
  <action>
    Create lib/prague-date.ts exporting getPragueTodayISO(now: Date = new Date()): string
    and shiftIsoDate(iso: string, days: number): string. getPragueTodayISO formats via
    Intl.DateTimeFormat('en-CA', { timeZone: 'Europe/Prague' }) — en-CA yields YYYY-MM-DD
    directly and Node's bundled ICU handles Prague CET/CEST DST correctly (zero new
    dependencies; do NOT add date-fns-tz or luxon). shiftIsoDate parses `${iso}T00:00:00Z`,
    applies setUTCDate(getUTCDate()+days), returns the date portion. This helper is
    server-only — it exists so route handlers compute the cutoff, honoring D-01
    ("today is server-computed, not client-derived"). Write tests/prague-date.test.ts
    FIRST (RED) covering the UTC-day-boundary and shift cases in behavior, then implement
    (GREEN).
  </action>
  <verify>
    <automated>npx vitest run tests/prague-date.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/prague-date.test.ts asserts getPragueTodayISO(new Date('2026-08-28T23:30:00Z')) === '2026-08-29' and a winter same-day case, and both shiftIsoDate cases in <behavior>
    - `npx vitest run tests/prague-date.test.ts` passes
    - `grep -c "date-fns-tz\|luxon" lib/prague-date.ts` returns 0 (no new date library imported)
    - `grep -c "Intl.DateTimeFormat('en-CA'" lib/prague-date.ts` returns 1
  </acceptance_criteria>
  <done>The helper returns correct Prague dates across DST/UTC boundaries with zero new dependencies, proven by a passing pure-unit test.</done>
</task>

<task type="auto">
  <name>Task 2: Migrations 058 (pricing_globals columns) + 059 (admin_search_bookings p_sort)</name>
  <files>supabase/migrations/058_pricing_globals_dispatch_horizon.sql, supabase/migrations/059_admin_search_bookings_sort.sql</files>
  <read_first>
    - supabase/migrations/054_admin_search_bookings_status_filter.sql (the CURRENT live RPC body — copy verbatim, change only the sort; note the two ORDER BY sites at the paged CTE and the jsonb_agg)
    - supabase/migrations/057_security_rls_hardening.sql (REVOKE history — PUBLIC/anon/authenticated already revoked; re-GRANT service_role ONLY)
    - .planning/phases/65-dispatch-future-first-bookings-list/65-RESEARCH.md (Code Examples: migration 058 shape + migration 059 full body; Pitfall 1 two-ORDER-BY sync; Pitfall 3 pickup_date type caveat)
    - .planning/phases/65-dispatch-future-first-bookings-list/65-PATTERNS.md (059/058 migration sections; TEXT+CHECK-over-ENUM precedent)
  </read_first>
  <action>
    Write 058_pricing_globals_dispatch_horizon.sql: ALTER TABLE public.pricing_globals ADD
    COLUMN dispatch_default_horizon text NOT NULL DEFAULT 'future' CHECK
    (dispatch_default_horizon IN ('future','last_n_days','all')), ADD COLUMN
    dispatch_horizon_days integer NOT NULL DEFAULT 7 CHECK (dispatch_horizon_days > 0).
    Use TEXT+CHECK (not a Postgres ENUM) per the customer_profiles.account_type precedent.
    The DEFAULT 'future' is load-bearing for DISP-01/D-03.

    Write 059_admin_search_bookings_sort.sql: DROP FUNCTION IF EXISTS
    public.admin_search_bookings(text, text, text, text, text, integer, integer) — the
    current live 7-arg signature (054's, p_status included). CREATE OR REPLACE the function
    with a new p_sort text DEFAULT 'created_desc' parameter inserted before p_offset
    (new 8-arg signature), keeping SECURITY DEFINER + SET search_path TO 'public','pg_temp'
    + the p_limit clamp + search/pagination/{rows,total_count} shape byte-for-byte from 054.
    Replace BOTH ORDER BY sites (the paged CTE and the jsonb_agg) with the IDENTICAL static
    CASE ladder: order by pickup_date ASC when p_sort='pickup_asc', pickup_date DESC when
    p_sort='pickup_desc', with created_at DESC as the final tiebreak/default. Do NOT build
    the ORDER BY with string-concatenated dynamic SQL. Finish with GRANT EXECUTE ON FUNCTION
    public.admin_search_bookings(text, text, text, text, text, text, integer, integer) TO
    service_role — re-GRANT the new 8-arg signature to service_role ONLY (do not re-grant
    PUBLIC/anon/authenticated). Add a header comment documenting these are applied live by
    the operator (Task 3).
  </action>
  <verify>
    <automated>test -f supabase/migrations/058_pricing_globals_dispatch_horizon.sql && test -f supabase/migrations/059_admin_search_bookings_sort.sql && grep -cE "dispatch_default_horizon|dispatch_horizon_days" supabase/migrations/058_pricing_globals_dispatch_horizon.sql && [ "$(grep -c "CASE WHEN p_sort = 'pickup_asc'" supabase/migrations/059_admin_search_bookings_sort.sql)" -ge 2 ]</automated>
  </verify>
  <acceptance_criteria>
    - 058 contains `dispatch_default_horizon text NOT NULL DEFAULT 'future'` with `CHECK (dispatch_default_horizon IN ('future', 'last_n_days', 'all'))`
    - 058 contains `dispatch_horizon_days integer NOT NULL DEFAULT 7` with `CHECK (dispatch_horizon_days > 0)`
    - 059 contains exactly one `DROP FUNCTION IF EXISTS public.admin_search_bookings(text, text, text, text, text, integer, integer)` (7-arg drop)
    - 059 CREATE signature is the 8-arg form ending `p_sort text DEFAULT 'created_desc'::text, p_offset integer DEFAULT 0, p_limit integer DEFAULT 20` (p_sort inserted before p_offset)
    - `grep -c "CASE WHEN p_sort = 'pickup_asc'" 059...sql` is >= 2 (present in both the paged CTE ORDER BY and the jsonb_agg ORDER BY)
    - 059 ends with `GRANT EXECUTE ON FUNCTION public.admin_search_bookings(text, text, text, text, text, text, integer, integer) TO service_role;` and grants to no other role
    - Neither migration contains a `|| p_sort` or `EXECUTE '` dynamic-SQL ORDER BY construction
  </acceptance_criteria>
  <reversibility rating="costly">Both migrations are reversible by re-issuing DROP/CREATE and DROP COLUMN, but each is a live DB change (RPC signature / persisted-setting shape) — flagged per D-02, D-03.</reversibility>
  <done>Both migration files exist with the exact column shapes, the 8-arg RPC with a static-CASE adaptive sort in both ORDER BY sites, the 7-arg DROP, and a service_role-only re-GRANT.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: [BLOCKING] Operator applies migrations 058 + 059 live</name>
  <read_first>
    - supabase/migrations/058_pricing_globals_dispatch_horizon.sql (present the exact SQL to apply)
    - supabase/migrations/059_admin_search_bookings_sort.sql (present the exact SQL to apply)
    - .planning/phases/65-dispatch-future-first-bookings-list/65-RESEARCH.md (Pitfall 3 / Open Question 3: confirm live bookings.pickup_date column type at apply time)
    - .planning/codebase/CONVENTIONS.md (migrations are applied live by the operator, never auto-pushed)
  </read_first>
  <precondition>Live Supabase database is reachable and the operator can run SQL against it (SQL Editor or MCP apply_migration). Next free migration number on disk is 058 (057 is latest).</precondition>
  <what-built>
    Two migration files (from Task 2): 058_pricing_globals_dispatch_horizon.sql (adds
    dispatch_default_horizon + dispatch_horizon_days columns to pricing_globals) and
    059_admin_search_bookings_sort.sql (DROP the 7-arg admin_search_bookings, CREATE the
    8-arg form with a static-CASE p_sort, re-GRANT EXECUTE to service_role). This repo
    applies migrations LIVE by the operator — nothing has been pushed automatically. Build
    and type checks pass WITHOUT this apply (types come from config, not the live DB), so
    verification would false-positive until the SQL actually lands.
  </what-built>
  <how-to-verify>
    Apply the two files to the live Supabase database, in order 058 then 059 (SQL Editor or
    MCP apply_migration). 059 is an RPC signature change: it must DROP the old 7-arg function,
    CREATE the new 8-arg one, and re-GRANT EXECUTE to service_role (already in the file).
    Then confirm, at apply time:
    1. `select dispatch_default_horizon, dispatch_horizon_days from pricing_globals where id = 1;`
       returns 'future' and 7.
    2. `\d bookings` (or an information_schema.columns query) — the pickup_date >= text
       comparison in the RPC works, i.e. NO `operator does not exist: date >= text` error
       (Pitfall 3 — confirm pickup_date's live type).
    3. Smoke: `select * from admin_search_bookings(p_sort := 'pickup_asc', p_limit := 1);`
       returns without error.
    Do NOT start Plan 65-02 / 65-03 verification until all three pass.
  </how-to-verify>
  <resume-signal>Type "applied" (or "approved") once 058 + 059 are live and the three checks above pass; otherwise paste the SQL error so the migration can be corrected.</resume-signal>
  <acceptance_criteria>
    - Operator has run 058 then 059 against the live DB and reported success
    - pricing_globals id=1 row returns dispatch_default_horizon='future' and dispatch_horizon_days=7
    - admin_search_bookings accepts a p_sort argument and is EXECUTE-granted to service_role
    - No `date >= text` operator error surfaced (pickup_date type confirmed compatible)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service_role → admin_search_bookings RPC | The SECURITY DEFINER function runs with elevated privilege; its sort clause must not be attacker-influenced |
| operator → live DB | Manual SQL apply of a signature-changing migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-65-01 | Tampering | admin_search_bookings ORDER BY (059) | high | mitigate | Static CASE-expression ORDER BY only; never string-concatenated/dynamic SQL — injection-proof by construction inside SECURITY DEFINER regardless of upstream whitelisting |
| T-65-02 | Elevation of Privilege | admin_search_bookings GRANT | high | mitigate | Re-GRANT EXECUTE to service_role ONLY on the new 8-arg signature; PUBLIC/anon/authenticated stay revoked (per 057) so the RPC is not reachable unauthenticated |
| T-65-06 | Tampering | pricing_globals horizon columns | low | mitigate | DB-level CHECK constraints bound dispatch_default_horizon to the 3-value set and dispatch_horizon_days > 0 |
| T-65-SC | Tampering | package installs | low | accept | This phase installs NO packages (no npm/pip/cargo) — nothing to audit; getPragueTodayISO uses built-in Intl |
</threat_model>

<verification>
- `npx vitest run tests/prague-date.test.ts` green (helper unit)
- Migration files present with the exact column/param/GRANT shapes (Task 2 automated grep)
- Operator confirms live apply of 058 + 059 (Task 3 blocking human-verify)
- `npx vitest run` full suite stays green (no regression from the new helper file)
</verification>

<success_criteria>
- lib/prague-date.ts computes Prague "today" correctly across DST/UTC boundaries, server-side, zero new deps (D-01)
- admin_search_bookings has an adaptive static-CASE p_sort in both ORDER BY sites, re-GRANTed to service_role (D-02)
- pricing_globals has the two dispatch-horizon columns with the shipped 'future' default (D-03)
- Migrations applied live and confirmed by the operator (blocking gate cleared)
</success_criteria>

<output>
Create `.planning/phases/65-dispatch-future-first-bookings-list/65-01-SUMMARY.md` when done
</output>
