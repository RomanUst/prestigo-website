---
phase: 70-string-externalization-booking-account
plan: 06
type: execute
wave: 6
depends_on: ["70-04"]
files_modified:
  - components/booking/steps/Step1TripType.tsx
  - components/booking/steps/Step2DateTime.tsx
  - components/booking/steps/Step5Passenger.tsx
  - components/booking/steps/Step6Payment.tsx
  - messages/en.json
  - messages/ru.json
  - messages/es.json
  - messages/fr.json
  - messages/ar.json
  - messages/hi.json
  - messages/zh.json
  - tests/Step1TripType.test.tsx
  - tests/Step2DateTime.test.tsx
  - tests/Step5Passenger.test.tsx
  - tests/Step6Payment.test.tsx
autonomous: true
requirements: [STR-02]
user_setup: []

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Step1TripType renders swap/pickup/destination copy from Booking.step1"
    - "Step2DateTime renders its date/time column copy from Booking.step2, and the lead-time notice uses t.rich('leadTimeNotice', {wa, hours}) with the WhatsApp link as a chunk callback and {hours} as a named arg"
    - "Step5Passenger's zod schema messages are sourced from useTranslations at component-body scope (schema built in the body, not module scope) with validation RULES unchanged; the flight-status block uses named-ICU (e.g. airport mismatch)"
    - "Step6Payment renders its payment-UI copy from Booking.step6, its vehicle-class label reuses Booking.vehicleClasses, and the Stripe Elements locale option stays literal 'en'"
    - "Each touched test runs under renderWithIntl and stays green; English byte-for-byte unchanged; 6 stubs in sync"
  artifacts:
    - path: messages/en.json
      provides: "Booking.step1 / step2 (leadTimeNotice t.rich) / step5 (validation+flight) / step6"
      contains: "\"leadTimeNotice\""
    - path: components/booking/steps/Step2DateTime.tsx
      provides: "lead-time notice via t.rich with WhatsApp chunk"
      contains: "t.rich('leadTimeNotice'"
  key_links:
    - from: components/booking/steps/Step5Passenger.tsx
      to: messages/en.json
      via: "zod schema messages read from useTranslations('Booking.validation'/'Booking.step5') at component-body scope"
      pattern: "useTranslations\\('Booking\\.(validation|step5)'\\)"
    - from: components/booking/steps/Step6Payment.tsx
      to: messages/en.json
      via: "vehicle-class label reuses Booking.vehicleClasses; Stripe locale stays 'en'"
      pattern: "vehicleClasses"
  prohibitions:
    - statement: "Do NOT change the Stripe Elements locale option ('en') in Step6Payment — it is a separate i18n system out of scope"
      status: prohibited
      verification: "grep -c \"locale: 'en'\" components/booking/steps/Step6Payment.tsx returns >= 1 (unchanged)"
    - statement: "Do NOT change any zod validation RULE (.min/.email/.refine args other than the message string)"
      status: prohibited
      verification: "diff shows only the message argument changed; .min/.email/.refine predicates unchanged"
    - statement: "Do NOT re-add a local vehicle-class label map in Step6Payment; reuse Booking.vehicleClasses"
      status: prohibited
      verification: "grep -rn \"First Class\" components/booking/steps/Step6Payment.tsx returns 0"
---

<objective>
Externalize the four remaining wizard steps: `Step1TripType` (swap/pickup/destination copy), `Step2DateTime` (date/time columns + the lead-time notice with an embedded WhatsApp link, via `t.rich` — PATTERNS Pattern D), `Step5Passenger` (zod schema messages moved to the component body + the flight-check status block via named-ICU), and `Step6Payment` (payment-UI copy + reuse of `Booking.vehicleClasses`, with the Stripe Elements `locale: 'en'` left untouched — RESEARCH Pitfall 7). Step5 extends the shared `Booking.validation` namespace seeded by the tracer.

Purpose: These four steps carry the phase's richest interaction copy (rich-text link, zod validation, interpolated flight status). Handling them together keeps the validation namespace coherent and isolates the Stripe-locale boundary.

Output: `Booking.step1`, `Booking.step2` (leadTimeNotice), extended `Booking.validation` + `Booking.step5`, `Booking.step6`; four converted steps; migrated tests.
</objective>

<assumption_delta_decision>
- Assumption (Pattern 4/Code Example): Step5's zod schema currently sits at module scope; since messages now depend on `t`, the schema is constructed in the component body (a `useTranslations` hook result), which is safe because `useTranslations` is a hook. Validation RULES are identical; only the message argument changes.
- Assumption (Pattern D): Step2's lead-time notice embeds a WhatsApp `<a href="https://wa.me/...">` link; it becomes `t.rich('leadTimeNotice', {wa: chunks => <a ...>{chunks}</a>, hours})` with `{hours}` = MIN_LEAD_HOURS. The href is a constant kept in code, not in the catalog.
- Assumption (ENCODING probe): all copy and interpolated flight-status text is byte-for-byte unchanged; named-ICU args (`{airport}`, `{hours}`) match the existing catalog interpolation shape.
- Assumption (Pitfall 7): Stripe Elements' `locale: 'en'` is a separate i18n system and stays literal — asserted by a positive grep.
- Assumption (empty/plural probe): no genuinely plural-sensitive sentence found in these steps; if Task 2/3 finds one, use ICU `{count, plural, ...}` rather than a ternary.
- Depends on 70-04 (Booking.vehicleClasses) and 70-01 (Booking.validation) — both earlier waves. No checkpoint (convention locked).
</assumption_delta_decision>

<ai_integration>
No external API integration in this plan. Step5's flight-check calls a pre-existing endpoint whose request/response is unchanged; only the displayed status *strings* move. Step6's Stripe Elements integration is untouched. (schema-gate: no DB schema change.)
</ai_integration>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-string-externalization-booking-account/70-RESEARCH.md
@.planning/phases/70-string-externalization-booking-account/70-PATTERNS.md

# Analogs:
@components/Nav.tsx
@components/CookieBanner.tsx
@messages/en.json
@tests/nav-auth.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Step1TripType + Step2DateTime (t.rich lead-time notice)</name>
  <files>components/booking/steps/Step1TripType.tsx, components/booking/steps/Step2DateTime.tsx, messages/en.json</files>
  <read_first>
    - components/booking/steps/Step1TripType.tsx (~12 strings: swap-origin/destination aria, pickup/destination placeholders)
    - components/booking/steps/Step2DateTime.tsx (date/time labels; lead-time notice at ~lines 300-310 with the WhatsApp `<a href="https://wa.me/...">` link and `MIN_LEAD_HOURS`)
    - components/CookieBanner.tsx lines 168-188 (the `t.rich` Privacy/Terms chunk-callback pattern — Pattern D)
    - messages/en.json (existing named-ICU entries; Booking.validation from the tracer)
  </read_first>
  <action>
    Add `useTranslations('Booking.step1')` to `Step1TripType.tsx` and replace every placeholder/aria/label with `t('<key>')`; add `Booking.step1` to the catalog. Add `useTranslations('Booking.step2')` to `Step2DateTime.tsx`, replace the date/time column labels and the "Select a pickup date to continue" copy with `t('<key>')`, and convert the lead-time notice to `t.rich('leadTimeNotice', { wa: (chunks) => <a href="https://wa.me/...">{chunks}</a>, hours: MIN_LEAD_HOURS })` — keep the href constant in code. Add `Booking.step2` with a `leadTimeNotice` value using `{hours}` and a `<wa>…</wa>` chunk tag, copying the current wording byte-for-byte. All other Step2 copy moves to plain `t('<key>')`.
  </action>
  <verify>
    <automated>node -e "const m=require('./messages/en.json'); if(!m.Booking.step1||!m.Booking.step2||!m.Booking.step2.leadTimeNotice) throw new Error('missing step1/step2/leadTimeNotice'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslations('Booking.step1')" components/booking/steps/Step1TripType.tsx` returns >= 1
    - `grep -c "t.rich('leadTimeNotice'" components/booking/steps/Step2DateTime.tsx` returns >= 1
    - `node -e "const m=require('./messages/en.json'); if(!/\{hours\}/.test(m.Booking.step2.leadTimeNotice)||!/<wa>/.test(m.Booking.step2.leadTimeNotice)) throw new Error('leadTimeNotice not rich/ICU'); console.log('ok')"` exits 0
  </acceptance_criteria>
  <done>Step1 and Step2 render from the catalog; the lead-time notice uses t.rich with the WhatsApp chunk and {hours}.</done>
</task>

<task type="auto">
  <name>Task 2: Step5Passenger (zod messages + flight status) + Step6Payment (Stripe locale untouched)</name>
  <files>components/booking/steps/Step5Passenger.tsx, components/booking/steps/Step6Payment.tsx, messages/en.json</files>
  <read_first>
    - components/booking/steps/Step5Passenger.tsx (~25 strings: zod schema at ~32-43, flight-check status block with interpolated lines, CHECK/RE-CHECK/CHECKING states)
    - components/booking/steps/Step6Payment.tsx (SECURE PAYMENT, promo code UI, Remove; Stripe `options` with `locale: 'en'` at ~370-383; local vehicle-class label map at ~16-17)
    - 70-RESEARCH.md lines 395-409 (zod-message code example) and 342-345 (Pitfall 7 Stripe locale)
    - messages/en.json (Booking.validation from tracer; Booking.vehicleClasses from 70-04)
  </read_first>
  <action>
    In `Step5Passenger.tsx`: add `const t = useTranslations('Booking.step5')` and `const tv = useTranslations('Booking.validation')` at component-body scope; move the zod schema construction into the component body (it is currently module-scope) and pass `tv('<key>')` as the message argument to each `.min()`/`.email()`/`.refine()` — leaving every predicate/rule identical. Reuse existing `Booking.validation` keys where the message matches; add new validation keys (e.g. `firstNameRequired`, `invalidEmail`, `invalidFlightFormat`) for Step5-specific messages. Externalize the flight-status block via named-ICU (e.g. `t('airportMismatch', {airport})`) and the CHECK/RE-CHECK/CHECKING button states via `t('<key>')`. In `Step6Payment.tsx`: add `useTranslations('Booking.step6')`, externalize SECURE PAYMENT / promo-code / Remove copy, remove the local vehicle-class label map and source it from `Booking.vehicleClasses`, and DO NOT touch the Stripe `options` object — the `locale: 'en'` line stays exactly as-is. Add `Booking.step5` and `Booking.step6` namespaces copying the current literals byte-for-byte.
  </action>
  <verify>
    <automated>node -e "const m=require('./messages/en.json'); if(!m.Booking.step5||!m.Booking.step6) throw new Error('missing step5/step6'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "useTranslations\('Booking\.(validation|step5)'\)" components/booking/steps/Step5Passenger.tsx` returns >= 1
    - `grep -c "locale: 'en'" components/booking/steps/Step6Payment.tsx` returns >= 1 (Stripe locale unchanged)
    - `grep -rn "First Class" components/booking/steps/Step6Payment.tsx` returns 0
    - `grep -c "useTranslations('Booking.step6')" components/booking/steps/Step6Payment.tsx` returns >= 1
  </acceptance_criteria>
  <done>Step5 sources zod messages + flight status from the catalog (rules unchanged); Step6 renders payment copy from the catalog with Stripe locale untouched and class label reusing vehicleClasses.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate the 4 touched tests to renderWithIntl, re-sync stubs, run the slice</name>
  <files>tests/Step1TripType.test.tsx, tests/Step2DateTime.test.tsx, tests/Step5Passenger.test.tsx, tests/Step6Payment.test.tsx, messages/ru.json, messages/es.json, messages/fr.json, messages/ar.json, messages/hi.json, messages/zh.json</files>
  <read_first>
    - tests/Step1TripType.test.tsx, tests/Step2DateTime.test.tsx, tests/Step5Passenger.test.tsx, tests/Step6Payment.test.tsx (the files being modified — bare `render` imports)
    - tests/nav-auth.test.tsx lines 1-15 (aliased renderWithIntl import)
  </read_first>
  <action>
    Swap the bare-`render` import for the aliased `renderWithIntl as render` in each of the 4 test files, leaving call sites and assertions unchanged. Re-sync the 6 stub locales (`cp messages/en.json messages/{ru,es,fr,ar,hi,zh}.json`) and run the touched-test slice.
  </action>
  <verify>
    <automated>for l in ru es fr ar hi zh; do cmp -s messages/en.json messages/$l.json || { echo "DRIFT: $l"; exit 1; }; done; npx vitest run tests/Step1TripType.test.tsx tests/Step2DateTime.test.tsx tests/Step5Passenger.test.tsx tests/Step6Payment.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - each of the 4 test files: `grep -c "renderWithIntl" <file>` returns >= 1
    - `npx vitest run` over the 4 files exits 0
    - all 6 `cmp -s` stub checks succeed
  </acceptance_criteria>
  <done>The 4 step tests are green under renderWithIntl; stubs in sync.</done>
</task>

</tasks>

<artifacts_produced>
**New/extended catalog namespaces:** `Booking.step1`, `Booking.step2` (incl. `leadTimeNotice` t.rich), `Booking.validation` (extended with Step5 zod keys), `Booking.step5` (flight status), `Booking.step6`
**Modified source:** Step1TripType, Step2DateTime, Step5Passenger, Step6Payment (vehicle-label map removed → reuse Booking.vehicleClasses)
**Modified tests:** Step1TripType, Step2DateTime, Step5Passenger, Step6Payment
**Untouched by design:** Stripe Elements `locale: 'en'` in Step6Payment
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog → zod messages | Validation messages are display copy; the validation RULES remain the trust control |
| Step6 → Stripe Elements | Stripe's own i18n (locale 'en') is a separate system; not routed through next-intl |
| t.rich chunk → markup | The lead-time notice renders a WhatsApp `<a>` via a chunk callback; href is a constant in code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-06-01 | Tampering | Step5 zod validation | medium | mitigate | Only the message argument changes; `.min/.email/.refine` predicates are asserted unchanged in diff review, so input validation strength is preserved |
| T-70-06-02 | Tampering | Step2 t.rich WhatsApp link | low | mitigate | The href is a code constant, not a catalog value, so a catalog edit cannot redirect the link; the chunk callback renders only the anchor text |
| T-70-06-03 | Information Disclosure | Step6 payment copy | low | mitigate | Only static payment-UI labels move; no card/PII/Stripe key enters the catalog; Stripe Elements chrome remains Stripe-controlled |
</threat_model>

<verification>
- `npx vitest run` over the 4 touched tests green
- `t.rich('leadTimeNotice'` present with `{hours}` + `<wa>` in the catalog; Stripe `locale: 'en'` grep unchanged; zod rules unchanged
- residual vehicle-label grep returns 0 in Step6Payment
- 6 stubs byte-identical to en.json
- (wave-merge gate) `npx vitest run` full suite green
</verification>

<success_criteria>
All four wizard steps render every string from the catalog — with the lead-time notice as t.rich, zod messages sourced at component scope with rules intact, flight status via named-ICU, and the Stripe locale + vehicle-class single-source respected — tests green under renderWithIntl, English byte-for-byte unchanged, locales in sync.
</success_criteria>

<output>
Create `.planning/phases/70-string-externalization-booking-account/70-06-SUMMARY.md` when done.
</output>
