---
phase: 70-string-externalization-booking-account
plan: 08
type: execute
wave: 8
depends_on: ["70-04", "70-07"]
files_modified:
  - app/[locale]/account/page.tsx
  - app/[locale]/account/trips/page.tsx
  - app/[locale]/account/profile/page.tsx
  - components/account/ProfileForm.tsx
  - app/[locale]/account/actions.ts
  - app/[locale]/account/reset-password/page.tsx
  - messages/en.json
  - messages/ru.json
  - messages/es.json
  - messages/fr.json
  - messages/ar.json
  - messages/hi.json
  - messages/zh.json
  - tests/account-trips.test.tsx
  - tests/profile-actions.test.ts
  - tests/passenger-actions.test.ts
autonomous: true
requirements: [STR-02]
user_setup: []

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "account/page.tsx and account/trips/page.tsx (Server Components) render all strings via await getTranslations('Account.*'); the 'Signed in as {email}' line uses named-ICU"
    - "account/trips vehicle-class labels reuse Booking.vehicleClasses; its status-label map keeps EXACTLY the 4 existing keys with the same ?? pending fallback (the pre-existing gap is preserved, not expanded); the en-GB date format is untouched"
    - "ProfileForm renders contact/corporate/passenger-editor strings from Account.profile; its useActionState calls bind the locale (.bind(null, locale)); account/actions.ts returns localized errors via getTranslations({namespace:'Errors', locale}) reusing the shared Errors keys plus account-specific keys"
    - "reset-password/page.tsx renders from the catalog"
    - "PHASE GATE: full npx vitest run is green AND npm run build succeeds for all 7 locale subpaths with no MISSING_MESSAGE AND no hardcoded vehicle-class display-label literal remains anywhere under components/booking or app/[locale]/account"
  artifacts:
    - path: messages/en.json
      provides: "Account.dashboard / trips (statusLabels) / profile + extended Errors keys"
      contains: "\"dashboard\""
    - path: app/[locale]/account/actions.ts
      provides: "locale-threaded account Server Actions reusing Errors"
      contains: "getTranslations({ namespace: 'Errors'"
    - path: components/account/ProfileForm.tsx
      provides: "profile UI externalized + locale bound into useActionState"
      contains: "useLocale()"
  key_links:
    - from: components/account/ProfileForm.tsx
      to: app/[locale]/account/actions.ts
      via: "useActionState(action.bind(null, locale), null) threads the locale into the account mutations"
      pattern: "\\.bind\\(null, locale\\)"
    - from: app/[locale]/account/trips/page.tsx
      to: messages/en.json
      via: "getTranslations reuses Booking.vehicleClasses for class labels; Account.trips.statusLabels for status"
      pattern: "vehicleClasses"
  prohibitions:
    - statement: "Do NOT expand account/trips STATUS_STYLES from 4 keys to 8 — preserve the pre-existing fallback gap (byte-for-byte EN behavior)"
      status: prohibited
      verification: "node check: Account.trips.statusLabels has exactly 4 keys; the ?? pending fallback remains"
    - statement: "Do NOT change the en-GB toLocaleDateString format in trips/page.tsx (date formatting is L10N-PRICE, deferred)"
      status: prohibited
      verification: "grep -c \"'en-GB'\" app/[locale]/account/trips/page.tsx returns >= 1 (unchanged)"
    - statement: "Do NOT touch the ownership checks (user_id from getUser()) or any control flow in account/actions.ts — only error string values change"
      status: prohibited
      verification: "diff of account/actions.ts touches only string literals + the added locale param + getTranslations call"
    - statement: "Do NOT call useTranslations in account/actions.ts ('use server')"
      status: prohibited
      verification: "grep -c useTranslations app/[locale]/account/actions.ts returns 0"
    - statement: "Do NOT re-add a local vehicle-class label map; reuse Booking.vehicleClasses"
      status: prohibited
      verification: "grep -rn \"First Class\" app/[locale]/account/trips/page.tsx returns 0"
---

<objective>
Externalize the account + auth account surface and run the Phase 70 gate. Server Components `account/page.tsx` and `account/trips/page.tsx` move to `await getTranslations('Account.*')` (dashboard cards, the "Signed in as {email}" named-ICU line, the trips empty-state + per-trip labels). `account/trips` reuses `Booking.vehicleClasses` for class labels, preserves its pre-existing 4-key status map with the same `?? pending` fallback, and leaves the `en-GB` date format alone. `ProfileForm.tsx` (contact/corporate/passenger-editor) externalizes to `Account.profile` and binds the locale into its `useActionState` calls; `account/actions.ts` returns localized errors via `getTranslations({namespace:'Errors', locale})`, reusing the shared `Errors` keys from 70-02 plus account-specific keys. `reset-password/page.tsx` externalizes too. `profile/page.tsx` is pure composition (0 strings) and needs no change beyond confirmation.

Purpose: Completes STR-02 (Success Criteria 2 + 3) and runs the phase gate — full test suite, 7-locale build, and the phase-wide single-source vehicle-label grep — proving the whole booking + account + auth surface renders from the catalog with English byte-for-byte unchanged.

Output: `Account.dashboard`, `Account.trips` (statusLabels), `Account.profile`, extended `Errors`; six converted account/auth files; extended tests; green phase gate.
</objective>

<assumption_delta_decision>
- Assumption (Pitfall 8): `account/trips` `STATUS_STYLES` only defines 4 of 8 real statuses; the other four fall back to `pending`. This plan externalizes exactly those 4 keys with the same fallback — preserving the byte-for-byte EN behavior (Success Criterion 4). The gap is a pre-existing bug flagged for a future phase, NOT fixed here.
- Assumption (RESEARCH note): `trips/page.tsx` `formatDate` uses `toLocaleDateString('en-GB', ...)` — date formatting is L10N-PRICE (deferred to v2), out of STR-02 scope; left untouched.
- Assumption (Pattern 3 reuse): `account/actions.ts` reuses the Server-Action locale-threading pattern proven in 70-02; the shared `Errors` namespace is extended (not duplicated) with `fullNameRequired`, `phoneRequired`, `duplicateDefaultPassenger`, `invalidAccountType`.
- Assumption (ENCODING probe): all account copy + the "Signed in as {email}" ICU line are byte-for-byte unchanged.
- Assumption (SECURITY): ownership checks derive `user_id` from `getUser()` (never FormData); only the returned error strings change. Recorded as the mitigation for T-70-08-02.
- Depends on 70-04 (vehicleClasses) and 70-02 (Errors) — both earlier waves. No checkpoint (convention locked).
</assumption_delta_decision>

<ai_integration>
No external API integration in this plan. Supabase Auth/data calls in the account actions are pre-existing and unchanged; only returned error strings are localized. (schema-gate: no DB schema change.)
</ai_integration>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-string-externalization-booking-account/70-RESEARCH.md
@.planning/phases/70-string-externalization-booking-account/70-PATTERNS.md

# Analogs (Server Component + the Pattern-E precedent from 70-02):
@components/Testimonials.tsx
@app/[locale]/account/actions.ts
@components/account/ProfileForm.tsx
@messages/en.json
@tests/nav-auth.test.tsx
</context>

<!-- planner-discipline-allow: .bind(null, locale) -->

<tasks>

<task type="auto">
  <name>Task 1: Account Server Components (dashboard + trips) via getTranslations</name>
  <files>app/[locale]/account/page.tsx, app/[locale]/account/trips/page.tsx, app/[locale]/account/profile/page.tsx, messages/en.json</files>
  <read_first>
    - app/[locale]/account/page.tsx (Server Component: "My Account", "Signed in as {email}", two nav cards)
    - app/[locale]/account/trips/page.tsx (Server Component: empty-state block, per-trip labels; CLASS_LABELS at ~8-11 → reuse Booking.vehicleClasses; STATUS_STYLES 4-key map at ~14-19 with `?? pending` fallback at ~114 — preserve; `en-GB` formatDate at ~24 — untouched)
    - app/[locale]/account/profile/page.tsx (Server Component, 0 strings — confirm pure composition)
    - components/Testimonials.tsx (full — the `await getTranslations('Namespace')` Server Component pattern — Pattern C)
    - messages/en.json (Booking.vehicleClasses from 70-04)
  </read_first>
  <action>
    In `account/page.tsx`, replace any `useTranslations` import with `await getTranslations('Account.dashboard')`, keep the async signature, and render the heading, the two nav cards, and the "Signed in as" line via `t(...)` — the email line uses named-ICU `t('signedInAs', {email})`. In `account/trips/page.tsx`, use `await getTranslations('Account.trips')` for the empty-state and per-trip labels; delete the local `CLASS_LABELS` map and resolve the class label from `Booking.vehicleClasses` (via a separate `getTranslations('Booking.vehicleClasses')` or an index into the loaded messages); externalize the status-label text into `Account.trips.statusLabels` keeping EXACTLY the 4 existing keys and the same `STATUS_STYLES[trip.status] ?? STATUS_STYLES.pending` fallback; leave the `en-GB` date format and the color/style values untouched. Confirm `profile/page.tsx` has no user-facing string (leave it unchanged if so). Add `Account.dashboard` and `Account.trips` to the catalog copying the current literals byte-for-byte.
  </action>
  <verify>
    <automated>node -e "const m=require('./messages/en.json'); const s=m.Account.trips.statusLabels; if(Object.keys(s).length!==4) throw new Error('statusLabels must stay 4 keys, got '+Object.keys(s).length); if(!m.Account.dashboard) throw new Error('missing dashboard'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getTranslations('Account.dashboard')" app/[locale]/account/page.tsx` returns >= 1
    - `grep -c "getTranslations('Account.trips')" app/[locale]/account/trips/page.tsx` returns >= 1
    - `grep -c "'en-GB'" app/[locale]/account/trips/page.tsx` returns >= 1 (date format unchanged)
    - `grep -rn "First Class" app/[locale]/account/trips/page.tsx` returns 0
    - `node -e "const m=require('./messages/en.json'); if(Object.keys(m.Account.trips.statusLabels).length!==4) throw new Error('status gap not preserved'); console.log('ok')"` exits 0
  </acceptance_criteria>
  <done>Account dashboard + trips render via getTranslations; class labels reuse vehicleClasses; the 4-key status gap and en-GB format are preserved.</done>
</task>

<task type="auto">
  <name>Task 2: ProfileForm + account/actions.ts locale threading + reset-password</name>
  <files>components/account/ProfileForm.tsx, app/[locale]/account/actions.ts, app/[locale]/account/reset-password/page.tsx, messages/en.json</files>
  <read_first>
    - components/account/ProfileForm.tsx (contact/corporate/passenger-editor; useActionState calls at ~234/242/243/244; interpolated `Edit ${name}`/`Delete ${name}` aria at ~565/612)
    - app/[locale]/account/actions.ts (updateProfile ~24, addPassenger ~79, updatePassenger ~145, deletePassenger ~208; repeated "Not authenticated."/"Something went wrong."/"Full name is required."/"Phone number is required."/"Another default passenger already exists." + unique "Invalid account type.")
    - app/[locale]/account/reset-password/page.tsx (recovery-session gating + password-update form)
    - messages/en.json (Errors namespace from 70-02 — reuse `notAuthenticated`, `genericRetry`)
    - 70-PATTERNS.md lines 147-185 (Pattern E — the proven client `.bind(null, locale)` + server `getTranslations({namespace,locale})` shape)
  </read_first>
  <action>
    In `account/actions.ts`: add `import { getTranslations } from 'next-intl/server'`; add `locale: string` as the FIRST parameter of `updateProfile`, `addPassenger`, `updatePassenger`, `deletePassenger`; inside each, `const t = await getTranslations({ namespace: 'Errors', locale })`; replace the returned error literals with shared keys — not-authenticated → `t('notAuthenticated')`, generic-retry → `t('genericRetry')` — and EXTEND the `Errors` namespace with `fullNameRequired`, `phoneRequired`, `duplicateDefaultPassenger`, `invalidAccountType` (values copied byte-for-byte), used by the corresponding branches. Do NOT touch the `getUser()` ownership checks or any control flow. In `ProfileForm.tsx`: add `import { useLocale } from 'next-intl'` + `const locale = useLocale()`, change each `useActionState(<action>, null)` to `useActionState(<action>.bind(null, locale), null)`, add `useTranslations('Account.profile')`, replace all visible strings (contact fields, account-type toggle, corporate fields, saved-passenger list/editor/delete-bar) with `t('<key>')`, and convert the `Edit ${name}`/`Delete ${name}` aria to named-ICU `t('editPassengerAria', {name})` / `t('deletePassengerAria', {name})`. In `reset-password/page.tsx`, add `useTranslations('Account.profile')` (or a `Account.resetPassword` sub-namespace) and externalize its strings. Add `Account.profile` to the catalog.
  </action>
  <verify>
    <automated>node -e "const m=require('./messages/en.json'); if(!m.Account.profile) throw new Error('missing Account.profile'); ['fullNameRequired','phoneRequired','duplicateDefaultPassenger','invalidAccountType'].forEach(k=>{if(!m.Errors[k])throw new Error('Errors missing '+k)}); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getTranslations({ namespace: 'Errors'" app/[locale]/account/actions.ts` returns >= 4
    - `grep -c "useTranslations" app/[locale]/account/actions.ts` returns 0
    - `grep -c ".bind(null, locale)" components/account/ProfileForm.tsx` returns >= 4
    - `grep -c "useLocale()" components/account/ProfileForm.tsx` returns >= 1
    - `grep -nE "aria-label=\{\`(Edit|Delete) " components/account/ProfileForm.tsx` returns 0 (named-ICU aria)
  </acceptance_criteria>
  <done>ProfileForm + reset-password render from the catalog; account actions localize errors via a threaded locale reusing/extending Errors; ownership checks untouched.</done>
</task>

<task type="auto">
  <name>Task 3: PHASE GATE — extend tests, re-sync stubs, full suite + 7-locale build + single-source grep</name>
  <files>tests/account-trips.test.tsx, tests/profile-actions.test.ts, tests/passenger-actions.test.ts, messages/ru.json, messages/es.json, messages/fr.json, messages/ar.json, messages/hi.json, messages/zh.json</files>
  <read_first>
    - tests/account-trips.test.tsx (swap to renderWithIntl if it renders the component), tests/profile-actions.test.ts, tests/passenger-actions.test.ts (extend to assert the account actions return the exact prior English error text through Errors, called with locale='en')
    - tests/nav-auth.test.tsx (vi.hoisted mock pattern for the action tests)
    - messages/en.json (final catalog after Task 2)
  </read_first>
  <action>
    Migrate `tests/account-trips.test.tsx` to `renderWithIntl` if it renders the server/client component; in `tests/profile-actions.test.ts` and `tests/passenger-actions.test.ts`, update the action calls to pass `locale='en'` as the first argument and add/adjust one assertion each proving the returned error equals the exact prior English string (Errors round-trip). Re-sync the 6 stub locales (`cp messages/en.json messages/{ru,es,fr,ar,hi,zh}.json`). Then run the PHASE GATE: full test suite, the 7-locale production build, and the phase-wide single-source vehicle-label grep across the whole in-scope tree.
  </action>
  <verify>
    <automated>for l in ru es fr ar hi zh; do cmp -s messages/en.json messages/$l.json || { echo "DRIFT: $l"; exit 1; }; done; npx vitest run && npm run build && test $(grep -rn "First Class" components/booking "app/[locale]/account" | grep -vc messages) -eq 0 && echo "PHASE GATE GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - all 6 `cmp -s messages/en.json messages/<locale>.json` succeed
    - `npx vitest run` (full suite) exits 0 — no regression from the Phase 69 baseline (105 files / 1184 tests) plus this phase's additions
    - `npm run build` exits 0 with no MISSING_MESSAGE for any of the 7 locale subpaths
    - `grep -rn "First Class" components/booking "app/[locale]/account" | grep -v messages` returns nothing (no hardcoded vehicle-class display label remains in the in-scope tree; admin files under components/admin and app/(internal) are out of scope)
    - `tests/profile-actions.test.ts` and `tests/passenger-actions.test.ts` pass with the locale='en' first argument and the Errors round-trip assertion
  </acceptance_criteria>
  <done>Full suite green; 7-locale build clean with no MISSING_MESSAGE; no residual hardcoded vehicle-class label in scope — Phase 70 gate passes.</done>
</task>

</tasks>

<artifacts_produced>
**New/extended catalog namespaces:** `Account.dashboard`, `Account.trips` (incl. `statusLabels` 4-key), `Account.profile` (+ reset-password copy); `Errors` extended with `fullNameRequired`, `phoneRequired`, `duplicateDefaultPassenger`, `invalidAccountType`
**Modified source:** account/page.tsx, account/trips/page.tsx, account/profile/page.tsx (confirmed 0-string), ProfileForm.tsx (locale-bound + externalized), account/actions.ts (locale-threaded), reset-password/page.tsx
**Modified tests:** account-trips (renderWithIntl), profile-actions + passenger-actions (locale arg + Errors round-trip)
**Phase gate artifacts:** full `npx vitest run` green, `npm run build` clean (7 locales), phase-wide single-source vehicle-label grep == 0
**Reused single-sources:** `Booking.vehicleClasses` (from 70-04), `Errors` (from 70-02)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → account Server Action | `locale` is passed from ProfileForm into the mutations; must be allow-listed, never trusted user input |
| account Server Action → data (ownership) | `updateProfile`/passenger mutations enforce `user_id` from `getUser()`; only their error strings change |
| catalog → 7-locale build | Every locale must resolve every key or the build fails |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-08-01 | Spoofing / Tampering | locale param into account actions | medium | mitigate | `locale` is bound from `useLocale()` (validated route segment), never from FormData; `getTranslations` receives an allow-listed value |
| T-70-08-02 | Elevation of Privilege | account/actions.ts ownership checks | high | mitigate | This plan changes only error string values + adds a locale param + getTranslations; the `getUser()`-derived `user_id` ownership checks and all control flow are asserted untouched in diff review — access control cannot regress |
| T-70-08-03 | Repudiation | localized error rendering | low | mitigate | A wrong/blank error would mask a real failure; profile-actions/passenger-actions tests assert the exact English text; stub-sync keeps non-EN locales resolving the key |
| T-70-08-04 | Information Disclosure | Account/Errors namespaces | low | mitigate | Only visible copy is externalized; no user profile PII, token, or session detail enters the catalog |
</threat_model>

<verification>
- Full `npx vitest run` green (no regression + new/extended assertions)
- `npm run build` clean for all 7 locales — no MISSING_MESSAGE
- Phase-wide single-source grep for the vehicle-class label literal returns 0 across `components/booking` + `app/[locale]/account`
- account actions: `getTranslations({namespace:'Errors', locale})` used, no `useTranslations`; ProfileForm `.bind(null, locale)` >= 4; `useLocale()` present
- 4-key status gap preserved; `en-GB` date format unchanged; 6 stubs byte-identical to en.json
</verification>

<success_criteria>
The account + auth account surface renders every string from the catalog (Server Components via getTranslations, ProfileForm + reset-password via useTranslations, account actions via a threaded locale reusing the shared Errors namespace), the pre-existing status gap and en-GB format are preserved, ownership checks are provably untouched, and the phase gate — full test suite, 7-locale build with no MISSING_MESSAGE, and a clean single-source vehicle-label grep — is green, completing STR-02.
</success_criteria>

<output>
Create `.planning/phases/70-string-externalization-booking-account/70-08-SUMMARY.md` when done.
</output>