---
phase: 70-string-externalization-booking-account
plan: 07
type: execute
wave: 7
depends_on: ["70-06"]
files_modified:
  - components/booking/MultiDayForm.tsx
  - components/booking/DayCard.tsx
  - components/booking/steps/Step3Auth.tsx
  - messages/en.json
  - messages/ru.json
  - messages/es.json
  - messages/fr.json
  - messages/ar.json
  - messages/hi.json
  - messages/zh.json
  - tests/MultiDayForm.test.tsx
  - tests/DayCard.test.tsx
  - tests/Step3Auth.test.tsx
autonomous: true
requirements: [STR-02]
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "MultiDayForm renders all its labels, the start-date aria, and the role=alert validation block from Booking.multiDayForm"
    - "DayCard's six-plus interpolated 'Day {index+1} …' aria templates use named-ICU t('key',{day}) — no template-literal aria remains"
    - "Step3Auth (the in-wizard sign-in/register UI) renders all visible strings from Auth.inWizard, reusing identical labels where the English text matches Auth.login"
    - "A minimal renderWithIntl smoke test covers Step3Auth (no prior test existed); MultiDayForm and DayCard tests run under renderWithIntl and stay green"
    - "English byte-for-byte unchanged; 6 stubs in sync"
  artifacts:
    - path: messages/en.json
      provides: "Booking.multiDayForm + Booking.dayCard + Auth.inWizard"
      contains: "\"inWizard\""
    - path: components/booking/DayCard.tsx
      provides: "named-ICU day aria"
      contains: "useTranslations('Booking.dayCard')"
    - path: tests/Step3Auth.test.tsx
      provides: "new renderWithIntl smoke test for the in-wizard auth UI"
      contains: "renderWithIntl"
  key_links:
    - from: components/booking/DayCard.tsx
      to: messages/en.json
      via: "t('hourAria',{day}) named-ICU replaces `Day ${index+1} …` templates"
      pattern: "useTranslations\\('Booking\\.dayCard'\\)"
    - from: components/booking/steps/Step3Auth.tsx
      to: messages/en.json
      via: "useTranslations('Auth.inWizard') renders the separate in-wizard auth form"
      pattern: "useTranslations\\('Auth\\.inWizard'\\)"
  prohibitions:
    - statement: "Do NOT build interpolated day aria via template literals; use named-ICU"
      status: prohibited
      verification: "grep -nE 'aria-label=\\{`Day ' components/booking/DayCard.tsx returns 0"
    - statement: "Do NOT assume Step3Auth shares code with login/page.tsx; it has its own client-side Supabase calls — externalize its own copy, only SHARE identical catalog strings"
      status: prohibited
      verification: "Step3Auth uses useTranslations('Auth.inWizard'); its Supabase call logic is unchanged"
    - statement: "Teaser-wrapper marketing headlines (BookingSection/HourlyBookingSection) are NOT externalized here — deferred to Phase 71 (Marketing & SEO Pages)"
      status: deferred
      verification: "BookingSection.tsx / HourlyBookingSection.tsx are not in this plan's files_modified; the embedded BookingWidget was already externalized in 70-05"
---

<objective>
Externalize the multi-day booking flow (`MultiDayForm`, `DayCard` — the `/book/multi-day` surface) and the in-wizard auth step (`Step3Auth` — a SEPARATE sign-in/register UI with its own client-side Supabase calls, under `Auth.inWizard`). `DayCard`'s repeated interpolated `Day {n} …` aria templates move to named-ICU. The teaser-wrapper marketing headlines in `BookingSection`/`HourlyBookingSection` are explicitly deferred to Phase 71 (their embedded `BookingWidget` was already externalized in 70-05, so no booking string is lost).

Purpose: Completes the booking-flow surface (Success Criterion 1) by covering the multi-day path and the in-wizard auth, while keeping the marketing teaser copy where it belongs (Phase 71).

Output: `Booking.multiDayForm`, `Booking.dayCard`, `Auth.inWizard`; three converted files; two migrated tests + one new smoke test.
</objective>

<assumption_delta_decision>
- Assumption (RESEARCH A2): `Auth.inWizard` (Step3Auth) and `Auth.login` (login/page.tsx) are separately-implemented forms; they get independent namespaces but SHARE identical catalog strings where the English text matches ("Email", "Password", "Create one"). Worst case is minor key duplication, not a functional bug.
- Assumption (RESEARCH A3, medium risk — resolved): `BookingSection`/`HourlyBookingSection` wrap `BookingWidget` (externalized in 70-05) with their own `<h2>` marketing headline; those headlines are Phase 71 (Marketing) scope and are deferred here, recorded so they are not silently dropped.
- Assumption (ENCODING probe): all copy and the interpolated day aria are byte-for-byte unchanged; day aria uses `{day}` named args.
- Assumption: Step3Auth has no existing test; a minimal renderWithIntl smoke test is added to satisfy the Nyquist automated-verify requirement without re-testing Supabase auth logic (mock the client per nav-auth.test.tsx's vi.hoisted structure or render with mocked deps).
- Depends on 70-02 (Auth namespace exists). No checkpoint (convention locked).
</assumption_delta_decision>

<ai_integration>
No external API integration in this plan. Step3Auth's Supabase client calls are pre-existing and unchanged; only its displayed strings move. (schema-gate: no DB schema change.)
</ai_integration>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-string-externalization-booking-account/70-RESEARCH.md
@.planning/phases/70-string-externalization-booking-account/70-PATTERNS.md

# Analogs:
@components/Nav.tsx
@messages/en.json
@tests/nav-auth.test.tsx
@tests/helpers/renderWithIntl.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: MultiDayForm + DayCard (named-ICU day aria)</name>
  <files>components/booking/MultiDayForm.tsx, components/booking/DayCard.tsx, messages/en.json</files>
  <read_first>
    - components/booking/MultiDayForm.tsx (~18 strings: `aria-label="Select start date"`/`"Start date"`, the role=alert validation block)
    - components/booking/DayCard.tsx (~11+ strings: six-plus interpolated `Day ${index+1} …` aria templates, HOUR/MIN/"Start time" labels, address sub-labels)
    - messages/en.json line 66 (named-ICU `ratingAria` shape)
    - components/Nav.tsx lines 1-8, 24-26 (useTranslations analog)
  </read_first>
  <action>
    Add `useTranslations('Booking.multiDayForm')` to `MultiDayForm.tsx` and replace every visible label, the start-date aria, and the role=alert validation strings with `t('<key>')`; add `Booking.multiDayForm` to the catalog. Add `useTranslations('Booking.dayCard')` to `DayCard.tsx`; convert each `Day ${index+1} …` aria template to named-ICU `t('<key>', {day: index + 1})` (one key per aria variant), and replace the HOUR/MIN/"Start time"/address sub-labels with `t('<key>')`. Add `Booking.dayCard` with `{day}` named-arg values copying the current wording byte-for-byte.
  </action>
  <verify>
    <automated>node -e "const m=require('./messages/en.json'); if(!m.Booking.multiDayForm||!m.Booking.dayCard) throw new Error('missing multiDayForm/dayCard'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslations('Booking.multiDayForm')" components/booking/MultiDayForm.tsx` returns >= 1
    - `grep -c "useTranslations('Booking.dayCard')" components/booking/DayCard.tsx` returns >= 1
    - `grep -nE "aria-label=\{\`Day " components/booking/DayCard.tsx` returns 0 (no template-literal day aria remains)
  </acceptance_criteria>
  <done>MultiDayForm and DayCard render from the catalog; day aria uses named-ICU.</done>
</task>

<task type="auto">
  <name>Task 2: Step3Auth (in-wizard auth) → Auth.inWizard</name>
  <files>components/booking/steps/Step3Auth.tsx, messages/en.json</files>
  <read_first>
    - components/booking/steps/Step3Auth.tsx (~25+ strings, ~502 lines: Sign In / Create Account tabs, Code/Password sub-tabs, own client-side Supabase calls)
    - app/[locale]/login/page.tsx (for the identical-copy strings to share — "Email", "Password", "Create one") — reference only, do not edit here
    - messages/en.json (Auth.login from 70-02 — reuse identical strings where the English matches)
    - components/Nav.tsx lines 1-8, 24-26 (useTranslations analog)
  </read_first>
  <action>
    Add `useTranslations('Auth.inWizard')` to `Step3Auth.tsx` and replace every visible string (tab labels, form labels, buttons, helper copy, status messages) with `t('<key>')`. Where the English text is byte-identical to an existing `Auth.login`/`Auth.oauth` key, reference that shared key instead of duplicating; add the remaining Step3Auth-specific strings under `Auth.inWizard`. Do NOT change any Supabase client call, OTP/password/register logic, or control flow — only string literals. Add the `Auth.inWizard` namespace copying the current literals byte-for-byte.
  </action>
  <verify>
    <automated>node -e "const m=require('./messages/en.json'); if(!m.Auth.inWizard) throw new Error('missing Auth.inWizard'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslations('Auth.inWizard')" components/booking/steps/Step3Auth.tsx` returns >= 1
    - `grep -nE "placeholder=\"[A-Za-z]" components/booking/steps/Step3Auth.tsx` returns 0 (no hardcoded placeholder literals remain)
    - `node -e "const m=require('./messages/en.json'); if(Object.keys(m.Auth.inWizard).length<3) throw new Error('inWizard too sparse'); console.log('ok')"` exits 0
  </acceptance_criteria>
  <done>Step3Auth renders all visible strings from Auth.inWizard, reusing shared auth keys where identical; Supabase logic unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate MultiDayForm/DayCard tests + add Step3Auth smoke test, re-sync stubs</name>
  <files>tests/MultiDayForm.test.tsx, tests/DayCard.test.tsx, tests/Step3Auth.test.tsx, messages/ru.json, messages/es.json, messages/fr.json, messages/ar.json, messages/hi.json, messages/zh.json</files>
  <read_first>
    - tests/MultiDayForm.test.tsx, tests/DayCard.test.tsx (the files being modified — bare `render` imports)
    - tests/nav-auth.test.tsx (full — the vi.hoisted mock + renderWithIntl pattern to model the new Step3Auth smoke test on)
  </read_first>
  <behavior>
    - tests/Step3Auth.test.tsx: renders Step3Auth via renderWithIntl (Supabase client mocked per the vi.hoisted pattern) and asserts a known Auth.inWizard label appears — proves the component renders inside a next-intl provider without throwing
    - MultiDayForm/DayCard tests keep every assertion and pass under renderWithIntl
  </behavior>
  <action>
    Swap the bare-`render` import for the aliased `renderWithIntl as render` in `tests/MultiDayForm.test.tsx` and `tests/DayCard.test.tsx`, leaving call sites/assertions unchanged. Create `tests/Step3Auth.test.tsx` modeled on `tests/nav-auth.test.tsx`: mock the Supabase client via `vi.hoisted`, render Step3Auth with `renderWithIntl`, and assert one visible `Auth.inWizard` label is present. Re-sync the 6 stub locales (`cp messages/en.json messages/{ru,es,fr,ar,hi,zh}.json`) and run the touched-test slice.
  </action>
  <verify>
    <automated>for l in ru es fr ar hi zh; do cmp -s messages/en.json messages/$l.json || { echo "DRIFT: $l"; exit 1; }; done; npx vitest run tests/MultiDayForm.test.tsx tests/DayCard.test.tsx tests/Step3Auth.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "renderWithIntl" tests/MultiDayForm.test.tsx tests/DayCard.test.tsx tests/Step3Auth.test.tsx` shows all three files matched
    - `npx vitest run tests/MultiDayForm.test.tsx tests/DayCard.test.tsx tests/Step3Auth.test.tsx` exits 0
    - all 6 `cmp -s` stub checks succeed
  </acceptance_criteria>
  <done>MultiDayForm/DayCard tests green under renderWithIntl; a new Step3Auth smoke test passes; stubs in sync.</done>
</task>

</tasks>

<artifacts_produced>
**New catalog namespaces:** `Booking.multiDayForm`, `Booking.dayCard` (named-ICU day aria), `Auth.inWizard`
**Modified source:** MultiDayForm, DayCard, Step3Auth
**Modified tests:** MultiDayForm, DayCard
**New test:** `tests/Step3Auth.test.tsx` (renderWithIntl smoke)
**Deferred to Phase 71:** BookingSection / HourlyBookingSection marketing headlines (embedded BookingWidget already done in 70-05)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Step3Auth → Supabase Auth | The in-wizard auth performs client-side Supabase calls; only its display strings change |
| catalog → interpolated day aria | Day-index args are UI-derived integers rendered as escaped text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-07-01 | Spoofing / Elevation of Privilege | Step3Auth Supabase calls | low | mitigate | Only string literals change; auth control flow (OTP/password/register) is untouched, so the sign-in path cannot regress |
| T-70-07-02 | Information Disclosure | Auth.inWizard namespace | low | mitigate | Only visible auth-form copy is externalized; no token, session, or PII enters the catalog |
| T-70-07-03 | Denial of Service | 7-locale build | low | accept | Missing key surfaces at build (MISSING_MESSAGE); stub-sync guard prevents partial catalogs |
</threat_model>

<verification>
- `npx vitest run tests/MultiDayForm.test.tsx tests/DayCard.test.tsx tests/Step3Auth.test.tsx` green
- named-ICU day aria (no template-literal aria); `Auth.inWizard` present and reuses shared keys where identical
- 6 stubs byte-identical to en.json
- (wave-merge gate) `npx vitest run` full suite green
</verification>

<success_criteria>
The multi-day flow and in-wizard auth render every string from the catalog (day aria via named-ICU, Step3Auth under Auth.inWizard reusing shared keys), a new smoke test covers Step3Auth, marketing teaser headlines are explicitly deferred to Phase 71, tests are green under renderWithIntl, English is byte-for-byte unchanged, and locales are in sync.
</success_criteria>

<output>
Create `.planning/phases/70-string-externalization-booking-account/70-07-SUMMARY.md` when done.
</output>
