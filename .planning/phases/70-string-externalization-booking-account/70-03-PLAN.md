---
phase: 70-string-externalization-booking-account
plan: 03
type: execute
wave: 3
depends_on: ["70-02"]
files_modified:
  - components/booking/TripTypeTabs.tsx
  - components/booking/AddressInput.tsx
  - components/booking/AddressInputNew.tsx
  - components/booking/DurationSelector.tsx
  - components/booking/StopList.tsx
  - components/booking/StopItem.tsx
  - components/booking/RouteMap.tsx
  - components/booking/ProgressBar.tsx
  - components/booking/Stepper.tsx
  - messages/en.json
  - messages/ru.json
  - messages/es.json
  - messages/fr.json
  - messages/ar.json
  - messages/hi.json
  - messages/zh.json
  - tests/TripTypeTabs.test.tsx
  - tests/AddressInput.test.tsx
  - tests/DurationSelector.test.tsx
  - tests/StopList.test.tsx
  - tests/StopItem.test.tsx
  - tests/RouteMap.test.tsx
  - tests/ProgressBar.test.tsx
  - tests/Stepper.test.tsx
autonomous: true
requirements: [STR-02]
user_setup: []

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "TripTypeTabs renders its tab labels from a Booking.tripTypeTabs.items JSON array index-paired with the structural TRIP_TYPES array (label field removed from the in-code array) — the Nav NAV_LINKS/items split"
    - "AddressInput and AddressInputNew both render placeholders/aria from one shared Booking.addressInput namespace"
    - "DurationSelector, StopList, StopItem, RouteMap render every visible string + aria from the catalog"
    - "RouteMap, ProgressBar, and Stepper interpolated aria-labels use named-ICU args (t('key',{arg})) — no template-literal string building"
    - "Every touched component's existing test runs under renderWithIntl and stays green"
    - "English output byte-for-byte unchanged; 6 stub catalogs byte-identical to en.json"
  artifacts:
    - path: messages/en.json
      provides: "Booking.tripTypeTabs / addressInput / durationSelector / stopList / stopItem / routeMap / progressBar / stepper"
      contains: "\"tripTypeTabs\""
    - path: components/booking/TripTypeTabs.tsx
      provides: "tab labels via t.raw('items') index-zip"
      contains: "t.raw('items')"
    - path: components/booking/RouteMap.tsx
      provides: "empty-state + named-ICU aria via useTranslations"
      contains: "useTranslations('Booking.routeMap')"
  key_links:
    - from: components/booking/TripTypeTabs.tsx
      to: messages/en.json
      via: "t.raw('items') array index-paired with structural TRIP_TYPES (label removed from code array)"
      pattern: "t\\.raw\\('items'\\)"
    - from: components/booking/AddressInputNew.tsx
      to: messages/en.json
      via: "shared Booking.addressInput namespace reused by both AddressInput and AddressInputNew"
      pattern: "useTranslations\\('Booking\\.addressInput'\\)"
  prohibitions:
    - statement: "Do NOT wrap the whole TRIP_TYPES object array in t.raw (that would translate kind/value/href and break routing) — keep structural fields in code, only the label array in the catalog"
      status: prohibited
      verification: "grep shows TRIP_TYPES retains kind/value/href fields; label field removed; t.raw('items') used for labels"
    - statement: "Do NOT build interpolated aria via template literals; use named-ICU t('key',{arg})"
      status: prohibited
      verification: "grep -nE 'aria-label=\\{`' components/booking/RouteMap.tsx components/booking/ProgressBar.tsx returns 0"
---

<objective>
Externalize the booking input primitives: `TripTypeTabs`, `AddressInput` + `AddressInputNew` (one shared namespace), `DurationSelector`, `StopList`, `StopItem`, `RouteMap`, `ProgressBar`, and `Stepper`. Apply the two established Phase 69 shapes: the Nav `NAV_LINKS`/`items` structural-array split for `TripTypeTabs` (PATTERNS Pattern B), and named-ICU interpolation for the `RouteMap`/`ProgressBar`/`Stepper` aria templates (PATTERNS Pattern D / named-ICU). Migrate each component's existing test to `renderWithIntl`.

Purpose: These are the highest-frequency, lowest-string-count building blocks of the wizard; getting them onto shared namespaces early (especially the shared `Booking.addressInput` used by two files) removes duplication before the larger step components consume them.

Output: `Booking.tripTypeTabs`, `Booking.addressInput`, `Booking.durationSelector`, `Booking.stopList`, `Booking.stopItem`, `Booking.routeMap`, `Booking.progressBar`, `Booking.stepper`; externalized components; migrated tests.
</objective>

<assumption_delta_decision>
- Assumption (ENCODING probe): English text byte-for-byte unchanged; interpolated aria uses named args (`{origin}`, `{destination}`, `{step}`, `{total}`, `{label}`) matching the existing `ratingAria`/`copyright` catalog entries.
- Assumption: `AddressInput.tsx` and `AddressInputNew.tsx` share enough identical placeholder/aria copy to justify ONE `Booking.addressInput` namespace (RESEARCH inventory notes shared labels). If a string exists in only one, it still lives under `addressInput` with a distinct key — no functional risk.
- Assumption: `Stepper`'s `Decrease ${label}` / `Increase ${label}` aria uses a `label` prop passed from the call site; the catalog key takes `{label}` as a named arg, and the call site continues to pass the same value.
- Assumption: Phase 69 convention LOCKED and inherited; no checkpoint.
</assumption_delta_decision>

<ai_integration>
No external API integration in this plan. (RouteMap uses the pre-existing Google Maps loader; no map/API change — only its label strings move.) schema-gate: no DB schema change.
</ai_integration>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-string-externalization-booking-account/70-RESEARCH.md
@.planning/phases/70-string-externalization-booking-account/70-PATTERNS.md

# Analogs:
@components/Nav.tsx
@messages/en.json
@tests/nav-auth.test.tsx
@tests/helpers/renderWithIntl.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: TripTypeTabs (array split) + AddressInput + AddressInputNew (shared namespace)</name>
  <files>components/booking/TripTypeTabs.tsx, components/booking/AddressInput.tsx, components/booking/AddressInputNew.tsx, messages/en.json</files>
  <read_first>
    - components/booking/TripTypeTabs.tsx (full — `TRIP_TYPES` at ~11-15 mixing `{kind, value|href}` with a hardcoded `label`)
    - components/booking/AddressInput.tsx and components/booking/AddressInputNew.tsx (placeholders + aria — the two share label copy)
    - components/Nav.tsx lines 14-21, 25, 136-149 (the NAV_LINKS structural array + `t.raw('items')` index-zip — Pattern B)
    - messages/en.json lines 2-13 (Nav namespace, including the `items` array shape)
  </read_first>
  <action>
    In `TripTypeTabs.tsx`: drop the `label` field from the in-code `TRIP_TYPES` array so it holds only structural `{kind, value|href}` entries; add `const t = useTranslations('Booking.tripTypeTabs')` and `const labels = t.raw('items') as string[]`; render each tab's text as `labels[i]` index-paired with `TRIP_TYPES.map((tab, i) => ...)`. Add `Booking.tripTypeTabs.items` to the catalog as a JSON array copying the current tab labels in the exact same order. In `AddressInput.tsx` and `AddressInputNew.tsx`: add `useTranslations('Booking.addressInput')` to each and replace every placeholder/aria/label literal with `t('<key>')` from the single shared `Booking.addressInput` namespace (define each distinct English string once; both files reference the same keys where the copy is identical). All catalog values copy the current literals byte-for-byte.
  </action>
  <verify>
    <automated>node -e "const m=require('./messages/en.json'); if(!Array.isArray(m.Booking.tripTypeTabs.items)) throw new Error('items not array'); if(!m.Booking.addressInput) throw new Error('no addressInput'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "t.raw('items')" components/booking/TripTypeTabs.tsx` returns >= 1
    - `grep -c "useTranslations('Booking.addressInput')" components/booking/AddressInput.tsx` returns >= 1
    - `grep -c "useTranslations('Booking.addressInput')" components/booking/AddressInputNew.tsx` returns >= 1
    - `node -e "const m=require('./messages/en.json'); if(m.Booking.tripTypeTabs.items.length<2) throw new Error('too few tabs'); console.log('ok')"` exits 0
  </acceptance_criteria>
  <done>TripTypeTabs renders labels via the array split; both address inputs share one namespace; English unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: DurationSelector, StopList, StopItem, RouteMap, ProgressBar, Stepper + named-ICU aria</name>
  <files>components/booking/DurationSelector.tsx, components/booking/StopList.tsx, components/booking/StopItem.tsx, components/booking/RouteMap.tsx, components/booking/ProgressBar.tsx, components/booking/Stepper.tsx, messages/en.json</files>
  <read_first>
    - components/booking/DurationSelector.tsx, StopList.tsx, StopItem.tsx (full — small string counts; aria-labels + placeholders)
    - components/booking/RouteMap.tsx (~line 322 interpolated route aria), ProgressBar.tsx (~line 15 interpolated progress aria), Stepper.tsx (`Decrease ${label}`/`Increase ${label}` aria templates)
    - messages/en.json lines 21, 23, 66 (`priceAnchor`/`copyright`/`ratingAria` — the named-ICU shape to copy)
  </read_first>
  <action>
    Add `useTranslations('Booking.durationSelector')`, `useTranslations('Booking.stopList')`, `useTranslations('Booking.stopItem')`, `useTranslations('Booking.routeMap')`, `useTranslations('Booking.progressBar')`, `useTranslations('Booking.stepper')` to the respective components and replace each visible literal / aria-label with `t('<key>')`. For the three interpolated aria templates, use named-ICU: RouteMap → `t('aria', {origin, destination})`; ProgressBar → `t('aria', {step, total})`; Stepper → `t('decreaseAria', {label})` / `t('increaseAria', {label})` (Stepper's `label` remains the prop value passed from the call site). Catalog entries for these keys use `{origin}`/`{destination}`/`{step}`/`{total}`/`{label}` named placeholders and copy the current English wording byte-for-byte. Add all keys under their `Booking.*` sub-namespaces.
  </action>
  <verify>
    <automated>node -e "const m=require('./messages/en.json'); ['durationSelector','stopList','stopItem','routeMap','progressBar','stepper'].forEach(k=>{if(!m.Booking[k])throw new Error('missing Booking.'+k)}); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslations('Booking.routeMap')" components/booking/RouteMap.tsx` returns >= 1
    - `grep -nE "aria-label=\{\`" components/booking/RouteMap.tsx components/booking/ProgressBar.tsx components/booking/Stepper.tsx` returns 0 (no template-literal aria remains)
    - `node -e "const m=require('./messages/en.json'); if(!/\{origin\}/.test(JSON.stringify(m.Booking.routeMap))) throw new Error('routeMap aria not named-ICU'); console.log('ok')"` exits 0
  </acceptance_criteria>
  <done>All six components render from the catalog with named-ICU aria; English unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate the 8 touched test files to renderWithIntl, re-sync stubs, run the slice</name>
  <files>tests/TripTypeTabs.test.tsx, tests/AddressInput.test.tsx, tests/DurationSelector.test.tsx, tests/StopList.test.tsx, tests/StopItem.test.tsx, tests/RouteMap.test.tsx, tests/ProgressBar.test.tsx, tests/Stepper.test.tsx, messages/ru.json, messages/es.json, messages/fr.json, messages/ar.json, messages/hi.json, messages/zh.json</files>
  <read_first>
    - tests/TripTypeTabs.test.tsx, tests/AddressInput.test.tsx, tests/DurationSelector.test.tsx, tests/StopList.test.tsx, tests/StopItem.test.tsx, tests/RouteMap.test.tsx, tests/ProgressBar.test.tsx, tests/Stepper.test.tsx (the files being modified — each currently imports bare `render` from '@testing-library/react')
    - tests/nav-auth.test.tsx lines 1-15 (aliased `renderWithIntl as render` import — Pattern F)
  </read_first>
  <action>
    In each of the 8 test files, swap the bare-`render` import for the aliased helper (`import { renderWithIntl as render } from './helpers/renderWithIntl'`, keeping `screen`/`fireEvent` from '@testing-library/react'), leaving all call sites and assertions unchanged. Then re-sync the 6 stub locales (`cp messages/en.json messages/{ru,es,fr,ar,hi,zh}.json`) and run the full touched-test slice.
  </action>
  <verify>
    <automated>for l in ru es fr ar hi zh; do cmp -s messages/en.json messages/$l.json || { echo "DRIFT: $l"; exit 1; }; done; npx vitest run tests/TripTypeTabs.test.tsx tests/AddressInput.test.tsx tests/DurationSelector.test.tsx tests/StopList.test.tsx tests/StopItem.test.tsx tests/RouteMap.test.tsx tests/ProgressBar.test.tsx tests/Stepper.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - each of the 8 test files: `grep -c "renderWithIntl" <file>` returns >= 1
    - `npx vitest run` over the 8 files exits 0
    - all 6 `cmp -s` stub checks succeed
  </acceptance_criteria>
  <done>All 8 tests green under renderWithIntl; stubs in sync.</done>
</task>

</tasks>

<artifacts_produced>
**New catalog namespaces:** `Booking.tripTypeTabs` (items array), `Booking.addressInput` (shared by 2 files), `Booking.durationSelector`, `Booking.stopList`, `Booking.stopItem`, `Booking.routeMap`, `Booking.progressBar`, `Booking.stepper`
**Modified source:** TripTypeTabs, AddressInput, AddressInputNew, DurationSelector, StopList, StopItem, RouteMap, ProgressBar, Stepper
**Modified tests:** 8 files migrated to renderWithIntl (no AddressInputNew test exists)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog → rendered aria/labels | Interpolated aria values (addresses, step numbers) are user/route-derived and rendered as escaped text nodes via named-ICU args |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-03-01 | Tampering | TripTypeTabs structural/label split | low | mitigate | Only the `label` field moves to the catalog; `kind`/`value`/`href` (routing) stay in code, so a catalog edit cannot alter navigation targets |
| T-70-03-02 | Information Disclosure | messages/en.json | low | mitigate | Only visible UI copy is externalized; interpolated aria args are addresses already shown on screen — no new data exposure |
| T-70-03-03 | Denial of Service | 7-locale build | low | accept | Missing key surfaces at build time (MISSING_MESSAGE); stub-sync + cmp guard prevents partial catalogs |
</threat_model>

<verification>
- `npx vitest run` over the 8 touched test files green
- `grep` gates for `t.raw('items')`, shared `addressInput` hook, named-ICU aria pass
- 6 stubs byte-identical to en.json
- (wave-merge gate) `npx vitest run` full suite green
</verification>

<success_criteria>
The booking input primitives render every string from shared catalog namespaces (with the TripTypeTabs array split and named-ICU aria applied), their tests are green under renderWithIntl, English is byte-for-byte unchanged, and all locales stay in sync.
</success_criteria>

<output>
Create `.planning/phases/70-string-externalization-booking-account/70-03-SUMMARY.md` when done.
</output>
