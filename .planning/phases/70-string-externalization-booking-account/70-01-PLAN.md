---
phase: 70-string-externalization-booking-account
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/booking/EntryBar.tsx
  - messages/en.json
  - messages/ru.json
  - messages/es.json
  - messages/fr.json
  - messages/ar.json
  - messages/hi.json
  - messages/zh.json
  - tests/EntryBar.test.tsx
autonomous: true
requirements: [STR-02]
user_setup: []

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "EntryBar renders every visible string (labels, placeholders, aria-labels, hand-rolled validation errors) through useTranslations('Booking.entryBar') / useTranslations('Booking.validation') — no user-facing literal remains in the component"
    - "The rendered English EntryBar is byte-for-byte identical to before (catalog values copy the exact prior English text)"
    - "tests/EntryBar.test.tsx runs under renderWithIntl and stays green with no assertion changes"
    - "All 6 non-EN stub catalogs are byte-identical to messages/en.json (no MISSING_MESSAGE for any locale)"
  artifacts:
    - path: messages/en.json
      provides: "Booking.entryBar namespace + seed Booking.validation shared namespace"
      contains: "\"entryBar\""
    - path: components/booking/EntryBar.tsx
      provides: "EntryBar fully externalized via next-intl (the phase tracer)"
      contains: "useTranslations('Booking.entryBar')"
    - path: tests/EntryBar.test.tsx
      provides: "EntryBar test migrated to renderWithIntl"
      contains: "renderWithIntl"
  key_links:
    - from: components/booking/EntryBar.tsx
      to: messages/en.json
      via: "useTranslations('Booking.entryBar') + useTranslations('Booking.validation') resolve keys from the catalog"
      pattern: "useTranslations\\('Booking\\."
    - from: messages/en.json
      to: messages/ru.json
      via: "stub re-sync (cp) keeps every configured locale renderable"
      pattern: "entryBar"
  prohibitions:
    - statement: "Do NOT edit lib/extras.ts (out of this plan's scope; shared with server email)"
      status: prohibited
      verification: "git diff --name-only does not list lib/extras.ts"
    - statement: "Do NOT route analytics event names/params (GA4 'checkout_progress'/'form_start', currency 'EUR', step_name) through the catalog — they are third-party schema identifiers, not user-facing copy"
      status: prohibited
      verification: "grep -n \"step_name: 'entry_bar'\" components/booking/EntryBar.tsx still returns the raw identifier unchanged"
    - statement: "Do NOT hand-roll locale+href concatenation for internal links; reuse Link/usePathname from @/i18n/routing where any link is touched"
      status: prohibited
      verification: "grep -nE \"\\`/\\$\\{locale\\}\" components/booking/EntryBar.tsx returns 0"
---

<objective>
Tracer slice for Phase 70: fully externalize `components/booking/EntryBar.tsx` end-to-end — every visible string (labels, placeholders, aria-labels, and the hand-rolled `validate()` error strings) moves into `messages/en.json` under `Booking.entryBar` and a seeded shared `Booking.validation` namespace, rendered via next-intl `useTranslations`, with its existing test migrated to `renderWithIntl` and all 6 non-EN stub catalogs re-synced.

Purpose: EntryBar is the phase's thinnest complete proof — it exercises the full externalization loop for a booking Client Component (component string move + a hand-rolled validation message + its test-harness migration + stub re-sync + build-parity) using the Phase 69 convention verbatim, before the other 27 booking files and the account/auth surface fan out from it. Proving this on the agent's best early-context tokens catches any convention or wiring surprise after one commit instead of after ten.

Output: `Booking.entryBar` + seed `Booking.validation` catalog entries; a fully-externalized EntryBar; a migrated `EntryBar.test.tsx`; 6 re-synced stub catalogs.
</objective>

<assumption_delta_decision>
- Assumption (ENCODING probe, real bearing): English output must be byte-for-byte unchanged; catalog values copy the exact prior literal text (including punctuation/case). Any named-ICU interpolation uses `{name}` args (Phase 69 convention), never positional/`%s`. Verified by a green pre-existing test whose assertions are unchanged.
- Assumption (EMPTY probe, real bearing): EntryBar has no genuinely plural-sensitive sentence (counts render as bare integers/units). If one is discovered, use ICU `{count, plural, ...}` rather than an `n===1?` ternary. Low risk.
- Assumption (ADJACENCY/ORDERING probes): not applicable to a string-move refactor; recorded and dropped.
- Assumption: The Phase 69 namespace/key/array/ICU/stub convention is LOCKED (STATE.md 69-01 checkpoint) and inherited unchanged — this plan does not re-open it, so no `checkpoint:decision` is emitted.
- Assumption: EntryBar's `validate()` returns several near-identical "X is required to continue." strings; these are seeded into a shared `Booking.validation` namespace (reused by later step plans) rather than duplicated per-field, per RESEARCH Pitfall 5's shared-key guidance.
</assumption_delta_decision>

<ai_integration>
No external API integration in this plan. No AI/model call, no new external service. (schema-gate: no DB schema change.)
</ai_integration>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-string-externalization-booking-account/70-RESEARCH.md
@.planning/phases/70-string-externalization-booking-account/70-PATTERNS.md
@.planning/phases/69-string-externalization-ui-chrome/69-01-SUMMARY.md

# The locked convention analog (read to copy the exact pattern):
@components/Nav.tsx
@messages/en.json
@tests/helpers/renderWithIntl.tsx
@tests/nav-auth.test.tsx
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Externalize EntryBar visible strings + validation into the catalog (end-to-end tracer)</name>
  <files>components/booking/EntryBar.tsx, messages/en.json</files>
  <read_first>
    - components/booking/EntryBar.tsx (the file being modified — full read; note the `validate()` body at lines ~85-107 returning literal error strings, placeholders at ~214/245/377, aria-labels "Time"/"Flight number"/"Return time")
    - components/Nav.tsx lines 1-8, 24-26, 129, 148 (Phase-69 analog: `useTranslations('Nav')` hook + `t('key')` usage — PATTERNS.md Pattern A)
    - messages/en.json lines 1-27 (existing Nav/Hero namespaces — copy the PascalCase-namespace / camelCase-key shape; note named-ICU `priceAnchor`/`ratingAria`)
  </read_first>
  <action>
    Add a `useTranslations('Booking.entryBar')` hook and a `useTranslations('Booking.validation')` hook at the top of EntryBar's component body (mirroring Nav.tsx's `const t = useTranslations('Nav')`). Replace every visible literal — the pickup/destination placeholders, the flight-number placeholder, all aria-labels, and any button/label copy — with `t('<camelCaseKey>')`. For the hand-rolled `validate()` error strings, source each message from the validation hook: keep the existing `Record<string,string>` error-state shape and the existing validation logic exactly; only swap the literal on the right-hand side for `tv('<key>')`. Seed `Booking.validation` with the recurring "required to continue" family as shared keys (e.g. `originRequired`, `destinationRequired`, `dateRequired`, `timeRequired`, `returnDateRequired`, `returnTimeRequired`, `returnAfterPickup`) so later step plans reuse them. Add the matching `Booking.entryBar` and `Booking.validation` objects to `messages/en.json` with values copied byte-for-byte from the current literals. Do NOT touch the analytics event payloads (`form_start`, `checkout_progress`, `step_name`, `currency`), the booking-store calls, or any validation condition — only string literals change. Because the hook now runs, the schema/error text is read at render time; no logic moves.
  </action>
  <verify>
    <automated>node -e "const m=require('./messages/en.json'); if(!m.Booking||!m.Booking.entryBar||!m.Booking.validation) throw new Error('missing Booking.entryBar/validation'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslations('Booking.entryBar')" components/booking/EntryBar.tsx` returns >= 1
    - `grep -c "useTranslations('Booking.validation')" components/booking/EntryBar.tsx` returns >= 1
    - `node -e "const m=require('./messages/en.json'); ['originRequired','destinationRequired','dateRequired','timeRequired'].forEach(k=>{if(!m.Booking.validation[k])throw new Error('missing '+k)}); console.log('ok')"` exits 0
    - `grep -nE "placeholder=\"[A-Za-z]" components/booking/EntryBar.tsx` returns 0 (no hardcoded placeholder literals remain)
    - `grep -n "step_name: 'entry_bar'" components/booking/EntryBar.tsx` still returns the analytics identifier unchanged (event params NOT externalized)
  </acceptance_criteria>
  <done>EntryBar renders all visible strings and validation errors from the catalog; English text unchanged; analytics payloads untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate tests/EntryBar.test.tsx to renderWithIntl</name>
  <files>tests/EntryBar.test.tsx</files>
  <read_first>
    - tests/EntryBar.test.tsx (the file being modified — note line 2 `import { render, screen } from '@testing-library/react'` and the ~9 `render(<EntryBar />)` call sites)
    - tests/nav-auth.test.tsx lines 1-15 (Phase-69 analog: `renderWithIntl as render` aliased import — PATTERNS.md Pattern F)
    - tests/helpers/renderWithIntl.tsx (full — the wrapper is already built; no changes needed)
  </read_first>
  <behavior>
    - EntryBar.test.tsx renders inside a NextIntlClientProvider (via renderWithIntl) so `useTranslations` resolves against messages/en.json
    - Every existing assertion passes unchanged (the visible English text is identical to pre-externalization)
    - No test asserts on a now-translated string in a way that would break if the key changed — assertions target the same English copy
  </behavior>
  <action>
    Swap the bare `render` import for the aliased helper exactly as `nav-auth.test.tsx` does: replace `import { render, screen } from '@testing-library/react'` with `import { screen } from '@testing-library/react'` plus `import { renderWithIntl as render } from './helpers/renderWithIntl'`. Keep all `render(<EntryBar />)` call sites and every assertion byte-identical — the alias means no call-site edits. If any assertion queried a string that this plan moved to the catalog, leave the asserted English text as-is (it is unchanged). Do not add new test cases here.
  </action>
  <verify>
    <automated>npx vitest run tests/EntryBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "renderWithIntl" tests/EntryBar.test.tsx` returns >= 1
    - `npx vitest run tests/EntryBar.test.tsx` exits 0
    - `grep -c "from '@testing-library/react'" tests/EntryBar.test.tsx` returns 1 (screen still imported from testing-library; render now aliased)
  </acceptance_criteria>
  <done>EntryBar test suite green under renderWithIntl with zero assertion changes.</done>
</task>

<task type="auto">
  <name>Task 3: Re-sync 6 non-EN stub catalogs and prove no MISSING_MESSAGE</name>
  <files>messages/ru.json, messages/es.json, messages/fr.json, messages/ar.json, messages/hi.json, messages/zh.json</files>
  <read_first>
    - messages/en.json (the updated source-of-truth catalog after Task 1)
    - .planning/phases/69-string-externalization-ui-chrome/69-01-SUMMARY.md (the "6 EN-copy stub locale files re-synced after every catalog change" pattern — patterns-established block)
  </read_first>
  <action>
    Copy the updated `messages/en.json` over each of the 6 stub locales so every configured locale still renders EN copy with no MISSING_MESSAGE: `cp messages/en.json messages/ru.json && cp messages/en.json messages/es.json && cp messages/en.json messages/fr.json && cp messages/en.json messages/ar.json && cp messages/en.json messages/hi.json && cp messages/en.json messages/zh.json`. This is the locked Phase-69 stub-sync step (translation of non-EN locales is Phase 72/73, not now). Confirm the copies are byte-identical.
  </action>
  <verify>
    <automated>for l in ru es fr ar hi zh; do cmp -s messages/en.json messages/$l.json || { echo "DRIFT: $l"; exit 1; }; done; echo "all stubs in sync"</automated>
  </verify>
  <acceptance_criteria>
    - `cmp -s messages/en.json messages/ru.json && cmp -s messages/en.json messages/zh.json` exits 0 (all 6 stubs byte-identical to en.json)
    - `node -e "['en','ru','es','fr','ar','hi','zh'].forEach(l=>{const m=require('./messages/'+l+'.json'); if(!m.Booking.entryBar) throw new Error(l+' missing Booking.entryBar')}); console.log('ok')"` exits 0
  </acceptance_criteria>
  <done>All 7 catalogs carry the new Booking.entryBar/validation keys; stubs byte-identical to en.json.</done>
</task>

</tasks>

<artifacts_produced>
**New catalog namespaces (messages/en.json + 6 stubs):**
- `Booking.entryBar.*` — EntryBar labels, placeholders, aria-labels
- `Booking.validation.*` (seed) — shared client-side validation error strings (`originRequired`, `destinationRequired`, `dateRequired`, `timeRequired`, `returnDateRequired`, `returnTimeRequired`, `returnAfterPickup`); extended by later step plans

**Modified source:** `components/booking/EntryBar.tsx` (now consumes the catalog)
**Modified tests:** `tests/EntryBar.test.tsx` (render → renderWithIntl)
**Phase-level rollup (all Phase 70 plans, for downstream reference):** top-level namespaces this phase adds/extends are `Booking.*`, `Account.*`, `Auth.*`, and a shared `Errors.*`; a single-source `Booking.vehicleClasses` label map (70-04) replaces ~9 duplicated in-scope maps; no new shared source module beyond the catalog; new test file `tests/login-actions.test.ts` (70-02).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog → rendered UI | Translated strings are rendered as React text nodes (auto-escaped); `t.rich` chunks (none in this plan) would render markup |
| build pipeline → 7 locale subpaths | Each locale catalog must resolve every key or the build throws MISSING_MESSAGE |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-01-01 | Information Disclosure | messages/en.json | low | mitigate | Only visible EntryBar copy is moved; no secret, token, or PII enters the catalog (Task 1 action limits scope to UI literals; analytics params and store values excluded) |
| T-70-01-02 | Tampering | messages/{6 locales}.json | low | mitigate | Stub re-sync (Task 3) keeps every locale byte-identical to EN, preventing a stale/partial catalog rendering a blank or wrong string; cmp guard fails the task on drift |
| T-70-01-03 | Denial of Service | build (7 subpaths) | low | accept | A missing key surfaces deterministically as a build-time MISSING_MESSAGE, not a runtime outage; caught by the wave gate before ship |
</threat_model>

<verification>
- `npx vitest run tests/EntryBar.test.tsx` green
- All 7 catalogs parse as JSON; 6 stubs byte-identical to en.json (`cmp -s`)
- `grep -c "useTranslations('Booking" components/booking/EntryBar.tsx` >= 1; no hardcoded placeholder literals remain
- (wave-merge gate, run once after this wave) `npx vitest run` full suite green
</verification>

<success_criteria>
EntryBar is fully externalized via the Phase 69 convention, its test is green under renderWithIntl, English output is byte-for-byte unchanged, and all 6 stub locales are in sync — establishing the working end-to-end pattern the remaining Phase 70 plans expand from.
</success_criteria>

<output>
Create `.planning/phases/70-string-externalization-booking-account/70-01-SUMMARY.md` when done.
</output>
