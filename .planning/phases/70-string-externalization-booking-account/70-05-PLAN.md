---
phase: 70-string-externalization-booking-account
plan: 05
type: execute
wave: 5
depends_on: ["70-04"]
files_modified:
  - components/booking/PriceSummary.tsx
  - components/booking/BookingSummaryBlock.tsx
  - components/booking/steps/Step4Extras.tsx
  - components/booking/BookingWidget.tsx
  - components/booking/BookingWizard.tsx
  - messages/en.json
  - messages/ru.json
  - messages/es.json
  - messages/fr.json
  - messages/ar.json
  - messages/hi.json
  - messages/zh.json
  - tests/PriceSummary.test.tsx
  - tests/BookingSummaryBlock.test.tsx
  - tests/Step4Extras.test.tsx
  - tests/BookingWidget.test.tsx
  - tests/BookingWizard.test.tsx
autonomous: true
requirements: [STR-02]
user_setup: []

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "PriceSummary, BookingSummaryBlock, and Step4Extras render extras label/description from a Booking.extras.<key>.{label,description} catalog namespace while still reading key/price/alwaysSelected structurally from lib/extras.ts — lib/extras.ts itself is untouched"
    - "BookingWidget renders all its pick-up/drop-off/date/time/slot strings from Booking.bookingWidget"
    - "BookingWizard's adjacent UI text renders from the catalog and its vehicle-class label reads the shared Booking.vehicleClasses keys; GA4/Meta event names and params are unchanged"
    - "PriceSummary/BookingSummaryBlock/BookingWizard class labels reuse Booking.vehicleClasses (no new local label map)"
    - "Each touched test runs under renderWithIntl and stays green; English byte-for-byte unchanged; 6 stubs in sync"
  artifacts:
    - path: messages/en.json
      provides: "Booking.extras (label/description) + priceSummary + summaryBlock + bookingWidget + bookingWizard"
      contains: "\"extras\""
    - path: components/booking/Step4Extras.tsx
      provides: "extras label/description via catalog, structure via lib/extras.ts"
      contains: "extras."
  key_links:
    - from: components/booking/Step4Extras.tsx
      to: messages/en.json
      via: "iterate EXTRAS_CONFIG for key/price; render t(`extras.${key}.label`)/t(`extras.${key}.description`)"
      pattern: "extras\\.\\$\\{"
    - from: components/booking/BookingWizard.tsx
      to: messages/en.json
      via: "vehicle-class label reuses Booking.vehicleClasses from plan 70-04"
      pattern: "vehicleClasses"
  prohibitions:
    - statement: "Do NOT edit lib/extras.ts (shared with lib/email.ts server-side transactional emails)"
      status: prohibited
      verification: "git diff --name-only does not list lib/extras.ts"
    - statement: "Do NOT alter GA4/Meta event names or params in BookingWizard (e.g. begin_checkout, item_category, checkout_step)"
      status: prohibited
      verification: "grep for the analytics event identifiers in BookingWizard.tsx returns them unchanged"
    - statement: "Do NOT re-add a local vehicle-class label map; reuse Booking.vehicleClasses"
      status: prohibited
      verification: "grep -rn \"First Class\" components/booking/PriceSummary.tsx components/booking/BookingSummaryBlock.tsx components/booking/BookingWizard.tsx returns 0"
---

<objective>
Externalize the pricing/summary surface and the standalone widget/wizard shell: `PriceSummary`, `BookingSummaryBlock`, `Step4Extras` (all three consume `lib/extras.ts` `EXTRAS_CONFIG`), plus `BookingWidget` (the embeddable `/book` widget) and `BookingWizard` (the orchestrator). This plan creates the `Booking.extras.<key>.{label,description}` namespace as a READ-ONLY mirror of `EXTRAS_CONFIG`'s shape — `lib/extras.ts` stays untouched because it is also imported by `lib/email.ts` for transactional emails (RESEARCH Pitfall 1). Vehicle-class labels in these files reuse the `Booking.vehicleClasses` single source created in plan 70-04.

Purpose: Concentrating all three `EXTRAS_CONFIG` consumers in one plan guarantees the extras label/description are single-sourced without touching the email-coupled data file, and keeps the widget/wizard shell coherent.

Output: `Booking.extras`, `Booking.priceSummary`, `Booking.summaryBlock`, `Booking.bookingWidget`, `Booking.bookingWizard`; five converted files; migrated tests.
</objective>

<assumption_delta_decision>
- Assumption (RESEARCH Pitfall 1): `lib/extras.ts` `EXTRAS_CONFIG` feeds both UI and `lib/email.ts`; the label/description must be mirrored into the catalog for UI while the data file stays English-only for email. Enforced by a `git diff --name-only` prohibition on `lib/extras.ts`.
- Assumption (ENCODING probe): extras labels/descriptions and pricing-row copy are byte-for-byte unchanged; the catalog `Booking.extras` keys mirror the exact `EXTRAS_CONFIG` keys (`meetAndGreet`, `infantSeat`, `childSeat`, `boosterSeat` or whatever the file defines — Task 1 confirms the actual keys before authoring).
- Assumption: BookingWizard's translatable copy is limited to a small amount of adjacent UI text plus a vehicle-class label; its analytics event firing (GA4/Meta) is NOT copy and is excluded.
- Assumption: Phase 69 convention LOCKED and inherited; no checkpoint.
- Depends on 70-04: `Booking.vehicleClasses` must already exist (it does, since 70-04 is an earlier wave).
</assumption_delta_decision>

<ai_integration>
No external API integration in this plan. BookingWizard's analytics dispatch is pre-existing browser instrumentation (GA4/Meta), untouched. (schema-gate: no DB schema change.)
</ai_integration>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-string-externalization-booking-account/70-RESEARCH.md
@.planning/phases/70-string-externalization-booking-account/70-PATTERNS.md

# Analogs + the coupled data file (read to know the keys, do NOT edit it):
@components/Nav.tsx
@lib/extras.ts
@messages/en.json
@tests/nav-auth.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Booking.extras namespace + convert the 3 EXTRAS_CONFIG consumers (lib/extras.ts untouched)</name>
  <files>components/booking/PriceSummary.tsx, components/booking/BookingSummaryBlock.tsx, components/booking/steps/Step4Extras.tsx, messages/en.json</files>
  <read_first>
    - lib/extras.ts (READ ONLY — note the exact `EXTRAS_CONFIG` array shape and each entry's `key`, `label`, `description`, `price`, `alwaysSelected`; do NOT modify this file)
    - components/booking/Step4Extras.tsx (renders EXTRAS_CONFIG label/description with no hardcoded JSX of its own)
    - components/booking/PriceSummary.tsx (renders EXTRAS_CONFIG.label at ~line consumers; has a local vehicle-class label map at ~42-43 to REMOVE and source from Booking.vehicleClasses)
    - components/booking/BookingSummaryBlock.tsx (renders EXTRAS_CONFIG; Outbound/Subtotal/Final labels; local class-label map at ~10-11 to REMOVE)
    - .planning/phases/70-string-externalization-booking-account/70-RESEARCH.md lines 309-313 (Pitfall 1 email coupling) and 70-PATTERNS.md lines 71-92 (Pattern B extras)
  </read_first>
  <action>
    Add `Booking.extras` to the catalog as one object per extra, keyed by the SAME key `EXTRAS_CONFIG` uses, each with `label` and `description` copied byte-for-byte from `lib/extras.ts`. In `Step4Extras.tsx`, `PriceSummary.tsx`, and `BookingSummaryBlock.tsx`: keep iterating `EXTRAS_CONFIG` for `key`/`price`/`alwaysSelected` exactly as today, but render the label as `t(\`extras.${key}.label\`)` and the description as `t(\`extras.${key}.description\`)` (add `useTranslations('Booking')` or the appropriate sub-namespace hook). Replace the remaining visible strings in `PriceSummary` (Outbound/Combined row labels → `Booking.priceSummary.*`) and `BookingSummaryBlock` (Outbound/Subtotal/Final → `Booking.summaryBlock.*`) with catalog keys. Remove the local vehicle-class label maps from `PriceSummary` and `BookingSummaryBlock` and source the label from `Booking.vehicleClasses` (created in 70-04). Do NOT edit `lib/extras.ts`.
  </action>
  <verify>
    <automated>node -e "const m=require('./messages/en.json'); if(!m.Booking.extras||!m.Booking.priceSummary||!m.Booking.summaryBlock) throw new Error('missing extras/priceSummary/summaryBlock'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only lib/extras.ts` produces no output (file untouched)
    - `grep -c "extras.\${" components/booking/Step4Extras.tsx` returns >= 1 (label/description sourced by key)
    - `grep -rn "First Class" components/booking/PriceSummary.tsx components/booking/BookingSummaryBlock.tsx` returns 0
    - `node -e "const {EXTRAS_CONFIG}=require('./lib/extras.ts'); const m=require('./messages/en.json'); EXTRAS_CONFIG.forEach(e=>{if(!m.Booking.extras[e.key]) throw new Error('catalog missing extra '+e.key)}); console.log('ok')" || echo "NOTE: if ts require fails, assert key parity via a vitest instead"</automated></acceptance_criteria>
  <done>All three extras consumers render label/description from the catalog by key; lib/extras.ts untouched; class labels reuse vehicleClasses.</done>
</task>

<task type="auto">
  <name>Task 2: Externalize BookingWidget + BookingWizard (analytics untouched)</name>
  <files>components/booking/BookingWidget.tsx, components/booking/BookingWizard.tsx, messages/en.json</files>
  <read_first>
    - components/booking/BookingWidget.tsx (~17 strings: pick-up/drop-off placeholders, pickup date/time aria, "Choose a slot")
    - components/booking/BookingWizard.tsx (orchestrator; GA4/Meta event firing — do NOT touch event names/params; local vehicle-class label map at ~124-125 to REMOVE)
    - components/Nav.tsx lines 1-8, 24-26 (useTranslations analog)
    - messages/en.json (Booking.vehicleClasses from 70-04)
  </read_first>
  <action>
    Add `useTranslations('Booking.bookingWidget')` to `BookingWidget.tsx` and replace every placeholder/aria/label with `t('<key>')`; add the `Booking.bookingWidget` namespace copying the current literals. In `BookingWizard.tsx`, add `useTranslations('Booking.bookingWizard')` for the adjacent UI text, remove the local vehicle-class label map and source the label from `Booking.vehicleClasses`, and add the `Booking.bookingWizard` namespace. Leave every analytics call — event name strings, `checkout_step`, `item_category`, `currency`, and any dataLayer/gtag/fbq param — exactly as-is; only user-visible copy changes.
  </action>
  <verify>
    <automated>node -e "const m=require('./messages/en.json'); if(!m.Booking.bookingWidget||!m.Booking.bookingWizard) throw new Error('missing bookingWidget/bookingWizard'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslations('Booking.bookingWidget')" components/booking/BookingWidget.tsx` returns >= 1
    - `grep -c "useTranslations('Booking.bookingWizard')" components/booking/BookingWizard.tsx` returns >= 1
    - `grep -rn "First Class" components/booking/BookingWizard.tsx` returns 0
    - `grep -c "begin_checkout" components/booking/BookingWizard.tsx` is unchanged from before (analytics event name preserved)
  </acceptance_criteria>
  <done>BookingWidget + BookingWizard render UI copy from the catalog with analytics params untouched; class label reuses vehicleClasses.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate the 5 touched tests to renderWithIntl, re-sync stubs, run the slice</name>
  <files>tests/PriceSummary.test.tsx, tests/BookingSummaryBlock.test.tsx, tests/Step4Extras.test.tsx, tests/BookingWidget.test.tsx, tests/BookingWizard.test.tsx, messages/ru.json, messages/es.json, messages/fr.json, messages/ar.json, messages/hi.json, messages/zh.json</files>
  <read_first>
    - tests/PriceSummary.test.tsx, tests/BookingSummaryBlock.test.tsx, tests/Step4Extras.test.tsx, tests/BookingWidget.test.tsx, tests/BookingWizard.test.tsx (the files being modified — bare `render` imports)
    - tests/nav-auth.test.tsx lines 1-15 (aliased renderWithIntl import)
  </read_first>
  <action>
    Swap the bare-`render` import for the aliased `renderWithIntl as render` in each of the 5 test files, leaving call sites and assertions unchanged. Re-sync the 6 stub locales (`cp messages/en.json messages/{ru,es,fr,ar,hi,zh}.json`) and run the touched-test slice.
  </action>
  <verify>
    <automated>for l in ru es fr ar hi zh; do cmp -s messages/en.json messages/$l.json || { echo "DRIFT: $l"; exit 1; }; done; npx vitest run tests/PriceSummary.test.tsx tests/BookingSummaryBlock.test.tsx tests/Step4Extras.test.tsx tests/BookingWidget.test.tsx tests/BookingWizard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - each of the 5 test files: `grep -c "renderWithIntl" <file>` returns >= 1
    - `npx vitest run` over the 5 files exits 0
    - all 6 `cmp -s` stub checks succeed
  </acceptance_criteria>
  <done>The 5 tests are green under renderWithIntl; stubs in sync.</done>
</task>

</tasks>

<artifacts_produced>
**New catalog namespaces:** `Booking.extras` (label/description mirror of EXTRAS_CONFIG, read-only), `Booking.priceSummary`, `Booking.summaryBlock`, `Booking.bookingWidget`, `Booking.bookingWizard`
**Modified source:** PriceSummary, BookingSummaryBlock, Step4Extras, BookingWidget, BookingWizard (local vehicle-label maps removed; reuse Booking.vehicleClasses)
**Modified tests:** PriceSummary, BookingSummaryBlock, Step4Extras, BookingWidget, BookingWizard
**Untouched by design:** `lib/extras.ts` (email-coupled)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EXTRAS_CONFIG (data) → UI vs email | The same data array feeds translatable UI and English-only email; they must diverge without editing the shared file |
| analytics params → GA4/Meta | Event identifiers are a third-party contract, not user copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-05-01 | Tampering | lib/extras.ts email coupling | medium | mitigate | UI reads label/description from the catalog by key; the data file is asserted untouched (git diff empty), so transactional emails keep their English copy |
| T-70-05-02 | Tampering | GA4/Meta event params | low | mitigate | Only visible copy changes; analytics event-name grep asserts the identifiers are unchanged, preserving reporting continuity |
| T-70-05-03 | Information Disclosure | messages/en.json | low | mitigate | Only visible extras/pricing copy moves; pricing numbers and config flags stay in code; no PII |
</threat_model>

<verification>
- `npx vitest run` over the 5 touched tests green
- `git diff --name-only lib/extras.ts` empty; extras rendered by key from the catalog
- residual vehicle-label grep returns 0 in the 3 files that had a local map
- analytics event-name grep unchanged
- 6 stubs byte-identical to en.json
- (wave-merge gate) `npx vitest run` full suite green
</verification>

<success_criteria>
Extras label/description are single-sourced in the catalog without touching the email-coupled data file, the widget/wizard shell renders UI copy from the catalog with analytics untouched, class labels reuse the shared vehicleClasses source, tests are green under renderWithIntl, English is byte-for-byte unchanged, and locales are in sync.
</success_criteria>

<output>
Create `.planning/phases/70-string-externalization-booking-account/70-05-SUMMARY.md` when done.
</output>
