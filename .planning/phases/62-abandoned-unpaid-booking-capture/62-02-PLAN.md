---
phase: 62-abandoned-unpaid-booking-capture
plan: 02
type: execute
wave: 2
depends_on: [62-01]
files_modified:
  - lib/booking-store.ts
  - components/booking/steps/Step6Payment.tsx
  - app/api/create-payment-intent/route.ts
  - app/api/webhooks/stripe/route.ts
  - lib/supabase.ts
  - tests/create-payment-intent.test.ts
  - tests/webhooks-stripe.test.ts
autonomous: true
requirements: [ABND-01, ABND-06]
user_setup: []

estimate:
  tokens: 74000
  raw_tokens: 49000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A stable client-generated attempt_id (crypto.randomUUID, held in the Zustand store, sessionStorage-backed) is sent to create-payment-intent and used as the dedup key (D-06)"
    - "A retry / currency-toggle re-POST with the same attempt_id UPDATEs the existing unpaid row in place — overwriting payment_intent_id, booking_reference, amounts and promo — instead of inserting a second row (D-06, ABND-06)"
    - "A round-trip attempt persists TWO unpaid rows (leg='outbound' and leg='return'), sharing the attempt, keyed per leg (D-07, ABND-01)"
    - "payment_intent.succeeded on a round-trip reconciles BOTH unpaid legs to confirmed atomically and fires the round-trip side-effects exactly once for the pair; redelivery fires none (D-11, ABND-06)"
    - "A late retry that arrives after the row is already confirmed does not create a second unpaid row (capture skips when a confirmed row exists for the attempt)"
  prohibitions:
    - "reconciliation must never create a second bookings row for one attempt"
    - "a retry must never leave the admin-visible unpaid row showing a stale booking_reference or amount that disagrees with the active PaymentIntent"
  artifacts:
    - lib/booking-store.ts::attemptId
    - lib/supabase.ts::reconcileRoundTripToConfirmed
  key_links:
    - "capture UPSERT conflict target is (attempt_id, leg) WHERE status='unpaid'; the row's payment_intent_id is rewritten to the current PI on every retry"
    - "round-trip reconcile UPDATE matches both legs by shared payment_intent_id AND status='unpaid'"
---

<objective>
Expand the proven one-way tracer to full capture coverage: introduce the `attempt_id`
dedup anchor (D-06) so retries/currency toggles update ONE unpaid row in place instead
of polluting the follow-up queue, and extend both capture and reconciliation to the
round-trip two-leg shape (D-07).

Purpose: the operator's unpaid queue must be clean (one row per attempt/leg) and every
paid attempt — one-way or round-trip — must reconcile to confirmed with side-effects
firing exactly once.
Output: attempt_id plumbing (store + Step6Payment), attempt-keyed UPSERT-in-place capture,
round-trip capture (2 rows), and round-trip reconcile.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-abandoned-unpaid-booking-capture/62-CONTEXT.md
@.planning/phases/62-abandoned-unpaid-booking-capture/62-RESEARCH.md
@.planning/phases/62-abandoned-unpaid-booking-capture/62-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: attempt_id dedup — client plumbing + attempt-keyed UPSERT-in-place capture</name>
  <files>lib/booking-store.ts, components/booking/steps/Step6Payment.tsx, app/api/create-payment-intent/route.ts, lib/supabase.ts, tests/create-payment-intent.test.ts</files>
  <read_first>
    - lib/booking-store.ts (store shape; crypto.randomUUID precedent ~line 75; partialize ~line 147; resetBooking ~line 114)
    - components/booking/steps/Step6Payment.tsx (useEffect fetch to create-payment-intent ~line 287-348; dependency array ~line 348)
    - app/api/create-payment-intent/route.ts (createPaymentIntentSchema ~line 47-67; capture write added in 62-01; response ~line 327)
    - lib/supabase.ts (buildBookingRow, saveBooking, reconcileBookingToConfirmed)
    - .planning/phases/62-abandoned-unpaid-booking-capture/62-RESEARCH.md (Pitfall 3 — retry regenerates PI AND booking_reference; Open Question 3 — SELECT-then-INSERT-or-UPDATE; Security §V5/V6 — validate attempt_id as UUID)
  </read_first>
  <behavior>
    - The store exposes `attemptId: string | null` with a setter; a fresh attemptId is generated once (crypto.randomUUID) when checkout reaches payment and is persisted via the sessionStorage partialize; resetBooking clears it.
    - Step6Payment sends `attemptId` in the create-payment-intent request body.
    - create-payment-intent validates attemptId as a UUID in createPaymentIntentSchema and uses it as the capture key.
    - First POST for an attempt inserts one unpaid row; a second POST with the same attemptId (new PI, new bookingReference) updates the SAME row in place — payment_intent_id, booking_reference, amount_czk/amount_eur, promo all overwritten — and does NOT insert a second row.
    - If a confirmed row already exists for (attempt_id, leg), capture is a no-op (no new unpaid row).
  </behavior>
  <action>
In `lib/booking-store.ts` add `attemptId: string | null` (initial null), a `setAttemptId(id)` setter, include `attemptId` in the `partialize` object (so it survives the 3DS redirect / same-tab reload like promoCode does), and reset it to null in `resetBooking`. Add the field + setter to the `BookingStore` type in `types/booking.ts` if the store type is declared there (follow the existing setter typing — check where BookingStore is defined). Generate the id lazily: in Step6Payment, on first mount where payment is reached, if `attemptId` is null call `setAttemptId(crypto.randomUUID())`.

In `components/booking/steps/Step6Payment.tsx` read `attemptId` (and `setAttemptId`) from the store, ensure it is generated before the fetch, and add `attemptId` to the JSON body sent to `/api/create-payment-intent`. Add `attemptId` to the useEffect dependency array only if needed (it is stable per attempt, so it need not trigger a refetch).

In `app/api/create-payment-intent/route.ts` add `attemptId: z.string().uuid().optional()` to `bookingData` in `createPaymentIntentSchema` (V5 input validation). Replace the one-way capture from 62-01 with attempt-keyed capture: when `attemptId` is present, SELECT the existing row by `(attempt_id, leg)`; if none → INSERT a fresh unpaid row carrying `attempt_id`; if the existing row is status='unpaid' → UPDATE it in place overwriting the full mutable field set (payment_intent_id, booking_reference, amounts, promo, and all trip/PII fields from buildBookingRow) while pinning status='unpaid'; if the existing row is status='confirmed' → skip (no-op). Implement this as an exported helper in `lib/supabase.ts` (e.g. `captureUnpaidBooking(row, attemptId, leg)`) so the route stays thin and the logic is unit-testable. Keep capture best-effort (try/catch, never 500 the payment path). When attemptId is absent, retain the 62-01 payment_intent_id-keyed insert as fallback.

Extend `tests/create-payment-intent.test.ts`: assert (a) a POST with a new attemptId inserts one unpaid row carrying that attempt_id; (b) a second POST with the SAME attemptId issues an UPDATE (not a second insert) that overwrites payment_intent_id and booking_reference; (c) attemptId that is not a valid UUID yields HTTP 400. Mock the SELECT/UPDATE/INSERT chain on the supabase stub following the existing vi.hoisted pattern.
  </action>
  <verify>
    <automated>npx vitest run tests/create-payment-intent.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `attemptId` field + setter present in lib/booking-store.ts and included in partialize and cleared in resetBooking.
    - createPaymentIntentSchema validates attemptId with `z.string().uuid()`; a malformed value returns 400 (asserted by test).
    - Vitest: same-attemptId re-POST performs an UPDATE targeting the existing unpaid row (single row) and overwrites payment_intent_id + booking_reference; no second insert.
    - Capture no-ops when a confirmed row already exists for (attempt_id, leg).
  </acceptance_criteria>
  <done>attempt_id is generated client-side, persisted, and threaded to the server; capture dedups in place on retry/currency-toggle and never duplicates the unpaid queue; create-payment-intent suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Round-trip capture (2 legs) + round-trip reconcile (both legs, side-effects once)</name>
  <files>app/api/create-payment-intent/route.ts, app/api/webhooks/stripe/route.ts, lib/supabase.ts, tests/create-payment-intent.test.ts, tests/webhooks-stripe.test.ts</files>
  <read_first>
    - app/api/create-payment-intent/route.ts (round_trip branch ~line 127-215; returnBookingReference ~line 269; metadata ~line 284-325)
    - app/api/webhooks/stripe/route.ts (handleRoundTripSucceeded ~line 249-386; buildBookingRows/saveRoundTripBookings usage)
    - lib/supabase.ts (buildBookingRows ~line 153; saveRoundTripBookings ~line 249; reconcileBookingToConfirmed from 62-01)
    - .planning/phases/62-abandoned-unpaid-booking-capture/62-CONTEXT.md (D-07, D-11 point 4)
    - .planning/phases/62-abandoned-unpaid-booking-capture/62-RESEARCH.md (Pattern 2 extends to round-trip; both legs share one payment_intent_id)
  </read_first>
  <behavior>
    - A round-trip POST persists two unpaid rows: leg='outbound' and leg='return', sharing attempt_id, both status='unpaid', both carrying the single shared payment_intent_id.
    - Retry with the same attemptId updates BOTH existing unpaid legs in place (no duplicate legs).
    - payment_intent.succeeded for a round-trip PI flips both unpaid legs to confirmed in one atomic UPDATE and fires sendRoundTripClientConfirmation, sendRoundTripManagerAlert, GA4 purchase, and per-leg QStash reminders exactly once.
    - Redelivery (both legs already confirmed) fires zero side-effects and creates zero rows.
  </behavior>
  <action>
In `app/api/create-payment-intent/route.ts`, for the round_trip branch, capture two unpaid rows using the attempt-keyed helper from Task 1 once per leg: build outbound + return rows via the existing `buildBookingRows(meta, paymentIntent.id)` shape but with status pinned to 'unpaid' (reuse buildBookingRow('unpaid') for outbound and the return-row builder logic — extend buildBookingRows or add a status-aware variant so the two returned rows carry status 'unpaid' and attempt_id). Both rows share the single `paymentIntent.id`; dedup key is (attempt_id, leg). Keep best-effort try/catch.

In `lib/supabase.ts` add and export `reconcileRoundTripToConfirmed(paymentIntentId)`: `update({ status: 'confirmed' }).eq('payment_intent_id', paymentIntentId).eq('status', 'unpaid').select('id')` — this matches BOTH legs (they share the PI) in one atomic statement; return the rows (0, 1, or 2). Reuse the same "empty = already handled" gate contract as the one-way helper.

In `app/api/webhooks/stripe/route.ts` rewrite `handleRoundTripSucceeded` so it calls `reconcileRoundTripToConfirmed(paymentIntent.id)` (via withRetry) first: a non-empty result is the fresh transition → fire the round-trip side-effects once and schedule QStash reminders for the returned leg ids. On an empty result, fall back to the existing `saveRoundTripBookings(outboundRow, returnRow)` path (defensive fresh insert when capture was lost → side-effects once; null/duplicate → skip). Keep the promo claim, RoundTripEmailData construction, ICS build, and GA4 blocks as they are, driven off the combined gate. Preserve stripe_processed_events ordering.

Extend `tests/create-payment-intent.test.ts` (round-trip capture → two unpaid rows, legs outbound+return, shared PI) and `tests/webhooks-stripe.test.ts` (round-trip reconcile → both legs updated, round-trip side-effect mocks each called once; redelivery → zero). Add `reconcileRoundTripToConfirmed` to the `@/lib/supabase` mock.
  </action>
  <verify>
    <automated>npx vitest run tests/create-payment-intent.test.ts tests/webhooks-stripe.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Round-trip POST captures exactly two unpaid rows (leg='outbound' and leg='return') sharing one payment_intent_id (asserted by test).
    - `reconcileRoundTripToConfirmed` UPDATE is scoped by `.eq('payment_intent_id', ...).eq('status','unpaid')` and returns the flipped leg ids.
    - Vitest: round-trip reconcile fires sendRoundTripClientConfirmation + sendRoundTripManagerAlert + GA4 + QStash exactly once; redelivery fires zero.
    - Lost-capture round-trip falls back to saveRoundTripBookings and still fires side-effects once.
  </acceptance_criteria>
  <done>Round-trip attempts capture two unpaid legs and reconcile both to confirmed atomically with side-effects firing once; retries dedup per leg; both Vitest suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → create-payment-intent | client now supplies `attempt_id`, used as a DB write key |
| Stripe → webhook | round-trip reconciliation UPDATE across two legs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-62-01 | Tampering/EoP (IDOR) | attempt_id-keyed capture UPDATE | high | mitigate | UPDATE scoped `WHERE attempt_id=$1 AND leg=$2 AND status='unpaid'` — a guessed/stolen attempt_id cannot mutate a `confirmed` row (status guard) and cannot exfiltrate data (route returns only clientSecret + references, never the row). attempt_id validated as a well-formed UUID in createPaymentIntentSchema before any query. Generated with crypto.randomUUID (not a secret, uniqueness only). |
| T-62-02 | Denial of Service | flood of distinct attempt_ids polluting the unpaid queue | medium | mitigate | Existing checkRateLimit on /api/create-payment-intent applies unchanged; note the raised per-request cost (each distinct attempt now writes rows). No auto-expire in this phase (D-10) is an accepted follow-up (FOLLOW-01). |
| T-62-03 | Spoofing | webhook round-trip reconcile | high | mitigate | constructEvent signature verification unchanged gates reconciliation. |
| T-62-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase. |
</threat_model>

<verification>
- `npx vitest run tests/create-payment-intent.test.ts` (attempt_id dedup + round-trip capture)
- `npx vitest run tests/webhooks-stripe.test.ts` (round-trip reconcile + idempotency)
- `npx vitest run` (full suite green after the wave)
</verification>

<success_criteria>
- Retry/currency-toggle keeps ONE unpaid row per attempt/leg (no queue pollution) (D-06).
- Round-trip attempts capture two unpaid legs and reconcile both to confirmed with side-effects once (D-07, D-11, ABND-01/06).
- Late retry after confirmation creates no duplicate; both Vitest suites green.
</success_criteria>

<output>
Create `.planning/phases/62-abandoned-unpaid-booking-capture/62-02-SUMMARY.md` when done.
</output>
