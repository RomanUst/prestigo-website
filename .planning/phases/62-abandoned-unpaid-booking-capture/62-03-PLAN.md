---
phase: 62-abandoned-unpaid-booking-capture
plan: 03
type: execute
wave: 2
depends_on: [62-01]
files_modified:
  - components/admin/StatusBadge.tsx
  - components/admin/BookingsTable.tsx
  - lib/booking-transitions.ts
  - app/api/admin/bookings/route.ts
  - tests/BookingsTable.test.tsx
  - tests/admin-bookings.test.ts
autonomous: true
requirements: [ABND-03, ABND-04]
user_setup: []

estimate:
  tokens: 66000
  raw_tokens: 44000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "StatusBadge renders an `unpaid` variant labelled 'Unpaid' with a distinct amber/red warning hex not used by the other 7 variants (D-03, ABND-03)"
    - "In the default 'All' admin view, unpaid rows appear inline among confirmed rows, distinguished by the Unpaid badge and a light amber row tint (D-09, ABND-03)"
    - "An 'Unpaid' filter chip alongside the trip-type chips filters the list to status='unpaid'; it is a second, independent filter dimension that coexists with the trip-type chips (D-08, ABND-04)"
    - "GET /api/admin/bookings?status=unpaid threads a p_status argument to the admin_search_bookings RPC call (D-08, ABND-04)"
    - "Admin PATCH accepts the transitions unpaid→confirmed and unpaid→cancelled and rejects any other transition out of unpaid; the unpaid entry exists in BOTH the API-route inline VALID_TRANSITIONS and lib/booking-transitions VALID_TRANSITIONS (D-04, D-10)"
    - "An empty unpaid queue renders the standard 'No bookings found' state without error (edge: empty result)"
  prohibitions:
    - "an unpaid row must never be silently hidden from the default 'All' view (the operator must not miss it)"
    - "the Unpaid filter must never mix in `pending` (admin-created) rows — `pending` and `unpaid` are distinct statuses (RESEARCH Pitfall 5)"
  artifacts:
    - "components/admin/StatusBadge.tsx unpaid variant"
    - "components/admin/BookingsTable.tsx Unpaid chip + statusFilter + row tint"
  key_links:
    - "BookingsTable statusFilter state → fetchBookings `status` query param → GET handler → RPC p_status"
    - "unpaid entry present in api-route VALID_TRANSITIONS (server enforcement) AND lib/booking-transitions VALID_TRANSITIONS (source of UI_TRANSITIONS dropdown)"
---

<objective>
Build the admin follow-up surface for the `unpaid` status proven by the tracer: a
distinct badge + row tint so operators cannot miss an unpaid booking (D-03/D-09), an
Unpaid filter chip to work the queue (D-08), and the unpaid→confirmed/unpaid→cancelled
transitions wired through both status maps (D-04/D-10).

Purpose: turn captured unpaid rows into a usable revenue-recovery queue.
Output: StatusBadge variant, BookingsTable chip + tint + badge casts, GET p_status
threading, PATCH Zod value + transitions, and the transition-map edits.
Note: the live `admin_search_bookings` RPC gains the `p_status` PARAMETER in migration
054 (authored + applied in 62-04); this plan wires the GET handler to PASS `p_status`
(the RPC is mocked in tests) so the code and migration meet at the [BLOCKING] apply.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-abandoned-unpaid-booking-capture/62-CONTEXT.md
@.planning/phases/62-abandoned-unpaid-booking-capture/62-RESEARCH.md
@.planning/milestones/v2.0-phases/52-extended-booking-statuses/52-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StatusBadge unpaid variant + BookingsTable badge, label, and 'All'-view row tint</name>
  <files>components/admin/StatusBadge.tsx, components/admin/BookingsTable.tsx, tests/BookingsTable.test.tsx</files>
  <read_first>
    - components/admin/StatusBadge.tsx (variant union ~line 4; variantStyles hex map ~line 8-19)
    - components/admin/BookingsTable.tsx (STATUS_LABELS ~line 73-81; StatusBadge cast sites ~line 461-463, 770-772, 1247-1249; desktop row style ~line 1071-1082; mobile card style ~line 695-699)
    - tests/BookingsTable.test.tsx (render harness, mock booking shape ~line 40-105)
    - .planning/milestones/v2.0-phases/52-extended-booking-statuses/52-CONTEXT.md (StatusBadge variant conventions)
  </read_first>
  <behavior>
    - StatusBadge with variant='unpaid' and label='Unpaid' renders with a warning colour distinct from the existing 7 status variants.
    - A booking whose status is 'unpaid' shows the Unpaid badge in the list and detail views.
    - In the default 'All' view an unpaid row carries a light amber tint distinguishing it from confirmed rows, while hover/expanded styling still works.
    - STATUS_LABELS maps 'unpaid' → 'Unpaid'.
  </behavior>
  <action>
In `components/admin/StatusBadge.tsx` add `'unpaid'` to the `variant` union and add a `unpaid` entry to `variantStyles` with a distinct warning palette — use bg `#3a1a12`, color `#f59e0b`, border `1px solid rgba(245,158,11,0.3)` (an amber-on-dark tone not equal to `pending` #fb923c or `on_location` #fbbf24). (D-03; exact hex is Claude's-discretion per CONTEXT — this value satisfies "clearly reads not-paid and differs from the other 7".)

In `components/admin/BookingsTable.tsx`: add `unpaid: 'Unpaid'` to `STATUS_LABELS`; extend the three `StatusBadge` variant casts (the desktop status column ~461, the mobile card ~770, the detail panel ~1247) to include `'unpaid'` in the cast union so an unpaid status renders its badge. Add a light row tint for unpaid rows: in the desktop `<tr>` style (~1076) and the mobile card background (~698), when `row.original.status === 'unpaid'` (desktop) / `booking.status === 'unpaid'` (mobile) apply a background of `rgba(245,158,11,0.06)` (falling through to the existing hover/expanded colour when hovered/expanded so interaction is preserved). Do not hide unpaid rows in any view (D-09).

Extend `tests/BookingsTable.test.tsx`: add a booking fixture with status 'unpaid' and assert the rendered list shows the 'Unpaid' label. If a focused StatusBadge assertion is easier, add a small render test that StatusBadge variant='unpaid' produces the amber color; keep to the existing render harness.
  </action>
  <verify>
    <automated>npx vitest run tests/BookingsTable.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "unpaid" components/admin/StatusBadge.tsx` shows the variant union member and the variantStyles entry.
    - The unpaid hex (`#f59e0b`) is not equal to any existing variant `color` value in variantStyles.
    - STATUS_LABELS has `unpaid: 'Unpaid'` and all three badge cast unions include `'unpaid'`.
    - Vitest: a status='unpaid' booking renders the 'Unpaid' label; the render test passes.
  </acceptance_criteria>
  <done>The unpaid status renders a distinct amber 'Unpaid' badge everywhere it appears, unpaid rows are tinted inline in the 'All' view, and the BookingsTable suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unpaid filter chip + GET p_status threading + PATCH unpaid transitions in both maps</name>
  <files>components/admin/BookingsTable.tsx, app/api/admin/bookings/route.ts, lib/booking-transitions.ts, tests/admin-bookings.test.ts</files>
  <read_first>
    - components/admin/BookingsTable.tsx (filterChips ~line 501-506; handleFilterChange ~line 333; fetchBookings params ~line 275-319; chip render ~line 642-665)
    - app/api/admin/bookings/route.ts (GET searchParams + RPC call ~line 51-93; inline VALID_TRANSITIONS ~line 17-25; bookingPatchSchema status enum ~line 27-44)
    - lib/booking-transitions.ts (VALID_TRANSITIONS ~line 11-19; UI_TRANSITIONS spread ~line 23-27)
    - tests/admin-bookings.test.ts (GET rpc-param assertions Test 4-7 ~line 210-262; PATCH transition tests ~line 266-328)
    - .planning/phases/62-abandoned-unpaid-booking-capture/62-RESEARCH.md (Pattern 3 — THREE maps; edit map #1 in route.ts and map #2 in booking-transitions; Pitfall 5 — keep unpaid distinct from pending)
  </read_first>
  <behavior>
    - A new 'Unpaid' chip sits alongside All/Transfer/Hourly/Daily; it is a SEPARATE status dimension (its own state), not a trip-type value, so it coexists with the trip-type selection.
    - Selecting the Unpaid chip sends `status=unpaid` in the fetchBookings query; the GET handler reads `status` and passes `p_status` to the RPC; deselecting/'All' passes p_status=null.
    - Admin PATCH: bookingPatchSchema accepts 'unpaid'; unpaid→confirmed and unpaid→cancelled succeed (200); unpaid→anything-else and non-unpaid→unpaid are rejected (422).
    - The unpaid transition row exists in both the api-route inline VALID_TRANSITIONS and lib/booking-transitions VALID_TRANSITIONS (which feeds UI_TRANSITIONS).
  </behavior>
  <action>
In `app/api/admin/bookings/route.ts` GET: read `const statusFilter = searchParams.get('status')`, bound/whitelist it (accept only known status strings), and add `p_status: statusFilter ?? null` to the `admin_search_bookings` rpc argument object (D-08). Add `'unpaid'` to `bookingPatchSchema`'s status `z.enum([...])`. Add `unpaid: ['confirmed', 'cancelled']` to the inline `VALID_TRANSITIONS` map so the server actually enforces it (D-04). Do NOT add any transition INTO unpaid from other statuses (unpaid is only reached by the capture write, never by a manual PATCH). Leave the confirmed/cancelled email + QStash side-effect logic untouched — an unpaid→confirmed PATCH is the manual-safety path; the automatic path is the webhook.

In `lib/booking-transitions.ts` add `unpaid: ['confirmed', 'cancelled']` to `VALID_TRANSITIONS`; it flows into `UI_TRANSITIONS` via the existing spread (no override needed since the admin-dropdown transitions equal the API transitions), so the BookingsTable status dropdown offers Confirmed/Cancelled from an unpaid row (D-04, D-10 manual disposition).

In `components/admin/BookingsTable.tsx` add a `statusFilter` state (`'all' | 'unpaid'`, default 'all'); render an 'Unpaid' chip after the trip-type chips whose active styling reflects `statusFilter === 'unpaid'` and whose onClick toggles statusFilter via handleFilterChange(setStatusFilter) between 'unpaid' and 'all' (resetting page). In `fetchBookings`, when `statusFilter !== 'all'` set `params.set('status', statusFilter)`, and add statusFilter to the useCallback dependency array so the list refetches when it changes. Keep the trip-type chips driving `tripType` independently (the two dimensions coexist).

Extend `tests/admin-bookings.test.ts`: add a GET test asserting `status=unpaid` passes `expect.objectContaining({ p_status: 'unpaid' })` to the `admin_search_bookings` rpc mock (mirroring Test 6's p_trip_type assertion); add PATCH tests asserting unpaid→confirmed returns 200 and unpaid→completed returns 422 (seed the fetched current row with status 'unpaid' as Test 3/4 do).
  </action>
  <verify>
    <automated>npx vitest run tests/admin-bookings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - GET with `?status=unpaid` calls the rpc mock with `p_status: 'unpaid'`; without it, `p_status: null` (asserted by test).
    - bookingPatchSchema status enum includes `'unpaid'`; inline VALID_TRANSITIONS has `unpaid: ['confirmed','cancelled']`.
    - `grep -n "unpaid" lib/booking-transitions.ts` shows the VALID_TRANSITIONS entry.
    - Vitest: unpaid→confirmed → 200; unpaid→completed → 422.
    - An 'Unpaid' chip is present in filterChips rendering and drives a `status` query param independent of the trip-type chips.
  </acceptance_criteria>
  <done>Operator can filter to unpaid via a dedicated chip (threaded to p_status), and can manually move an unpaid booking to confirmed or cancelled; both transition maps carry the unpaid entry; admin-bookings suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → GET/PATCH /api/admin/bookings | authenticated operator input (status filter, transition) |
| GET handler → admin_search_bookings RPC | new p_status parameter bound positionally |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-62-04 | Access Control (V4) | GET/PATCH p_status + unpaid transitions | medium | mitigate | All handlers remain behind the existing getAdminUser() 401/403 guard (unchanged); p_status is whitelisted to known status strings before binding, and bound positionally by the RPC (no filter-string injection). |
| T-62-05 | Tampering | PATCH status transition into/out of unpaid | medium | mitigate | Server inline VALID_TRANSITIONS enforces unpaid→{confirmed,cancelled} only; no transition INTO unpaid is permitted from a manual PATCH (unpaid is set solely by the capture write). Double-gated with lib/booking-transitions for the UI. |
| T-62-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase. |
</threat_model>

<verification>
- `npx vitest run tests/BookingsTable.test.tsx` (badge/tint)
- `npx vitest run tests/admin-bookings.test.ts` (p_status threading + transitions)
- `npx vitest run` (full suite green after the wave)
- Note: the live p_status filter is only exercisable after 62-04 applies migration 054 (RPC gains the parameter). Tests mock the RPC and pass without the live DB.
</verification>

<success_criteria>
- Unpaid bookings are visually distinct in the 'All' view and filterable via a dedicated chip (ABND-03/04, D-03/D-08/D-09).
- Operator can manually move an unpaid booking to confirmed or cancelled; transitions are double-gated (D-04/D-10).
- BookingsTable + admin-bookings suites green.
</success_criteria>

<output>
Create `.planning/phases/62-abandoned-unpaid-booking-capture/62-03-SUMMARY.md` when done.
</output>
