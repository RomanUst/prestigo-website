---
phase: 73-non-latin-rtl-infra-ar-hi-zh
plan: 03
type: execute
wave: 2
depends_on: [73-01]
files_modified:
  - components/CookieBanner.tsx
  - components/Hero.tsx
  - components/HowItWorks.tsx
  - components/FeatureStrip.tsx
  - components/TestimonialsCarousel.tsx
autonomous: true
requirements: [RTL-01]
estimate:
  tokens: 80000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The CookieBanner toggle-switch knob (`absolute top-0.5 left-0.5`) converts to `start-0.5`, so the knob sits on the reader-correct 'off' side and mirrors under `dir=\"rtl\"` (RTL-01, genuine RTL-meaningful position)."
    - "Hero decorative corner accents and the centered scroll cue convert only the direction axis (`left-*`/`right-*`→`start-*`/`end-*`); the `left-1/2 -translate-x-1/2` centering pair is preserved so centering does not break (RTL-01, do-not-convert callout)."
    - "FeatureStrip's `lg:border-l` divider and TestimonialsCarousel's `ml-2`/`border-l-2`/`pl-6` convert to `border-s`/`ms-2`/`border-s-2`/`ps-6` at identical token values (RTL-01)."
    - "Direction-encoding chevrons/step arrows in these components carry `rtl:rotate-180` (or `rtl:-scale-x-100`); non-directional marks (checkmarks, WhatsApp glyph, social, star ratings, lucide concept icons) do NOT flip (D-03, mirroring split)."
    - "DNT tokens (prices `€…`, phone `+420 725 986 855`) rendered inside RTL paragraphs are wrapped in `<bdi>` / `dir=\"ltr\"` + `unicode-bidi: isolate` at the existing rich-text tag boundaries, staying LTR and un-reversed on `/ar/` (D-11, bidi-corruption guard)."
    - "All five components render byte-for-byte identically under `dir=\"ltr\"` for en/ru/es/fr after conversion (RTL-01, D-12)."
  artifacts:
    - "components/CookieBanner.tsx (toggle knob logical + any bidi-isolated tokens)"
    - "components/Hero.tsx (corner accents logical, centering preserved, bidi price span)"
    - "components/HowItWorks.tsx (absolute left/right audit)"
    - "components/FeatureStrip.tsx (border-s divider)"
    - "components/TestimonialsCarousel.tsx (ms/border-s/ps)"
  key_links:
    - "dir=\"rtl\" (html) → Tailwind logical utilities + rtl: variant → mirrored component layout + mirrored directional icons"
    - "glossary richTextTags (price/strong) render sites → <bdi> isolation → LTR numerals inside RTL text"
  prohibitions:
    - "MUST NOT flip non-directional marks (WhatsApp glyph, checkmarks, social icons, star ratings, `UserRound`/`Car`/`ShieldCheck`/`Clock`) — only direction-encoding chevrons/arrows mirror (D-03)."
    - "MUST NOT convert a centering transform pair (`left-1/2` + `-translate-x-1/2`); changing the transform breaks centering (Hero do-not-convert callout)."
    - "MUST NOT introduce `space-x-*` or `flex-row-reverse` during the fix (0 occurrences today, keep it that way)."
    - "MUST NOT change any spacing/pixel value — only the physical→logical axis name (spacing parity)."
    - "MUST keep Latin brand tokens (prices, phone, PRESTIGO, vehicle-class names) in the Latin brand face even inside RTL/non-Latin copy — bidi isolation preserves their reading order and font (D-05); use native Tailwind logical utilities only, no plugin (D-02)."
  assumptions:
    - "[ASSUMPTION · RTL-01 · edge:adjacency] When a logical class and a physical class touch on the same element, the logical one governs the mirrored side. Planner assumption: no element mixes an un-converted physical and a converted logical class on the same axis after this plan (grep gate catches leftovers). Flagged, verified by grep-count 0."
    - "[ASSUMPTION · RTL-01 · edge:concurrency] RTL mirroring is a pure CSS property resolved per-render by the browser; there is no parallel/interrupted-state concern. Planner assumption: no JS race introduced by the dir-aware icon logic. Flagged; verified by vitest + visual QA."
  api_coverage: "No external API integration in this plan."
---

<objective>
Convert the five shared components that carry judgment-requiring physical-direction classes (a toggle knob that MUST mirror, decorative corners that must NOT break centering, border/margin dividers, directional chevrons, and bidi-sensitive price/phone tokens) to logical properties + `rtl:` mirroring + `<bdi>` isolation — expanding the pattern the tracer proved on `Nav.tsx`.

Purpose: These components appear on nearly every public page, so getting their RTL behavior right (and byte-parity-safe for LTR) covers the bulk of the "chrome" a `/ar/` visitor sees before any route/blog body.
Output: CookieBanner, Hero, HowItWorks, FeatureStrip, TestimonialsCarousel converted, with directional icons mirrored and DNT tokens bidi-isolated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-PATTERNS.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-UI-SPEC.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RESEARCH.md
</context>

## Artifacts this phase produces (declared by this plan)

- **Converted components**: `components/CookieBanner.tsx`, `components/Hero.tsx`, `components/HowItWorks.tsx`, `components/FeatureStrip.tsx`, `components/TestimonialsCarousel.tsx`.
- **New rendering pattern**: `<bdi>` / `dir="ltr"` isolation wrappers around DNT price/phone tokens at existing rich-text render sites.
- **New RTL variant usage**: `rtl:rotate-180` / `rtl:-scale-x-100` on direction-encoding icons only.

<tasks>

<task type="auto">
  <name>Task 1: CookieBanner + Hero + HowItWorks — knob, decorative corners, absolute-position audit</name>
  <files>components/CookieBanner.tsx, components/Hero.tsx, components/HowItWorks.tsx</files>
  <read_first>
    - components/CookieBanner.tsx (line ~387 toggle knob `absolute top-0.5 left-0.5`)
    - components/Hero.tsx (lines ~36-40 decorative corners; line ~96 `left-1/2 -translate-x-1/2` centering)
    - components/HowItWorks.tsx (absolute left/right occurrences from grep)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-PATTERNS.md (per-file callouts: knob MUST convert, corners optional/safe, centering do-not-convert)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-UI-SPEC.md (Direction & Logical-Property Conversion; Mirroring Rules)
  </read_first>
  <action>
    In components/CookieBanner.tsx convert the toggle-switch knob `left-0.5` to `start-0.5` (and its paired "on" position `end-*` if the on-state uses a right offset) — this is a genuine RTL-meaningful position that MUST mirror. In components/Hero.tsx convert the decorative corner-accent positions (`top-0 left-0`→`top-0 start-0`, `bottom-0 right-0`→`bottom-0 end-0`) — safe/consistent — but for the scroll-cue centering pair keep `-translate-x-1/2` unchanged (convert only `left-1/2`→`start-1/2` if desired, never the transform), so centering is preserved. In components/HowItWorks.tsx audit each absolute `left-*`/`right-*`: convert reading-direction-meaningful ones to `start-*`/`end-*`, leave genuinely decorative/centered ones intact, and record which were converted vs skipped in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -nE "(^| )(left|right)-[0-9]" components/CookieBanner.tsx | grep -v '^\s*//'` returns 0 for the toggle knob (converted to start/end).
    - Hero centering: `grep -c "\-translate-x-1/2" components/Hero.tsx` is unchanged from pre-edit count (transform preserved).
    - `npx vitest run tests/route-page-render.test.tsx` passes (en/ru/es/fr byte-parity for Hero/CookieBanner where snapshotted).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/route-page-render.test.tsx</automated>
  </verify>
  <done>Toggle knob mirrors, Hero corners logical with centering preserved, HowItWorks audited; LTR byte-parity holds; committed.</done>
</task>

<task type="auto">
  <name>Task 2: FeatureStrip + TestimonialsCarousel — border/margin/padding logical conversion</name>
  <files>components/FeatureStrip.tsx, components/TestimonialsCarousel.tsx</files>
  <read_first>
    - components/FeatureStrip.tsx (line ~28 `lg:border-l` divider)
    - components/TestimonialsCarousel.tsx (line ~20 `ml-2`; line ~33 `border-l-2 ... pl-6`)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-PATTERNS.md (FeatureStrip/TestimonialsCarousel exact swaps)
  </read_first>
  <action>
    In components/FeatureStrip.tsx swap the responsive divider `lg:border-l`→`lg:border-s` at the identical breakpoint/token. In components/TestimonialsCarousel.tsx swap `ml-2`→`ms-2`, `border-l-2`→`border-s-2`, and `pl-6`→`ps-6` at identical values. No other logic changes; do not touch the lucide/star icons (non-directional, no mirror).
  </action>
  <acceptance_criteria>
    - `grep -cE "(^| )(border-l|border-r|ml-|mr-|pl-|pr-)[0-9]?" components/FeatureStrip.tsx components/TestimonialsCarousel.tsx | awk -F: '{s+=$2} END{print s}'` returns 0 (all converted).
    - `npx vitest run` passes (no snapshot regression on en/ru/es/fr).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/route-page-render.test.tsx</automated>
  </verify>
  <done>FeatureStrip and TestimonialsCarousel border/margin/padding are logical; LTR byte-parity holds; committed.</done>
</task>

<task type="auto">
  <name>Task 3: Directional-icon mirroring (rtl:) + bidi isolation of DNT price/phone tokens</name>
  <files>components/Hero.tsx, components/HowItWorks.tsx, components/FeatureStrip.tsx, components/TestimonialsCarousel.tsx, components/CookieBanner.tsx</files>
  <read_first>
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-UI-SPEC.md (Mirroring Rules table; Mixed LTR-in-RTL table)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-PATTERNS.md (Chevron/arrow rtl: mirroring; Bidi isolation block)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RESEARCH.md (Pattern 4; Code Examples — bidi span; Don't Hand-Roll — use <bdi>)
    - i18n/glossary.json (doNotTranslate.structural.richTextTags, pricePlaceholderPattern, phoneNumbers)
  </read_first>
  <action>
    For any direction-encoding chevron/step/progress/journey arrow rendered by these five components, add `rtl:rotate-180` (or `rtl:-scale-x-100`) so it points reader-correct on `/ar/`; apply it ONLY to the "Yes" rows of the UI-SPEC Mirroring Rules table — never to non-directional marks (WhatsApp glyph, checkmarks, social, star ratings, `UserRound`/`Car`/`ShieldCheck`/`Clock`). Where these components render DNT tokens (prices `€…`, phone `+420 725 986 855`) inside translatable copy, wrap the token in a `<bdi>` element (or `dir="ltr"` + `unicode-bidi: isolate`) at the same rich-text-tag render boundary the DNT pipeline already isolates — do not add a new parsing pass. If a given component renders no directional icon and no DNT token, record that as a no-op in the SUMMARY (nothing to change).
  </action>
  <acceptance_criteria>
    - `grep -rc "rtl:rotate-180\|rtl:-scale-x-100" components/` > 0 where a directional icon exists, and 0 occurrences on non-directional icon elements (verified by reading the touched lines).
    - `grep -rc "<bdi\|unicode-bidi: isolate\|dir=\"ltr\"" components/` > 0 where a DNT price/phone token renders inside these components (or SUMMARY records no such token exists).
    - `npx vitest run` full suite passes (byte-parity preserved — `rtl:` and `<bdi>` are no-ops under `dir="ltr"`).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/route-page-render.test.tsx tests/i18n-completeness.test.ts</automated>
  </verify>
  <done>Directional icons mirror on `/ar/`; DNT price/phone tokens stay LTR via `<bdi>`; non-directional marks unflipped; LTR byte-parity holds; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translated string → rendered component | ar/hi/zh copy (incl. DNT tokens) rendered inside these components under `dir="rtl"` |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-01 | Tampering (XSS) | component rendering of translated strings | low | mitigate | Strings rendered through React (auto-escaped) / next-intl `t`/`t.rich` with a fixed tag whitelist; no `dangerouslySetInnerHTML` added |
| T-73-02 | Spoofing (bidi-override) | RTL text rendering | low | mitigate | `<bdi>`/`unicode-bidi: isolate` scopes DNT runs; translated source already leakage/DNT-verified in 73-02 |
| T-73-SC | Tampering (supply chain) | npm installs | low | accept | No new packages — `rtl:` variant + logical utilities are core Tailwind v4 |

No `high`-severity threats — block-on threshold not triggered.
</threat_model>

<verification>
- `grep` shows 0 remaining physical-direction classes in the five components.
- `rtl:` mirroring only on directional icons; `<bdi>` only on DNT tokens.
- `npx vitest run` full suite green (LTR byte-parity intact).
</verification>

<success_criteria>
Five shared components converted to logical properties with correct mirror/no-mirror icon split and bidi-isolated DNT tokens; en/ru/es/fr render byte-for-byte unchanged (RTL-01, D-12).
</success_criteria>

<output>
Create `.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-03-SUMMARY.md` when done.
</output>
