---
phase: 73-non-latin-rtl-infra-ar-hi-zh
plan: 05
type: execute
wave: 2
depends_on: [73-01]
files_modified:
  - app/[locale]/blog/prague-airport-taxi-vs-chauffeur/page.tsx
  - app/[locale]/blog/prague-airport-to-city-center/page.tsx
  - app/[locale]/blog/prague-vienna-transfer-vs-train/page.tsx
autonomous: true
requirements: [RTL-01]
estimate:
  tokens: 55000
  raw_tokens: 33000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The three blog pages' pull-quote pattern (`border-l-2 pl-8` / `pl-6`) converts to `border-s-2 ps-8` / `ps-6`, and `text-left`/`text-right` (incl. the results table `w-full text-left`) converts to `text-start`/`text-end` (RTL-01)."
    - "Prices/phone/flight/time DNT tokens inside blog body copy stay LTR and un-reversed on `/ar/` via `<bdi>` / `dir=\"ltr\"` isolation at the existing rich-text boundaries (D-11)."
    - "The three blog pages render byte-for-byte identically under `dir=\"ltr\"` for en/ru/es/fr after conversion (RTL-01, D-12)."
  artifacts:
    - "3 converted app/[locale]/blog/*/page.tsx (border-s/ps/text-start + bidi-isolated tokens)"
  key_links:
    - "html dir=\"rtl\" → border-s-2/ps-8 mirror the pull-quote accent to the reading-start side on /ar/"
  prohibitions:
    - "MUST NOT convert the 3 legacy JSX blog posts to ar/hi/zh content — they stay EN-only (locked in Phase 72); this plan only converts the JSX page-body Tailwind classes of the 3 pages that carry physical-direction classes."
    - "MUST NOT change spacing tokens, prices, or copy — border/padding/text axis swap + bidi isolation only."
  assumptions:
    - "[ASSUMPTION · RTL-01 · edge:adjacency] The pull-quote `border-l-2 pl-8` is a paired accent+padding on the same edge; both convert together to `border-s-2 ps-8` so the accent and its inset stay on the same (reading-start) side. Verified by visual QA on /ar/ + byte-parity on LTR."
    - "[ASSUMPTION · RTL-01 · edge:empty] A blog page with no physical-direction class on a given element is a no-op for that element. Verified by grep-count 0."
    - "[ASSUMPTION · RTL-01 · edge:ordering] Multiple equal `text-left` occurrences convert positionally 1:1 with no reordering. Verified by byte-parity snapshot technique."
  api_coverage: "No external API integration in this plan."
---

<objective>
Convert the three blog article pages that carry richer physical-direction classes (pull-quote `border-l`/`pl` accents plus `text-left`/`text-right` including a results table) to logical properties, and bidi-isolate the DNT price/phone/flight/time tokens that appear in blog body copy so they read correctly inside `/ar/` RTL paragraphs.

Purpose: Blog pages are the last of the ~40 physical-direction files and the ones most likely to mix Latin numerals (prices, train times, flight codes) into long translated prose — the highest bidi-corruption risk surface in the phase.
Output: 3 blog pages converted with bidi-safe DNT tokens.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-PATTERNS.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-UI-SPEC.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RESEARCH.md
</context>

## Artifacts this phase produces (declared by this plan)

- **Converted blog pages**: `app/[locale]/blog/prague-airport-taxi-vs-chauffeur/page.tsx`, `app/[locale]/blog/prague-airport-to-city-center/page.tsx`, `app/[locale]/blog/prague-vienna-transfer-vs-train/page.tsx`.
- **New bidi isolation**: `<bdi>` / `dir="ltr"` wrappers around DNT price/phone/flight/time tokens in blog body copy.

<tasks>

<task type="auto">
  <name>Task 1: Blog pull-quote + text-alignment logical conversion (3 pages)</name>
  <files>app/[locale]/blog/prague-airport-taxi-vs-chauffeur/page.tsx, app/[locale]/blog/prague-airport-to-city-center/page.tsx, app/[locale]/blog/prague-vienna-transfer-vs-train/page.tsx</files>
  <read_first>
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-PATTERNS.md ("Blog pull-quote pattern" — line ~499 `border-l-2 border-copper pl-8`; line ~528 `w-full text-left`)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-UI-SPEC.md (conversion table)
    - each blog page (grep for `border-l`, `pl-`, `text-left`, `text-right`)
  </read_first>
  <action>
    In each of the 3 blog pages convert the pull-quote `border-l-2`→`border-s-2` and its paired `pl-8`/`pl-6`→`ps-8`/`ps-6` together (accent + inset stay on the same reading-start edge), and swap `text-left`→`text-start` / `text-right`→`text-end` (including the results-table `w-full text-left`) at identical token values. No other change.
  </action>
  <acceptance_criteria>
    - `grep -rlE "text-(left|right)|border-(l|r)($|[- ])|(^| )p(l|r)-[0-9]" app/[locale]/blog` returns no files.
    - `npx vitest run tests/route-page-render.test.tsx` passes (byte-parity technique confirms LTR-identical rendering).
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(grep -rlE 'text-(left|right)|border-(l|r)($|[- ])|(^| )p(l|r)-[0-9]' app/[locale]/blog 2>/dev/null)" && echo OK</automated>
  </verify>
  <done>All 3 blog pages have zero physical-direction classes; LTR byte-parity holds; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Bidi-isolate DNT price/phone/flight/time tokens in blog body copy</name>
  <files>app/[locale]/blog/prague-airport-taxi-vs-chauffeur/page.tsx, app/[locale]/blog/prague-airport-to-city-center/page.tsx, app/[locale]/blog/prague-vienna-transfer-vs-train/page.tsx</files>
  <read_first>
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RESEARCH.md (Code Examples — bidi span; Don't Hand-Roll — use <bdi>)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-UI-SPEC.md (Mixed LTR-in-RTL table: prices, phone, flight numbers, times)
    - i18n/glossary.json (doNotTranslate.structural: pricePlaceholderPattern, phoneNumbers, richTextTags)
  </read_first>
  <action>
    Where the 3 blog pages render DNT tokens inside translatable prose — prices (`€…`), phone `+420 725 986 855`, flight numbers, train times (`14:30`) — wrap each in a `<bdi>` element (or `dir="ltr"` + `unicode-bidi: isolate`) at the existing rich-text-tag render boundary so it stays LTR and un-reversed inside `/ar/` RTL paragraphs. Do not add a new parsing pass; wrap only at the same call sites the DNT/rich-text isolation already uses. If a page renders such tokens only in fixed structural chrome (not inside translated prose), record that as a scoped no-op in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -rc "<bdi\|unicode-bidi: isolate\|dir=\"ltr\"" app/[locale]/blog` > 0 (or SUMMARY records no in-prose DNT token exists on a given page).
    - `npx vitest run tests/route-page-render.test.tsx` passes (`<bdi>`/`dir=ltr` are no-ops under `dir="ltr"` — byte-parity preserved).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/route-page-render.test.tsx</automated>
  </verify>
  <done>DNT price/phone/flight/time tokens in blog prose are bidi-isolated; LTR byte-parity holds; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translated blog prose → rendered page | ar/hi/zh blog copy (incl. Latin numerals) rendered under `dir="rtl"` |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-01 | Tampering (XSS) | blog page rendering | low | mitigate | Blog page JSX renders through React (auto-escaped); no `dangerouslySetInnerHTML` added by the class/bidi conversion |
| T-73-02 | Spoofing (bidi-override) | RTL blog prose with Latin numerals | low | mitigate | `<bdi>`/`unicode-bidi: isolate` scopes each DNT run; translated source leakage/DNT-verified in 73-02 |
| T-73-SC | Tampering (supply chain) | npm installs | low | accept | No new packages — core Tailwind + native `<bdi>` |

No `high`-severity threats — block-on threshold not triggered.
</threat_model>

<verification>
- `grep -rlE "text-(left|right)|border-(l|r)|p(l|r)-[0-9]" app/[locale]/blog` returns no files.
- `<bdi>`/`dir=ltr` present where DNT tokens render in prose.
- `npx vitest run tests/route-page-render.test.tsx` byte-parity green.
</verification>

<success_criteria>
Three blog pages use logical properties and bidi-isolated DNT tokens; en/ru/es/fr byte-for-byte unchanged (RTL-01, D-11, D-12).
</success_criteria>

<output>
Create `.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-05-SUMMARY.md` when done.
</output>
