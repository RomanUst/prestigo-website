---
phase: 73-non-latin-rtl-infra-ar-hi-zh
plan: 14
type: execute
wave: 2
depends_on: [73-13]
gap_closure: true
autonomous: true
requirements: [RTL-01, FONT-01]
files_modified:
  - tests/rtl-backstop.test.ts
  - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md

estimate:
  tokens: 26000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "tests/rtl-backstop.test.ts CR-02 block asserts, per route page, the SPECIFIC openingParagraphs/routeNarrative/faqs render call sites use interpolateBidi (not mere file-level presence), verifies the FAQPage JSON-LD text stays plain f.a, and FAILS if the plain interpolate() call returns to the openingParagraphs/routeNarrative sites — npx vitest run tests/rtl-backstop.test.ts exits 0 (RTL-01, D-11)."
    - "73-RTL-QA.md Group 3 route-page row (previously FAIL) now reads PASS, noting openingParagraphs/routeNarrative/faqs are bidi-isolated via interpolateBidi() (gap cycle 73-13/73-14); a live /ar/routes/prague-vienna + prague-berlin structural re-check confirms the prose price tokens render inside <bdi> (RTL-01, D-10/D-11)."
  artifacts:
    - "tests/rtl-backstop.test.ts — strengthened per-field CR-02 assertions across all 30 route pages"
    - ".planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md — corrected Group 3 route-page row"
  key_links:
    - "backstop test -> specific openingParagraphs/routeNarrative/faqs render call sites -> interpolateBidi (per-field, not file-level)"
    - "backstop test -> FAQPage JSON-LD text field -> plain interpolate() (carve-out asserted)"
---

<objective>
Close GAP-2 (WARNING) and GAP-3 (WARNING) from 73-VERIFICATION.md, which are the two
hardening follow-ons to the 73-13 code fix:

GAP-2 — The current CR-02 backstop (`tests/rtl-backstop.test.ts` lines ~140-146) asserts only
`/interpolateBidi/.test(src) || /<bdi>/.test(src)` ANYWHERE in each route-page file, so it
passed 46/46 even while the GAP-1 defect was live (because sibling fields like hero.intro are
wrapped). Strengthen it to verify the SPECIFIC `openingParagraphs`/`routeNarrative`/`faqs`
render call sites use `interpolateBidi`, that the plain `interpolate()` call is gone from those
sites, and that the FAQPage JSON-LD `text` field stays plain — per-field correctness, not
file-level presence.

GAP-3 — `73-RTL-QA.md`'s Group 3 route-page row currently records a FAIL (correctly, before the
fix). After 73-13, re-run the D-10/D-11 Group 3 structural bidi check on
`/ar/routes/prague-vienna` and `/ar/routes/prague-berlin` to confirm the prose price tokens now
render inside `<bdi>`, then correct that row from FAIL to PASS with a note referencing this gap
cycle. The live re-check is an EXECUTOR VERIFICATION step (does the token render inside `<bdi>`),
NOT a glyph-tofu sign-off.

Depends on 73-13 (the code fix must land first — this plan's test asserts the fixed call sites
and the QA re-check observes the fixed render).

Purpose: make the machine backstop actually catch this bug class (so it cannot silently
reappear on a content edit) and correct the factually-wrong QA record.
Output: strengthened backstop test + corrected QA record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-VERIFICATION.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md
@tests/rtl-backstop.test.ts
@app/[locale]/routes/prague-vienna/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen the CR-02 backstop to per-field render-call-site correctness (GAP-2)</name>
  <files>tests/rtl-backstop.test.ts</files>
  <read_first>
    - tests/rtl-backstop.test.ts (the CR-02 describe block, lines ~104-161 — the routeSlugs list of 30 route pages and the current weak `hasBidiEvidence = /interpolateBidi/.test(src) || /<bdi>/.test(src)` assertion at lines ~140-146 that this task replaces)
    - app/[locale]/routes/prague-vienna/page.tsx (post-73-13: the exact render-call-site strings to assert — openingParagraphs/routeNarrative interpolateBidi precomputes, the `aBidi: interpolateBidi(f.a, prices)` faqs field, the `faq.aBidi` render, and the `text: f.a` JSON-LD carve-out)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-VERIFICATION.md (GAP-2 missing item: "check the specific openingParagraphs/faqs render call sites use interpolateBidi, not just file-level presence")
  </read_first>
  <action>
    Replace the weak per-route-slug CR-02 assertion (the one that currently accepts any
    interpolateBidi/<bdi> anywhere in the file) with per-field, per-render-call-site
    assertions that hold for all 30 route slugs already listed in the test's routeSlugs array.
    For each route page source, assert POSITIVELY that: the openingParagraphs precompute maps
    through interpolateBidi; the routeNarrative.paragraphs precompute maps through
    interpolateBidi; the faqs precompute defines the aBidi field via interpolateBidi over f.a;
    and the FAQ answer JSX renders the aBidi field. Assert the CARVE-OUT positively: the
    FAQPage JSON-LD acceptedAnswer text field still reads the plain string field f.a. Assert
    NEGATIVELY that the plain-helper form no longer appears at the openingParagraphs precompute
    site nor the routeNarrative.paragraphs precompute site (anchor each negative on its full
    field-qualified left-hand side so it does not false-match the sibling chauffeurNarrative
    precompute, which legitimately stays on the plain helper, nor the interpolateBidi form —
    the token boundary immediately after the helper name distinguishes them).

    Keep the existing servicePages assertions and every other describe block (Nav chevron,
    carousel, globals.css, blog CTA) untouched. The strengthened test must be a source-reading
    assertion (fs.readFileSync), consistent with the file's existing style — no browser, no
    live render.
  </action>
  <verify>
    <automated>npx vitest run tests/rtl-backstop.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/rtl-backstop.test.ts` exits 0 with the strengthened CR-02 assertions passing for all 30 route slugs against the post-73-13 tree.
    - The test asserts the specific render call sites: it greps each source for `content.openingParagraphs.map((p) => interpolateBidi(p, prices))`, `content.routeNarrative.paragraphs.map((p) => interpolateBidi(p, prices))`, and `aBidi: interpolateBidi(f.a, prices)` (all required present), and for `text: f.a` (JSON-LD carve-out required present).
    - The test contains negative assertions anchored on the full field-qualified LHS for the openingParagraphs and routeNarrative.paragraphs plain-`interpolate()` precompute forms (required absent) — NOT a bare `interpolate(` file-level check.
    - Regression proof that the test would have caught GAP-1: temporarily reverting the openingParagraphs precompute on a single route page to the plain interpolate() form makes `npx vitest run tests/rtl-backstop.test.ts` FAIL (restore the file afterward; this is a verification step, do not commit the revert).
    - The old file-level `hasBidiEvidence = /interpolateBidi/.test(src) || /<bdi>/.test(src)` route-page assertion no longer exists: `grep -c "hasBidiEvidence" tests/rtl-backstop.test.ts` counts only the servicePages usage (not the route-page loop).
  </acceptance_criteria>
  <done>The CR-02 backstop fails when any route page's openingParagraphs/routeNarrative/faqs render site drops interpolateBidi or when the JSON-LD carve-out breaks; it passes on the post-73-13 tree; the weak file-level route-page check is gone.</done>
  <reversibility rating="reversible">Test-only change; revertible.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Re-run the D-10/D-11 Group 3 structural bidi check and correct the 73-RTL-QA.md record (GAP-3)</name>
  <files>.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md</files>
  <read_first>
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md (Task 3 Group 3 table, the route-page row ~line 181 currently marked "FAIL (corrected post-verification, WR-01)" — the row to correct; and the Harvest note ~lines 193-204)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-VERIFICATION.md (GAP-3 missing item + the note that the Group 3 PASS was factually wrong for openingParagraphs/faqs)
    - app/[locale]/routes/prague-vienna/page.tsx and app/[locale]/routes/prague-berlin/page.tsx (post-73-13: the render sites now on interpolateBidi/aBidi — the structural fact the re-check confirms)
  </read_first>
  <action>
    Re-run the D-10/D-11 Group 3 route-page check on `/ar/routes/prague-vienna` and
    `/ar/routes/prague-berlin` as a STRUCTURAL bidi verification: confirm the opening-paragraph
    and FAQ-answer prose price tokens now render inside a `<bdi>` element under `dir="rtl"`
    (i.e. the DNT price run is bidi-isolated, no longer bare in RTL prose). Prefer the
    already-proven machine method (the 73-13 render test / a server-component render of the two
    pages under locale `ar`, asserting `<bdi>` around the price token) so the re-check is
    deterministic and does not depend on local Supabase pricing (local dev renders "€0" — a
    data gap, not an RTL defect; the structural `<bdi>` isolation is what matters and is
    independent of the numeric value). This is an executor verification step; it is NOT a
    glyph-tofu sign-off (that stays with independent human review — see the human-verification
    note below).

    Then correct 73-RTL-QA.md: update the Group 3 route-page row from its FAIL wording to PASS,
    noting that after gap cycle 73-13/73-14 the openingParagraphs/routeNarrative.paragraphs/faqs
    fields are bidi-isolated via interpolateBidi() (with the FAQPage JSON-LD text kept plain),
    and that the strengthened tests/rtl-backstop.test.ts CR-02 assertions now guard it per-field.
    Update the Harvest note to reflect that the previously-open Group 3 route-page gap is closed.
    Do not alter the other (already-PASS) rows or the FONT-01 tofu human-verification routing.
  </action>
  <verify>
    <automated>npx vitest run tests/route-page-render.test.tsx tests/rtl-backstop.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The structural re-check is recorded: the 73-13 render test (or an equivalent /ar render assertion for prague-vienna AND prague-berlin) proves the opening-paragraph and FAQ-answer price tokens render inside `<bdi>`; `npx vitest run tests/route-page-render.test.tsx tests/rtl-backstop.test.ts` exits 0.
    - 73-RTL-QA.md Group 3 route-page row no longer reads FAIL: `grep -n "FAIL" .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md` shows the Group 3 route-page row now marked PASS (any remaining FAIL text is only the historical narrative of the found-and-fixed defect, explicitly labeled as closed by 73-13/73-14).
    - The corrected row cites the fix: `grep -n "interpolateBidi" .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md` shows the Group 3 row references interpolateBidi() isolation of openingParagraphs/routeNarrative/faqs and the JSON-LD plain-text carve-out.
    - The FONT-01 glyph-tofu human-verification routing is unchanged (still routed to independent human sign-off; not claimed closed here).
  </acceptance_criteria>
  <done>The Group 3 route-page row reads PASS with a note citing the 73-13/73-14 interpolateBidi() closure and the JSON-LD carve-out, backed by a green structural render re-check on both /ar reference pages; the FONT-01 tofu item stays with human review.</done>
  <reversibility rating="reversible">QA-record documentation edit plus a re-run; revertible.</reversibility>
</task>

</tasks>

<human_verification_note>
FONT-01 glyph-tofu / fallback-box visual sign-off (behavior_unverified truth #5) is
explicitly routed to INDEPENDENT HUMAN review per verification policy and is NOT a code gap.
This plan does NOT close it and does NOT gate execution on it. 73-RTL-QA.md already records a
detailed live-browser walkthrough (screenshots + quoted rendered ar/hi/zh strings, all PASS);
the outstanding item is only the independent human confirmation of no tofu on `/ar/`,
`/hi/login`, `/zh/login` across the 5 D-10 pages. FONT-01 is carried in this plan's
`requirements` solely to attach this non-gating note — no task attempts a code fix for it.
</human_verification_note>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backstop test -> route-page source | source-reading assertions guard the render-call-site correctness of the 73-13 fix |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-14-01 | Repudiation | tests/rtl-backstop.test.ts CR-02 gate | medium | mitigate | The strengthened per-field + negative assertions make a future silent regression (dropping interpolateBidi on these fields, or leaking a ReactNode into the JSON-LD text) fail the gate instead of passing green — closing the false-confidence hole GAP-2 identified. |
| T-73-14-02 | Tampering | FAQPage JSON-LD text field | high | mitigate | Test asserts `text: f.a` (plain string) remains the JSON-LD source across all 30 pages — guards the structured-data carve-out from regressing to a ReactNode. |
| T-73-14-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this plan; no dependency changes. |
</threat_model>

<verification>
- `npx vitest run tests/rtl-backstop.test.ts` green with the strengthened per-field CR-02 assertions.
- A temporary single-page revert to the plain helper makes the backstop FAIL (regression proof), then restored.
- `npx vitest run tests/route-page-render.test.tsx tests/rtl-backstop.test.ts` green (structural re-check).
- 73-RTL-QA.md Group 3 route-page row corrected to PASS with the interpolateBidi()/carve-out note.
</verification>

<success_criteria>
GAP-2 closed: the CR-02 backstop verifies per-field render-call-site correctness (and the
JSON-LD carve-out) rather than file-level presence, and would catch the GAP-1 defect.
GAP-3 closed: 73-RTL-QA.md's Group 3 route-page row is factually corrected to PASS, backed by a
green structural re-check on both /ar reference pages. FONT-01 tofu sign-off remains, correctly,
a non-gating independent-human item.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (new/changed symbols)
- `tests/rtl-backstop.test.ts` — CR-02 describe block rewritten from a file-level presence check to per-field render-call-site assertions (positive: openingParagraphs/routeNarrative interpolateBidi precomputes, `aBidi` faqs field, `faq.aBidi` render, `text: f.a` JSON-LD carve-out; negative: plain-helper precompute forms absent).
- `.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md` — Group 3 route-page row corrected FAIL -> PASS; Harvest note updated to record the closed gap.
</artifacts_produced>

<output>
Create `.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-14-SUMMARY.md` when done.
</output>
