---
phase: 73-non-latin-rtl-infra-ar-hi-zh
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Nav.tsx
  - components/TestimonialsCarousel.tsx
autonomous: true
gap_closure: true
requirements: [RTL-01]
estimate:
  tokens: 22000
  raw_tokens: 18000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The /ar/ account-menu chevron points reader-correct (down = closed, up = open); the open/closed indicator is no longer inverted for Arabic (RTL-01, D-03 — closes CR-01, mirrors failed VERIFICATION truth #2 'no mirrored-layout breakage')."
    - "The signed-in account dropdown panel anchors to the inline-end (reading-end) edge under dir=rtl instead of the physical right edge (RTL-01 — closes WR-01)."
    - "Testimonial carousel ArrowRight/ArrowLeft keyboard navigation moves in the reader-correct direction under dir=rtl (RTL-01 — closes WR-03)."
  artifacts:
    - components/Nav.tsx
    - components/TestimonialsCarousel.tsx
  key_links:
    - "resolved locale (ar) -> Nav chevron rotation (open/closed only, no RTL addend) -> correct glyph"
    - "resolved locale (ar) -> Nav dropdown insetInlineEnd -> panel mirrors to reading-end edge"
    - "resolved locale (ar) -> carousel key handler -> slide direction matches visual forward"
---

<objective>
Close the two Nav.tsx RTL defects (CR-01 chevron inversion — the phase's one BLOCKER, and WR-01 physical dropdown inset) and the WR-03 carousel arrow-key direction gap. All three are JS/inline-style direction-logic bugs in client components that the mechanical Tailwind swap could not reach.

Purpose: CR-01 is the concrete instance of "mirrored-layout breakage" on a D-10 QA page (account/login) that failed VERIFICATION truth #2. Fixing it (plus WR-01/WR-03) is a prerequisite for the D-10/D-11 visual walkthrough in 73-12.
Output: components/Nav.tsx and components/TestimonialsCarousel.tsx with correct RTL direction logic.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-REVIEW.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix account-menu chevron RTL inversion (CR-01)</name>
  <files>components/Nav.tsx</files>
  <read_first>
    - components/Nav.tsx (lines 24-27 for the locale/RTL boolean declarations; lines 197-217 for the chevron `<svg>` and its inline-style rotation)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-REVIEW.md § CR-01 (root-cause walkthrough of the four states)
  </read_first>
  <action>
    In `components/Nav.tsx`, the account-menu chevron `<svg>` (lines 197-217) composes its rotation at line 211 as the open/closed rotation PLUS an extra +180-degree right-to-left mirror addend. That addend is wrong: the chevron path `M2 4l4 4 4-4` is horizontally symmetric about x=6, so it needs no mirror, and the addend inverts the open/closed indicator for `ar` (RTL+closed shows "open", RTL+open shows "closed"). Reduce the inline `transform` at line 211 to the open/closed rotation only — `rotate(${menuOpen ? 180 : 0}deg)` — keeping the existing `transition: 'transform 0.2s ease'`. Then delete the now-unused right-to-left boolean constant declared at line 26 (it was referenced only by the addend you just removed); leave the `locale` constant on line 25 intact, since it is used elsewhere. Update the explanatory comment above the `transform` (lines 207-210) so it no longer describes a JS-composed RTL rotation. Do NOT introduce a `scaleX(-1)` — this shape genuinely needs no mirror (per 73-REVIEW.md CR-01 fix).
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo && grep -c "isRtl" components/Nav.tsx | grep -qx 0 && grep -q "menuOpen ? 180 : 0" components/Nav.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "node_modules" | grep -i "Nav.tsx" ; test $? -ne 0 && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isRtl" components/Nav.tsx` returns `0` (both the declaration and the addend removed; no other reference existed).
    - `grep -c "menuOpen ? 180 : 0" components/Nav.tsx` returns `1` (rotation is now open/closed only).
    - `components/Nav.tsx` still type-checks (no unused-variable or reference error for the removed constant).
  </acceptance_criteria>
  <done>On /ar/ the account-menu chevron points down when the menu is closed and up when open — same as LTR. No inverted indicator.</done>
</task>

<task type="auto">
  <name>Task 2: Convert account dropdown panel to a logical inline-end inset (WR-01)</name>
  <files>components/Nav.tsx</files>
  <read_first>
    - components/Nav.tsx (lines 219-240 — the dropdown panel `<div>` inline `style` object; the physical inset is at line 227)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-REVIEW.md § WR-01 (proposed logical-inset fix)
  </read_first>
  <action>
    In `components/Nav.tsx`, the account dropdown panel `<div>` (lines 219-240) is positioned with a hardcoded physical right-edge inset at line 227, so under `dir=rtl` it keeps anchoring to the physical right edge of the trigger instead of the reading-end edge. Replace that physical inset property with the logical property `insetInlineEnd: 0` in the same inline `style` object, leaving `position: 'absolute'`, `top: '100%'`, and the rest unchanged. This mirrors the panel to the reading-end edge in `ar` and is a no-op in LTR (inline-end === right for LTR).
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo && grep -q "insetInlineEnd: 0" components/Nav.tsx && ! grep -q "right: 0" components/Nav.tsx && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "insetInlineEnd: 0" components/Nav.tsx` returns a match on the dropdown-panel style object.
    - `grep -c "right: 0" components/Nav.tsx` returns `0` (no residual physical inset remains in the file).
  </acceptance_criteria>
  <done>The signed-in account menu opens flush to the reading-end edge on /ar/ and the physical-right edge on LTR locales.</done>
</task>

<!-- planner-discipline-allow: isRtl -->
<task type="auto">
  <name>Task 3: RTL-aware carousel arrow-key navigation (WR-03)</name>
  <files>components/TestimonialsCarousel.tsx</files>
  <read_first>
    - components/TestimonialsCarousel.tsx (line 4 import from next-intl; lines 54-84 — component body and `onKeyDown`)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-REVIEW.md § WR-03 (proposed isRtl branch)
  </read_first>
  <action>
    In `components/TestimonialsCarousel.tsx`, the `onKeyDown` handler (lines 76-84) maps `ArrowRight` -> next and `ArrowLeft` -> previous with no direction awareness, which is backwards for an RTL reader. Extend the existing `next-intl` import on line 4 to also import `useLocale`, then inside `TestimonialsCarousel` (near the existing `const t = useTranslations('Testimonials')` on line 55) derive the RTL flag from `useLocale() === 'ar'`. In `onKeyDown`, make `ArrowRight` advance by `isRtl ? -1 : 1` and `ArrowLeft` by `isRtl ? 1 : -1`, preserving the existing `e.preventDefault()` calls and the `goTo(activeIndex + delta)` shape. `StarBadge` (a separate component) must not be touched.
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo && grep -q "useLocale" components/TestimonialsCarousel.tsx && grep -q "isRtl ? -1 : 1" components/TestimonialsCarousel.tsx && grep -q "isRtl ? 1 : -1" components/TestimonialsCarousel.tsx && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useLocale" components/TestimonialsCarousel.tsx` returns at least `1`.
    - Both `isRtl ? -1 : 1` (ArrowRight) and `isRtl ? 1 : -1` (ArrowLeft) appear in the `onKeyDown` handler.
    - `e.preventDefault()` is still called on both branches.
  </acceptance_criteria>
  <done>On /ar/ pressing the physical ArrowRight/ArrowLeft moves the carousel in the visually-forward direction of the mirrored layout; LTR behaviour is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
No new trust boundary. These are presentational client-component direction-logic edits; no new input crosses any boundary, no data flow changes, no dependency added.

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73gap-07 | Tampering | Nav.tsx / TestimonialsCarousel.tsx | low | accept | No new attack surface; no package install; changes are inline style + key-handler direction only. |
</threat_model>

<artifacts_produced>
## Artifacts this plan produces
- `components/Nav.tsx` — chevron rotation reduced to open/closed only (RTL addend removed); unused RTL boolean deleted; dropdown panel `right`-inset replaced by `insetInlineEnd: 0`.
- `components/TestimonialsCarousel.tsx` — `useLocale` import added; `onKeyDown` ArrowRight/ArrowLeft deltas conditioned on `isRtl`.
- No new symbols exported; no files created.
</artifacts_produced>

<verification>
- `grep -c "isRtl" components/Nav.tsx` == 0; `grep -c "right: 0" components/Nav.tsx` == 0; `grep -q "insetInlineEnd: 0" components/Nav.tsx`.
- `grep -q "useLocale" components/TestimonialsCarousel.tsx` and both isRtl-conditioned deltas present.
- `npx tsc --noEmit` shows no new error in either file.
</verification>

<success_criteria>
CR-01, WR-01, WR-03 all closed; both files type-check; /ar/ account menu chevron and dropdown, and carousel keyboard nav, behave reader-correct.
</success_criteria>

<output>
Create `.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-07-SUMMARY.md` when done.
</output>
