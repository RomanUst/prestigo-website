---
phase: 73-non-latin-rtl-infra-ar-hi-zh
plan: 12
type: execute
wave: 3
depends_on: ["73-07", "73-08", "73-09", "73-10", "73-11"]
files_modified:
  - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md
  - tests/rtl-backstop.test.ts
autonomous: false
gap_closure: true
requirements: [RTL-01, FONT-01]
estimate:
  tokens: 30000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - statement: "RTL visual QA on /ar/ passes — the D-10/D-11 checklist in 73-RTL-QA.md is completed with a recorded PASS/FAIL per row across all 5 representative pages (Groups 1-4), no row left PENDING (ROADMAP SC#4, D-10/D-11 — closes failed VERIFICATION truth 'RTL visual QA on /ar/ passes')."
      verification: backstop
    - statement: "/ar/, /hi/, /zh/ render Noto glyphs with no tofu/fallback boxes — confirmed by a live visual pass on /ar/, /hi/login, /zh/login across the 5 D-10 page types (ROADMAP SC#3, FONT-01 — closes the behavior-unverified truth)."
      verification: backstop
    - statement: "No reversed price/phone/time token and no mirrored-layout breakage on any /ar/ D-10 page (RTL-01, D-11 — re-confirms VERIFICATION truth #2 after the CR-01/CR-02/WR fixes)."
      verification: backstop
  artifacts:
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md
    - tests/rtl-backstop.test.ts
  key_links:
    - "73-07..73-11 code fixes -> /ar render -> human visual pass -> recorded PASS in 73-RTL-QA.md"
---

<objective>
Complete the D-10/D-11 RTL visual QA walkthrough that 73-06 left as a PENDING scaffold, and fold in the FONT-01 tofu confirmation. This is the deliverable the ROADMAP SC#4 requires ("RTL visual QA on /ar/ passes" as an already-true fact, not a scaffold) and that VERIFICATION.md failed on. It runs LAST because the fixes it validates (CR-01/CR-02 land on D-10 pages; WR-02/03/04/05 touch adjacent chrome) must be in place first.

Purpose: turn the four PENDING checklist groups in 73-RTL-QA.md into recorded PASS/FAIL results, and add machine backstops so a re-verify has evidence for the human-only items.
Output: a completed 73-RTL-QA.md walkthrough + an automated RTL backstop test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-VERIFICATION.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated RTL backstops before the human pass</name>
  <files>tests/rtl-backstop.test.ts</files>
  <read_first>
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-07-SUMMARY.md, 73-08-SUMMARY.md, 73-09-SUMMARY.md, 73-10-SUMMARY.md, 73-11-SUMMARY.md (confirm each fix landed)
    - components/Nav.tsx, components/TestimonialsCarousel.tsx, app/globals.css (grep targets for the code-level gates)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md Task 2 (the build-output inspection method for Noto class presence — reuse it as the tofu-delivery backstop)
  </read_first>
  <action>
    Create `tests/rtl-backstop.test.ts` (vitest) that machine-asserts the code-level half of the RTL fixes so the human pass has backstops for the reversal/mirroring items that are otherwise human-only:
    (1) `components/Nav.tsx` contains no RTL-mirror addend on the chevron (`grep`-style source assertion: file does not contain the RTL boolean) and positions the dropdown via `insetInlineEnd`.
    (2) `components/TestimonialsCarousel.tsx` uses `useLocale` and RTL-conditioned arrow deltas.
    (3) `app/globals.css` uses `inset-inline-start`/`inset-inline-end`/`text-align: start` and has no residual `transform-origin: left center`.
    (4) The three static blog CTAs no longer apply `display-italic` on a `<bdi>` element.
    (5) Bidi isolation is present: read the fixed route/service page files and assert each renders `interpolateBidi` or an inline `<bdi>` at a price site (reuse the 73-09 sweep list).
    Load each file with `fs.readFileSync` and assert with string/regex checks (no browser needed). This is the deterministic backstop; the visual judgments stay with Task 2.
    NOTE: do not attempt to programmatically prove "no tofu" or "no visual reversal" — those are rendering-time facts reserved for the human pass; this test only proves the code that prevents them is in place.
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo && npx vitest run tests/rtl-backstop.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/rtl-backstop.test.ts` passes with assertions covering Nav (chevron + inset), carousel (useLocale + deltas), globals.css (three logical props, no left transform-origin), blog CTAs (no whole-sentence bdi), and route/service bidi presence.
    - The test reads source files (no live server); it fails loudly if any 73-07..73-10 fix regresses.
  </acceptance_criteria>
  <done>A single vitest file green-lights every code-level RTL fix as a re-runnable backstop.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Complete the D-10/D-11 visual QA walkthrough + FONT-01 tofu pass</name>
  <files>.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md</files>
  <read_first>
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md (the scaffold — Groups 1-4, all currently pending; the 5 D-10 pages table)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-CONTEXT.md D-10/D-11 (the acceptance bar)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-VERIFICATION.md (the two failed truths + human_verification items this pass must satisfy)
  </read_first>
  <what-built>
    All 73-07..73-11 RTL fixes are landed and the code-level backstop test (Task 1) is green. The site can be built and served for a live visual pass; 73-RTL-QA.md holds the D-10/D-11 checklist scaffold (Groups 1-4) awaiting recorded results.
  </what-built>
  <how-to-verify>
    Build and serve the site (`npx next build && npx next start`, or the dev server), then walk the 5 D-10 representative pages on /ar/ (Home `/ar/`, Booking `/ar/book`, a Route page e.g. `/ar/routes/prague-vienna` plus one CR-02-fixed page e.g. `/ar/routes/prague-berlin`, a Blog post, `/ar/login`), plus `/hi/login` and `/zh/login` for tofu. RECORD a PASS/FAIL result into every checklist row of `.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md`, replacing the pending-operator placeholders:
    - Group 1 Mirroring: labels/icons swap sides; directional chevrons/arrows point reader-correct (esp. the Nav account-menu chevron fixed in 73-07 — confirm down = closed / up = open on /ar/login); non-directional marks do NOT flip.
    - Group 2 Tofu (FONT-01): no fallback-glyph boxes on /ar/, /hi/login, /zh/login across all 5 page types — every non-Latin glyph paints via its Noto face.
    - Group 3 Mixed LTR-in-RTL (D-11): prices (incl. the CR-02-fixed route/service price tokens), phone +420..., flight numbers, and times stay LTR and un-reversed; EntryBar + RouteMap do not break under dir=rtl. A reversed token here is a BUG requiring a fix, not an accepted state.
    - Group 4 Backstops: long ar/hi strings do not clip/overflow; long zh headings break acceptably; taller ar/hi line-height does not overlap a section boundary.
    For each row write PASS or FAIL plus a one-line note; if any FAIL is found, log it and route a follow-up fix (do NOT mark the phase gate green with an open FAIL — per the phase prohibition, a positive finding is a bug). Update the Harvest note to record the pass was actually performed (who/when), superseding the scaffold language. Then confirm completion:
    `cd /Users/romanustyugov/Desktop/Prestigo && test $(grep -c "PENDING — operator" .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md) -eq 0 && grep -Eq "PASS|FAIL" .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md && echo GATE_PASS`
  </how-to-verify>
  <resume-signal>Type "approved" once every Group 1-4 row is recorded PASS (or FAIL with a logged follow-up) and no /ar/ page shows a reversed price/phone/time token or mirrored-layout breakage; otherwise describe the failing rows.</resume-signal>
  <acceptance_criteria>
    - `grep -c "PENDING — operator" .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md` returns `0` — every checklist cell has a real result.
    - Each of Groups 1-4 across the 5 D-10 pages has a recorded PASS (or a FAIL with a logged follow-up fix); the tofu group records /ar/, /hi/login, /zh/login explicitly.
    - The Harvest note states the walkthrough was performed (not scaffolded/queued).
  </acceptance_criteria>
</task>

</tasks>

<excluded_out_of_scope>
## Consciously excluded (NOT gaps for this closure — do not plan or fix here)
- **7 static pages `getLocale()` bug** (about/terms/corporate/privacy/faq/blog-index/contact render EN on every locale, incl. missing Noto class): pre-existing cross-phase bug (WINDOWS #7), predates Phase 73, touches none of the 5 D-10 pages. Tracked in deferred-items.md + WINDOWS.md. Out of scope by decision.
- **/book decorative heading "Your transfer,"** never externalized: Phase 70 STR-02 gap, not RTL-01/FONT-01/TR-02. Out of scope by decision.
- **Stale test fixtures**: already resolved in 73-04/05 (deferred-items.md update note). Nothing to do.
These are recorded so the executor and re-verifier see the omission is deliberate, not an oversight. If the human pass happens to load one of the 7 static pages, note the known EN-fallback but do NOT treat it as a Phase 73 FAIL.
</excluded_out_of_scope>

<threat_model>
## Trust Boundaries
No new trust boundary. QA-only plan: reads/serves the already-built site and records results into a planning doc + a source-reading test. No production code path changes.

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73gap-12 | Repudiation | 73-RTL-QA.md record | low | accept | Human pass recorded with who/when in the Harvest note; automated backstop test provides re-runnable evidence for the code-level half. |
</threat_model>

<artifacts_produced>
## Artifacts this plan produces
- `tests/rtl-backstop.test.ts` — NEW re-runnable source-level backstop asserting every 73-07..73-10 RTL fix is in place.
- `.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md` — Groups 1-4 completed with recorded PASS/FAIL per row; Harvest note updated to "performed".
- No production source symbols created.
</artifacts_produced>

<verification>
- `tests/rtl-backstop.test.ts` green (code-level backstop).
- 73-RTL-QA.md has zero remaining "PENDING — operator"; PASS/FAIL recorded per row; tofu pass covers /ar/, /hi/login, /zh/login.
</verification>

<success_criteria>
The two failed VERIFICATION truths flip to satisfiable on re-verify: (1) /ar/ RTL with no mirrored-layout breakage (CR-01/CR-02/WR fixes + recorded PASS), (2) RTL visual QA on /ar/ passes (completed checklist). FONT-01 tofu behavior-unverified item is confirmed by the recorded live pass.
</success_criteria>

<output>
Create `.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-12-SUMMARY.md` when done.
</output>
