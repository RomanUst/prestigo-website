---
phase: 73-non-latin-rtl-infra-ar-hi-zh
plan: 02
type: execute
wave: 2
depends_on: [73-01]
files_modified:
  - i18n/translation-manifest.json
  - messages/ar.json
  - messages/hi.json
  - messages/zh.json
  - content/routes/ar
  - content/pages/ar
  - content/blog/ar
  - content/routes/hi
  - content/pages/hi
  - content/blog/hi
  - content/routes/zh
  - content/pages/zh
  - content/blog/zh
  - i18n/QA-REPORT.md
autonomous: true
requirements: [TR-02]
user_setup:
  - service: anthropic
    why: "The real (non-dry-run) ar/hi/zh generation run calls the Anthropic API"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: ".env.local (read internally by scripts/i18n-translate.mjs loadEnvLocal(); the sandbox-restricted file is read by the script, not by the agent directly)"
estimate:
  tokens: 70000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "`messages/{ar,hi,zh}.json` have 0 missing keys vs `messages/en.json` with value-type parity — `node scripts/i18n-translate.mjs --check --locales ar,hi,zh` exits 0 (TR-02, SC#1)."
    - "The stale English placeholder catalogs are replaced: a non-`RoutePage` key such as `Nav.items` in `messages/ar.json` is real Arabic, not the pre-existing English string (Pitfall 1 warning-sign cleared)."
    - "`content/{routes,pages,blog}/{ar,hi,zh}/` directories are created and populated by the run (they did not exist before — Pitfall 4)."
    - "`messages/en.json`, `content/*/en/**`, and all `ru`/`es`/`fr` catalog + content files are byte-for-byte unchanged — `git diff --exit-code` on them passes (D-12, SC#4)."
    - "A QA sampling pass across ar/hi/zh is recorded (per-locale completeness, no-English-leakage, DNT/ICU/plural, MDX-structure) in `i18n/QA-REPORT.md` (SC#4)."
    - statement: "Adjacent/duplicate source keys that translate to the same target string are preserved as distinct catalog entries by the pipeline's per-unitKey manifest — no silent merge (TR-02 edge:adjacency)."
      verification: backstop
  artifacts:
    - "i18n/translation-manifest.json (reset then re-recorded for 6 locales) + i18n/translation-manifest.json.pre-73.bak"
    - "messages/ar.json, messages/hi.json, messages/zh.json (real translations)"
    - "content/{routes,pages,blog}/{ar,hi,zh}/** (generated)"
    - "i18n/QA-REPORT.md (ar/hi/zh sampling)"
  key_links:
    - "manifest reset (empty units) → unitsNeedingTranslation() selects all units for ar/hi/zh → translator writes only requested-locale files"
    - "glossary.locales.{ar,hi,zh} (from 73-01) → buildSystemPrompt → per-locale tone + plural verification"
  prohibitions:
    - "MUST NOT ship a catalog that passes the completeness gate while remaining stale English placeholder text — the manifest reset MUST precede the real run (Pitfall 1)."
    - "MUST NOT translate or reverse DNT tokens (prices, 'Prestigo', E-/S-/V-Class, phone, domain) — they stay verbatim in every locale (glossary DNT, verifyDntPreserved)."
    - "MUST NOT emit Unicode bidi-override control characters (U+202A–U+202E, U+2066–U+2069) in translated output that could spoof displayed text direction (defense-in-depth, T-73-02)."
    - "MUST NOT touch en/ru/es/fr output — an `ar,hi,zh`-scoped run writes only those three locales' files."
  assumptions:
    - "[ASSUMPTION · TR-02 · edge:empty] Empty/whitespace source units are excluded from translator calls but still recorded in the manifest (Phase 72 behavior). Planner assumption: this holds for ar/hi/zh; a missing/empty content file is created fresh (Pitfall 4), never 404s. Verified by --check exiting 0."
    - "[ASSUMPTION · TR-02 · edge:ordering] When two source strings are equal, output order/keying is deterministic (driven by the stable en.json key order). Planner assumption: no ordering instability introduced by adding locales. Verified by byte-parity git-diff on en/ru/es/fr."
    - "[ASSUMPTION · A2] The initial ar/hi/zh generation is an orchestrator-run local script invocation (mirroring Phase 72's ru/es/fr local run), not the CI workflow_dispatch path. ANTHROPIC_API_KEY is read from .env.local by loadEnvLocal(); if run in CI instead, the key must be a GitHub secret and the manifest reset a preceding committed step."
  api_coverage: "No external API integration added: re-runs the existing Phase 72 pipeline (Anthropic translator) for three more locales; no new external-API surface."
---

<objective>
Generate complete, real `ar`/`hi`/`zh` catalogs and localized content by re-running the locked Phase 72 pipeline — after a mandatory one-time manifest reset that makes the locale-blind change-detection actually select every unit for the three new locales. Then verify completeness, non-regression, and record a QA sampling pass.

Purpose: This is the TR-02 payload for the three non-Latin locales. The manifest reset is the single most important ordering constraint in the phase: skipping it produces catalogs that pass the completeness gate while staying ~99% English placeholder text (session-verified Pitfall 1).
Output: `messages/{ar,hi,zh}.json` + `content/{routes,pages,blog}/{ar,hi,zh}/**` + `i18n/QA-REPORT.md`, with en/ru/es/fr untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-CONTEXT.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RESEARCH.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-PATTERNS.md
</context>

## Artifacts this phase produces (declared by this plan)

- **New content dirs/files**: `content/routes/{ar,hi,zh}/*.json`, `content/pages/{ar,hi,zh}/*.json`, `content/blog/{ar,hi,zh}/*.mdx` (created fresh — did not exist before).
- **Regenerated catalogs**: `messages/ar.json`, `messages/hi.json`, `messages/zh.json` (overwrite stale EN placeholders).
- **Manifest**: `i18n/translation-manifest.json` (reset then re-recorded for six locales) + backup `i18n/translation-manifest.json.pre-73.bak`.
- **QA report**: `i18n/QA-REPORT.md` (ar/hi/zh sampling section).

<tasks>

<task type="auto">
  <name>Task 1: Manifest reset (mandatory) → real ar/hi/zh generation run → completeness + byte-parity gates</name>
  <files>i18n/translation-manifest.json, messages/ar.json, messages/hi.json, messages/zh.json, content/routes/ar, content/pages/ar, content/blog/ar, content/routes/hi, content/pages/hi, content/blog/hi, content/routes/zh, content/pages/zh, content/blog/zh</files>
  <read_first>
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RESEARCH.md (Pattern 1 — manifest-safe backfill; Pitfall 1 — manifest no-op; Pitfall 4 — content dirs absent)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-PATTERNS.md ("Manifest-safe locale backfill" block)
    - scripts/i18n-translate.mjs (runFullTranslation write loop; --check / --locales handling)
    - scripts/lib/i18n-manifest.mjs (unitsNeedingTranslation, manifest schema)
    - i18n/glossary.json (locales.ar/hi/zh must already exist from 73-01)
  </read_first>
  <precondition>Plan 73-01 committed: `i18n/glossary.json` has `locales.ar/hi/zh` and `PHASE_72_LOCALES` covers six locales. ANTHROPIC_API_KEY resolvable by loadEnvLocal() from .env.local.</precondition>
  <action>
    Back up the manifest to `i18n/translation-manifest.json.pre-73.bak`, then reset `i18n/translation-manifest.json` to an empty units object (`{"version":1,"units":{}}`). This MUST happen before the translator run — the manifest is locale-blind, so an un-reset run skips every unit Phase 72 already processed for ru/es/fr and leaves ar/hi/zh as stale English placeholders (Pitfall 1). Then run the real pipeline scoped to only the three new locales: `node scripts/i18n-translate.mjs --locales ar,hi,zh`. The write loop is locale-scoped via `targetPathFor(locale)`, so ru/es/fr/EN files are never opened for writing regardless of manifest state. After the run, run `node scripts/i18n-translate.mjs --check --locales ar,hi,zh` (0 missing keys), and assert non-regression with `git diff --exit-code` on `messages/en.json content/routes/en content/pages/en content/blog/en messages/ru.json messages/es.json messages/fr.json content/routes/ru content/routes/es content/routes/fr content/pages/ru content/pages/es content/pages/fr content/blog/ru content/blog/es content/blog/fr`. Finally spot-check a non-`RoutePage` key in `messages/ar.json` (e.g. `Nav.items`) is real Arabic, not the pre-existing English placeholder — the Pitfall-1 warning-sign check.
  </action>
  <acceptance_criteria>
    - `node scripts/i18n-translate.mjs --check --locales ar,hi,zh` exits 0 (0 missing keys per locale).
    - `git diff --exit-code -- messages/en.json content/routes/en content/pages/en content/blog/en` exits 0 (EN untouched).
    - `git diff --exit-code -- messages/ru.json messages/es.json messages/fr.json content/routes/ru content/routes/es content/routes/fr content/pages/ru content/pages/es content/pages/fr content/blog/ru content/blog/es content/blog/fr` exits 0 (ru/es/fr untouched, D-12).
    - `ls content/routes/ar content/pages/ar content/blog/ar content/routes/hi content/routes/zh` all exist and are non-empty.
    - `node -e "const a=require('./messages/ar.json'); const s=JSON.stringify(a.Nav||{}); process.exit(/[؀-ۿ]/.test(s)?0:1)"` exits 0 (Arabic script present in Nav namespace — placeholder replaced).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/i18n-translate.mjs --check --locales ar,hi,zh && git diff --exit-code -- messages/en.json messages/ru.json messages/es.json messages/fr.json</automated>
  </verify>
  <done>Manifest reset ran before generation; ar/hi/zh catalogs + content generated with 0 missing keys and real (non-placeholder) script; en/ru/es/fr byte-for-byte unchanged; committed.</done>
</task>

<task type="auto">
  <name>Task 2: QA sampling record (ar/hi/zh) — leakage, DNT, plural, MDX structure, mixed-content spot-check</name>
  <files>i18n/QA-REPORT.md</files>
  <read_first>
    - scripts/lib/i18n-qa-report.mjs (generateQaReport, checkNoEnglishLeakage sections)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RESEARCH.md (Validation Architecture — Test Map; D-11 mixed LTR-in-RTL)
    - .planning/phases/72-ai-translation-pipeline-catalogs/72-05-SUMMARY.md (Phase 72 QA sampling precedent)
    - i18n/QA-REPORT.md (existing ru/es/fr sections, if present)
  </read_first>
  <action>
    Ensure the pipeline's QA report at `i18n/QA-REPORT.md` covers ar/hi/zh: per-locale completeness, no-English-leakage (scoped to processed units, DNT-only values excluded), DNT/ICU-variable/plural-category, and MDX-structure sections. Add a recorded mixed LTR-in-RTL sampling note (D-11): spot-check that DNT tokens inside sampled `ar` strings — prices (`€…`), phone `+420 725 986 855`, flight/time patterns — appear verbatim and un-reversed in the source JSON (the bidi-isolated visual rendering is verified in Plan 73-06). Record the sample count per locale.
  </action>
  <acceptance_criteria>
    - `grep -c "ar\|hi\|zh" i18n/QA-REPORT.md` shows all three locales present in the report.
    - `grep -c "No-English-Leakage\|DNT\|Plural\|MDX" i18n/QA-REPORT.md` > 0 (all QA sections present).
    - `npx vitest run tests/i18n-completeness.test.ts` passes (report-generation + leakage tests green, now including ar/hi/zh fixtures).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/i18n-completeness.test.ts</automated>
  </verify>
  <done>i18n/QA-REPORT.md records the ar/hi/zh sampling pass with completeness/leakage/DNT/plural/MDX results and the D-11 mixed-content spot-check; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anthropic API → catalog/content files | Model output written to JSON/MDX consumed at build/render time — semi-trusted output of a locked system prompt |
| glossary.json (owner-editable) → system prompt | DNT/tone rules feed the translator prompt; malformed glossary fails loud (zod) |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-01 | Tampering (XSS via localized MDX/JSON) | translated blog/route content | medium | mitigate | React auto-escapes rendered content; blog MDX is compiled (not `dangerouslySetInnerHTML`); existing `verifyDntPreserved`/`verifyMdxStructure` fail-closed on structural breakage |
| T-73-02 | Spoofing (RTL bidi-override injection) | translated ar output | low | mitigate | Content originates from a locked-prompt Anthropic call (not untrusted user input); QA leakage/DNT verifiers run; prohibition bars emitting U+202A–U+202E / U+2066–U+2069 |
| T-73-04 | Tampering (glossary/DNT bypass) | pipeline verifiers | medium | mitigate | `verifyDntPreserved` + `verifyPluralCategories` run per unit; a unit failing verification falls back to EN and is excluded from the manifest (retried next run), never silently shipped broken |
| T-73-SC | Tampering (supply chain) | npm installs | low | accept | No new packages this phase — pipeline reuses installed `@anthropic-ai/sdk@0.125.0` |

No `high`-severity threats — block-on threshold not triggered.
</threat_model>

<verification>
- `node scripts/i18n-translate.mjs --check --locales ar,hi,zh` exits 0.
- `git diff --exit-code` on en + ru/es/fr catalogs/content passes (D-12).
- `npx vitest run` full suite green.
- `i18n/QA-REPORT.md` records ar/hi/zh sampling.
</verification>

<success_criteria>
Complete ar/hi/zh catalogs + content generated (0 missing keys, real script, DNT preserved, correct plural categories); en/ru/es/fr non-regression proven; QA sampling recorded (TR-02, SC#1 + SC#4).
</success_criteria>

<output>
Create `.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-02-SUMMARY.md` when done.
</output>
