---
phase: 73-non-latin-rtl-infra-ar-hi-zh
plan: 09
type: execute
wave: 2
depends_on: ["73-08"]
files_modified:
  - app/[locale]/routes/prague-bratislava/page.tsx
  - app/[locale]/routes/prague-brno/page.tsx
  - app/[locale]/routes/prague-budapest/page.tsx
  - app/[locale]/routes/prague-ceske-budejovice/page.tsx
  - app/[locale]/routes/prague-cesky-krumlov/page.tsx
  - app/[locale]/routes/prague-dresden/page.tsx
  - app/[locale]/routes/prague-frantiskovy-lazne/page.tsx
  - app/[locale]/routes/prague-graz/page.tsx
  - app/[locale]/routes/prague-hradec-kralove/page.tsx
  - app/[locale]/routes/prague-karlovy-vary/page.tsx
  - app/[locale]/routes/prague-krakow/page.tsx
  - app/[locale]/routes/prague-kutna-hora/page.tsx
  - app/[locale]/routes/prague-leipzig/page.tsx
  - app/[locale]/routes/prague-liberec/page.tsx
  - app/[locale]/routes/prague-linz/page.tsx
  - app/[locale]/routes/prague-marianske-lazne/page.tsx
  - app/[locale]/routes/prague-munich/page.tsx
  - app/[locale]/routes/prague-nuremberg/page.tsx
  - app/[locale]/routes/prague-olomouc/page.tsx
  - app/[locale]/routes/prague-ostrava/page.tsx
  - app/[locale]/routes/prague-pardubice/page.tsx
  - app/[locale]/routes/prague-passau/page.tsx
  - app/[locale]/routes/prague-plzen/page.tsx
  - app/[locale]/routes/prague-regensburg/page.tsx
  - app/[locale]/routes/prague-salzburg/page.tsx
  - app/[locale]/routes/prague-vienna/page.tsx
  - app/[locale]/routes/prague-warsaw/page.tsx
  - app/[locale]/routes/prague-wroclaw/page.tsx
  - app/[locale]/routes/prague-zlin/page.tsx
  - app/[locale]/services/airport-transfer/page.tsx
  - app/[locale]/services/page.tsx
autonomous: true
gap_closure: true
requirements: [RTL-01]
estimate:
  tokens: 55000
  raw_tokens: 44000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every remaining route page (the 29 prague-* pages other than prague-berlin) plus services/airport-transfer and services/page.tsx isolate each interpolated price/number token rendered inside translated Arabic prose in a <bdi> (RTL-01, D-11 — closes CR-02 sweep; mirrors failed VERIFICATION truth #2 'no mirrored-layout breakage')."
    - "No full translated sentence is wrapped in a single <bdi> anywhere in the swept files (avoids the WR-05 inverse bug)."
  artifacts:
    - "29 route page.tsx files under app/[locale]/routes/"
    - app/[locale]/services/airport-transfer/page.tsx
    - app/[locale]/services/page.tsx
  key_links:
    - "translated route/service content + prices -> interpolateBidi()/inline <bdi> -> isolated price token in RTL prose"
---

<objective>
Complete the CR-02 audit-and-fix sweep the code review called for: apply the same DNT price bidi-isolation pattern proven in 73-08 to the remaining route and service pages that render AI-translated Arabic content with interpolated prices. These pages share prague-berlin's template shape, so the fix is mechanical but must be verified per-file (metadata `interpolate()` must NOT be swapped).

Purpose: prague-berlin was the "representative sample" — the review explicitly recommends auditing the other ~29 route pages + remaining service pages for the identical gap. Leaving them unfixed means a reversed price/number token is a live possibility on /ar/ route/service pages, which the D-10/D-11 walkthrough (73-12) would flag as a bug.
Output: all listed route/service pages isolate their rendered price tokens.

Depends on 73-08 for the `interpolateBidi` helper.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-REVIEW.md
@app/[locale]/routes/prague-berlin/page.tsx
@lib/content-interpolate.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep the 29 remaining route pages (CR-02)</name>
  <files>app/[locale]/routes/prague-*/page.tsx (all 29 except prague-berlin — full list in frontmatter files_modified)</files>
  <read_first>
    - app/[locale]/routes/prague-berlin/page.tsx (the fixed reference from 73-08 — the exact render-site pattern to replicate)
    - lib/content-interpolate.ts (`interpolate` for metadata vs `interpolateBidi` for render sites)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-08-SUMMARY.md (the concrete site-by-site decisions taken on prague-berlin)
    - For each target file: grep it first for `interpolate(` and `{v.price}`/`{c.price}` to locate its render sites before editing.
  </read_first>
  <action>
    For each of the 29 route pages listed in `files_modified` (all `app/[locale]/routes/prague-*/page.tsx` EXCEPT prague-berlin, which 73-08 already fixed): mirror the prague-berlin fix from 73-08. Concretely, per file: (1) add `interpolateBidi` to the existing `interpolate` import from `@/lib/content-interpolate`; (2) at RENDER sites where a price token is interpolated into translated prose (the hero-intro paragraph and the CTA `headingItalic` span — the sites that call `interpolate(content.X, prices)` inside JSX, not in `generateMetadata`), swap `interpolate(` for `interpolateBidi(`; (3) for bare price-token renders `{v.price}` and `{c.price}`, wrap the rendered value inline as `<bdi>{v.price}</bdi>` / `<bdi>{c.price}</bdi>`. Do NOT touch the `interpolate()` calls inside `generateMetadata()` (title/description/ogTitle/ogDescription) — those must stay plain strings. If a given file's render sites differ from prague-berlin's (some routes may omit a day-trip block), apply the pattern only where a price token is actually rendered — never wrap a whole sentence. Process files in small batches and type-check between batches.
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo && MISSING=$(for f in $(ls app/[locale]/routes/prague-*/page.tsx | grep -v prague-berlin); do grep -q "interpolateBidi\|<bdi>" "$f" || echo "$f"; done); echo "UNISOLATED: [$MISSING]"; test -z "$MISSING" && npx tsc --noEmit 2>&1 | grep -c "routes/prague" | grep -qx 0 && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - Every `app/[locale]/routes/prague-*/page.tsx` (excluding prague-berlin) contains at least one `interpolateBidi` or inline `<bdi>` at a price render site — the sweep leaves no route page unisolated (the `UNISOLATED: []` list is empty).
    - No route page has a `generateMetadata` `interpolate()` call converted to `interpolateBidi` (metadata still returns strings; `npx tsc --noEmit` reports zero errors under `routes/prague`).
    - No route page wraps a full translated sentence in a single `<bdi>` (spot-check: `interpolateBidi` used for sentence fields, inline `<bdi>` only around bare tokens).
  </acceptance_criteria>
  <done>All 29 remaining route pages isolate their rendered price tokens for /ar/, matching prague-berlin; metadata untouched; all type-check.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep airport-transfer + services index (CR-02)</name>
  <files>app/[locale]/services/airport-transfer/page.tsx, app/[locale]/services/page.tsx</files>
  <read_first>
    - app/[locale]/services/airport-transfer/page.tsx (render sites: line 82 `vehicleClasses` map -> `{v.price}` at line 263; `heroIntro` at line 83; metadata `interpolate` at line 42 — keep as string)
    - app/[locale]/services/page.tsx (render site: `{s.price}` at lines 186-188; `s.price` precompute at line 103; metadata/JSON-LD `interpolate`/`s.price.replace` untouched)
    - app/[locale]/services/city-rides/page.tsx (the 73-08 reference fix for the service-page shape)
    - lib/content-interpolate.ts
  </read_first>
  <action>
    Apply the same CR-02 pattern to the two remaining service files. `app/[locale]/services/airport-transfer/page.tsx`: import `interpolateBidi`; if `heroIntro` (line 83) is rendered inside JSX prose with an embedded price, render it via `interpolateBidi` at its render site (or switch its precompute); wrap the bare vehicle-price render `{v.price}` at line 263 as `<bdi>{v.price}</bdi>`; leave the metadata `interpolate` at line 42 as a string. `app/[locale]/services/page.tsx`: import `interpolateBidi`; wrap the rendered `{s.price}` at lines 186-188 as `<bdi>{s.price}</bdi>` (a bare price token); leave the JSON-LD `s.price.replace(...)` at line 128 and the FAQ/metadata `interpolate()` calls as strings. Never wrap a full sentence in one `<bdi>`.
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo && grep -q "<bdi>{v.price}</bdi>" app/[locale]/services/airport-transfer/page.tsx && grep -q "<bdi>{s.price}</bdi>" app/[locale]/services/page.tsx && npx tsc --noEmit 2>&1 | grep -Ei "services/(airport-transfer|page)" ; test $? -ne 0 && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `app/[locale]/services/airport-transfer/page.tsx` wraps its rendered vehicle price token in `<bdi>` and (if `heroIntro` carries a price) isolates it via `interpolateBidi`; metadata `interpolate` at line 42 unchanged.
    - `app/[locale]/services/page.tsx` wraps the rendered `{s.price}` in `<bdi>`; JSON-LD `s.price.replace` and FAQ/metadata `interpolate` unchanged.
    - Both files type-check.
  </acceptance_criteria>
  <done>airport-transfer and the services index isolate their rendered price tokens for /ar/; JSON-LD and metadata untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
No new trust boundary. Identical presentational bidi-isolation as 73-08 replicated across more files; no input, no dependency, no data-flow change.

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73gap-09 | Tampering | route/service page.tsx files | low | accept | React `createElement`/JSX auto-escaped `<bdi>`; no string HTML; no package install. |
</threat_model>

<artifacts_produced>
## Artifacts this plan produces
- 29 `app/[locale]/routes/prague-*/page.tsx` files (all except prague-berlin) — `interpolateBidi`/inline `<bdi>` at price render sites; metadata `interpolate()` untouched.
- `app/[locale]/services/airport-transfer/page.tsx` — vehicle price + hero-intro isolation.
- `app/[locale]/services/page.tsx` — `{s.price}` isolation.
- No new symbols; consumes `interpolateBidi` from 73-08.
</artifacts_produced>

<verification>
- Sweep leaves no route page unisolated (empty `UNISOLATED` list); both service files isolate their price renders.
- `npx tsc --noEmit` clean across the swept files; metadata/JSON-LD `interpolate()` calls unchanged.
</verification>

<success_criteria>
CR-02 fully closed across all translated route/service pages; no whole-sentence bdi; no metadata regression.
</success_criteria>

<output>
Create `.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-09-SUMMARY.md` when done.
</output>
