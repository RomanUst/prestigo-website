---
phase: 73-non-latin-rtl-infra-ar-hi-zh
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/[locale]/blog/prague-airport-taxi-vs-chauffeur/page.tsx
  - app/[locale]/blog/prague-airport-to-city-center/page.tsx
  - app/[locale]/blog/prague-vienna-transfer-vs-train/page.tsx
autonomous: true
gap_closure: true
requirements: [RTL-01]
estimate:
  tokens: 26000
  raw_tokens: 21000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Residual physical-direction CSS in app/globals.css (.skip-link, .rdp-nav, .rdp-month_caption, .cta-text) uses logical properties so the skip-link, calendar nav/caption and CTA hover-zoom anchor to the reading-start side under dir=rtl (RTL-01 — closes WR-02; mirrors failed VERIFICATION truth #2 'no mirrored-layout breakage')."
    - "The three static blog CTA headings isolate only the DNT price/number token in <bdi>, not the whole sentence (RTL-01, D-11 — closes WR-05, the inverse of CR-02)."
  artifacts:
    - app/globals.css
    - app/[locale]/blog/prague-airport-taxi-vs-chauffeur/page.tsx
    - app/[locale]/blog/prague-airport-to-city-center/page.tsx
    - app/[locale]/blog/prague-vienna-transfer-vs-train/page.tsx
  key_links:
    - "dir=rtl -> logical CSS insets/text-align -> reading-start anchoring"
    - "blog CTA price token -> narrow <bdi> -> isolated numeral, prose translatable"
---

<objective>
Close the two lower-severity RTL residuals the review fully diagnosed: WR-02 (physical-direction CSS left unconverted in `app/globals.css`) and WR-05 (over-broad `<bdi>` scope in the three static blog CTA headings). Both are already fully specified in 73-REVIEW.md with the exact file:line and fix.

Purpose: These residuals are on or adjacent to the D-10 QA surfaces; converting them removes remaining mirrored-layout anchoring bugs and brings the blog CTAs in line with the Hero.tsx isolate-the-token-only pattern before the 73-12 visual walkthrough.
Output: logical-property CSS in globals.css; narrowed `<bdi>` scope in three blog pages.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-REVIEW.md
@components/Hero.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert residual physical-direction CSS to logical properties (WR-02)</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (`.cta-text` around line 308; `.skip-link` around lines 554-562; `.rdp-nav` around line 579; `.rdp-month_caption` around line 588)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-REVIEW.md section WR-02 (the four rules + proposed logical equivalents)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-PATTERNS.md section globals.css (additive :lang() pattern precedent — same file is already RTL-aware for letter-spacing)
  </read_first>
  <action>
    In `app/globals.css` convert four residual physical-direction rules to logical equivalents:
    (1) `.skip-link` (~line 555): replace the physical left inset with `inset-inline-start: 1rem;` so the focus-visible skip link pins to the reading-start side (the `:focus` top rule is unaffected).
    (2) `.rdp-nav` (~line 579): replace the physical right inset with `inset-inline-end: 0;` so the calendar prev/next nav sits at the reading-end side (keep the `!important` and other declarations).
    (3) `.rdp-month_caption` (~line 588): change the physical text-align value to `text-align: start;` so the caption follows reading direction.
    (4) `.cta-text` (~line 308): the hover-zoom transform-origin is physically left-anchored; change it to the default `transform-origin: 50% 50%;` (center) so the zoom is direction-neutral. Leave all other `.cta-text` declarations (transition, will-change, border-bottom) untouched.
    Convert values only — do not rename selectors, add `!important` where none existed, or touch unrelated rules.
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo && grep -q "inset-inline-start: 1rem" app/globals.css && grep -q "inset-inline-end: 0" app/globals.css && grep -q "text-align: start" app/globals.css && test $(grep -c "transform-origin: left center" app/globals.css) -eq 0 && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "inset-inline-start: 1rem" app/globals.css` matches (`.skip-link` converted); `grep -c "left: 1rem" app/globals.css` returns `0`.
    - `grep -q "inset-inline-end: 0" app/globals.css` matches (`.rdp-nav` converted).
    - `grep -q "text-align: start" app/globals.css` matches (`.rdp-month_caption` converted).
    - `grep -c "transform-origin: left center" app/globals.css` returns `0` (`.cta-text` de-anchored to center).
  </acceptance_criteria>
  <done>Skip-link, calendar nav/caption, and CTA hover-zoom follow reading direction under dir=rtl; LTR rendering is visually unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Narrow blog CTA <bdi> scope to the DNT token only (WR-05)</name>
  <files>app/[locale]/blog/prague-airport-taxi-vs-chauffeur/page.tsx, app/[locale]/blog/prague-airport-to-city-center/page.tsx, app/[locale]/blog/prague-vienna-transfer-vs-train/page.tsx</files>
  <read_first>
    - app/[locale]/blog/prague-airport-taxi-vs-chauffeur/page.tsx (CTA heading around line 488)
    - app/[locale]/blog/prague-airport-to-city-center/page.tsx (CTA heading around line 599)
    - app/[locale]/blog/prague-vienna-transfer-vs-train/page.tsx (CTA heading around line 541)
    - components/Hero.tsx lines 79-89 (the correct pattern: isolate ONLY the numeral/price run, keep prose outside the isolate)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-REVIEW.md section WR-05 (proposed narrowed fix)
  </read_first>
  <action>
    In each of the three blog CTA headings the whole closing sentence is currently wrapped in a single `<bdi>` element that also carries the `display-italic` class, so plain prose words sit inside the isolate — the inverse of the CR-02 pattern. Restructure each so the `display-italic` class moves to a surrounding `<span>` and each `<bdi>` wraps only its DNT numeric run:
    - prague-airport-taxi-vs-chauffeur (~line 488): isolate the price `€69`; the words "fixed, chauffeur inside Arrivals." stay as ordinary prose inside the `display-italic` span, outside the `<bdi>`.
    - prague-airport-to-city-center (~line 599): isolate the price `€69`; "fixed. Chauffeur inside Arrivals." stays outside the `<bdi>`.
    - prague-vienna-transfer-vs-train (~line 541): isolate BOTH numeric runs in their own `<bdi>` — the price `€455` and the duration `3h 15min` (a time run per D-11); "fixed." and "door-to-door." stay as prose outside the isolates.
    Keep the visible text and the `display-italic` styling identical; only the isolate boundary changes. Do NOT introduce a translation call (these pages are static English JSX by design — see the out-of-scope note).
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo && test $(grep -rc 'bdi className="display-italic"' app/[locale]/blog/prague-airport-taxi-vs-chauffeur/page.tsx app/[locale]/blog/prague-airport-to-city-center/page.tsx app/[locale]/blog/prague-vienna-transfer-vs-train/page.tsx | awk -F: '{s+=$2} END{print s}') -eq 0 && grep -q "<bdi>€69</bdi>" app/[locale]/blog/prague-airport-taxi-vs-chauffeur/page.tsx && grep -q "<bdi>€455</bdi>" app/[locale]/blog/prague-vienna-transfer-vs-train/page.tsx && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - None of the three files still applies `display-italic` directly on a `<bdi>` element (the whole-sentence isolate is gone) — the summed count of `bdi className="display-italic"` across the three files is `0`.
    - Each `<bdi>` now wraps only a numeric/price run: `<bdi>€69</bdi>` in the two airport posts, `<bdi>€455</bdi>` plus a `<bdi>` around the `3h 15min` duration in the vienna post.
    - Visible CTA text and `display-italic` styling are unchanged (class moved to a wrapping span).
  </acceptance_criteria>
  <done>Blog CTA headings isolate only the DNT price/time tokens, matching the Hero.tsx pattern; no prose sits inside a `<bdi>`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
No new trust boundary. CSS logical-property value swaps and static-JSX markup re-scoping only; no input, no data flow, no dependency.

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73gap-10 | Tampering | app/globals.css / blog page.tsx | low | accept | Presentational only; no package install; no injection surface. |
</threat_model>

<artifacts_produced>
## Artifacts this plan produces
- `app/globals.css` — `.skip-link` -> `inset-inline-start`, `.rdp-nav` -> `inset-inline-end`, `.rdp-month_caption` -> `text-align: start`, `.cta-text` -> centered `transform-origin`.
- `app/[locale]/blog/prague-airport-taxi-vs-chauffeur/page.tsx` — CTA `<bdi>` narrowed to `€69`.
- `app/[locale]/blog/prague-airport-to-city-center/page.tsx` — CTA `<bdi>` narrowed to `€69`.
- `app/[locale]/blog/prague-vienna-transfer-vs-train/page.tsx` — CTA `<bdi>` narrowed to `€455` and `3h 15min`.
- No new symbols; no files created.
</artifacts_produced>

<excluded_out_of_scope>
The three blog pages are intentionally static English JSX (locked EN-only in Phase 72, per 73-CONTEXT Deferred Ideas). WR-05 is a bidi-scope correctness fix only — do NOT wire these pages to the translation pipeline; that is explicitly out of scope for Phase 73.
</excluded_out_of_scope>

<verification>
- globals.css: all four logical conversions present; no residual physical `left: 1rem` / `transform-origin: left center`.
- Blog CTAs: no whole-sentence `bdi className="display-italic"`; `<bdi>` wraps only numeric/price/time runs.
</verification>

<success_criteria>
WR-02 and WR-05 closed; LTR rendering visually unchanged; blog CTAs match the Hero.tsx isolate-the-token pattern.
</success_criteria>

<output>
Create `.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-10-SUMMARY.md` when done.
</output>
