---
phase: 73-non-latin-rtl-infra-ar-hi-zh
plan: 04
type: execute
wave: 2
depends_on: [73-01]
files_modified:
  - app/[locale]/routes/prague-berlin/page.tsx
  - app/[locale]/routes/prague-bratislava/page.tsx
  - app/[locale]/routes/prague-brno/page.tsx
  - app/[locale]/routes/prague-budapest/page.tsx
  - app/[locale]/routes/prague-ceske-budejovice/page.tsx
  - app/[locale]/routes/prague-cesky-krumlov/page.tsx
  - app/[locale]/routes/prague-dresden/page.tsx
  - app/[locale]/routes/prague-frantiskovy-lazne/page.tsx
  - app/[locale]/routes/prague-graz/page.tsx
  - app/[locale]/routes/prague-hradec-kralove/page.tsx
  - app/[locale]/routes/prague-karlovy-vary/page.tsx
  - app/[locale]/routes/prague-krakow/page.tsx
  - app/[locale]/routes/prague-kutna-hora/page.tsx
  - app/[locale]/routes/prague-leipzig/page.tsx
  - app/[locale]/routes/prague-liberec/page.tsx
  - app/[locale]/routes/prague-linz/page.tsx
  - app/[locale]/routes/prague-marianske-lazne/page.tsx
  - app/[locale]/routes/prague-munich/page.tsx
  - app/[locale]/routes/prague-nuremberg/page.tsx
  - app/[locale]/routes/prague-olomouc/page.tsx
  - app/[locale]/routes/prague-ostrava/page.tsx
  - app/[locale]/routes/prague-pardubice/page.tsx
  - app/[locale]/routes/prague-passau/page.tsx
  - app/[locale]/routes/prague-plzen/page.tsx
  - app/[locale]/routes/prague-regensburg/page.tsx
  - app/[locale]/routes/prague-salzburg/page.tsx
  - app/[locale]/routes/prague-vienna/page.tsx
  - app/[locale]/routes/prague-warsaw/page.tsx
  - app/[locale]/routes/prague-wroclaw/page.tsx
  - app/[locale]/routes/prague-zlin/page.tsx
  - app/[locale]/services/city-rides/page.tsx
autonomous: true
requirements: [RTL-01]
estimate:
  tokens: 95000
  raw_tokens: 57000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every `text-left`/`text-right` occurrence across the 30 route pages + the city-rides service page is converted to `text-start`/`text-end` — `grep -rlE \"text-(left|right)\" app/[locale]/routes app/[locale]/services` returns no files (RTL-01)."
    - "The indexable/noindex split and every route's current English URL/output are unchanged — the conversion renders identically under `dir=\"ltr\"` (RTL-01, D-12, SC preserved)."
    - "No spacing/pixel value changed — only the `text-left→text-start` / `text-right→text-end` axis name (spacing parity)."
  artifacts:
    - "30 converted app/[locale]/routes/*/page.tsx"
    - "app/[locale]/services/city-rides/page.tsx converted"
  key_links:
    - "html dir=\"rtl\" → text-start/text-end resolve to right-alignment on /ar/, left-alignment identical to pre-conversion on LTR locales"
  prohibitions:
    - "MUST NOT change any spacing token, layout structure, price, or content — this plan is a pure `text-*` axis swap."
    - "MUST NOT introduce `space-x-*`/`flex-row-reverse` or alter the indexable/noindex metadata split."
  assumptions:
    - "[ASSUMPTION · RTL-01 · edge:adjacency] Route pages contain only `text-left`/`text-right` physical classes (grep-confirmed — no border/margin/absolute). Planner assumption: no route page mixes a converted and un-converted directional class on one element. Verified by grep-count 0 post-conversion."
    - "[ASSUMPTION · RTL-01 · edge:empty] A route page with zero `text-left`/`text-right` occurrences (if any) is a no-op — the swap simply finds nothing. Verified by per-file grep."
    - "[ASSUMPTION · RTL-01 · edge:ordering] Where multiple `text-*` classes appear on sibling elements, their converted order is positionally identical (1:1 in-place swap, no reordering). Verified by byte-parity snapshot."
  api_coverage: "No external API integration in this plan."
---

<objective>
Apply the mechanical, judgment-free physical→logical text-alignment swap (`text-left`→`text-start`, `text-right`→`text-end`) across all 30 route pages and the city-rides service page, so `/ar/` route bodies right-align while en/ru/es/fr output stays byte-for-byte identical.

Purpose: This is the bulk of the RTL-01 file count (31 of ~40 files), and it is deliberately isolated because it is a single repeated swap with zero design decisions — the byte-parity snapshot gate is the safety net. Scope is the public `app/[locale]/**` tree only (D-01 — admin/driver stay physical LTR), using native Tailwind logical utilities with no plugin (D-02).
Output: 31 route/service pages converted to logical text alignment.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-PATTERNS.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-UI-SPEC.md
</context>

## Artifacts this phase produces (declared by this plan)

- **Converted route pages**: all 30 `app/[locale]/routes/*/page.tsx` files (text-alignment logical).
- **Converted service page**: `app/[locale]/services/city-rides/page.tsx`.

Note on file counts: each task below batches ~10 near-identical files. This exceeds the usual 5-file soft cap intentionally — the change is a single greppable 1:1 axis swap with no per-file design decision, and the byte-parity snapshot (`tests/route-page-render.test.tsx`) plus a grep-count gate verify correctness cheaply. Use grep to locate each occurrence and edit that line; a full read of every large page is not required.

<tasks>

<task type="auto">
  <name>Task 1: Convert route pages batch A (berlin → hradec-kralove) + city-rides service</name>
  <files>app/[locale]/routes/prague-berlin/page.tsx, app/[locale]/routes/prague-bratislava/page.tsx, app/[locale]/routes/prague-brno/page.tsx, app/[locale]/routes/prague-budapest/page.tsx, app/[locale]/routes/prague-ceske-budejovice/page.tsx, app/[locale]/routes/prague-cesky-krumlov/page.tsx, app/[locale]/routes/prague-dresden/page.tsx, app/[locale]/routes/prague-frantiskovy-lazne/page.tsx, app/[locale]/routes/prague-graz/page.tsx, app/[locale]/routes/prague-hradec-kralove/page.tsx, app/[locale]/services/city-rides/page.tsx</files>
  <read_first>
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-PATTERNS.md ("Route pages text-right" — line ~315 pattern; general conversion table)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-UI-SPEC.md (conversion table: text-left→text-start, text-right→text-end)
    - each file being edited (grep each for `text-left`/`text-right`, then edit those lines)
  </read_first>
  <action>
    In each of the 11 listed files, replace every `text-left` with `text-start` and every `text-right` with `text-end` at the identical position — no other change. These files contain only `text-*` physical classes (grep-confirmed: no border/margin/absolute-position physical classes). Do not touch prices, metadata, structure, or spacing tokens.
  </action>
  <acceptance_criteria>
    - `grep -rlE "text-(left|right)" app/[locale]/routes/prague-berlin app/[locale]/routes/prague-bratislava app/[locale]/routes/prague-brno app/[locale]/routes/prague-budapest app/[locale]/routes/prague-ceske-budejovice app/[locale]/routes/prague-cesky-krumlov app/[locale]/routes/prague-dresden app/[locale]/routes/prague-frantiskovy-lazne app/[locale]/routes/prague-graz app/[locale]/routes/prague-hradec-kralove app/[locale]/services/city-rides` returns no files.
    - `npx vitest run tests/route-page-render.test.tsx` passes (byte-parity on the snapshotted route, e.g. prague-vienna, is in batch C but the technique confirms LTR-identical rendering).
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(grep -rlE 'text-(left|right)' app/[locale]/routes/prague-berlin app/[locale]/routes/prague-brno app/[locale]/services/city-rides 2>/dev/null)" && echo OK</automated>
  </verify>
  <done>Batch A + service page have zero `text-left`/`text-right`; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Convert route pages batch B (karlovy-vary → olomouc)</name>
  <files>app/[locale]/routes/prague-karlovy-vary/page.tsx, app/[locale]/routes/prague-krakow/page.tsx, app/[locale]/routes/prague-kutna-hora/page.tsx, app/[locale]/routes/prague-leipzig/page.tsx, app/[locale]/routes/prague-liberec/page.tsx, app/[locale]/routes/prague-linz/page.tsx, app/[locale]/routes/prague-marianske-lazne/page.tsx, app/[locale]/routes/prague-munich/page.tsx, app/[locale]/routes/prague-nuremberg/page.tsx, app/[locale]/routes/prague-olomouc/page.tsx</files>
  <read_first>
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-PATTERNS.md (conversion table)
    - each file being edited (grep for `text-left`/`text-right`)
  </read_first>
  <action>
    Same pure `text-left`→`text-start` / `text-right`→`text-end` swap across the 10 listed route pages. No other change.
  </action>
  <acceptance_criteria>
    - `grep -rlE "text-(left|right)" app/[locale]/routes/prague-karlovy-vary app/[locale]/routes/prague-krakow app/[locale]/routes/prague-kutna-hora app/[locale]/routes/prague-leipzig app/[locale]/routes/prague-liberec app/[locale]/routes/prague-linz app/[locale]/routes/prague-marianske-lazne app/[locale]/routes/prague-munich app/[locale]/routes/prague-nuremberg app/[locale]/routes/prague-olomouc` returns no files.
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(grep -rlE 'text-(left|right)' app/[locale]/routes/prague-karlovy-vary app/[locale]/routes/prague-munich app/[locale]/routes/prague-olomouc 2>/dev/null)" && echo OK</automated>
  </verify>
  <done>Batch B route pages have zero `text-left`/`text-right`; committed.</done>
</task>

<task type="auto">
  <name>Task 3: Convert route pages batch C (ostrava → zlin) + byte-parity snapshot gate</name>
  <files>app/[locale]/routes/prague-ostrava/page.tsx, app/[locale]/routes/prague-pardubice/page.tsx, app/[locale]/routes/prague-passau/page.tsx, app/[locale]/routes/prague-plzen/page.tsx, app/[locale]/routes/prague-regensburg/page.tsx, app/[locale]/routes/prague-salzburg/page.tsx, app/[locale]/routes/prague-vienna/page.tsx, app/[locale]/routes/prague-warsaw/page.tsx, app/[locale]/routes/prague-wroclaw/page.tsx, app/[locale]/routes/prague-zlin/page.tsx</files>
  <read_first>
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-PATTERNS.md (prague-vienna line ~315 `text-right` example)
    - tests/route-page-render.test.tsx (prague-vienna golden-HTML snapshot — in this batch)
    - each file being edited (grep for `text-left`/`text-right`)
  </read_first>
  <action>
    Same pure `text-left`→`text-start` / `text-right`→`text-end` swap across the 10 listed route pages (includes prague-vienna, which is the byte-parity snapshot fixture). No other change. After conversion, run the byte-parity snapshot to confirm prague-vienna renders identically under `dir="ltr"` for en/ru/es/fr.
  </action>
  <acceptance_criteria>
    - `grep -rlE "text-(left|right)" app/[locale]/routes` returns no files across ALL 30 route pages (phase-wide route completeness).
    - `npx vitest run tests/route-page-render.test.tsx` passes (prague-vienna byte-parity, `priceValidUntil` normalized).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/route-page-render.test.tsx && test -z "$(grep -rlE 'text-(left|right)' app/[locale]/routes 2>/dev/null)" && echo OK</automated>
  </verify>
  <done>All 30 route pages have zero `text-left`/`text-right`; prague-vienna byte-parity green; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Pure presentational class swap — no data, auth, or input boundary crossed |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-05 | Tampering (accidental content/SEO change) | route/service pages | low | mitigate | Pure `text-*` axis swap; byte-parity snapshot + grep-count gates catch any unintended diff; indexable/noindex metadata untouched |
| T-73-SC | Tampering (supply chain) | npm installs | low | accept | No new packages — core Tailwind logical utilities |

No `high`-severity threats — block-on threshold not triggered.
</threat_model>

<verification>
- `grep -rlE "text-(left|right)" app/[locale]/routes app/[locale]/services` returns no files.
- `npx vitest run tests/route-page-render.test.tsx` byte-parity green.
</verification>

<success_criteria>
All 30 route pages + city-rides service page use logical text alignment; en/ru/es/fr output byte-for-byte unchanged; indexable/noindex split preserved (RTL-01, D-12).
</success_criteria>

<output>
Create `.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-04-SUMMARY.md` when done.
</output>
