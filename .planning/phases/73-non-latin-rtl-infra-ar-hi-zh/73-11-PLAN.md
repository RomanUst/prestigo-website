---
phase: 73-non-latin-rtl-infra-ar-hi-zh
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/en.json
  - messages/ar.json
  - messages/hi.json
  - messages/zh.json
  - messages/ru.json
  - messages/es.json
  - messages/fr.json
  - components/SiteChrome.tsx
autonomous: true
gap_closure: true
requirements: [RTL-01, TR-02]
user_setup:
  - service: anthropic-translation-pipeline
    why: "scripts/i18n-translate.mjs calls the AI translation engine to translate the one new skip-link key into ar/hi/zh/ru/es/fr (AI-only translation discipline, TR-02). Project memory: translation runs are performed locally with the live key."
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: ".env.local (read internally by scripts/i18n-translate.mjs; do not print it)"
estimate:
  tokens: 24000
  raw_tokens: 19000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The accessibility skip-to-content link renders in the active locale on /ar/, /hi/, /zh/, /ru/, /es/, /fr/ instead of hardcoded English (RTL-01 chrome-i18n gap — closes WR-04)."
    - "The new skip-link message key exists and is translated across all 7 locale catalogs via the AI pipeline, with existing keys byte-unchanged (TR-02 — AI-only translation discipline honored)."
  artifacts:
    - messages/en.json
    - messages/ar.json
    - messages/hi.json
    - messages/zh.json
    - messages/ru.json
    - messages/es.json
    - messages/fr.json
    - components/SiteChrome.tsx
  key_links:
    - "getTranslations('Common') in SiteChrome -> t('skipToContent') -> per-locale skip-link label"
---

<objective>
Close WR-04: the accessibility skip-to-content link in `components/SiteChrome.tsx` is a hardcoded English literal that renders in English under every locale, including ar/hi/zh. Externalize it into the translation catalogs (via the AI pipeline, honoring the AI-only translation discipline) and route SiteChrome through `getTranslations`.

Purpose: The skip-link is the one piece of chrome in SiteChrome that never got the i18n treatment; assistive-tech users on the non-Latin locales are the audience most likely to notice. This plan is also the one that touches the translation catalogs, so it carries TR-02 coverage for the gap-closure set.
Output: a new `Common.skipToContent` message key in all 7 catalogs + SiteChrome wired to it.

Ordering note: the catalog key is added (en) and translated (6 locales) BEFORE SiteChrome is rewired, so if the pipeline precondition is unmet the rewire is not applied and no missing-message state ships.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-REVIEW.md
@components/SiteChrome.tsx
@scripts/i18n-translate.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a Common.skipToContent source key to messages/en.json</name>
  <files>messages/en.json</files>
  <read_first>
    - messages/en.json (top-level namespace shape — existing namespaces: Booking, Nav, Hero, Footer, ...; add a new sibling top-level namespace)
    - components/SiteChrome.tsx lines 168-173 (the current hardcoded skip-link label text — copy its EXACT current visible string as the English value; do not retype it from memory)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-REVIEW.md section WR-04
  </read_first>
  <action>
    Add a new top-level namespace `"Common"` to `messages/en.json` containing a single key `"skipToContent"` whose value is the EXACT English string currently hardcoded as the skip-link label in `components/SiteChrome.tsx` (line 171). Copy that string verbatim from the source file — do not paraphrase. Keep `messages/en.json` valid JSON (trailing-comma-free, existing keys untouched). Do NOT hand-edit any other locale catalog in this task — the non-en values are produced by the pipeline in Task 2.
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo && node -e "const m=require('./messages/en.json'); if(!m.Common||!m.Common.skipToContent) process.exit(1); console.log('OK '+m.Common.skipToContent)"</automated>
  </verify>
  <acceptance_criteria>
    - `messages/en.json` has `Common.skipToContent` set to the exact string previously hardcoded in SiteChrome.tsx.
    - `messages/en.json` remains valid JSON (`node -e "require('./messages/en.json')"` exits 0).
    - No other `messages/*.json` file was hand-edited in this task.
  </acceptance_criteria>
  <done>The English source catalog carries the new skip-link key under a shared `Common` namespace.</done>
</task>

<task type="auto">
  <name>Task 2: Translate the new key into ar/hi/zh/ru/es/fr via the AI pipeline (TR-02)</name>
  <files>messages/ar.json, messages/hi.json, messages/zh.json, messages/ru.json, messages/es.json, messages/fr.json</files>
  <precondition>The AI translation engine used by `scripts/i18n-translate.mjs` is reachable (translation API credential available in the environment). Project memory records these runs are performed locally with the live key — if the credential is unavailable to this agent, halt and surface this task as a user-run step rather than hand-writing translations.</precondition>
  <read_first>
    - scripts/i18n-translate.mjs (the re-runnable pipeline; `--locales` flag; per-key hash-manifest idempotency)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-PATTERNS.md section "Manifest-safe locale backfill" (idempotency — only new/changed keys translate)
    - i18n/glossary.json (tone guides ar/hi/zh; DNT terms)
  </read_first>
  <action>
    Run the translation pipeline for the six non-English locales so the new `Common.skipToContent` key is translated into each: `node scripts/i18n-translate.mjs --locales ar,hi,zh,ru,es,fr`. The manifest is per-key hash-idempotent, so only the single new key (absent from the manifest) is translated — every other key is skipped and left byte-identical. Then run `node scripts/i18n-translate.mjs --check --locales ar,hi,zh,ru,es,fr` to confirm 0 missing keys. Do NOT reset the manifest (a full reset would re-translate all 2197 units — not wanted here). Confirm with `git diff --stat messages/` that only the six non-en catalogs changed and each changed by only the added key; `messages/en.json` should show no further change beyond Task 1.
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo && node -e "for(const l of ['ar','hi','zh','ru','es','fr']){const m=require('./messages/'+l+'.json'); if(!m.Common||!m.Common.skipToContent){console.error('MISSING '+l);process.exit(1)}} console.log('ALL_PRESENT')" && node scripts/i18n-translate.mjs --check --locales ar,hi,zh,ru,es,fr 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `Common.skipToContent` is present and non-empty in all of messages/ar,hi,zh,ru,es,fr.json (ar/hi/zh values are in-script, not English placeholder).
    - `node scripts/i18n-translate.mjs --check --locales ar,hi,zh,ru,es,fr` reports 0 missing keys.
    - `git diff` shows the six non-en catalogs changed only by the added key; no pre-existing key value changed.
  </acceptance_criteria>
  <done>All 7 catalogs carry a real per-locale skip-link translation; existing translations byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Route the SiteChrome skip-link through the Common namespace (WR-04)</name>
  <files>components/SiteChrome.tsx</files>
  <read_first>
    - components/SiteChrome.tsx lines 1-10 (imports — `getLocale, getMessages` from `next-intl/server` already present; add `getTranslations`), lines 132-173 (async server component body; the hardcoded skip-link `<a>` at 170-172)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-REVIEW.md section WR-04 (proposed `t('skipToContent')` fix)
  </read_first>
  <action>
    In `components/SiteChrome.tsx`, extend the existing `next-intl/server` import to also import `getTranslations`. Inside the async `SiteChrome` component body (near the existing `const locale = await getLocale()` on line 135), add `const t = await getTranslations('Common')`. Replace the hardcoded skip-link label inside the `<a href="#main-content" className="skip-link btn-primary">` element (lines 170-172) with `{t('skipToContent')}`. Leave the `href`, `className`, and surrounding structure unchanged. Because `app/(internal)/layout.tsx` shares this SiteChrome and resolves the locale to `en`, admin/driver chrome correctly renders the English skip-link with no extra branching.
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo && grep -q "getTranslations" components/SiteChrome.tsx && grep -q "t('skipToContent')" components/SiteChrome.tsx && test $(grep -c "Skip to content" components/SiteChrome.tsx) -eq 0 && npx tsc --noEmit 2>&1 | grep -i "SiteChrome" ; test $? -ne 0 && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "getTranslations" components/SiteChrome.tsx` and `grep -q "t('skipToContent')" components/SiteChrome.tsx` both match.
    - `grep -c "Skip to content" components/SiteChrome.tsx` returns `0` (no hardcoded label remains in the component).
    - `components/SiteChrome.tsx` type-checks; admin/driver chrome still renders the English skip-link (locale resolves to en there).
  </acceptance_criteria>
  <done>The skip-link renders in the active locale on all public locales and stays English on internal chrome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
No new trust boundary. Adds one UI string to catalogs and routes it through the existing next-intl provider; no new input, no new endpoint. The translation pipeline call uses an existing credential already used in Phases 72/73.

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73gap-11 | Information Disclosure | scripts/i18n-translate.mjs run | low | mitigate | API key read internally from .env.local; never printed/committed (feedback_no_secrets_in_commits). Only messages/*.json committed. |
</threat_model>

<artifacts_produced>
## Artifacts this plan produces
- `messages/en.json` — new `Common.skipToContent` (English source).
- `messages/{ar,hi,zh,ru,es,fr}.json` — `Common.skipToContent` translated per locale via the pipeline.
- `components/SiteChrome.tsx` — `getTranslations('Common')` import + `t('skipToContent')` replacing the hardcoded label.
- New message key: `Common.skipToContent`.
</artifacts_produced>

<verification>
- All 7 catalogs carry `Common.skipToContent`; `--check` reports 0 missing; existing keys byte-unchanged.
- SiteChrome uses `getTranslations` + `t('skipToContent')`; no hardcoded label; type-checks.
</verification>

<success_criteria>
WR-04 closed; skip-link localized on all public locales; AI-only translation discipline honored; no secret leaked; TR-02 covered.
</success_criteria>

<output>
Create `.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-11-SUMMARY.md` when done.
</output>
