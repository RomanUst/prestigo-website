---
phase: 73-non-latin-rtl-infra-ar-hi-zh
plan: 13
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [RTL-01]
files_modified:
  - app/[locale]/routes/prague-berlin/page.tsx
  - app/[locale]/routes/prague-bratislava/page.tsx
  - app/[locale]/routes/prague-brno/page.tsx
  - app/[locale]/routes/prague-budapest/page.tsx
  - app/[locale]/routes/prague-ceske-budejovice/page.tsx
  - app/[locale]/routes/prague-cesky-krumlov/page.tsx
  - app/[locale]/routes/prague-dresden/page.tsx
  - app/[locale]/routes/prague-frantiskovy-lazne/page.tsx
  - app/[locale]/routes/prague-graz/page.tsx
  - app/[locale]/routes/prague-hradec-kralove/page.tsx
  - app/[locale]/routes/prague-karlovy-vary/page.tsx
  - app/[locale]/routes/prague-krakow/page.tsx
  - app/[locale]/routes/prague-kutna-hora/page.tsx
  - app/[locale]/routes/prague-leipzig/page.tsx
  - app/[locale]/routes/prague-liberec/page.tsx
  - app/[locale]/routes/prague-linz/page.tsx
  - app/[locale]/routes/prague-marianske-lazne/page.tsx
  - app/[locale]/routes/prague-munich/page.tsx
  - app/[locale]/routes/prague-nuremberg/page.tsx
  - app/[locale]/routes/prague-olomouc/page.tsx
  - app/[locale]/routes/prague-ostrava/page.tsx
  - app/[locale]/routes/prague-pardubice/page.tsx
  - app/[locale]/routes/prague-passau/page.tsx
  - app/[locale]/routes/prague-plzen/page.tsx
  - app/[locale]/routes/prague-regensburg/page.tsx
  - app/[locale]/routes/prague-salzburg/page.tsx
  - app/[locale]/routes/prague-vienna/page.tsx
  - app/[locale]/routes/prague-warsaw/page.tsx
  - app/[locale]/routes/prague-wroclaw/page.tsx
  - app/[locale]/routes/prague-zlin/page.tsx
  - tests/route-page-render.test.tsx
  - tests/__snapshots__/route-page-render.test.tsx.snap

estimate:
  tokens: 46000
  raw_tokens: 23000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All 30 app/[locale]/routes/prague-*/page.tsx render content.openingParagraphs and content.routeNarrative.paragraphs via interpolateBidi() (each embedded {ePrice}/{vPrice}/{sPrice} DNT price token isolated in <bdi>), not the plain interpolate() helper (RTL-01, D-11)."
    - "The faqs[].a RENDER site on all 30 route pages renders via an interpolateBidi()-derived field (aBidi), while the FAQPage JSON-LD acceptedAnswer.text keeps the plain interpolate()-derived f.a string — no ReactNode/<bdi> ever reaches JSON.stringify (RTL-01, D-11)."
    - "prague-vienna EN byte-parity render snapshot regenerated; the ONLY diff vs the prior snapshot is added <bdi> tags (stripping them reproduces the prior snapshot byte-for-byte) — en/ru/es/fr output otherwise unchanged (D-12)."
  artifacts:
    - "30 route pages app/[locale]/routes/prague-*/page.tsx with openingParagraphs/routeNarrative/faqs bidi-isolated at render, JSON-LD carve-out preserved"
    - "tests/route-page-render.test.tsx — /ar prague-vienna assertion proving openingParagraphs[0] + FAQ answer render <bdi> around the price token AND the FAQPage JSON-LD text stays plain"
    - "tests/__snapshots__/route-page-render.test.tsx.snap — regenerated EN golden snapshot"
  key_links:
    - "content.openingParagraphs / content.routeNarrative.paragraphs / faqs[].a (render) -> <bdi> isolation -> interpolateBidi()"
    - "faqs[].a (FAQPage JSON-LD acceptedAnswer.text) -> plain string -> interpolate() (carve-out; must NOT become a ReactNode)"
---

<objective>
Close GAP-1 from 73-VERIFICATION.md (RTL-01 BLOCKER): `content.openingParagraphs`,
`content.routeNarrative.paragraphs`, and `faqs[].a` on all 30
`app/[locale]/routes/prague-*/page.tsx` files interpolate embedded DNT price tokens
(`{ePrice}`/`{vPrice}`/`{sPrice}`) with the plain `interpolate()` helper and render them raw
with zero `<bdi>` protection — the exact Unicode-bidi-scrambling risk `interpolateBidi()` was
built to prevent, live today on `/ar/routes/prague-vienna` (a D-10 QA page) and 29 siblings.
The 73-08/73-09 CR-02 sweep deliberately excluded these three fields.

This plan switches those three render call sites to the already-proven `interpolateBidi()`
helper (D-11 bidi isolation) across all 30 route pages, mirroring the 73-08/73-09 pattern,
with ONE critical carve-out: the `faqs` array also feeds the FAQPage JSON-LD
`acceptedAnswer.text` field, which is serialized via `JSON.stringify` and MUST stay a plain
string — so the plain `interpolate()`-derived `a` field is kept for JSON-LD and a NEW
`aBidi` (ReactNode via `interpolateBidi()`) field is added for the render site only.

Scope is EXACTLY these three fields per the VERIFICATION gap. `inclusions`,
`chauffeurNarrative`, and `dayTrip.configurations[].body` were NOT flagged by verification and
are OUT OF SCOPE (no scope creep into unrelated route-page refactors).

Purpose: make "no reversed price token on /ar" (ROADMAP SC#2/SC#4, RTL-01, D-11) true on the
route-page surface, closing the live blocker on a D-10 representative page.
Output: 30 corrected route pages + a render-test assertion + a regenerated EN snapshot.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-VERIFICATION.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-REVIEW.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-08-SUMMARY.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-09-SUMMARY.md
@lib/content-interpolate.ts
@app/[locale]/routes/prague-vienna/page.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove the bidi-isolation + JSON-LD carve-out on the two D-10 reference pages (prague-vienna, prague-berlin)</name>
  <files>app/[locale]/routes/prague-vienna/page.tsx, app/[locale]/routes/prague-berlin/page.tsx, tests/route-page-render.test.tsx, tests/__snapshots__/route-page-render.test.tsx.snap</files>
  <read_first>
    - app/[locale]/routes/prague-vienna/page.tsx (the precompute block ~lines 53-72: openingParagraphs, routeNarrativeParagraphs, faqs; the FAQPage JSON-LD builder ~lines 79-86 where acceptedAnswer.text = f.a; the FAQ render site ~line 292; the already-correct interpolateBidi call sites at hero.intro ~line 111 and cta.headingItalic ~line 330)
    - app/[locale]/routes/prague-berlin/page.tsx (same shape; the 73-08 reference fix already applied hero.intro/cta.headingItalic/v.price/c.price/copper-highlight isolation here)
    - lib/content-interpolate.ts (both helpers: interpolate() returns string for metadata/JSON-LD; interpolateBidi() returns ReactNode wrapping each substituted token in <bdi> — do NOT use it for JSON.stringify targets)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-08-SUMMARY.md (the proven sentence-level interpolateBidi() pattern + the "metadata/JSON-LD interpolate() untouched" rule)
    - tests/route-page-render.test.tsx (existing PragueViennaPage byte-parity + PragueBerlinPage /ar bidi assertions — the file to extend)
  </read_first>
  <behavior>
    - openingParagraphs render output on /ar prague-vienna wraps the substituted ePrice value in a <bdi> element (isolated), not bare in the RTL paragraph.
    - The FAQ answer render output on /ar prague-vienna wraps its substituted price token in <bdi>.
    - The FAQPage JSON-LD payload (the application/ld+json script contents) contains the FAQ answer text as a PLAIN string with no <bdi> markup — JSON.stringify never receives a ReactNode.
    - EN (default-locale) render of prague-vienna is unchanged except for added <bdi> tags at the newly-isolated sites.
  </behavior>
  <action>
    On both reference pages, change the openingParagraphs precompute and the routeNarrative.paragraphs precompute so their `.map` callbacks call `interpolateBidi` instead of `interpolate` (import `interpolateBidi` is already present on both pages from 73-08/73-09). The render sites that consume `openingParagraphs[i]` and `routeNarrativeParagraphs[i]` need no edit — they now hold ReactNodes.

    For the faqs precompute, keep the existing `a: interpolate(f.a, prices)` string field UNCHANGED (it feeds the FAQPage JSON-LD `acceptedAnswer.text` via `f.a`, which is JSON.stringify'd), and ADD a sibling field `aBidi: interpolateBidi(f.a, prices)` derived from the same raw `f.a` template. Switch ONLY the FAQ answer JSX render reference (the `<p>` in the FAQ section) from the plain `a` field to the new `aBidi` field. Leave the JSON-LD `acceptedAnswer.text` on `f.a` untouched.

    Do NOT touch inclusions, chauffeurNarrative, dayTrip.configurations[].body, highlights, v.price, or c.price — those are either out of scope for this gap or already isolated by 73-08/73-09.

    Extend tests/route-page-render.test.tsx: add (or strengthen) a PragueViennaPage /ar assertion proving (a) the opening-paragraph render output contains a `<bdi>` wrapping the interpolated ePrice value, (b) the FAQ answer render output contains a `<bdi>` around its price token, and (c) the FAQPage JSON-LD script text is a plain string containing NO `<bdi>` substring. Mirror the price-resolution approach the existing PragueBerlinPage /ar test already uses.

    Regenerate the prague-vienna EN golden byte-parity snapshot with `npx vitest run tests/route-page-render.test.tsx -u`, then verify programmatically that the only diff from the prior snapshot is added `<bdi>`/`</bdi>` tags (stripping every `<bdi>`/`</bdi>` tag from the new snapshot reproduces the prior snapshot byte-for-byte), exactly as 73-09 did.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/route-page-render.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "content.openingParagraphs.map((p) => interpolateBidi(p, prices))" app/[locale]/routes/prague-vienna/page.tsx app/[locale]/routes/prague-berlin/page.tsx` returns a match in BOTH files.
    - `grep -n "content.routeNarrative.paragraphs.map((p) => interpolateBidi(p, prices))" app/[locale]/routes/prague-vienna/page.tsx app/[locale]/routes/prague-berlin/page.tsx` returns a match in BOTH files.
    - `grep -n "aBidi: interpolateBidi(f.a, prices)" app/[locale]/routes/prague-vienna/page.tsx app/[locale]/routes/prague-berlin/page.tsx` returns a match in BOTH files; the FAQ answer JSX render references `faq.aBidi` (grep `{faq.aBidi}` matches) and the bare `{faq.a}` JSX render is gone.
    - The FAQPage JSON-LD builder still reads the plain string field: `grep -n "text: f.a" app/[locale]/routes/prague-vienna/page.tsx app/[locale]/routes/prague-berlin/page.tsx` returns a match in BOTH files (the carve-out is intact — JSON-LD stays plain).
    - `npx tsc --noEmit` exits 0; `npx vitest run tests/route-page-render.test.tsx` exits 0 with the new /ar bidi + JSON-LD-plain assertions passing.
    - Stripping all `<bdi>`/`</bdi>` tags from the regenerated tests/__snapshots__/route-page-render.test.tsx.snap reproduces the prior committed snapshot byte-for-byte (no non-`<bdi>` drift).
  </acceptance_criteria>
  <done>On both D-10 reference pages, openingParagraphs/routeNarrative render via interpolateBidi(), the FAQ answer renders via aBidi while the FAQPage JSON-LD text stays a plain interpolate() string, the render test proves it, and the EN snapshot changed only by added <bdi> tags.</done>
  <reversibility rating="reversible">Pure render-helper swap plus an added derived field; revertible by restoring interpolate() and dropping aBidi.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Sweep the identical transformation across the remaining 28 route pages</name>
  <files>app/[locale]/routes/prague-bratislava/page.tsx, app/[locale]/routes/prague-brno/page.tsx, app/[locale]/routes/prague-budapest/page.tsx, app/[locale]/routes/prague-ceske-budejovice/page.tsx, app/[locale]/routes/prague-cesky-krumlov/page.tsx, app/[locale]/routes/prague-dresden/page.tsx, app/[locale]/routes/prague-frantiskovy-lazne/page.tsx, app/[locale]/routes/prague-graz/page.tsx, app/[locale]/routes/prague-hradec-kralove/page.tsx, app/[locale]/routes/prague-karlovy-vary/page.tsx, app/[locale]/routes/prague-krakow/page.tsx, app/[locale]/routes/prague-kutna-hora/page.tsx, app/[locale]/routes/prague-leipzig/page.tsx, app/[locale]/routes/prague-liberec/page.tsx, app/[locale]/routes/prague-linz/page.tsx, app/[locale]/routes/prague-marianske-lazne/page.tsx, app/[locale]/routes/prague-munich/page.tsx, app/[locale]/routes/prague-nuremberg/page.tsx, app/[locale]/routes/prague-olomouc/page.tsx, app/[locale]/routes/prague-ostrava/page.tsx, app/[locale]/routes/prague-pardubice/page.tsx, app/[locale]/routes/prague-passau/page.tsx, app/[locale]/routes/prague-plzen/page.tsx, app/[locale]/routes/prague-regensburg/page.tsx, app/[locale]/routes/prague-salzburg/page.tsx, app/[locale]/routes/prague-warsaw/page.tsx, app/[locale]/routes/prague-wroclaw/page.tsx, app/[locale]/routes/prague-zlin/page.tsx</files>
  <read_first>
    - app/[locale]/routes/prague-vienna/page.tsx (the now-fixed reference from Task 1 — the exact render-site shape to replicate)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-09-SUMMARY.md (the proven exact-occurrence-count-guarded single-pass scripted-replace approach across 29 files, plus the snapshot precedent — reuse this method, not 28 sequential edits)
    - lib/content-interpolate.ts (interpolate vs interpolateBidi contract — reconfirm before scripting)
  </read_first>
  <action>
    Apply the exact same four transformations proven in Task 1 to each of the 28 remaining route pages via a single exact-occurrence-count-guarded Node script (the 73-09 pattern): (1) openingParagraphs precompute `.map` callback switched from `interpolate` to `interpolateBidi`; (2) routeNarrative.paragraphs precompute `.map` callback switched to `interpolateBidi`; (3) add the `aBidi: interpolateBidi(f.a, prices)` sibling field to the faqs precompute, keeping the plain `a` field for JSON-LD; (4) switch the FAQ answer JSX render reference to `faq.aBidi`. All 30 route content files carry these three fields uniformly (grep confirmed openingParagraphs.map / routeNarrative.paragraphs.map / content.faqs.map present in 30/30 files), so each substitution must assert exactly one expected occurrence per file before writing; any file with an unexpected count is flagged and skipped, not silently mis-edited.

    `interpolateBidi` is already imported on all these pages (from 73-09). Do NOT edit generateMetadata() interpolate() calls, the JSON-LD `text: f.a` field, or any field outside the three flagged (inclusions/chauffeurNarrative/dayTrip stay on plain interpolate()).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - Across all 30 route pages, `grep -l "content.openingParagraphs.map((p) => interpolateBidi(p, prices))" app/[locale]/routes/prague-*/page.tsx | wc -l` prints 30, and the same count-30 holds for the `content.routeNarrative.paragraphs.map((p) => interpolateBidi(p, prices))` pattern and the `aBidi: interpolateBidi(f.a, prices)` pattern.
    - The plain-helper form is gone from these two precompute sites across all 30 files: `grep -l "content.openingParagraphs.map((p) => interpolate(p, prices))" app/[locale]/routes/prague-*/page.tsx | wc -l` prints 0, and the same count-0 holds for `content.routeNarrative.paragraphs.map((p) => interpolate(p, prices))`.
    - The JSON-LD carve-out is intact everywhere: `grep -l "text: f.a" app/[locale]/routes/prague-*/page.tsx | wc -l` prints 30 (every FAQPage JSON-LD acceptedAnswer.text still uses the plain string field).
    - `npx tsc --noEmit` exits 0 across the whole project; `npx vitest run` (full suite) exits 0 with no failures.
  </acceptance_criteria>
  <done>All 30 route pages isolate openingParagraphs/routeNarrative/faqs price tokens via interpolateBidi() at render while the FAQPage JSON-LD text stays plain; tsc and the full vitest suite are green.</done>
  <reversibility rating="reversible">Mechanical, count-guarded, scripted swap; fully revertible.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content JSON -> rendered HTML | translated ar/hi/zh prose with embedded DNT price tokens crosses into React render |
| faqs data -> JSON-LD `<script>` | FAQ answer text is serialized via JSON.stringify into structured data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-13-01 | Tampering | FAQPage JSON-LD acceptedAnswer.text | high | mitigate | Keep the JSON-LD source on the plain interpolate()-derived `f.a` string; NEVER pass the interpolateBidi() ReactNode into JSON.stringify. A ReactNode there would serialize to `{}`/broken structured data (SEO tampering) and could smuggle markup. Enforced by the `text: f.a` grep + JSON-LD-plain render-test assertion. |
| T-73-13-02 | Information Disclosure | interpolateBidi <bdi> render | low | accept | interpolateBidi builds `<bdi>` via React.createElement (auto-escaped), no dangerouslySetInnerHTML on the prose sites — no injection surface introduced. |
| T-73-13-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this plan; no dependency changes. Package-legitimacy gate not applicable. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean across the whole project.
- `npx vitest run` full suite green (mirrors 73-09's 1463-test green baseline; no en/ru/es/fr regression).
- 30/30 route pages: openingParagraphs + routeNarrative precomputes on interpolateBidi(); faqs render on aBidi; JSON-LD text on plain f.a.
- prague-vienna EN snapshot diff is `<bdi>`-tags-only.
</verification>

<success_criteria>
GAP-1 closed: no unprotected DNT price token remains in openingParagraphs/routeNarrative.paragraphs/faqs[].a on any of the 30 route pages; the FAQPage JSON-LD text stays a plain string; en/ru/es/fr output unchanged apart from added `<bdi>` tags.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (new/changed symbols)
- `faqs[].aBidi` — new per-route-page render field (ReactNode from `interpolateBidi(f.a, prices)`); the FAQ answer `<p>` now renders `faq.aBidi`, while `faqs[].a` (plain string) is retained solely for the FAQPage JSON-LD `acceptedAnswer.text`.
- `openingParagraphs` / `routeNarrativeParagraphs` precomputes on all 30 route pages now yield `ReactNode[]` (via `interpolateBidi`) instead of `string[]`.
- `tests/route-page-render.test.tsx` — new /ar prague-vienna assertions: opening-paragraph `<bdi>` isolation, FAQ-answer `<bdi>` isolation, FAQPage JSON-LD text-plain (no `<bdi>`).
- `tests/__snapshots__/route-page-render.test.tsx.snap` — regenerated EN golden (added `<bdi>` tags only).
</artifacts_produced>

<output>
Create `.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-13-SUMMARY.md` when done.
</output>
