---
phase: 73-non-latin-rtl-infra-ar-hi-zh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/SiteChrome.tsx
  - app/globals.css
  - components/Nav.tsx
  - i18n/glossary.json
  - scripts/lib/i18n-glossary.mjs
  - .github/workflows/i18n-translate.yml
  - tests/i18n-completeness.test.ts
  - tests/middleware-i18n.test.ts
  - tests/route-page-render.test.tsx
autonomous: true
requirements: [FONT-01, RTL-01, TR-02]
estimate:
  tokens: 105000
  raw_tokens: 62000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "`next build` compiles with all three Noto loaders declared (no nextFontError) — the Noto Sans SC subset is a valid value, not the build-breaking UI-SPEC value (FONT-01, Pitfall 2)."
    - "On `/ar/`, `<body>` carries the `--font-noto-arabic` CSS variable class; on `/hi/` `--font-noto-devanagari`; on `/zh/` `--font-noto-sc`; on `/en`,`/ru`,`/es`,`/fr` and internal (`/admin`,`/driver`) routes it carries neither (D-04, FONT-01)."
    - "`<html dir=\"rtl\">` is asserted specifically for locale `ar` (not just that `rtlLocales` contains `ar` in isolation) (RTL-01)."
    - "Positive letter-spacing (`.label` 0.28em and the `--letter-spacing-*` tokens) is reset to `normal` under `:lang(ar)` and `:lang(hi)`, leaving `zh` and Latin/Cyrillic tracking unchanged (RTL-01, tofu/joining guard)."
    - "`components/Nav.tsx` physical-direction classes are converted to logical (`text-left`→`text-start`, `-mr-1`→`-me-1`, `left-0 right-0`→`start-0 end-0`) and render byte-for-byte identically under `dir=\"ltr\"` for en/ru/es/fr (RTL-01, D-12)."
    - "The `Nav.tsx` inline-style dropdown chevron composes a `dir==='rtl'` 180° rotation with its existing open/closed rotation, so it points the reader-correct way on `/ar/` (D-03)."
    - "`i18n/glossary.json` gains `locales.ar`, `locales.hi`, `locales.zh` entries that zod-validate via `loadGlossary()`; `ar.pluralCategories` is the full 6-category CLDR set `[zero,one,two,few,many,other]` (TR-02, Pitfall 3)."
    - "`PHASE_72_LOCALES` resolves to all six production locales (ru,es,fr,ar,hi,zh) and the CI workflow `add-paths`/commit-message/PR title cover all six (A1 decision: extend now)."
    - statement: "Any `ar`/`hi`/`zh` text node resolves through its Noto face before the Latin fallback stack — a tofu/fallback box on `/ar/`,`/hi/`,`/zh/` is a regression, verified in the D-10 visual QA."
      verification: backstop
  artifacts:
    - "components/SiteChrome.tsx — notoArabic/notoDevanagari/notoSC module-scope loaders + `localeFontClassName` applied on `<body>`"
    - "CSS variables --font-noto-arabic, --font-noto-devanagari, --font-noto-sc"
    - "app/globals.css — additive `:lang(ar)`,`:lang(hi)` letter-spacing overrides"
    - "components/Nav.tsx — logical-property classes + dir-aware chevron rotation"
    - "i18n/glossary.json — locales.ar/hi/zh tone + pluralCategories"
    - "scripts/lib/i18n-glossary.mjs — PHASE_72_LOCALES (6 locales)"
    - ".github/workflows/i18n-translate.yml — 6-locale add-paths/commit/PR"
    - "tests/i18n-completeness.test.ts — ar/hi/zh leakage fixture cases"
  key_links:
    - "getLocale() (SiteChrome) → localeFontClassName → <body> className → Noto CSS var → font-family fallback chain"
    - "rtlLocales.includes(locale) (app/[locale]/layout.tsx, unchanged) → <html dir> → Tailwind logical utilities mirror"
    - "PHASE_72_LOCALES (i18n-glossary.mjs) → default locale set for scripts/i18n-translate.mjs and the CI workflow"
  prohibitions:
    - "MUST NOT flip non-directional brand marks (PRESTIGO logo/wordmark, checkmarks, WhatsApp glyph, social icons, star ratings) under RTL — only direction-encoding chevrons/arrows mirror (D-03)."
    - "MUST NOT apply any Noto font (weight or subset) to a locale that does not need it — `/en`,`/ru`,`/es`,`/fr` ship zero Noto bytes (D-04 weight budget)."
    - "MUST NOT change any spacing/pixel value during a physical→logical class swap — only the axis name changes (UI-SPEC spacing parity)."
    - "MUST use native Tailwind v4 logical utilities only — no `tailwindcss-rtl` or other RTL plugin, zero new deps (D-02)."
    - "MUST keep Latin brand tokens (PRESTIGO wordmark, prices, E-/S-/V-Class, phone, domain) in the Latin brand face inside non-Latin copy via the font-family fallback stack (D-05)."
  assumptions:
    - "[ASSUMPTION · FONT-01 · edge:unclassified] The font loader edge probe returned unclassified. Planner assumption: the only build-time failure mode for the three Noto loaders is an invalid `subsets` value (Pitfall 2, corrected here); no other loader edge (weight list, variable name collision) is expected. Verified by `next build` passing."
    - "[ASSUMPTION · A1] CONTEXT.md defers the PHASE_72_LOCALES/CI extension to Claude's Discretion. Planner decision: extend both to all six locales in this phase, closing the latent bug where future EN edits would silently skip ar/hi/zh re-translation in CI. Recorded, not silently taken."
  api_coverage: "No external API integration: re-runs the existing Phase 72 translation pipeline for ar/hi/zh; no new API surface added. (COVERAGE.md written accordingly.)"
  assumption_delta: "no-change — adding ar/hi/zh variants to the already-plural locale model established in Phases 68-72; no singular assumption re-introduced."
---

<objective>
Prove the Phase-73 architecture end-to-end on a single vertical slice: one non-Latin locale (`ar`) rendering through every layer this phase modifies — translation config (glossary + locale set), per-locale Noto font loading, and physical→logical RTL class conversion — using `components/Nav.tsx` as the one representative component wired all the way to a rendered `/ar/` page. Also lands the Wave-0 test scaffolds the whole phase verifies against.

Purpose: The two architectural dead-ends in this phase (the `Noto_Sans_SC` subset build break, and a logical-class conversion that silently changes LTR output) are both caught here, on the first commit, before the ~40-file expansion and the heavyweight translation run.
Output: Corrected font loaders + locale-conditional application, `:lang()` tracking overrides, Nav converted + chevron-mirrored, glossary ar/hi/zh + 6-locale pipeline config, and the ar/hi/zh test scaffolds.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-CONTEXT.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RESEARCH.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-PATTERNS.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-UI-SPEC.md
</context>

## Artifacts this phase produces (declared by this plan)

- **Font loader consts** (`components/SiteChrome.tsx`): `notoArabic` (`Noto_Sans_Arabic`), `notoDevanagari` (`Noto_Sans_Devanagari`), `notoSC` (`Noto_Sans_SC`); the selector const `localeFontClassName`.
- **CSS variables**: `--font-noto-arabic`, `--font-noto-devanagari`, `--font-noto-sc`.
- **CSS rules** (`app/globals.css`): additive `:lang(ar)`, `:lang(hi)` letter-spacing overrides on `.label` and the `--letter-spacing-*` selectors.
- **Glossary keys** (`i18n/glossary.json`): `locales.ar`, `locales.hi`, `locales.zh` (each `formality`/`dialect`/`toneGuide`/`pluralCategories`/`termMap`).
- **Pipeline config** (`scripts/lib/i18n-glossary.mjs`): `PHASE_72_LOCALES` extended to six locales.
- **CI config** (`.github/workflows/i18n-translate.yml`): 6-locale `add-paths`, commit-message, PR title/body.
- **Test files**: `tests/i18n-completeness.test.ts` ar/hi/zh fixture cases; `dir="rtl"`-for-`ar` assertion in `tests/middleware-i18n.test.ts`; byte-parity snapshot coverage in `tests/route-page-render.test.tsx`.
- **Dir attribute wiring**: unchanged — `app/[locale]/layout.tsx` already emits `dir` from `rtlLocales`; this plan does not touch it (verify only).

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end `/ar/` slice — Noto fonts + `:lang` tracking + Nav logical conversion + chevron mirror</name>
  <files>components/SiteChrome.tsx, app/globals.css, components/Nav.tsx</files>
  <read_first>
    - components/SiteChrome.tsx (existing Fraunces/Inter loaders at lines 18-33, `const locale = await getLocale()` at 106, `<body className>` at 126)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-PATTERNS.md (SiteChrome loader shape; Nav.tsx line-by-line conversion 122/341/406/196; globals `.label` override)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RESEARCH.md (Pitfall 2 — SC subset; Pattern 2 — conditional className; Pattern 4 — inline-style chevron)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-UI-SPEC.md (Font Loading Per Locale; Typography letter-spacing rule; Mirroring Rules)
    - app/globals.css (`.label` rule ~110-118; `--letter-spacing-nav/-wide/-logo`; `!important` tracking rules ~568-571)
    - components/Nav.tsx (lines 122, 196, 341, 406)
    - app/[locale]/layout.tsx (line 25 — `dir` already wired; do NOT modify)
  </read_first>
  <action>
    In components/SiteChrome.tsx: import `Noto_Sans_Arabic`, `Noto_Sans_Devanagari`, `Noto_Sans_SC` from `next/font/google` alongside the existing Fraunces/Inter import. Declare three module-scope loaders matching the existing `variable`/`subsets`/`weight`/`display:'swap'` shape: `notoArabic` (variable `--font-noto-arabic`, subset `arabic`), `notoDevanagari` (variable `--font-noto-devanagari`, subset `devanagari`), `notoSC` (variable `--font-noto-sc`, subset `latin`, weights 400/500/600). Use the subset value `latin` for the SC loader — this is the Pitfall-2 correction; do NOT use the CJK-named subset from the UI-SPEC Font Loading table, which throws a build-time nextFontError (the full CJK glyph set ships regardless of the subset value). After the existing `const locale = await getLocale()`, add `localeFontClassName` selecting `notoArabic.variable` for `ar`, `notoDevanagari.variable` for `hi`, `notoSC.variable` for `zh`, empty string otherwise, and append it to the existing `<body className={`${fraunces.variable} ${inter.variable} ${localeFontClassName}`}>`. Internal/EN routes resolve `getLocale()` to `en` so the class stays empty — no extra branch needed.
    In app/globals.css: add additive `:lang(ar) .label, :lang(hi) .label { letter-spacing: normal; }` and equivalent `:lang(ar)`/`:lang(hi)` overrides for every `--letter-spacing-nav`/`--letter-spacing-wide`/`--letter-spacing-logo`-bearing selector and the `!important` tracking rules the UI-SPEC cites; do not modify the base Latin rules; add no `zh` override.
    In components/Nav.tsx: swap `text-left`→`text-start` (line ~406), `-mr-1`→`-me-1` (line ~341), and the `left-0 right-0` full-bleed pair on the fixed nav bar (line ~122) to `start-0 end-0` at identical token values. For the dropdown chevron (line ~196) whose rotation is set via inline `style` (not a Tailwind class), read the resolved locale, compute `isRtl = locale === 'ar'`, and compose the rotation so total degrees = existing open/closed rotation + (isRtl ? 180 : 0); the `rtl:` variant cannot reach an inline style, so this is JS-computed per Pattern 4.
  </action>
  <acceptance_criteria>
    - `npx next build` completes without a `nextFontError` referencing "Noto Sans SC" (FONT-01 / Pitfall 2 gate).
    - `grep -n "Noto_Sans_SC" components/SiteChrome.tsx` shows the loader declared with `subsets: ['latin']` (positive assertion).
    - `grep -cE "(^| )(text-left|text-right|-?mr-1|left-0 right-0)( |\")" components/Nav.tsx` (excluding comments via `grep -v '^\s*//'`) returns 0.
    - `grep -n "font-noto-arabic\|font-noto-devanagari\|font-noto-sc" components/SiteChrome.tsx` shows all three variables applied through `localeFontClassName`.
    - `npx vitest run tests/route-page-render.test.tsx` passes — Nav renders byte-identically under `dir="ltr"` for en/ru/es/fr after conversion.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/route-page-render.test.tsx tests/middleware-i18n.test.ts</automated>
  </verify>
  <done>`next build` passes with the corrected SC subset; `/ar/` renders `dir="rtl"` with the `--font-noto-arabic` body class and a reader-correct mirrored Nav chevron; en/ru/es/fr Nav output is byte-identical; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Translation config — glossary ar/hi/zh + PHASE_72_LOCALES + CI workflow (A1 decision)</name>
  <files>i18n/glossary.json, scripts/lib/i18n-glossary.mjs, .github/workflows/i18n-translate.yml</files>
  <read_first>
    - i18n/glossary.json (existing `locales.ru/es/fr` shape; `placeNames.policy`)
    - scripts/lib/i18n-glossary.mjs (line 24 `PHASE_72_LOCALES`; `localeEntrySchema` at ~35-41)
    - .github/workflows/i18n-translate.yml (add-paths block lines ~75-88; commit-message/title lines ~63/66)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-PATTERNS.md (exact ar/hi/zh glossary entries to add; PHASE_72_LOCALES extension)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RESEARCH.md (Pitfall 3 — ar 6-category CLDR set; Open Question 1 / A1)
  </read_first>
  <action>
    In i18n/glossary.json add `locales.ar`, `locales.hi`, `locales.zh` following the existing `locales.ru/es/fr` object shape (`formality`,`dialect` where applicable,`toneGuide`,`pluralCategories`,`termMap:{}`), with values locked by D-06/D-07/D-08: `ar` = Modern Standard Arabic fusḥā formal premium register, `pluralCategories: ["zero","one","two","few","many","other"]` (the full CLDR set per Pitfall 3 — do NOT copy es/fr's 2-category list, which makes verifyPluralCategories silently no-op); `hi` = formal आप register keeping English travel loanwords, `pluralCategories: ["one","other"]`; `zh` = Simplified mainland/PRC polite 您 register, `pluralCategories: ["other"]`. Leave `placeNames.policy` unchanged (already `use-standard-exonym`, D-09).
    In scripts/lib/i18n-glossary.mjs extend `PHASE_72_LOCALES` so its filter also admits `ar`,`hi`,`zh` (all six production locales). (A1 decision: extend now.)
    In .github/workflows/i18n-translate.yml add the `messages/{ar,hi,zh}.json` and `content/{routes,pages,blog}/{ar,hi,zh}/**` globs to the `add-paths` list, and update the hardcoded `commit-message`/`title`/PR body from "RU/ES/FR" to cover all six locales — so future auto-PR runs stage ar/hi/zh output instead of silently dropping it.
  </action>
  <acceptance_criteria>
    - `node -e "const g=require('./i18n/glossary.json'); const c=g.locales.ar.pluralCategories; process.exit(c.length===6 && c.includes('few') && c.includes('many') ? 0 : 1)"` exits 0.
    - `node scripts/i18n-translate.mjs --check --locales ar,hi,zh` runs without a glossary-validation error (loadGlossary zod-passes for ar/hi/zh).
    - `grep -c "'ar'\|'hi'\|'zh'" scripts/lib/i18n-glossary.mjs` on the PHASE_72_LOCALES line shows all three added.
    - `grep -c "messages/ar.json\|content/routes/ar\|content/blog/zh" .github/workflows/i18n-translate.yml` is > 0.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const g=require('./i18n/glossary.json'); if(!(g.locales.ar&&g.locales.hi&&g.locales.zh)) throw new Error('missing locale'); if(g.locales.ar.pluralCategories.length!==6) throw new Error('ar plural cats'); console.log('ok')"</automated>
  </verify>
  <done>Glossary ar/hi/zh entries validate with the correct plural sets; PHASE_72_LOCALES and the CI workflow cover all six locales; committed.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-0 test scaffolds — ar/hi/zh leakage fixtures, dir=rtl-for-ar assertion, byte-parity coverage; confirm route EN-fallback</name>
  <files>tests/i18n-completeness.test.ts, tests/middleware-i18n.test.ts, tests/route-page-render.test.tsx</files>
  <read_first>
    - tests/i18n-completeness.test.ts (existing `checkNoEnglishLeakage` `ru` fixture cases ~41-56; `loadGlossary()` at 15)
    - tests/middleware-i18n.test.ts (existing locale/dir assertions)
    - tests/route-page-render.test.tsx (existing prague-vienna golden-HTML snapshot; priceValidUntil normalization)
    - lib/route-content.ts (EN-fallback at line 106 `fs.existsSync(localized) ? localized : fallback` — confirms /ar/routes/* renders EN, no 404, before generation; A3)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RESEARCH.md (Validation Architecture — Wave 0 Gaps)
  </read_first>
  <action>
    In tests/i18n-completeness.test.ts duplicate the existing `ru` `checkNoEnglishLeakage` fixture cases for `ar`, `hi`, `zh` (inline fixtures, no real catalog needed): a leaked-English case must be flagged, a real-translation case must not, and a DNT-only identical-across-locales case (e.g. `Prestigo`, `€185`) must not be flagged — reuse the glossary loaded at file top (now containing ar/hi/zh from Task 2).
    In tests/middleware-i18n.test.ts assert `dir="rtl"` is emitted specifically for locale `ar` (not merely that `rtlLocales` contains `ar`); if the existing coverage already asserts this for `ar`, leave it and record the confirmation in the SUMMARY.
    In tests/route-page-render.test.tsx broaden the byte-parity golden-HTML technique so the D-12 gate can assert en/ru/es/fr output is unchanged for the components/pages this phase converts (at minimum add a Nav-inclusive snapshot); keep the existing `priceValidUntil` (today+365) normalization — do not invent a new date approach.
    Add a one-line assertion (or SUMMARY note) confirming `lib/route-content.ts` falls back to `content/routes/en/<slug>.json` for a locale with no file yet (A3 resolved — `/ar/routes/*` renders EN, never 404s, pre-generation).
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/i18n-completeness.test.ts` passes with ar/hi/zh leakage cases present (`grep -c "'ar'\|'hi'\|'zh'" tests/i18n-completeness.test.ts` > 0).
    - `grep -c "dir=\"rtl\"\|dir: 'rtl'\|'rtl'" tests/middleware-i18n.test.ts` > 0 and the assertion references locale `ar`.
    - `npx vitest run tests/route-page-render.test.tsx` passes and normalizes `priceValidUntil`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/i18n-completeness.test.ts tests/middleware-i18n.test.ts tests/route-page-render.test.tsx</automated>
  </verify>
  <done>ar/hi/zh leakage fixtures pass; `dir="rtl"` asserted for `ar`; byte-parity technique covers the converted surfaces; route EN-fallback confirmed; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anthropic API → repo files | Translated ar/hi/zh strings (produced next plan) flow into rendered HTML/JSON — treated as semi-trusted output of a locked system prompt |
| next/font build fetch → self-hosted asset | Noto `.woff2` fetched from Google at build time, then self-hosted under `/_next/static` — no runtime third-party origin |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-01 | Tampering (XSS) | glossary/font config | low | mitigate | Glossary is zod-validated at load (`glossarySchema`); no `dangerouslySetInnerHTML` introduced; font vars are static custom properties |
| T-73-03 | Information Disclosure | next/font Noto loaders | low | accept | Fonts self-hosted after build-time fetch (same class as existing Fraunces/Inter); no new runtime origin, no `preconnect` to a third-party font CDN |
| T-73-SC | Tampering (supply chain) | npm installs | low | accept | No new packages installed this phase (Noto loaders are first-party Next.js infra; Tailwind logical utils are core) — package-legitimacy gate N/A, no `[ASSUMED]`/`[SUS]` packages |

No `high`-severity threats — block-on threshold not triggered.
</threat_model>

<verification>
- `npx next build` passes (Noto SC subset correction, FONT-01 build gate).
- `npx vitest run` full suite green after this plan's tasks.
- `/ar/` emits `dir="rtl"` + `--font-noto-arabic` body class + mirrored Nav chevron; en/ru/es/fr Nav byte-identical.
- `node scripts/i18n-translate.mjs --check --locales ar,hi,zh` runs (glossary validates) — note it still reports the pre-reset manifest no-op state; the real generation + reset is Plan 73-02.
</verification>

<success_criteria>
FONT-01 loaders declared and build-safe; per-locale conditional application proven; RTL logical-class + chevron mirror proven byte-parity-safe on one component; glossary + pipeline config extended to six locales; ar/hi/zh test scaffolds green.
</success_criteria>

<output>
Create `.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-01-SUMMARY.md` when done.
</output>
