---
phase: 73-non-latin-rtl-infra-ar-hi-zh
plan: 06
type: execute
wave: 3
depends_on: [73-02, 73-03, 73-04, 73-05]
files_modified:
  - i18n/QA-REPORT.md
  - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md
autonomous: true
requirements: [RTL-01, FONT-01, TR-02]
estimate:
  tokens: 60000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The full vitest suite is green and `node scripts/i18n-translate.mjs --check --locales ar,hi,zh` exits 0 at the phase gate (all requirements' automated coverage passes)."
    - "`en` root + `ru`/`es`/`fr` subpath output is byte-for-byte unchanged after the whole phase — `git diff --exit-code` on en/ru/es/fr catalogs+content passes and the golden-HTML byte-parity snapshot (priceValidUntil-normalized) is green (D-12, SC#4)."
    - "No physical-direction Tailwind class remains in the in-scope `app/[locale]/**` + shared `components/**` set except the documented decorative/centering exceptions — `grep` over the converted set returns 0 unexplained occurrences (RTL-01, SC#2)."
    - "`/ar/` renders right-to-left with no mirrored-layout breakage across the 5 representative template types (home, booking flow, one route page, one blog post, account/login) — RTL visual QA recorded (RTL-01, D-10, SC#2 + SC#4)."
    - "`/ar/`, `/hi/`, `/zh/` render Noto glyphs (no tofu/fallback boxes), and `/en`,`/ru`,`/es`,`/fr` request zero Noto font bytes while each non-Latin locale requests only its own Noto family (FONT-01, D-04, SC#3)."
    - "Mixed LTR-in-RTL holds: prices (`€…`), phone `+420 725 986 855`, flight numbers, and times stay LTR and un-reversed inside `/ar/` RTL paragraphs; the booking EntryBar and RouteMap do not break under `dir=\"rtl\"` (D-11)."
    - statement: "Translated Arabic/Hindi strings that run longer/shorter than English do not clip or overflow fixed-width UI (nav labels, buttons, EntryBar chips) on the 5 QA pages — any clipped label is treated as a bug, not an accepted state."
      verification: backstop
    - statement: "Long Chinese headings/labels on `/zh/` do not break mid-character awkwardly (no word-space breaks); flag and fix any that need `overflow-wrap`/`word-break`."
      verification: backstop
    - statement: "Taller `ar`/`hi` line-height does not break a section boundary (no overlap/clipping) even though it shifts vertical rhythm vs the EN baseline; locale-specific rhythm drift without breakage is acceptable."
      verification: backstop
  artifacts:
    - ".planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md (D-10 5-page + D-11 checklist + tofu + weight-budget results)"
    - "i18n/QA-REPORT.md (final phase-gate confirmation)"
  key_links:
    - "all Wave-2 outputs (catalogs, converted components/pages, fonts) → phase gate byte-parity + visual QA → RTL-01/FONT-01/TR-02 satisfied"
  prohibitions:
    - "MUST NOT declare the phase complete while a `/ar/` QA page shows mirrored-layout breakage, a tofu glyph, or a reversed price/phone token — those are bugs, not accepted states (D-10/D-11)."
    - "MUST NOT accept a byte-parity diff on en/ru/es/fr as an acceptable tradeoff — a logical class must render identically under `dir=\"ltr\"` (D-12)."
    - "MUST NOT ship Noto bytes to a locale that does not need them (weight budget, D-04)."
  assumptions:
    - "[ASSUMPTION · FONT-01] No stack tool emits a machine-checkable per-route font-request manifest; the no-Noto-bytes-on-LTR gate is verified by build-output/`.next/static/media` inspection where possible, else recorded as human_needed in 73-RTL-QA.md (RESEARCH Wave-0 gap)."
    - "[ASSUMPTION · RTL-01 · edge:concurrency] Visual QA is a single-observer manual pass; there is no concurrent-render guarantee to prove — the checkpoint records a point-in-time result per page."
  api_coverage: "No external API integration in this plan."
---

<objective>
Close the phase with the goal-backward gate: prove all four success criteria hold together — complete ar/hi/zh generation (already in 73-02), `/ar/` RTL with no breakage, Noto fonts per locale with no tofu and no LTR weight-budget leak, and en/ru/es/fr + EN-root byte-for-byte non-regression — via an automated gate plus the D-10/D-11 human visual QA the stack cannot automate.

Purpose: RTL layout correctness, tofu-free glyph rendering, and mixed-content bidi behavior are visual properties that unit tests cannot fully capture; this plan is the one place a human confirms them, against a fixed checklist, before `/gsd-verify-work`.
Output: A recorded RTL/tofu/weight-budget QA pass (`73-RTL-QA.md`) and a green automated phase gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-CONTEXT.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-UI-SPEC.md
@.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RESEARCH.md
</context>

## Artifacts this phase produces (declared by this plan)

- **QA record**: `.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md` (D-10 5-page RTL results, D-11 mixed-content checklist, tofu check, FONT-01 weight-budget result).
- **Updated**: `i18n/QA-REPORT.md` phase-gate confirmation line.

<tasks>

<task type="auto">
  <name>Task 1: Automated phase gate — full suite + completeness + phase-wide byte-parity + residual-class grep</name>
  <files>i18n/QA-REPORT.md</files>
  <read_first>
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RESEARCH.md (Validation Architecture — Sampling Rate / Phase gate)
    - tests/route-page-render.test.tsx (byte-parity snapshot)
    - 73-01..73-05 SUMMARYs (converted-file set + documented decorative exceptions)
  </read_first>
  <action>
    Run the full suite `npx vitest run` and `node scripts/i18n-translate.mjs --check --locales ar,hi,zh`. Assert phase-wide non-regression with `git diff --exit-code` on en + ru/es/fr catalogs and content. Run a residual-physical-class grep over the in-scope set (`app/[locale]/**` + the converted `components/**`) and confirm 0 occurrences except the exceptions each SUMMARY documented (decorative Hero corners already converted; centering transforms intentionally kept). Record the gate result (pass/fail per requirement) in `i18n/QA-REPORT.md`.
  </action>
  <acceptance_criteria>
    - `npx vitest run` exits 0 (full suite green).
    - `node scripts/i18n-translate.mjs --check --locales ar,hi,zh` exits 0.
    - `git diff --exit-code -- messages/en.json messages/ru.json messages/es.json messages/fr.json content/routes/en content/routes/ru content/routes/es content/routes/fr content/pages/en content/pages/ru content/pages/es content/pages/fr content/blog/en content/blog/ru content/blog/es content/blog/fr` exits 0.
    - `grep -rlE "text-(left|right)|border-(l|r)($|[- ])|(^| )-?(pl|pr|ml|mr)-[0-9]" app/[locale] components | grep -vE "SiteChrome|globals" | wc -l` returns 0 (all in-scope files converted; document any intentional residual in 73-RTL-QA.md).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run && node scripts/i18n-translate.mjs --check --locales ar,hi,zh && git diff --exit-code -- messages/en.json messages/ru.json messages/es.json messages/fr.json</automated>
  </verify>
  <done>Full suite + completeness + phase-wide byte-parity green; residual-class grep clean; gate result recorded; committed.</done>
</task>

<task type="auto">
  <name>Task 2: FONT-01 weight-budget check — no Noto bytes on LTR locales, one family per non-Latin locale</name>
  <files>.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md</files>
  <read_first>
    - components/SiteChrome.tsx (localeFontClassName — only one Noto variable applied per locale)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-UI-SPEC.md (Non-Regression Contract — no Noto bytes on /en,/ru,/es,/fr)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RESEARCH.md (Validation Architecture — FONT-01 build-output inspection gap)
  </read_first>
  <action>
    Verify the weight-budget rule: build the app (`npx next build`) and inspect the emitted per-route font references (page HTML `<link rel="preload" ... .woff2>` and/or `.next/static/media` usage) to confirm `/en`,`/ru`,`/es`,`/fr` carry zero Noto font bytes, `/ar/` requests only Noto Sans Arabic, `/hi/` only Noto Sans Devanagari, `/zh/` only Noto Sans SC. Because the stack emits no machine-checkable per-route font manifest, where a byte-level assertion isn't possible, confirm the source guarantee (single `localeFontClassName` branch applies exactly one Noto variable per locale, `''` for LTR/internal) and record the inspection method + result in `73-RTL-QA.md`, flagging any part as `human_needed` if build-output inspection is inconclusive.
  </action>
  <acceptance_criteria>
    - `npx next build` succeeds.
    - `73-RTL-QA.md` records: /en,/ru,/es,/fr → zero Noto bytes (or human_needed with method); /ar,/hi,/zh → exactly their own Noto family.
    - `grep -c "localeFontClassName" components/SiteChrome.tsx` > 0 and the branch applies exactly one variable per locale (source guarantee).
  </acceptance_criteria>
  <verify>
    <automated>npx next build</automated>
  </verify>
  <done>Weight-budget rule verified (build-output inspection + source guarantee), result recorded in 73-RTL-QA.md; committed.</done>
</task>

<task type="auto">
  <name>Task 3: RTL visual QA scaffold (D-10 5 pages) + mixed LTR-in-RTL (D-11) + tofu + backstops — human-check at end-of-phase</name>
  <files>.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md</files>
  <read_first>
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-CONTEXT.md (D-10 5 template types; D-11 checklist)
    - .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-UI-SPEC.md (UI Considerations backstop rows; Mixed LTR-in-RTL table)
  </read_first>
  <action>
    Author `73-RTL-QA.md` as a fixed checklist covering the D-10 five representative `/ar/` pages (home; booking flow EntryBar → wizard → vehicle cards; one route page; one blog post; account/login), each with four groups: (1) mirroring — labels/icons swap sides, directional chevrons/arrows point reader-correct, non-directional marks (logo, WhatsApp, checkmarks, stars) do NOT flip; (2) tofu — no fallback boxes on `/ar/`,`/hi/`,`/zh/` (include `/hi/` + `/zh/` spot-checks); (3) mixed LTR-in-RTL (D-11) — prices `€…`, phone `+420 725 986 855`, flight numbers, times stay LTR and un-reversed, EntryBar + RouteMap do not break under `dir="rtl"`; (4) backstops — no clipped/overflowing translated labels, no awkward mid-character CJK line breaks on `/zh/`, no section-boundary overlap from taller ar/hi line-height. Pre-fill any items already confirmable from the automated gate; leave the visual cells for the operator. Any breakage/tofu/reversed-token is a bug to fix (loop back), not an accepted state.
  </action>
  <acceptance_criteria>
    - `73-RTL-QA.md` contains the D-10 table (5 pages × 4 checklist groups) with a cell per item awaiting an explicit pass/fail.
    - Backstop rows (overflow, CJK breaking, rhythm drift) each have a dedicated row — never silently omitted.
    - The file exists and is committed (`test -f .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md`).
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-RTL-QA.md && echo OK</automated>
    <human-check>Operator loads the running site and walks the D-10 five `/ar/` pages + `/hi/`,`/zh/` spot-checks, completing every checklist cell in 73-RTL-QA.md with an explicit pass/fail: no mirrored-layout breakage, no tofu glyphs, prices/phone/flight/times stay LTR un-reversed, EntryBar + RouteMap intact under dir=rtl, and no clipping/overflow/awkward-CJK-break/section-overlap. Harvested at end-of-phase into the UAT flow.</human-check>
  </verify>
  <done>73-RTL-QA.md checklist scaffold is committed; the D-10/D-11 visual QA is queued for the end-of-phase human-check harvest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered `/ar//hi//zh/` pages → end user | Final rendered non-Latin/RTL output the user sees; QA confirms no spoofing/breakage |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-02 | Spoofing (bidi-override / reversed tokens) | rendered `/ar/` text | low | mitigate | D-11 visual checklist confirms prices/phone/flight/times stay LTR and un-reversed; bidi isolation verified end-to-end |
| T-73-01 | Tampering (XSS) | translated content rendering | low | mitigate | React-escaped rendering confirmed; no `dangerouslySetInnerHTML` in the converted surfaces (verified in Wave-2 plans) |
| T-73-SC | Tampering (supply chain) | npm installs | low | accept | No new packages across the phase |

No `high`-severity threats — block-on threshold not triggered.
</threat_model>

<verification>
- `npx vitest run` full suite green; `--check --locales ar,hi,zh` exits 0.
- `git diff --exit-code` on en/ru/es/fr passes (D-12).
- `npx next build` passes; weight-budget recorded.
- `73-RTL-QA.md` records the D-10/D-11 human QA pass.
</verification>

<success_criteria>
All four phase success criteria proven together: complete ar/hi/zh generation (73-02), `/ar/` RTL with no breakage (D-10), Noto fonts per locale with no tofu and no LTR leak (FONT-01/D-04), and en/ru/es/fr + EN-root byte-for-byte non-regression (D-12) — RTL-01, FONT-01, TR-02 satisfied.
</success_criteria>

<output>
Create `.planning/phases/73-non-latin-rtl-infra-ar-hi-zh/73-06-SUMMARY.md` when done.
</output>
