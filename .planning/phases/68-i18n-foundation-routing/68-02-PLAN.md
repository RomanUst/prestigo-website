---
phase: 68-i18n-foundation-routing
plan: 02
type: execute
wave: 2
depends_on: [68-01]
files_modified:
  - tests/middleware-i18n.test.ts
  - tests/i18n-routing.test.ts
  - app/(internal)/not-found.tsx
autonomous: false
requirements: [I18N-01, I18N-02, I18N-03, I18N-04]

estimate:
  tokens: 45000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "GET /xx/ (an unconfigured 2-letter prefix) resolves to a 404 via hasLocale rejection — never a reflected redirect."
    - "GET /ru/this-does-not-exist renders the per-locale not-found (app/[locale]/not-found.tsx) as a 404, for every configured locale."
    - "All 7 configured locales (en, ru, es, fr, ar, hi, zh) render their subpath (/, /ru/, /es/, /fr/, /ar/, /hi/, /zh/) in English with no missing-message throw."
    - "/ar/ emits <html lang=\"ar\" dir=\"rtl\"> and no other locale emits dir=\"rtl\"."
    - "A non-existent path under the (internal) tree (e.g. /admin/xyz-nonexistent) returns a 404 without falling back to a broken/blank page."
    - "GET /ru/admin does not render admin content and does not silently pass into the admin auth flow — it resolves to a 404."
  artifacts:
    - app/(internal)/not-found.tsx
  key_links:
    - "app/[locale]/not-found.tsx is reached for any unmatched path under a valid locale segment (App-Router segment 404)."
    - "app/(internal)/not-found.tsx (if the built-in fallback is inadequate) provides the 404 for the admin/driver root-layout branch."
    - "stripLocalePrefix strips ONLY exact configured lowercase locale codes — /RU/, /EN/ and lookalike segments are not treated as locales."
  prohibitions:
    - "MUST NOT accept an unconfigured or case-variant locale prefix as a valid locale (no /XX/, /RU/, /Ru/ locale resolution). [flagged-unverified]"
    - "MUST NOT reflect an attacker-supplied locale/path segment into a redirect Location. [flagged-unverified]"
    - "MUST NOT let /ru/admin, /ru/api, /ru/driver reach the guarded internal surfaces — they 404 at the [locale] segment. [flagged-unverified]"
  edges:
    - statement: "Adjacency: no existing top-level public route segment collides with a configured locale code (all current public segments are English words / prague-* slugs, none is a 2-letter code in {en,ru,es,fr,ar,hi,zh}); stripLocalePrefix therefore never mis-strips a real route segment."
      verification: backstop
    - statement: "Concurrency: two concurrent requests to /admin receive distinct per-request nonces (nonce is generated inside the handler per invocation, not module-scoped)."
      verification: backstop
    - statement: "Trailing-slash / case variance on the locale segment (/ru vs /ru/, /RU/) resolves deterministically to either the ru locale (exact lowercase match) or a fixed-target 307 redirect to the canonical lowercase locale — never a 500 or an attacker-reflected open redirect. ACCEPTED DEVIATION (Task 3, human-confirmed): case-variant prefixes (/RU/, /Ru/) canonicalize via next-intl's own 307 redirect rather than a hard 404; the original wording anticipated a 404 for this case, but the redirect-to-fixed-canonical behavior satisfies the same 'never a 500 or an open redirect' safety property and was reviewed and accepted as-is (SEO-beneficial, non-bypass preserved)."
      verification: backstop
  flagged_assumptions:
    - "I18N-02 probe (empty/ordering/concurrency) and I18N-03 probe (empty/adjacency/ordering): translated to the concrete locale-routing edges above; the concurrency/nonce-freshness and trailing-slash cases are covered by backstop markers rather than explicit unit assertions because per-request timing and Next.js trailing-slash normalization are not deterministically assertable in the vitest middleware harness — surfaced for human confirmation in the Task 3 checkpoint."
    - "Open Question #3 (RESEARCH): whether app/(internal)/ needs its own not-found.tsx is resolved empirically in Task 2 — added only if the built-in Next fallback renders broken; otherwise the file is created minimal for parity and noindex hygiene."
---

<objective>
Broaden the Plan 01 tracer from one proven locale to full 7-locale coverage and harden the security/edge surface: prove unconfigured and case-variant locale prefixes 404 (no reflected redirect), the /ru/admin non-bypass holds, every configured locale renders EN, per-locale and internal-branch not-found pages resolve, and /ar renders RTL.

Purpose: This is the expansion + hardening pass on top of the risk-first tracer. It authors the spec-less probe edges (unconfigured/case-variant/adjacency/concurrency) into concrete tests and closes the not-found completeness gap (RESEARCH Open Question #3), then gates the visually/experientially-verifiable behavior (RTL, all-7 smoke, EN-unchanged, live CSP nonce) behind a human checkpoint.
Output: enriched `tests/middleware-i18n.test.ts` + `tests/i18n-routing.test.ts` with edge/security probes and all-7-locale coverage; `app/(internal)/not-found.tsx`; a human-verify sign-off on RTL + all-locale render.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/68-i18n-foundation-routing/68-RESEARCH.md
@.planning/phases/68-i18n-foundation-routing/68-VALIDATION.md
@.planning/phases/68-i18n-foundation-routing/68-01-PLAN.md
</context>

## Artifacts this phase produces

New symbols/files created by THIS plan:
- `app/(internal)/not-found.tsx` — minimal noindex 404 for the internal (admin/driver) root-layout branch (created for parity; RESEARCH Open Question #3).
- Additional test cases appended to `tests/middleware-i18n.test.ts` and `tests/i18n-routing.test.ts` (both created in Plan 01).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Edge + security probe tests (unconfigured/case-variant locales, /ru/admin non-bypass, all-7 resolution)</name>
  <files>tests/i18n-routing.test.ts, tests/middleware-i18n.test.ts</files>
  <read_first>
    - tests/i18n-routing.test.ts (Plan 01 config-contract tests to extend)
    - tests/middleware-i18n.test.ts (Plan 01 composition tests to extend)
    - i18n/routing.ts (locales, hasLocale usage)
    - middleware.ts (stripLocalePrefix, isNonLocalizedRoute, handleI18nRouting composition)
    - .planning/phases/68-i18n-foundation-routing/68-RESEARCH.md (## Security Domain — the /ru/admin probe; ## Common Pitfalls 1)
  </read_first>
  <behavior>
    tests/i18n-routing.test.ts additions:
    - hasLocale(routing.locales, 'xx') === false and hasLocale for each of the 7 codes === true (all-7 coverage).
    - stripLocalePrefix('/RU/account', routing.locales) does NOT strip (uppercase is not a configured code) → returns '/RU/account'; stripLocalePrefix('/de/x', routing.locales) does NOT strip; stripLocalePrefix('/ru', routing.locales) === '/' and stripLocalePrefix('/ru/book', routing.locales) === '/book'.
    - Adjacency guard: stripLocalePrefix leaves a real route-lookalike first segment (e.g. '/routes', '/services') untouched.
    tests/middleware-i18n.test.ts additions (mock handleI18nRouting/updateSession as in Plan 01):
    - Unconfigured prefix: a request to /xx/ enters the public branch, handleI18nRouting is invoked (the mock represents next-intl's 404-bound rewrite for an unmatched locale), and middleware returns a non-3xx, non-admin-redirect response with static CSP (no nonce, no reflected redirect Location echoing 'xx').
    - Case variance: /RU/ is NOT treated as the ru locale (stripLocalePrefix does not strip) — no ru-locale-specific handling fires.
    - /ru/admin (repeat of the Plan 01 security assertion, kept here as the authoritative edge case): no admin auth redirect, no nonce CSP.
    - All-7 dynamic parity: /es/book, /fr/book, /ar/book, /hi/book, /zh/book each route through handleI18nRouting then the dynamic branch (updateSession invoked) with static CSP — parametrize over routing.locales minus 'en'.
  </behavior>
  <action>Append the edge/security/all-7 cases to the two Plan 01 test files per the behavior block. Parametrize the per-locale cases over `routing.locales` so the 7-locale set stays single-source (I18N-04) rather than hard-coded. Keep assertions objective (return values, status codes, header presence/absence, redirect Location). Where a case is a backstop edge (concurrency/nonce-freshness, trailing-slash normalization), do not fake an assertion — leave it for the Task 3 checkpoint and reference the must_haves backstop marker in a test comment.</action>
  <verify>
    <automated>npx vitest run tests/i18n-routing.test.ts tests/middleware-i18n.test.ts</automated>
  </verify>
  <done>Edge + security + all-7 cases pass; the 7-locale coverage is parametrized off routing.locales; /xx/ and /RU/ provably do not resolve as configured locales; /ru/admin non-bypass asserted.</done>
  <reversibility rating="reversible">Test-only; git-revertible.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Not-found completeness across the three route trees</name>
  <files>app/(internal)/not-found.tsx</files>
  <read_first>
    - app/[locale]/not-found.tsx (per-locale 404 created in Plan 01 — the content pattern)
    - app/not-found.tsx (minimal top-level fallback created in Plan 01)
    - .planning/phases/68-i18n-foundation-routing/68-RESEARCH.md (## Open Questions #3; ## State of the Art — per-segment not-found)
  </read_first>
  <action>Verify the built-in 404 behavior for the (internal) branch by hitting a non-existent path under /admin (e.g. /admin/xyz-nonexistent) in a dev server. If the built-in Next.js fallback renders correctly, still create a minimal `app/(internal)/not-found.tsx` for parity and noindex hygiene (small noindex page with a link back to /admin — no Nav/Footer, since the internal surface has its own chrome); if the built-in fallback is broken/blank (RESEARCH Open Question #3), this file is required. Keep the per-locale `app/[locale]/not-found.tsx` and top-level `app/not-found.tsx` from Plan 01 intact. Do NOT localize copy (English is correct for all locales this phase — translation is Phase 72/73).</action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
    <human-check>Dev server: /ru/this-does-not-exist → per-locale 404; /admin/xyz-nonexistent → internal 404 (not blank); / (unmatched top-level like /zzz) → top-level 404.</human-check>
  </verify>
  <done>Build green with app/(internal)/not-found.tsx present; all three not-found surfaces (per-locale, internal, top-level fallback) render a 404 rather than a blank/broken page.</done>
  <reversibility rating="reversible">Additive noindex page; git-revertible.</reversibility>
</task>

<task type="checkpoint:human-verify">
  <name>Checkpoint: RTL + all-7-locale render + EN-unchanged + live CSP nonce smoke</name>
  <read_first>
    - .planning/phases/68-i18n-foundation-routing/68-VALIDATION.md (## Manual-Only Verifications — RTL browser check)
    - .planning/phases/68-i18n-foundation-routing/68-RESEARCH.md (## Assumptions Log A3 — smoke one page per locale)
  </read_first>
  <reason>These behaviors are visual/experiential and not deterministically assertable in the vitest middleware harness: RTL layout correctness, per-locale render fidelity, byte-identical EN URLs, and a live per-request CSP nonce in a real browser. They gate the phase before /gsd-verify-work.</reason>
  <verify>
    <human-check>
      In a real browser against `npm run dev` (or a preview build):
      1. Load /ar/ — confirm `<html lang="ar" dir="rtl">` and no mirrored-layout breakage (nav, hero, footer read correctly).
      2. Load /, /ru/, /es/, /fr/, /hi/, /zh/ — each renders the English page with no missing-message error and `<html lang>` equal to the locale (dir="ltr" for all except ar).
      3. Spot-check 2-3 existing EN URLs (e.g. /book, /routes/prague-vienna, /services/airport-transfer) — visually byte-identical to pre-phase, no /en prefix.
      4. Open DevTools on /admin — confirm the Content-Security-Policy response header carries a `nonce-...` value and reload shows a different nonce (per-request freshness — the concurrency backstop edge).
      5. Confirm /xx/ and /admin/xyz-nonexistent return a 404 page, not a redirect or blank.
    </human-check>
  </verify>
  <done>Human confirmed: /ar RTL correct; all 7 locales render EN with correct lang; EN URLs unchanged; live CSP nonce present and per-request-fresh; unconfigured/internal 404s correct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → [locale] segment | Attacker-chosen prefix (unconfigured, case-variant, or a guarded surface like /ru/admin) crosses into locale resolution |
| middleware → redirect Location | Any locale/path segment that could be reflected into a redirect target |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-68-06 | Tampering / Information Disclosure | [locale] segment injection | high | mitigate | **ACCEPTED DEVIATION (Task 3 checkpoint, human-confirmed):** /xx/ (unconfigured) 404s at the [locale] layer with no reflected redirect, as originally specified. Case-variant prefixes (/RU/, /Ru/, /aR/) do NOT 404 — next-intl's own middleware case-normalizes the locale segment and 307-redirects to the fixed, known-good canonical lowercase locale (/RU → /ru, /aR → /ar); this is next-intl-native behavior, not app logic, and was reviewed and accepted as safe: the redirect target is always one of the 7 configured locales (never attacker-controlled/reflected), only the locale segment is normalized (arbitrary path segments beyond it are untouched), and non-bypass still holds post-normalization (/RU/admin → /ru/admin → 404, verified live). Verified: Task 1 edge tests (updated for the accepted behavior) + Task 3 checkpoint (live: /RU, /Ru, /aR, /%52%55 → /ru or /ar; /RU/admin → /ru/admin → 404). |
| T-68-07 | Elevation of Privilege | /ru/admin, /ru/api, /ru/driver reaching guarded surfaces | high | mitigate | These are not isNonLocalizedRoute matches and not valid [locale]-child routes → 404; updateSession admin checks key on raw pathname so no admin flow fires. Verified: Task 1 /ru/admin case + Task 3. |
| T-68-08 | Spoofing | Open redirect via reflected locale/path | medium | mitigate | Unconfigured-prefix responses are static-CSP non-redirects; safeReturnTo() still guards return-to targets. Verified: Task 1 (no reflected Location) + Task 3. |
</threat_model>

<verification>
- `npx vitest run` full suite green including the enriched edge/security/all-7 cases.
- `npm run build` green with app/(internal)/not-found.tsx present.
- Human checkpoint signed off (RTL, all-7 render, EN-unchanged, live nonce, 404 edges).
</verification>

<success_criteria>
- I18N-01/04: all 7 configured subpaths render EN without 404; unconfigured/case-variant prefixes 404 with no reflected redirect.
- I18N-02: /ru/admin (and /ru/api, /ru/driver) do not bypass guards; CSP nonce per-request-fresh in a live browser.
- I18N-03: /ar renders RTL; every locale emits correct <html lang>/dir; per-locale + internal not-found resolve.
</success_criteria>

<output>
Create `.planning/phases/68-i18n-foundation-routing/68-02-SUMMARY.md` when done.
</output>
