---
phase: 58-sign-in-ui-account-dashboard
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/nav-auth.test.tsx
  - tests/account-trips.test.tsx
  - tests/profile-actions.test.ts
  - tests/passenger-actions.test.ts
autonomous: true
requirements: [NAV-01, NAV-02, ACCT-01, ACCT-02, ACCT-03]
must_haves:
  truths:
    - "Four RED test files exist and run, each failing because the production symbol it targets does not exist yet"
    - "Every Phase 58 requirement (NAV-01, NAV-02, ACCT-01, ACCT-02, ACCT-03) is covered by at least one test assertion"
    - "All test mocks use the project's vi.hoisted convention (no top-level mock variable references before hoist)"
  artifacts:
    - path: "tests/nav-auth.test.tsx"
      provides: "RED tests for Nav guest/signed-in rendering + sign-out action (NAV-01, NAV-02)"
      contains: "vi.hoisted"
    - path: "tests/account-trips.test.tsx"
      provides: "RED test for /account/trips empty state (ACCT-01)"
      contains: "No trips yet"
    - path: "tests/profile-actions.test.ts"
      provides: "RED tests for updateProfile success/unauthed/ownership (ACCT-02, ACCT-03)"
      contains: "Not authenticated."
    - path: "tests/passenger-actions.test.ts"
      provides: "RED tests for addPassenger / updatePassenger / deletePassenger ownership (ACCT-02)"
      contains: "vi.hoisted"
  key_links:
    - from: "tests/profile-actions.test.ts"
      to: "@/app/account/actions"
      via: "import updateProfile (does not exist yet → RED)"
      pattern: "from '@/app/account/actions'"
    - from: "tests/nav-auth.test.tsx"
      to: "@/components/Nav"
      via: "render(<Nav />) reading mocked onAuthStateChange"
      pattern: "onAuthStateChange"
---

<objective>
Create the four Wave-0 RED test files named in 58-VALIDATION.md. These tests
encode the observable behavior for every Phase 58 requirement BEFORE any
production code exists, so each subsequent implementation plan has an automated
target to turn green. All four files fail on first run — that is the expected
RED state.

Purpose: Nyquist validation. No 3 consecutive implementation tasks may run
without an automated verify; these scaffolds are the verify targets for plans
03, 04, and 05.
Output: tests/nav-auth.test.tsx, tests/account-trips.test.tsx,
tests/profile-actions.test.ts, tests/passenger-actions.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-sign-in-ui-account-dashboard/58-VALIDATION.md
@.planning/phases/58-sign-in-ui-account-dashboard/58-PATTERNS.md
@.planning/phases/58-sign-in-ui-account-dashboard/58-UI-SPEC.md

# Test analogs — copy the vi.hoisted + module-mock structure verbatim, adapt mocks
@tests/auth-customer.test.ts
@tests/middleware-customer.test.ts
# Production targets these tests reference (read for exact import paths / copy):
@components/Nav.tsx
@app/account/page.tsx
@app/login/actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nav auth-state + trips-empty-state RED tests</name>
  <files>tests/nav-auth.test.tsx, tests/account-trips.test.tsx</files>
  <read_first>tests/auth-customer.test.ts (vi.hoisted + vi.mock structure), components/Nav.tsx (current export — default export `Nav`), 58-UI-SPEC.md Copywriting Contract (exact strings), 58-VALIDATION.md Per-Task Verification Map</read_first>
  <action>
    Create tests/nav-auth.test.tsx covering NAV-01 and NAV-02 against `@/components/Nav` (default import). Use vi.hoisted to declare mockOnAuthStateChange and mockGetUser; vi.mock('@supabase/ssr') so createBrowserClient returns an auth object exposing onAuthStateChange (and getUser); vi.mock('@/app/login/actions', () => ({ customerSignOut: vi.fn() })). Write assertions:
    (a) NAV-01 guest: when onAuthStateChange invokes its callback with ('SIGNED_OUT', null), render(<Nav />) shows a "Sign in" element whose href is "/login" (query the desktop instance);
    (b) NAV-02 signed-in: when the callback fires with a session whose user has an email, the "Sign in" text is absent and an account trigger with aria-expanded is present;
    (c) NAV-02 dropdown: after firing a signed-in session and clicking the account trigger, the three menuitem labels "My trips", "Profile", "Sign out" are visible and the My trips/Profile items link to /account/trips and /account/profile;
    (d) NAV-02 sign out: the "Sign out" control is inside a `<form>` whose action is the mocked customerSignOut (assert the form's action prop / that customerSignOut is wired).
    Use the exact copy strings from the UI-SPEC Copywriting Contract. These assertions MUST fail now because Nav has no auth state — that is the intended RED result. Do NOT modify Nav.tsx in this plan.
    Create tests/account-trips.test.tsx covering ACCT-01 against `@/app/account/trips/page` (does not exist yet → import resolution / render fails = RED). vi.mock('@/lib/supabase/server') so createClient resolves to an auth object whose getUser returns a user (no `from` query needed — Phase 58 trips makes no DB call per D-01). Render the async server component and assert the empty-state heading "No trips yet", body containing "Your booked transfers will appear here", and a link with href "/book" labelled "Book a transfer" are present.
  </action>
  <verify>
    <automated>npx vitest run tests/nav-auth.test.tsx tests/account-trips.test.tsx 2>&1 | grep -Eiq 'fail|error|cannot find module' && echo "RED-OK (expected failing)" || (echo "UNEXPECTED: tests passed before implementation" && exit 1)</automated>
  </verify>
  <done>Both files exist, use vi.hoisted, assert the exact UI-SPEC copy, and FAIL on run (RED) because Nav has no auth state and app/account/trips/page.tsx does not exist.</done>
</task>

<task type="auto">
  <name>Task 2: Profile + passenger server-action RED tests</name>
  <files>tests/profile-actions.test.ts, tests/passenger-actions.test.ts</files>
  <read_first>tests/auth-customer.test.ts (mockGetUser, mockFrom, ownership-stripping test at the SECURITY assertions; unauthenticated-guard assertion), app/login/actions.ts (saveBookingWithUserId ownership pattern, return-shape `{ error?, success? }`), 58-PATTERNS.md (mock for customer_profiles + saved_passengers tables; ownership security test pattern), 58-RESEARCH.md Security Domain</read_first>
  <action>
    Create tests/profile-actions.test.ts importing `updateProfile` from `@/app/account/actions` (module does not exist yet → RED). Use vi.hoisted for mockGetUser and a mockFrom returning an object with an `update` that returns `{ eq: mockEq }` resolving to `{ error: null }`. vi.mock('@/lib/supabase/server') so createClient resolves to `{ auth: { getUser: mockGetUser }, from: mockFrom }`. Stub revalidatePath via vi.mock('next/cache', () => ({ revalidatePath: vi.fn() })). Assertions for ACCT-02 + ACCT-03:
    (a) success: authed user + FormData with full_name/phone/account_type → returns `{ success: true }` and `update` was called with an object containing full_name and phone;
    (b) unauthenticated: mockGetUser returns `{ data: { user: null } }` → returns `{ error: 'Not authenticated.' }` and `from` (the update) is NOT called;
    (c) SECURITY (IDOR/mass-assignment): FormData sets a forged `user_id`; assert the `.eq('user_id', ...)` call uses the session user id, never the forged value, and that the object passed to `update` does NOT contain a caller-supplied user_id;
    (d) ACCT-03 corporate: account_type='corporate' with company_name/ico/vat_id in FormData → update object includes company_name, ico, vat_id; account_type='personal' is also accepted.
    Create tests/passenger-actions.test.ts importing `addPassenger`, `updatePassenger`, `deletePassenger` from `@/app/account/actions` (RED). Mirror the same mock setup; mockFrom must support `insert`, `update().eq()`, and `delete().eq()`. Assertions for ACCT-02:
    (a) addPassenger inserts a saved_passengers row with user_id derived from getUser and the submitted full_name/phone;
    (b) addPassenger unauthenticated → `{ error: 'Not authenticated.' }`, no insert;
    (c) deletePassenger scopes the delete by BOTH the row id AND the session user_id (own-row only); a forged user_id in FormData cannot widen the scope;
    (d) updatePassenger updates only fields named explicitly (no raw FormData spread).
    These fail now because @/app/account/actions does not exist — intended RED. Do NOT create app/account/actions.ts in this plan.
  </action>
  <verify>
    <automated>npx vitest run tests/profile-actions.test.ts tests/passenger-actions.test.ts 2>&1 | grep -Eiq 'fail|error|cannot find module' && echo "RED-OK (expected failing)" || (echo "UNEXPECTED: tests passed before implementation" && exit 1)</automated>
  </verify>
  <done>Both files exist, use vi.hoisted, encode success + unauthenticated + ownership/mass-assignment + corporate-field assertions, and FAIL on run (RED) because @/app/account/actions does not exist.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 01 portion)

New test files (RED scaffolds):
- `tests/nav-auth.test.tsx` — NAV-01, NAV-02
- `tests/account-trips.test.tsx` — ACCT-01
- `tests/profile-actions.test.ts` — ACCT-02, ACCT-03
- `tests/passenger-actions.test.ts` — ACCT-02

These reference (but do not create) the downstream symbols: `Nav` auth state,
`app/account/trips/page.tsx`, and the `@/app/account/actions` exports
`updateProfile`, `addPassenger`, `updatePassenger`, `deletePassenger`.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → production modules | Tests import production symbols; no untrusted runtime input. Risk surface is encoding the WRONG security expectation (a permissive test that lets an insecure implementation pass). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Tampering | profile/passenger action tests | mitigate | Tests assert ownership is derived from session getUser and that forged `user_id` in FormData cannot override it — encodes the IDOR/mass-assignment guard as a RED expectation so the implementation MUST satisfy it (per D-05, D-06, D-07). |
| T-58-02 | Repudiation | test correctness | accept | Tests run in CI/local only; no production data. A wrong assertion is caught at GREEN time when implementation is written. |
| T-58-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — RESEARCH.md Package Legitimacy Audit confirms zero new packages; all deps (@supabase/ssr, vitest, @testing-library/react) already in package.json. |
</threat_model>

<verification>
- `npx vitest run tests/nav-auth.test.tsx tests/account-trips.test.tsx tests/profile-actions.test.ts tests/passenger-actions.test.ts` runs all four files (they FAIL — RED is correct here).
- Every requirement ID (NAV-01, NAV-02, ACCT-01, ACCT-02, ACCT-03) has at least one assertion across the four files.
- All mock factories use `vi.hoisted()` (grep each file: `grep -L 'vi.hoisted' tests/nav-auth.test.tsx tests/account-trips.test.tsx tests/profile-actions.test.ts tests/passenger-actions.test.ts` returns nothing).
</verification>

<success_criteria>
Four RED test files exist, are syntactically valid (vitest collects them without a parse error), use the project's vi.hoisted convention, assert the exact UI-SPEC copy and the security behaviors, and fail only because the production symbols they target do not yet exist. No production source is modified in this plan.
</success_criteria>

<output>
Create `.planning/phases/58-sign-in-ui-account-dashboard/58-01-SUMMARY.md` when done.
</output>
