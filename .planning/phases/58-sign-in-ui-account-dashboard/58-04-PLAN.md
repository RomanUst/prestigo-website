---
phase: 58-sign-in-ui-account-dashboard
plan: 04
type: execute
wave: 1
depends_on: [58-01]
files_modified:
  - app/account/page.tsx
  - app/account/trips/page.tsx
autonomous: true
requirements: [ACCT-01]
must_haves:
  truths:
    - "The /account overview shows 'My Account', the signed-in email, and two cards (My Trips, Profile) linking to /account/trips and /account/profile (D-02)"
    - "The /account/trips page renders an intentional empty state ('No trips yet' + 'Book a transfer' CTA to /book) with NO real history query (D-01)"
    - "Both pages are auth-gated server components with export const dynamic = 'force-dynamic'"
  artifacts:
    - path: "app/account/trips/page.tsx"
      provides: "My Trips empty-state shell (ACCT-01)"
      contains: "No trips yet"
    - path: "app/account/page.tsx"
      provides: "Account overview: heading, email, two navigation cards"
      contains: "Signed in as"
  key_links:
    - from: "app/account/trips/page.tsx"
      to: "/book"
      via: "empty-state CTA Link"
      pattern: "href=\"/book\""
    - from: "app/account/page.tsx"
      to: "/account/trips and /account/profile"
      via: "overview card CTAs"
      pattern: "/account/(trips|profile)"
---

<objective>
Build the /account overview page and the /account/trips empty-state shell. Per
D-02 the account zone uses SEPARATE routes (/account overview, /account/trips,
/account/profile), not tabs on one page.

- /account evolves the current placeholder into the overview per the UI-SPEC:
  "My Account" heading, copper line, "Signed in as {email}", and two cards
  (My Trips → /account/trips, Profile → /account/profile) (D-02).
- /account/trips ships as an empty-state UI SHELL ONLY (D-01): "No trips yet" +
  a "Book a transfer" CTA to /book. NO bookings RLS, NO booking↔user_id linking,
  NO real history query — that is Phase 60's scope per migration 045's header.
- Out of scope here (D-04): cost-centre and "Book for a guest" are deferred —
  no such fields/affordances are added on these pages.

Purpose: ACCT-01. This plan has no DB-schema dependency (the trips page makes no
query in Phase 58), so it runs in parallel with the migrations plan.
Output: app/account/page.tsx (overview), app/account/trips/page.tsx (empty
state); tests/account-trips.test.tsx (from Plan 01) turns GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-sign-in-ui-account-dashboard/58-UI-SPEC.md
@.planning/phases/58-sign-in-ui-account-dashboard/58-PATTERNS.md

# Analog + the file to evolve
@app/account/page.tsx
# Boundary anchor — read the header comment: trips history is Phase 60, not here
@supabase/migrations/045_bookings_user_id.sql
# The RED test this plan must turn GREEN
@tests/account-trips.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: /account/trips empty-state shell</name>
  <files>app/account/trips/page.tsx</files>
  <read_first>app/account/page.tsx (force-dynamic + createClient().auth.getUser() shell, container + heading styles), 58-UI-SPEC.md "/account/trips — My Trips (Empty State Only)" + Copywriting Contract, 58-PATTERNS.md trips section, supabase/migrations/045_bookings_user_id.sql header (Phase 60 boundary), tests/account-trips.test.tsx</read_first>
  <behavior>
    - Renders the page heading "My Trips" + copper line.
    - Renders the empty-state panel: heading "No trips yet", body "Your booked transfers will appear here. Ready to travel?", and a primary CTA Link to /book labelled "Book a transfer".
    - Makes NO bookings query; user is read only to stay inside the gated zone (D-01).
    - tests/account-trips.test.tsx passes.
  </behavior>
  <action>
    Create app/account/trips/page.tsx as an async server component (a distinct route under /account/* per D-02). Copy the shell from app/account/page.tsx: import createClient from '@/lib/supabase/server', export const dynamic = 'force-dynamic', read the user via supabase.auth.getUser(). Per D-01, do NOT query bookings and do NOT add any bookings RLS or user_id linking — the user is guaranteed non-null by the /account/* middleware gate; void user to suppress the unused warning. Render the same container/heading treatment as the UI-SPEC overview (max-w-7xl mx-auto, pt-24 pb-16, Cormorant 28px "My Trips" heading + .copper-line). Then the empty-state panel exactly per UI-SPEC: max-w-md mx-auto mt-16 text-center, bg-anthracite-mid border border-anthracite-light rounded p-12, an inline 48x48px muted SVG icon (color var(--anthracite-light) — NOT copper), Cormorant 18px offwhite heading "No trips yet", Montserrat 14px weight-300 warmgrey body "Your booked transfers will appear here. Ready to travel?", and a Link to /book with className btn-primary and style padding 12px 32px labelled "Book a transfer". Use the exact Copywriting Contract strings.
  </action>
  <verify>
    <automated>npx vitest run tests/account-trips.test.tsx</automated>
  </verify>
  <done>tests/account-trips.test.tsx is GREEN: /account/trips is a force-dynamic auth-gated server component that renders the "No trips yet" empty state with the "Book a transfer" CTA to /book and makes no bookings query (D-01 honored).</done>
</task>

<task type="auto">
  <name>Task 2: /account overview page (heading, email, two cards)</name>
  <files>app/account/page.tsx</files>
  <read_first>app/account/page.tsx (current placeholder to evolve — keep force-dynamic + getUser), 58-UI-SPEC.md "/account — Overview Page" + Copywriting Contract, 58-PATTERNS.md account/page.tsx section</read_first>
  <action>
    Evolve app/account/page.tsx into the overview per the UI-SPEC (the route split is fixed by D-02). Keep export const dynamic = 'force-dynamic' and the server createClient().auth.getUser() read. Container: max-w-7xl mx-auto px-6 md:px-12 pt-24 pb-16 (update the existing 48px padding to pt-24=96px so it clears the fixed nav). Heading: Cormorant 28px offwhite 0.12em "My Account" + a .copper-line div (marginBottom 24px). Below the line: "Signed in as {user.email}" in Montserrat 12px weight-400 warmgrey 0.08em. Then a grid grid-cols-1 md:grid-cols-2 gap-6 mt-8 of two cards, each bg-anthracite-mid border border-anthracite-light rounded p-6 with a .label (copper-light 11px 0.28em uppercase), a Cormorant 18px offwhite card heading, a .body-text body, and a .btn-ghost CTA Link (padding 10px 24px, fontSize 10px, marginTop 20px). Card 1: label "My Trips", heading "Your journey history", body "View all bookings associated with your account.", CTA "View trips" → /account/trips. Card 2: label "Profile", heading "Account details", body "Update your contact information and manage saved passengers.", CTA "Edit profile" → /account/profile. The sign-out affordance now lives in the Nav dropdown (Plan 03); remove the inline sign-out form from this page to avoid duplication (Nav owns sign-out), and drop the now-unused customerSignOut import. Use exact Copywriting Contract strings.
  </action>
  <verify>
    <automated>grep -q 'Signed in as' app/account/page.tsx && grep -q '/account/trips' app/account/page.tsx && grep -q '/account/profile' app/account/page.tsx && grep -q 'My Account' app/account/page.tsx && echo OK</automated>
  </verify>
  <done>/account renders "My Account", the copper line, "Signed in as {email}", and the two cards (My Trips → /account/trips, Profile → /account/profile) per the UI-SPEC (D-02), with the inline sign-out form removed (Nav owns sign-out).</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 04 portion)

New routes:
- `app/account/trips/page.tsx` — My Trips empty-state shell (default export AccountTripsPage), force-dynamic, no DB query

Modified:
- `app/account/page.tsx` — overview page: "My Account" heading, "Signed in as {email}", two navigation cards; inline sign-out form removed (Nav owns sign-out)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated request → /account, /account/trips | Both pages live under the /account/* middleware gate from Phase 57; an unauthenticated request is redirected to /login before the component runs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-10 | Elevation of Privilege | /account, /account/trips access | mitigate | Both pages declare export const dynamic = 'force-dynamic' and read getUser() server-side; the /account/* middleware gate (Phase 57) redirects unauthenticated requests to /login. No data is rendered before that gate. |
| T-58-11 | Information Disclosure | trips history scope | mitigate | Per D-01 the trips page makes NO bookings query — there is no history data path to leak this phase; booking↔user_id linking + bookings RLS are deferred to Phase 60 (migration 045 header). Cost-centre / book-for-a-guest are likewise absent (D-04). |
| T-58-12 | Information Disclosure | XSS via email display | mitigate | "Signed in as {email}" is rendered through React JSX (auto-escaped); no dangerouslySetInnerHTML. |
| T-58-SC | Tampering | npm/pip/cargo installs | accept | No package installs — RESEARCH Package Legitimacy Audit confirms zero new packages. |
</threat_model>

<verification>
- `npx vitest run tests/account-trips.test.tsx` GREEN (ACCT-01).
- `grep -q 'Signed in as' app/account/page.tsx` and the two card links to /account/trips and /account/profile present (D-02).
- /account/trips makes no bookings query: `! grep -Eq "from\\('bookings'\\)|\\.from\\(\"bookings\"\\)" app/account/trips/page.tsx` (D-01).
</verification>

<success_criteria>
The overview and trips pages render exactly per the UI-SPEC (copy, layout, card CTAs) as separate routes (D-02), both are force-dynamic auth-gated server components, the trips page is an empty-state shell with no history query (D-01), no deferred fields appear (D-04), and tests/account-trips.test.tsx passes.
</success_criteria>

<output>
Create `.planning/phases/58-sign-in-ui-account-dashboard/58-04-SUMMARY.md` when done.
</output>
