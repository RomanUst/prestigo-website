---
phase: 22-mobile-admin-promo-code-system
plan: 04
type: execute
wave: 3
depends_on: ["22-02", "22-03"]
files_modified: []
autonomous: false
requirements: [UX-01, PROMO-03]

must_haves:
  truths:
    - "Admin panel is usable on 375px screen"
    - "Promo code system works end-to-end in the booking wizard"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of the complete Phase 22 deliverables: mobile admin responsiveness and promo code system end-to-end.

Purpose: Confirm all UX-01 and PROMO requirements work as intended before marking the phase complete.
Output: Human-verified sign-off on mobile admin and promo code flows.
</objective>

<execution_context>
@/Users/romanustyugov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/romanustyugov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-mobile-admin-promo-code-system/22-01-SUMMARY.md
@.planning/phases/22-mobile-admin-promo-code-system/22-02-SUMMARY.md
@.planning/phases/22-mobile-admin-promo-code-system/22-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite</name>
  <files>prestigo/tests</files>
  <read_first>prestigo/vitest.config.ts</read_first>
  <action>
    Run the full Vitest test suite to confirm no regressions:
    ```
    cd prestigo && npx vitest run --reporter=verbose
    ```
    All tests must pass. If any fail, diagnose and fix before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo/prestigo && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run exits 0
    - Output includes PASS for admin-promo-codes.test.ts
    - Output includes PASS for validate-promo.test.ts
    - Output includes PASS for create-payment-intent.test.ts
  </acceptance_criteria>
  <done>Full test suite passes with zero failures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification</name>
  <files>n/a</files>
  <action>
    Present the verification checklist to the user and wait for approval. The following flows must be tested manually:

    **Mobile Admin (UX-01):**
    1. Open admin panel in Chrome DevTools at 375px
    2. Verify hamburger icon, sidebar overlay, 44px touch targets
    3. Verify bookings card layout, Promos nav item
    4. Resize to 768px+ — verify desktop layout returns

    **Promo Admin (PROMO-01, PROMO-02):**
    5. Create, deactivate, reactivate, and delete a promo code at /admin/promo-codes

    **Booking Wizard Promo (PROMO-03):**
    6. Apply valid code — see discount inline
    7. Apply invalid code — see error message
    8. Remove code — total reverts
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify — requires manual approval"</automated>
  </verify>
  <acceptance_criteria>
    - User types "approved" after testing all flows
  </acceptance_criteria>
  <done>Human confirms mobile admin works at 375px, promo CRUD works, and promo code applies correctly in the booking wizard.</done>
</task>

</tasks>

<verification>
Full test suite green + human visual/functional verification of all Phase 22 features.
</verification>

<success_criteria>
- All automated tests pass
- Mobile admin works at 375px with hamburger, touch targets, and card layout
- Promo admin CRUD works (create, deactivate, reactivate, delete)
- Promo in booking wizard shows discount inline and handles errors
- Human approves all flows
</success_criteria>

<output>
After completion, create `.planning/phases/22-mobile-admin-promo-code-system/22-04-SUMMARY.md`
</output>
