---
phase: 57-customer-auth-foundation
plan: 03
type: execute
wave: 3
depends_on: [57-01, 57-02]
files_modified:
  - types/database.types.ts
autonomous: false
requirements: [AUTH-06, ACCT-04]
user_setup:
  - service: supabase
    why: "Enable Google + Apple OAuth providers for customer sign-in (AUTH-02, AUTH-03)"
    dashboard_config:
      - task: "Enable Google provider; set authorized redirect to {SITE_URL}/auth/callback and the Supabase callback URL"
        location: "Supabase Dashboard → Authentication → Providers → Google"
      - task: "Enable Apple provider; configure Services ID + .p8 key; record the 6-month secret rotation date"
        location: "Supabase Dashboard → Authentication → Providers → Apple"
      - task: "Add {SITE_URL}/auth/callback to Authentication → URL Configuration → Redirect URLs"
        location: "Supabase Dashboard → Authentication → URL Configuration"
must_haves:
  truths:
    - "Migrations 044 and 045 are applied to the LIVE Supabase database (customer_profiles table + bookings.user_id column exist in the live schema)"
    - "bookings RLS status was checked before applying 045 and anonymous booking inserts/reads are confirmed un-regressed"
    - "Generated TypeScript types reflect the live schema (customer_profiles + bookings.user_id present)"
    - "Full test suite is green including the ACCT-04 anonymous-booking regression test"
  artifacts:
    - path: "types/database.types.ts"
      provides: "Regenerated Supabase types including customer_profiles + bookings.user_id"
      contains: "customer_profiles"
  key_links:
    - from: "live Supabase schema"
      to: "types/database.types.ts"
      via: "generate_typescript_types after apply_migration"
      pattern: "customer_profiles"
---

<objective>
Apply migrations 044 and 045 to the LIVE Supabase database, regenerate TypeScript types from the live schema, and run the full verification gate (RLS isolation check + anonymous-booking regression). This is the [BLOCKING] schema-push step — without it the phase passes build/type checks against stale generated types and produces a false-positive verification state.

Purpose: Move the phase from "code written" to "schema live + verified end-to-end".
Output: Applied migrations, regenerated types, green verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/57-customer-auth-foundation/57-RESEARCH.md
@.planning/phases/57-customer-auth-foundation/57-VALIDATION.md
@supabase/migrations/044_customer_profiles.sql
@supabase/migrations/045_bookings_user_id.sql

<interfaces>
<!-- Apply method: Supabase MCP (this project applies migrations via the MCP apply_migration tool). -->
Tools (Supabase MCP server):
- list_tables → confirm bookings current RLS (rowsecurity) status BEFORE applying 045 (RESEARCH Open Question 3 / Assumption A5)
- apply_migration(name, query) → apply 044 then 045
- execute_sql → verify RLS policies + run targeted checks
- generate_typescript_types → regenerate types after both migrations land

This project's generated types file: types/database.types.ts (regenerate target). If the repo uses a different generated-types path, detect it via `grep -rl "export type Database" types/ lib/` and write there.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Check bookings RLS, apply migrations 044 + 045, regenerate types</name>
  <read_first>
    - supabase/migrations/044_customer_profiles.sql (the SQL to apply)
    - supabase/migrations/045_bookings_user_id.sql (the SQL to apply)
    - .planning/phases/57-customer-auth-foundation/57-RESEARCH.md (Open Question 3 — check bookings rowsecurity before 045; Assumption A5)
    - lib/supabase.ts (buildBookingRow — anonymous insert path that must stay valid)
  </read_first>
  <files>types/database.types.ts</files>
  <action>
This task is MANDATORY and BLOCKING — the phase cannot pass verification without it. Build and tsc pass WITHOUT this push because types come from the committed generated file, not the live DB, which would be a false positive.

1. **Check bookings RLS first** (RESEARCH Open Question 3 / A5): via Supabase MCP `list_tables` (or `execute_sql`: `SELECT relrowsecurity FROM pg_class WHERE relname = 'bookings';`). Record the result in the SUMMARY. If RLS is OFF → migration 045 is safe as written. If RLS is ON with a deny-all policy → note that anonymous inserts still work because saveBooking() uses the service-role client (which bypasses RLS); do NOT add or change any bookings RLS policy in this phase.
2. **Apply migration 044**: `apply_migration(name: "044_customer_profiles", query: <contents of supabase/migrations/044_customer_profiles.sql>)`. Confirm success.
3. **Apply migration 045**: `apply_migration(name: "045_bookings_user_id", query: <contents of supabase/migrations/045_bookings_user_id.sql>)`. Confirm success.
4. **Verify schema landed** via `execute_sql`:
   - `SELECT column_name, is_nullable FROM information_schema.columns WHERE table_name='customer_profiles';` → confirm columns + account_type/company_name present.
   - `SELECT column_name, is_nullable FROM information_schema.columns WHERE table_name='bookings' AND column_name='user_id';` → confirm `user_id` exists and `is_nullable = 'YES'`.
   - `SELECT polname FROM pg_policy WHERE polrelid = 'public.customer_profiles'::regclass;` → confirm 3 policies (select/insert/update own).
5. **Regenerate types**: call Supabase MCP `generate_typescript_types` and write the output to the project's generated types file (default `types/database.types.ts`; detect actual path via `grep -rl "export type Database" types/ lib/`). The regenerated file MUST contain `customer_profiles` and a `user_id` field on the `bookings` row type.
6. Do NOT hand-edit the generated types beyond what the tool emits.
  </action>
  <verify>
    <automated>grep -q "customer_profiles" types/database.types.ts && grep -q "user_id" types/database.types.ts && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Supabase MCP confirms both migrations applied (apply_migration returned success for 044 and 045)
    - `execute_sql` confirms bookings.user_id exists with is_nullable = 'YES'
    - `execute_sql` confirms 3 customer_profiles policies exist
    - The committed generated types file contains the string `customer_profiles`
    - `npx tsc --noEmit` passes against the regenerated types
    - SUMMARY records the bookings RLS status that was observed before applying 045
  </acceptance_criteria>
  <done>044 + 045 applied to the live DB after an RLS pre-check; live schema verified; generated types regenerated and containing customer_profiles + bookings.user_id; tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Full verification gate — RLS isolation + anonymous-booking regression</name>
  <read_first>
    - tests/webhooks-stripe.test.ts (existing ACCT-04 anonymous-booking regression coverage)
    - .planning/phases/57-customer-auth-foundation/57-VALIDATION.md (Sampling Rate → "Phase gate: full suite green + ACCT-04 regression")
    - .planning/phases/57-customer-auth-foundation/57-RESEARCH.md (AUTH-06 manual RLS check)
  </read_first>
  <files>.planning/phases/57-customer-auth-foundation/57-VALIDATION.md</files>
  <action>
1. Run the full test suite: `npx vitest run` — MUST be fully green, including tests/webhooks-stripe.test.ts (ACCT-04 anonymous insert with user_id NULL).
2. RLS isolation check (AUTH-06, automated via Supabase MCP rather than manual where possible): using `execute_sql`, confirm the `customer_profiles_select_own` / `insert_own` / `update_own` policies exist and use `auth.uid() = user_id`. Document that cross-row reads are blocked. (A full two-user runtime RLS test requires two live sessions and is recorded as a manual verification in VALIDATION.md if not automatable in this environment.)
3. Anonymous booking regression (ACCT-04): confirm via `execute_sql` that a representative existing booking row reads back fine and that an INSERT omitting user_id succeeds (run against a test/staging row or assert via the existing test). Confirm no booking write path was forced to supply user_id.
4. Update `.planning/phases/57-customer-auth-foundation/57-VALIDATION.md`: fill the Per-Task Verification Map table rows (Task IDs → requirements → automated commands → ✅), set `nyquist_compliant: true` and `wave_0_complete: true` in its frontmatter, and check off the Validation Sign-Off boxes that now hold.
  </action>
  <verify>
    <automated>npx vitest run && echo SUITE_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 (full suite green)
    - tests/webhooks-stripe.test.ts is green (ACCT-04 anonymous path un-regressed)
    - 57-VALIDATION.md frontmatter has `nyquist_compliant: true` and `wave_0_complete: true`
    - 57-VALIDATION.md Per-Task Verification Map has no remaining `⬜ pending` rows for AUTH-01..07/ACCT-04 automatable behaviors
    - SUMMARY documents the customer_profiles RLS policy verification and the anonymous-booking regression result
  </acceptance_criteria>
  <done>Full suite green; RLS isolation + anonymous-booking regression confirmed; VALIDATION.md finalized and Nyquist-compliant.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual OAuth + email round-trip verification</name>
  <files>(manual verification — no files modified)</files>
  <action>Pause for human verification of the live auth round-trips per the steps in how-to-verify below. Automation is complete; this checkpoint confirms the OAuth + email flows (which require a real inbox and live Google/Apple providers) work end-to-end before sign-off.</action>
  <verify>Human confirms each how-to-verify step passes (manual round-trip; no automated command).</verify>
  <done>All how-to-verify steps approved by the user, or failures reported for follow-up.</done>
  <what-built>
Customer auth foundation: /login (magic-link + password + register + reset), Google/Apple OAuth buttons, /auth/callback code exchange + profile upsert, middleware gating, /account with sign-out. Migrations 044 + 045 applied to the live DB; types regenerated; full automated suite green.
  </what-built>
  <how-to-verify>
Prerequisite: Google + Apple providers enabled in the Supabase dashboard and {SITE_URL}/auth/callback added to Redirect URLs (see user_setup).
1. Magic-link: visit `/login`, enter your email, click "Send sign-in link". Confirm "Check your email" appears. Open the email, click the link → you land on `/account` ("You are signed in.") authenticated as a customer. In Supabase → Table editor → customer_profiles, confirm a row exists for your user with account_type = personal.
2. Google OAuth: from `/login` click "Continue with Google", complete Google sign-in → you return to `/account` (or your return-to). Confirm a customer_profiles row (account_type personal). Confirm browser console shows NO CSP violations on the Google redirect.
3. Apple OAuth: same as Google via "Continue with Apple" (only if Apple provider is configured; otherwise note as deferred).
4. Register corporate: `/login` → "Create one" → choose Corporate, enter a company name + email + password → confirm email → land on /account; customer_profiles row has account_type = corporate and the company_name.
5. Password reset: `/login` → "Forgot password?" → submit email → click reset email link → land on `/account/reset-password`, set a new password → redirected to /account; sign in again with the new password.
6. Admin separation: while signed in as this customer, visit `/admin` → you are redirected to `/` (home), NOT to /admin/login and NOT a 403. Confirm admin login at `/admin/login` still works for an admin account.
7. Sign-out: on `/account` click "Sign out" → redirected to `/`; then visit `/account` directly → redirected to `/login?return-to=%2Faccount`.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any step that failed (with the screen/console output).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live DB | Schema change applied to production Supabase; must not regress anonymous bookings |
| customer session → customer_profiles (live RLS) | Runtime RLS now enforced on the live table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-13 | Denial of Service | applying 045 regresses anonymous booking inserts | mitigate | Pre-check bookings RLS via MCP; 045 is nullable/no-default; ACCT-04 regression test must pass post-apply |
| T-57-14 | Information Disclosure | live RLS misconfigured → cross-customer profile read | mitigate | execute_sql confirms 3 own-row policies using auth.uid() = user_id before sign-off |
| T-57-15 | Spoofing | Apple secret expiry causes silent OAuth failure | accept | 6-month rotation date recorded in dashboard notes (user_setup); Apple returns invalid_client when expired |
</threat_model>

<verification>
- Migrations confirmed applied via Supabase MCP execute_sql (customer_profiles columns + policies; bookings.user_id nullable)
- `npx vitest run` full suite green incl. ACCT-04 regression
- `npx tsc --noEmit` green against regenerated types
- Manual OAuth + email round-trips approved
</verification>

<success_criteria>
- Live DB has customer_profiles (RLS-isolated) + bookings.user_id (nullable)
- Anonymous bookings still insert/read (ACCT-04 un-regressed)
- Generated types current; full suite + tsc green
- Manual auth round-trips verified
</success_criteria>

<output>
After completion, create `.planning/phases/57-customer-auth-foundation/57-03-SUMMARY.md`
</output>
