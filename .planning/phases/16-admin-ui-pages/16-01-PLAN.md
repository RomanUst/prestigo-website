---
phase: 16-admin-ui-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prestigo/package.json
  - prestigo/components/admin/AdminSidebar.tsx
  - prestigo/components/admin/StatusBadge.tsx
  - prestigo/components/admin/KPICard.tsx
autonomous: true
requirements:
  - ZONES-03
must_haves:
  truths:
    - "Five new packages are installed and importable"
    - "AdminSidebar shows active state with copper left border on current page"
    - "AdminSidebar uses next/link for prefetching"
    - "StatusBadge renders 4 variants with correct colors"
    - "KPICard renders label, value, and sub-label"
  artifacts:
    - path: "prestigo/components/admin/StatusBadge.tsx"
      provides: "Reusable status badge with 4 variants"
      exports: ["StatusBadge"]
    - path: "prestigo/components/admin/KPICard.tsx"
      provides: "Metric display card"
      exports: ["KPICard"]
    - path: "prestigo/components/admin/AdminSidebar.tsx"
      provides: "Upgraded sidebar with active state and next/link"
  key_links:
    - from: "prestigo/components/admin/AdminSidebar.tsx"
      to: "next/link, next/navigation"
      via: "usePathname() for active detection"
      pattern: "usePathname.*isActive.*startsWith"
---

<objective>
Install all required packages for Phase 16 and build shared UI primitives (StatusBadge, KPICard) used across multiple admin pages. Upgrade AdminSidebar with active nav state and next/link.

Purpose: Foundation layer — all subsequent plans depend on these packages and shared components.
Output: Package.json updated, 2 new components, 1 upgraded component.
</objective>

<execution_context>
@/Users/romanustyugov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/romanustyugov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-ui-design-contract/15-UI-SPEC.md
@.planning/phases/16-admin-ui-pages/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages and create StatusBadge + KPICard components</name>
  <files>prestigo/package.json, prestigo/components/admin/StatusBadge.tsx, prestigo/components/admin/KPICard.tsx</files>
  <read_first>
    - prestigo/package.json (current dependencies)
    - .planning/phases/15-ui-design-contract/15-UI-SPEC.md (component specs)
  </read_first>
  <action>
1. Run `cd prestigo && npm install @vis.gl/react-google-maps terra-draw terra-draw-google-maps-adapter recharts @tanstack/react-table` from the prestigo directory.

2. After install, verify package.json contains all 5 new dependencies. If `react-is` is missing and recharts warns about it, also run `npm install react-is`.

3. Create `prestigo/components/admin/StatusBadge.tsx`:
```tsx
'use client'

interface StatusBadgeProps {
  variant: 'active' | 'inactive' | 'pending' | 'quote'
  label: string
}

const variantStyles: Record<StatusBadgeProps['variant'], { bg: string; color: string; border: string }> = {
  active:   { bg: '#1a3a2a', color: '#4ade80', border: '1px solid rgba(34,197,94,0.25)' },
  inactive: { bg: '#2a1a1a', color: '#f87171', border: '1px solid rgba(239,68,68,0.25)' },
  pending:  { bg: '#3a2a1a', color: '#fb923c', border: '1px solid rgba(249,115,22,0.25)' },
  quote:    { bg: '#1a2a3a', color: '#60a5fa', border: '1px solid rgba(59,130,246,0.25)' },
}

export function StatusBadge({ variant, label }: StatusBadgeProps) {
  const s = variantStyles[variant]
  return (
    <span style={{
      display: 'inline-flex', alignItems: 'center', gap: '8px',
      padding: '4px 8px', backgroundColor: s.bg, color: s.color,
      border: s.border, borderRadius: '2px',
      fontFamily: 'var(--font-montserrat)', fontSize: '11px',
      textTransform: 'uppercase', letterSpacing: '0.08em',
    }}>
      <span style={{ width: '6px', height: '6px', borderRadius: '50%', backgroundColor: s.color }} />
      {label}
    </span>
  )
}
```

4. Create `prestigo/components/admin/KPICard.tsx`:
```tsx
'use client'

interface KPICardProps {
  label: string
  value: string
  subLabel?: string
}

export function KPICard({ label, value, subLabel }: KPICardProps) {
  return (
    <div style={{
      background: 'var(--anthracite-mid)',
      border: '1px solid var(--anthracite-light)',
      borderRadius: '4px',
      padding: '24px',
      minWidth: '200px',
      display: 'flex',
      flexDirection: 'column',
      gap: '8px',
      transition: 'border-color 300ms ease',
      cursor: 'default',
    }}
    onMouseEnter={(e) => { e.currentTarget.style.borderColor = 'var(--copper)' }}
    onMouseLeave={(e) => { e.currentTarget.style.borderColor = 'var(--anthracite-light)' }}
    >
      <span style={{
        fontFamily: 'var(--font-montserrat)', fontSize: '11px', fontWeight: 400,
        textTransform: 'uppercase', letterSpacing: '0.4em', color: 'var(--copper)',
      }}>
        {label}
      </span>
      <span style={{
        fontFamily: 'var(--font-cormorant)', fontSize: '32px', fontWeight: 300,
        color: 'var(--offwhite)', lineHeight: 1,
      }}>
        {value}
      </span>
      {subLabel && (
        <span style={{
          fontFamily: 'var(--font-montserrat)', fontSize: '11px', fontWeight: 400,
          color: 'var(--warmgrey)',
        }}>
          {subLabel}
        </span>
      )}
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo/prestigo && cat package.json | grep -c '"@vis.gl/react-google-maps"\|"terra-draw"\|"terra-draw-google-maps-adapter"\|"recharts"\|"@tanstack/react-table"' && test -f components/admin/StatusBadge.tsx && test -f components/admin/KPICard.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `@vis.gl/react-google-maps`, `terra-draw`, `terra-draw-google-maps-adapter`, `recharts`, `@tanstack/react-table`
    - StatusBadge.tsx contains `export function StatusBadge`
    - StatusBadge.tsx contains all 4 variant keys: `active`, `inactive`, `pending`, `quote`
    - StatusBadge.tsx uses `fontSize: '11px'` and `letterSpacing: '0.08em'`
    - KPICard.tsx contains `export function KPICard`
    - KPICard.tsx label uses `fontSize: '11px'`, `letterSpacing: '0.4em'`, `color: 'var(--copper)'`
    - KPICard.tsx value uses `fontSize: '32px'`, `fontWeight: 300`, `fontFamily: 'var(--font-cormorant)'`
    - KPICard.tsx hover border transitions to `var(--copper)` with `300ms ease`
  </acceptance_criteria>
  <done>5 packages in package.json, StatusBadge renders 4 variants, KPICard renders label/value/subLabel with correct typography</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade AdminSidebar with active nav state and next/link</name>
  <files>prestigo/components/admin/AdminSidebar.tsx</files>
  <read_first>
    - prestigo/components/admin/AdminSidebar.tsx (current implementation)
    - .planning/phases/15-ui-design-contract/15-UI-SPEC.md (active state spec)
  </read_first>
  <action>
Modify `prestigo/components/admin/AdminSidebar.tsx` to:

1. Add imports at top (file already has `'use client'`):
```tsx
import Link from 'next/link'
import { usePathname } from 'next/navigation'
```

2. Inside the `AdminSidebar` component function, add:
```tsx
const pathname = usePathname()
```

3. Replace the `<a>` tag in the nav items map with `<Link>`. The active detection uses `pathname.startsWith(item.href)`. Active items get:
   - `borderLeft: '3px solid var(--copper)'`
   - `paddingLeft: '17px'` (compensate for 3px border)
   - `color: 'var(--offwhite)'`
   Inactive items get:
   - `borderLeft: '3px solid transparent'`
   - `paddingLeft: '20px'` (original padding)
   - `color: 'var(--warmgrey)'`

4. Add hover transition: `transition: 'color 150ms ease'` on all nav items.

5. Update sign-out button fontSize from `'12px'` to `'11px'` per typography consolidation in UI-SPEC.md.

6. Update "Admin" sublabel fontSize from `'10px'` to `'11px'` and marginTop from `'2px'` to `'4px'` per UI-SPEC.md consolidation.

IMPORTANT: Do NOT change any other existing measurements (padding, letter-spacing, logo font size). The `10px 20px` nav padding and `24px 20px` header padding are locked legacy values.
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo/prestigo && grep -c "import Link from 'next/link'" components/admin/AdminSidebar.tsx && grep -c "usePathname" components/admin/AdminSidebar.tsx && grep -c "3px solid var(--copper)" components/admin/AdminSidebar.tsx && grep -c "fontSize: '11px'" components/admin/AdminSidebar.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - AdminSidebar.tsx contains `import Link from 'next/link'`
    - AdminSidebar.tsx contains `import { usePathname } from 'next/navigation'`
    - AdminSidebar.tsx contains `usePathname()` call
    - AdminSidebar.tsx contains `pathname.startsWith(item.href)` or equivalent active detection
    - AdminSidebar.tsx contains `3px solid var(--copper)` for active border
    - AdminSidebar.tsx contains `paddingLeft: '17px'` for active state
    - AdminSidebar.tsx contains `<Link` replacing `<a`
    - AdminSidebar.tsx sign-out button uses `fontSize: '11px'` (not '12px')
    - AdminSidebar.tsx "Admin" sublabel uses `fontSize: '11px'` and `marginTop: '4px'`
    - AdminSidebar.tsx contains `transition: 'color 150ms ease'` on nav items
    - AdminSidebar.tsx still has `padding: '10px 20px'` (locked legacy)
    - AdminSidebar.tsx still has `padding: '24px 20px'` (locked legacy header)
  </acceptance_criteria>
  <done>AdminSidebar uses next/link, shows copper active state on current page, typography consolidated to 11px</done>
</task>

</tasks>

<verification>
1. `cd prestigo && npm ls @vis.gl/react-google-maps terra-draw terra-draw-google-maps-adapter recharts @tanstack/react-table` — all 5 packages listed
2. `grep -c "export function StatusBadge" components/admin/StatusBadge.tsx` — returns 1
3. `grep -c "export function KPICard" components/admin/KPICard.tsx` — returns 1
4. `grep "import Link" components/admin/AdminSidebar.tsx` — shows next/link import
5. Build check: `npm run build -- --no-lint` passes
</verification>

<success_criteria>
- All 5 packages installed and in package.json
- StatusBadge component renders 4 variants with exact colors from UI-SPEC
- KPICard component renders label (copper 11px), value (Cormorant 32px), sub-label (warmgrey 11px)
- AdminSidebar uses next/link with usePathname active detection (copper border on active)
- AdminSidebar typography consolidated (11px sign-out, 11px sublabel, 4px margin-top)
- Build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-admin-ui-pages/16-01-SUMMARY.md`
</output>
