---
phase: 01-foundation-trip-entry
plan: 05
type: execute
wave: 4
depends_on: [01-04]
files_modified: []
autonomous: false
requirements: [WIZD-01, WIZD-02, WIZD-03, WIZD-04, WIZD-05, WIZD-06, STEP1-01, STEP1-02, STEP1-03, STEP1-04, STEP1-05, STEP1-06, STEP1-07]

must_haves:
  truths:
    - "All Phase 1 success criteria verified by human inspection"
  artifacts: []
  key_links: []
---

<objective>
Human visual verification of the complete Phase 1 implementation.

Purpose: Confirm the wizard shell, progress bar, Step 1 trip entry, and all interactions work correctly in the browser before marking Phase 1 complete.
Output: Verification approval or issue list
</objective>

<execution_context>
@/Users/romanustyugov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/romanustyugov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-trip-entry/01-CONTEXT.md
@.planning/phases/01-foundation-trip-entry/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and verify TypeScript compilation</name>
  <files></files>
  <read_first>prestigo/package.json</read_first>
  <action>
Run the full build to verify everything compiles and renders:
```bash
cd prestigo && npx tsc --noEmit && npm run build
```

If build fails, diagnose and fix errors. Common issues:
- Missing imports (check all @/ paths resolve)
- Type mismatches between store and components
- Missing 'use client' directives on components using hooks

After build succeeds, start dev server:
```bash
cd prestigo && npm run dev
```
Leave it running for the human verification step.
  </action>
  <verify>
    <automated>cd /Users/romanustyugov/Desktop/Prestigo/prestigo && npx tsc --noEmit && echo "TSC OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0 with no errors
    - Dev server starts without crashing
  </acceptance_criteria>
  <done>Full TypeScript compilation and Next.js build pass without errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Phase 1 wizard</name>
  <files></files>
  <action>
Human verifies the complete Phase 1 implementation in browser. Dev server must be running at http://localhost:3000/book. Walk through 22 verification items covering progress bar, trip type tabs, address fields, airport auto-fill, hourly mode, passengers/luggage steppers, validation, navigation, mobile layout, and animation.
  </action>
  <verify>Human confirms all items pass or provides issue list</verify>
  <acceptance_criteria>
    - Human has visited http://localhost:3000/book
    - Human has tested all 5 trip types
    - Human has tested address autocomplete
    - Human has tested Next button disabled state
    - Human has tested Back navigation preserves data
  </acceptance_criteria>
  <done>Human approves Phase 1 visual and functional verification</done>
  <what-built>
Complete Phase 1: Booking wizard shell with 6-step progress bar and fully functional Step 1 (trip type selection, Google Places Autocomplete, airport auto-fill, passengers/luggage steppers, hourly duration selector, validated Continue button).
  </what-built>
  <how-to-verify>
Start dev server if not running: `cd prestigo && npm run dev`

Visit http://localhost:3000/book and verify these items:

**Progress Bar:**
1. See 6 numbered circles with Step 1 highlighted in copper
2. Steps 2-6 show as pending (grey circles)

**Trip Type Tabs:**
3. See 5 tabs: ONE-WAY, AIRPORT PICKUP, AIRPORT DROPOFF, HOURLY, DAILY
4. Click each tab — active tab shows copper underline/text (NOT filled background)

**Address Fields:**
5. Select ONE-WAY — see origin and destination fields with swap icon between them
6. Type 2+ characters in origin — see Google Places suggestions in dark dropdown
7. Select a suggestion — field fills with address
8. Click clear (X) — field clears

**Airport Auto-fill:**
9. Select AIRPORT PICKUP — destination shows "Vaclav Havel Airport Prague (PRG)" with plane icon, read-only
10. Select AIRPORT DROPOFF — origin shows PRG with plane icon, read-only

**Hourly Mode:**
11. Select HOURLY — destination field disappears, duration selector appears with 7 segments (1h-12h)
12. Click a duration segment — it highlights with copper

**Passengers and Luggage:**
13. See stepper controls for passengers (starts at 1) and luggage (starts at 0)
14. Click +/- — values change within bounds (passengers 1-8, luggage 0-8)
15. At min/max, +/- button is visually dimmed

**Validation:**
16. Clear origin field, click Continue — see error "Pickup location is required to continue."
17. Fill origin, click Continue — advances to Step 2 (shows stub)

**Navigation:**
18. On Step 2+ — see Back button, click it — returns to Step 1 with all data intact
19. Progress bar shows Step 1 as completed (copper check), Step 2 as active

**Mobile (resize to 375px or use DevTools):**
20. Continue/Back buttons fixed at bottom of viewport
21. All fields usable on narrow screen

**Animation:**
22. Step transitions have subtle fade+slide-up animation
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
All Phase 1 success criteria from ROADMAP.md:
1. User can open /book and see a 6-step wizard with a visible progress bar showing Step 1 active
2. User can select any of the 5 trip types and see the form fields adapt
3. User can type in origin/destination fields and see Google Places Autocomplete suggestions
4. User can click Back on any step and return to the previous step with all previously entered data intact
5. User can click Next and it remains disabled until all required fields in the current step are filled
</verification>

<success_criteria>
- Human confirms all 22 verification items pass
- No visual regressions on the /book page
- TypeScript and Next.js build clean
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-trip-entry/01-05-SUMMARY.md`
</output>
