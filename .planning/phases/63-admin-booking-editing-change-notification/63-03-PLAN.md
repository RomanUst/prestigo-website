---
phase: 63-admin-booking-editing-change-notification
plan: 03
type: execute
wave: 3
depends_on: ["63-02"]
files_modified:
  - app/api/admin/bookings/route.ts
  - tests/admin-bookings.test.ts
autonomous: true
requirements: [AEDIT-02, AEDIT-03, AEDIT-06, AEDIT-07]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A vehicle_class or route (origin/destination + distance_km) change in PATCH triggers a server-side fare recompute from pricing_config via computeOutboundLegTotal + computeExtrasTotal + getPricingConfig + dateDiffDays (D-03) — the client amount is never trusted (AEDIT-02, AEDIT-03, AEDIT-07)."
    - "When the submitted amount_czk diverges from the recomputed amount by more than ADMIN_PRICE_TOLERANCE_CZK (=2) and override_price is not set, PATCH returns 422 with computedCzk + submittedCzk (D-04, D-06)."
    - "With override_price=true, PATCH accepts the operator's amount_czk, records it as authoritative, and writes an override audit note (D-04) — the price is never changed silently (AEDIT-07)."
    - "Phase 63 only records the new amount — no payment link, no auto-charge, no top-up is created for any price difference (D-05)."
    - "A price change writes an audit row {field:'amount_czk', old_value:<old>, new_value:<new>} so it appears in both the history UI and the change email (D-07, D-10)."
    - "A trip-detail edit on a booking_source='gnet' booking persists locally and does NOT call any GNet push (no such API exists) — local-only."
    - statement: "The authoritative amount saved is an integer CZK value (amount_czk is z.number().int()); EUR rounding follows the existing server-pricing roundUpToFive path — the edit path introduces no second rounding rule (AEDIT-07 precision contract)."
      verification: explicit
    - "editing the outbound leg's vehicle/route recomputes and saves only that leg; the linked return leg row is byte-identical afterward (AEDIT-06)."
  artifacts:
    - "app/api/admin/bookings/route.ts :: bookingPatchSchema extended with vehicle_class, origin_address, destination_address, origin_lat/lng, destination_lat/lng, distance_km, amount_czk, override_price"
    - "app/api/admin/bookings/route.ts :: price-affecting recompute+override sub-branch inside the trip-edit branch"
    - "tests/admin-bookings.test.ts (recompute, 422-divergence, override, GNet-local-only, leg-isolation, idempotency cases)"
  key_links:
    - "Recompute reuses the exact POST-handler block (route.ts:404-455): getPricingConfig -> computeOutboundLegTotal -> computeExtrasTotal -> eurToCzk -> tolerance check vs ADMIN_PRICE_TOLERANCE_CZK (reuse the constant at route.ts:348, do NOT redefine)."
    - "distance_km is client-supplied (from the Plan 05 /api/calculate-price round trip) and trusted at the price level only — the tolerance check on the resulting price is the safety net (Pitfall 2), not a second server geocode."
  prohibitions:
    - "MUST NOT redefine ADMIN_PRICE_TOLERANCE_CZK — reuse the existing constant."
    - "MUST NOT create, generate, or send a Stripe payment link / top-up for a price difference (that is Phase 64) — D-05."
    - "MUST NOT push trip-detail changes to GNet — lib/gnet-client.ts only pushes status; trip edits are local-only."
    - "MUST NOT add a second, independent Google Maps / geocode call in the PATCH handler to re-verify distance (Pitfall 2)."
---

<objective>
Expand the proven trip-edit branch (Plan 02) with the price dimension: when a vehicle_class or route change is submitted, recompute the fare server-side from pricing_config, tolerance-check it, and accept an explicit operator override — never trusting the client amount and never changing the price silently (D-03, D-04, D-06, AEDIT-02, AEDIT-03, AEDIT-07). Record the amount change in the audit log so it flows to both the history UI and the change email. Confirm GNet-sourced edits stay local-only and re-assert leg isolation + idempotency at the price level.

Purpose: This is the security-critical dimension (price tampering). It is a faithful port of the already-correct POST-handler recompute+override block — the risk is silent divergence from that block, so the tests pin the behavior.
Output: price-affecting recompute/override sub-branch + comprehensive vitest coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/63-admin-booking-editing-change-notification/63-CONTEXT.md
@.planning/phases/63-admin-booking-editing-change-notification/63-RESEARCH.md
@.planning/phases/63-admin-booking-editing-change-notification/63-PATTERNS.md
@.planning/phases/63-admin-booking-editing-change-notification/63-02-SUMMARY.md
@app/api/admin/bookings/route.ts
@lib/server-pricing.ts
@lib/gnet-client.ts
</context>

<artifacts_produced>
- **Schema:** `bookingPatchSchema` extended with `vehicle_class` (enum business/first_class/business_van), `origin_address`, `destination_address`, `origin_lat`/`origin_lng`, `destination_lat`/`destination_lng`, `distance_km`, `amount_czk` (int positive), `override_price` (boolean).
- **Handler:** price-affecting recompute+override sub-branch inside the Plan 02 trip-edit branch — reuses `getPricingConfig`, `computeOutboundLegTotal`, `computeExtrasTotal`, `dateDiffDays`, `eurToCzk`/`czkToEur`, `ADMIN_PRICE_TOLERANCE_CZK`.
- **Audit:** an `amount_czk` change entry (+ override note text when override applied).
- **Tests:** recompute / 422-divergence / override-accept / GNet-local-only / leg-isolation / at-most-once-email cases in `tests/admin-bookings.test.ts`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Price recompute + override sub-branch (AEDIT-02, AEDIT-03, AEDIT-07)</name>
  <files>app/api/admin/bookings/route.ts, tests/admin-bookings.test.ts</files>
  <read_first>
    - app/api/admin/bookings/route.ts (lines 300-348 manualBookingSchema + ADMIN_PRICE_TOLERANCE_CZK; 369-463 POST recompute+override block; 465-502 override operator_notes note; the Plan 02 trip-edit branch)
    - lib/server-pricing.ts (computeOutboundLegTotal signature: vehicleClass, distanceKm, hours, days, tripType, pickupDate, pickupTime, isAirport, rates; roundUpToFive path)
    - lib/extras.ts (computeExtrasTotal), lib/pricing-config.ts (getPricingConfig), lib/pricing.ts (dateDiffDays), lib/currency.ts (eurToCzk/czkToEur)
    - .planning/phases/63-admin-booking-editing-change-notification/63-RESEARCH.md (Pattern 2, Pitfall 2, GNet Push Decision)
  </read_first>
  <behavior>
    - PATCH { id, vehicle_class:'first_class', amount_czk:<matches recompute> } on a transfer booking -> 200, saves the recomputed amount, writes an amount_czk audit entry.
    - PATCH { id, vehicle_class:'first_class', amount_czk:<off by 50> } without override_price -> 422 with body { computedCzk, submittedCzk }.
    - Same request with override_price:true -> 200, saves the submitted amount as authoritative, appends an override note to operator_notes recording standard-rate-would-be, writes the amount audit entry.
    - PATCH { id, origin_address, destination_address, distance_km, amount_czk } recomputes using the supplied distance_km (no second geocode call in the handler).
    - PATCH price change on booking_source='gnet' -> persists + audits locally, pushGnetStatus is NOT called.
    - Recompute uses the booking's own trip_type / pickup_date / pickup_time / return_date (from current row, overlaid with any submitted changes).
  </behavior>
  <action>Extend `bookingPatchSchema` with the price-affecting optional fields (see artifacts). Inside the Plan 02 trip-edit branch, detect a price-affecting change (`vehicle_class`, `origin_address`, `destination_address`, or `distance_km` present). When present: load `rates = await getPricingConfig()` (503 on failure, mirror POST:377-382); derive effective trip inputs by overlaying submitted fields on the current row (vehicle_class, distance_km, trip_type, pickup_date, pickup_time, return_date, is_airport); compute `days = return_date ? dateDiffDays(pickup_date, return_date) : 1`; `outboundLegEur = computeOutboundLegTotal(...)` with the same arg order as POST:406-416; `extrasEur = computeExtrasTotal(...)` from the current row's extra_* flags; `computedTotalEur = outboundLegEur + extrasEur`; `computedTotalCzk = eurToCzk(computedTotalEur)`; `priceDiverges = Math.abs(computedTotalCzk - amount_czk) > ADMIN_PRICE_TOLERANCE_CZK` (reuse the constant at route.ts:348 — do NOT redefine). If `priceDiverges && !override_price` return 422 with `{ error: 'Price mismatch — server recompute diverges from submitted amount', submittedCzk, computedCzk }` (same shape as POST:439-448). Else set `authoritativeAmountCzk = priceDiverges ? amount_czk : computedTotalCzk` and `amount_eur = priceDiverges ? czkToEur(amount_czk) : computedTotalEur`; put both into updatePayload; when priceDiverges, append an override note to operator_notes (mirror POST:499-501) and console.warn with adminUserId. Add an `amount_czk` entry to the field-changes list so it is audited and emailed. Do NOT call any GNet push for trip fields (guard: even when booking_source==='gnet', skip pushGnetStatus — it only accepts status). Do NOT add a second geocode call. Add the behavior-block vitest cases.</action>
  <verify>
    <automated>npx vitest run tests/admin-bookings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/admin-bookings.test.ts` passes recompute / 422 / override cases.
    - `grep -c "ADMIN_PRICE_TOLERANCE_CZK" app/api/admin/bookings/route.ts` shows the constant referenced in both POST and the new branch but declared once (single `const ADMIN_PRICE_TOLERANCE_CZK =`).
    - A test asserts pushGnetStatus is not called on a gnet-source trip edit.
    - A test asserts the 422 body carries computedCzk and submittedCzk.
    - `grep -n "getPricingConfig\|computeOutboundLegTotal" app/api/admin/bookings/route.ts` shows reuse inside the trip-edit branch (not a reimplemented arithmetic).
  </acceptance_criteria>
  <done>Price-affecting edits recompute server-side, reject silent divergence, accept explicit override with an audit note, and never touch GNet — records only, no collection (D-05).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Leg-isolation, idempotency, and precision assertions (AEDIT-06, AEDIT-05)</name>
  <files>tests/admin-bookings.test.ts</files>
  <read_first>
    - app/api/admin/bookings/route.ts (the completed trip-edit + price branch)
    - .planning/phases/63-admin-booking-editing-change-notification/63-RESEARCH.md (Pitfall 5 leg isolation; Validation Architecture test map)
    - tests/admin-bookings.test.ts (Plan 02 + Task 1 cases)
  </read_first>
  <behavior>
    - Editing outbound leg (round-trip pair sharing payment_intent_id): the supabase update/insert calls reference only the outbound id; a spy on the return leg's id shows zero writes (byte-identical).
    - Saving the same edit twice: logEmail dedup means at most one change email is sent for the pair (bookingId, 'booking_changed', recipient).
    - amount_czk persisted is an integer (no fractional CZK); the test asserts Number.isInteger(savedAmountCzk).
  </behavior>
  <action>Add focused vitest cases that pin the boundary/idempotency/precision edges surfaced by the spec-less probe: (1) AEDIT-06 boundary — construct a round-trip pair (two rows, shared payment_intent_id, distinct ids/leg), PATCH the outbound id, and assert every supabase mutation was scoped to the outbound id and the return leg id received no update/insert; (2) AEDIT-05 idempotency — invoke the same PATCH twice and assert logEmail was consulted each time but the dedup gate yields at most one actual send; (3) AEDIT-07 precision — assert the saved amount_czk is an integer and equals eurToCzk(recomputed EUR) for the non-override path. These are test-only additions; no handler change unless a test reveals a defect (then fix the handler in Task 1's file and note it).</action>
  <verify>
    <automated>npx vitest run tests/admin-bookings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/admin-bookings.test.ts` passes the leg-isolation, idempotency, and precision cases.
    - The leg-isolation test fails if a developer reintroduces a payment_intent_id-scoped write (guards Pitfall 5 by behavior, not by comment).
    - `npx vitest run` full suite green.
  </acceptance_criteria>
  <done>Leg isolation, at-most-once email, and integer-CZK precision are pinned by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser -> PATCH (price fields) | Operator-supplied amount_czk + distance_km (untrusted; must not set the fare directly). |
| API -> pricing_config | Server-authoritative recompute source. |
| API -> GNet (NOT crossed) | Trip edits are local-only; no outbound push. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-03-01 | Tampering | client-supplied amount_czk | high | mitigate | Server recompute from pricing_config + ADMIN_PRICE_TOLERANCE_CZK tolerance; client amount accepted only via explicit override_price, which is logged + audited (test: 422 without override, override note written). |
| T-63-03-02 | Tampering | client-supplied distance_km | medium | accept | Distance is trusted at the price level only; the resulting price is tolerance-checked (existing deliberate boundary, Pitfall 2). A wrong distance yields a wrong price caught by the tolerance gate. |
| T-63-03-03 | Tampering (cross-leg) | price recompute + save | high | mitigate | Recompute + update + audit scoped by .eq('id', ...); leg-isolation test (Task 2). |
| T-63-03-04 | Repudiation | price override | low | mitigate | Override appends an operator_notes note (standard-rate-would-be) + amount audit row + console.warn with adminUserId. |
| T-63-03-05 | Data integrity | GNet-sourced trip edit drift | low | accept | Local-only by design (no GNet trip API); passive UI warning added in Plan 05 (not a security control). |
</threat_model>

## Planner Assumptions (spec-less edge probe — surfaced, not dropped)

Explicit flagged assumptions for this plan's requirements (AEDIT-02, AEDIT-03, AEDIT-06, AEDIT-07):

- **[ASSUMPTION] AEDIT-02 / AEDIT-03 (unclassified):** a vehicle-class or route change ALWAYS routes through the price-review step (never a silent direct commit), even if the recomputed price happens to equal the old price — the operator still confirms. This is the safe reading of D-02/D-06.
- **[ASSUMPTION] AEDIT-07 (boundary):** the divergence boundary is `|computedCzk - amount_czk| > ADMIN_PRICE_TOLERANCE_CZK` (=2). Exactly 2 CZK apart is WITHIN tolerance (accepted without override); 3 CZK triggers the 422. This matches the POST handler's existing boundary verbatim.
- **[ASSUMPTION] AEDIT-07 (precision):** the authoritative saved amount is integer CZK (`amount_czk` is `z.number().int()`); EUR is derived via the existing `czkToEur`/server-pricing `roundUpToFive` path — the edit path adds no second rounding rule. Lifted to `must_haves.truths`.
- **[ASSUMPTION] AEDIT-06 (precision):** leg isolation has no numeric-precision axis; the boundary is byte-identity of the linked leg row (lifted to `must_haves.truths`).
- **[ASSUMPTION] distance trust boundary:** `distance_km` is client-supplied (from the Plan 05 `/api/calculate-price` round trip) and trusted only at the price level — the tolerance check on the resulting price is the safety net (RESEARCH Pitfall 2). No second server-side geocode is added.

<verification>
- `npx vitest run tests/admin-bookings.test.ts` green.
- `npx vitest run` full suite green at wave merge.
- grep acceptance checks pass; single tolerance constant confirmed.
</verification>

<success_criteria>
- Price-affecting edits recompute authoritatively, reject silent divergence (422), accept audited override, and record-only (no payment link).
- Leg isolation, at-most-once email, integer-CZK precision proven by tests.
</success_criteria>

<output>
Create `.planning/phases/63-admin-booking-editing-change-notification/63-03-SUMMARY.md` when done.
</output>
