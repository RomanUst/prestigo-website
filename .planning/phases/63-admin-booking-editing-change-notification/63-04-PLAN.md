---
phase: 63-admin-booking-editing-change-notification
plan: 04
type: execute
wave: 3
depends_on: ["63-02"]
files_modified:
  - components/admin/BookingChangeHistory.tsx
  - tests/booking-change-history.test.tsx
autonomous: true
requirements: [FOLLOW-02]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "BookingChangeHistory lazily fetches GET /api/admin/bookings/[id]/audit-log when the booking row is expanded (not on mount of the table) — FOLLOW-02, D-11."
    - "Audit entries render newest-first; multiple field-changes from one PATCH are grouped under a shared changed_at/operator header (D-10)."
    - "Zero audit rows render the empty state: heading 'No changes recorded yet.' + body 'Edits to this booking will appear here.' (UI-SPEC Copywriting Contract)."
    - "While the fetch is in flight, a muted 'Loading history…' line (11px, var(--warmgrey)) is shown (UI-SPEC E3 loading)."
    - "A fetch failure renders 'Couldn't load change history — try again.' with a retry affordance (UI-SPEC E3 error)."
    - "Each entry shows field label: old -> new, operator, timestamp, and whether the client was notified (D-10)."
    - statement: "The history block scrolls internally past a height threshold (~max-height 240px, overflow-y auto) rather than growing the expanded row unbounded on a heavily-edited booking (UI-SPEC E3 overflow)."
      verification: backstop
    - "Long changed-values in an entry wrap within the block — no truncation/ellipsis (UI-SPEC E3 long-text)."
  artifacts:
    - "components/admin/BookingChangeHistory.tsx :: BookingChangeHistory"
    - tests/booking-change-history.test.tsx
  key_links:
    - "Component fetches the Plan 02 GET route; renders inside the expanded booking row (mounted by Plan 05)."
    - "Follows the FlightStatusBlock lazy-per-row-fetch pattern (useState/useCallback, inline-style, no shadcn)."
  prohibitions:
    - "MUST NOT fetch on table mount — only when the row is expanded (lazy), per Pitfall 4 / D-11."
    - "MUST NOT truncate values with text-overflow ellipsis (UI-SPEC long-text)."
    - "MUST NOT introduce shadcn/Radix/Tailwind component classes — use the existing inline-style navy/gold language (UI-SPEC)."
---

<objective>
Build the read side of the audit trail: a `BookingChangeHistory` component that lazily fetches and renders a booking's change history inside its expanded admin row (D-11, FOLLOW-02). It reuses the FlightStatusBlock lazy-per-row-fetch pattern and the project's inline-style design language, covering every UI state the UI-SPEC enumerated (empty / loading / error / populated / overflow / long-text).

Purpose: Decoupling the history component from the big BookingsTable edit work (Plan 05) lets it be built in parallel with the price backend (Plan 03) and keeps each plan focused.
Output: `components/admin/BookingChangeHistory.tsx` + jsdom tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/63-admin-booking-editing-change-notification/63-CONTEXT.md
@.planning/phases/63-admin-booking-editing-change-notification/63-PATTERNS.md
@.planning/phases/63-admin-booking-editing-change-notification/63-UI-SPEC.md
@.planning/phases/63-admin-booking-editing-change-notification/63-02-SUMMARY.md
@components/admin/FlightStatusBlock.tsx
@components/admin/StatusBadge.tsx
</context>

<artifacts_produced>
- **Component:** `components/admin/BookingChangeHistory.tsx` exporting `BookingChangeHistory({ bookingId }: { bookingId: string })` — lazy-fetches the audit log, groups rows by `changed_at`, renders entries newest-first with field label / old -> new / operator / timestamp / notified badge, plus empty / loading / error / overflow states.
- **Types:** local `interface AuditRow { id; field; old_value; new_value; operator_id; changed_at; notified }` (do NOT import from the stale types/database.types.ts — Pitfall 1).
- **Tests:** `tests/booking-change-history.test.tsx` (empty, loading, error+retry, populated+grouping).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: BookingChangeHistory component (lazy fetch + all states)</name>
  <files>components/admin/BookingChangeHistory.tsx</files>
  <read_first>
    - components/admin/FlightStatusBlock.tsx (lazy per-row fetch: useState/useCallback, fetch('/api/admin/flight-refresh'), inline error treatment, refreshing state, inline styles)
    - components/admin/StatusBadge.tsx (variant/label props — reuse for the notified badge)
    - .planning/phases/63-admin-booking-editing-change-notification/63-UI-SPEC.md (Copywriting Contract rows: empty heading/body, error; Color: history block background var(--anthracite-mid), timestamp/operator micro-label var(--copper-light); Typography 11px labels/13px body; UI Considerations E3 rows)
    - .planning/phases/63-admin-booking-editing-change-notification/63-02-SUMMARY.md (GET /api/admin/bookings/[id]/audit-log response shape { rows: [...] })
  </read_first>
  <action>Create `components/admin/BookingChangeHistory.tsx` exporting `BookingChangeHistory({ bookingId }: { bookingId: string })`. On first render / when it becomes visible, lazily `fetch('/api/admin/bookings/' + bookingId + '/audit-log')` once (guard with a hasFetched ref/state so it does not refetch on every parent re-render). Track state `idle | loading | loaded | error`. Local `interface AuditRow` (id, field, old_value, new_value, operator_id, changed_at, notified) — do NOT import types/database.types.ts (stale, Pitfall 1). States: loading -> a muted 'Loading history…' line (11px, color var(--warmgrey)); error -> "Couldn't load change history — try again." with a retry button that re-runs the fetch (mirror FlightStatusBlock inline-error shape); empty (rows.length===0) -> heading "No changes recorded yet." + body "Edits to this booking will appear here."; populated -> group rows by changed_at (rows sharing a changed_at render under one header showing the humanized timestamp + operator in an uppercase micro-label, var(--copper-light)), each row rendering `<field label>: <old_value> -> <new_value>` (13px body, values wrap, no ellipsis) and a StatusBadge for notified (variant 'active' label 'Notified' when true, else a muted 'Not notified'). Container: background var(--anthracite-mid), padding 16px, `max-height: 240px; overflow-y: auto` so a heavily-edited booking scrolls internally instead of growing the row unbounded (UI-SPEC E3 overflow backstop — verify visually at build time). Humanize field keys to labels (pickup_date -> 'Pickup date', pickup_time -> 'Pickup time', client_first_name -> 'First name', client_last_name -> 'Last name', client_email -> 'Email', client_phone -> 'Phone', flight_number -> 'Flight number', vehicle_class -> 'Vehicle', origin_address -> 'From', destination_address -> 'To', amount_czk -> 'Total price'). Use only inline React.CSSProperties + CSS custom-property colors (no shadcn/Tailwind classes).</action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "BookingChangeHistory" | grep -q '^0$' && echo TYPECHECK_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "audit-log" components/admin/BookingChangeHistory.tsx` shows the GET fetch to the Plan 02 route.
    - `grep -n "No changes recorded yet.\|Couldn't load change history" components/admin/BookingChangeHistory.tsx` shows the empty + error copy verbatim from the UI-SPEC.
    - `grep -n "overflow-y\|maxHeight\|max-height" components/admin/BookingChangeHistory.tsx` shows the internal-scroll container.
    - `grep -ci "className=\|tailwind\|@radix\|shadcn" components/admin/BookingChangeHistory.tsx` returns 0 (inline-style only).
    - No fetch is issued at module import time (fetch only inside an effect/handler tied to visibility).
  </acceptance_criteria>
  <done>The history component renders all six states and fetches lazily from the audit-log route.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Component tests (empty / loading / error+retry / populated grouping)</name>
  <files>tests/booking-change-history.test.tsx</files>
  <read_first>
    - components/admin/BookingChangeHistory.tsx (the component from Task 1)
    - tests/admin-bookings.test.ts (existing vi.hoisted + fetch-mock conventions to mirror)
    - vitest.config.ts (jsdom env, @ alias)
  </read_first>
  <behavior>
    - Mount with fetch resolving { rows: [] } -> shows "No changes recorded yet." and "Edits to this booking will appear here.".
    - Mount with fetch pending -> shows "Loading history…".
    - Mount with fetch rejecting -> shows "Couldn't load change history — try again." and a retry control that, when clicked, re-issues the fetch.
    - Mount with two rows sharing one changed_at + one row at a different changed_at -> renders two grouped headers, newest-first; an entry shows 'old -> new' text and a notified badge.
  </behavior>
  <action>Create `tests/booking-change-history.test.tsx` using @testing-library/react (already used in the repo's component tests) + vitest, mocking global fetch. Cover the four behavior cases. Assert grouping (two distinct changed_at groups produce two headers), ordering (newest changed_at first), the empty/loading/error copy strings, and that clicking retry after an error triggers a second fetch call.</action>
  <verify>
    <automated>npx vitest run tests/booking-change-history.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/booking-change-history.test.tsx` passes all four cases.
    - The error test asserts a second fetch call after clicking retry.
    - The populated test asserts newest-first ordering and shared-timestamp grouping.
  </acceptance_criteria>
  <done>Component behavior is pinned by jsdom tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-log API -> browser render | Audit values (which include prior user-supplied field values) are rendered as text in the admin DOM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-04-01 | Injection (stored XSS) | rendering old_value/new_value | medium | mitigate | Values are rendered as React text children (auto-escaped), never via dangerouslySetInnerHTML — inherent React escaping. |
| T-63-04-02 | Information Disclosure | history fetch | low | accept | Route is admin-guarded (Plan 02); the component only calls it, adds no new exposure. |
</threat_model>

<verification>
- `npx vitest run tests/booking-change-history.test.tsx` green.
- Typecheck clean for the new component.
- grep acceptance checks (copy, inline-style, lazy fetch, overflow) pass.
</verification>

<success_criteria>
- A lazily-fetched, inline-styled change-history block renders every UI-SPEC state and is ready for Plan 05 to mount in the expanded row.
</success_criteria>

<output>
Create `.planning/phases/63-admin-booking-editing-change-notification/63-04-SUMMARY.md` when done.
</output>
