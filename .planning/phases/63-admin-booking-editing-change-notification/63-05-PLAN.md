---
phase: 63-admin-booking-editing-change-notification
plan: 05
type: execute
wave: 4
depends_on: ["63-02", "63-03", "63-04"]
files_modified:
  - components/admin/BookingsTable.tsx
autonomous: true
requirements: [AEDIT-01, AEDIT-02, AEDIT-03, AEDIT-04, AEDIT-05, AEDIT-06, AEDIT-07, FOLLOW-02]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All trip/passenger fields become editable simultaneously in one inline edit mode inside the existing expandable row of BookingsTable.tsx — no separate page/modal (D-01, AEDIT-01..04)."
    - "Each field has its own save control (per-field granularity, D-02): cheap fields commit directly; price-affecting fields (vehicle_class, route) open the price-review step (D-06) instead of committing silently."
    - "Cheap-field save controls carry specific labels: 'Save Date & Time', 'Save Name', 'Save Email', 'Save Phone', 'Save Flight Number'; price-affecting fields use 'Review Price →' (UI-SPEC Copywriting Contract)."
    - "Editing a route/vehicle triggers a client round trip to POST /api/calculate-price to obtain a fresh distanceKm + preview price before the price-review step (AEDIT-03, D-03) — reusing AddressInput (not AddressInputNew)."
    - "The price-review step shows old -> new amount (new amount in var(--copper)), an override amount input, a 'Notify client of this change' toggle, and a 'Confirm & Save' primary CTA (D-06, AEDIT-07)."
    - "On a 422 price-mismatch response, the review step surfaces computedCzk vs submittedCzk inline (#f87171) and requires an explicit override before saving (AEDIT-07)."
    - "The 'Notify client of this change' toggle is submitted as notify_client with the save; its helper text reads 'Client will receive a branded email showing exactly what changed.' (D-08, AEDIT-05)."
    - "completed / cancelled bookings show '{Status} bookings are final and cannot be edited.' in place of edit controls (RESEARCH editable-status set)."
    - "booking_source='gnet' bookings show the passive banner 'This booking originated from a GNet partner — edits are recorded locally but not synced back to GNet.' and remain editable (non-blocking)."
    - "The expanded row mounts BookingChangeHistory for the booking (D-11, FOLLOW-02)."
    - "Editable fields render blank (not a placeholder dash) when the underlying value is null; route/destination fields are hidden for hourly (non-transfer) bookings; long values wrap with no truncation (UI-SPEC E1/E2 considerations)."
    - "Per-field save shows the three-state inline hint 'Saving...' / 'Saved' / 'Error saving'; the price-review Confirm button shows a disabled 'Calculating…' state while a request is in flight (UI-SPEC E1 loading / E2 loading)."
  artifacts:
    - "components/admin/BookingsTable.tsx :: inline trip-edit mode + per-field save controls"
    - "components/admin/BookingsTable.tsx :: price-review step (old->new, override input, notify toggle, Confirm & Save)"
    - "components/admin/BookingsTable.tsx :: terminal-status read-only notice + GNet passive banner + mounted BookingChangeHistory"
  key_links:
    - "Cheap fields reuse the flushNotes per-field save-state machine (BookingsTable.tsx:185-220) keyed by booking.id; patchBooking (158-169) body type extended with the new trip fields + amount_czk + override_price + notify_client."
    - "Route editing replicates ManualBookingForm.tsx:103-149,196-224 (AddressInput -> POST /api/calculate-price -> distanceKm) then submits distance_km in the PATCH."
    - "Price-review reads computedCzk/submittedCzk from the 422 body (Plan 03 contract); Confirm & Save posts { vehicle_class?, origin_address?, destination_address?, distance_km?, amount_czk, override_price?, notify_client }."
  prohibitions:
    - "MUST NOT introduce a separate edit page or wizard modal — inline in the expandable row only (D-01)."
    - "MUST NOT use AddressInputNew or the NEXT_PUBLIC_USE_NEW_PLACES_API flag — use AddressInput, matching the admin ManualBookingForm precedent (Pitfall 6)."
    - "MUST NOT rename the pre-existing operator_notes / driver_price_czk 'Save' controls (out of scope, UI-SPEC exception)."
    - "MUST NOT commit a price-affecting field silently — it always routes through the price-review step (D-02, D-06)."
    - "MUST NOT call the Google Routes API directly from the client — go through /api/calculate-price (Pitfall / Don't Hand-Roll)."
    - "MUST NOT introduce shadcn/Radix/Tailwind component classes — inline-style navy/gold language only (UI-SPEC)."
---

<objective>
Build the operator-facing edit surface: the inline trip-edit mode in the existing expandable booking row (D-01), with per-field save controls (D-02), the price-review step for vehicle/route changes (D-06, AEDIT-07), the notify-client toggle (D-08), the terminal-status read-only notice and GNet passive banner, and the mounted change-history block (D-11). This is the far end of the tracer — it wires the proven backend (Plans 02/03) and the history component (Plan 04) into the surface the operator actually uses (AEDIT-01..07).

Purpose: Composition of already-shipped inline-edit patterns (operator_notes/driver_price per-field save; ManualBookingForm distance round trip; FlightStatusBlock lazy block) — low architectural risk, high surface area, so it lands last.
Output: extended `components/admin/BookingsTable.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/63-admin-booking-editing-change-notification/63-CONTEXT.md
@.planning/phases/63-admin-booking-editing-change-notification/63-RESEARCH.md
@.planning/phases/63-admin-booking-editing-change-notification/63-PATTERNS.md
@.planning/phases/63-admin-booking-editing-change-notification/63-UI-SPEC.md
@.planning/phases/63-admin-booking-editing-change-notification/63-02-SUMMARY.md
@.planning/phases/63-admin-booking-editing-change-notification/63-03-SUMMARY.md
@.planning/phases/63-admin-booking-editing-change-notification/63-04-SUMMARY.md
@components/admin/BookingsTable.tsx
@components/admin/ManualBookingForm.tsx
@components/booking/AddressInput.tsx
</context>

<artifacts_produced>
- **BookingsTable.tsx additions:** inline trip-edit mode state (keyed by booking.id); per-field save controls for pickup date+time (committed together per D-02), first/last name, email, phone, flight number, each reusing the three-state save indicator; extended `patchBooking` body type; terminal-status read-only notice; GNet passive banner; mounted `<BookingChangeHistory bookingId={...} />` in both the card (mobile) and table (desktop) expanded views.
- **Price-review step:** an inline sub-panel triggered by 'Review Price →' on vehicle_class / route change — collects new AddressInput PlaceResults, calls `/api/calculate-price`, shows old -> new amount + override input + notify toggle + 'Confirm & Save', handles the 422 contract.
- No new files; no new npm packages.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Inline edit mode — cheap-field per-field save, notices, history mount</name>
  <files>components/admin/BookingsTable.tsx</files>
  <read_first>
    - components/admin/BookingsTable.tsx (18-65 Booking interface; 129 notesSaving state; 158-169 patchBooking; 185-220 flushNotes per-field save-state machine + debounce; 866-1010 card expanded view incl. operator-notes block; 1127-1360 table expanded view incl. notes block + UI_TRANSITIONS status control; 392,772,1621-1631 GNet copy)
    - .planning/phases/63-admin-booking-editing-change-notification/63-UI-SPEC.md (Copywriting Contract; Spacing sm/md/lg; Typography 11/13/26; Color accent rules; Visual Hierarchy; UI Considerations E1/E6 rows)
    - components/admin/PricingForm.tsx (inputBaseStyle / headerLabelStyle idioms referenced by UI-SPEC)
    - .planning/phases/63-admin-booking-editing-change-notification/63-04-SUMMARY.md (BookingChangeHistory import + props)
  </read_first>
  <action>Extend `patchBooking` (BookingsTable.tsx:158-169) body type union to include pickup_date, pickup_time, client_first_name, client_last_name, client_email, client_phone, flight_number, vehicle_class, origin_address, destination_address, origin_lat/lng, destination_lat/lng, distance_km, amount_czk, override_price, notify_client. Add an "Edit trip details" toggle in the expanded row that reveals a trip-edit field group when the booking status is NOT completed/cancelled; when status IS completed or cancelled, render the read-only notice "{Status} bookings are final and cannot be edited." in place of the edit controls (humanize status via the existing STATUS_LABELS map). For cheap fields, add per-field editable inputs pre-filled with the current value (render blank when the value is null — no dash placeholder), using the inputBaseStyle idiom (copper focus border on onFocus reverting on blur, full-width, values wrap). Give each a dedicated save control with the exact labels: pickup date + pickup time share one control "Save Date & Time" (committed together per D-02), "Save Name" (commits client_first_name + client_last_name together), "Save Email", "Save Phone", "Save Flight Number". Reuse the flushNotes three-state save machine (185-220) generalized per field/booking id, showing the inline hints "Saving..." / "Saved" / "Error saving" (11px, color-coded per UI-SPEC). Show field-level validation errors inline in #f87171 (e.g. invalid email). Hide route/destination inputs for hourly (non-transfer) bookings (mirror ManualBookingForm trip_type-conditional visibility). Render the GNet passive banner "This booking originated from a GNet partner — edits are recorded locally but not synced back to GNet." when booking_source==='gnet' (non-blocking, mirror the tone at BookingsTable.tsx:1621-1631). Mount `<BookingChangeHistory bookingId={booking.id} />` inside BOTH the card expanded view (~866-1010) and the table expanded view (~1127-1360). Keep the pre-existing operator_notes / driver_price_czk controls and their "Save" labels untouched. Inline-style only (no shadcn/Tailwind classes).</action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit 2>&1 | grep -c "BookingsTable" | grep -q '^0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Save Date & Time\|Save Name\|Save Email\|Save Phone\|Save Flight Number" components/admin/BookingsTable.tsx` shows all five labels present.
    - `grep -n "bookings are final and cannot be edited" components/admin/BookingsTable.tsx` shows the read-only notice.
    - `grep -n "recorded locally but not synced back to GNet" components/admin/BookingsTable.tsx` shows the GNet banner.
    - `grep -n "BookingChangeHistory" components/admin/BookingsTable.tsx` shows the import + mount (both expanded views).
    - `grep -c "AddressInputNew\|USE_NEW_PLACES_API" components/admin/BookingsTable.tsx` returns 0.
    - The pre-existing operator_notes/driver_price "Save" controls are unchanged (diff shows no rename).
    - `npx tsc --noEmit` reports no BookingsTable errors; `npx vitest run` stays green.
  </acceptance_criteria>
  <done>Operators can enter edit mode, commit cheap fields per-field, see terminal/GNet notices, and view change history in the expanded row.</done>
</task>

<task type="auto">
  <name>Task 2: Price-review step — address round trip, override, notify toggle, Confirm & Save</name>
  <files>components/admin/BookingsTable.tsx</files>
  <read_first>
    - components/admin/ManualBookingForm.tsx (103-149 handleCalculatePrice; 196-224 payload distance_km forwarding; AddressInput import at line 4)
    - components/booking/AddressInput.tsx (PlaceResult onSelect: {address, placeId, lat, lng})
    - components/admin/BookingsTable.tsx (patchBooking extended in Task 1; expanded-row edit group from Task 1)
    - .planning/phases/63-admin-booking-editing-change-notification/63-03-SUMMARY.md (422 body shape { error, submittedCzk, computedCzk }; recompute contract)
    - .planning/phases/63-admin-booking-editing-change-notification/63-UI-SPEC.md (Copywriting Contract price-review rows; Color accent for new amount + Confirm & Save; E2 loading/error/populated)
  </read_first>
  <action>Add the price-review step, triggered by a "Review Price →" control on the vehicle_class field and on the route (origin/destination) fields (these never commit directly — D-02/D-06). For route edits, reuse `AddressInput` (NOT AddressInputNew) to collect new origin/destination PlaceResults, then POST to `/api/calculate-price` with { origin:{lat,lng}, destination:{lat,lng}, tripType: booking.trip_type, pickupDate, pickupTime, ... } exactly like ManualBookingForm.handleCalculatePrice (103-149), reading `distanceKm` + preview price from the response. Render an inline price-review sub-panel (background var(--anthracite-mid), padding lg=24px) showing: the old amount and the new (recomputed) amount with the new amount in var(--copper) (Visual Hierarchy focal point); an override amount input (operator may adjust); a "Notify client of this change" toggle with helper "Client will receive a branded email showing exactly what changed."; and a primary CTA "Confirm & Save" (outline/ghost copper, never filled). While the calculate-price or PATCH request is in flight, disable the CTA and show "Calculating…" (mirror the existing "Updating..." disabled button). On Confirm & Save, PATCH { id, vehicle_class?, origin_address?, destination_address?, origin_lat/lng?, destination_lat/lng?, distance_km?, amount_czk, override_price?, notify_client }. Handle the 422 price-mismatch: read computedCzk/submittedCzk from the response, show them inline (#f87171) with the copy "Price mismatch — server recompute diverges from the submitted amount. Review and override if intentional.", and require an explicit override (set override_price=true from the override input) before re-submitting. On success, optimistically update the row's amount_czk and collapse the step. Addresses in the diff wrap (no ellipsis). Inline-style only.</action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit 2>&1 | grep -c "BookingsTable" | grep -q '^0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Confirm & Save\|Review Price" components/admin/BookingsTable.tsx` shows both CTAs.
    - `grep -n "Notify client of this change\|showing exactly what changed" components/admin/BookingsTable.tsx` shows the toggle label + helper.
    - `grep -n "/api/calculate-price" components/admin/BookingsTable.tsx` shows the distance/preview round trip.
    - `grep -n "computedCzk\|submittedCzk" components/admin/BookingsTable.tsx` shows the 422 handling reads both amounts.
    - `grep -n "override_price\|notify_client" components/admin/BookingsTable.tsx` shows both flow into the PATCH payload.
    - `grep -c "AddressInputNew" components/admin/BookingsTable.tsx` returns 0; AddressInput is imported.
    - `npx tsc --noEmit` clean for BookingsTable; `npx vitest run` green.
  </acceptance_criteria>
  <done>Vehicle/route changes route through a price-review step with server recompute, override, notify toggle, and 422 handling — the fare is never changed silently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator input -> PATCH payload | Field values + amount_czk + override_price + notify_client (all re-validated server-side by Plans 02/03). |
| AddressInput -> Google Places (client) | Place selection uses the restricted NEXT_PUBLIC key (Places-only). |
| client -> /api/calculate-price | Distance/preview round trip (server holds the Routes API key). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-05-01 | Tampering | client-set amount_czk via override input | high | mitigate | Client value is advisory only; the server recompute + tolerance + override audit (Plan 03) is authoritative — the UI cannot bypass the 422 without an explicit override that is logged. |
| T-63-05-02 | Info Disclosure | Google Routes API key | medium | mitigate | Client never calls Routes directly — only /api/calculate-price (server key); UI reuses AddressInput (restricted Places key), not a new key path. |
| T-63-05-03 | Tampering (cross-leg) | inline edit of one leg's row | high | mitigate | Every save is a PATCH scoped by that row's id (Plan 02/03 server scoping); the UI edits one booking.id at a time. |
| T-63-05-04 | Injection (stored XSS) | rendering current field values in inputs | low | mitigate | Values rendered as React-controlled input values / text children (auto-escaped). |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for BookingsTable.tsx.
- `npx vitest run` full suite green (phase gate).
- grep acceptance checks (CTA labels, notices, calculate-price, 422 handling, AddressInput, history mount) pass.
- Manual: per VALIDATION.md — edit each field type in the admin panel; confirm per-field save + price-review + branded email visual (old -> new).
</verification>

<success_criteria>
- Operator can edit pickup date/time, vehicle class, route, and passenger/contact/flight inline in the expanded row, with price changes reviewed before saving, the client optionally notified, and full change history visible — satisfying AEDIT-01..07 + FOLLOW-02 at the UI layer.
</success_criteria>

<output>
Create `.planning/phases/63-admin-booking-editing-change-notification/63-05-SUMMARY.md` when done.
</output>
