---
phase: 72-ai-translation-pipeline-catalogs
plan: 05
type: execute
wave: 5
depends_on: [72-04]
files_modified:
  - messages/ru.json
  - messages/es.json
  - messages/fr.json
  - content/routes/ru
  - content/routes/es
  - content/routes/fr
  - content/pages/ru
  - content/pages/es
  - content/pages/fr
  - content/blog/ru
  - content/blog/es
  - content/blog/fr
  - i18n/translation-manifest.json
  - i18n/QA-REPORT.md
autonomous: false
requirements: [TR-02]
user_setup:
  - service: anthropic
    why: "The first full-catalog run spends real Opus tokens; it runs in CI under the repo secret (D-02) and lands in a PR the owner reviews and merges (D-08/D-09)."

estimate:
  tokens: 55000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "After the first full run, messages/ru.json, messages/es.json, messages/fr.json each have exactly the same leaf-key set as messages/en.json — 0 missing (including the 22 previously-drifted RoutePage.* keys, backfilled by the missing-unitKey rule) and 0 extra (TR-02 SC#3)."
    - "content/routes/{ru,es,fr}/, content/pages/{ru,es,fr}/ (incl. services/), and content/blog/{ru,es,fr}/ are generated with the same file set and structure as their en/ sources (TR-02)."
    - "No untranslated English value remains in a processed translated surface (no-EN-leakage), per the QA report's no-leakage check (SC#4)."
    - "The English root output is byte-for-byte unchanged after the run — git diff --exit-code over messages/en.json + content/**/en/** is clean (SC#4)."
    - "ar/hi/zh catalogs are untouched and continue to render English via fallback (their real generation is Phase 73)."
    - "A QA sampling pass is recorded: i18n/QA-REPORT.md is committed in the PR and the owner performs a native-RU spot-check on the sampled diff before merge (D-09)."
    - statement: "If the first run is interrupted or a batch partially fails, the committed manifest marks only successfully-written units as done, so re-running the workflow resumes and re-translates only the not-yet-completed units."
      verification: backstop
  artifacts:
    - "messages/{ru,es,fr}.json — complete real translations (replacing the stale/drifted EN-copy stubs)"
    - "content/routes/{ru,es,fr}/*.json, content/pages/{ru,es,fr}/**/*.json, content/blog/{ru,es,fr}/*.mdx"
    - "i18n/translation-manifest.json — populated after the first full run"
    - "i18n/QA-REPORT.md — the recorded QA sampling pass (D-09)"
  key_links:
    - "workflow_dispatch (or first EN-touching push) -> pipeline full run (empty manifest => translate everything, Pitfall 4) -> PR -> owner review + RU spot-check -> merge -> Vercel prod (ru/es/fr live; EN unchanged)"
    - "the completeness check runs against the real generated corpus and gates the QA report"
  prohibitions:
    - "Never generate AR/HI/ZH real translations in this phase — they stay EN placeholders until Phase 73 (scope guard)."
    - "Never merge the PR before the owner's native-RU spot-check and a clean completeness/no-leakage QA report (D-09 — automated-only gating was rejected)."
---

<objective>
Execute the pipeline for the first time end-to-end via the CI workflow, producing complete ru/es/fr catalogs + content (TR-02), and record the QA sampling pass (D-09) that the owner reviews in the PR before merge. This is where the built pipeline becomes shipped translations while EN stays byte-for-byte unchanged and ar/hi/zh stay EN placeholders.

Purpose: TR-02 completion for ru/es/fr and the human QA gate. The first run is a full-catalog translation (empty manifest — Pitfall 4) and also reconciles the drifted placeholder catalogs (the 22 missing RoutePage.* keys backfill automatically).
Output: complete `messages/{ru,es,fr}.json` + `content/{routes,pages,blog}/{ru,es,fr}/**`, a populated manifest, and a committed `i18n/QA-REPORT.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-ai-translation-pipeline-catalogs/72-RESEARCH.md
@.planning/phases/72-ai-translation-pipeline-catalogs/72-COVERAGE.md
@.planning/phases/72-ai-translation-pipeline-catalogs/72-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Trigger the first full-catalog run (workflow_dispatch) and open the translation PR</name>
  <read_first>.planning/phases/72-ai-translation-pipeline-catalogs/72-RESEARCH.md (Pitfall 4 first-run sizing + Open Questions 2), .github/workflows/i18n-translate.yml</read_first>
  <action>The pipeline, workflow, and secret are all in place; the remaining step is dispatching the run, which spends real Opus tokens and requires the owner's go-ahead. Optionally run the zero-cost pre-check first: `node scripts/i18n-translate.mjs --dry-run --fixtures tests/fixtures/i18n`.</action>
  <instructions>
    Trigger the workflow manually: Actions -> i18n-translate -> Run workflow (or `gh workflow run i18n-translate.yml`). The first run has an empty manifest, so it translates the full catalog + all content across ru/es/fr (~602 catalog keys + ~47 content files + ~11 blog posts — far fewer API calls once batched per Pattern 3) and backfills the 22 drifted RoutePage.* keys. The job runs the EN-unchanged assertion, then peter-evans/create-pull-request opens the PR. npm ci is handled by the workflow runner (CI, not a subagent).
  </instructions>
  <precondition>ANTHROPIC_API_KEY is set as a repo secret (72-04 Task 2) and the 72-04 workflow is on the default branch so it is dispatchable.</precondition>
  <verification>Owner confirms the workflow run completed green and a PR was opened containing messages/{ru,es,fr}.json, content/{routes,pages,blog}/{ru,es,fr}/**, i18n/translation-manifest.json, and i18n/QA-REPORT.md; ar/hi/zh files are not in the diff.</verification>
  <acceptance_criteria>
    - The workflow run reaches the PR step without an auth error and without failing the EN-unchanged assertion.
    - A PR exists with the four artifact groups (catalogs, content, manifest, QA report).
    - ar/hi/zh catalog files are NOT in the PR diff (scope guard).
  </acceptance_criteria>
  <resume-signal>Type "done" once the run is green and the translation PR is open.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Assert completeness + EN-unchanged on the generated corpus</name>
  <files>i18n/QA-REPORT.md, messages/ru.json, messages/es.json, messages/fr.json</files>
  <read_first>i18n/QA-REPORT.md, .planning/phases/72-ai-translation-pipeline-catalogs/72-RESEARCH.md (Validation Architecture — Test Map), scripts/lib/i18n-qa-report.mjs</read_first>
  <action>On the PR branch, run the completeness + EN-unchanged assertions against the REAL generated corpus (these back the QA report). Confirm each of messages/{ru,es,fr}.json has the same leaf-key set as messages/en.json (0 missing, 0 extra) via the pipeline's check mode (node scripts/i18n-translate.mjs --check runs checkCompleteness + the EN-unchanged git assertion with no API calls; if --check is not wired, run tests/i18n-completeness.test.ts pointed at the real messages files). Read i18n/QA-REPORT.md and confirm the no-EN-leakage and DNT-preserved sections list no unresolved failures — any flagged unit must be triaged (re-run or manually corrected), not ignored.</action>
  <verify>
    <automated>git diff --exit-code -- messages/en.json content/routes/en content/pages/en content/blog/en && node scripts/i18n-translate.mjs --check</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --exit-code` over EN sources is clean (EN byte-for-byte unchanged).
    - `node scripts/i18n-translate.mjs --check` exits 0 — every ru/es/fr catalog leaf-key set equals en.json (0 missing).
    - i18n/QA-REPORT.md shows 0 unresolved no-EN-leakage and 0 unresolved DNT/MDX failures (or every flagged item is triaged in the SUMMARY).
    - ar/hi/zh message files are unchanged from before the run (git shows no diff on messages/{ar,hi,zh}.json).
  </acceptance_criteria>
  <done>The generated ru/es/fr corpus is complete, leak-free per the QA report, and EN is untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Owner native-RU spot-check + PR merge (D-09 human QA gate)</name>
  <read_first>i18n/QA-REPORT.md, .planning/phases/72-ai-translation-pipeline-catalogs/72-RESEARCH.md (D-09 QA gate)</read_first>
  <what-built>The translation PR is open with complete ru/es/fr catalogs + content + i18n/QA-REPORT.md (completeness, no-EN-leakage, DNT-preserved, MDX-structure results) and Task 2's automated completeness/EN-unchanged assertions are green. D-09 explicitly rejected automated-only gating — the owner's native-RU sign-off is required before merge.</what-built>
  <how-to-verify>
    1. Read i18n/QA-REPORT.md in the PR: confirm 0 missing keys per locale, 0 unresolved no-EN-leakage, 0 unresolved DNT/MDX failures.
    2. Native-RU spot-check on the sampled RU diff: formal «Вы» register, standard exonyms (e.g. Прага), no broken ICU variables or rich-text tags, premium tone.
    3. On approval, merge the PR to main (Vercel auto-deploys prod). If tone/quality issues are found, edit the target file directly (D-05 preserves the manual edit on future runs) or adjust i18n/glossary.json and re-run, then re-review.
    4. Post-merge smoke: /ru/ renders translated chrome+content, / (EN root) is unchanged, /ar/ still renders English (fallback).
  </how-to-verify>
  <acceptance_criteria>
    - Owner recorded a native-RU spot-check verdict in the plan SUMMARY (D-09).
    - PR merged; the QA report is committed to main as the recorded QA sampling pass.
    - Post-merge smoke: /ru/ renders translated content, / (EN root) unchanged, /ar/ still English (fallback).
  </acceptance_criteria>
  <resume-signal>Type "approved" after the RU spot-check + merge, or describe the tone/quality issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI job -> Anthropic API | the first full run spends real tokens under the repo secret |
| translation PR -> main / prod | model-generated content is promoted to production on merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-05 | Information Disclosure | secret/env value leaking into a generated catalog or the QA report committed in the PR | medium | mitigate | Task 2 asserts EN-unchanged + reviews the QA report (built only from sources/outputs/manifest); the PR diff is human-reviewed before merge (D-08) |
| T-72-09 | Tampering | a broken/low-quality translation reaching prod unreviewed | high | mitigate | D-09 blocking human-verify gate (Task 3) — owner native-RU spot-check + clean automated QA report both required before merge; automated-only gating rejected |
| T-72-02 | Tampering | prompt-injection-induced deviation surviving into a merged surface | low | accept | Owner-authored source; the fail-closed verifier (72-02) already flags deviations into the QA report the owner reviews here |
</threat_model>

<verification>
- `git diff --exit-code` over EN sources clean; `node scripts/i18n-translate.mjs --check` exits 0 (completeness).
- i18n/QA-REPORT.md committed with no unresolved failures.
- Owner native-RU spot-check recorded; PR merged; post-merge locale smoke (ru translated, en unchanged, ar English).
</verification>

<success_criteria>
- Complete ru/es/fr catalogs + content generated and merged (TR-02 for ru/es/fr).
- EN root byte-for-byte unchanged; ar/hi/zh still render English (Phase 73 scope).
- QA sampling pass recorded (D-09) with owner RU spot-check.
</success_criteria>

## Artifacts this plan produces

- Files (CI-generated, committed via PR): `messages/{ru,es,fr}.json` (complete real translations), `content/routes/{ru,es,fr}/*.json`, `content/pages/{ru,es,fr}/**/*.json`, `content/blog/{ru,es,fr}/*.mdx`, `i18n/translation-manifest.json` (populated), `i18n/QA-REPORT.md` (recorded QA pass)
- Unchanged (scope guard): `messages/{ar,hi,zh}.json`, all `content/**/en/**`, `messages/en.json`
- Pipeline flag exercised: `node scripts/i18n-translate.mjs --check` (completeness + EN-unchanged, no API calls)

<output>
Create `.planning/phases/72-ai-translation-pipeline-catalogs/72-05-SUMMARY.md` when done.
</output>
