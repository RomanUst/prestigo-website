---
phase: 72-ai-translation-pipeline-catalogs
plan: 03
type: execute
wave: 3
depends_on: [72-02]
files_modified:
  - scripts/i18n-translate.mjs
  - scripts/lib/i18n-mdx.mjs
  - scripts/lib/i18n-qa-report.mjs
  - tests/i18n-translate-mdx.test.ts
  - tests/i18n-completeness.test.ts
  - content/blog/en/prague-christmas-markets-chauffeur-2026.mdx
autonomous: true
requirements: [TR-01, TR-02]

estimate:
  tokens: 90000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Blog MDX is translated MDX-aware (D-10): frontmatter title/description are translated as short strings; date, dateModified, coverImage, category, author are copied byte-for-byte from the EN frontmatter; the markdown body is translated whole-document."
    - "MDX body structure is preserved: fenced-code-block count, heading count, and link count all match EN, and every EN link URL is present unchanged in the translation — a failed check flags the file for the QA report rather than shipping a structurally-broken MDX file (Pattern 4)."
    - "The stray content/blog/prague-christmas-markets-chauffeur-2026.mdx is relocated under content/blog/en/ so lib/blog.ts's getMDXPosts() and the pipeline both see it (Open Question 3)."
    - "The QA report records, per locale, key-completeness vs en.json, no-English-leakage on processed units, DNT-terms-preserved, and MDX structural-invariant results — plus a sampled diff for the owner's manual spot-check (D-09)."
    - "Generated locale files are structurally identical to their EN source (same keys, same nesting, same order), which is exactly what the completeness diff asserts (TR-02)."
  artifacts:
    - "scripts/lib/i18n-mdx.mjs — gray-matter frontmatter round-trip + whole-body translate + verifyMdxStructure()"
    - "scripts/lib/i18n-qa-report.mjs — checkCompleteness() + generateQaReport() writing i18n/QA-REPORT.md"
    - "tests/i18n-translate-mdx.test.ts, tests/i18n-completeness.test.ts"
    - "content/blog/en/prague-christmas-markets-chauffeur-2026.mdx (relocated)"
  key_links:
    - "scripts/i18n-translate.mjs -> scripts/lib/i18n-mdx.mjs (blog branch) reuses scripts/lib/i18n-verify.mjs for frontmatter DNT checks"
    - "scripts/lib/i18n-mdx.mjs uses gray-matter exactly as lib/blog.ts does (same parser, required-field set from lib/blog.ts:53)"
    - "scripts/lib/i18n-qa-report.mjs reads the manifest + generated outputs to compute completeness and no-leakage per locale (D-09)"
  prohibitions:
    - "Never translate or reorder MDX frontmatter keys, code fences, link URLs, heading markers, or JSX/HTML tags — translate only the visible prose and link text."
---

<objective>
Complete the unified pipeline's third surface — MDX-aware blog translation (D-10) — and build the QA-report generator that is the phase's recorded QA sampling pass (D-09). Relocate the stray blog file so it is visible to both the EN blog listing and the pipeline (Open Question 3).

Purpose: one pipeline handles catalogs + route/page JSON + blog MDX; the QA report is the artifact the owner reviews in the PR alongside a native-RU spot-check (D-09), and it is the machine backstop for no-EN-leakage + completeness.
Output: `scripts/lib/i18n-mdx.mjs`, `scripts/lib/i18n-qa-report.mjs`, MDX + completeness unit tests, and the relocated blog file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-ai-translation-pipeline-catalogs/72-RESEARCH.md
@.planning/phases/72-ai-translation-pipeline-catalogs/72-PATTERNS.md
@.planning/phases/72-ai-translation-pipeline-catalogs/72-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate the stray blog file into content/blog/en/</name>
  <files>content/blog/en/prague-christmas-markets-chauffeur-2026.mdx</files>
  <read_first>.planning/phases/72-ai-translation-pipeline-catalogs/72-RESEARCH.md (Open Question 3), lib/blog.ts (getMDXPosts / contentDirFor — reads only content/blog/<locale>/)</read_first>
  <action>Run `git mv content/blog/prague-christmas-markets-chauffeur-2026.mdx content/blog/en/prague-christmas-markets-chauffeur-2026.mdx`. This is a mechanical move that must land BEFORE the first live translation run (72-05) — while at content/blog/ root the file is invisible to lib/blog.ts's getMDXPosts() (which reads only content/blog/<locale>/) and thus to the pipeline. Do not edit the file contents.</action>
  <precondition>content/blog/prague-christmas-markets-chauffeur-2026.mdx exists at the content/blog/ root (git status confirms it as untracked/tracked at that path).</precondition>
  <verify>
    <automated>test -f content/blog/en/prague-christmas-markets-chauffeur-2026.mdx && test ! -f content/blog/prague-christmas-markets-chauffeur-2026.mdx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `content/blog/en/prague-christmas-markets-chauffeur-2026.mdx` exists; the content/blog/ root copy no longer exists.
    - The file body is unchanged (git records a rename, not a content edit).
  </acceptance_criteria>
  <done>The stray post lives under content/blog/en/ and is visible to the blog aggregator and pipeline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MDX-aware blog translation branch (D-10) + structural verification</name>
  <files>scripts/i18n-translate.mjs, scripts/lib/i18n-mdx.mjs, tests/i18n-translate-mdx.test.ts</files>
  <read_first>.planning/phases/72-ai-translation-pipeline-catalogs/72-RESEARCH.md (Pattern 4 + Code Examples MDX round-trip), .planning/phases/72-ai-translation-pipeline-catalogs/72-PATTERNS.md (content/blog write-target), lib/blog.ts (gray-matter usage + required-field list line 53), content/blog/en/luxury-chauffeur-service-prague-vip.mdx, scripts/lib/i18n-verify.mjs</read_first>
  <behavior>
    - gray-matter parse+stringify round-trip preserves date, dateModified, coverImage, category, author byte-for-byte; only title/description change.
    - verifyMdxStructure returns fencesMatch/linkCountMatch/urlsPreserved/headingCountMatch true for a faithful translation.
    - verifyMdxStructure returns a failing flag when a translation drops a link URL or a code fence.
    - The body unitKey is a single `<path>.mdx::body` unit; frontmatter fields are `<path>.mdx::frontmatter.title` / `.description` units (manifest-compatible with 72-01).
  </behavior>
  <action>Create `scripts/lib/i18n-mdx.mjs` exporting a translateMdxFile branch: parse with gray-matter (same import as lib/blog.ts), translate data.title + data.description via the short-string path, translate `content` (body) whole-document with explicit preservation instructions (preserve fenced code blocks, markdown link syntax — translate visible text, never the URL — heading markers, list markers, JSX/HTML tags), use streaming for the body call (long output). Rebuild with matter.stringify({...data, title, description}, translatedBody). Export verifyMdxStructure(enBody, trBody) per RESEARCH Pattern 4 (fence count, link count, URL preservation, heading count). Wire the blog branch into scripts/i18n-translate.mjs so content/blog/en/*.mdx (11 files after Task 1) produce content/blog/{ru,es,fr}/*.mdx, manifest-gated. A failed verifyMdxStructure marks the file for the QA report (Task 3), not a silent write. Author tests/i18n-translate-mdx.test.ts covering the behaviors with a fixture MDX (mock translator).</action>
  <verify>
    <automated>npx vitest run tests/i18n-translate-mdx.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/i18n-translate-mdx.test.ts` passes: frontmatter non-prose fields identical, body translated, structure verified.
    - A fixture translation that removes a link URL is caught by verifyMdxStructure (urlsPreserved false).
    - A dry-run over a fixture MDX produces content/blog/<locale>/<slug>.mdx with the same frontmatter keys in the same order.
  </acceptance_criteria>
  <done>Blog MDX translates frontmatter prose + body while preserving all structure; broken structure is flagged, not shipped.</done>
</task>

<task type="auto">
  <name>Task 3: QA-report generator (completeness, no-EN-leakage, DNT-preserved, sampled diff) — D-09</name>
  <files>scripts/i18n-translate.mjs, scripts/lib/i18n-qa-report.mjs, tests/i18n-completeness.test.ts</files>
  <read_first>.planning/phases/72-ai-translation-pipeline-catalogs/72-RESEARCH.md (D-09 spec + Validation Architecture TR-02 test map), .planning/phases/72-ai-translation-pipeline-catalogs/72-PATTERNS.md (QA-report artifact), messages/en.json, scripts/lib/i18n-verify.mjs</read_first>
  <action>Create `scripts/lib/i18n-qa-report.mjs` exporting checkCompleteness(enFlat, localeFlat) (returns missing[] + extra[] keys — the leaf-key-set diff) and generateQaReport({locales, results, manifest}) that writes `i18n/QA-REPORT.md`: per-locale key-completeness vs en.json (0 missing target), a no-English-leakage check on units the run processed (a translated string byte-equal to its EN source on a non-DNT unit is flagged), DNT-terms-preserved summary (from i18n-verify results), MDX structural-invariant results, and a sampled per-locale diff section for the owner's manual RU spot-check. Wire generateQaReport as the final step of scripts/i18n-translate.mjs after all surfaces run. Also add a `--check` mode to scripts/i18n-translate.mjs that makes NO API calls: it runs checkCompleteness for ru/es/fr against messages/en.json and asserts EN is unchanged (git diff over EN sources), exiting non-zero on any missing key or EN mutation (used by 72-05's automated verify and by CI). Author tests/i18n-completeness.test.ts exercising checkCompleteness against fixtures (a locale missing a key is reported; a complete locale reports 0 missing). Keep the leakage heuristic scoped to processed non-DNT units so brand/DNT terms that are legitimately identical across locales are not false-flagged.</action>
  <verify>
    <automated>npx vitest run tests/i18n-completeness.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/i18n-completeness.test.ts` passes: a fixture locale missing a key yields that key in `missing[]`; a complete fixture yields `missing: []`.
    - A `--dry-run --fixtures` run writes `i18n/QA-REPORT.md` containing a per-locale completeness section and a no-leakage section.
    - `node scripts/i18n-translate.mjs --check` exits 0 on a complete fixture corpus and non-zero when a locale is missing a key or EN was mutated (no API calls made).
    - The no-leakage check does not flag a DNT-only unit whose value is identical across locales (asserted in the test).
  </acceptance_criteria>
  <done>The pipeline emits i18n/QA-REPORT.md with completeness, no-leakage, DNT, and MDX-structure results plus a sampled diff — the recorded QA sampling pass (D-09).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EN MDX body -> LLM prompt | owner-authored prose + embedded structure flows into the model |
| QA report artifact -> PR / owner | a generated markdown artifact is committed and read by a human |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-07 | Tampering | MDX body translation drops/reorders code fences, links, or JSX | medium | mitigate | verifyMdxStructure post-hoc check flags the file for QA rather than shipping a broken MDX (Task 2) |
| T-72-05 | Information Disclosure | secret/env value leaking into i18n/QA-REPORT.md | medium | mitigate | QA report is built only from EN sources, translated outputs, and manifest data — never from process.env; no request headers or client config are serialized into the report (Task 3) |
</threat_model>

<verification>
- `npx vitest run tests/i18n-translate-mdx.test.ts tests/i18n-completeness.test.ts` green.
- Stray blog file relocated (test -f checks).
- A dry-run emits i18n/QA-REPORT.md.
</verification>

<success_criteria>
- Unified pipeline handles catalogs + route/page JSON + blog MDX (D-10, one pipeline).
- QA-report generator produces the D-09 recorded QA sampling pass artifact.
- Stray blog file visible to the EN listing and the pipeline.
</success_criteria>

## Artifacts this plan produces

- Files: `scripts/lib/i18n-mdx.mjs`, `scripts/lib/i18n-qa-report.mjs`, `tests/i18n-translate-mdx.test.ts`, `tests/i18n-completeness.test.ts`, `i18n/QA-REPORT.md` (generated on run)
- Modified: `scripts/i18n-translate.mjs` (blog branch + final QA step), `content/blog/en/prague-christmas-markets-chauffeur-2026.mdx` (relocated from content/blog/ root)
- Exports (i18n-mdx.mjs): `translateMdxFile()`, `verifyMdxStructure(enBody, trBody)`
- Exports (i18n-qa-report.mjs): `checkCompleteness(enFlat, localeFlat)`, `generateQaReport(opts)`

<output>
Create `.planning/phases/72-ai-translation-pipeline-catalogs/72-03-SUMMARY.md` when done.
</output>
