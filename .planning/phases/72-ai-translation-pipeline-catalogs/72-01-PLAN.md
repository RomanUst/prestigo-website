---
phase: 72-ai-translation-pipeline-catalogs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - i18n/glossary.json
  - scripts/lib/i18n-glossary.mjs
  - scripts/lib/i18n-manifest.mjs
  - scripts/i18n-translate.mjs
  - i18n/translation-manifest.json
  - tests/i18n-translate-manifest.test.ts
  - tests/fixtures/i18n/messages/en.json
  - tests/fixtures/i18n/messages/ru.json
autonomous: false
requirements: [TR-01]
user_setup:
  - service: anthropic
    why: "Local dry-runs of the pipeline read ANTHROPIC_API_KEY from .env.local (CI uses the repo secret, wired in 72-04). The tracer's automated verify uses a MOCK client and needs no key; only a real (optional) local smoke test needs one."
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> Settings -> API keys (optional for local smoke test only; the committed tests mock the SDK)"

estimate:
  tokens: 78000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Running the pipeline twice against an unchanged EN source produces zero output diff on the second run (idempotency, D-04/D-05, TR-01 SC#1)."
    - "A unit whose EN sha256 matches its manifest entry is skipped — no API call, existing target value untouched (D-05)."
    - "A unit absent from the manifest, or whose EN hash differs, is selected for translation (D-04) — this is what makes the first run and the drifted-catalog backfill self-healing."
    - "Empty-string and whitespace-only EN units are handled without a wasted API call; a structurally-empty container (empty array/object) is not fabricated into content."
    - "Hashing and file I/O use UTF-8 consistently and target files are written NFC-normalized, so Cyrillic output does not re-translate on the next run purely due to encoding/normalization drift."
    - "Catalog output is written with the EN source's key order preserved, so an unchanged re-run yields a byte-identical file and a clean git diff (deterministic ordering)."
    - "Two units that happen to share an identical EN value are tracked independently by their distinct unitKeys; one being unchanged never merges with or overwrites the other."
    - "A malformed i18n/glossary.json fails the run loudly at startup (zod validation) rather than silently translating with missing DNT rules."
  artifacts:
    - "i18n/glossary.json — owner-editable DNT + per-locale tone/term data (D-06/D-07)"
    - "scripts/lib/i18n-glossary.mjs — zod glossary schema + loadGlossary() + buildSystemPrompt(glossary, locale)"
    - "scripts/lib/i18n-manifest.mjs — sha256(), flattenEnSource(), unitsNeedingTranslation(), loadManifest(), writeManifest()"
    - "scripts/i18n-translate.mjs — CLI entry wiring one key end-to-end for one locale, injectable translator client, --dry-run/--fixtures flags"
    - "i18n/translation-manifest.json — flat unitKey -> {enHash,lastTranslatedAt} sidecar (D-04)"
    - "tests/i18n-translate-manifest.test.ts — idempotency + skip/select unit tests with a mocked client"
  key_links:
    - "scripts/i18n-translate.mjs imports locales from i18n/routing.ts (filtered to ru/es/fr) — never a hardcoded duplicate list"
    - "scripts/i18n-translate.mjs -> scripts/lib/i18n-manifest.mjs (change detection) -> Anthropic client (injected) -> messages/ru.json write -> i18n/translation-manifest.json update"
    - "buildSystemPrompt() reads glossary DNT/tone so the single Opus call is glossary-governed from the tracer onward"
  prohibitions:
    - "Never mutate any EN source or EN output file (messages/en.json, content/**/en/**) — EN stays byte-for-byte unchanged."
    - "Never overwrite a target value whose EN source hash is unchanged — a hand-edited ru.json string survives a re-run (D-05)."
---

<objective>
Prove the whole translation architecture on the thinnest possible end-to-end slice: translate ONE catalog key for ONE locale (ru), driven by the real glossary and the hash manifest, then prove a second run is idempotent. Establish the three foundation modules (glossary, manifest, entry) and the owner-editable glossary data file (D-06/D-07) that every later surface reuses.

Purpose: catch an architectural dead-end (manifest contract D-04, glossary-governed prompt D-06/D-07, idempotency D-05) after one committed slice instead of after all surfaces are built. The manifest format becomes the contract Phase 73 (AR/HI/ZH) extends — get it right first.
Output: `i18n/glossary.json`, `scripts/lib/i18n-glossary.mjs`, `scripts/lib/i18n-manifest.mjs`, `scripts/i18n-translate.mjs` (one-key/one-locale tracer), `i18n/translation-manifest.json`, and a mocked-client idempotency test. `@anthropic-ai/sdk` installed and pinned.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-ai-translation-pipeline-catalogs/72-RESEARCH.md
@.planning/phases/72-ai-translation-pipeline-catalogs/72-PATTERNS.md
@.planning/phases/72-ai-translation-pipeline-catalogs/72-COVERAGE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve + install @anthropic-ai/sdk (package-legitimacy gate, T-72-SC)</name>
  <read_first>.planning/phases/72-ai-translation-pipeline-catalogs/72-RESEARCH.md (Package Legitimacy Audit + Standard Stack)</read_first>
  <what-built>The pipeline requires `@anthropic-ai/sdk` — the first time this package enters the repo. The legitimacy tool returned [SUS] (reason: `too-new` on the latest patch — the false-positive pattern matching the Phase 68 next-intl precedent). No install has happened yet; per the package-legitimacy gate a [SUS] verdict requires this blocking-human approval BEFORE install (never auto-approvable, ignores workflow.auto_advance).</what-built>
  <how-to-verify>
    1. Open npmjs.com/package/@anthropic-ai/sdk and confirm: source repo github.com/anthropics/anthropic-sdk-typescript, official `anthropics` org, ~29.7M weekly downloads.
    2. Confirm the [SUS] signal is the too-new-on-latest-patch false positive (same disposition as next-intl at Phase 68), not a genuine slop/typosquat signal.
    3. On approval, the orchestrator installs pinned to an EXACT version (no caret range), target `0.125.0` — `npm install @anthropic-ai/sdk@0.125.0` (npm install runs in the orchestrator, not inside a subagent, per project mechanics).
  </how-to-verify>
  <acceptance_criteria>
    - Owner explicitly approved the package at the checkpoint (recorded in the plan SUMMARY).
    - After install, `package.json` lists `@anthropic-ai/sdk` at an exact version (no `^`/`~`).
    - `node -e "require('@anthropic-ai/sdk')"` resolves without error (also proven downstream: Task 3's tracer test imports the SDK).
  </acceptance_criteria>
  <resume-signal>Type "approved" to authorize the exact-pinned install, or "reject" with a reason.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author i18n/glossary.json + glossary schema/prompt module (D-06, D-07)</name>
  <files>i18n/glossary.json, scripts/lib/i18n-glossary.mjs</files>
  <read_first>.planning/phases/72-ai-translation-pipeline-catalogs/72-RESEARCH.md (Architecture Patterns — Pattern 2), messages/en.json (for the exact ICU/tag tokens to encode), i18n/routing.ts (locale source of truth)</read_first>
  <action>Create `i18n/glossary.json` exactly per RESEARCH Pattern 2: `doNotTranslate.brand` (Prestigo, PRESTIGO, chelautotrans s.r.o.), `doNotTranslate.vehicleClasses` (Mercedes-Benz, E-Class, S-Class, V-Class), `doNotTranslate.structural` (icuVariablePattern, richTextTags = highlight/price/privacy/strong/terms/wa, pricePlaceholderPattern, phoneNumbers = "+420 725 986 855", domains = rideprestigo.com), a `placeNames` block with policy `use-standard-exonym` (Assumption A1 — cities get target-language exonyms, NOT frozen English; owner can flip since it is data), and `locales.{ru,es,fr}` each with formality, toneGuide, pluralCategories, termMap. Encode D-07 verbatim: ru formality formal («Вы»); es dialect es-ES `usted` European lexis; fr `vous`. ru pluralCategories = one/few/many/other; es/fr = one/other. Create `scripts/lib/i18n-glossary.mjs` exporting a `zod` schema (zod is already installed) that validates this shape, `loadGlossary()` that reads+validates+throws-loud on malformed (V5 fail-closed), and `buildSystemPrompt(glossary, locale)` that renders the DNT rules + per-locale tone into a system-prompt string. Do NOT hardcode the locale list here — import `locales` from `i18n/routing.ts` and filter to ru/es/fr.</action>
  <verify>
    <automated>npx vitest run tests/i18n-translate-manifest.test.ts -t "glossary"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "import('./scripts/lib/i18n-glossary.mjs').then(m=>m.loadGlossary()).then(g=>{if(!g.locales.ru||g.locales.ru.formality!=='formal')process.exit(1);if(!g.locales.es.dialect)process.exit(1)})"` exits 0.
    - Corrupting `i18n/glossary.json` (invalid JSON or missing `doNotTranslate`) makes `loadGlossary()` throw a descriptive error (fail-loud), asserted in the test.
    - `buildSystemPrompt(glossary,'ru')` output contains the DNT brand terms and the ru tone directive; `buildSystemPrompt(glossary,'ru')` declares the four plural categories for ru.
  </acceptance_criteria>
  <done>Glossary data file + validated loader + per-locale system-prompt builder exist and are schema-checked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TRACER — one key, one locale, end-to-end (read EN -> manifest -> glossary-governed Opus call -> write ru -> manifest update -> idempotent re-run)</name>
  <files>scripts/lib/i18n-manifest.mjs, scripts/i18n-translate.mjs, i18n/translation-manifest.json, tests/i18n-translate-manifest.test.ts, tests/fixtures/i18n/messages/en.json, tests/fixtures/i18n/messages/ru.json</files>
  <read_first>.planning/phases/72-ai-translation-pipeline-catalogs/72-RESEARCH.md (Pattern 1 + Code Examples), .planning/phases/72-ai-translation-pipeline-catalogs/72-PATTERNS.md (scripts/indexnow-submit.mjs + get-business-location.mjs analogs), scripts/indexnow-submit.mjs, scripts/get-business-location.mjs, i18n/routing.ts, messages/en.json</read_first>
  <behavior>
    - unitsNeedingTranslation selects a unit whose unitKey is absent from the manifest.
    - unitsNeedingTranslation selects a unit whose EN sha256 differs from the recorded enHash.
    - unitsNeedingTranslation SKIPS a unit whose EN sha256 equals the recorded enHash (D-05).
    - A full run against the fixture EN corpus, then a second run with the EN unchanged, yields an identical target file and identical manifest (zero diff) — idempotency.
    - Empty-string / whitespace-only EN unit: no translator call is issued for it.
    - The injected mock translator is called only for selected units; the manifest gains an entry only for units whose write succeeded.
  </behavior>
  <action>Create `scripts/lib/i18n-manifest.mjs` exporting `sha256(value)` (node:crypto, prefix "sha256:"), `flattenEnSource(obj, sourcePath)` yielding {unitKey: "<relative-path>::<dot-path>", value} pairs, `unitsNeedingTranslation(enUnits, manifest)` (RESEARCH Code Examples verbatim: select when absent OR enHash differs), `loadManifest(path)` (init to {version:1,units:{}} when absent) and `writeManifest()`. Create `scripts/i18n-translate.mjs` following the indexnow-submit.mjs shape (doc-comment header stating the CI trigger, async main(), top-level .catch()+process.exit(1), the checkmark/cross console markers) and the get-business-location.mjs .env.local manual-parse + fail-loud ANTHROPIC_API_KEY guard (no dotenv dependency). The entry must accept an INJECTED translator function (default = a real `@anthropic-ai/sdk` client calling claude-opus-5 with the glossary system block + ephemeral cache_control, per 72-COVERAGE) so tests substitute a deterministic mock; support `--dry-run --fixtures <dir>` to run the whole mechanism against tests/fixtures/i18n with the mock. For the tracer, wire exactly ONE catalog key (e.g. a single Nav leaf) for locale ru: flatten messages/en.json -> manifest check -> buildSystemPrompt -> translator call -> write that one key into messages/ru.json preserving EN key order and UTF-8/NFC -> update i18n/translation-manifest.json. Write the manifest entry ONLY after the target write succeeds. Create tests/fixtures/i18n/{messages/en.json,messages/ru.json} (tiny corpus) and tests/i18n-translate-manifest.test.ts covering the behaviors above with the mock translator (never the real API).</action>
  <verify>
    <automated>npx vitest run tests/i18n-translate-manifest.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/i18n-translate-manifest.test.ts` passes, including an assertion that a second dry-run over the fixture corpus produces a byte-identical target file (idempotency).
    - A test asserts a unit with a matching manifest hash triggers zero mock-translator calls (D-05 skip).
    - After a dry-run, `git diff --exit-code -- tests/fixtures/i18n/messages/en.json` is clean (EN fixture untouched by the run).
    - The manifest entry count equals the number of selected+written units, never the total unit count on an unchanged re-run.
  </acceptance_criteria>
  <reversibility rating="costly">The flat `unitKey -> {enHash,lastTranslatedAt}` manifest format (D-04) becomes the contract every re-run and the Phase 73 AR/HI/ZH extension depend on; changing it later forces a manifest migration. Rationale for costly (not one-way): a migration script can rewrite the sidecar, so it is recoverable, not irreversible.</reversibility>
  <done>One key translates end-to-end for ru through the real glossary + manifest; a second unchanged run is idempotent; all proven by a mocked-client test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> repo | a new third-party dependency (`@anthropic-ai/sdk`) crosses into the build |
| glossary.json (owner-edited data) -> pipeline | untrusted-shape config drives the run |
| EN source content -> LLM prompt | owner-authored source text flows into the model prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-SC | Tampering | `npm install @anthropic-ai/sdk` | high | mitigate | Blocking-human package-legitimacy checkpoint (Task 1) + exact-version pin; never auto-approvable |
| T-72-04 | Denial of Service (CI) | malformed `i18n/glossary.json` | medium | mitigate | zod validation at startup fails the run loudly (Task 2); DNT regexes are simple bounded token matches, no open-ended backtracking |
| T-72-02 | Tampering | prompt injection via EN source string | low | accept | EN source is owner-authored, not third-party input; post-hoc DNT/structural verification (72-02/72-03) is the defense-in-depth backstop regardless |
</threat_model>

<verification>
- `npx vitest run tests/i18n-translate-manifest.test.ts` green (manifest select/skip + idempotency + glossary validation).
- A `--dry-run --fixtures tests/fixtures/i18n` run executes twice with a byte-identical second-run output.
- `@anthropic-ai/sdk` importable and exact-pinned.
</verification>

<success_criteria>
- The end-to-end path (EN read -> manifest change-detect -> glossary-governed translate -> target write -> manifest update -> idempotent re-run) works for one key/one locale and is committed.
- Foundation modules exported for reuse by later surface plans; glossary encodes D-06/D-07 verbatim.
</success_criteria>

## Artifacts this plan produces

- Files: `i18n/glossary.json`, `scripts/lib/i18n-glossary.mjs`, `scripts/lib/i18n-manifest.mjs`, `scripts/i18n-translate.mjs`, `i18n/translation-manifest.json`, `tests/i18n-translate-manifest.test.ts`, `tests/fixtures/i18n/messages/en.json`, `tests/fixtures/i18n/messages/ru.json`
- Modified: `package.json`, `package-lock.json` (add `@anthropic-ai/sdk`)
- Exports (scripts/lib/i18n-glossary.mjs): `glossarySchema`, `loadGlossary()`, `buildSystemPrompt(glossary, locale)`
- Exports (scripts/lib/i18n-manifest.mjs): `sha256(value)`, `flattenEnSource(obj, sourcePath)`, `unitsNeedingTranslation(enUnits, manifest)`, `loadManifest(path)`, `writeManifest(path, manifest)`
- CLI flags (scripts/i18n-translate.mjs): `--dry-run`, `--fixtures <dir>`, `--locales <list>` (default ru,es,fr from i18n/routing.ts)
- Config keys (i18n/glossary.json): `doNotTranslate.brand`, `doNotTranslate.vehicleClasses`, `doNotTranslate.structural.{icuVariablePattern,richTextTags,pricePlaceholderPattern,phoneNumbers,domains}`, `placeNames.policy`, `locales.{ru,es,fr}.{formality,dialect,toneGuide,pluralCategories,termMap}`

<output>
Create `.planning/phases/72-ai-translation-pipeline-catalogs/72-01-SUMMARY.md` when done.
</output>
