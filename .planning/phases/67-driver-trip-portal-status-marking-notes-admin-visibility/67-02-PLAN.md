---
phase: 67-driver-trip-portal-status-marking-notes-admin-visibility
plan: 02
type: execute
wave: 2
depends_on: [67-01]
files_modified:
  - app/driver/trip/[token]/TripProgressClient.tsx
  - app/driver/trip/[token]/page.tsx
  - components/admin/DriverAssignmentSection.tsx
  - tests/driver-trip-progress.test.ts
  - tests/DriverAssignmentSection.test.tsx
autonomous: true
requirements: [DTRIP-05, DTRIP-06]
estimate:
  tokens: 34000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    # DTRIP-06 — driver optional note
    - "A driver types free text in a note field on /driver/trip/[token] and submits it; the text persists to driver_assignments.trip_note for that assignment and trip_progress_updated_at is bumped (DTRIP-06, D-06)"
    - "The note field is fully optional and independent of status marking: a POST carrying only { note } succeeds without setting trip_progress, and a POST carrying neither progress nor note is rejected 400 by the schema .refine (DTRIP-06)"
    # DTRIP-05 — admin sees the note + freshness
    - "Expanding a booking row with an assigned driver in admin shows the driver's trip_note text (as auto-escaped JSX, wrapping, no truncation) and a 'last updated' timestamp from trip_progress_updated_at, alongside the trip-progress badge from Plan 67-01 (DTRIP-05, D-05)"
  artifacts:
    - "TripProgressClient.tsx note <textarea> + note submit handler (extends the Plan 67-01 island in place)"
    - "DriverAssignmentSection.tsx trip_note + trip_progress_updated_at render (extends the Plan 67-01 assigned branch)"
  key_links:
    - "TripProgressClient note submit → POST /api/driver/trip/[token]/progress { note } → same isTripLinkValid re-check → driver_assignments.update({ trip_note, trip_progress_updated_at })"
    - "DriverAssignmentSection reads assignment.trip_note / assignment.trip_progress_updated_at already carried by the Plan 67-01 GET select — no route change needed"
  prohibitions:
    # Isolation reaffirmed — the note path shares the Plan 67-01 route and must stay isolated
    - "The note write path uses the SAME app/api/driver/trip/[token]/progress route as status — it is not permitted to add any import of the GNet client or status-transitions module, nor any update against the bookings table (the Plan 67-01 grep gates must still return 0 after this plan)"
    - "trip_note is rendered as React JSX text only (auto-escaped) — never via dangerouslySetInnerHTML"
---

<objective>
Expand the Plan 67-01 tracer along ONE new axis: the optional driver note (DTRIP-06)
and its admin visibility (DTRIP-05). The write route already accepts `note` (its zod
schema was built with `progress?` + `note?` + the at-least-one `.refine` in the
tracer), so this plan adds the note UI on the client island, renders the note +
freshness timestamp in the admin detail view, and adds the note-path test cases. No
route, migration, middleware, or rate-limit change — the seam is already in place.

Purpose: DTRIP-06 is the last requirement; keeping it a thin expansion on top of the
proven tracer means the isolation guarantee (DTRIP-04) is inherited, not re-proven.
Output: note capture end-to-end + admin note/timestamp visibility.
</objective>

<artifacts_this_phase_produces>
This plan adds only UI + tests on top of Plan 67-01's route/schema/columns:

| Symbol / artifact | Kind | Notes |
|---|---|---|
| `TripProgressClient` note `<textarea>` + `handleSubmitNote` | UI | posts `{ note }` to the existing route |
| `trip_note` + `trip_progress_updated_at` render block in `DriverAssignmentSection` (assigned branch) | UI | reads fields already in the GET select from 67-01 |
| note-path cases in `tests/driver-trip-progress.test.ts` | tests | note-only success, neither-field 400 |
| note-render case in `tests/DriverAssignmentSection.test.tsx` | tests | note text + timestamp appear |

No new columns, routes, schemas, rate-limit keys, or CSRF prefixes.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-driver-trip-portal-status-marking-notes-admin-visibility/67-RESEARCH.md
@.planning/phases/67-driver-trip-portal-status-marking-notes-admin-visibility/67-CONTEXT.md
@.planning/phases/67-driver-trip-portal-status-marking-notes-admin-visibility/67-01-SUMMARY.md

# Files this plan extends (built/extended in Plan 67-01):
@app/driver/trip/[token]/TripProgressClient.tsx
@app/api/driver/trip/[token]/progress/route.ts
@components/admin/DriverAssignmentSection.tsx
@app/driver/response/DriverResponseClient.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Driver note field (DTRIP-06) — TripProgressClient textarea + submit, note-path tests</name>
  <precondition>app/api/driver/trip/[token]/progress/route.ts accepts an optional note (progressSchema has note?: z.string().max(2000)) — built in Plan 67-01</precondition>
  <files>app/driver/trip/[token]/TripProgressClient.tsx, app/driver/trip/[token]/page.tsx, tests/driver-trip-progress.test.ts</files>
  <behavior>
    Extend tests/driver-trip-progress.test.ts:
    - POST { note: "Passenger running late" } (no progress) for a valid token → 200 { ok: true },
      and the driver_assignments update payload contains trip_note but NOT trip_progress.
    - POST {} (neither progress nor note) → 400 (the .refine rejects it).
    - A note-only write still leaves bookings untouched (isolation assertion reused).
  </behavior>
  <read_first>
    - app/driver/trip/[token]/TripProgressClient.tsx (the Plan 67-01 island to extend — reuse its SubmitState + fetch shape)
    - app/driver/response/DriverResponseClient.tsx (inline dark-theme styling conventions, error-line pattern)
    - app/api/driver/trip/[token]/progress/route.ts (confirm note is already accepted and the field-by-field non-clobber update — no route change needed)
    - tests/driver-trip-progress.test.ts (the suite to extend)
  </read_first>
  <action>
    In app/driver/trip/[token]/TripProgressClient.tsx add a labeled multiline note field: a
    controlled &lt;textarea&gt; (state seeded from a new optional initialNote prop, maxLength 2000),
    styled to match the trip-sheet dark theme (var(--anthracite) background, var(--anthracite-light)
    border, var(--offwhite) text — mirror the failure-fallback input styling in
    DriverAssignmentSection), plus a Submit button. On submit, POST { note } (the current textarea
    value) to /api/driver/trip/${token}/progress reusing the existing fetch/SubmitState machinery;
    on json.ok show a brief confirmation, on failure show the same error line as the status path.
    Keep status and note as two independent submit paths against the same endpoint (status buttons
    post { progress }, the note posts { note }). Update the page.tsx mount to pass initialNote from
    the assignment (extend the page select to include trip_note and TripSheetAssignmentRow to carry
    it) so a returning driver sees a previously-saved note.
  </action>
  <verify>
    <automated>npx vitest run tests/driver-trip-progress.test.ts 2>&amp;1 | tail -5 &amp;&amp; grep -Ec "gnet-client|VALID_TRANSITIONS|booking-transitions" "app/api/driver/trip/[token]/progress/route.ts" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/driver-trip-progress.test.ts` green including: note-only POST → 200 with trip_note in the update payload and no trip_progress; empty POST → 400.
    - `TripProgressClient.tsx` renders a note textarea (maxLength 2000) + Submit that POSTs `{ note }` to the existing route; status buttons still POST `{ progress }` (both paths intact).
    - `page.tsx` select includes trip_note and passes initialNote to the island.
    - The Plan 67-01 isolation greps on the route file STILL return 0 (route unchanged; no coupling introduced).
    - `npx tsc --noEmit` reports zero NEW errors.
  </acceptance_criteria>
  <done>A driver submits an optional note from the trip sheet and it persists to trip_note without setting a status; empty submits are rejected; isolation is intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Admin note + freshness visibility (DTRIP-05) — DriverAssignmentSection render</name>
  <precondition>GET /api/admin/bookings/[id]/assignment already selects trip_note and trip_progress_updated_at (Plan 67-01 Task 5) and the Assignment interface already carries them</precondition>
  <files>components/admin/DriverAssignmentSection.tsx, tests/DriverAssignmentSection.test.tsx</files>
  <behavior>
    Extend tests/DriverAssignmentSection.test.tsx: when the assignment fetch returns trip_note:
    "Passenger running late" and trip_progress_updated_at set, the assigned-mode render shows the
    note text and a last-updated line; when trip_note is null, no note block renders.
  </behavior>
  <read_first>
    - components/admin/DriverAssignmentSection.tsx (the `mode === 'assigned'` branch extended in Plan 67-01 — Assignment interface already has trip_note/trip_progress_updated_at; add the render only)
    - app/driver/trip/[token]/page.tsx special_requests row (the wrapping/whitespace style precedent for long free text)
    - tests/DriverAssignmentSection.test.tsx (conventions to extend)
  </read_first>
  <action>
    In components/admin/DriverAssignmentSection.tsx `mode === 'assigned'` branch, below the driver
    name / trip-progress badge row: when assignment.trip_note is a non-empty string, render it as a
    labeled block ("Driver note") using normal JSX text interpolation only (so React auto-escapes) —
    never a raw-HTML injection sink — with whiteSpace normal / wordBreak break-word so long notes wrap
    and are never truncated. When assignment.trip_progress_updated_at is set, render a small muted
    "Updated {localized date-time}" line (format the ISO timestamp with toLocaleString). Both blocks
    render only when their field is present (omit whole block when null — no blank label). Reuse the
    existing sectionLabelStyle / var(--warmgrey) muted styling; do not alter the accept/decline,
    Copy Trip Link, Reassign, or trip-progress-badge markup from Plan 67-01.
  </action>
  <verify>
    <automated>npx vitest run tests/DriverAssignmentSection.test.tsx 2>&amp;1 | tail -5</automated>
    <human-check>On a mobile viewport, open a real /driver/trip/[token] for an active assignment: the five status buttons and the note field are legible and tappable (police-show context). Then in admin, expand that booking row and confirm the current trip-progress badge, the driver note text, and the "Updated" timestamp all appear beside the existing booking status. (Deferred to /gsd-verify-work per project convention — jsdom cannot exercise mobile ergonomics or the live dark-theme render.)</human-check>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/DriverAssignmentSection.test.tsx` green: non-null trip_note renders the note text + an "Updated" line; null trip_note renders no note block.
    - The note is rendered via JSX text only — `grep -c "dangerouslySetInnerHTML" components/admin/DriverAssignmentSection.tsx` returns 0.
    - The trip-progress badge, Copy Trip Link, Reassign, and accept/decline StatusBadge from Plan 67-01 are unchanged (their existing tests stay green).
    - Full suite `npx vitest run` green before the plan closes.
  </acceptance_criteria>
  <done>Admin, on booking-row expand, sees the driver's trip-progress badge, note text, and last-updated timestamp together — DTRIP-05 and DTRIP-06 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driver's mobile browser → `POST /api/driver/trip/[token]/progress` (note payload) | Same unauthenticated, token-gated write as Plan 67-01 — the note is a new input value on the existing surface |
| `trip_note` value → admin detail render | Untrusted driver free text crosses into the admin DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-67-08 | Denial of Service | oversized note payload | medium | mitigate | `z.string().max(2000)` (route, from Plan 67-01) + `enforceMaxBody(request, 10000)` already gate the body — no change needed, reaffirmed |
| T-67-09 | Tampering (XSS) | driver note rendered in admin | high | mitigate | rendered as React JSX text only (auto-escaped); `dangerouslySetInnerHTML` absent (grep = 0); note is never used in an email header, so no CRLF regex needed (matches special_requests precedent) |
| T-67-10 | Tampering / EoP | note write reaching `bookings`/GNet via a shared route | high | mitigate | note reuses the Plan 67-01 isolated route unchanged; the isolation grep gates on the route file still return 0 after this plan |
| T-67-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed this phase — nothing to vet |
</threat_model>

<verification>
- `npx vitest run` — full suite green (note-path route cases + admin note render + all Plan 67-01 and pre-existing tests).
- Isolation grep gates on `app/api/driver/trip/[token]/progress/route.ts` still return 0 (route not modified by this plan).
- `grep -c dangerouslySetInnerHTML components/admin/DriverAssignmentSection.tsx` returns 0.
- `<human-check>` (Task 2) harvested at `/gsd-verify-work`: mobile tap-target ergonomics + live admin badge/note/timestamp render.
</verification>

<success_criteria>
- DTRIP-06: a driver leaves an optional note from the trip sheet and it persists.
- DTRIP-05: admin sees the driver's note + trip-progress + last-updated freshness on booking-row expand.
- Isolation (DTRIP-04) remains intact — no new coupling introduced by the note path.
</success_criteria>

<output>
Create `.planning/phases/67-driver-trip-portal-status-marking-notes-admin-visibility/67-02-SUMMARY.md` when done.
</output>
