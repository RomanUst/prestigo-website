---
phase: 64-admin-created-bookings-with-payment-link
plan: 04
type: execute
wave: 4
depends_on: ["64-01", "64-02", "64-03"]
files_modified: []
autonomous: false
requirements: [ANEW-02, ANEW-04, ANEW-05]
user_setup:
  - service: stripe
    why: "The live webhook endpoint must forward checkout.session.completed for payment-link reconciliation (ANEW-04)"
    dashboard_config:
      - task: "Add checkout.session.completed to the endpoint's subscribed events"
        location: "Stripe Dashboard → Developers → Webhooks → (the /api/webhooks/stripe endpoint) → Events"

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Migration 056 is applied to the live Supabase project — the bookings table has physical payment_link_url and payment_link_id columns (verification-blocking; build/type checks pass without it because types come from generated config, creating a false-positive verification state)."
    - "The live Stripe webhook endpoint subscribes to checkout.session.completed — without it, a real link payment succeeds in Stripe but the booking never reconciles, silently and with no error in the codebase (RESEARCH Pitfall 4 / A2)."
    - statement: "An end-to-end live pay-link round-trip (operator creates booking → link → client pays via Stripe test/live → row flips unpaid→confirmed with no duplicate + confirmation side-effects) succeeds."
      verification: backstop
  prohibitions:
    - "The phase must never be marked verified while migration 056 is only a file and not applied to the live database."
  artifacts:
    - "Live DB: bookings.payment_link_url, bookings.payment_link_id columns present"
    - "Live Stripe: webhook subscribed to checkout.session.completed"
  key_links:
    - "migration 056 file (Plan 01) -> Supabase MCP apply_migration -> live columns -> webhook reconcile can persist payment_link_url/id + payment_intent_id"
---

<objective>
Close the two operational gaps that no code change can satisfy: apply migration 056 to the live Supabase project, and confirm the live Stripe webhook endpoint forwards `checkout.session.completed`. These are the verification-blocking prerequisites — unit tests are green without them (mocks), but the real feature does not work until both are done.

Purpose: Prevent the documented false-positive verification state (schema-gate contribution) and the silent-non-reconciliation trap (RESEARCH Pitfall 4).
Output: Applied migration + confirmed webhook subscription (no repo files changed).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-admin-created-bookings-with-payment-link/64-RESEARCH.md
@.planning/phases/64-admin-created-bookings-with-payment-link/64-VALIDATION.md
@.planning/phases/64-admin-created-bookings-with-payment-link/64-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Apply migration 056 to the live Supabase project + verify columns</name>
  <files>supabase/migrations/056_bookings_payment_link.sql</files>
  <read_first>
    - supabase/migrations/056_bookings_payment_link.sql (the file authored in Plan 01 — the SQL to apply)
    - .planning/phases/62-abandoned-unpaid-booking-capture/62-04-PLAN.md if present (the established "apply migration to live DB via Supabase MCP + verify" precedent for 053/054)
  </read_first>
  <action>
    Apply migration 056 to the live Supabase project using the Supabase MCP `apply_migration` tool (migration name `056_bookings_payment_link`, body = the contents of `supabase/migrations/056_bookings_payment_link.sql`). This matches how migrations 053/054 were applied in Phase 62 (this repo applies via the Supabase MCP, not `supabase db push` in CI). The migration is additive and idempotent (`ADD COLUMN IF NOT EXISTS`), so re-application is safe.

    Then verify the columns exist on the live `bookings` table — use the Supabase MCP `list_tables` (inspect the `bookings` columns) or run a probe `select payment_link_url, payment_link_id from bookings limit 0` via the MCP. Confirm both `payment_link_url` and `payment_link_id` are present and typed `text`, nullable.

    This task is BLOCKING: the phase cannot pass `/gsd-verify-work` without it. Build and `tsc` pass without the applied migration because types come from the generated config, not the live DB — do not treat green unit tests as evidence the schema is live.
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS" supabase/migrations/056_bookings_payment_link.sql | grep -qx 2 && echo "migration file intact — apply + column-existence confirmed via Supabase MCP list_tables/probe (record result in SUMMARY)"</automated>
  </verify>
  <acceptance_criteria>
    - The Supabase MCP `apply_migration` call for `056_bookings_payment_link` completes without error (record the tool response in the SUMMARY).
    - A Supabase MCP `list_tables` (or `select ... limit 0`) confirms `bookings.payment_link_url` and `bookings.payment_link_id` exist, type `text`, nullable — quote the confirmation in the SUMMARY.
    - The migration file still contains exactly two `ADD COLUMN IF NOT EXISTS` statements.
  </acceptance_criteria>
  <precondition>Supabase MCP is connected and authorized for the live Prestigo project (user_setup / prior-phase workflow). If the MCP is unavailable, halt and request the user apply the migration.</precondition>
  <done>Migration 056 applied to the live database; both columns confirmed present via the Supabase MCP.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm Stripe webhook subscribes to checkout.session.completed + live E2E smoke</name>
  <what-built>The `checkout.session.completed` webhook branch (Plan 01/02) and migration 056 (Task 1) are in place; the two remaining facts live outside the codebase — the Stripe Dashboard event subscription and a real link-payment reconciliation. RESEARCH Pitfall 4 / A2: .env.local's Stripe key is a dead placeholder, so the live smoke runs via a user-executed local script with the live/test key.</what-built>
  <how-to-verify>
    1. Stripe Dashboard → Developers → Webhooks → the `/api/webhooks/stripe` endpoint → Events: confirm `checkout.session.completed` is subscribed. If missing, add it (without it, real link payments succeed in Stripe but never reconcile — silent, no error).
    2. Run the end-to-end smoke with the live/test key: create an admin booking with "collect payment", open the emailed/copied link, complete a Stripe payment, then confirm in the admin bookings list that the SAME booking flipped `unpaid → confirmed`, no duplicate row appeared, and exactly one client confirmation email was sent.
  </how-to-verify>
  <resume-signal>Type "approved" once both the subscription and a no-duplicate reconciliation are confirmed, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live infrastructure | Human-applied DB migration + Stripe Dashboard config; both are privileged operational actions outside the request path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-10 | Denial of Service (silent non-reconciliation) | Stripe webhook subscription | high | mitigate | Blocking human-verify checkpoint confirms `checkout.session.completed` is subscribed before the phase is considered done (RESEARCH Pitfall 4). |
| T-64-11 | Tampering | live migration apply | medium | mitigate | Additive, idempotent `ADD COLUMN IF NOT EXISTS`; applied via the audited Supabase MCP; verified by column probe. |
| T-64-SC | Tampering | npm/pip/cargo installs | low | accept | No package install in this phase. |
</threat_model>

<verification>
- Supabase MCP confirms `bookings.payment_link_url` / `payment_link_id` live.
- Operator confirms the Stripe webhook subscription + a live E2E reconciliation.
- `npx vitest run` full suite green (phase gate).
</verification>

<success_criteria>
- Migration 056 is live; both columns present and nullable.
- The live Stripe endpoint forwards checkout.session.completed.
- A real pay-link payment reconciles in place with no duplicate.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 04 subset)
- Live database: `bookings.payment_link_url`, `bookings.payment_link_id` columns.
- Live Stripe webhook endpoint: `checkout.session.completed` subscription (operational, not a repo file).
</artifacts_produced>

<output>
Create `.planning/phases/64-admin-created-bookings-with-payment-link/64-04-SUMMARY.md` when done.
</output>
