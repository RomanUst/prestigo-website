---
phase: 64-admin-created-bookings-with-payment-link
plan: 02
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - app/api/admin/bookings/[id]/payment-link/route.ts
  - app/api/webhooks/stripe/route.ts
  - tests/admin-bookings-payment-link.test.ts
  - tests/webhooks-stripe-checkout-session.test.ts
autonomous: true
requirements: [ANEW-02, ANEW-03, ANEW-04]
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # ── D-05 attach-later + resend ──
    - "An operator can attach a payment link LATER to an existing unpaid/pending booking via POST /api/admin/bookings/[id]/payment-link — same link + email + reconciliation machinery as create-time (D-05)."
    - "Attaching a link to a currently 'pending' cash booking sets its status directly to 'unpaid' inside the dedicated route (bypassing VALID_TRANSITIONS, which has no pending->unpaid edge — Pitfall 2), then persists the link and emails it (ANEW-05 ordering edge)."
    - "The [id]/payment-link route rejects a booking that is already confirmed/cancelled (or any status outside unpaid|pending) — a paid booking never gets a fresh payable link (guard)."
    - "The route generates a link only for a booking with no existing payment_link_url; a booking that already has one is never given a second, different URL (D-04 one-link-per-booking / ANEW-02 regenerate edge)."
    - "The explicit resend action uses its own code path that calls sendPaymentRequestEmail directly, bypassing the logEmail dedup window — an operator clicking resend within 10 minutes is never silently swallowed (D-07 / Pitfall 5)."
    # ── Round-trip (attach-later only) ──
    - "When the target booking is a round-trip leg (leg outbound|return with a sibling unpaid row sharing the same stale payment_intent_id), the generated link carries linkedBookingId in metadata so one checkout.session.completed reconciles BOTH legs (Pitfall 3)."
    - "checkout.session.completed with linkedBookingId present reconciles the sibling leg to confirmed with the same payment_intent_id, and fires the confirmation side-effect suite exactly once for the pair (no double email, no duplicate)."
    # ── Edge probes (resolved: explicit) ──
    - "If link generation for the [id]/payment-link route fails at the Stripe call, the booking status is not left half-changed in a way that loses the row — the route returns an error and the booking remains queryable for retry (ANEW-02 edge: link-fails)."
    # ── Edge probes (resolved: backstop) ──
    - statement: "Round-trip sibling detection keys on shared payment_intent_id (the real leg link), never on linked_booking_id which no insert path populates in this codebase."
      verification: backstop
    # ── UI Considerations lift (E4 round-trip email zero-one-many) ──
    - "A round-trip payment link produces ONE payment-request email whose EUR figure is the combined amount and whose body carries the 'covers both legs' framing — never a many-item list (E4 zero-one-many)."
  prohibitions:
    - "A resend must never be blocked by the initial-send logEmail dedup window — resend is an explicit operator re-send by design (D-07)."
    - "The attach-later route must never mutate a booking that is already confirmed or cancelled."
    - "A second, different payment-link URL is never minted for a booking that already has one."
    # Canon: IDOR / auth on the new route dropped to ASVS canon — see <threat_model> T-64-04.
  artifacts:
    - app/api/admin/bookings/[id]/payment-link/route.ts
    - app/api/webhooks/stripe/route.ts (round-trip linkedBookingId reconciliation)
  key_links:
    - "[id]/payment-link route -> sibling-leg lookup by shared payment_intent_id -> createBookingPaymentLink(linkedBookingId) -> persist + status='unpaid' + email"
    - "handlePaymentLinkSucceeded -> reconcile primary + linkedBookingId sibling with one payment_intent_id -> side-effects once"
---

<objective>
Expand the proven tracer to the two paths it deliberately left out: (1) D-05 "attach a payment link later" to an already-saved unpaid/pending booking via a dedicated `[id]/payment-link` route with its own resend code path (D-07), and (2) round-trip reconciliation — a link generated for one leg of a Phase-62-captured round-trip pair reconciles BOTH legs on a single `checkout.session.completed` (Pitfall 3).

Purpose: Cover ANEW-02/ANEW-03 for existing bookings (cash-booking rescue) and ANEW-04 for round-trip, reusing every primitive built in Plan 01.
Output: `app/api/admin/bookings/[id]/payment-link/route.ts` (+ resend), and the round-trip branch of `handlePaymentLinkSucceeded`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-admin-created-bookings-with-payment-link/64-CONTEXT.md
@.planning/phases/64-admin-created-bookings-with-payment-link/64-RESEARCH.md
@.planning/phases/64-admin-created-bookings-with-payment-link/64-PATTERNS.md
@.planning/phases/64-admin-created-bookings-with-payment-link/64-01-PLAN.md
</context>

<planner_decision>
Open Question 1 (RESEARCH) — attach-later on a `pending` booking: RESOLVED per RESEARCH recommendation (a). The dedicated `[id]/payment-link` route sets `status='unpaid'` directly (like `[id]/assign/route.ts` sets driver fields directly), bypassing the generic `VALID_TRANSITIONS`-gated PATCH branch. No edit to either `VALID_TRANSITIONS` map is required for the API to function; the Phase-62 recovery queue (filters `status='unpaid'`) surfaces these late-attached-link bookings with zero queue-definition changes.
Planner assumption (RESEARCH A1 — currency): Payment Link amount is EUR (`amount_eur`), matching create-payment-intent's EUR-first convention. Flagged for user confirmation; single-line change to CZK if the business prefers.
</planner_decision>

<tasks>

<task type="auto">
  <name>Task 1: D-05 attach-later route + resend + round-trip sibling metadata</name>
  <files>app/api/admin/bookings/[id]/payment-link/route.ts, tests/admin-bookings-payment-link.test.ts</files>
  <read_first>
    - app/api/admin/bookings/[id]/assign/route.ts (FULL — enforceMaxBody → getAdminUser guard → await params → zod parse → service client; sets status/fields directly without VALID_TRANSITIONS; logEmail + `after()` email dispatch)
    - lib/stripe-payment-links.ts (createBookingPaymentLink signature — created in Plan 01)
    - lib/email.ts sendPaymentRequestEmail + lib/email-log.ts logEmail (created/known in Plan 01)
    - app/api/webhooks/stripe/route.ts (round-trip metadata expectations set by handlePaymentLinkSucceeded)
    - 64-RESEARCH.md Pitfall 2 (pending→unpaid), Pitfall 3 (round-trip sibling via shared payment_intent_id; linked_booking_id is never written — do not rely on it), Pitfall 5 (resend bypasses dedup)
    - 64-PATTERNS.md `[id]/payment-link/route.ts` scaffold + Admin auth guard shared pattern
    - 64-UI-SPEC.md ## Copywriting Contract (round-trip linked-leg notice, resend states)
    - tests/admin-bookings.test.ts (vi.hoisted harness to mirror in the new test file)
  </read_first>
  <action>
    Create `app/api/admin/bookings/[id]/payment-link/route.ts` mirroring `[id]/assign/route.ts` structure exactly: `enforceMaxBody(request, 5_000)`; `getAdminUser()` 401/403 guard; `await params` for `{ id }`; zod body schema `z.object({ resend: z.boolean().optional() })`.

    Fetch the booking (`id, booking_reference, status, amount_eur, leg, payment_intent_id, payment_link_url, payment_link_id, client_email, client_first_name, ...trip fields for the email`).
    - 404 if not found.
    - RESEND path (`resend === true`): require an existing `payment_link_url`; call `sendPaymentRequestEmail(...)` DIRECTLY (own code path — do NOT call logEmail; D-07/Pitfall 5). Return `{ resent: true }`.
    - GENERATE path (default): 422/409 guard — reject unless `status` is `'unpaid'` or `'pending'`; reject if `payment_link_url` already set ("link already exists" — D-04). Round-trip sibling detection: if `leg` is `'outbound'|'return'` and `payment_intent_id` is non-null, query for a sibling row `.eq('payment_intent_id', payment_intent_id).neq('leg', leg).eq('status','unpaid')`; if found, capture its id as `linkedBookingId`. Call `createBookingPaymentLink({ bookingId: id, bookingReference, amountEur: booking.amount_eur, leg: booking.leg ?? null, linkedBookingId })`. Persist: `UPDATE bookings SET payment_link_url, payment_link_id, status='unpaid' WHERE id=id` (set status directly — bypass VALID_TRANSITIONS). Then `logEmail({ bookingId:id, emailType:'payment_request', recipient: booking.client_email })`; if allowed, `after(() => sendPaymentRequestEmail(...))`. Return `{ paymentLinkUrl, linkedBookingId: linkedBookingId ?? null }`. On a Stripe failure, return 502/500 with an error message and do not persist a half-state (leave status/link untouched).

    In `tests/admin-bookings-payment-link.test.ts` (new, mirror tests/admin-bookings.test.ts harness): cover — generate for an unpaid booking (link persisted, status stays unpaid, email logged+sent); generate for a pending booking (status flips to unpaid); reject when status='confirmed' (409/422); reject when payment_link_url already exists; round-trip sibling found → linkedBookingId in createBookingPaymentLink args; resend path calls sendPaymentRequestEmail without logEmail; 401/403 guard. Also add the ANEW-05 invariant test from Plan 01's assumption_delta note: a no-link POST yields one row with payment_intent_id NULL and payment_link_url NULL.
  </action>
  <verify>
    <automated>npx vitest run tests/admin-bookings-payment-link.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `POST /api/admin/bookings/[id]/payment-link` returns 401/403 without an admin session (guard test passes).
    - Generating for `pending` → the persisted row has `status='unpaid'` (test asserts the UPDATE payload includes `status:'unpaid'`).
    - Generating for `status='confirmed'` returns 4xx and makes NO Stripe call (spy call count 0).
    - Generating when `payment_link_url` is already set returns 4xx and makes NO Stripe call.
    - Round-trip: a sibling unpaid leg sharing `payment_intent_id` results in `createBookingPaymentLink` being called with `linkedBookingId` set to the sibling's id.
    - Resend path: `sendPaymentRequestEmail` is called and `logEmail` is NOT called (spy assertions).
    - The route sets `status` directly and does not import/consult `VALID_TRANSITIONS`.
  </acceptance_criteria>
  <done>Attach-later route works for unpaid and pending bookings, guards confirmed/cancelled and existing-link cases, carries round-trip linkedBookingId, and resend bypasses dedup. New test file green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Round-trip reconciliation in the checkout.session.completed branch</name>
  <files>app/api/webhooks/stripe/route.ts, tests/webhooks-stripe-checkout-session.test.ts</files>
  <behavior>
    - handlePaymentLinkSucceeded(bookingId, linkedBookingId, paymentIntentId): reconcile the primary bookingId; if linkedBookingId is non-null, ALSO reconcileBookingByIdToConfirmed(linkedBookingId, paymentIntentId). Side-effects (client confirmation + manager alert + GA4 + reminder) fire once for the reconciled set. If neither row was newly reconciled (both already confirmed), no side-effects.
    - A duplicate delivery (stripe_processed_events already has event.id) short-circuits before any reconcile.
  </behavior>
  <read_first>
    - app/api/webhooks/stripe/route.ts (the checkout.session.completed branch + handlePaymentLinkSucceeded added in Plan 01; handleRoundTripSucceeded lines ~299-490 for the round-trip side-effect suite shape — sendRoundTripClientConfirmation / sendRoundTripManagerAlert if a round-trip-specific email exists, else reuse the one-way suite once)
    - lib/supabase.ts reconcileBookingByIdToConfirmed (Plan 01) and reconcileRoundTripToConfirmed (for the "both legs share one payment_intent_id" pattern)
    - tests/webhooks-stripe-checkout-session.test.ts (extend — the round-trip case scaffolded RED in Plan 01 Task 1)
  </read_first>
  <action>
    Extend `handlePaymentLinkSucceeded` (in app/api/webhooks/stripe/route.ts) so that when `linkedBookingId` is present it reconciles both the primary and the sibling row to `confirmed` with the same `paymentIntentId` (two `reconcileBookingByIdToConfirmed` calls, or a single `payment_intent_id`-keyed reconcile after the primary sets it — prefer two explicit by-id calls for clarity, each status-gated so a retry is a no-op). Determine "newly reconciled" from the union of both returned arrays; fire the confirmation side-effect suite ONCE (client confirmation + manager alert + GA4 purchase + QStash reminder per reconciled leg with a pickup_utc) sourced from the reconciled DB rows. Keep the outer stripe_processed_events read-check-first / claim-after ordering unchanged. Guard: if both reconcile calls return empty arrays (already confirmed), return without side-effects. Do not send two client emails for the pair — the round-trip produces one combined confirmation, matching how handleRoundTripSucceeded emits a single round-trip confirmation.
  </action>
  <verify>
    <automated>npx vitest run tests/webhooks-stripe-checkout-session.test.ts tests/webhooks-stripe.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A `checkout.session.completed` with `metadata.linkedBookingId` set reconciles BOTH the primary and sibling rows to `confirmed` with the shared `payment_intent_id` (test asserts two by-id reconcile calls with the same PI id).
    - The round-trip pair fires exactly one client confirmation (test asserts confirmation call count is 1, not 2).
    - A duplicate delivery of the same event fires zero additional reconcile calls (stripe_processed_events short-circuit test).
    - Already-confirmed pair (both reconcile calls return []) fires zero side-effects.
    - `npx vitest run tests/webhooks-stripe.test.ts` still green (payment_intent.succeeded path unchanged).
  </acceptance_criteria>
  <reversibility rating="costly">Round-trip reconciliation shares one payment_intent_id across two rows — the same core no-duplicate contract as D-08, extended to the pair.</reversibility>
  <done>Round-trip link payments reconcile both legs once, idempotently, with a single confirmation; one-way path and payment_intent.succeeded path unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → [id]/payment-link route | Operator triggers link generation/resend for an existing booking id. |
| Stripe → webhook | Untrusted POST; signature-verified before the branch runs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-04 | Tampering/Information Disclosure (IDOR) | [id]/payment-link route | high | mitigate | `getAdminUser()` 401/403 guard (same as [id]/assign); no anonymous/cross-tenant access exists in the admin surface; status guard restricts action to unpaid|pending rows. |
| T-64-01 | Tampering | payment-link amount (attach-later) | high | mitigate | Amount is `booking.amount_eur` read from the persisted row (already server-authoritative) — never a request-body figure. |
| T-64-03 | Repudiation | round-trip double-reconcile | medium | mitigate | `stripe_processed_events` claim + status-gated by-id UPDATEs (each flips once). |
| T-64-07 | Tampering | link regeneration | medium | mitigate | Route refuses to generate when `payment_link_url` already set — no second URL minted. |
| T-64-SC | Tampering | npm/pip/cargo installs | low | accept | No new package installed; `stripe` already present/audited. |
</threat_model>

<verification>
- `npx vitest run tests/admin-bookings-payment-link.test.ts tests/webhooks-stripe-checkout-session.test.ts tests/webhooks-stripe.test.ts` green.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- Operator can attach + email a payment link to an existing unpaid/pending booking; pending flips to unpaid.
- Resend bypasses the dedup window.
- Round-trip link reconciles both legs once with a single confirmation.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02 subset)
- `app/api/admin/bookings/[id]/payment-link/route.ts` — POST (generate + resend), round-trip sibling detection.
- `app/api/webhooks/stripe/route.ts` — round-trip branch of `handlePaymentLinkSucceeded`.
- Test file: `tests/admin-bookings-payment-link.test.ts` (+ extended `tests/webhooks-stripe-checkout-session.test.ts`).
</artifacts_produced>

<output>
Create `.planning/phases/64-admin-created-bookings-with-payment-link/64-02-SUMMARY.md` when done.
</output>
