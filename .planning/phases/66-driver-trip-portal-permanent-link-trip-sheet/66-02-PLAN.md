---
phase: 66-driver-trip-portal-permanent-link-trip-sheet
plan: 02
type: execute
wave: 2
depends_on: [66-01]
files_modified:
  - lib/email.ts
  - app/api/admin/bookings/[id]/assign/route.ts
  - app/api/admin/bookings/[id]/assignment/route.ts
  - components/admin/DriverAssignmentSection.tsx
  - tests/admin-assignment.test.ts
  - tests/DriverAssignmentSection.test.tsx
autonomous: true
requirements: [DTRIP-01, DTRIP-07]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    # --- DTRIP-01 delivery: permanent link reaches the driver + the dispatcher ---
    - "The driver assignment email includes a permanent trip-sheet link built as {siteUrl}/driver/trip/{trip_token}, rendered as a 'VIEW TRIP SHEET' CTA alongside the existing ACCEPT TRIP / DECLINE TRIP buttons, using the neutral gold/border style (not the red decline style) (D-10a / D-10)"
    - "The trip link URL is HTML-escaped in the email exactly like acceptUrl/declineUrl (escapeHtml(data.tripUrl)) — same convention as every other interpolated value in the email builder"
    - "POST /api/admin/bookings/[id]/assign selects trip_token on the assignment insert and passes tripUrl into sendDriverAssignmentEmail; the raw trip_token is NOT returned in the POST JSON response body (delivered only via the email URL, mirroring the existing SEC-18 token omission)"
    - "GET /api/admin/bookings/[id]/assignment returns trip_token in the assignment object so the admin copy-link control can build the URL (this is a read-only GET — no CSRF-list entry needed)"
    # --- D-10b admin copy-link (UI-SPEC 'Copy Trip Link' rows) ---
    - "In DriverAssignmentSection's 'assigned' branch, a 'Copy Trip Link' control (Copy lucide icon, 44px min height, copper-accent border matching reassignButtonStyle weight) sits next to the driver name/status badge and copies {window.location.origin}/driver/trip/{assignment.trip_token} via navigator.clipboard (D-10b / D-10)"
    - "On a successful clipboard write the control label swaps to 'Copied!' for ~2s then reverts (UI-SPEC copy-control loading row, matching DriverResponseClient state-swap)"
    - "On a clipboard write failure the control falls back to a selectable read-only text field containing the full trip URL and the copy 'Couldn't copy — select and copy the link manually.' (UI-SPEC copy-control error row)"
    - "The 'Copy Trip Link' control never appears for terminal bookings — DriverAssignmentSection already returns null for completed/cancelled, so the affordance is only shown while a link would actually resolve"
    # --- DTRIP-07 coexistence (regression) ---
    - "The existing accept/decline flow is unchanged: /api/driver/respond, app/driver/response/page.tsx, and the DriverResponseClient still behave identically (existing tests green) — trip_token is purely additive and the single-use token/token_expires_at/token_used_at columns and their logic are untouched (DTRIP-07)"
    - "The assignment email still renders the ACCEPT TRIP and DECLINE TRIP buttons with their existing URLs and the '48 hours' validity note — adding VIEW TRIP SHEET does not remove or alter them (DTRIP-07)"
  artifacts:
    - "lib/email.ts — DriverAssignmentEmailData.tripUrl + VIEW TRIP SHEET CTA in buildDriverAssignmentHtml"
    - "app/api/admin/bookings/[id]/assign/route.ts — insert selects trip_token, builds tripUrl, threads it to the email"
    - "app/api/admin/bookings/[id]/assignment/route.ts — GET select adds trip_token"
    - "components/admin/DriverAssignmentSection.tsx — trip_token on Assignment + Copy Trip Link control"
    - "tests/admin-assignment.test.ts — assert tripUrl threaded + accept/decline regression"
    - "tests/DriverAssignmentSection.test.tsx — assert Copy Trip Link renders + clipboard call"
  key_links:
    - "assign/route.ts insert .select('...trip_token') → tripUrl → sendDriverAssignmentEmail(tripUrl) (email delivery)"
    - "assignment/route.ts GET .select('...trip_token') → DriverAssignmentSection Assignment.trip_token → Copy Trip Link (admin delivery)"
    - "trip link URL shape {origin}/driver/trip/{trip_token} MUST match the Plan 01 route path exactly"
  prohibitions:
    - "The permanent trip link must NOT replace, gate, or alter the accept/decline links — it is strictly additive; removing or changing the ACCEPT/DECLINE buttons or the /api/driver/respond logic is forbidden (DTRIP-07)"
    - "The raw trip_token must NOT be exposed to the browser through the assign POST response body — delivery is via the email URL and the assignment GET (read surface) only, preserving the existing SEC-18 discipline"
---

<objective>
Deliver the permanent trip link built in Plan 01 to the two humans who need it — the driver (via the
assignment email) and the dispatcher (via an admin copy-link control) — WITHOUT disturbing the existing
single-use accept/decline flow.

Purpose: DTRIP-01 delivery (D-10a email link + D-10b admin copy link) and DTRIP-07 (the permanent link
is additive; accept/decline still works unchanged, proven by an explicit regression guard).

Output: `tripUrl` in the assignment email, `trip_token` threaded through the assign insert and exposed
by the assignment GET, and a "Copy Trip Link" control in DriverAssignmentSection.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-driver-trip-portal-permanent-link-trip-sheet/66-CONTEXT.md
@.planning/phases/66-driver-trip-portal-permanent-link-trip-sheet/66-RESEARCH.md
@.planning/phases/66-driver-trip-portal-permanent-link-trip-sheet/66-PATTERNS.md
@.planning/phases/66-driver-trip-portal-permanent-link-trip-sheet/66-UI-SPEC.md
@.planning/phases/66-driver-trip-portal-permanent-link-trip-sheet/66-01-SUMMARY.md
</context>

<planner_assumptions>
- **Reuse, do not re-derive:** `isTripLinkValid`/`TERMINAL_STATUSES` (lib/trip-token) and the exported
  `formatVehicleLabel` (lib/email) already exist from Plan 01 — this plan consumes them, it does not
  redefine any of them. The trip URL path `/driver/trip/{trip_token}` MUST match Plan 01's route exactly.
- **lib/email.ts overlap with Plan 01 is intentional and sequential:** Plan 01 only added `export` to
  `formatVehicleLabel`; this plan (Wave 2, after Plan 01 commits) adds the `tripUrl` field + CTA — no
  concurrent edit, no conflict.
- **UI-SPEC dismissed rows:** the "empty / partial / overflow / long-text" states for the Copy Trip Link
  control were dismissed in 66-UI-SPEC as N/A (fixed-label secondary button, no data-driven content) —
  carried as-is, surfaced not dropped.
</planner_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the permanent trip link to the assignment email (DriverAssignmentEmailData.tripUrl + VIEW TRIP SHEET CTA)</name>
  <files>lib/email.ts</files>
  <read_first>
    - lib/email.ts (DriverAssignmentEmailData interface lines ~1403-1418; buildDriverAssignmentHtml CTA button block lines ~1488-1492; escapeHtml usage throughout the driver-assignment HTML)
    - .planning/phases/66-driver-trip-portal-permanent-link-trip-sheet/66-PATTERNS.md (lib/email.ts section — interface extension + CTA button pattern)
    - tests/admin-assignment.test.ts (how sendDriverAssignmentEmail is stubbed + asserted)
  </read_first>
  <behavior>
    - DriverAssignmentEmailData has a required `tripUrl: string` field
    - buildDriverAssignmentHtml renders a link whose href is escapeHtml(data.tripUrl) with the label VIEW TRIP SHEET
    - the existing ACCEPT TRIP and DECLINE TRIP anchors and the "48 hours" note remain present and unchanged
  </behavior>
  <action>
    In lib/email.ts add `tripUrl: string` to the `DriverAssignmentEmailData` interface (after
    declineUrl). In `buildDriverAssignmentHtml`, add a third CTA anchor in (or directly below) the
    existing accept/decline button block: `href="${escapeHtml(data.tripUrl)}"`, label "VIEW TRIP SHEET",
    styled with the neutral gold/border inline style (1px solid #BFA06A, color #BFA06A — same family as
    ACCEPT TRIP, NOT the #CC3333 decline style). Do not touch the accept/decline anchors, the booking
    reference box, the trip-details table, or the 48-hour note.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "tripUrl" lib/email.ts` shows the field on DriverAssignmentEmailData AND an escapeHtml(data.tripUrl) usage in the HTML builder
    - `grep -c "ACCEPT TRIP\|DECLINE TRIP" lib/email.ts` is unchanged from before this task (both buttons still present)
    - `grep -n "VIEW TRIP SHEET" lib/email.ts` matches exactly once
    - `npx tsc --noEmit` fails until every sendDriverAssignmentEmail call site provides tripUrl (proves the field is required) — resolved by Task 2
  </acceptance_criteria>
  <done>DriverAssignmentEmailData.tripUrl added; VIEW TRIP SHEET CTA renders alongside unchanged accept/decline buttons.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread trip_token through the assign insert + expose it on the assignment GET</name>
  <files>app/api/admin/bookings/[id]/assign/route.ts, app/api/admin/bookings/[id]/assignment/route.ts, tests/admin-assignment.test.ts</files>
  <read_first>
    - app/api/admin/bookings/[id]/assign/route.ts (insert .select at line 73; email URL construction lines 172-193; SEC-18 token-omission comment line 203)
    - app/api/admin/bookings/[id]/assignment/route.ts (GET select at line 22)
    - tests/admin-assignment.test.ts (assign + assignment describe blocks; makeFullAssignMock; mockAssignment shape; the accept/decline respond tests that MUST stay green)
    - .planning/phases/66-driver-trip-portal-permanent-link-trip-sheet/66-PATTERNS.md (assign/route.ts + assignment/route.ts sections)
  </read_first>
  <behavior>
    - assign POST builds tripUrl = `${siteUrl}/driver/trip/${assignment.trip_token}` and passes it to sendDriverAssignmentEmail
    - assign POST response JSON does NOT contain trip_token (only id/driver_id/status, as today)
    - assignment GET response includes trip_token in the assignment object
    - the existing /api/driver/respond accept + decline tests still pass (regression)
  </behavior>
  <action>
    In assign/route.ts: extend the insert select from `'id, driver_id, status, token'` to
    `'id, driver_id, status, token, trip_token'`. In the email block, add
    `const tripUrl = ${siteUrl}/driver/trip/${assignment.trip_token}` (template literal, matching the
    acceptUrl/declineUrl construction just above) and pass `tripUrl` into the sendDriverAssignmentEmail
    call. Do NOT add trip_token to the final `NextResponse.json` assignment object — keep the SEC-18
    omission (the token reaches the browser only through the email URL and the assignment GET).
    In assignment/route.ts: extend the GET select from
    `'id, driver_id, status, created_at, drivers(name, email)'` to
    `'id, driver_id, status, created_at, trip_token, drivers(name, email)'`. This is the ONLY change —
    it is a read-only GET, so no CSRF-list entry.
    In tests/admin-assignment.test.ts: add `trip_token` to the assign-route mockAssignment fixtures and
    add an assertion that sendDriverAssignmentEmail is called with an object whose tripUrl contains
    `/driver/trip/`. Do not modify the existing /api/driver/respond accept/decline tests.
  </action>
  <verify>
    <automated>npx vitest run tests/admin-assignment.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "trip_token" app/api/admin/bookings/\[id\]/assign/route.ts` shows it in the insert select
    - `grep -n "/driver/trip/" app/api/admin/bookings/\[id\]/assign/route.ts` shows the tripUrl construction
    - assign POST response assertion in tests confirms the JSON assignment object has NO trip_token property
    - `grep -n "trip_token" app/api/admin/bookings/\[id\]/assignment/route.ts` shows it in the GET select
    - tests/admin-assignment.test.ts green INCLUDING the unchanged DRIVER-04 /api/driver/respond accept + decline + invalid_token tests (DTRIP-07 regression)
  </acceptance_criteria>
  <done>tripUrl delivered via email from the assign route; assignment GET returns trip_token; accept/decline respond tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Admin "Copy Trip Link" control in DriverAssignmentSection (D-10b)</name>
  <files>components/admin/DriverAssignmentSection.tsx, tests/DriverAssignmentSection.test.tsx</files>
  <read_first>
    - components/admin/DriverAssignmentSection.tsx (Assignment interface lines 11-19; the mode==='assigned' branch lines 213-242; reassignButtonStyle + its hover handlers; the existing assignment fetch lines 43-58)
    - app/driver/response/DriverResponseClient.tsx (button state-swap pattern for the 'Copied!' feedback)
    - tests/DriverAssignmentSection.test.tsx (render + fetch-mock conventions; the assigned-mode test setup)
    - .planning/phases/66-driver-trip-portal-permanent-link-trip-sheet/66-UI-SPEC.md (Copywriting Contract 'Copy Trip Link' / 'Couldn't copy' rows; Appendix admin control spec)
  </read_first>
  <behavior>
    - In assigned mode the section renders a control labeled "Copy Trip Link" next to the driver name + status badge
    - Clicking it calls navigator.clipboard.writeText with a URL ending in /driver/trip/{trip_token}
    - After a successful copy the label reads "Copied!" (then reverts)
    - When clipboard.writeText rejects, a selectable read-only text field containing the trip URL appears with the "Couldn't copy" message
    - The control is absent for completed/cancelled bookings (component already returns null)
  </behavior>
  <action>
    In DriverAssignmentSection.tsx add `trip_token: string` to the `Assignment` interface (it flows in
    automatically from the assignment GET selected in Task 2 — no fetch change needed). In the
    `mode === 'assigned'` branch, add a "Copy Trip Link" button next to the existing Reassign button,
    reusing the reassignButtonStyle inline-style family + the same onMouseEnter/onMouseLeave hover
    handlers, plus a `Copy` icon from lucide-react (44px min height, copper-accent border). Add a
    `handleCopyTripLink` async handler that builds `${window.location.origin}/driver/trip/${assignment.trip_token}`
    and calls `navigator.clipboard.writeText(...)`; on success set a brief "copied" state flag that swaps
    the label to "Copied!" for ~2s (mirror the DriverResponseClient state-swap); on rejection set a
    "copy-failed" state that renders a selectable read-only text input holding the URL and the message
    "Couldn't copy — select and copy the link manually." Do not alter the loading / no-assignment /
    reassigning / submitting / error branches.
    In tests/DriverAssignmentSection.test.tsx add a case: assigned-mode render (assignment fetch returns
    an object including trip_token) shows a "Copy Trip Link" control, and clicking it invokes a mocked
    navigator.clipboard.writeText with a value matching /\/driver\/trip\//.
  </action>
  <verify>
    <automated>npx vitest run tests/DriverAssignmentSection.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "trip_token" components/admin/DriverAssignmentSection.tsx` shows it on the Assignment interface
    - `grep -n "Copy Trip Link\|handleCopyTripLink" components/admin/DriverAssignmentSection.tsx` matches
    - `grep -n "/driver/trip/" components/admin/DriverAssignmentSection.tsx` shows the URL is built from window.location.origin + assignment.trip_token
    - tests/DriverAssignmentSection.test.tsx green: assigned mode renders the Copy Trip Link control and a click calls navigator.clipboard.writeText with a /driver/trip/ URL
    - existing DriverAssignmentSection tests (completed/cancelled → null, onAssigned) still pass
  </acceptance_criteria>
  <done>Copy Trip Link control renders in the assigned branch, copies the /driver/trip/{trip_token} URL, shows Copied!/fallback states, and never appears for terminal bookings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → POST /assign, GET /assignment | Authenticated admin routes (getAdminUser guard, unchanged) |
| assignment email → driver's inbox | The trip link travels as a URL in email; the token is the credential |
| admin browser → navigator.clipboard | Client-side copy of the trip URL built from the GET-exposed trip_token |

## STRIDE Threat Register (ASVS L1, block on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-66-07 | Information Disclosure | assign POST response body | high | mitigate | trip_token is NOT added to the POST JSON response (SEC-18 discipline preserved) — it reaches the browser only via the email URL and the admin GET (read surface) |
| T-66-08 | Tampering / XSS | assignment email HTML | high | mitigate | data.tripUrl is wrapped in escapeHtml() exactly like every other interpolated value in buildDriverAssignmentHtml — no raw interpolation |
| T-66-09 | Elevation (regression) | accept/decline flow | high | mitigate | trip_token is purely additive; assign response shape, /api/driver/respond, and the single-use token columns are untouched — explicit DTRIP-07 regression tests stay green |
| T-66-10 | Tampering (CSRF) | assignment GET | low | accept | GET is read-only; middleware CSRF only guards mutation methods — no CSRF-list entry needed, and no new POST endpoint is introduced |
| T-66-SC | Tampering (supply chain) | package installs | low | accept | No new packages installed (lucide-react already present); no install vector |
</threat_model>

<verification>
- `npx vitest run tests/admin-assignment.test.ts tests/DriverAssignmentSection.test.tsx` green — including the unchanged /api/driver/respond accept/decline/invalid_token tests (DTRIP-07 regression).
- `npx tsc --noEmit` passes (tripUrl now provided at the sole call site).
- Full suite `npx vitest run` green before the phase gate.
- Email VIEW TRIP SHEET, admin Copy Trip Link, and Plan 01's /driver/trip/{token} all use the identical URL shape.
</verification>

<success_criteria>
- The driver receives a permanent trip-sheet link in the assignment email alongside unchanged accept/decline buttons (D-10a, DTRIP-07).
- The dispatcher can copy the same permanent link from the admin assigned-driver row (D-10b).
- trip_token reaches the browser only through the email URL and the assignment GET — never the assign POST body.
- The accept/decline flow is provably unchanged (regression tests green) (DTRIP-07).
</success_criteria>

<artifacts_this_phase_produces>
New/changed symbols introduced by THIS plan (exclude from drift verification — net-new here):
- `lib/email.ts`: new field `DriverAssignmentEmailData.tripUrl`; new VIEW TRIP SHEET CTA anchor.
- `app/api/admin/bookings/[id]/assign/route.ts`: insert select gains `trip_token`; new local `tripUrl`; `sendDriverAssignmentEmail` call gains `tripUrl`.
- `app/api/admin/bookings/[id]/assignment/route.ts`: GET select gains `trip_token`.
- `components/admin/DriverAssignmentSection.tsx`: `Assignment.trip_token`; new `handleCopyTripLink`; new "Copy Trip Link" control + Copied!/fallback states.
- Test additions in `tests/admin-assignment.test.ts` and `tests/DriverAssignmentSection.test.tsx`.
- Consumes (does not create): `isTripLinkValid`/`TERMINAL_STATUSES` (lib/trip-token), `formatVehicleLabel` (lib/email), and the `/driver/trip/[token]` route — all from Plan 01.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/66-driver-trip-portal-permanent-link-trip-sheet/66-02-SUMMARY.md` when done.
</output>
